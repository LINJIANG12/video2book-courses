好咧，那字符串拼接的這個練習，大家完成得怎麼樣了？多個字符串我們使用加號來進行拼接，大家敲代碼敲得爽不爽？哎，那大家會發現啊，其實我們如果說涉及到多個字符串以及多個變量進行拼接，要拼湊成一個完整的一個字符串，那使用加號來進行拼接還是比較麻煩的。那主要會存在這麼三個問題。第一個問題：拼接會比較繁瑣，因為只要涉及到兩個部分的拼接，那我們就需要使用加號來將其拼在一起。然後第二個問題呢，就是這種基於加號來進行拼接啊，它會破壞字符串的完整性。所謂破壞字符串的完整性指的就是，我們在拼接的時候，在代碼當中我們是看不到這個字符串的全貌的。你看：「大家好，我是今年歲愛好」，這是一句什麼話？我並不能清楚地看到這個字符串的一個完整的結構。這是第二個問題。那麼第三個問題呢，就是還要進行類型轉換。如果說是進行字符串與非字符串進行拼接，那我們這裡還要進行類型轉換，轉換了之後才可以進行拼接。因為我們提到，加號在進行字符串拼接的時候，它只能將字符串與字符串拼接起來，而不能將字符串與非字符串拼接起來。那這就是我們使用加號來拼接大量字符串所存在的三點問題。那出現了這個問題，有沒有對應的解決方案？有沒有更加優雅、效率更高的方式呢？哎，有。那這就是我們這小節所要講解的字符串的格式化。那通過字符串的格式化呢，就可以來解決掉上面的這幾個問題。那接下來呢，我們來看一下什麼是字符串的格式化。其實字符串的格式化呢，本質也是用來做字符串的拼接的。只不過呢，它不用再使用加號進行拼接了，而是要使用一個佔位符。通過百分號這個佔位符就可以快速地完成字符串與變量之間的拼接。來看一下它的語法。大家來看。那上面這一塊定義了這麼一個變量s1，這就是一個變量。然後在下面這一塊呢，我需要輸出這麼一句話：「大家好，我是誰誰誰，歡迎大家進入到Python課程的學習。」而大家會發現，這一塊呢我使用到了一個佔位符：%s。那這裡的這個百分號呢，其實代表的就是我要佔位了。而後面的這個s，注意啊，這個百分號和s要挨著寫。後面的這個s就表示，那在這個位置呢，我需要將最終的這個變量要轉為字符串類型，然後填充在這個位置。那這個s大家可以理解為就是string的簡寫。那%s就代表我要將字符串填充在這個位置當中。那將哪個字符串填充在這個位置啊？前面這一塊有一個佔位符，在我們的這個字符串的後面，在百分號之後我們要指定一個變量。那這個變量呢就是最終我要替代這個%s的這個變量。那最終這個s1這個變量，它就會轉換為字符串類型，然後填充在%s這個位置，會把%s替代掉。那這是只有一個變量的情況下。那如果有多個變量呢？大家來看。那如果說有多個變量，那前面在編寫的時候你就編寫多個%s就行了。你看，這就相當於是有多個佔位符，有兩個佔位符。那前面有兩個佔位符，在這個字符串的結尾百分號之後，就應該寫上兩個變量。那麼最終第一個變量它會替代掉第一個佔位符，第二個變量會替代掉第二個佔位符。而如果說百分號後面這塊有多個變量，那多個變量需要使用括號將其括起來，然後使用逗號分隔。那這裡呢大家要保持一個原則，就是前面的這個字符串當中有多少個佔位符，那麼在百分號之後就要傳遞多少個變量或者說多少個數據，前後的數量呢要保持一致。好的，那這是我們這一塊所講到的基於百分號佔位符的形式來進行字符串的格式化。那接下來呢，我們就回到我們的PyCharm當中，基於這個字符串格式化的語法來解決剛才這一塊字符串拼接的問題。那麼我們就將上面剛才寫的這個代碼呢，我給它全部註釋掉啊。然後我把這個代碼複製一份，然後註釋掉，然後到這兒來。那麼，這是我們字符串格式化的第一種方式，我們使用%s這種佔位符的形式來進行格式化。那我們來看一下，那這一塊該怎麼改造呢？首先大家來看，我是誰誰誰，那這一塊是不是要佔位啊？最終要使用誰來佔位？name。那這一塊我就先寫百分號什麼？s。直接把加號刪了，直接寫%s。然後這個位置我要填充的是不是就是這個年齡age啊？還是一樣，這一塊直接寫%s。那緊接著再往後走，我學習的專業是什麼？那這一塊把這個引號和中間的這兩個加號都刪掉，然後刪掉之後這一塊來，%s。那愛好是什麼？%s。後面這一塊的加號去掉。好，那麼大家現在來看，這個字符串當中，有幾個%s這個佔位符啊？是不是有四個？那如果有四個%s佔位符，也就意味著我們在字符串的結尾這一塊加上百分號，百分號後面需要跟上幾個變量？幾項數據？四項。那麼如果是多項數據，我們在這一塊需要使用括號將其括起來。當然對於這個PyCharm來說，它已經提示出來了啊。那我們來看，後面的這幾個變量的書寫順序，來我問大家，有沒有要求？哎注意了，這個變量的順序是有要求的。第一個變量會替代第一個佔位符，第二個變量會替代掉第二個佔位符，所以大家就要去看一下，那麼第一個佔位符最終這一塊要拼接的內容是什麼呀？是不是name啊？哎所以呢name在這一塊是第一個變量，然後緊接著第二個就是age，第三個是pro，第四個是hobby。好，我這一塊按一下Tab鍵啊，讓開發工具自動補全。好，這樣呢就可以了。那最終在運行的時候，它就會將name這個變量的值填充在第一個佔位符這個位置。age這個變量的值就會填充在第二個佔位符這個位置。那pro就會填充在第三個位置，而這個hobby就會填充在第四個位置。好的，那麼接下來呢，我們就直接右鍵，然後運行。運行之後大家來看，這一塊是不是也完成了字符串的拼接了？哎那這種寫法呢，相對於剛才我們所講解的這個基於加號的方式來進行拼接，那麼就會簡潔很多。而且呢，這個字符串也是一個完整的一個結構。最終我們通過後面的變量來替代掉對應的佔位符就可以了。而且呢還不用去做類型轉換，因為大家會發現，這個age最終它是不是也可以成功地給我們替代掉這個%s啊？因為呢%s我們剛才提到，它會將我們後面的數據給我們轉換成什麼呀？字符串，是自動地轉為字符串，然後替代掉前面的這個佔位符。好的，那這是我們這一塊所講到的這個字符串格式化的第一種方式啊。我這個後面寫一下，這是方式一。那麼其實字符串格式化呢，還有第二種方式。那我們回到PPT來看一下，那第二種方式是怎麼進行格式化呢？那第二種格式化方式呢，其實就是在我們的字符串前面，在這一塊加上一個什麼呢？加上一個f，來標識一下我要對這個字符串，後面的這個字符串要進行格式化了。那麼怎麼進行格式化呢？那我們在這一塊呢就可以使用這麼一個佔位符啊，就是大括號。哎直接寫大括號，然後裡面，大括號裡面寫上我們具體的變量名或者是表達式就可以了。你看，那這種方式呢是直接將我們對應的變量直接給我們拼接在這個位置，直接把我們的變量名寫在大括號裡面就可以了。比如大家來看。你看這，我定義了一個變量name為濤哥，然後後面這一塊輸出啊，「大家好，我是誰誰誰」。那此時你會發現，這一塊我們並沒有使用一個固定的%s來佔位了，而是使用了大括號裡面寫上name。那這個name是和我們上面這一塊的這個變量名是一致的。那最終在輸出的時候，它會自動地將name這個變量的值給我們拼接在這個位置。所以這個大括號呢其實也是一個佔位符。只不過呢我們在後面就不用再指定那第一個佔位符我們要使用哪個變量來替代。那這一塊我們已經直接把變量名寫在括號裡面了。而這種寫法呢大家記得，在這個字符串的前面，我們需要加上一個標識，就是f。那麼緊接著再來看，那如果是多個變量呢？那多個變量我們在這個裡面用多個大括號就可以了。比如這個位置我要拼接的是第一個變量s1，那這個位置我要拼接的是第二個變量s2。而需要記得啊，這種方式需要在字符串的前面加上一個f這麼一個標識，標識我要對這個字符串進行格式化處理了。而這種方式呢，也是現在企業開發的推薦方式。好，那接下來呢我們再來演示一下這種格式化的方式。來，打開PyCharm。那麼打開PyCharm之後，來，還是相同的案例啊，我在這一塊複製一份下來，把上面這一塊的代碼註釋掉。那方式二呢在進行格式化的時候呢，那我們在這一塊呢就需要字符串的這個前面啊，我在這一塊寫一下，在字符串的前面在這一塊加上一個標識f，然後在字符串中間呢在這一塊使用大括號來進行佔位，裡面這一塊寫上具體的變量名或者這個裡面其實也可以寫什麼呀？寫表達式也是可以的。好的，那接下來呢我們就來改造一下這一塊的代碼。那麼這個時候呢我們在這一塊啊就不用再去寫這個%s了，來看一下怎麼改造。%s全部刪掉。那我們要想使用這種格式化方式啊，需要在字符串的前面加上一個標識f。直接寫上一個f。然後緊接著我是誰誰誰，這個位置我們是不是需要使用這個name這個變量來替代啊？哎所以在這塊直接寫大括號裡面寫上誰啊？name，這就可以了。然後緊接著這個位置呢，大括號裡面寫上age。專業這個位置，大括號裡面寫上pro。那最後愛好呢，大括號裡面寫上hobby。這樣呢就OK了。來，大家可以上下兩種方式再做一個對比，那大家覺得這兩種方式哪種方式會更簡潔？是不是方式二這種格式化方式會更加簡潔一些啊？哎而這種方式大家記得啊，字符串的前面這個f可不能少。如果前面沒有這個f，你最終會發現它會將這個佔位符大括號name原樣輸出出來，它會認為這就是字符串的內容，而不是一個佔位符。所以記得前面f不能少。現在大家來看，加上f之後，最終name這個變量是不是就替代這個位置了？然後age，18，沒問題；pro，軟件工程；那hobby，Python、Java，沒毛病。好，那這樣呢我們就基於字符串的格式化方式啊完成了這個字符串的拼接。那麼最後我在這一塊標註一下，那這個呢是我們企業開發當中的，哎，推薦方式。好的，那這是我們這小節所講解的字符串的格式化。那以後呢我們這種方式會用得更多一些。好，那麼最後呢我們就回到PPT來簡單做一個小結，主要明確這麼三個問題。第一個問題：字符串在拼接的時候我們可以使用什麼符號啊？是不是加號？而加號在進行字符串拼接的時候能不能將字符串與非字符串拼接起來？哎是不能的，那我們需要通過類型轉換，轉化為字符串，然後再進行拼接。那這裡呢我們需要用到這麼一個工具啊，其實就是一個函數，就是這個str來進行轉換。好，那麼緊接著第二個問題：%這種方式進行字符串格式化它的一個語法。那百分號後面我們需要寫一個s，那%s其實就是一個佔位符。最終呢我們需要通過字符串後面的百分號之後的這個變量來將這個%s替代掉，它會將變量轉為字符串，然後呢替代掉這個佔位符。而這裡呢大家需要注意的是，前面這裡有多少個佔位符，百分號後面括號裡面就應該有多少個變量。好，那這是字符串格式化的第一種方式。而第二種格式化的方式呢，就是在我們字符串前面加上一個f，然後緊接著在字符串裡面我們就可以通過大括號的形式來進行佔位。而在大括號當中呢，我們可以寫上具體的變量名，也可以寫表達式，那最終就會將我們指定的變量的值替代掉這個佔位符。而記得前面需要加上f，標識我要對這個字符串進行格式化了。好的，那這是我們這小節所講解的字符串的格式化。黑馬程序員，為千萬學生少走彎路，我們一直在努力。
