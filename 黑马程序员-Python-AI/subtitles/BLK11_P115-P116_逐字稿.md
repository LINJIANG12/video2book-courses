好的，同学们，那上一小节呢，我们已经搞定了会话管理当中啊新建会话以及保存会话这一块的功能实现。那接下来呢，我们要完成的就是下面这一部分的功能实现。那么这一块的功能呢，主要包括啊加载历史会话列表，啊就是我需要将所有的历史会话信息啊在这一块加载并展示出来。那这一块具体有多少个历史会话，它并不是固定的。我们得去看一下我们到底保存了多少个历史会话的文件。有多少个文件，那么这一块这个列表当中是不是就有多少个历史会话呀？哎，就有多少个按钮，啊其实这就是一个个的按钮。我们点击之后是不是就要加载出对应的历史会话？ 哎，所以第二功能呢就是我们点击指定的历史会话的这个按钮之后，那需要将这个历史会话对应的数据呢加载并展示在这个消息列表当中。 哎，那是第二块功能。 那么第三块功能呢，呃就是后面的这一块的这个删除按钮。那我点击这个后面的删除按钮，那就代表我要将前面这个会话给它删除掉。那我点击第三个删除按钮，那就代表我要将第三个会话给它删除掉。 所以呢，关于下面的这一部分呢，我们主要包括这么三项功能。 那么第一项功能呢就是展示历史会话列表。那第二项功能呢就是当我点击指定的这个历史会话之后啊，那我们需要加载出这个历史会话，并将其展示出来。 那么第三个功能呢就是点击后面的这个删除按钮的时候，那我需要删除掉指定的历史会话。 那么接下来这小节呢，我们就先来完成第一个功能，展示历史会话列表。那我们刚才提到，具体有多少个历史会话，这个历史会话列表当中到底有多少个条目，它不是固定的，是取决于什么呢？ 哎，取决于我们在 sessions 这个目录下到底有多少个历史会话的文件。有多少个文件，那么在历史会话列表当中我是不是就要展示多少个条目呀？ 而在展示这个历史会话名字的时候呢，呃这个里面待会看到后面的这个 .json 呢我就不需要了，啊我只需要展示前面这个会话的名字，也就是这个时间就可以了。 那么接下来呢，我们就先来完成第一部分，展示历史会话列表。啊下一小节呢，我们再来搞定后面这两部分内容。 好，那么接下来呢，我们就直接打开 PyCharm。那首先我是不是需要去加载所有的会话历史啊？哎，要将这个 sessions 这个目录下所有的文件要加载出来，看下具体有多少个文件。 呃那这里呢关于加载 sessions 目录下的文件列表，呃那这里呢我就直接定义一个单独的函数啊，那我就不在这个主干逻辑当中操作了，我直接在上面在这一块再定义一个函数。 呃那这里呢我们在下面这一块啊再定义一个函数，那这个函数呢是用来加载所有的会话列表信息。 好，那么在这一块呢，我们来定义这么一个函数。定义函数呢就用 def，加载所有的会话，那就叫 load_sessions，啊加载所有的会话。 那最终啊这个会话列表呢，我们可以将其保存到一个列表当中啊。那这一块呢我就来定义这么一个列表就叫 session_list，啊。那么这一块先给它一个空列表。那这个 session_list 这个列表当中最终到底有多少个元素，是不是取决于 sessions 这个目录下到底有多少个文件啊？ 哎，所以接下来呢，我们在这一块写下注释啊，那这里呢我们就需要来加载 sessions 目录下的所有会话信息啊的文件就可以了。 好，那这里呢我们在这一块可以去做一个条件判断， 啊做一个条件判断。判断一下啊， os.path.exists 先去判断一下这个 sessions 目录存不存在。 哎，如果说它存在，那我是不是再去加载这个目录下的内容？如果说连这个目录都不存在，那我就没必要去加载这个目录下的内容了。 呃所以这个呢就是一个健壮性的判断。那接下来我是不是就想知道这个 sessions 目录下到底有哪些个文件啊？ 呃那这里呢其实我们可以去调用 os 这个模块当中有这么一个方法叫做 listdir，哎就是罗列出指定目录下的文件。好，那我们在这一块就来指定 sessions。那我们鼠标往这个 os 这个模块的这个函数上面一放，大家来看，它的返回值，你看这，这不就是函数的返回值啊？是不是就是一个 list？哎那我这一块呢就叫做 file_list。 那接下来我们是不是就可以遍历这个 file_list 啊？而其实这个 file_list 里面存的是什么呢？ 存的就是这些文件的文件名，所以这个 file_list 当中的每一个元素啊，大家再来看，其实每一个元素就是一个字符串，而这个字符串就是这个文件的名字。 啊所以接下来呢，我就在这一块直接进行一个 for 循环啊，for。 好，那么此时大家来看，AI 在这一块已经给了我们提示啊，那如果说这一块的逻辑我不想让 AI 提示，我就想自己敲一下怎么做呢？ 呃那么这个时候大家可以把自动触发提示这个开关关了就行了。 啊找到这个，在右下角这一块是不是有个通义灵码？点一下，这一块有一个叫做云端模型自动触发，把这个开关关掉就行了。关掉之后大家再来看，现在你在这块去敲代码，你会发现没有自动的提示了。那如果说此时我还想让通义灵码给我提示，你就得手动触发了，按快捷键哈，Alt + P。 好，此时大家会发现这一块是不是又给我提示了？ 哎这啊，你看这，Alt + P 这个键。好，那这里呢我们就手动来敲一下哈，来熟悉一下这个 API。 大家来看， 这个 file_list 里面存的就是一个一个的文件名，所以这一块我就叫 for file_name in file_list，遍历这个 file_list。 然后我们要获取的是不是就是其中一个一个的文件啊？ 但是我们要获取的是以 .json 为后缀的文件，啊就是需要 .json 的这个文件，其他文件我不需要。 所以呢在这一块我们可以做一个判断。如果说这个 file_name. 是以 .json 结尾的话，哎那我就需要这样的文件，所以这一块判断一下。如果说 file_name.endswith，后面这一块来一个 .json。 哎这就代表如果这个文件名是以 .json 为后缀的话，那就说明这是我们历史会话信息的文件。然后接下来呢我是不是就需要获取出这个文件的前面这一部分，后面的 .json 我是不是就不需要了？ 哎所以这里呢我们在这一块可以来做一个操作哈，就是 file_name，我怎么去截取前面这一部分字符串出来呢？ 呃大家现在就要回想一下前面在讲解基础知识的时候，我们所讲到的字符串的切片、列表的切片等知识了，还记得吗？ 哎那切片的语法是这样子的，后面这一块写个中括号，然后紧接着问你，你要从哪切到哪以及步长是多少。 啊那这里呢我们就要从最开始的位置是不是一直切到哪个位置啊？ 切到这个位置啊。你可以从前面去数可以，或者我们也是不是也可以从后面往前数？那最后一个是 -1，这是 -2，这是 -3，这是 -4，这是 -5，是不是切到 -5 这个位置啊？ 哎，所以这一块怎么写呢？我们可以直接冒号 -5，这一块冒号 1 就可以了。 当然如果步长是 1 你可以将其省略，因为默认就是 1。 好，那么接下来呢你看我们截取出来之后，我们再将这个文件名，也就是我们的会话的名字啊我存到 session_list 当中是不是就可以了？ 哎这块直接 append 将其添加到 session_list 这个列表当中就可以了。好，这样呢就可以了。那最后啊那我们在这一块再来一个 return，将 session_list 直接返回就可以。注意哦，这个 session_list 大家不要写到 if 里面去了，要写到外面。 啊因为无论这个 sessions 这个目录是否存在，那这个函数调用完了，它都是要有返回值的。如果说这个目录不存在，那这个条件不成立，不成立的话我是不是就返回一个空列表就行了？好，那这就是加载会话列表的这个逻辑，啊就是去加载 sessions 目录下的文件，然后拿到所有的文件列表之后，接下来呢去判断这个文件名是不是 .json 后缀。如果是 .json 后缀，那就说明是我们的历史会话的文件，然后呢我们去截取前面的这部分字符串就可以了，将其存入这个 session_list 会话列表当中。好，那这个函数呢我就定义好了。 好，那么定义好了之后，接下来呢我们就要来完成界面的布局了。 来，我们回到 PPT 啊，大家来看。 那我们就需要在我们的新建会话下面这一块是不是增加会话历史的这个列表啊？ 来，我们看一下我们怎么布局这个页面啊。 先找到对应的位置啊，来往下走往下走，往下走，找到侧边栏。你看侧边栏首先这一块是 AI 控制面板，然后接下来是不是新建会话的这个按钮啊？然后往下走这一块是不是有个会话历史？这么一个文本，然后下面这一块就是按钮。那这个会话历史呢其实就是一个普通文本，它并不是和上面的 AI 控制面板一样是一个标题，它就是一个普通文本。 好，我们在这一块写下啊。 呃这是会话历史。 好，这里呢我在这一块将这个自动触发再打开啊。那我这一块告诉大家有这么一个开关之后啊，大家下来在操作的时候你可以自己去控制啊。好，那么接下来呢这个会话历史就是一个普通文本，那这里呢我们可以直接调用 st. 那不用去调用 title，也不用去调用 header、subheader，就是一个普通文本的话直接调用 text。 啊这就是输出一个普通文本。好嘞，那这样呢这个会话历史这一块就展示出来了，当然你叫历史会话、会话历史都可以。然后呢我们打开浏览器先看一下啊。 大家来看，会话历史这个文本是不是就展示出来了？而在改会话历史的下面，我们这块是不是要展示对应的一些按钮？钮啊。你看这其实是按钮啊，无论是这个后面删除的这个叉叉，还是前面的这个都是一个按钮，是可以点击的。而具体有多少个按钮，是不是取决于有多少个历史会话呀？而历史会话的信息，我们刚才是不是已经封装到这个函数当中了？要获取历史会话，我们直接去调用这个函数就可以。好，所以，大家来看。那在下面这个位置呢，我可以来调用一下这个函数 load_sessions，来获取到会话列表哈，我们这块就叫 session_list。好。那接下来呢，我们是不是就可以遍历这个 session_list 了？那么，我们这块直接来遍历这个 session_list。走。好，那里面这个代码呢，我们现在先不用哈，先将其删掉。那遍历完 session_list 之后，我们是不是就可以拿到每一个 session，也就是每一个会话的名字了？然后接下来，我们是不是有多少个会话我就展示多少个按钮？当然，大家可能会想到，那这里呢，我们展示按钮嘛，这个我们刚才都已经在讲到过了，我直接来 st.button。然后前面这块我问大家，那这个会话的名字这块怎么写？就是 session 啊。你看，这个 session 就是这个会话的名字。然后这是这个宽度，然后再往后走，这是这个 icon。呃那 icon 我们换一个哈，来我再次打开我的表情库哈，来找一下我们这块的这个文件，那就这个吧。好，复制一下，复制之后，我直接将这个 icon 给它删掉，粘进来。好，这样就可以了。那大家别忘了，除了这个前面的会话的这个按钮以外，后面是不是还有一个叉叉还有一个删除的按钮啊？那大家可能会想到这个删除的按钮呢，那你不在再去定义一个 button 不就可以了嘛？那这个按钮呢，呃并没有名字，没有名字的话你这块给它一个空字符串就行。那后面这块我们再给它一个叉叉这么一个 icon 是不是就行了？来，我这块再次打开我的表情库哈，找一下。好，那大家来看，这块是一个叉叉，哎叉号。我们点击一下复制，复制之后再将这一块替换掉。好，那么现在我们先不考虑啊其中的逻辑是什么，我先看一下这两个按钮能不能展示出来。来，我打开浏览器，这块刷新一下。注意哦，这一块报错大家先不用管，先关注哪儿呢？你先关注这个位置。大家来看，按钮展示出来没有？是不是展示出来了？你看，这是我们的那个会话的名字，这是后面的删除按钮。但是大家会发现，他们俩好像显示在两行当中了，不是放在一行当中的。那这个时候怎么做呢？那如果说在 PyCharm 当中，这个 width，啊就是这个宽度假设哈，我不控制呢，它能不能放到一行当中呢？来，我再次打开浏览器，我这块再刷新一下，刷新之后我问大家，它有放到一行当中吗？没有。即使后面这块有空间，它也是独占一行的。每一个 button，每一个组件都是独占一行的。好，那么接下来呢，我们就要来说一下，那怎么样才能在一行当中展示两个组件呢？那这块呢，我们就需要用到 streamlit 当中的布局组件了。来，打开 streamlit 的官方文档。那么打开官方文档之后啊，那如果说我们要布局页面，那么此时找到这项：Layouts and Containers，那就是布局和容器。这个就是控制页面布局的。而在控制页面布局的这个方法当中有这么一个方法，叫做 st.columns。哎就是，将我们一行分为若干列。那你比如，这块是不是就是将一行分为两列呀？哎一个呢是会话的名字，一个是删除的这个按钮。是不是拆分成两列？那么这个里面呢其实你想拆分为多少列都可以，通过 st.columns 来实现就可以了。而在在这个 st.columns 当中呢，呃有这么一个必备的参数，啊叫 spec，啊就是规格。而这个规格呢你看这，可以是一个 int 类型的数据，或者是一个可以迭代的数据，啊比如像一个列表。你看这也有样例哈你看，中括号 0.7 0.3 啥意思呢？呃就是第一个组件要占用的宽度是 0.7，第二个组件要占用的宽度是 0.3。或者呢你也可以设置为 1 2 3，那就相当于，是整块一行分为六等份。那第一个组件占一等份，第二个组件占两等份，第三个组件占三等份。那如果说你在这块呢你传递了个 int 类型的数据，比如我就传递个 int 类型的 2，啥意思？就代表这一行我会平均分为两等份。啊如果说不想平均分，那你就手动的去指定具体的宽度占用的宽度。好，那么其实这个呢，呃在下面这块也有样例哈，大家来看。这不提供了样例啦？你看这啊，样例是 st.column 这块写个 3，代表这一行会分为三等份。那么每一等份呢其实就是一个组件。你看，这是分成的第一个组件，这是第二个组件，这是第三个组件。然后接下来呢，在这块通过 with 上下文管理器来指定，那第一个组件里面你要存放什么东西啊？存个标题，然后再定一个图片。然后第二个组件里面要放哪些组件呀？哎定一个标题，再放一个图片。啊是这种形式。好，那接下来呢我就截个图哈，切这儿。切在这儿之后，接下来我们就看一下怎么操作。这一块代码我先将其注释掉哈。那注释掉之后大家来看，那这个里面怎么操作呀？我们需要将这一行分为几列？是不是两列？这两列是平均分吗？不是。如果不是的话那就不要使用这种方式了。啊那我们这块就来，st.columns，然后括号里面这块需要指定，啊你要分为几列，以及它们占用的宽度。比如我这块写个 4 逗号 1，啥意思？哎这就代表我要将这一行哈，就这一行的这个空间分为两列。那第一列占用的是四等份空间，第二列占用一等份空间，就是 4:1 的宽度分配。啊当然这个大家可以自己去尝试啊，看一下 4:1 合适还是 3:2 合适，对吧，自己调。好，那么既然分为了两等份，那前面这一块呢我们就要通过两个变量来接收一下。啊其实就是这个解包的操作哈。那这块第一个叫 column1，第二个呢就叫 column2。好，然后接下来下面这一块的写法呢，就照猫画虎了哈，我就参照官方文档来。呃那么 column1 里面要放什么东西呢？column1 里面我在这块要定义这么一个按钮，那这个按钮呢名字就是这个会话的名字 session，icon 就是这么一个 icon。然后后面这一块啊，那这个我们目前先不用，先将其删掉。然后前面呢，我在这块再加上一个 if。如果说这个按钮点击了，是不是就说明我们是要去加载指定的会话的？哎当然这个逻辑啊我们先不实现，那我这块呃给它一个 pass 先。呃那么这个呢，就是来加载会话信息哈。点击这个按钮就会加载会话信息。然后紧接着呢，我们再来指定呃第二列要展示的内容。来，继续 with，这块来一个 column2。你看啊，我在这块写的时候，我都是参照官方文档写。那第二列我要定义的，是不是还是一个按钮？哎然后这个按钮呢 icon 设置为叉叉。啊那这块呢它是用来删除指定会话的，删除会话信息的。那下面这个注释呢我就将其删掉。好，我们现在先来看一下页面的结构。然后对于这里加载指定的会话信息以及删除指定的会话信息这块的功能呢，我们等会儿再来实现。啊我们先看一下页面的布局搞定了没有。来，我们回到浏览器啊，这一块来刷一下。刷新之后大家来看，那这个布局我们是不是就搞定了？哎左侧这块是会话的名字，右侧这块是一个删除的按钮。然后我们再回到这个 PyCharm 当中啊，呃那其实我们可以为这个按钮呢再先加上一个宽度的控制哈，将这个 width 设置为 stretch。啊那这样呢就代表呃那么它会占用这个父容器的宽度，简单说就是会铺满整个这个区域，而不是受里面的内容的空间控制。啊内容有多长就展示多长。好，我们这块设置为 stretch。那设置为 stretch 呢就代表它会占满整个父元素的宽度，父容器的宽度，简单说就是铺满整个区域，而不是受这个里面的这个按钮的内容的影响。啊内容长了就占用的长，内容短了就占用的短，不是这样子的。好，那么加上之后，我们再回到浏览器啊，这块再刷新一下。那刷新之后呢大家会发现没什么变化哈，因为这个里面的这个文本的内容本来就比较长，啊把整个按钮已经撑起来了。那这样呢布局就搞定了。但是大家会发现这里怎么没有展示出那个叉叉呢？而且页面怎么还报错呢？我们整个页面的这个结构不都已经定义好了吗？我们空结构是不是也指定了？怎么页面还报错呢？那么这个时候呢，呃大家就要去读一下这个里面的报错信息了。啊那这个里面的报错信息啊告诉我们 streamlit 重复的元素 ID。噢那么对于大家来说呢，大家可能并不清楚那这块具体是什么原因报错了。那么此时怎么做呢？那这个时候啊我们其实可以通过 AI 助手来帮我们解决这块的问题。好，那么此时呢，我将这块的报错信息啊我在这块截个图。啊截个图之后，然后打开我们的 PyCharm。打开 PyCharm 之后，找到右侧这里的通义灵码。然后呢，Ctrl+V 将图片粘进来。那么粘进来之后，然后接下来呢我在这块再点击加号，添加一个上下文。哎就是告诉 AI 啊，那么你要去找这个错误，我会告诉他你去哪个文件里面找这个错误。要不然目标太大，错误呢定位的可能并不精准。所以在这块我可以在搜索栏当中啊来搜索一下06。 哎这块我是不是要定位到这个文件，哎告诉AI啊你要结合着这个上下文的信息来排查这个错误。 页面啊渲染展示的时候报错了， 帮我排查一下这个错误。我们来看一下AI能不能把这个问题帮我们解决掉。好，那如果说AI搞不定，那还得我们来出手。好，来，我在这块直接回车啊。 那这次呢我在这块选择的是智能默认智能问答的方式啊，那它会告诉我具体是哪块有问题。那如果说大家选择的是智能体，它会直接去修改你的文件。 啊那这里呢我们只是让它给我去告诉我哪块具体有问题就可以了，告诉我怎么改就可以了，我不想让它修改我的文件，所以我就默认用的是这个智能问答。好，那么这个错误告诉AI之后啊AI进行了一系列的分 析，我们来看一下它是怎么分析的哈。 大家来看 这个错误呢是Streamlit当中的典型错误，原因是因为多个st.button元素使用了相同的参数，呃尤其是空标签，导致了Streamlit无法为它生成唯一的ID。然后紧接着下面这块是不是又分析了错误产生的原因啊，你看，这是我们刚才的代码。 你看这块是st.button，这块指定了session，这块是st.button，这块是不是就是一个固定的空串啊。 你看 第一个按钮st.button指定的这个按钮的名字是session，那每个按钮的标签不同，那这块告诉我们session这个名称就是按钮的名称，所以这是没问题的。然后第二个按钮st.button这一块给订了一个空字符串，那所有的删除按钮 它都使用的是空字符串作为这个组件的标识的，所以Streamlit会认为它们是相同的元素，从而就报错了。 那这个时候我又不想给这个按钮指定一个名字，我就想显示一个叉叉，怎么做呢？ 那下面这块也给了我们解决方案，大家来看。解决方案是什么样子的？ 哎你可以为每个删除按钮传入一个唯一的key，比如使用session这个名称作为key。你看这块指定这个key啊，这个key其实就是为这个组件设置一个唯一的标识。你如果没有设定key，它默认使用的是这个按钮的名字，就是这个label作为这个组件的唯一标识。 那我们刚才没有设定这个key，那这块你看都是空串是不是就重复了？因为有多个删除按钮是不是就重复了，就报错了。 如果你设定了key，哎那你只需要保证这个key每一个按钮是不重复的就可以了。 你看它这块给我们设置了一个叫做delete session，那这个里面指定的session是不是就绘画的名字啊？因为他前面加了一个f，是不是一个格式化的一个字符串？ 好，这样呢就解决了这个问题了。然后下面这一块还告诉我们那完整的代码怎么写。啊那这里呢我们就将这个key直接拷贝过来哈。 来，我这一块直接去Ctrl C复制一下，复制之后我直接拿过来哈，试一下行还是不行哈。 放在这里面。好，我们这块直接把这个多余的逗号删掉。那其实上面这个呢我们也可以照猫画虎也给它一个key哈。 你比如我在这块也给它设这个key，作为这个组件的唯一标识。就是每一个组件都得有一个唯一标识，如果重复了就报错了。而刚才上面这个组件没报错，主要是因为你看它默认是使用这个组件的名字，就这个按钮的名字作为那个唯一标识的，那每一个session是不一样的，所以没报错。但下面这一块都是一样的，是不是就报错了？ 好，那我这块就换一下吧，这块换成load。那此时大家来看， 我们在整个for循环的过程当中，每一个按钮它的key是不是都是不一样的？因为session不一样嘛，那session不一样key就不一样，那这个时候每一个组件都有自己的唯一标识，这个标识就不会重复。 好，那接下来呢我们打开浏览器，这一块我再刷新一下。刷新之后我问大家，现在还有问题没有？ 没有了。 我们这里的会话历史列表是不是正常的加载出来了？并且刚才的bug呢我们也解决掉了。 那么再次强调一下，刚才问题产生的原因啊就是因为在Streamlit当中啊那所有的组件呢都得有一个唯一标识。 啊当然如果你没有指定那个key的话，默认啊那么在这一块它是根据这个组件你所指定的这个名字，比如这个按钮的名字来作为唯一标识的。而我们刚才这个会话历史列表的这些组件是在for循环当中动态生成的，有可能展示的是两个，有可能是十个。那对于前面这个按钮来说，这个按钮的名字是不是就是这个会话的名字啊？那会话的名字并没有重复，所以是没问题的。但是后面的这个删除的按钮呢？ 哎for循环的过程当中，我们给它设定的这个按钮的名字是一个空字符串，无论你生成两个还是生成十个都是空字符串，那这样的话这两个删除按钮这两个组件名字是不是就重复了？那标识呢也就重复了。 而既然是唯一标识，唯一那就是不允许多重复的，如果重复就报错了。 如果说我们想为这个组件去设置一个唯一标识，啊那么此时呢可以设置一下这个属性，就是key，啊你只需要保证每个组件的key不重复就可以了。 而我们刚才在解决这个问题的时候，大家会发现我们有没有去查官方文档？ 没有，是让AI来帮我们去 分析的，AI来帮我们解决这个问题的。 那这也是现在在AI时代大家解决问题的一个思路，特别是屏幕前的小伙伴，大家在自学的过程当中一旦出现问题， 那就可以直接把问题出现的bug直接截图喂给AI，让AI帮我们来分析那这个问题产生的原因是什么，以及这个问题该怎么去解决。 那通过AI生成这一块的这个代码，通过这块的分析以及对应的解决方案，第一个我们能够把bug解决掉，第二个我们是不是也能够学到新的东西啊？ 哎这就是在AI时代大家在解决问题以及在学习的时候的一种 方式。 而最后强调一点啊，AI生成的内容可不一定正确。如果说AI没有将我们这个bug解决掉，那这个时候呢那对于一个新的技术的话大家就需要去查阅官方文档，然后再来解决这个问题了。 好的，那这是我们这小节所讲解的会话历史加载这一块的功能。而在完成这一块页面布局的时候啊，我们使用到了st.columns这个方法，啊那这个方法呢可以将一行分为若干列，啊然后呢再进行按列进行 布局。 好的，那这是我们这小节所讲解的历史会话列表这一块的加载展示。那么下一小节呢我们再来完成其中这块的逻辑啊，就是加载指定会话信息这一块的功能，然后完成之后我们再来完成删除会话信息的功能开发。 好勒，那么接下来呢大家就可以暂停视频，完成这一块的功能实现。 黑马程序员为千万学生少走弯路，我们一直在努力。

那在上一小节呢我们已经完成了历史会话列表的展示，那接下来这小节呢我们再来完成第二块功能，就是我们点击具体的会话的这个按钮之后啊，它要加载指定 的历史会话。 那其实加载指定的历史会话呢就是将我们的这个会话文件当中的消息数据直接加载出来，并且展示在 消息列表当中，是不是就可以了？ 好嘞，那么接下来呢我们就来完成这一步操作，加载历史会话。来，我们直接打开PyCharm。 那么加载指定历史会话呢其实是不是就是完成这一块的这个逻辑啊？ 哎那这里呢我们刚才只是定义了一个空的结构，那接下来我们将pass删掉啊，在这个里面就来完成加载指定会话的信息。 呃那这里的这个逻辑呢我也可以单独的再定义一个函数啊，封装到一个函数当中，那这样呢会使我们的主干的结构会更加清晰一些。 啊让我们这一块的这个逻辑呢主要聚焦于页面布局，所涉及到的一些逻辑操作呢直接去调用对应的函数就可以了。 啊所以在这块呢我再去调用这么一个函数哈，当然这个函数呢就叫load_ session。那load_sessions呢是加载所有的会话信息，用来展示这个会话列表的。那load_session就是加载指定的会话。 那这个里面我们加载哪个会话呢？ 你需要将这个会话的名字传递给我。 那这样的话大家想 那我点击的具体是哪个按钮，那我传递的是不是就是这个按钮对应的这个会话的名字啊？ 哎然后我再根据这个名字去加载对应的会话就可以了。 而这一块之所以报错是因为这个函数是不是还不存在啊？ 哎那在上面呢我们就来定义这个函数。 来，我们往上走啊，这个函数的定义你会发现我是不是统一都放在上面这一块了？ 来，在这块再定义这么一个函数哈，是加载指定会话信息哈，这块写一下，是加载指定的会话数据。 好，那我们这块来定义这么一个函数哈，就叫做load_ session。啊那这里面需要接收这么一个参数啊，就是session_name，就是会话的名字。然后接下来呢在这个里面我们就可以来加载指定的会话数据了。 啊当然这里呢呃我们还是来做一步健壮性的判断啊，类似是这样，做一步健壮性的判断。 如果说os.path.exists来判断一下这个文件存在不存在。 哎那这一块的写法应该是 判断一下sessions，这是目录，我是吃现在需要判断的是目录下的指定的文件存在不存在？ 那么这一块来一个/，/后面这一块来一对大括号，里面写上session_name。好，前面加上一个 f。而这个文件呢，它是有后缀的，是不是 .json 啊？所以后面这一块加上一个 .json。好，这就阔以了。那这就代表，我要判断啊在 sessions 这个目录下有没有这么一个 json 文件？那如果有的话，就说明这个会话数据是不是存在的？诶如果存在，那我就再去读取这个 json 文件当中的数据。那么读取数据这个，我们前面都讲过哈，这一块写一下。厄就是来读取这个会话数据。厄那么这一块直接来哈。厄 AI 已经提示了，我们看一下。with open 打开这个文件。打开哪个文件？诶就是打开 sessions 目录下的这个会话文件，没问题。厄是读取，所以这一块模式为 r，UTF-8 的编码，没问题。而我们现在要读取的这个文件，大家会发现是不是就是一个 json 格式的文件？诶那这里呢，在进行读取加载的时候，我们这一块直接调用 json.load 就阔以了。然后呢，再将我们这一块的这个 f 传递进来。啊将这个文件传递进来。那传递进来之后，我们在这一块是不是就可以给我们转换成一个 Python 当中的对象啊？那这个对象的名字呢，我们就叫 session_data。那最终它就会将这个 json 文件当中的数据给我们读取出来，并且封装到这个 Python 对象当中。那这个对象当中所保存的数据是不是就是这个文件里面的数据啊？里面呢一共这么几项 key，分别是 nickname、nature、current_session 以及 message。那大家再想，那我拿到这些信息之后，我需要做什么呢？我们所有与会话相关的数据，我们是不是都保存在这个 session_state 当中了？你看我们在页面当中啊，我们在去展示聊天信息的时候，我们是不是也是去遍历的 session_state 当中的 message？哎所以呢，我们读取出来之后，直接为 session_state 的 message 赋值就行了。那么其它的这些属性：nickname、nature、current_session 是不是一致的？哎是一致的。好，我们往上走哈。那么这一块来看一下怎么做哈。那么这一块直接来！st.session_state.messages，你看，这个就是我们的会话消息信息。然后呢，我们为它赋一个值。值是什么呢？值就是我们所读取会话文件当中的这个 messages。那这一块直接来，session_data 方括号 messages，没问题。好，就根据这个 key 来取值就可以了。那下面的我们就照猫画虎啊，快速操作一下。nickname 的、nature 的，还有一个是 current_session 的。那么 current_session 的这个值呢，厄当然你也可以从我们的这个文件当中读取出来可以，或者呢，那就是这个 session_name，你直接赋值为 session_name 也是可以的。好，那这样呢，就可以了。而这里呢，我们在去加载这个指定的会话信息的时候啊，厄那如果成功了，哎那我们就为这个 session_data 当中的这些个键来赋值就行了。那如果失败了呢？啊如果失败了，我们可以给它一个提示信息啊。所以这里呢，厄我可以进行一个异常处理。那么在这一块我们直接来一个 try。我们刚才写的代码我就放在 try 这个代码块当中的。然后紧接着下面这一块来捕获一下所有的异常啊，except Exception as e。然后紧接着如果出现异常的话，我需要在页面当中给它一个提示，提示一个错误的消息。那这个时候啊，我们其实可以使用 Streamlit 当中给我们提供的一个状态消息的组件啊，就是这个 Status elements。那么如果说是成功的消息，那你调用 success 方法，哎来输出就可以了。那如果是失败呢，那就是这个 st.error，就是错误消息。那如果你仅仅想输出异常的内容，那你可以调用 st.exception。啊那这里面要传递的呢就是一个异常。但是如果说你直接输出这么一个异常的话，对于用户的话，他并不知道这个异常是什么意思。啊所以这里呢，我们选择这个 error，啊就输出一条错误信息就可以了。哎比如你看，st.error 里面告诉他具体是什么问题。好，那这个时候我们再回来，回来之后啊，我们这一块直接来。直接来 st.error。好，这一块我们就直接提示一个错误信息哈。厄加载会话失败哈，加载会话失败。好，这就阔以了。啊当然呢，如果说你不需要用到这个异常的信息，那后面的 as e 呢，你是可以直接将其删掉的，这也是可以的。好嘞，那这样呢，加载指定会话信息这一块的这个代码我们就搞定了。啊那么加载指定会话信息呢，其实核心就是根据我们这个会话的名字，去读取对应的文件，将文件当中的数据读取出来，然后赋值给 session_state 当中的 message、nickname、nature 和 current_session 就可以了。好，那么接下来呢，我们就来做一个测试。那么在测试的时候呢，其实也很简单，我们只需要去点击一下会话展示的这个 button 这个按钮，是不是就会调用 load_session 这个方法？哎其实呢，一旦加载完了之后，那么我们已经为这个 session_state 当中的 message 啊、nickname、nature、current_session 已经赋上值了之后啊，那记得让我们的页面再重新运行一次。那运行之后，大家来看，那下面这一块是不是就可以遍历展示我们赋值之后的这个 messages，来展示聊天的消息了？然后呢，在我们的这个左侧侧边栏的组件当中啊，那么在渲染的时候呢，那对应的昵称，你看，那它对应的值，你看是不是也就展示出来了？你看这个 value 还有下面这一块的这个 nature，那展示的都是 session_state 当中的值。我们一旦为它重新赋了值之后，让它重新运行一把，就会展示出当前这个会话对应的 nickname 昵称以及 nature 性格。好，所以这里呢，大家还记得要做一件事啊，要做什么事呢？啊就是调用 st.rerun，重新运行一次。那么接下来呢，我们就可以来做一个测试，来看一下点击这个按钮之后，能不能将会话数据加载出来，然后重新运行之后，能不能渲染展示出来？好，那么接下来呢，我们打开浏览器。在这一块来刷新一下哈。刷新之后，大家看好了，我点击一下。我问大家，加载出来没啊？加载出来了。你好，啊今天怎么样？对吧，还行吧。是不是加载出来了？然后我点击第二个，走！你看，第二个里面应该是没有任何的聊天信息的哈，我们直接打开看一下，看一下这个文件吧，你看，里面是空的。所以呢，什么信息都没展示出来。那么接下来呢，我可以改一下这一块的这个昵称啊，把这个昵称改为小美。然后呢，厄活泼开朗的厄随便换一个吧，啊长沙姑娘。好，那接下来呢，我在这块发一个消息哈，你好。这一块再来，啊还行啊。啊可以。好的，那么此时大家来看，那现在我这一块是不是进行了一轮交互了？交互完了之后，如果说我想新建会话，我就可以点击这儿，来新建一个会话。好，那么此时大家会发现，是不是又是一个全新的会话？好，那在这一块呢，我们再来一个哈，1+1 等于几哈？厄为了以示区分啊，我们在这块来个 1+1 等于几。好，那么这一块回答出来了，当然是 2 没问题哈。那接下来大家再来看哈，我点击第一个。啊加载出第一个对应的会话数据。我点击第二个，就是 5048 这个，我点击一下，加载出第二个，是不是没问题啊？我点击第三个。诶？大家会发现，当我点击第三个的时候，它怎么没有将之前的数据保存下来呢？我再给大家演示一下，你看，1+1 等于几？走！我再与它交互一次，是 2 没问题对吧？好，现在大家看好了，我现在对应的这个会话呢，其实是 2315 这个。然后紧接着大家再来看好，现在我交互完了之后，我点击第一个，我想看一下之前的会话内容，我点击。之前的能加载出来，前面两个都可以，然后我再点第三个，你会发现，诶？这一次还是没保存成功。那么此时我们打开 PyCharm，你找到最后一个文件，你会发现 messages 这个列表当中的消息是空的。好，那么接下来呢，我们再回到浏览器，我们就要思考一下为什么？大家可以想想。我们之前在开发会话管理这一块功能的时候，这个消息列表，就是这个会话信息是什么时候保存的？是不是我们点击新建会话的时候才会保存？对吧？那我们刚才在这块输入 1+1 等于几的时候，输入完毕之后交互完了，我有没有点击新建会话？没有，我是不是直接去看之前的历史信息了？直接看之前的历史会话了。那并没有点击新建会话这个按钮，所以当前这次会话的消息保存下来了没有？没有。这就是产生这个问题的原因。那此时大家再思考一下，那这个会话的数据到底什么时候需要将其保存下来呢？哎，就是 AI 大模型给我们响应完数据之后，我们就需要将这个消息保存下来，对吧？哎所以这个时候呢，我们再次回到 PyCharm 当中啊，来解决掉这个 bug。大家来看。那下面这一块呢，就是与 AI 大模型进行交互，这是厄往我们的 session_state 的 messages 当中来保存大模型的这个返回的结果。那完事之后，我们需不需要保存当前会话，把当前会话的数据我直接保存到文件当中呢？哎需要，所以这里呢，最后一步啊，我在这块写一下，就是保存会话信息。那保存会话信息呢，我们这里直接去调用一下这个 save_session 之前我们定义的这个函数，是不是就可以了？哎所以在里呢，最后这一块哈，就是AI大模型给我们响应完了之后，记得也需要去调用save_session这个函数来保存会话信息。好，这一步做了之后就可以了。那这样呢，其实我们刚才这个问题就解决掉了。好，我们在这块先把界面刷新一下哈，刷新之后你看加载第一个会话没问题，然后加载第二个没问题，然后点击加载第三个。啊那第三个会话我这一块的这个昵称我变一下哈，变成一个小西，啊那么这一块来一个性格火辣的四川啊姑娘。好，那么接下来呢，我们在这一块来一个，啊你好呀，走。好，完事之后，我问大家，现在我什么都不点击，我就问大家，这一次会话里面的这些消息保存下来没有？哎保存下来了，你只要AI大模型响应完数据之后，我们是不是就调用了save_session？那么这一次会话的数据就会保存下来。来，我现在双击打开，大家来看保存下来没有？保存下来了。好，那此时再回来，回来之后我这一块再来啊，那1+1等于几呀？走。好，大家来看，哎这一块是不是也响应回来了？然后接下来大家看好了，我点击第一个加载第一个会话，走。你看，会话列表展示出来了，注意哦，昵称展示出来没有？性格展示出来没有？是不是也展示出来了？哎那么这一块的这个昵称，这个性格以及这个会话列表其实都是这个会话当中所记录的数据。你再看我点击第二个，第二个你看消息列表展示出来了，你看，昵称小美，长沙姑娘，是不是也展示出来了？哎然后紧接着我再点击第三个，你看，这一块的列表展示出来了，小西啊四川姑娘是不是也展示出来了？哎那这样的话大家会发现，我们加载并展示指定会话信息这一块的功能呢，我们就已经实现了。好，但是呢，其实对这个功能来说，呃其实还有一点需要完善一下，哪一块呢？我问大家，现在展示的是哪一个历史会话呀？啊大家并不知道。对吧？大家会发现我们点过之后好像这一块并没有任何的标记。我们不妨来看一下DeepSeek是怎么做的哈。来，打开我的DeepSeek，打开DeepSeek之后，来，我找到之前的这个会话，我点击一下。我现在问大家，你知不知道当前这个交互的这个信息是哪个会话当中的信息？那一眼就知道了，是不是这个呀？因为这个你看这一块它是不是有颜色的高亮啊？你比如我点击这个，你看这个信息我一眼就知道，哎是这个会话当中的信息，是有高亮的。哎所以这里呢，我们也可以给它增加一个高亮。那怎么去控制这个按钮的颜色呢？呃那这个时候我们再次回到Streamlit当中啊来找一下按钮。找到这一块的button。呃那么其实在button当中啊大家会看到这一块有这么一个属性哈，叫做type。那type呢其实控制的就是这个按钮的颜色。那它里面的选项呢可以取以下的三只，分别是primary、secondary以及tertiary。啊这么三个值，啊你看，三个值。那默认是谁呢？默认是secondary。那这里呢我们可以将这个参数值啊我直接截个图贴在这个位置，然后接下来呢我们来看一下设置为这三个值的效果哈。来，我们直接打开我的PyCharm。打开PyCharm之后，来，我找到上面这一块的这个按钮哈，就是这个会话的这个按钮。然后呢我在这块给它加上一个参数type。那type的值呢？那么在这块呢呃可以取primary，看一下设置为primary什么效果哈。来，我在这块打开我的浏览器，这块刷新一下。哎大家会发现，设置为primary是不是就是红色的效果？默认是secondary，默认呢就是这个里面没有任何颜色标识。那还有一个呢就是这个tertiary，这个我们不需要知道，我们就像让它展示为红色就可以了。但是现在的问题是所有的按钮是不是都红了？那我先做的是只让当前会话的这个按钮红，就是我选中的这个当前会话的按钮变成红色的背景。那其他的呢你保持原来的颜色就可以，保持这个secondary这个type类型对应的颜色就可以。来看下怎么做哈。呃那这里呢其实我们就可以去做一个判断。怎么判断呢？如果说这个session和我们当前会话的名字是一致的，那我就设置为primary。否则的话，那我就设置为secondary是不是就可以了？哎那这里呢其实我们可以加上这么一个条件判断哈。那这一块给大家介绍一种新的语法，那这个语法呢叫做三元运算符。三元哈运算符。那么先说一下三元运算符它的作用是什么？就是进行条件判断的。如果条件为真，则返回第一个表达式的值；否则返回第二个表达式的值。那么在这块再来说一下它的语法是什么样子的哈。那么语法，哎AI已经提示出来了。啊前面这块就写表达式1，啊就是第一个值。或者你在这块就写什么呢？呃值1，值1之后写if，if之后写条件判断，条件是什么；else之后写值2。好，那么这块说一下这个三元运算符它的作用，它是怎么判断的哈。那么在执行的时候它会先去判断这个条件。如果这个条件成立，那我这块就取第一个值值1，如果这个条件不成立，那我就取else之后的这个值2。啊所以这里呢我们可以这么做，你看，这就是我们要取的值1。什么时候我才取这个值1啊？哎就是当前我遍历出来的这个session和我加载出来的这个current_session，session_state当中的当前会话current_session如果是一致的，那我就取这个primary，type就设置为primary就行了，否则是不是设置为secondary？哎所以在这块哈你看这个语法，后面接下来去写一个if，if之后我们在这块就要来做比较了。如果说这个session，你看，这个session就是我们遍历出来的这个session，如果这个session等于等于st.session_state.current_session，这个session_state当中的current_session代表的是我们页面当中当前会话的这个session标识会话标识。如果当前这个会话与我遍历出来的session是一致的，那就说明我们当前打开的是不是就这个会话？然后呢在后面我们这块再加上一个else，如果不是的话呢，我在这块就来显示这个secondary哈。别写错，s-e-c-o-n-d-a-r-y就可以了。那这样呢就OK了。好，我再梳理一下这块的逻辑哈。就是代码在执行的时候呢，它会去判断这块的这个条件表达式。如果条件表达式的值为True，好，那么此时type的取值就是primary；如果条件表达式的值为False，那么它就会取else之后的这个secondary。那如果说type为primary，这个按钮是不是就会展示为红色背景色？否则呢，就没有红色背景色。好，那这一块代码编写完毕之后，来，测试一把。打开我们的浏览器啊在这块刷新一下。刷新之后大家看好了，我点击第一个。我问大家，红了没有？红了。我点击第二个，你会发现第二个是不是红了？点击第三个，第三个是不是红了？好，那现在呢我能够知道哈我现在打开的到底是哪一个会话当中的消息。当然呢，我们也可以在我们的这个核心页面当中啊在这个位置，啊就是在这个消息的上方我把当前会话名字我是不是可以展示出来啊？呃那这里呢我们就可以打开PyCharm啊。看一下展示在哪呢？呃大家要去挑这个消息列表展示的地方，大家来看，这块展示的是不是就是消息列表啊？你看它去遍历这个session_state当中的message，然后呢呃将消息遍历出来，然后一条一条的展示出来。那么在消息列表的上方这个位置我来展示一下就可以了。啊那这里呢我们就可以st.text，然后去展示st.session_state.current_session。呃当然呢我们在这块需要指定一下哈，指定一下这块展示的是什么信息啊？这是我们的会话的名称。好，那这个里面呢我们就呃前面加上一个f哈，啊进行一个格式化的输出。好，这样呢就可以了。来，我们试一下行还是不行哈，刷新一下。哎刷新之后大家来看，这一块是不是就展示出会话名称了？来，我点击一下，你看，现在哎我们点击哪个按钮，那对应的这个会话会话名称是不是就展示在上面这个位置了？好嘞，那这是我们这一小节所完成的加载指定会话信息这一块的功能实现。那么在完成这一块功能的时候呢，那涉及到了一个新的知识点。好，就是我们刚才所提到的三元运算符的写法。来，再熟悉一下这个三元运算符的语法哈。那三元运算符呢其实也是用来做条件判断的，那它的写法是这样子的：首先是值1，就是条件如果为True取的值；然后if之后写条件表达式；else之后写如果条件表达式为值为False的话取的值。那最终在进行条件判断的时候，它就会去判断这个条件表达式的值，如果条件为True，取if之前的值；如果条件表达式为False，取else之后的值。好，这就是三元运算符的写法。好的，那这是我们这小节所完成的加载指定会话信息这一块的功能实现。那么下一小节呢，我们再来完成最后一个功能，删除会话的功能。黑马程序员，为千万学生少走弯路，我们一直在努力。