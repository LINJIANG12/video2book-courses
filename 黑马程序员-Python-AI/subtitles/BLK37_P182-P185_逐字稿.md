行，那么完成了前面的这四个功能之后，接下来这一小节呢，我们再来完成最后一个功能，就是删除会话的功能。那删除会话的功能呢，也是我们这些所有功能当中最为简单的一个功能。那我们一起来看。

那删除会话呢指的就是啊，在我们绘画列表加载出来之后，大家会看到在我们每个绘画的右侧这一块是不是都有这么一个叉叉呀？哎有个删除的符号。那我们点击后面的这个叉叉，就可以将当前这个会话删掉。

那删除会话是不是就是将这个会话对应的文件给它删掉，就完事了？

哎那么接下来呢，我们就来看一下，那么在点击这个叉叉，在删除会话的时候，那么前端给服务端发送的请求，请求方式是什么？请求参数又是什么？

那么直接来看哈。

那这里的请求参数呢，呃其实和我们刚才加载指定会话的请求参数是一样的。在路径当中传递了这么一个参数哈，就是会话的名字。

啊因为大家来看，你点击这一块的这个删除，那你是不是得告诉服务端你要删的是哪个会话呀？哎你需要将会话的名字传递过来。啊那这里呢依然是通过路径参数传递的。而由于当前是删除的操作，那么在 RESTful 风格的接口当中啊，那么删除对应的这个请求方式就是 DELETE。

啊所以这一块要用到的请求方式啊是 DELETE。那么删除的话，你只需要将对应的这个会话的文件删掉就行了，并不需要额外的给前端返回数据。所以大家会看到响应给前端的数据呢，data 这一块返回了一个叫做 None，这个叫 None 哈。那么在 JSON 当中这个 None 就代表没有数据，啊不需要给前端返回数据，代表的是空的。好嘞，那么接下来呢，我们就来完成这一块的需求。来，我将这一块的这个接口的信息啊，那我这里呢截个图，然后贴在屏幕的上方。接下来呢打开 PyCharm。

来，在这里呢，我们添加一个注释哈，那这是删除指定会话。

好，那这一块我直接让 AI 给我提示了，app.delete，请求方式是不是 DELETE 啊？然后请求路径 /api/sessions/，这一块是不是又是一个占位符？又是一个路径参数？

哎那这个我们刚才都做过哈，我这一块直接让 AI 给我生成了。

呃其实大家来看，你看这些体力活 AI 是不是完全可以帮我们做了？

来，我们这一块来看一下哈。

呃定义了这么一个函数 delete_session，然后呢去接收这个路径参数 session_id。那响应的是一个 APIResponse，没问题。输出日志也没问题。根据 session_id 是不是获取到我们绘画文件的这个文件名啊？拿到会话的文件的文件名之后，接下来呢在这一块执行删除操作。

呃当然在这一块我们在删除之前可以去做一个判断哈。呃如果说这个文件它存在的话，那我再去调用 os 当中的 remove，就是操作系统这个模块当中的 remove 这个函数将这个文件直接删掉。然后接下来给前端返回对应的结果，啊 200 就代表成功了，啊删除会话成功，那 data 就是 None。啊那这个 None 呢，就是我们 Python 当中的这个空哈，那么最终转换为 JSON 的话，那么其实就是这个 None。

好，那么接下来呢，代码编写完毕之后，来，我们再来快速做一个测试。

来，这里呢我们右键再次运行，好这里停止并重新运行哈。呃一旦这个 Python 代码变了，大家就需要重新启动一下哈，来加载最新的代码。然后接下来呢，我来这一块打开浏览器，打开浏览器之后，我在这一块先刷新一下哈。

呃刷新之后，我要来删除会话了。那这里呢，我们点击这里的删除。

好，问你要不要删除啊？啊给你进行一个确认，来，这里点击确定。

你看，这个删了。删完之后，来，我在这块我再新建一个，新建之后，我在这块再来。

这一块来一个，你好！

啊再与 AI 交互一次哈。

哎你看，AI 大模型是不是给我响应回来了？好，那么接下来大家再来看，那我先在删除最后一个呢？注意，我删除的是 2030 这个，我点击删除。

确定删除。

哎你看，把最后一个删了。啊那前面两个是不是还在？

好，然后紧接着我在这块我再点击这里的删除，然后点击确定。那这个删了之后，是不是就剩下第二个了？哎那么这个第二个会话的数据又加载展示出来了。

好嘞，那么到此呢，关于我们汉字迷盒这个应用当中所有的功能呢，我们就已经开发完毕了。

那么这里呢，我们所开发的功能一共包括这么五个。那第一个呢就是新建会话的功能；然后第二个呢就是与 AI 大模型交互的这个功能；那么第三个呢就是会话列表加载的功能；第四个就是加载指定会话的功能；那最后一个呢就是删除指定会话的功能。

好嘞，那么到此呢，关于汉字迷盒这个应用呢，我们就已经开发完毕了。那么接下来呢，大家可以暂停视频，完成这个项目当中大家所没有完成的功能，继续把这一块的功能搞定。

黑马程序员为千万学生少走弯路，我们一直在努力。

好的，那么到此呢，关于汉字迷盒这个应用呢，我们就已经开发完毕了。那么在开发这个应用的过程当中啊，我们在控制台当中是不是输出很多的一些日志啊，来辅助我们进行调试的。那刚才呢我们在输出这些个日志的时候啊，那其实大家会看到我们是不是都是基于 print 输出的这个语句来输出的日志啊？

哎那么接下来呢，我们就需要回过头来思考一下，那如果说基于 print 语句我们来记录日志，有没有问题？如果有问题，又有什么样的问题呢？

那么其实大家会发现，我们之前在开发一些案例、开发一些小项目的时候，我们是不是都是基于 print 来记录的日志啊？好像也并没有什么问题。

那接下来呢，我们需要思考一点哈。

就是现在呢，我们整个项目是不是已经开发完毕了？哎也已经调试测试通过了，那现在假设这个项目要上线，那这个时候我之前记录的这些日志我现在已经不需要了，因为这些日志是辅助我们开发、辅助我们调试的，项目都已经测试完毕、都已经通过了，这些日志是不是就没有存在的必要了？那我现在的问起来了，如果基于 print 这种方式来记录日志，那我如何关闭掉这些日志呢？

哎那这个时候其实你是没办法去关闭的，你如果不想让这个语句输出了，两种方式。要么把所有的文件当中所有的 print 语句怎么着？删掉。要么注释掉。哎就这两种方式，否则它会一直输出。

这是第一个问题，没办法关闭。然后第二个问题呢，就是我们基于 print 再去记录日志的时候啊，大家其实会看到在控制台当中是不是会输出对应的信息啊？哎你比如我们创建会话，哎这块就会输出创建会话。那我问大家，这句日志是在哪个文件当中输出的？是在哪一行输出的？什么时间点输出的？你知不知道？

哎你是不知道的。你只知道，哦这句日志输出了，什么时间点输出的、在哪一行输出的你都不知道。

这就是第二个问题。

啊日志输出的时间、输出的位置，我们是一无所知的，我们只知道日志输出了。那这样的话，它是不利于我们去排查问题，不利于我们去分析并解决问题的。

那么这就是我们之前在基于 print 语句在记录日志的时候所存在的两点问题。

那么既然有问题，我们就要来解决问题。所以呢，在这块的核心功能开发完毕之后，接下来这一小节呢，我们就来完成这一块的程序优化，来解决这一块日志记录的问题。

那怎么解决呢？

呃那么对于这一块的日志记录啊，其实我们是可以借助于 Python 官方所提供的 logging 这个日志记录模块来解决的。那 logging 这个模块就是 Python 官方所提供的专门用来记录日志的模块。那通过这个 logging 模块记录的日志呢，会更加的规范，而且呢会更加的灵活。也就是说我们可以灵活的控制哪些日志我要输出，哪些日志我不想输出，我如果不想输出了，我只需要去改一个开关就完事了。而且呢，它还可以灵活的去控制我们输出日志的格式。

那么接下来呢，我们就来看一下，那 logging 这个模块要来记录日志具体该怎么做呢？

呃那这里呢其实分为这么三步操作。首先第一步，那么要想用这个 logging 模块，它是 Python 官方提供的，是标准库当中提供的，你只需要将其 import 导入进来就可以了。

那么导入进来之后第二步呢，那我们就需要去配置日志的一些基本信息。

啊那这里呢，我们可以去调用这个 basicConfig 这个函数来配置日志的一些基本信息，主要配置这么两项信息。第一项叫 level。那 level 呢控制的是日志的级别，啊就是指定我要输出什么级别的日志。那这里指定的是我们要输出的是 INFO 级别的日志。那 INFO 呢其实就是我们的常规日志。那到底有哪些级别呢？

那这里呢，我们介绍一下这个日志级别哈。那日志级别其实是给日志信息贴上的一个标签，通过标签来区分这个日志的一个重要程度。那常见的日志级别呢主要有这么五个。第一个 DEBUG，啊记录的是一些底层的一些调试信息，那这块的信息呢相对来说是比较详细的。如果说你把日志的开关设置为 DEBUG，输出的内容呢是比较多的。

然后第二个呢就是 INFO，就是我们常规记录的一些详细信息哈，那么就可以通过 INFO 这种级别来记录。那第三个呢是 WARNING，WARNING 记录的是一些警告信息，啊比如我们的磁盘空间不足了，对吧？哎可能会给我们提示一些警告信息。然后走再往下走就是 ERROR，记录的是错误信息。那 CRITICAL 这个叫致命的错误，啊是非常严重的错误。

那这是五种日志级别。那如果说我们这块设置为 INFO 代表什么意思呢？

就代表那我要输出的日志啊是 INFO 及以上级别。那刚才给大家介绍的这 5 种日志级别呢，它的日志级别依次升高。DEBUG 日志级别是最低的，接下来是 INFO、WARNING、ERROR、CRITICAL。那如果这块级别设置的是 INFO，就代表啊，那我要输出的是 INFO 及以上级别，也就是说 DEBUG 日志我就不输出了。啊，我要输出的是 INFO 及以上的这些级别。那如果说你把这块的这个 level 设置为 WARNING，那么也就意味着，那这个 DEBUG 和 INFO 我就不输出了，那我要输出的日志呢是 WARNING 及以上级别的日志。

哎，这是这个日志级别。然后紧接着下面的这个 format 呢，它代表的是日志输出的格式。啊，因为如果说基于 logging 这个模块来记录日志的话，那这个日志格式我们是可以自己去定义的。啊，你想怎么输出，就怎么输出。那之前基于 print 输出是不是只输出一句具体的消息啊？哎，只输出具体的信息。那如果基于 logging 这个模块来记录日志，那这里呢我们可以指定日志输出的时间。啊，这个就代表要记录的是日志输出的时间。以及这个日志的级别，什么级别的日志呀？然后紧接着这块指定的就是，那这个日志是哪个文件当中输出的。冒号之后这块还有一个百分号 line number，这个代表的是哪一行当中输出的。那百分号 message 代表的是输出的日志信息是什么。而中间的这个横杠呢，其实就是把多个区域呢在这块给它分割起来。

好，那么这就是指定日志输出的格式。那这个格式大家不要去记。啊，不用去记，你记不住的。啊，你直接将来如果说你想调整，问 AI 就可以了。

好，那么这是第 2 步，啊就是配置日志的基本信息。

那么第 3 步呢，就是在我们记录日志的时候就可以直接去调用 logging 这个模块当中的方法来记录了，比如 logging.info，那么记录的就是 INFO 级别的日志。那如果说你想记录的是一些底层的一些调试信息，DEBUG 级别的日志，那就 logging.debug 就可以了。啊，那么这里呢每一种日志级别都会有对应的这个函数。

好嘞，那这是我们这块介绍了一下，那么基于 logging 这个模块在记录日志的时候具体该怎么做。

那么简单介绍之后，接下来呢我们就直接打开 PyCharm 来演示一下。

来，我将这块服务先将其停掉。停掉之后，来，我们往上走啊，往上走。在这里呢我们要再去导入一个模块，logging 这个模块。直接来，import 这块导入这个 logging。

好，那么导入进来之后，第 2 步呢我们需要去配置日志的信息哈。呃配置日志的基本信息。那主要配置一下日志级别和格式。啊，那这里呢我们就可以去调用 logging 当中的一个函数叫 basicConfig。

好，那这里呢我们指定这么两项参数啊，我在这块稍微换一行哈。一个是 level，代表的是日志级别；一个是 format，是格式。

哎，我这块写一下。这个是日志级别，这个是日志输出的格式。

好，那这里呢，AI 提示，首先，在这块输出日志的时候要输出一个时间。啊，没问题，这个百分号括号里面写一个 asctime，就代表时间。后面 s 就代表这是一个字符串。然后紧接着第 2 个呢是日志的级别，啊就是是 INFO 级别的，还是 DEBUG 级别的，还是 ERROR 级别的，啊。然后第 3 个呢就是日志的信息，啊输出日志的详细信息。当然这里呢如果大家需要，啊那么这个日志到底是哪一行输出出来的，什么时间点输出出来的，哪个文件当中输出出来的，你都可以指定。

呃比如我在这块我再指定一下。呃这里来一个叫百分号，那里面文件名这块应该是 filename 哈。那我让 AI 给我提示哈，我也记不住，哈。这块再来。那这是文件名，冒号之后我在这块再来一个，呃就是行号，行号的话，诶，提示出来了，是这个 line number，啊这一行号。那行号那应该是一个数字，这块来个 d 就可以了。

那么这块我给大家介绍一下，我就写在上面这个位置哈。呃那么在这块说一下。那这个呢代表的是时间。诶，AI 给我提示了。然后紧接着呢，那这个呢代表的是日志的级别。哎，这块我写个分号哈。那这个代表的是日志的级别。那这个 message 代表的是日志的信息。然后紧接着这块还有一个 filename 代表的是文件哈。那代表的是文件名是什么。然后这块还有一个百分号 line number，那这个呢代表的是就是具体的行号。啊，行数行号都可以。好，我这里呢给大家写个注释哈。

行。那这样呢我们第 2 步就搞定了。

那第 2 步搞定之后，接下来第 3 步就是要去改造我们之前记录日志的方式了。来，往下走啊，往下走走走，走，走到这。

呃现在是不是就不用再 print 了？哎，那这里呢我们要调用的就是 logging 当中的这个函数 info。啊，我们这里记录正常的日志啊，大家用 info 就可以了。然后这块呢我们也直接来哈，那这块就改为 logging.info 就可以了。然后一块一块改哈，然后再往下走，这块也改为 logging.info。

好，再往下，那么再往这，走这，你看，print 这块输出具体的日志，后面这块是不是跟了一个参数啊？呃那这里你看，这个参数它是 print 这个函数当中的这个参数，那它会将这块的数据是不是拼到一起啊？呃那这里呢在改造之前啊，我先把这块改一下。那这里呢我换成这个格式化的这个字符串。啊，来个大括号。好，然后把前面这块改为 logging.info。

好，然后紧接着下面这块是不是也是类似的這個道理啊？好，前面这块加上一个 f，然后呢我们这块来一个叫做 logging.info。

好，那么这块哎都是类似的哈。session_data 前面加个 f，来，这个是 logging.info。提示一下呀。好。

呃那么再往下走，这块再来 logging.info。来，再往下，这块来 logging.info。好，这块还有一个啊。来，logging.info。来，下面这块就没了。

好嘞。那这样呢，我们所有的记录日志这块的这个程序呢，我们都改造成基于 logging 这个模块来记录日志，而不再是基于 print 语句记录日志了。

那么接下来呢代码改造完毕之后，我们就来做一个测试，看一下有什么变化。

那这里呢在测试的时候啊，我将上面刚才我们的这个日志输出的格式啊，这块的这个 format 指定的这个格式，我在这块我截个图，贴在屏幕的上方。接下来呢，来，我们这里直接右键运行。

好，运行之后，这是我们启动这个服务，它默认输出的日志啊，这个不用管。来，我们清除所有日志，接下来打开我们的浏览器。打开浏览器之后，来，我们在这块点一下，再点一下。啊那其实你直接执行一顿操作就行了，我们的目的呢就是让它控制台给我输出一堆日志。来，我回到我们的 PyCharm 当中，大家来看。

控制台当中现在是不是输出了一堆的日志啊？而在输出日志的时候，大家来看哦，要区分清楚。这个红色的，是我们自己记录的日志；啊黑色的，这个是服务器它默认输出的日志。你看，是不是这这不都服务器默认输出的日志，访问日志啊。

好，那么这里我们主要先看一下红色日志的格式啊。大家来看。这里我们获取指定的会话信息，我们找到这行日志输出的位置啊在哪一行的？174 行，看这。是 main.py 这个文件的 174 行输出的。来，我们看一下，找到 174 行。来，我们往下走往下走，找 174 行。

174，大家来看，174 行获取指定的会话信息，会话名称是什么，在这块输出出了。但是大家会发现，我在调用 info 这个函数的时候，它只是指定了里面这一个字符串，只是指定了后面这部分，那前面这一堆哪来的？

这就是 logging 这个日志模块默认给我们输出的。你看，我们刚才指定格式，第 1 个，你看这，第 1 个是不是时间啊？你看，这就是那个时间。然后紧接着空格，这块是不是有个横杠？横杠之后又有一个空格，啊你看这块，这是不是原样输出的，就是连接符嘛。然后百分号 level name 是不是指定的是我们日志输出的这个级别，什么级别的日志啊？哎，INFO 级别的日志。然后后面又是一个横杠，左右两个空格，你看，这块是不是原样输出了？哎，然后紧接着就是我们具体日志输出的这个文件是哪个，以及在文件的多少行，你看。这是文件。啊这是文件。这是具体多少行。中间用什么分隔？你看这，我用的是冒号分隔，冒号是不是原样输出啊？然后后面这块又是一个横杠，左右两个空格。

所以这个格式我们是可以自己定义的。啊那也就意味着，如果我不想要这个日志输出的级别，我不想要这个日志输出的时间，你去看前面这块的这个内容怎么着？删掉就完事了。啊所以这个输出的这个日志的格式呢是非常灵活的，我们可以自由的定制。

而且呢大家再来看，那如果说啊这些 INFO 级别的日志我不想输出了，我觉得我现在程序已经开发完了，我也已经调试通过了，我不想看这个 INFO 级别的日志了。那这个时候怎么办？啊这个时候你就找到最上面我们日志配置的这个基础信息，把这块改一下就行了。啊你想看什么样的日志你就怎么调，我只想看一些错误信息，那这块你就把它调整什么什么？调整成 ERROR。那么像 DEBUG，像 INFO，像 WARNING 都不展示了，只展示错误信息。

好，那么接下来大家再来看，现在我在这块重新启动。啊，重新启动之后，注意，这个再次强调，这不是我们输出的日志，这是服务器启动的日志，是不受影响的。来，我们在这一块再次清空，清空之后，来，我点一下，再访问。好，我们在这一块再回答一个，啊，不知道。

好，那么接下来呢，我们再次回到我们的 PyCharm 当中，大家来看，现在刚才的那一堆日志还有没有了？没有了，因为当前我们程序运行是没报错的，所以呢是没有错误日志的。那么刚才所输出的这些个 info 级别的日志，现在还有输出吗？没有了。哎，我们的控制台是不是就干净了？

哎，那这就是基于专业的日志记录模块 logging 来记录日志它的一定效果。我们是可以灵活地控制我们要展示哪些日志，灵活地控制我们要关闭哪些日志，可以灵活地控制日志输出的格式。

而最后呢，大家所看到的控制台当中的这些日志，这是服务器内部我们在访问的过程当中，它所输出的日志。啊，当然这些日志呃建议大家保留着。啊，如果你不想留，啊我也可以告诉大家怎么关啊。大家来看，这是不是就是启动我们的服务器啊？哎，那么在这里呢，其实如果说你不想看这些访问日志，呃这个时候大家可以在里面有这么一个参数，啊叫做 access_log。啊，就是访问日志你还要不要？你不要，你就把这一块设置为 False 就可以了。来，我再重启一下，来看效果啊。

好，那这是启动的日志，然后紧接着我们在这一块打开我们的浏览器，大家看好了，我多点几下就行了，啊多点几下，对吧？点了之后我问大家控制台还有日志没有？没有。啊，因为这个访问日志你也把它关了。好，呃当然这个访问日志啊我说一下啊，呃大家在学习阶段你把它留着啊，不建议关啊。那我这一块写一下，这是关闭访问日志。

好嘞，那这就是我们这一小节所讲解的关于日志记录这一块的程序优化。啊，那么在进行项目开发时呢，我们一般都推荐啊使用专门的日志记录的这个模块 logging 来记录项目当中的日志。好的，那这是我们这一小节所讲解的程序优化，啊优化记录日志这一块的代码。那么接下来呢，大家就可以打开自己的 PyCharm，按照我们刚才在这一块所讲解的日志记录这一块代码优化的一个思路，来将大家案例当中的这个代码呢进行一个优化改造。

黑马程序员，为千万学生少走弯路，我们一直在努力。

好的，那么优化完了日志记录这一块的代码之后啊，接下来呢我们再来对汉字迷盒这个项目再来进行一个测试啊。那刚才呢我们在整个的测试过程当中呢，我们只是测试了一些正常情况，并没有测试异常情况。而在进行项目测试的时候，大家一定要把各种情况都要测试到。啊，那么接下来呢，我们再来测试一下异常情况。

那这里的项目呢我已经启动了，接下来呢我就直接打开我的浏览器，这里我再去新建一个会话。呃那这一块我们呃简单和 AI 大模型进行一个交互啊。那么大家会看到这一块是不是又是一个新的会话？哎然后呢我们切换会话，在这一块去加载指定会话这一块的功能是不是依然是正常的？好，现在呢，大家注意了。

现在我要回到我的 PyCharm 当中，要将刚刚创建的这个会话，也就是 161653 这个会话，我要将其会话文件删掉。来，我直接打开我的 PyCharm。呃因为我们的代码写的基本上是天衣无缝的，对吧？哎那这里呢我们就手动的来操作啊。这里我将这个会话文件怎么着？啊我给它删掉了。啊会话文件直接将其删掉。那删掉之后，接下来我再问大家，现在我再去加载这个会话，由于页面没有刷新，所以这个会话列表当中的这个 161653 是不是还在呢？现在注意了，我要去点击加载这个会话了，看一下会发生什么现象。

来，这里呢我们点击一下。点击之后大家会看到这一块加载会话失败，哎这个信息是不是也提示出来了？然后紧接着我们要去看一下啊，我按下 F12。那按下 F12 之后，接下来找到 Network，找到网络这一栏。接下来我再次点击一下加载指定会话，那么此时大家来看，网络这一栏是不是已经报错了？哎服务器端状态码 500。然后紧接着我们点一下，大家会看到这儿，服务器端响应回来的数据是什么？

哎服务器内部错误，是不是就是一个字符串啊？哎服务器端并没有响应任何的数据回来。

但是大家再思考一下，我们前面讲到我们当前这个项目当中所有的功能接口，最终给前端响应回来的应该都是一个 JSON 格式的数据，里面是不是包括三项 key？哎这三项 key 分别是：第一个 code，然后第二个呢是不是 message 啊？哎第三个呢就是 data。但是大家会发现，现在一旦程序运行出现问题，是不是就没有按照我们的要求来返回啊？

那我们期望的是，无论程序是正常运行还是异常运行，你都应该按照这个格式给前端返回，然后让前端按照这个格式来进行一个解析，然后再将这个错误信息也就是这个 message 是不是提示出来啊？但是现在明显大家会发现，嗷正常情况下呢，哎我们是给返回一个 JSON，里面三项信息都有，但是一旦出现异常，那这个时候返回的数据是不是就不符合我们项目的规范，不符合我们的要求了？哎那这是为什么呢？

啊原因呢其实很简单，就是因为在我们当前开发这个应用的所有功能的时候，我们有没有考虑异常处理的问题啊？哎并没有考虑异常处理。那这个时候就会出现这样的问题。

好，那么出现问题我们要解决问题，接下来我们就要思考一下，那针对于这个问题我们又该怎么去解决呢？

哎那么其实解决这个问题啊，我们前面讲到过异常处理嘛，解决这个问题其实也很简单，怎么做呀？哎你把所有的这些功能里这个代码怎么着？哎你都进行一个怎么着？哎你都进行一个 try，然后下面这一块呢，再进行一个异常的捕获是不是就行了？哎 try except，然后将我们的这些业务代码直接包裹在 try 这个代码块当中是不是就行了？哎然后去捕获所有的异常。一旦出现异常，那我们这一块也给它返回一个 ApiResponse，也给它响应 code、message、data 是不是就行了？

哎当然这是这个功能。那么其他功能要不要按照类似的方式全部做一遍？要不要？哎需要。你直接基于 try except 去捕获所有的异常，捕获所有功能当中的异常就可以了。那这么去做的话可以，啊但是呢这么去做的话会比较繁琐。我们这个项目当中所有的功能是不是都需要按照这个结构进行一个改造啊？而且你会发现存在大量的重复性的代码。啊所以这种方式可以，但是这种方式呢不推荐啊。来，我再将代码恢复了。

那接下来呢我们就来说一下，那针对于这一块的这个问题，我们该怎么去解决呢？

来，我们回到 PPT。那么其实针对于这个问题啊，我们可以借助于 FastAPI 当中所提供的异常处理器来进行解决。那么这个异常处理器啊，它是可以去捕获我们项目当中所有的异常的。来看一下具体怎么做。

呃那么大家来看，现在我们的流程是这样子的：前端在页面当中发起请求，请求服务器端，访问服务器端的这些功能接口。那如果说服务器端这些功能接口都正常执行的话，那么服务器端是不是会给前端返回这么一个 JSON 格式的数据啊？哎里面有三个 key，分别是 code、message、data。而当前一旦我们服务器端的这一块的功能接口运行出现错误，那么此时大家会发现，最终是不是就没有按照这个格式给前端响应了？

但是我们期望的是什么？即使出现错误，你也要按照指定的这个格式给前端响应数据。比如一旦出现错误，我就给前端 code 响应 500，啊 500 就代表服务器端异常，message 就告诉他服务器内部错误，啊请联系管理员之类的信息，那 data 我这一块就返回一个 None，就代表 data 我是不需要额外返回数据的。那也就意味着出现异常我是不是也期望返回的是这样一个标准的 JSON 格式的数据啊？

哎那这个时候怎么做呢？

哎我们刚才提到，这里呢我们可以借助于 FastAPI 当中的异常处理器来解决。那这里呢我们就可以定义这样一个异常处理器。那这个异常处理器我们在定义的时候是可以去指定我们要捕获哪一类型的异常的。那么一旦指定了我们要捕获的是哪一类型的异常，那么接下来在我们当前这个项目当中，那么所抛出来的异常只要满足我们对应的异常类型，它都会被这个异常处理器捕获到。

也就是说啊，那么在这些功能接口当中你就不用再去处理异常了，所有的异常处理你全部交给异常处理器来进行处理。那么异常处理器处理完异常之后，再给前端响应这么一个结果是不是就可以了？哎响应符合规范的这样一个 JSON 格式的数据就可以了。

好，那这样的话，我们在项目当中处理异常，我只需要去定义一个异常处理器。那其他的这些功能接口当中呢你都不需要再考虑异常的处理了。好嘞，那么这就是我们这一块所提到的统一异常处理的一个思路。那么接下来的核心呢，就聚焦在这一块了，那怎么样去定义这样一个异常处理器呢？

哎那这里呢定义异常处理器其实相对也是比较简单的。啊我们只需要在这一块去定义一个函数，然后在函数上面加上这么一个装饰器 exception_handler。那 exception 就是异常的意思，handler 就是处理器，这也就是去声明这样一个异常处理器。然后指定，括号里面指定，那当前这个异常处理器你要捕获的是什么样的异常啊？那我们这块写的是不是叫 Exception 啊？这就代表我们要捕获所有的异常。那么也就意味着，这个异常处理器它会捕获我们这些功能接口所抛出来的所有的异常，都会被它捕获到。

那捕获到异常之后，它就会自动地来调用这个函数。然后在调用这个函数的时候，它会自动地传递两个参数进来：一个呢，就是 request，也就是请求，那你当前这个异常是哪一个请求啊在这块抛出来的，那这是第一个参数；然后第二个呢，就是 Exception，也就是抛出来的异常，什么异常啊？异常的类型是什么呀？那这块我们是可以获取到这个异常信息的。哎，然后接下来呢，那捕获到异常之后，我接下来我就可以记录一下这个错误日志。那记录错误级别的日志，那我们就可以调用 error 这个函数来记录，然后最后再给前端返回一个结果。

但是这里呢需要注意哈，就是在异常处理器当中，那如果说我们要给前端返回数据，那就不能再使用我们自己定义的那个 ApiResponse 了。那这里呢，我们要使用的是 Response 或者是 Response 当中的子类对象来构造给前端返回的结果。好，那这里呢，我们就可以使用 JSONResponse，然后指定我们要给前端返回的这个内容是什么。内容就是这么一个 JSON 格式的数据。那最终呢，它就会将我们在 JSONResponse 当中所封装的这个内容响应给前端。那么前端呢拿到这块的内容就知道，哦，是服务器端内部错误了，它就会将这块的信息呢直接提示在我们的页面上。

好嘞，那么简单介绍之后，接下来呢，我们就来演示一下。来，这里呢，我们直接打开 PyCharm。那当前我们的所有的功能接口当中是不是都没有异常处理啊？都没有去处理异常。那接下来呢，我在最后这块我来定义一个异常处理器。那这个异常处理器呢，它是要去捕获所有的异常的。来看一下怎么做。

好，AI 已经提示了哈。那么在这块首先要用到这么一个装饰器 exception_handler，然后指定我们要捕获哪些异常。你写 Exception 就代表捕获所有异常。然后紧接着呢，我在这块来定义这么一个函数，那这个函数我们就叫 handler_exception。然后在这个里面呢，它可以接收两个参数：一个参数就是请求对象，也就是我们发送哪一个请求的时候报错了。那通过这个请求对象呢，我们就可以获取到请求的一些信息，包括像请求的 URL 啊相关信息。那这个呢就是异常信息。那既然是捕获异常，你抛出来的是什么类型的异常？是什么异常啊？这个异常信息我们也可以获取到。那获取到这个异常信息之后，我就可以在这块去调用 error 这个函数来记录异常信息了。好，那么在这块呢，我们可以来记录一下捕获到了什么样的异常。那在前面呢，我们在这块也可以再输出一下哈，就是处理异常，那对应的这个请求路径是什么呀？那其实我们都可以从这个 request 对象当中获取到的。打一个点你看，request 当中啊它其实有很多的属性，里面有这么一个属性叫 url，就是我们请求的 URL 地址是什么，对吧？然后后面在这块输出一下我们的异常信息。

好嘞，那么再往下走呢，下面这块我们刚才提到啊，那如果是定义异常处理器，那我们要返回的，这块写一下哈，要返回的这个对象的类型哈，对象的类型，那得是什么类型呢？哎，得是 Response 这个类型。当然这些呢在官方文档当中也都有说明哈。那这里呢，我们在这块可以使用 Response 下面的一个子类叫做 JSONResponse。来，我们这块直接 return 这么一个叫做 JSONResponse。好，那这个 JSONResponse 是谁提供的呢？其实我刚才在这块敲了一下之后，它上面这块应该自动给我导进来了，这个呢就是 Starlette 当中提供的 JSONResponse。好，然后紧接着呢，我们在这块在 JSONResponse 当中我们就可以指定这么一个参数叫 content。那 content 就是我们要给前端响应的内容。那想的内容是不是就是涉及到这么三个 key 啊？第一个是 code: 500；然后第二个，message: 服务器内部错误，后面在这块我们再来一个，请联系管理员；好，那么第三个，data，那如果说哈已经出错了，那我们用不用再去给前端返回数据了？直接返回一个 None 就可以了。

好嘞，那这就是我们在这块所定义的这个异常处理器。那这个异常处理器它会捕获所有的异常，也就意味着，那么当我们程序当中在运行的过程当中一旦出现异常，最终它就会自动地调用这个函数，那这个函数一调用，最终就会给前端返回我们这块指定的这个内容。

好嘞，那这块的代码我们编写完毕之后，接下来呢，我们就可以来做一个测试。来，这里呢，我们右键在这块直接运行。好，那这里呢已经运行成功了。来，我们清空所有的日志哈。那重点看一下处理异常请求路径是什么，这句日志它会不会输出？如果能输出是不是就说明出现异常它自动地来调用这个函数了？

好嘞，那么接下来呢，我们打开浏览器，我们继续来加载这个会话。这个会话文件在服务器端我刚才是不是已经删掉了？来，清空日志哈。来，点击一下。好，点击之后大家来看，已经提示了：服务器内部错误，请联系管理员。这是不是就是我们刚才给前端返回的这个提示信息啊？然后紧接着大家再来看一下，那么下面这块，你看这儿，这个里面看这儿，即使出现错误，那么服务器端返回回来的这个数据格式是不是依然是一个 JSON 格式的数据？依然有 code、message、data。那么此时你会发现，无论我们当前的這個功能服务器端返回回来的数据是成功了还是失败了，那么最终啊它都会返回一个 JSON 格式的数据。都是满足我们开发规范的，都会包含 code、message、data。

好，那么紧接着呢，我们在这块再回过来再来看一下，那我们控制台当中输出的这个错误信息哈。来，我们往上走，往上走。那么走到这儿大家来看，你看这儿，是不是 error 级别的日志啊？处理异常请求路径是什么，你看这儿，是访问哪个路径的时候出错了，这块是不是输出出来了？然后捕获到的异常信息是什么，那后面这块呢日志也输出出来了，告诉我们没有这么一个文件。没有哪个文件？没有 sessions 目录下的这个 1653 的这个 json 文件。那这是不是就说明一旦出现异常就会自动地调用我们这个函数？那这个函数一旦调用，最终就会给前端返回我们这块的这个异常的结果信息。当然也是一个标准的 JSON 格式的数据，里面三项 key：code、message、data 都是有的。

好嘞，那么这就是我们最后一块在这块优化了一下我们当前这个项目当中的异常处理的方式。那这里呢，我们是通过自定义了一个异常处理器来捕获项目当中所有的异常。

好，那这里呢，大家需要关注的就是这个异常处理器它的定义方式。那大家需要关注的点呢有这么几个：第一个就是上面我们所使用的这个装饰器，叫做 exception_handler；然后另外一个大家需要关注的就是这块的这个返回值。那这里呢最常见的哈，我们在这块要返回一个 JSONResponse 这么一个对象，然后在里面去封装那我们最终要给前端返回的数据是什么。

那么关于这个异常处理器的定义呢，那么其实在官方文档当中也有详细的说明。我最后呢带着大家去看一眼官方文档。那么在官方文档当中大家找到这块的用户指南，找到用户指南之后啊，那之前我们去接收各种各样的参数，其实在官方文档当中都有说明，你比如怎么去接收路径参数，对吧？哎，怎么接收请求体当中的参数，官方文档都有说明。那接下来呢，我们往下走哈去找，这块有一个叫做处理错误，啊，就是处理异常。那具体怎么处理异常？怎么样去定义这个异常处理器？那么在下面这块都有。往下走，你看，自定义异常处理器。怎么去定义，用哪一个装饰器，是不是在这块都有说明啊？以及这个装饰器我加了之后，那么这个函数它里面能够接收什么样的参数？你看，第一个接收 request，第二个接收异常，对吧？那么这个和我们刚才写的一样。然后紧接着最终我们要给前端返回的结果怎么去封装，你看我们刚才用的 JSONResponse 是不是也是来自于官方文档啊？哎，那这些呢其实在官方文档当中都有详细的说明。

好嘞，那这是我们最后一块在这块带着大家优化了一下我们案例程序当中的异常处理的代码。

好的，各位同學，那么到此呢，关于我们 Python 项目实战之 Web 开发这一章节的内容呢，我们就已经讲解完毕了。那接下来呢，大家就可以暂停视频，将我们当前这个项目当中所有的代码呢全部搞定。

黑马程序员，为千万学生少走弯路，我们一直在努力。

那愉快的时光总是短暂的，转眼间我们又到了故事的终章，又到了说再见的时候了。那经过前面 50 多个小时的学习呢，我们已经学习完了这套课程的所有内容，一共包含六个章节，包括 Python 起航、Python 的核心语法、Python 项目实战，则是 AI 应用、爬虫程序、数据分析、Web 开发等。那通过项目实战的方式来加深大家对于 Python 核心语法的理解和掌握，同时，也通过项目实战的方式让大家知道 Python 在各个领域的应用。

那在这里呢，再强调一下，那我们这套课程呢是一套 Python 基础课程，目的呢就是通过这套课程的学习，为大家后续的学习打下坚实的基础，同时也拓展大家的视野，让大家知道 Python 主要的应用领域和应用场景。所以接下来呢，如果大家想深入地去学习某一个领域的技术，那还需要去学习它的进阶知识。那如果说大家想深入地去学习数据分析这一块的进阶知识，那么大家可以去看我们这套 Python 数据分析的课程。那如果说大家想深入地学习一下 FastAPI 这样的 Web 框架，那大家可以去看我们这套 Python Web 开发的课程，那么在里面呢会深入地去讲解 FastAPI 的方方面面。

而对于 Python 来说，当前最为火爆、最有前景，也是就业薪资最高的方向，那当然是 AI 开发。那如果说大家想走 AI 应用开发或者是 AI 大模型开发的方向，那么大家可以在学完我们这套 Python 基础课程之后啊，可以去看看像 Coze、Dify 这样的智能体平台，以及像 LangChain 全家桶的课程。然后呢，再基于 LangChain、LangGraph 再去开发对应的 RAG 以及 Agent 的项目。当然这些个进阶课程学习完之后啊，大家还要去做核心的一些项目，包括 RAG 项目，包括智能体项目、多智能体协同的项目，然后呢再去学习大模型的微调、大模型的底层原理等高级的一些知识，然后才可以就业。

当然呢，我也在课程的评论区当中啊看到了很多同学的留言，包括像：“那学完这个课程我能做什么？”啊，以及：“如何变现？”“学完这个课程我接下来我去做测开可不可以？”“学完这个课程我走 Agent 开发还需要去学习哪些内容？”以及：“学完这个课程我在广州我能拿到多少 K？”

那么这里再次强调一遍，我们这套课程仅仅是 Python 的基础课程，学完之后肯定是没法直接就业的。无论是数据分析还是 Web 开发，还是当前最为火爆、最有前景的 AI 开发，那么大家都需要再去学习对应的一些进阶知识，然后再去做各个方向核心的一些项目，以及学习相关的一些高级知识，然后才可以就业。

当然呢，我们黑马无论是线上班级还是线下班级的课程，那么所有的学科我们在 25 年的时候都已全面 AI 化，并且呢也在不断地迭代升级。那如果说大家想往 AI 开发方向走，包括像 AI 应用开发、AI 大模型开发、AI 嵌入式、AI 运维、AI 测试等相关的方向感兴趣的小伙伴，想看一看自身的学历、自身的年龄到底适合走哪一个方向的，或者说对于我们线下以及线上最新的 AI 课程感兴趣的，那么大家都可以去评论区的第一条置顶评论当中啊预约一下我们的老师，让我们的老师给大家做一个详细的课程介绍和一对一的职业规划。

好嘞，各位同学，那么到此呢，关于我们这套 Python + AI 的课程呢就要真正地画上句号了。那 50 多个小时、六个章节，我们从一个变量、一个循环开始，一步一步地走到 AI 应用、爬虫、数据分析、Web 开发。那我知道这不仅是一段学习旅程，更是一场坚持与成长的见证。那编程这条路从来都不容易，但请相信你的每一份努力、你的每一份坚持都会在未来的某一天闪闪发光，如期回响。那些熬过的夜、那些改不完的 bug、那些啃下的难题，终将化作你前行的底气，成全独一无二的自己。那最后呢，祝屏幕前所有的同学学有所成，顶峰相见。那我们下一套课程再见！