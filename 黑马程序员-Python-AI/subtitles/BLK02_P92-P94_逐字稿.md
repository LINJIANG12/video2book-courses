好的，那大模型部署完毕之后，接下来呢，我们再来讲解第二部分，大模型的调用。 那大模型的调用呢指的是我们要在自己的电脑上，基于 DeepSeek 官方开放的 API 接口来调用 DeepSeek 官方提供的大模型。哎，那这个过程呢我们就称之为大模型的调用。 而在这种网络结构当中呢，我们把 DeepSeek 官方提供的这个 DeepSeek 大模型呢我们就称之为服务端。 那它对外提供了一系列的 API 接口，也就是提供了一系列的服务。那我们呢就可以通过自己的电脑来调用服务端所提供的这些服务了。那我们自己的电脑呢我们叫它客户端。 那最终客户端是需要去连接服务端来访问服务端所提供的这些功能的。而客户端要想调用服务端，那这里呢就会涉及到网络通信，所以呢在调用大模型之前啊，我们还需要介绍一下网络的基础知识。那等我们了解了网络的基础知识之后，接下来呢我们会讲解两种大模型调用的方式。那一种方式呢就是基于 Apifox 这样的测试工具来调用大模型，那另外一种呢就是基于 Python 代码来调用大模型。 那么这一小节呢我们就先来讲解第一部分，网络的基础知识。 那么网络呢相信大家并不陌生啊，那这一块这个网络指的就是我们通常所说的互联网 Internet。那网络呢它是由无数个计算机网络设备连接起来形成的一个全球性的网络基础设施。 而这里所提到的计算机网络设备呢呃可以是我们的电脑，可以是我们的手机，可以是平板，可以是智能穿戴以及车机都算是网络设备。 那么大家会看到这个网络是不是就类似于我们城市当中四通八达的道路啊，哎那这里呢就是通过网络将我们各个网络设备连接起来，从而呢就可以实现不同网络设备之间的数据传输了。 那大家来看，那现在如果说我想找到这台手机，然后给这台手机要去推送一条消息的话，那我怎么定位到这个网络设备呢？ 哎那这里呢就需要用到 IP 地址了。 那 IP 地址呢大家可以理解为啊就是我们设备在网络上的一个唯一的身份证。 那每一个接入这个网络的设备呢，它都会有自己专属的 IP 地址。那 IP 地址的核心作用呢就是来定位这个设备在网络当中的位置的。 那比如图中的这个网络结构当中啊，各个网络设备它都会有自己的 IP 地址，那这些 IP 地址呢就相当于这些设备唯一的一个身份证号，它是不会重复的。 那我们来看一下这个 IP 地址的形式啊。你看，132.12.86.125。那一个 IP 地址大家会看到是不是有这么四段组成的？哎，中间通过三个点将这一串数字分割成了四段。 那么其实从格式上来说啊，这个 IP 地址呢分为两类。一类呢是 IPv4，另外一类是 IPv6。而大家平时所见到的这类的 IP 地址啊，其实都是 IPv4 类型的 IP 地址。那 IPv4 呢它是由32位的二进制组成的。而我们平时所看到的这些数字，我问大家这个 IP 地址里面132、12、86、125，这是二进制吗？哎明显不是啊，那二进制是不是就是由0和1组成的？哎那这个呢其实是二进制转化成十进制的表示形式，这个是十进制。 那大家会看到，这个十进制的 IP 地址啊它是由四段组成的，而每一段的取值范围呢都是在0到255之间。 那就比如132.12.86.125这个 IP 地址，那它所对应的二进制呢，哎就是这样子的。那每一段呢对应于8位二进制。比如132对应的二进制数呢，哎就是这8位二进制来表示的。那12呢对应于这个二进制数，86是这个，125是这个。那下面表示的这个呢就是二进制的表示形式。那最终大家会看到，每一段是不是都是由8位二进制来表示的？那最终4*8=32位。那 IPv4 是不是就是由32位的二进制来表示的。 而由于这个二进制呢它取值呢就是0和1，那这个8位二进制它所能够表达的这个数值范围是多少呢？哎那就是2的8次方，2的8次方呢就是256。而由于这一块这个取值是从0开始的，所以每一位啊这四段每一段它们的取值范围都是0到255之间。 好，那这就是这个 IP 地址它的一个表示形式。当然这是 IPv4 它的表示形式。而这一块呢还有一种叫 IPv6。那 IPv6 呢它是由128位二进制组成的。那它主要是为了解决 IPv4 地址不够用的问题的。因为我们全世界的这些网络设备太多了，而且每年啊都在激增。那如果说 IPv4 不够用了，那这个时候我们就可以使用 IPv6 的地址，它是由128位的二进制来表示的。那这个呢大家知道一下就可以了。哎因为我们平时日常生活当中所接触到的这些 IP 地址啊，那目前都是 IPv4 的这种表示形式。 而这里呢有一个比较特殊的 IP 地址大家需要记得，就是127.0.0.1。那这个127.0.0.1呢是一个非常特殊的 IP 地址，代表的是我们本机的地址，就是我当前电脑的 IP 地址，那就是127.0.0.1。那这个呢我们也称之为本地回环地址，哎也叫本机地址。那我们在进行项目开发的时候啊，那经常会用到这个本机地址。比如我要访问我自己电脑上的某一个服务的话，那所需要编写的 IP 地址呢就是127.0.0.1。 好，那这是我们这一块先介绍一下 IP。那 IP 呢就是用来定位网络当中的设备的。 那我们已经知道了 IP 地址的作用了，那接下来呢我们来看。那现在我要在我自己的电脑上来访问110.242.69.21。那当我访问这个 IP 地址的时候啊，那请求呢就会经过我们的交换机最终来定位到我们网络当中的这台设备。那最终它就是根据这个 IP 地址来找到对应的这台设备。 那大家发现没有，我们网络当中的这些 IP 地址长的是不是都很像啊？哎都是一串数字，而且这数字呢都是由四段组成的，看起来好像都差不多。那么记起来呢也是比较麻烦的。比如我们自己如果要想记住百度的这个 IP 地址110.242.69.21，那这个记起来是不是很麻烦？很容易混淆。但是反过来大家再想一下，那我们在上网的时候，我有没有去记住百度的 IP 地址？我有没有记住爱奇艺的地址，有没有记住腾讯视频的地址啊？ 没有。 因为我们在访问百度的时候，我们是不是直接打开浏览器，我们在地址栏当中直接输入 www.baidu.com 就可以直接访问到百度了，我们有没有输入 IP 地址啊？是没有的。那其实我们所输入的这个 www.baidu.com 呢，那这个其实是百度这个网站的域名。 那接下来呢我们再来介绍一下域名。那什么是域名呢？域名的作用又是什么呢？来，我们介绍一下这个域名。那域名呢其实就是由一串用点分割的英文字母组成的。那它主要的作用呢就是降低我们 IP 地址记忆和书写的成本。哎因为 IP 地址呢就是一串没有规律的数字，记起来呢是并不方便的。那么有了域名之后，我们直接去记这个域名就可以了。那通过域名呢我们依然可以定位到对应的网络设备。 但是这一块大家要注意了，我们在互联网上定位网络设备的只有 IP 地址，那通过域名又是如何定位到具体的设备的呢？ 哎那么这个时候呢就出现了一个东西，叫 DNS，叫域名解析服务器。 简单来说呢就是，当我在我的浏览器地址栏当中输入 www.baidu.com 这个域名之后，我一敲回车，它并不是直接去访问这个域名对应的这个服务器。它会先怎么做呢？它会先去请求一台服务器，这台服务器叫 DNS，就是域名解析服务器。 哎它会先请求域名解析服务器，那么在域名解析服务器当中呢就记录了域名与 IP 地址的映射关系，哎就是这个域名对应的 IP 地址是哪一个，每一个域名对应的 IP 地址都会在 DNS 这台服务器当中记录下来。 好，那么请求完 DNS 之后，那我们就拿到了，那 www.baidu.com 这个域名对应的 IP 地址是多少啊？就拿到了是110.242.69.21。那拿到了 IP 地址之后，接下来呢我就可以来访问这个 IP 地址了。最终，哎就可以定位到这台主机。 所以，有了域名之后啊，那我们以后再去访问这些服务器的时候，如果它有域名，那我就直接基于域名来访问就可以了。那这样呢它就可以大大的简化我们 IP 地址记忆的成本了。哎所以域名存在的目的呢主要就是为了降低我们 IP 地址记忆的成本。 那紧接着呢我们继续来看，那我们定位到这台服务器哈，那这台服务器呢就相当于是百度的服务器了。 那么大家要知道，在一台服务器当中，我们是可以去部署多个应用的。这就像当于在我们的台电脑上我是不是可以安装多个软件多个程序啊？

那么在一台服务器上，我们也是可以部署多个应用的。那么就拿百度的这台服务器来说，那百度旗下是不是有很多的产品线，比如像百度搜索、比如像百度网盘、百度地图以及爱奇艺这类的视频网站。那么假设在这台服务器当中啊部署了四个应用，那它怎么知道我访问www.baidu.com，最终要访问的是其中的哪个应用呢？那么此时呢，我们就不等不提到另外一个重要的概念，那就是端口。

那端口呢，其实我们也叫端口号啊，那其实它就是一个整数，它的取值范围呢在0到65535之间。那端口呢，它主要是用来标示我们计算机设备上的运行的程序的。啊也就是说啊，我们在自己的电脑上所运行的这些程序，每一个程序都会有自己的端口号。哎比如，百度搜索它的端口号呢是443，那百度网盘端口号呢10010，啊这个爱奇艺端口号9127，那百度地图8848。那如果是一台计算机上，那它的端口号是不会重复的。

那这个时候我们通过端口号我是不是就可以定位到具体的这个应用程序了。那么紧接着大家再来看，那当我在浏览器地址栏当中输入www.baidu.com之后，那访问到的到底是哪一个应用呢？我并没有指定端口号啊。哎，那这个时候我可以打开浏览器，带着大家看一下，我这块直接访问一下百度。那么当我访问百度的时候啊，我们直接在浏览器地址栏输入www.baidu.com是不是就可以了？但是大家要注意咯，其实在我们的域名的前面，这块有一个东西，叫什么呀？https，那这个呢，我们在下一小节会讲到啊，这个叫协议部分。那这一块默认啊你看百度它所采用的协议是https协议。那么一旦我们在这块采用的是https协议，那么它是有默认端口号的。如果是http协议啊，默认端口号是80，如果是https协议，默认端口呢是443。

啊这就意味着啊，我们在访问百度的时候，你看，我们这个域名后面没有指定任何的端口，但是它是有端口的。那由于我们这块用的是https协议，你没有写端口，默认就是443。所以呢，最终它在访问这个IP地址的时候，其实它后面省略了一个东西，就是:443。那冒号之后的这部分呢，其实指定的就是端口号。如果是https协议，你不写也是443。那么此时大家来看，那这个时候我们是不是就通过IP地址就可以定位到了我们要操作的是哪台设备、要访问的是哪台设备，然后通过后面的这个端口号，我是不是就能够知道那我要访问的是这台设备当中的哪一个程序啊？哎我要访问的是百度搜索这个程序。那最终呢，再将这个搜索页面哎再返回给浏览器，浏览器呢再将其渲染展示出来。

好嘞，那这就是我们在进行网络请求、网络交互的时候所涉及到的一些核心的网络知识。那最后呢，我们就一起来做一个总结。那么这一小节，我们主要介绍了三块网络知识，分别是：IP、域名和端口。那我们一块一块来梳理一下，首先大家得知道IP地址的作用是什么？哎它的作用就是用来定位我们网络上的设备的。那IP地址呢，我们常见的就是IPv4，是由四段数字组成的。那这四段数字呢每一段它的取值都在0到255之间。而这里呢大家需要清楚一个比较特殊的IP地址：127.0.0.1。那这个地址呢代表的是我们的本机地址，我们也叫本地回环地址。然后紧接着在这一块再来说一下第二个概念，就是域名。那域名的作用是什么？哎我们提到域名呢是为了减轻我们IP地址的记忆成本的。哎为了降低IP地址的书写以及记忆的成本，所以呢才出现了这个域名。那这个域名呢大家也需要记住一个比较特殊的，你看，127.0.0.1这个代表的是我们本机的IP地址，那其实我们本机呢也有一个域名啊叫localhost。啊localhost，local本地，host主机，哎就是本地的这个主机，那本机的域名呢就是localhost。所以如果我们要访问我们自己电脑上所部署的一个应用程序的话，那可以通过127.0.0.1这个IP地址来访问，或者通过localhost来访问都是可以的。

啊那么最后再说一下端口的作用，我们刚才提到，IP地址是用来定位计算机设备的，那端口是用来定位这个设备当中的应用程序的。那每一个程序启动之后，它都会占用一个端口号。而端口号的取值范围呢在0到65535之间。啊端口号在我们一台设备当中是不会重复的。好的，那这是我们这小节所讲解的网络相关的一些基础知识。

黑马程序员，为千万学生少走弯路，我们一直在努力。

那通过上一小节的学习呢，我们已经知道了在网络请求当中啊，我们是根据IP地址或者是域名来定位网络当中的设备的，然后再通过端口号来定位设备上的应用程序。那现在客户端如果知道了服务器端的IP地址和端口号之后啊，那么它就可以与服务器端建立连接了，连接上了之后，两者之间就可以通信了。那客户端与服务器端之间进行通信，就会涉及到数据的传输。

那么接下来这一小节呢，我们再来介绍一下数据传输的规则。那这就涉及到我们的网络模型和网络协议了。那么接下来呢，我们先来介绍一下网络模型，然后再来介绍一下网络协议。那我们知道在互联网上啊它连接了数以亿计的设备，包括我们个人使用的这个电脑啊、手机啊、平板、智能手表、车机等各种各样的终端。

那么网络呢也是四通八达，数据传输呢就类似于我们城市道路上的这些车流，如果说没有规则，那么将会混乱不堪。啊因此呢，国际ISO组织呢，它就提供了一套统一的网络通信的模型和标准。哎也就是说国际ISO组织呢它就定义了一套网络通信的标准。那么这块包括两套标准哈，一套呢是OSI七层网络模型，另外一套呢是TCP/IP四层网络模型。

哎大家如果是科班出身的话，大家应该听到过这个七层网络模型啊。那OSI七层网络模型分别是，从上到下哈依次是应用层、表示层、会话层、传输层、网络层、数据链路层和物理层。而这个TCP/IP四层网络模型呢大家可以简单理解为就是OSI七层网络模型的一个简化的版本。啊那么在四层网络模型当中啊，它将前面这三层应用层、表示层、会话层合并到一层了，啊就是这个应用层。然后紧接着下面这一块第二层传输层、第三层网络层，再往后走最后的这两层像数据链路层以及物理层呢，其实它合并到了网络接口层当中了。

哎大家会看到啊这个网络协议当中呢它是分层的，那为什么要分层呢？哎这其实就像工厂里面的流水线，哈每道工序呢它都有自己的职责。啊比如一个汽车生产的流水线，那每道工序它的职责是不是都不一样啊。哎比如我第一道工序我这块处理完了，那接下来我是不是要交给第二道工序？第二道工序处理完了，哎我再将它交给第三道工序，第三道工序处理完了交给第四道工序，第四道工序处理完了再交给第五道工序。每一道工序都有自己的职责，各司其职。

那这样呢即使出现了问题，后续我也能快速地定位问题。那这块网络模型分层呢也是类似的道理。那么在这两种网络模型当中啊，那我们以后实际应用当中啊基本上都是四层网络模型。好，所以接下来呢我们就来研究一下那么这四层网络模型当中它每一层的职责是什么？那么首先先来说一下第一层啊，第一层叫应用层。啊那这个应用层呢其实就是直接和用户交互、和用户打交道的这一层。

啊比如我们打开浏览器，我们访问百度。那我们访问百度之后，如果我要在百度当中搜索相关的一些问题，我直接在输入框当中输入问题，然后直接点击这里的“百度一下”。那当我点击这一块按钮之后啊，那么此时呢，浏览器它就会采用特定的应用层的协议，啊包括像HTTP啊、FTP啊、SMTP等协议，会根据协议的格式将我们界面交互产生的这些数据呢会进行封装。

啊会按照指定协议的格式进行封装。那封装完毕之后呢，应用层它就会去调用传输层的API，哎将封装好的数据正式的移交给传输层。哎所以大家会看到，这个应用层它的核心的作用呢就是将用户所输入的这个业务数据呢转化为网络可以传输的标准格式，而不用去关心它的底层数据是怎么传输的，啊只需要将数据封装为对应的数据块，然后交给下一层就可以了。

那这个数据交给传输层之后啊，那传输层呢就负责要将这个应用程序封装好的数据包要准确地送达给另外一台设备上对应的应用程序。啊因为你看，当我们在这一块点击百度一下，那其实是不是就要请求百度的服务器啊？哎要来搜索对应的内容。那么这个时候就要访问我们百度搜索的服务了。那这个时候就需要将这个数据包要发送给百度搜索服务的这个应用程序。那我怎么定位到百度服务器当中的搜索服务这个应用程序呢？ 哎我们上一小节讲到啊， 那么IP地址它是用来定位设备的，谁是用来定位这个设备上的应用程序的？ 端口。 哎所以这里呢在传输层当中啊，我们需要指定我们要访问的是目标服务器的哪一个端口，啊会在数据包的基础上来加入我们要访问的是目标服务器的哪个端口。那这样呢传输层通过端口呢就可以标识不同应用的进程，那服务器端收到这个数据包之后， 它就会通过这个数据包当中所指定的这个端口的信息啊，将这个数据包交给对应的应用程序。 那么在这层当中啊涉及到两类核心协议，一类是UDP，一类是TCP。 啊简单介绍一下哈。 呃那么这个UDP呢它的特点就是速度快、开销小， 但是它不保证数据传输的可靠性。 啊主要是用在一些实时性要求比较高的场景，比如我们电脑屏幕的共享，哎我们电脑共屏或者是视频通话、语音通话这样的场景当中。 而另外一种呢叫做TCP，那TCP呢它主要是负责建立可靠稳定的连接，啊它是有确认机制的数据传输是安全的。 啊主要适用于一些呃不能丢失数据的场景，比如像银行卡转账啊、微信转账啊这类的场景。 好，那么这是传输层。那么这一层封装完毕之后啊，数据包就会携带数据传输的端口的相关信息， 然后再将这个数据包交给网络层，所以这个传输层呢它主要负责啊将数据准确的送达到对应的应用程序当中。 然后紧接着再来说一下网络层，你看， 在我们的传输层当中啊我们只是指定了端口，指定了那么这个数据包将来是要交给哪个应用程序来处理的，并没有指定设备的信息。 那么这个数据包最终要发送给哪个设备啊？ 哎那么这个时候在网络层当中呢，我们就会用到这个IP协议了。我们知道IP地址是不是就可以定位到具体的网络设备了？ 哎所以呢网络层呢它会将传递下来的这个数据包，在这个数据包的基础之上再继续进行封装，再加入IP地址的相关信息。那包括我们本机的IP以及包括我们目标服务器的IP地址。 哎所以这个网络层它的作用呢就是基于IP地址将数据包路由到对应的设备上。 好，那么这块数据包封装好了之后，最终呢会将数据包交给最后一层啊，就是这个网络接口层。那网络接口层呢其实里面就包含了这个数据链路层和物理层，那它主要负责在我们物理网络当中传输上面所封装好的这些数据包，处理与硬件设备的交互。 那最终我们会看到在网络模型当中的这四层当中啊，那每一层都有自己的职责，大家各司其职，最终协作起来完成了一次网络请求。 而对于这四层来说啊，其实我们作为开发人员我们以后只需要关心一层就可以了。 哪一层呢？ 啊就是应用层。这些网络底层那些东西呢我们就不用去关心了，我们只需要关注应用层就可以了。因为应用层就是负责我们与应用程序交互的这层。 而应用层的协议呢大家看到这一块像HTTP啊、FTP啊、SMTP啊，那这些都是属于应用层的协议。而对于这些个应用层的协议呢大家只需要关注其中的一个协议就可以了，谁呀？ HTTP协议。 因为我们在进行项目开发的时候那基本上都是HTTP协议。 而且呢我们等会儿在与AI大模型交互的时候，那么用的也是HTTP协议。 啊所以这个网络模型我们大概了解之后啊，那接下来呢我们就来介绍一下这个HTTP协议，来看一下什么是HTTP协议。 那HTTP呢英文全称啊叫HyperText Transfer Protocol，啊翻译过来呢叫超文本传输协议。 那这个协议它有什么用呢？ 哎那这个协议呢它规定了我们客户端与服务器端进行数据传输的规则。比如这是我们的客户端，这是我们的服务器端，那么客户端发送请求给服务器，服务器接收到请求之后对请求进行处理，最终要在给客户端响应数据。 那么 客户端发送请求到服务器端这个数据到底长什么样子的？那么第一行表示什么意思？第二行表示什么意思？第三行什么意思？那这些呢都是在HTTP协议当中都是规定好的。 那么客户端与服务器端交互的时候他们都遵循这一种规则，遵循这同一种协议就可以了。那如果说客户端和服务器端在进行交互的时候他们都遵循了HTTP协议，也就说都遵循了这同一套规则的话， 那你看， 客户端发送过来的数据包那服务器端按照相同的规则来解析是不是就能够拿到其中的请求数据了？服务器端是不是就能读懂了？ 那么同样的道理，那服务器端接收到了请求之后最终要对请求进行处理，处理完了是不是要给客户端响应数据啊？ 那服务器端组装好数据之后将数据响应给客户端，那么客户端要想能够成功的解析出这块的数据，那么服务器端和客户端是不是也需要遵循同一种规则？ 哎我按照这个规则给你封装的数据，数据交给你，你按照相同的规则来解析就可以了。 只有客户端和服务器端在进行请求和响应的时候都遵循了同一套规则，那么他们才能读懂来自于对方的数据。哎比如服务器端才能读懂来自于客户端的请求数据，那客户端才能够读懂或者说才能够解析来自于服务器端的响应结果。 那这就是HTTP协议它的作用，哎就是规定了客户端与服务器之间进行数据传输的规则，啊它就是用来定标准、定规则的。 好，那大概知道了什么是HTTP协议之后啊，那接下来呢我们这一块再来介绍一下HTTP协议的特点。 那么HTTP协议呢有这么三个特点哈。第一个特点是基于文本的协议，你看它叫什么？它叫超文本传输协议，啊它其实本质呢也是一个文本。 好，在请求和响应的时候协议部分的内容啊都是文本格式的，啊文本呢指的就是文本字符串格式的。而对于这个HTTP协议来说呢它的传输层协议啊其实用的是TCP协议，就是它的底层其实是TCP协议传输的，更加稳定、更加安全，啊所以稳定性是比较强的。这是它的第一个特点啊是基于文本的协议，协议的内容都是文本形式的。 然后第二个特点呢就是它是基于请求响应模型的。 那所谓请求响应模型指的就是 客户端发送一次请求给服务器，服务器哎给客户端一个响应，客户端再发送一个请求给服务器，服务器再给你一个响应。如果客户端不请求，服务器端是不会有响应的。客户端发送一个请求服务器端就对应一个响应，不会对应多个响应，不会发送一个请求服务器端给它响应多次，不会。 所以这个呢就叫基于请求响应模式。一次请求对应一次响应，而且这个请求必须得有客户端主动发起，服务器端才会有响应。 啊这是第二个特点。 那么第三个特点呢就是它是无状态的。 那所谓无状态呢指的就是 在我们客户端与服务器端进行请求响应的过程当中啊服务器端是不记录任何信息的，啊每一次请求和响应都是独立的，在后面的请求当中它并不会携带前面请求的数据，啊每一次请求之间相互独立，啊这个呢我们就称之为是无状态的。 好的，那这是我们这小节所讲解的网络模型与HTTP协议。 那最后呢我们就简单做一个小结，主要明确两个问题。第一个问题： TCP/IP四层网络模型是哪四层？ 哎那么第一层最上面一层是应用层，然后紧接着是传输层，接下来网络层和网络接口层。 那对于这四层网络模型来说啊每一层它的职责都是不一样的。好，那对于这个四层网络模型啊大家简单了解即可，啊不需要去死记硬背。 然后紧接着再来明确一下第二点就是HTTP协议。 那HTTP协议呢它是属于应用层的协议，啊是与用户交互的。 那么HTTP协议呢大家明确两个点。第一个，什么是HTTP协议？第二个呢，它有什么特点？ 那么关于HTTP协议我们刚才讲到它叫超文本传输协议，那这个协议呢它就规定了客户端与服务器端数据传输的规则，它是定规则的。 然后呢对于HTTP协议的特点呢刚才提到三个特点啊。第一个，它是基于文本的，啊协议部分都是文本格式的。然后第二个呢就是基于请求响应模型的。一次请求，一次响应，你没有请求它就不会有响应。 然后第三个特点它是无状态的。那所谓无状态指的就是 那么每一次请求和响应都是相互独立的，服务器端是不会记录我们每一次请求的数据的，在后一次的请求当中它也不会携带前一次请求的数据。 那这个呢我们就称之为无状态。 好的，那这是我们这小节所讲到的网络模型与HTTP协议。 那么下一小节呢我们就来详细地剖析一下，那么HTTP协议它定义的客户端与服务器端数据传输的规则，那这个规则到底是什么样子的？来详细地剖析一下HTTP协议当中请求数据的规则以及响应数据的规则。 那我们下一个视频再见。

黑马程序员为千万学生少走弯路，我们一直在努力。

好的各位同学，那上一小节呢我们介绍了一下网络模型与网络协议，重点介绍了一下这个 HTTP 协议。我们说 HTTP 协议呢它是超文本传输协议，规定了客户端与服务端数据传输的规则。那么接下来这一小节呢我们就来详细地剖析一下，那客户端与服务端在进行交互的时候，那么具体数据的规则到底是什么样子的。那么关于这一块的数据规则呢，我们将分为两块来看，那一块呢就是请求数据的规则，另外一块呢就是响应数据的规则。那首先我们先来研究一下请求数据的规则。那么请求数据的格式呢指的就是我们客户端给服务端在请求的时候，给服务端所发送的数据它的格式长什么样。那我们刚才提到，这个 HTTP 协议呢它的协议格式都是文本形式的，所以这块呢我已经截图下来了。那截图当中的这一部分呢，就是 HTTP 协议当中请求数据的数据格式。那么对这部分数据来说，其实它分为三个部分。首先第一个部分呢就是请求数据的第一行，那这个呢我们称之为请求行。那请求行当中的信息呢主要包括，你看这块第一个是 POST，你看啊这块是拿空格分开的啊，那这个是 POST，那这个代表的是请求方式，然后紧接着后面这一块有个叫做 /a/course，那这个代表的是请求的资源是什么，请求的是哪个资源。再往后走，HTTP/1.1 这个代表的是协议及协议的版本。那这里呢我们重点说一下这个请求方式啊。那么请求方式呢常见的有两种，一种呢是 GET，另外一种是 POST。那我们来看一下这个 GET 请求啊。那如果说请求方式为 GET，那么它的请求参数，就是我们客户端要请求服务端嘛，那你请求服务端的时候可能会携带一些参数，那如果是 GET 请求，这个请求参数它是在请求行当中携带的，哎，也就是在这个位置携带的。那么 GET 请求的话，这个请求参数在请求行携带在哪个位置呢？哎，它会在我们的这个请求资源路径后面携带。比如大家来看，如果是 GET 请求，我们需要给服务端携带数据的话，那么这个时候我们就可以在这个请求路径啊，那这个路径就是一个请求路径，后面这块大家会看到有一个叫问号。哎，这个路径后面如果一旦携带了一个问号，问号之后的这一部分，那其实就是我们携带的一些参数的信息了。那问号之后的这一部分携带参数的时候呢，它的表现形式就是 key 等于 value。你看这一块，你看问号之后是 name=python，然后紧接着这块有一个与，这个我们也叫 and 啊，就是 and status=1。那么这就相当于传递了两个参数，一个参数参数名呢是 name，参数值是 python，另外一个参数是 status，参数值是一。哎，这就代表我们在请求服务端的时候，给服务端传递了两个参数，参数名分别是 name 和 status。好，那么这是 GET 请求传递请求参数的方式，它会直接在我们的请求资源路径后面通过问号的形式把这个参数携带到服务端。但是这一块需要给大家提示一点哈，那么对于 GET 请求来说呢，它的请求参数大小在浏览器当中是有限制的，你是不能在 GET 请求当中传递大批量的数据的。比如我将一篇小说几兆的数据直接传递过去，是不可以的。GET 请求它的请求参数的大小是有限制的。那么紧接着再来看一下 POST 请求。那 POST 请求呢，它的请求参数一般来说并不会在我们的请求资源路径后面携带，而是在请求体当中携带的。那这个请求体呢，其实指的就是我们请求数据的最后一部分。大家来看，这个我们等会儿就会讲到，这个就是请求体。那 POST 请求它的请求参数是在请求体当中传递的，那请求体当中所传递的这个请求参数啊，它的大小是没有限制的。哎，所以如果说我们在请求服务端的时候，我们所需要携带的数据量比较大，假设我要传递几兆甚至于几个 G 的数据，那这个时候你得采用 POST 请求，不能选择 GET 请求。比如我们在一些项目开发的时候可能会涉及到文件上传这样的操作，我要上传几兆的图片，我要上传几个 G 的视频，那这个时候呢你就得采用 POST 请求了，不能采用 GET 请求。好，那这是我们这一块介绍的两种常见的请求方式，GET 和 POST。那这个呢我们等会儿还会给大家去演示的。好，那么紧接着呢我们再来看一下请求数据格式的第二个部分啊。那第二个部分呢就是从第二行开始，以 key: value 形式出现的这一部分。你看，这个 Accept 就相当于 key 啊，那冒号之后的这块是不是就相当于 value 啊，大家会发现我绿色框框起来的这一部分你会发现是不是都是 key: value 的这种形式啊？以键值对的形式出现的。那么这一部分呢我们称之为请求头。哎请求头，它的格式呢就是 key: value，那这个 key 也就是前面的这一部分哈，那这个呢我们叫请求头的名字，后面呢就是请求头的值。那如果说我们是在浏览器当中直接来访问服务端哈，也就是说我们的客户端如果是浏览器的话，那这一部分呢我们在请求服务端的时候，它会浏览器会自动的在我们的请求当中来增加这部分请求头。那不同的请求头呢，它所表示的含义也不一样，我们简单看一下哈，那这一块不需要大家去记哈。你比如这块有个 Accept 代表那我能够接收哪些格式的数据，我可以接收 application-json，json 格式的数据，我可以接收文本格式的数据，其它格式也能接受。以及我能够接受的语言是什么，Accept-Language，zh-CN 这个就代表的是简体中文。好，那么再往下走呢还有这一块的叫做 Content-Length、Content-Type，那这个则代表的是我们要给服务端发送的数据什么格式啊？是 json 格式的，那数据多长呀？是 139 个字符。好，那么其它的这些呢我就不再一个一个去看了。大家需要知道的就是从第二行开始，以 key: value 形式出现的这一块文本呢，那这个我们称之为请求头。那么紧接着最后一块，也就是大家最下面这一块所看到这块，那这个呢我们叫请求体。那注意哦，在 HTTP 协议当中啊这个请求头和请求体之间中间这一块是有个空行的，以示区分啊。空行之后的这一部分都是请求体的内容，所以中间空白的这部分其实它是一个空行，空行之后的就是请求体。而这个请求体呢其实就是请求参数的部分。而对于请求体来说呢，GET 请求是没有请求体的，但是 POST 请求是可以有请求体的。注意哦，我标的是可以有请求体，也就意味着它也可以没有请求体。好，那这就是我们刚才所说的，如果是 POST 请求，它的请求参数是在请求体当中携带到服务端的，也就是在我们的请求数据当中的第三个部分这个位置携带到服务端的。好的，那这是我们这块所讲解的 HTTP 协议当中请求数据的格式。那么为了让大家对这一块的格式有一个深刻的理解啊，那么接下来呢我就直接打开浏览器，我通过浏览器的抓包工具给大家抓取一下网络请求，然后来看一下 HTTP 协议它的格式到底是什么样子的，是不是我们分析的这样。来，我打开我的浏览器。那么打开浏览器之后啊，我在这块找到百度啊，找到百度之后，那我在这块我随便访问一个，我就访问这个，访问一个新闻，走。好，那么访问新闻之后，接下来我打开 F12 啊，这块我直接按一下我键盘上的 F12 就是打开浏览器的开发者工具。好，哎打开 F12，你看我按了一下 F12，按了一下 F12 在浏览器当中是不是出现了这么一个界面？那这个呢我们称之为浏览器的开发者工具。这个是前端开发人员在开发前端项目的时候啊的一个调试工具啊，那这个呢我们使用一下。那这个时候我可以定位到网络 Network 这一项哈，我这块这个版本是英文的浏览器哈，那这块定位到 Network，定位到 Network 之后，这块点击一下这里的 All，就是我要开始去抓取网络请求了。接下来大家看好了，我在这块直接点击这里的刷新。好，那么刷新之后大家来看，那这块呢发起了很多个网络请求，我们来找一下第一个网络请求，就这个请求，对吧？点击一下。点击之后找到这里的这个 Header 啊，我带着大家看一眼就行了，找到这个 Header。那么找到这个 Header 之后，大家再来看。这里呢，大家可以看一下这块的这几项信息哈。首先第一项呢，大家可以来看一下叫做 Request URL，就是你本次请求，请求的服务端 URL 地址是哪个呀？啊，请求的地址是这个，叫做 https://www.baidu.com/s。哎，大家此时会发现啊，这个 URL 后面加了一个什么？问号！来，我问大家这个问号大家见过没有？那这个问号之后携带的是不是就是请求参数了？因为大家来看，这个请求方式是什么方式？看这，Request Method 代表请求方式，请求方式是不是 GET？那我们提到，GET 方式呢它是没有请求体的，如果有请求参数，这个请求参数是不是会在我们的请求路径后面携带到服务端啊？这个 URL 其实代表的就是请求路径。哎，它会在请求路径当中将请求参数携带到服务端。啊，那到底携带了哪些参数呢？呃，那后面这块有个叫做 wd 哈，那 wd 后面这块有这么一长串，这是什么呢？呃，其实这里呢，它是对我们的这个中文进行了编码。啊，是对中文进行了一定的编码的。那这里呢，大家直接看浏览器地址栏就行了。你看这，它的这个请求路径不就在浏览器地址栏吗？你看这，问号之后 wd，这是不是就以第一个参数啊？哎，往服务端携带的第一个参数，这是参数名。京东巴黎仓库遭窃，对吧？哎，那这是不是就是它的参数值啊？然后 and 符，你看这块有个这个 and 符，那 and 这个符号后面这块有个 sa，这是不是就第二个参数了？然后，再往后走，这块又有个 and 符，你看，rsv_dl，这是不是就第三个参数了？你看，from 是不是就第四个参数啊？这块还有一个 cl，这就是第五个参数，第六个参数。大家会发现，在 GET 请求当中，哎，如果我们要往服务端传递多个参数，多个参数之间是拿什么分割？哎，拿这个 and 符，也就是拿这个，我们也叫与哈，拿这个 and 符来分割的。啊，那形式呢，就是 key=value & key=value。好，那么紧接着我们继续来看。那么这块你看，这个请求信息有很多啊，那这里大家来看，这块有一块呢叫做 Request Headers，另外一块叫做 Response Headers。那么 Request 代表的是请求的信息。那在这里呢，大家就可以看到，我们此次在访问百度这个服务器的时候，它在请求百度服务器的时候所携带的请求的数据信息。我们可以点击一下这个按钮，不，将它勾选一下，勾选之后我们就能够看到最原始的 HTTP 协议的数据啊。大家来看，我点击一下之后，大家会发现这块展示是不是有变化？哎，大家看到的就是最原始的 HTTP 协议的数据。大家来看，这个就是请求数据的第一行，由于它这块内容比较多，换行了啊，这就是请求数据的第一行，叫请求行。那么请求行当中第一个是不是就请求方式啊？GET。这是不是就请求资源的那个路径啊？哎，当然由于 GET 请求这个里面它携带了一系列的请求参数，所以这个路径你看问号之后拼接的参数比较多，就比较长。再往后走，这是不是就请求的协议啊？然后紧接着，从第二行开始以 key: value 形式出现的这些文本，这个我们叫什么？你看，下面这一堆都是 key: value。那这一部分呢我们叫做请求头。啊，从第二行开始以 key: value 形式出现的这个文本，那都是请求头。你看，这些请求头啊，如果你是基于浏览器给服务器发送的请求，这些请求头都是浏览器自动加的。啊，自动加的这些个请求头。好，那这是请求的信息。那由于是 GET 请求啊，它是没有请求体的。那么往上走，这里大家还会见到有一个叫做 Response Header，那这个呢是响应的数据。哎，那这个响应的数据呢，我们等会儿再来看。好，这是我在这块给大家抓了一个这个请求啊，是 GET 方式的请求。紧接着我再给大家看一个请求，是 POST 方式的。呃，那么这个时候呢，我在这块去访问一个网站，那这个项目呢，其实是我们黑马早期的一个教学项目哈，那我们在这块就拿这个项目来演示一下，大家来看。呃，那这里呢，我们在这块输入完用户名和密码之后，接下来在这块再输入验证码是不是就可以登录了？哎，输入一下验证码哈，6+4=10。好，那么接下来我在这块按一下 F12。那么按下 F12 之后，接下来定位到网络 Network 这一项，这块选择 All 哈，抓取所有的网络请求。接下来我们就来抓取一下这次登录请求。来，我这块直接点击登录。好，那么点击登录之后啊，大家会看到在我们的 Network 网络这栏它发起了很多请求，我们找到第一个，login 就是登录请求，点击一下。呃，那么点击之后，大家来看，Request Method 请求方式，什么请求？哎，这块是不是就 POST 请求了？哎，这是请求的这个 URL 地址。然后紧接着我们重点看的是什么呢？看 Request 请求数据这块。因为响应数据这块的格式还没讲呢哈，先看请求数据这块，可以点击一下上面的 raw。好，那么点击这个 raw 之后啊，那么我们就看到了最原始的 HTTP 协议的格式了。因为在浏览器当中啊，它默认是对原始的 HTTP 协议的格式是进行了格式化的，啊，所以在这块你要点击这个按钮才能看到最原始的，你看，请求方式是什么？POST。这是不是那个请求的资源路径啊？/api/login，哎，这是不是请求的协议啊？HTTP 协议。然后从第二行开始以 key: value 形式出现的这块文本，那都是请求头。那有没有请求体呢？POST 请求不应该有请求体吗？哎，那这块大家需要注意哈，我刚才提到现在的这些浏览器啊，它已经对 HTTP 协议最原始的那些格式已经进行了格式化了。呃，如果是 POST 请求，你要去看请求体的数据，那么在 Chrome 这样的浏览器当中啊，呃，其实它是放在这个位置了，叫做 Payload。啊，点击一下。那这块再点击一下这个按钮，view source，看原始的代码。好，这就是请求体部分的内容。哎，也就是请求数据的第三个部分，请求体的内容。哎，那么在这块传递的就是这么一串数据，你看。那你看它的格式就是 key: value，key: value，啊，key: value，是不是这种形式的？好，那这个就是请求体的数据。好，在浏览器当中呢，它是对这块的数据进行了格式化的，大家在这个 Payload 这块来看，或者中文的浏览器啊，这块叫负载。好的。那这是我们这块所讲到的 HTTP 协议当中请求数据的格式。啊，一共三个部分，分别是请求行、请求头和请求体。好，那么请求数据的格式清楚之后，接下来呢，再来介绍一下响应数据的格式。那么响应数据的格式呢，也是分为三个部分。首先，响应数据的第一行，那叫响应行。呃，那么响应行当中呢包括两块信息，一块呢是协议的信息，你看，HTTP/1.1，这是不是就协议的信息？然后 200 呢，那这个我们叫做状态码，啊，也叫响应状态码。那这个状态码呢，其实就是告诉客户端啊，你这次请求到底成功了还是失败了？那如果是失败了，具体什么原因失败的，就是通过状态码体现的。那这里呢，大家需要掌握这么四个状态码哈。第一个，200。啊，是我们程序员的幸运数字。啊，一旦见到 200，那就说明请求成功了。然后紧接着如果是 4 开头的状态码和 5 开头的状态码，那都代表的是失败了。啊，那这里呢我们需要掌握这么三个常见的失败的状态码哈，分别是 400，啊，404 和 500。那 400 呢代表的是我们请求服务端的时候，我们请求的参数传递错了，啊，就是说我们客户端传递的参数有问题。那 404 呢代表的是我们所请求的这个资源或者请求的数据不存在，啊，也就是我们输入的这个 URL 地址呢，呃，是有问题的，是有错误的，或者是网站的这个资源被删了，你访问不了了。啊，简单说就是请求资源不存在，就是 404。那 500 呢也是错误，但是 4 开头的错误呢，它都是客户端错误了。啊，客户端参数错了，客户端请求的资源不存在，那 5 开头的都是服务端错了。那如果是 500，就是服务端发生了不可预期的错误了，服务端异常了。好，那这是这块大家目前需要记住的这四个状态码，啊，也是以后我们遇到的最多的状态码。然后紧接着请求行之后，从第二行开始，以 key: value 形式出现的这个文本，那这个呢也叫头，但是它叫响应头。啊，响应头的格式呢，也是 key: value。比如你看这个 Server，这就是一个响应头，啊，代表的是你请求的服务器是哪个服务器啊？请求的是 Nginx 服务器。那 Date 呢代表的是我给你响应数据的时间，什么时间响应的。那 Content-Type 则代表的是我给你响应数据的格式是什么样子的。那这是第二个部分，响应头。好，那么紧接着在响应头下面这块依然有一个空行，在空行之后，那么这块呢就是响应体的内容。那所谓响应体呢指的就是服务端给客户端响应的数据，它就会放在响应体当中返回给客户端。好，那么这就是 HTTP 协议当中响应数据的格式。那么介绍完了这个格式之后啊，那接下来呢，我们再次回到浏览器当中啊，我们就来看一下，那么刚才我们在进行登录操作的时候，那服务端响应回来的数据长什么样？那这个时候我们可以找到这里的 Headers 啊，找到这块有个叫 Response Headers，就是响应回来的数据。我们点击一下这里的这个 raw，勾选一下。大家来看。响应数据的第一行是不是叫响应行？哎，那么首先这块呢有一个 HTTP 是不是协议啊？那这个 200 还记得这叫什么吗？状态码。一旦见到 200 状态码，我说了这是程序员的幸运数字啊，一旦见到 200 状态码，那就说明成功了。然后紧接着从第二行开始以 key: value 形式出现的这堆文本，那都是响应头的信息。啊，那这些响应头的信息大家只需要知道哈，key: value 出现的这些都是响应头，那具体响应头的含义呢，大家不用去记。紧接着再往下走，有没有响应体呢？没有。但是注意了，在浏览器当中啊它是对我们响应的数据也进行了格式化，它把响应体的内容单独的给我们放在哪了呢？ 放在这个位置了，叫 Response 这一栏。哎，所以这个时候你要想看有没有响应体，响应体的内容是什么，直接点击这里的 Response。点击 Response 之后大家会看到，这块是不是有响应体的内容啊？ 而大家会发现这个响应体的内容呢，呃也是 key冒号value形式的，你看。 key value，啊 key:value，啊 key:value。 这就是服务端响应回来的数据，就是响应体的内容。 但这个我们后面会介绍到哈。那么这种数据格式呢，我们叫 JSON 格式。 哎，其实大家会发现跟我们这个 Python 当中的字典类型是比较类似的，是不是都是存放的键值对啊？ 好嘞，那么这就是响应回来的响应体的内容，单独放在了这个 Response 这个里面。 好的，那这是我们这小节所讲解的关于 HTTP 协议当中请求数据的格式以及响应数据的格式。 那么讲解完毕之后，最后呢我们就回到 PPT，简单来做一个小结，主要就明确一个问题啊，就是 HTTP 协议呢我们说它规定了客户端与服务端数据传输的规则。 那具体是什么样的规则，规则是什么样子的呢？ 哎，其实它里面就定义了请求数据的格式和响应数据的格式。那我们说请求数据分为几个部分？ 哎，其实和响应数据一样都分为三个部分，分别是请求行、请求头和请求体。 那请求行当中呢又包括请求方式啊、资源访问的路径啊，对吧？那请求头呢就是从第二行开始，以 key冒号value形式出现的键值对，那都是请求头。 那请求体呢，和请求头之间是有一个空行的。 那么 GET 请求它是没有请求体的，但是 POST 请求是可以有请求体的。那对于 GET 请求来说，它的请求参数是在资源的路径，也就是在请求路径后面，通过问号的形式携带到服务端，问号之后跟上 key=value&key=value，将请求参数携带到服务端。 而对于 POST 请求来说呢， 哎，它的请求数据一般老说都是在请求体当中携带到服务端的。 而且请求体当中携带到服务端的这个数据量呢，它大小是没有限制的。 好，那么这是请求数据的格式。 那么响应数据的格式呢，也分为三个部分：响应行，也就是响应数据的第一行；然后从第二行开始，以 key冒号value形式出现的这个文本呢，叫做响应头；那第三个部分就是响应体。 而响应头和响应体之间也有一个空行。 而关于响应数据这块涉及到的状态码呢，那这块大家目前需要关注四个状态码，分别是 200、400、404 和 500。 那 200 呢就代表成功了； 那 400 呢代表的是请求参数错误，啊就是我们传递给服务端的请求参数是有问题的； 那 404 呢则代表的是我们请求的这个资源，请求的数据它是不存在的； 那 500 呢代表的就是服务端错误。 好嘞，那这是我们这一小节所讲解的 HTTP 协议当中请求数据的格式和响应数据的格式。那最后再次强调啊，大家只需要知道就是请求数据分为哪几个部分、响应数据分为哪几个部分就可以了。对于其中所涉及到的这些个请求头、响应头大家不用去记，啊因为在项目开发的过程当中啊，那这些东西呢我们也不用关心。 啊那如果说大家想详细的了解一下，那么各个响应头、请求头具体的作用是什么，大家可以找到我们这一章节的资料，在资料当中我已经给大家准备了一份文档，大家有兴趣的话可以详细地去查看这份在线文档。 好的，那这是我们这小节所讲解的 HTTP 协议当中请求数据和响应数据的格式。 那关于 HTTP 协议以及其中涉及到的请求以及响应的数据格式我们了解之后，那下一小节呢我们就带着大家基于 HTTP 协议来调用我们前面所部署的 DeepSeek 大模型。 黑马程序员为千万学生少走弯路，我们一直在努力。