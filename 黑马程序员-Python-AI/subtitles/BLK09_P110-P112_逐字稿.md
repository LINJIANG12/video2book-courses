那么在上一小节呢，我们介绍了一下在会话管理当中啊，我们新建会话以及保存会话这一块的思路。那我们提到，我们在进行新建会话、保存会话的时候，最终啊，会话的相关信息，我们是需要保存在磁盘文件当中的。那这个时候呢，就会涉及到磁盘文件的操作。那么接下来这一小节呢，我们就来讲解一下，那么基于Python语言，我们如何来操作磁盘当中的文件。那么大家可以思考一下，就是在我们的日常学习以及日常生活当中啊，现在我想去操作我电脑上磁盘当中的这个文件，我要对这个文件进行读或者是写，我该怎么操作呢？大家想，无论是你要对这个文件是进行读还是进行写，那首先你要第一步要干嘛？第一步你得双击打开这个文件。对吧？打开了这个文件之后，接下来第二步呢，你再去对这个文件当中的内容进行读或者是写。哎，比如我在这一块，我要往文件当中写内容哈，我在这一块又输出了一串横线。好，写完之后，接下来第三步呢？哎，第三步你是不是需要将这个文件关掉啊？所以呢，我们无论对文件是进行读还是写，那基本上呢，都是分为三步操作，分别是打开文件，对文件进行读或者是写，最后一步，关闭文件。那么在我们的程序操作当中呢，也是类似的。那么其实在我们的Python程序当中啊，也是一样的操作。那我们来看一下，我们要对文件进行读或者是写，去起代码该怎么实现？那么首先我们先来看一下读取文件啊。那读取文件呢，就是三步操作：第一步，打开文件；第二步，读取文件当中的内容；第三步，关闭文件。那这是读文件，其实写文件呢，也是类似的：打开文件，写入文件，关闭文件。好，那么接下来呢，我们就一步一步来看啊。首先先来看一下打开文件。那么在打开文件的时候啊，这里我们可以去调用Python当中给我们提供的一个内置的函数，啊那这个函数的名字呢就叫open。你看open翻译过来是不是就是打开的意思啊。而无论是我们将来对这个文件是进行读还是写，你看，open这个函数的调用方式是一致的。那么里面涉及到常见的这么三个参数，第一个参数代表的是我们要读取或者是我们要写哪个文件，啊指的是文件的名字。然后紧接着第二个参数呢，那这个代表的是我们的操作模式。啊那这个操作模式我们后面会详细讲解啊。在入门程序当中我先简单介绍一下。那这个r代表的就是读取，是read这个单词的简写，r。那这一块如果我们写入文件，那就是write，你看write这个单词的简写就是w，那就代表我要对这个文件进行写入操作。那如果说前面这一块指定的文件不存在，啊就没有这个文件，它会自动创建这个文件。好，那这是第二个参数，模式。那第三个参数呢，就是encoding。encoding呢，是编码。啊我们这一块指定编码为UTF-8。那么这一块说一下这个编码哈。大家知道，在我们的计算机当中啊，它只认识二进制的0和1。无论是在去读取数据的时候还是在去存数据的时候，最终在底层都是存储的是二进制当中的0和1。那么此时大家想，我们在这一块所存入的这些中文啊、英文啊以及这个字母，还有一些常见的符号，那这些数据最终在计算机的底层是怎么存的呢？哎我们刚才提到，底层存的就是0和1。那它又是怎么样将这一块的符号以及中文以及数字还有字母给我们转换成这个0和1的呢？哎，那其实和这一块的这个编码是密不可分的。所以呢，这个编码呢，它的作用就是将我们所提到的这些字符，包括像字母、数字、文字、符号转化为计算机底层能够存储和处理的数字代码的规则系统。啊也就是说啊，将我们这一块所提到的文字、数字、符号以及英文字母在这一块转换成二进制的数据。那这就是这个编码的作用。那常见的编码呢有很多啊，比如像第一个ASCII码。那这个ASCII码呢，它只能处理数字、字母以及常见的一些符号，它并不能处理中文以及一些特殊符号。所以呢，如果说我们要存中文，那你就不能选ASCII码。然后紧接着第二个呢，是可以存储中文的，就是这个GBK。哎，GBK是可以存储中文的。但是这个GBK呢它是按照中国的标准去设计的这个编码规范。那么对于韩文还有日文它其实呢并不兼容，并不能处理。那紧接着再往后走最后一种编码，也就是这个UTF-8这个编码标准。那这个呢，是全球统一的。既可以处理中文，也可以处理英文，也可以处理日文、韩文，都是可以的，并且它也可以兼容ASCII码。所以呢，在我们的项目开发当中啊，那我们如果没有一些特殊情况的话，那基本上现在选择的都是UTF-8这种编码，是目前全球最为通用、兼容性最好的编码方式。啊可以让全世界的文字呢都能够进行正确的存储和展示。哎，所以这里呢，我们就选择这个UTF-8这种编码方式。好，那这是这个open这个函数，是用来打开文件的。那么打开文件之后啊，我们可以获取到这个函数执行完之后的一个返回值。那这个返回值f呢，其实代表的就是我们所打开的这个文件。哎代表的就是我们打开的这个文件。然后紧接着呢，我们就可以调用这个文件当中的read这类的方法对文件进行读取操作。那如果你要写入文件，你就可以调用这个f，也就是这个文件对象当中的write方法来写入文件。好，那读或者是写操作完毕之后，最后一步大家记得一定要去关闭文件。那如果说我们在进行操作的过程当中啊，我们并没有调用close这个方法来关闭文件，那么这个时候如果说我们这段程序啊在项目的运行过程当中，这段程序还没有运行结束，还没有停止，那么此时，我们所打开的这个文件一直就会被这个Python程序占用，别的程序是无法再操作这个文件的。啊所以大家记得，我们一旦对文件进行操作完毕之后，一定要调用close这个方法，要将文件关闭掉。好，那这是我们这一块介绍了一下，那基于Python我们如何来读取文件以及如何来写入文件。那简单介绍之后，接下来呢我们就直接来打开PyCharm来演示一下。来，我打开PyCharm。打开PyCharm之后啊，那我这里呢在第三章这个目录下，我再来新建一个文件啊。呃那下面这一块呢都是我们AI智能伴侣这个项目的代码。那我创建的这个文件就按照02来排吧。啊入门程序的代码啊，我们演示的代码，我们都放上面，这一块就叫02。那这一块是文件操作入门。好，回车。好，那么接下来呢，在这个里面我们就来演示一下我们如何来读取文件，以及如何来写入文件。啊我们先来演示一下读文件。那我们刚才提到这里是不是分为三步走啊？哎，首先第一步，我们得打开文件。然后接下来第二步，要读文件，那第二步呢就是来读取文件的内容。那接下来第三步呢？读取完文件的内容之后，无论是大家读文件还是写文件，第三步一定要注意一定要去关闭文件。好，就分为这么三步操作。好，那这里呢我们要来读取一个文件啊，那这个文件呢我们找一下，啊就是我电脑这个桌面上的这个文件啊，“望庐山瀑布”，对吧？来，我这一块直接复制一下，复制一下为了便于管理哈，我这一块直接放到我的项目当中，放到这个位置。来，Ctrl+V直接粘贴进来就可以了。好，那么粘贴进来之后，这是不是我们刚才在这一块呃写入这个文件演示的时候加入的一串横线啊？啊我把它去掉哈，当然你去不去都行哈。好勒，那大家来看。那现在我要去读取文件，要读取的是不是就是resources这个目录下的“望庐山瀑布.txt”这个文件？哎，所以这里呢，首先第一步我们需要去打开文件，调用这么一个函数，叫做open。当然这个open这个函数当中啊，它里面所涉及到的参数比较多，但是这些参数其实很多都有默认值。好这里呢，我们必须要传递的就是这个file，你要读取哪个文件。然后第二个呢，就是你对这个文件操作的模式，你是读还是写？然后还有一个大家需要关注的就是这个编码。而大家会发现，这块中间是不是跳过了这么一个参数叫做buffering啊？那这个时候大家想，我要跳过这个参数，然后我要直接去设置encoding这个参数，那此时encoding这个参数你是不是就不能再使用位置参数传递了？因为位置参数是不是得一个一个挨个传递，按照顺序传递啊？哎那么这个时候你如果要传递这个编码，那你得通过关键字来传参就可以了。好，我们这一块去调用一下这个open这个函数。那么调用这个函数之后，那么这里AI给我提示了，你要去读取哪个文件啊？呃那我要去读取的就是我当前这个目录下，你看，这是Python文件，我要读取的是Python文件当前这个目录下的resources这个目录下的“望庐山瀑布.txt”。啊所以这一块呢我可以按照相对路径的写法哈。那么这一块我就可以来写：点杠，这一块来一个resources。你看开发工具是不是有提示啊？然后下面的哪个呢？来，我在这一块去复制一下这个文件名哈，就是“望庐山瀑布.txt”。

这就可以了。

而这里呢，我们前面也提到，这个点杠其实大家是可以省略的。

好，那么这就代表我要打开的是哪个文件，指定了这个文件的名字。

然后紧接着，逗号之后再跟上第二个参数，那么这一块指定我要对这个文件进行读取操作，那么这一块就是 r。

啊 r，我再次强调，这个 r 就是 read 那个单词的简写首字母。

然后紧接着，那第三个呢，指定一下编码。啊由于我们要操作中文啊，所以这一块我们就使用全球最为通用兼容性最好的这个编码 utf-8 就可以了，所以 encoding 设置为 utf-8。

那这就是打开文件。

那文件打开之后，调用完 open 这个函数，最终啊我们是可以拿到一个返回值的，那这个返回值代表的就是我们打开的这个文件对象。

好，那这是第一步：打开文件。

那打开完文件之后，接下来我们第二步是不是要读取文件的内容了？

哎，那么读取文件的内容呢，我们在这块就可以去调用 f，也就是这个文件对象当中的一个方法，read 开头的方法都是来读取这个文件。

而这一块大家会发现有多种方式啊。那第一个 read 这个方法呢，它一次性可以读取全部文件，或者呢你想读多长，你直接传递进来就可以了。

然后第二个是 readline，第三个是 readlines。那么 readline 呢，它是一行一行的读，而这个 readlines 呢，是一次性把所有的行都读出来，啊然后呢，你再去遍历输出就完事了。

好，那这里呢，我在这块先去调一个最简单的，就是 read，里面什么参数都不传递。

那你如果直接调这个方法，代表的是 AI 给我说了哈，是读取所有内容。

好，那在这块呢，我再通过一个变量来接收一下所有的内容，就是这个 content，然后呢，我这一块直接将 content print 输出出来就完事了。

好，这就是读取这个文件的所有内容。

然后紧接着第三步呢，就是关闭文件。好，那关闭文件呢，我们在这块再调用 f.close，那就是关闭文件。

好，那这样呢，我们就完成了望庐山瀑布.txt 这个文本文件的读取。

那么接下来呢，我们就来测试一下。来，右键，这一块直接点击这里的运行。那运行之后，大家来看，那这一块文件的内容是不是就读取出来了？

然后我们打开这个原始的文件大家来看啊，望庐山瀑布下面这块是不是个空行？你看，空行也展示出来了，然后日照香炉生紫烟，遥看瀑布挂前川，是不是全部展示出来了？

好勒，那么这就是读取文件。

那这里呢，我们调用的这个 read 这个方法是一次性读取文件的所有内容。所有的内容读取出来之后，最终啊你看，给我们返回这么一个字符串。这个字符串里面就包含了这个文件的所有内容。

好，那这里呢，我们再演示一下另外一个方法哈，我这一块先将其注释掉。

那我们刚才看到这个文件对象当中 read 开头的方法除了这个 read 以外，是不是还有一个叫 readline，还有一个叫做 readlines 啊？

哎，那么 readlines 呢，是读取所有的行。那我们这里再演示一下这个 readlines 哈，那 readlines 呢就是读取所有的行。

那么大家再来看，这个 readlines 它的返回值大家应该很容易理解是一个什么？是一个列表。这个列表当中的这个元素呢，是一个字符串。那所有的行，每一行是不是给我封装到一个字符串当中？最终再将多个行给我封装到一个列表当中就完事了。

好，那我这一块再来一个哈，那我这块我就叫 content_list。

好，然后紧接着呢，我在这块再将这个 content_list 再将它遍历输出出来是不是就可以了？

当然 AI 在这块给我提示的这一块输出的内容是 line.strip()，这个 strip 大家还记得吗？

哎，这个 strip 方法它是不是用来去除我们这个字符串前后多余的一些空白字符啊？哎，比如像空格呀、换行符呀、以及制表符，它是不是都会给我们去除掉啊？

好，那这里呢，我们先不需要这个 strip 啊，我删掉，删掉之后，我先来运行一下吧，大家看效果。来，我右键，这一块直接运行。运行之后大家来看：望庐山瀑布 李白，日照香炉生紫烟，遥看瀑布挂前川，是不是都输出出来了？

但是大家会发现好像有点问题，什么问题呢？

在原始文件当中，你看这儿，后面这块是不是并没有一个多余的空行？但是这儿后面是不是有一个多个空行啊？而前面这一块你看还多两个空行。那为什么会出现这种现象呢？

其实啊我们可以这么去做哈。我在这儿打个断点，右键这一块来进入调试模式，debug 跟踪一下，然后我们往下走一步！

那读取到所有的行之后，我们来看一下这个 content_list，大家来看，这就是这个 content_list 这个列表，这就是列表当中一共这么六个元素。而大家会发现，那么读取出来的内部在文本当中后面这块都会多一个什么？还记得这个 \n 吗？在字符串当中这个 \n 是不是代表的是换行的意思啊？你看这儿，这块有个 \n，这块有个 \n，这块 \n，\n。那么最终你如果在这块输出这个字符串，字符串后面带一个 \n 的话，它是不是会自动地换行啊？

而大家又知道，print 本身就有一个换行，然后输出的这个字符串当中你这块又多了一个换行，那最终出现的效果呢，就是最终啊它会在这块，你看，这儿换了一行，下面这块又换了一个空行。能理解吧？

哎因为在我们的原始文本这块你看，后面走这儿是换行了的，所以呢读取出来的这个字符串后面会自动给你加上一个 \n，表示这块是一个换行符的。所以呢，要想解决这个问题，要想去除掉多余的这个换行，那么其实就按照刚才 AI 给我们生成的代码，你去调一下这个 strip 就可以了。把前面以及后面多余的空行直接给它删掉就 ok 了。现在你看是不是就正常了？

好勒，那这是我们这块所演示的读取文件。

好，那么读取文件我们这块演示完毕之后啊，那么接下来呢，我们这块再来演示一下那如何来写入文件啊，写文件。

那么写文件其实和读文件这块的这个代码呢，基本上是类似的。我复制一下，我直接拿下来哈。

那么拿下来之后啊，那中间这块的内容我想将其删掉哈，那么这块就是来写入文件的内容。

那么我们在这块再去指定一个文件哈，那我这块再去指定一个叫做静夜思吧，来，静夜思.txt。那我问大家，在 resources 这个目录下有没有一个静夜思.txt 啊？没有。如果没有这个文件，那最终啊它会自动地在这块给我们创建一个文件，然后再往这个文件当中写对应的内容。

那如果说我们要写入文件的内容，那这个时候你需要将这个 r 换成 w。w 就是 write 这个单词的首字母。

然后接下来呢，我们在这块就可以去调用这个文件对象 f 当中的 write 方法来写数据了。

那么这块大家来看，可以 write，我们可以去写入对应的数据。然后这块你再看，writelines，我们是可以直接将多行数据我们给它封装起来，直接一次性写多行数据的。

好，那入门程序呢，我这块就演示一下这个 write，好，我们一行一行来写哈。

静夜思 李白，那后面这块加个 \n 什么意思呢？哎，字符串当中的 \n 就是换行的意思。那如果你在这块你再不换行的话，那么最终它就会放到一行当中。哎所以这块换个行。

再往下走，大家会发现，这就是使用 AI 的好处。你看，床前明月光是不是自动生成了？来，疑是地上霜，举头望明月，低头思故乡。好，生成了。

那这就相当于啊，我们往文件当中我写了五行内容。第一行内容是静夜思 李白，然后第二行呢就是床前明月光，疑是地上霜，举头望明月，低头思故乡。

好，那写入完毕之后，最后再将文件关闭掉。

好，那么接下来呢，我们右键这块直接运行，看效果。

那运行完毕之后，当然控制台没有任何的输出啊，但是大家会看到在 resources 这个目录下这块是不是多了一个文件？静夜思啊。

然后呢，我们双击打开这个静夜思，大家来看：静夜思 李白，床前明月光，疑是地上霜，这个古诗是不是就写入成功了？

而此时大家会发现，哎这个标题和这个古诗之间好像并没有多一个空行，我想多一个空行该怎么做呢？

哎，如果说静夜思 李白后面你想再多一个空行，那你这块再怎么着？我再加一个 \n 就可以了。第一个 \n 代表的是我正常在这块换一行，是不是换到下一行了？那么第二个 \n 呢，就是走到这儿我再换一行，那中间就多加了一个空行了。好，我们在这块再试一下哈。

来，我先将空行删掉。这个文件我先不删哈，我就看一下如果这个文件存在的情况下，那我再往里面写入会发生什么现象。

来，我们在这块右键，这块直接运行。运行之后并没有报错，程序成功运行。来，双击打开这个 txt 文件，大家来看，中间的空行有了没有？有了。

那也就意味着，如果说这个文件存在的情况下，我们再往这个文件当中写入数据，那它并不会报错，而是会将原来文件当中的内容给我们覆盖掉。好，这就是 w 这种模式它的特点。

好，当然关于这个文件操作的模式啊，我们入门程序当中啊大家先认识一下这个 r 和 w，那这个呢我们后面会详细讲解、详细来剖析。

好，那这样呢，关于文件最基础的读和写呢，我们这块就讲解完毕了。

那演示完毕之后，最后呢我们再回到 PPT，简单来做一个小结，主要明确一个问题：就是我们在对文件进行读或者是写，操作的时候一共分为几步啊？ 无论是读，或者是写，是不是一共就分三步？ 第一步， 打开文件。 第二步，对文件当中的内容进行操作，就是读或者是写的操作。那么第三步呢，就是关闭文件。 而在打开文件的时候呢，我们直接去调用Python当中提供的一个内置的函数啊，就是open这个函数就可以了。那open这个函数执行完毕之后，它是不是会给我们返回一个文件对象啊？然后接下来呢，我们就可以调用这个文件对象当中的 read开头的函数来进行读取文件当中的内容，write开头的方法就可以来往文件当中写具体的数据了。那操作完毕之后，最后调用一下close方法来关闭文件，那这一步呢，我们也叫释放资源。 啊如果说这个资源你不释放，你不关闭，那我们如果说这个项目一直运行着的话，那这个文件会长期处于占用的状态，那其它的这些程序是无法再操作这个文件的。 好的，那这是我们这小节所演示的Python当中如何来操作文件，文件操作的入门。 那接下来呢，大家可以打开自己的PyCharm快速练习一下。 黑马程序员为千万学生少走弯路，我们一直在努力。 好的，那通过上一小节的学习呢，我们已经知道了在Python当中啊，我们如何来操作文件，对文件进行读或者是写。哎是不是就是三板斧啊？ 打开文件、操作文件、关闭文件。 那接下来呢，我们一起来思考一个问题，就是我们在操作文件的过程当中啊，假设程序运行的过程当中出现了一次。 那这个时候大家想， 你比如啊，我在写入文件的时候，代码走到这儿出现了一长，那么一旦在这块出现了一长，那下面的代码是不是就不会再运行了？ 那下面的代码不会再运行，也就意味着关闭文件的这个代码是不是也就不会运行了？ 那文件此时就无法关闭了。那也就意味着这个文件可能一直处于占用状态，其他的程序就不能再操作这个文件了。那现在我要问大家的是，那针对这样的问题，我们该怎么解决呢？ 我们怎么样才能够让关闭文件这块的这个代码，无论程序是成功运行还是运行的过程当中出现异常，让这块的代码都能够执行呢？ 哎那么其实对于这块呢，我们之前是讲到过解决方案的，就是无论程序是否出现异常都会执行的代码，我们要写在哪个代码块当中，用到哪个关键字啊？来，大家在弹幕当中刷出来。 哎对了。那这里呢，我们只需要通过 finally代码块来解决这个问题就可以了。 所以呢，大家来看，我们在进行文件操作的时候，我们在关闭文件也就是释放资源的时候，我们具体的方案是什么样子的？ 那我们可以把文件操作的代码呢写在try这个代码块当中。 那如果说你想去捕获所有的异常，那么你在这块写个except，然后去捕获所有的异常就可以了。如果说你不想捕获异常，那这个时候你在下面这一块写个finally，然后在finally代码块当中来关闭这个文件是不是就可以了？ 因为我们前面在讲解异常处理的时候，我们提到 finally代码块当中的代码呢，无论程序执行的过程当中是否出现异常，那这段代码是不是都会运行啊？哎那也就意味着，即使上面这一块的这个程序运行的过程当中报错了，那下面这段代码呢，依然可以成功运行。 那这样呢，就能够保证即使程序出错了，我们的文件依然可以正常的关闭。 好，那么接下来呢，我们就来改造一下我们刚才的入门程序。来，我们打开PyCharm。 那么打开PyCharm之后啊，来我将这段代码我复制一下啊。呃这是我们刚才入门程序当中所写的这个代码。 来， 直接将其注释掉，然后下面这一块呢，我再加上一个注释哈，分割一下。 加上一个分割线。 呃然后下面这一块如果说我们想去关闭资源，我们可以将其放在finally代码块当中。那你看，首先第一步打开文件，然后第二步操作文件的时候啊，那我们在这块可以将其放在 try这个代码块当中。好，然后这块代码往后缩进，你看这块操作文件是不是就放在try这个代码块当中了？ 那注意哦， try代码块不能单独出现，那try呢你也可以配合着 except来捕获异常，也可以配合着finally。哈鼠标往这儿一放，它就会告诉你啊，你这个try呢可以配合着except捕获异常，然后后面这一块可以放一个finally，或者你直接try finally也是可以的。 那这里呢，我们就不处理异常了哈。 来，这块加上一个finally，然后将关闭文件的代码放在finally代码块当中就可以了。 好，那这个时候大家来看。 首先第一步，我们在这块去打开一个文件，获取到这个文件对象。然后紧接着第二步呢来操作文件。那操作文件的过程当中，如果出现异常，这个close关闭文件的代码是不是依然会正常运行啊？好，那这样呢这个问题我们就解决掉了。啊那现在我们在这块运行，你看没有任何的效果，因为没有异常。呃我们在这块就来试一下哈。呃那这里呢，我们就来一个叫做 1/0， 啊然后呢将1/0的结果呢，我赋值给一个变量i。然后接下来呢在下面这一块哈， 呃我在这里我输出一句话：关闭文件。我们重点看一下一旦出现异常，finally当中的代码到底会不会运行。但这块我们前面都讲解过，我们这块再来演示一下。走。 大家来看， 即使程序运行的过程当中出现了一行，你看 这个finally代码块当中的代码是不是依然会运行啊？文件依然可以正常关闭。 好，那这就是我们在这块所说的在进行文件操作的时候，那么释放资源正确的操作方式。 好，那么演示完毕之后，我们再回到PPT啊。 呃那么其实这种方式呢可以，但是是比较繁琐的。因为在操作的时候，我们需要把操作文件所有的代码写在try代码块当中，然后呢释放资源的要写在finally当中，那这一块是不是会增加我们的代码结构啊？需要写try，还需要写finally啊。 哎这一块是比较繁琐的。 那么接下来呢，再来说一下，那我们项目开发当中的推荐方式是什么样子的？ 那在我们的项目开发当中啊，我们推荐在这一块使用一个 关键字叫with来解决这个问题。 那这个with呢，其实它是Python当中提供的一个上下文管理器。我们在streamlit这个框架当中在使用的时候是不是用到过这个with啊？ 哎那么在Python当中，我们在进行资源释放的时候，也可以使用这个with。 这个with呢，它是Python当中的上下文管理器，它的核心作用就是确保我们这块所操作的这个文件资源啊所操作的这个资源能够被正确的释放。 注意哦，这一块所说的是总是被正确的释放。 那后面这一块也说了，即使程序运行出现异常，也能够被正确的释放，这也是项目开发当中的推荐方式。 来，我们看一下它的语法。 你看， with open，这块是不是直接调用了open这个函数？ as后面在这一块指定一个f，那其实这个后面的这个f呢，就是这个open这个函数调用的返回值，哎其实就是这个文件对象。 那接下来拿到这个文件对象，我们就可以对这个文件对象进行操作了。你进行读或者是写直接写在这个代码块当中就可以了。而这个时候大家再来看，我们有没有去处理关闭文件这个操作啊？ 没有。有没有写try？没有。有没有写finally？没有。 代码结构是不是会更加简洁啊？ 哎一旦这个代码块当中的代码执行完毕，这个文件它会自动的进行 关闭， 而且即使出现异常，那么依然可以正确的关闭。 那么这是我们项目开发当中的推荐方式。 好，那么接下来呢，我们再回到PyCharm当中啊，来演示一下。 来，那这是我们释放资源的方式一哈，我这块写一下，这是方式一。 然后紧接着呢，我将这段代码Ctrl C复制一下，然后拿下来，这块变一下，变成方式二。然后紧接着我将上面这段代码呢直接给它注释掉。注释掉之后，大家来看， 那么方式二的话怎么做呢？ 这段代码还用不用啊？ 释放资源关闭文件的代码不用，finally直接删掉，try用不用？不用，直接删掉。只留我们操作文件的核心代码。然后呢在这个open这个函数前面这一块写上一个关键字，这个关键字叫 with。 然后紧接着后面这一块呢，那么这个open这个函数调用之后它的返回值，我们在as后面来写一下，as f。 好， 这就代表 哎我要去调用open这个函数来打开这个文件，然后呢最终打开了这个文件，那文件对象呢就是这个f。 啊这个f其实就是open这个函数调用的返回值。然后呢再将我们操作文件的代码写在with这个代码块当中就可以了。 那这样的话，大家来看，这个结构是不是就非常清晰了？哎语法也非常的简洁。 好，我们在这块再次运行，依然是可以 正常运行的。 那这就是我们在项目开发当中，在进行文件操作的时候，在释放资源时我们的推荐方式。啊这是项目开发的，我这块写一下哈，最佳实践。 好咧，那这是我们这小节所讲解的在进行文件操作时，关于资源释放的两种方式，推荐第二种方式，使用with这种语法来操作。 好，那么讲解完毕之后，最后呢我们就回到PPT，好快速做一个小结啊。那么这小节呢，我们主要讲解了一下文件资源释放的操作。 那这个方式呢，我们讲解了两种，第一种方式 就是通过 try finally 这个语法来操作。 我们将资源释放也就是文件关闭这一块的操作呢，我们放在 finally 这个代码块当中，那这样呢就能够保证，无论程序是否出现异常，那这个文件最终肯定会被关闭。 但是这种方式呢比较繁琐，啊企业项目开发当中不推荐。那么推荐的呢就是使用 with 这种语法来操作。 啊那 with 呢是 Python 当中的上下文管理器，通过这种方式来操作的话，它总是能够保证我们所打开的这个文件，最终用完之后它会自动关闭。 而且即使出现异常，也会自动关闭。 啊那么这种方式呢，是我们项目开发的推荐方式。 好的，那这是我们这小节所讲解的关于文件操作时资源释放的解决方案。 黑马程序员为千万学生少走弯路，我们一直在努力。 好的，那在前面我们在分析会话管理这一块的实现思路的时候啊，我们提到 在新建会话时呢，我们需要将之前的这些会话的数据，是不是要保存到文件当中？ 而文件当中呢，我们在保存的时候，我们既需要保存交互的消息，还需要保存这个伴侣的名字，啊也就是伴侣的昵称，还有伴侣的性格，以及当前会话的这个名字。 那这些信息我们都需要保存到文件当中。而为了让我们的文件的结构会更加清晰，能够清楚地知道诶哪一块是交互的信息，哪一块是昵称，哪一块是性格。那么在文件当中啊，我们前面也提到，我们最终啊可以以 JSON 格式的数据来保存。 那这个时候，我们是不是就需要来往文件当中来写入 JSON 格式的这个字符串数据了。 哎然后将来呢，我在去加载这些历史会话的时候，最终我是吃需要再将这个文件当中的这个字符串数据再加载回来，再读取出来。 哎那这个时候呢，就会涉及到 JSON 数据的操作。 而由于 JSON 呢在我们项目开发当中啊是非常通用的数据格式。所以呢在 Python 的标准库当中啊，为了简化对于 Python 数据的操作，它就给我们提供了一个核心模块，就叫 json。 所以呢，我们要想操作 JSON 数据格式的文件啊，那我们就可以直接导入这个 json 模块，啊然后直接去调用这个模块当中的功能，啊比如调用这个模块当中提供的函数来处理就可以了。 而在 json 的这个核心模块当中啊，它提供了两个非常重要的操作，也是两个非常常见的操作。 那一个呢，就是将我们的 Python 对象，啊比如这是 Python 当中的一个字典。啊可以讲 Python 当中的这个对象直接给我们保存到这个文件当中。比如你看，左侧是我们 Python 代码当中的一个字典对象，然后右侧这一块呢就是一个文件，就是一个文本文件。只不过这个文本文件当中的数据呢是以 JSON 格式来保存的。 那我们想要将左侧的 Python 对象直接保存到右侧的这个 JSON 文件当中，我们直接去调用 json 这个模块当中的一个函数叫做 dump 这个函数就可以了。 直接调用这个函数就会将这个对象当中的数据保存到这个 JSON 文件当中。 那这个呢，我们也称之为序列化。那序列化呢指的就是直接将 Python 对象转化成 JSON 格式的字符串，并保存在文件当中。 那还有一个操作呢就是反序列化。 那反序列化呢指的是去读取这个文件当中的 JSON 格式的数据，啊就是一段 JSON 格式的字符串，然后呢将其加载成为 Python 当中的对象。那这个过程呢称之为反序列化。那序列化呢我们要调用的是 json 这个模块当中的 dump 这个函数。那反序列化呢我们要调用的是 json 这个模块当中的 load 这个函数。 啊所以这里呢，其实核心就是这两个函数，一个是 dump，一个是 load。 好，那么接下来呢，我们就来看一下，那具体代码该怎么写？ 那么首先，如果说我们想用这个核心模块 json 啊，那我们就需要在上面 import 直接将 json 导入进来。那么导入进来之后，那我们在这块先来看一下如何写入 JSON 格式的数据啊，就是将 JSON 格式数据直接写入到文件当中。那这里呢大家会看到我这块是不是定义了这么一个字典啊？哎字典嘛就是 key 冒号 value 键值对类型的这个数据。 然后紧接着呢，我在这块打开了这么一个文件，啊你看我要操作这个文件，是不是要往文件当中写东西啊？哎在这块打开了一个文件，我这块就叫 session.json 啊就是一个演示的，然后指定模式为 w，由于我们要操作中文啊，所以这一块编码啊我们也叫字符集设置为 utf-8。 那如果说我们想把上面的这个 Python 对象，啊这个字典对象直接写入到这个 json 这个文本文件当中啊，直接去调用这么一个函数，就是 json 这个模块当中的 dump 这个函数，直接来写入就可以了。 那这个过程呢，我们就称之为序列化。那最终它就会将 我们 Python 当中的这个对象直接给我们写入到这个 json 这个文本文件当中。 那如果说我们要执行反序列化操作，啊我们想将这个 json 这个文本文件当中的数据读取出来，那如果是读文件，那我们这块就写上 r，模式就是 r。然后呢这里直接去调用 json 这个模块当中的另外一个函数 load。啊 load 呢就是去读取这个文本文件当中的数据，然后将其转化成一个 Python 当中的对象。那这个 obj 其实就是 Python 当中的一个对象。 所以序列化所调用的这个函数就是 dump，指的就是将 Python 对象啊在这一块呢转换成 JSON 格式的字符串保存到文件当中，而反序列化呢指的就是将这个文本文件当中的 JSON 格式的字符串转换成 Python 当中的对象。哎就相当于是一个正向操作，一个是逆向操作。 那最终我们会发现啊，我们如果使用了这个 json 这个模块，那么来操作 JSON 数据文件的话，呃基本上行代码就搞定了。 好，那么简单介绍之后，接下来呢我们来打开我的 PyCharm 来演示一下。 呃那么在这里呢我将这个文件直接关掉啊，关掉之后我在第三章这个目录下我再来创建一个文件哈，我还是以 02 开头哈，以 02 开头我这个文件是不是就排到前面了。哎那我们在这块来说一下。 呃 json 模块的入门演示哈。 来，我们在这块直接回车。 好，那么接下来呢我们在这块就来演示一下。 呃首先先来演示一下，写入 JSON 文件哈，写入 JSON 数据文件。 那我们要想使用 json 这个核心模块，你得导入进来呀。所以在最上面哈，我们导入的代码一般写在最上面，import 这块直接导入 json。 好，那 json 这个模块导入进来之后，接下来呢我在这块首先先来构造我们 Python 当中的一个对象哈。 呃这个对象的名字呢给它起一个变量名啊，就叫 user。然后里面指定一下，那有哪些键值对呢？ 呃第一个是 name 是吧？好，那 name 呢我们在这块来一个涛哥，哈。 好，紧接着在这块再来一个 age，你看 AI 很智能，涛哥今年 18 岁对吧？ 啊，来往下走。gender，啊男，没问题。再往后走， 呃爱好对吧？呃 hobbies 爱好。那爱好 reading，阅读，swimming，啊游泳。行，啊那就这样。哈那爱好是不是可能会有多个啊，哎所以这块给我们来了一个列表。 那这，是不是就是我们 Python 当中的一个对象？ 哎是字典类型的对象。 那其实大家会发现，Python 当中的这个字典类型的这个结构啊，呃跟 JSON 数据格式当中的这个结构是不是非常类似啊？都是键值对。好，那么紧接着我们在这块就来演示一下，那现在我怎么样将这个 Python 对象给我写入到一个文本文件当中呢？ 哎那在这块要操作文本文件，要来写入这个文本文件， 那我们在这块就使用刚才所提到的 with 这种语法哈。那后面这块来一个 open，那 open 里面在这块我们需要指定这么三个参数啊，我直接按一下 Tab 键采纳 AI 的建议，大家来看。 呃那这块呢，我们要往哪个文件当中写入数据呢？ resources 下面的 user.json。 啊 resources 下面并没有一个 user.json 啊，那这块呢既然没有，它就会给我们创建这么一个文件。 啊文件后缀名其实你叫什么都行，你叫 .txt 也不影响哈。 那我们这块呢，呃我们就叫 .json，一看我就知道里面存储的是 JSON 格式的数据。那操作模式呢就是 write，是写。那紧接着这块的字符集也就是编码方式 utf-8，因为这个里面是不是有中文啊。 然后后面在这块呢，我们来一个 as f，那这个 f 是不是就是我们打开的这个文件啊。 好，那么紧接着在这块我们就可以调用 json. 调这个模块当中的一个函数啊，大家来看。 呃其实在 json 这个模块当中啊，它里面的函数并不多哈，常用的就这两个，一个是 dump，一个是 load。 虽然这两个函数里面的参数呢比较多，但是其实后面这些很多参数都有默认值。 哈，那如果说你想看一下，那哪些参数里面有默认值呢？你可以按着你的 Ctrl 键进入到源码当中，大家来看。 后面这些参数是不是都有默认值啊？ 哎那么核心就是前面这两个参数，一个是 obj，就是你要把哪个对象写入文件当中，然后第二个呢就是这个文件，fp 代表的就是文件。好勒，那么这一块我们可以继续来操作哈。 呃那么在这块我们来指定我们要写入哪个对象啊？ user 这个对象呀，写入哪个文件？ 那不就是f这个文件嘛。 好，这样就可以了。这就代表我要把user这个对象哎在这块转化为json格式的字符串，然后写入到这个文件当中。好，那么接下来呢，我们就可以直接右键来运行看一下效果。好，那么执行完毕了。那执行完毕之后，我们可以再次来打开我们的这个resources目录，大家来看user.json有了没有？ 有了。那接下来呢，我这块直接双击打开。打开之后，大家来看user.json这个文件里面的数据有了没有？有了。你看name，age，gender，hobby是不是都有了？ 但是大家会发现好像有点问题。什么问题呢？ 好像大家会发现啊，数字呀，英文啊好像都没问题，但是谁有问题？ 中文有问题。 好，那么这一块说一下为什么会出现这个现象哈，大家来看。 在我们调用dump这个函数的时候，里面有这么一个参数叫做ensure_ascii。那这个ascii在我们前面介绍编码的时候我们提到过啊。那这个ascii码呢，它里面只能处理数字以及字母还有常见符号，无法处理中文。而这个参数呢代表的是，对所有的非ascii码哈要进行转义处理。 啊其实在下面这一块也提到，你看这块，如果说这个里面包含了一些你看前面加了一个non，non-ascii就是非ascii码的字符，它会进行转义处理。哎最终就是大家所看到的这一块的这个效果，因为ascii码当中是无法处理这个中文的，那中文就属于非ascii码，非ascii码它就会进行这么处理。 好，那我们这一块是不是想让它保留中文的这个形式啊？ 所以这个时候我只需要将这个参数由默认的True改为啥？ 改为False就可以了。大家再来看哈。 我把这个参数复制一下，复制之后直接放在这儿哈。 那大家来看，那么对这个参数来说呢，中间是不是跳过了一个参数？所以这个时候我们得使用关键字传参。 啊 ensure_ascii这一块由True改为False。好，这样就可以了。 好，那么接下来我在这块右键再次运行，运行之后我再次打开这个json文件，我问大家，中文的问题解决了没有？ 解决了。你看，涛哥、男，中文这个问题是不是就解决掉了？ 好勒，那这样呢，我们就将Python对象啊，Python当中的这个字典对象成功地写入到这个文本文件当中了。那这个格式是不是就是json数据格式啊？你看所有的key呢都使用引號引起来了，啊这就是json格式的数据。 那这么去做呢，可以啊，但是其实并不太直观。啊如果说这个json数据格式当中的数据量比较大的话，呃它并没有进行一个格式化的处理，啊其实并不太直观啊。 呃那这里呢其实我们可以在加上一个参数让它进行格式化的处理，就是这个indent。 啊这个indent呢代表的是缩进的意思。 啊就是代表这个key前面要缩进几个空格呀？ 那这块AI给我提示缩进两个空格就可以了，来我们看一下有什么变化。 好，我们在这块右键再次运行。运行之后啊直接打开这个json文件，我问大家，能理解缩进什么意思吧？ 哎你看这个key的前面怎么着？缩进了两个空格，每一个key的前面都会缩进两个空格，然后每一个key是不是都会占一行啊？ 那这样的话大家来看，这个json格式的数据看起来是不是就比较好理解了？哎非常的直观。 好，那么这样呢我们就将这个Python对象写入到了这个json文件当中。 好，我们在这块上面给大家加一下注释哈。 呃那么这里呢，我们在这块主要说两个参数，一个是ensure_ascii，那还有一个呢就是这个indent。 那么我们在这块哈来看一下它的作用。这个indent呢刚才说了就是缩进，啊会在这个key的前面进行缩进啊，我在这块写一下。 会在这个呃输出的json数据中添加对应的缩进。好，那么这一块其实就是进行一个格式化的操作。 那上面的这个参数啊ensure_ascii，那这个呢参数啊它默认维度为True哈。默我这块写一下，啊默认维True，那默认维True呢代表的是啊呃确保所有的数据输出的啊都是ascii编码。那么非ascii码呢，那ascii码呢我们前面提到只能对数字、字母以及基本的一些符号进行处理，中文无法处理。啊那么中文呢就属于非ascii码，那非ascii码呢啊会进行啊转义。 好，那么紧接着呢我们在这块说一下，那我们刚才把它设为False，设为False啥意思？ 哎设为False代表的是非ascii码不转义哈。 非ascii码呢保留原样输出。好，保留原样输出。 好，那么大家记得哈，我们只要操作中文，你就将这一块的这个选项设为False就可以了。 好的，那么这是我们在这块啊去写入json数据文件。 那么紧接着我在下面这一块再来演示一下，那如何来读取json数据文件？ 呃那么读取json数据文件呢，我们就来读一下这个user.json哈。来，我在这块呃直接让AI给我生成哈。 大家来看，with open打开这个user.json，那读呢，这个模式就是r，字符集utf-8，没问题。好，那么紧接着下面这一块我要调用的就是另外一个方法，那就是load，就是来加载。啊我们要加载哪个文件？加载这个f这个文件。最终它会将文件当中的这个字符串啊，因为文件当中存储的都是字符串，会将字符串读取出来，并且自动的加载成一个Python对象。 好，那么下来呢，我在这块再将这个user输出出来。那除了输出这个user以外，那我在这块呢再使用我们type这个函数，然后将user传递进来，我看一下user到底属于什么类型。 好，那上面的这个代码刚才演示完了我就将其注释掉哈。注释掉之后来我右键这块直接运行，运行之后大家来看 这个user什么类型？ dict，是不是字典类型？ 哎你会发现，这个load呢它的作用就是将这个文本文件当中的数据加载出来，然后直接给我们转化成Python当中的对象。啊那转化过来的就是一个字典对象。那也就意味着上面输出的这个呢其实是一个字典。 好的，那这是我们这一块所演示的读取json数据文件。啊非常方便哈，直接调用json模块当中的load这个函数就可以了。 好的，那这是我们这小节所讲解的关于json数据文件的写入以及读取。 那么演示完毕之后，最后呢我们再回到PPT简单做一个小结，主要就明确两个方法。那一个呢是序列化，一个是反序列化。那么如果是序列化我们要调用的是json这个模块当中的dump这个函数。那这个函数它的作用呢就是将我们的Python对象，然后呢序列化为json格式的字符串并写入文件。而load这个函数的作用呢就是将文件当中的数据读取出来，啊读取文件当中json格式的数据，并将其反序列化也就是转化为这个Python对象。好的，那这是我们这小节所讲解的Python当中所提供的核心模块json，以及通过json这个模块如何来进行json数据文件的操作。 黑马程序员为千万学生少走弯路，我们一直在努力。
