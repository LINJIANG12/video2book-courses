好的，那Jupyter这一块的环境准备我们搞定之后啊，那接下来呢我们就来讲解数据分析的核心库，Pandas。那关于Pandas基础呢，我们将会有以下五个部分来讲解。那么首先，这一小节呢，我们先将通过一个入门程序让大家快速感受一下Pandas的基本使用，并体验一下基于Pandas进行数据分析的便捷性，然后呢我们再来剖析基于Pandas是如何进行数据的加载、过滤、清洗等一系列数据分析的具体操作的。

那首先呢我们就先来认识一下到底什么是Pandas。那Pandas呢是一个功能强大的结构化数据分析的工具集，其实就是Python的一个扩展库。那这个Pandas底层呢是基于NumPy构建的，那这个NumPy呢也是Python当中的一个扩展库，那这个NumPy主要是进行数学运算的，Pandas底层呢就是基于NumPy来构建的。那这个Pandas无论是在数据分析领域还是在大数据开发领域啊，都有显著的优势，而且也是数据分析领域绝对的王者。那下面这个呢就是Pandas它的官网，我们可以直接点击一下，打开Pandas的官网，简单来看一下。

那打开官网之后，映入眼帘的啊就是对于Pandas这一块的介绍，告诉我们Pandas是一个快速的，并且功能强大的、灵活的、容易使用的一个数据分析的一个库。那其实这里呢我可以打开这个翻译软件简单翻译一下，大家看一眼啊，你看：Pandas是一款快速、强大、灵活且易于使用的开源数据分析与操作的工具，构建在Python编程语言之上，也就是说啊它是一个Python的扩展库。那这个Pandas呢是Python的扩展库，并不是Python的标准库啊，所以你要想使用呢，你得安装，那这个安装呢我们等会儿再来说。

好，我们在这块再回到PPT啊。那么在Pandas当中呢有两大核心：第一大核心叫DataFrame，另外一大核心叫Series。那这个DataFrame呢其实就类似于是一张表格。比如大家来看，下面这个呢其实就是DataFrame的结构，其实就类似于是一个表格。你看，是不是有行？这是一行一行的数，然后有列，你看，这是name这一列，这是age这一列，这是sex这一列。而最前面的这一列呢，那其实是index叫索引，每一个DataFrame都有自己的索引。那这个索引呢，默认就是从0开始的，0 1 2 3 4这样子的。那这就是DataFrame的结构，就类似于是一张表格，是不是有行有列啊？哎那你看这就是一列，那这个列呢我们叫column。你再看，那这并不是就是一行？那一行呢我们叫row，row就是一行。哎这样呢就构成了这样一个表格，就是DataFrame。

然后紧接着在这块再来说一下这个Series。Series呢就类似于表格当中的一列。把这个DataFrame当中的任意一列拎出来，那么就是一个Series。比如大家来看，将name这一列我给它单独地拎出来，那么就组成了一个Series。当然了，那么对于Series来说它也有索引的概念，你看这是第一个，这一列当中的第一个元素，这是第二个元素，这是第三个元素，那索引呢，0 1 2 3 4。然后你再比如我将这块的这个age这一列我也给它单独地拎出来，哎那又组成了一个Series。哎所以这个Series呢就类似于这个DataFrame这个表格当中的一列。而这里呢大家需要注意咯，无论是这个DataFrame还是这个Series，那么它前面呢都会有一个索引，叫index。那index呢，默认就是从0开始的。

好，那这是Pandas当中啊我们在进行数据分析操作时所涉及到的两大核心对象，DataFrame和Series。

好，那么简单介绍一下这个Pandas之后啊，接下来呢我们就通过一个入门程序让大家快速感受一下Pandas的基本使用，并体验一下Pandas进行数据分析的便捷性。来，我们来看一下具体的需求。那这里要求我们基于Pandas统计班级学员各科成绩的最高分、最低分以及平均分。

好，那这里呢既然我们要使用这个Pandas，而我们刚才提到Pandas它不属于Python的标准库，是Python的扩展库，那要想使用你就得安装。而当前Pandas的最新版本啊刚刚发布的3.0.1这个版本。那这里呢我们不采用这个最新的版本，因为最新的版本和我们之前所用到的一个技术叫Streamlit呢并不兼容。那这里呢我们使用2.3.3这个版本。好，这个时候大家其实可以点击一下这儿啊，点击一下，点击之后你就可以看到它的详细的版本。那这里呢我们采用这么一个次新版本啊，2.3.3这个版本就可以了。

好，那么接下来呢我们就来看一下那怎么安装Pandas的2.3.3的这个版本呢？好，那这个时候可以直接打开我的PPT啊，那这里的命令也给大家提供了：pip install pandas==2.3.3，这就代表我们要联网安装Pandas 2.3.3这个版本。好，安装好了之后就可以在我们的Python代码当中啊，直接将Pandas import导入进来，给它起一个名字就叫pd，简化一下我们的书写。

然后我们就可以在我们的DataFrame，你看DataFrame是不是类似个表格？哎我们就可以构建这么一个DataFrame对象，在DataFrame对象当中啊将我们班级学员的一个各科的成绩信息是不是构造出来？哎然后再基于DataFrame这个对象来对学员的信息进行统计操作。那么所构建的这个DataFrame其实就类似于这样一个表格。那在这个表格当中呢，有多行数据，那一共有这么四列，分别是：姓名、语文、数学、英语，你看姓名、语文、数学、英语。然后针对于这个DataFrame这个结构，我们再去求取那么各科成绩当中的最低分、最高分以及平均分。

好，那么接下来呢我们就来演示一下。来，我们直接打开我的PyCharm。打开PyCharm之后啊，首先第一步我们得安装Pandas。那么安装Pandas呢，我们直接找到左侧这一块是不是有个终端啊？哎点击一下。那点击之后，直接来：pip install，来一个pandas。好，如果你直接执行，它默认安装的是最新的版本。那这里呢我们一般会选择一个稳定一些的版本啊，那后面我们在这块限定一下版本，==2.3.3。好，这就可以了。来，我们在这块直接回车。好，注意，安装Pandas这个过程一定记得要联网啊，确保网络畅通，然后呢回车之后耐心等待一下。好，那这样呢这个Pandas就装好了。你看，告诉我们啊，成功地安装了Pandas 2.3.3，是不是已经安装好了？

好，那这样呢准备工作就做好了。那做好了之后，接下来呢我们就可以在我们的这个目录下啊，第五章这个目录下，来右键，这一块点击创建一个Jupyter这么一个文件。来，我们就叫02_pandas_入门，回车。

好，那么创建好这么一个Jupyter这么一个文件之后，接下来在这个里面编写代码的时候，首先第一步得导入Pandas。来，导入Pandas直接 import pandas，后面在这块来一个 as pd。好，这样就可以了。好，那这块告诉我们没有安装类型提示，这个呢大家可以装可以不装啊，你想装了你直接点击一下安装。那这个时候你安装了这个Pandas这个包呢，会有对应的一些提示。好，这里呢我们就点一下啊。

好，那紧接着呢在这个里面，我们就可以来构造一个DataFrame。因为DataFrame里面存放的就是我们要操作的数据。所以在这一块首先第一步，我们就是去构造DataFrame。那这里呢我们要统计的是学员的成绩信息啊，那这个DataFrame当中存储的这个数据信息就是学员的成绩信息。好，然后紧接着第二步呢再进行统计计算。

好，那这里呢我们在这块要构造这么一个DataFrame，这里的字呢略微有点小啊，我把这个字呢稍微调大一点。找到我们这里的编辑器，找到字体，把字体我们调整为16号字体。好，点击确定。好，字体变大一点啊。

那么构造这个DataFrame，在入门程序当中大家先这么写啊，等会儿我们会详细介绍。直接调用Pandas当中的这么一个对象啊，构造这么一个对象，叫做DataFrame。你看DataFrame其实它是一个类，我们基于这个类来创建出DataFrame这个对象就可以了。然后在这个构造这个DataFrame这个对象的时候呢，里面传递的参数多种多样。入门程序当中大家先这么写，可以传递一个列表。那么在列表当中我们就可以来封装一个一个的对象，其实就是一个一个的字典数据。那你看，这一个字典数据其实就是一条记录。你看这一块小王，性别、年龄、成绩。那对于我们来说我们在这一块呢，它的数据结构是这样子的啊：小王，这一块是语文成绩是多少，然后第二个呢数学成绩多少，第三个英语成绩多少，后面的这些没有。

好，那这里呢我们就快速地让AI快速给我生成一些数据啊，来。往下，再走。

哎，稍等一下。这一块加上一个逗号。再往下。再往下。好，可以了。呃，大家来看，那么在这一块我们快速构建一下哈，就来一个小王 2 3 4。哈，然后分数呢，我们在这块也换一下，比如来一个 65，呃这一块来一个 55，呃这一块来一个 99。好，那么这里呢，我们再换一下哈。呃像 82，哈 78。那这里呢，再来一个 91。好，那这样呢，我就构建好了这么一个 DataFrame。我们刚才提到，DataFrame 是不是就类似于一张表啊？那么这个 DataFrame 对象构建好了之后啊，大家来看，这个 DataFrame 对象构建好了之后，我们是可以在前面去定一个变量来接收一下的。那这里呢，我们就叫 df。好，那么紧接着，我们走一步测一步。现在用的是 Jupyter 对吧？测起来很容易。这一块直接输出一下这个 df 看下效果。来，我们直接按 Ctrl+回车，走！好，大家来看。那下面这一块就给我输出出来了这个 DataFrame 它的一个结构。你会发现，是不是就类似于一张表啊？那前面的这个 0 1 2 3 4 5 6 7 8 这是什么东西呢？还记得吗大家？告诉我。来，在在弹幕当中刷出来，告诉我 0 1 2 3 4 5 6 7 8 这是我们刚才所介绍的 DataFrame 这个结构当中的什么东西啊？对了，这个叫 index，索引。然后，索引呢在每一个 DataFrame 当中都是有的，默认都会自动给你添加上的。那后面这一块你看，我们在这个字典当中，你看这一块的第一个 key 姓名，它是不是就作为这个表当中的这一列了？然后第二个呢，语文。你看，这是不是第二列语文？那第三个 key 你看数学，你看第三个 key 数学，第四个 key 英语，你看第四个 key 英语，是不是一共这么四列？哎，我们在这个字典当中的每一个 key 其实都作为表格当中的一列存在了。

好，那么接下来我再问大家。那么假设我们没有使用 pandas，我想去统计，那这批学生当中，语文成绩的最高分、最低分、平均分，数学成绩的最高分、最低分、平均分，英语成绩的最高分、最低分、平均分，我该怎么做？大家自己先思考一下。哎，如果没有 pandas，那我们按照原始的 Python 程序的写法应该怎么做啊？哎，首先，那这里我们是不是需要从这个列表当中去获取到每一个学生信息啊？哎，就是每一个这个字典当中的学生信息。然后拿到学生信息之后，是不是再去获取到他的各科成绩啊？然后我是不是再将其封装到一个列表 list 当中？哎，然后基于列表 list 呢，我再去求取它的最大值、最小值，哎然后呢，平均值。哎，就需要基于循环，然后一步一步去操作了。

好，那么接下来呢，我们就来看一下，那么基于 pandas 它是怎么操作的？好，一起来看。呃，那么基于 pandas 在进行操作的时候，大家来看哈，这里呢我们想获取到语文的最高分、最低分还有平均分，来看一下怎么做啊。现在我是不是想获取到语文这一列的数据，然后针对于这一列的数据计算最低分、最高分、平均分？那这里呢，我们就可以从这个 DataFrame df 是不是就这个 DataFrame？啊，我就可以从这个 DataFrame 当中来提取出语文这一列。那怎么提取出语文这一列呢？非常简单，后面写个中括号，然后里面写个语文。好，这样呢，就提取出来了语文这一列了。好，接下来大家看好了，我 Ctrl+回车，走一步测一步。我问大家拿到没有？你看，这就是语文这一列的数据。那前面这个是索引，后面就是语文这一列的数据。那其实，表格当中的每一列都是一个什么？大家还记得吗？哎，都是一个 Series。所以呢，其实你现在这个表达式拿到的结果它的类型就是一个 Series。大家可以看一下哈，我这一块来一个 type，然后将这个 df 语文直接扔进去。来，Ctrl+回车。你看，它的结果是一个什么？Series。哎，是不是一个 Series 对象？

好。那么，对于这个 Series 来说，我们要想去获取到它的最大值、最小值以及平均数，那这个时候我们直接去调用它里面的方法就可以了。它里面有这么一个方法，max。啊，我相信大家能猜到这是做什么的。这是求最大值的。然后紧接着后面这块再来一个，呃，df 语文 min。好，然后紧接着这一块再来一个。mean。那这个就是求最小值的。那这个求得是什么呢？这个求的就是平均值。好，我们在这块直接 Ctrl+回车。好，大家来看。这是最高分 91 分，最低分 60 分，平均分算下来是 78.444 分。啊因为平均数可能会存在小数，啊并且也可能会存在除不尽的情况。

好，那么接下来呢，我们在这块可以组装一下数据哈。呃那这里呢，我就使用一个格式化后的这个字符串哈。然后呢，我在这块使用括号将其括起来。呃稍等一下，我稍微改造一下哈。那这个是呃最大值哈。语文，来，语文最高分是多少，对吧？以格式化的形式输出出来。然后紧接着最低分是多少，大括号括起来，然后扔进来。那后面的这个 mean 这个是平均分。好，我们稍微格式化一下。后面在这块再来一个，那语文的平均分是多少呢？平均分。好，那后面括号再括起来。这就 OK 了。来，我们这块直接按 Ctrl+回车。大家来看。语文的最高分是多少，最低分是多少，平均分是多少，是不是就直接输出出来了？哎你看，它是一个字符串，所以左右两侧是引号引起来的。

那么这里之所以可以这么写，是因为我在这里省略了什么呢？哎，省略了那个 print 输出语句。好，那么紧接着呢，我们就可以去求取一下数学的最高分、最低分、平均分。那你看，方括号里面改成数学，是不是获取到数学这一列？那么英语呢，也是类似的。啊那么英语的最高分、最低分、平均分。好，那么接下来呢，我们直接 Ctrl+回车，走！那么输出完毕之后，大家会发现，最终啊在这一块只是输出了谁的？输出了英语的。那为什么语文的和数学的没输出呢？大家都记着哈。呃就是在 Jupyter 当中啊，它输出的内容永远是最后一行这个表达式。啊因为它是数据实验室，它就默认唯独你最后一个表达式是输出的，前面这表达式是不用输出的。那现在如果说我就想把这三行都输出来怎么办？那这个时候，你如果说你想输出多行，那这个时候你就需要把 print 加上了。啊你看，现在我将这个 print 给他加上，加上之后，大家再看效果。好，那我们在这块 Ctrl+回车，走！你看，语文的最高分、最低分、平均分，数学的最高分、最低分、平均分，还有英语的最高分、最低分、平均分是不是都获取到了？

哎，那么如果说大家看到这个除不尽的这个情况比较难受哈，你也可以保留一位或两位小数。啊那么之前我们讲到过在这个格式化的字符串当中啊，那么后面可以加上一个冒号，啊比如这一块你想保留两位小数那就是 2 f，啊就是 float 嘛，浮点数。好，那这就 OK 了。来，我们这块直接 Ctrl+回车。你看，是不是就可以了？好，那我们都给他保留两位小数。好嘞。那这样呢，我们就基于 DataFrame 来统计了班级学生当中语文、数学、英语这三科成绩的最高分、最低分、平均分。大家会发现，基于 DataFrame 在进行数据统计操作的时候呢，操作起来是非常快捷，而且也是非常方便的。如果通过原始的 Python 代码来实现，我们刚才也分析了，你是不是需要去循环这个列表？然后呢，循环列表的过程当中拿到每一个字典，然后再从字典当中提取出语文成绩，然后存到一个单独的语文成绩的列表当中。数学成绩存放到一个单独的数学成绩的列表当中，英语还是一样。然后再从列表当中，根据列表再计算最高分、最低分以及平均分。那操作起来呢，效率比较低，而且比较繁琐。好嘞，那这是我们这小节啊通过一个入门程序先让大家感受一下，基于 pandas 在进行数据分析的时候具体是怎么操作的，啊以及 pandas 的便捷性。

好，那入门程序完成之后，最后呢，我们在回到 PPT 简单来做一个小结，主要明确两个问题。第一个问题，什么是 DataFrame？什么是 Series？我们刚才提到，DataFrame 就类似于是一张表。那么既然是表，表当中是不是就是用来存储数据的？哎表当中呢有行，有列。而每一个 DataFrame 是不是都有一个 index 索引啊？默认索引是从 0 开始的，0 1 2 3 4 5 6 往上自增。然后紧接着，再来说一下这个 Series。那这个 Series 呢，其实就是 DataFrame 当中的一列。DataFrame 当中的每一列提取出来就是一个 Series。而对 Series 来说呢，它也有 index，也有索引的概念。啊默认呢，也是从 0 开始的。但是这一块呢，呃要给大家提示一点哈。就是这个 Series 呢，并不是说依赖于这个 DataFrame 的，它是可以独立存在的。只不过呢，DataFrame 当中的一列提取出来哎就是一个 Series。好的，那这是我们这小节啊通过一个入门程序去让大家快速体验一下Pandas。那么下一小节呢，我们还会针对这个DataFrame以及Series进行一个详细的讲解。那我们下一小节再见。

黑马程序员，为千万学生少走弯路，我们一直在努力。

那在上一小节，我们在讲解Pandas入门程序的时候，我们就提到在Pandas当中呢有两大核心：DataFrame和Series。那么接下来这一小节呢，我们就来详细地剖析一下这两大核心。那首先我们先来说一下这个DataFrame。这个DataFrame我们刚才提到，它就类似于是一张表。那表当中是不是有行、有列呀？那么刚才我们在入门程序当中啊，其实已经创建了这个DataFrame对象了。而其实对于这个DataFrame对象的创建方式呢，有多种。那我们一起来看一下。刚才在入门程序当中啊，我们是基于第一种方式来创建的：直接传递一个列表，列表当中呢是一个一个的字典对象。啊那么其实这一个字典呢就是其中的一条数据。那这个DataFrame当中到底有哪些列呢？诶其实就是这个字典当中的key。你看，有姓名、有语文、数学、英语。好，那里面一共就这么三条数据，最终就构成了这么一个DataFrame。而对于每一个DataFrame是不是都有自己的这个index索引啊？诶那你指定索引，索引默认就是从0开始，0 1 2 3 4 5 6这样子的。

好，那么除了这种构造方式以外啊，那我们还可以基于字典来构造。就是外层呢是一个字典，你看，大括号括起来的外层是不是一个字典？然后紧接着，在字典当中你看，诶这块呢有多个key。那字典当中的这个key，其实就和上面这一块的这个表头啊，就类似于这一列的这个表头的名字，你看：姓名、语文、数学、英语。好，那么姓名这一列当中有哪些数据呢？张三、李四、王五。那语文这一列呢，你看列表当中就存储了这一列当中的数据，85、78、92。啊那数学、英语都是一样。那通过这个字典的key来指定这一列的名字是什么，啊就是这个表头的名字是什么；那通过这一块的这个value，也就是这个value当中的这个字典来描述那这一列的数据长什么样。好，那么这是第二种方式。

那么第三种方式呢，呃就是通过一个列表来维护表中的数据。然后呢，呃里面有多行，那每一行呢可以通过一个元组来描述。你比如：张三 85 92 78，李四 78 88 95。那张三是什么？这个85、92、78分别是什么成绩呢？好，那这个时候呢，我们在构造这个DataFrame这个对象的时候啊，可以在这里面指定这么一个参数叫columns，是不是就类似于是一个关键字参数啊？啊然后columns当中呢指定这么一个列表来指定。噢那前面指定的这些数据那每一列代表什么意思？你看，第一个数据代表的是姓名，第二个数据代表的是语文成绩，第三个是数学成绩，第四个是英语成绩。好，这样呢就OK了。

啊当然除此之外还有一种方式啊。那么在第三种方式当中啊，这个列表当中你看每一行数据是不是通过一个元组来描述的？那么其实也可以通过一个列表来描述。你看外层是个列表，里面又套了一个列表。那里面的这每一个列表呢，其实代表的就是一行数据。然后通过columns来指定那么里面的数据代表什么意思，具体有哪几列。而这个index它代表的是我们手动的为这三行数据指定了一个索引，索引不再是默认的0 1 2 3 4 5 6，而是我们指定的a b c。那就相当于是第一行是a，第二行是b，第三行是c，就相当于我们自己去设置了一下这个index索引。好，那么这是DataFrame在进行创建的时候啊构造的多种方式，啊哪一种方式都可以。

然后紧接着呢，我们在这块再来说一下，那么关于DataFrame这个对象当中啊涉及到的一些常见的属性。呃首先第一个属性呢就是index啊，呃这个df指的就是DataFrame，啊.index。那属性的话后面不用加括号啊，直接df.index就拿到了这个索引了。那如果我们想拿到这个表头，也就是拿到具体有哪些列，可以直接调用columns这个属性来获取。然后紧接着继续来看，那么我们如果想拿到这个表格当中的数据，那这个时候就可以通过df.values，就可以拿到这个表格当中的数据，啊就拿到里面每一个单元格对应的数据。好，那么除此之外还有这么三个属性啊，一个是dtypes，这是获取每一列的类型。啊那要想获取每一列的类型就可以通过dtypes来获取。那还有一个叫size，size是获取到这个表格当中单元格的数量。那么所谓单元格的数量呢指的就是啊一共有多少行，然后再乘以多少列，那就是多少个单元格。你看，这是不是就一个单元格？这是一个单元格，这是一个单元格，这是一个单元格。那么对于当前这个DataFrame来说，那就是3*4=12个单元格。那最后一个属性呢叫shape，啊shape是获取数据的维度的，那么最后给我们返回的呢是一个元组，啊在这个元组当中呢有两个值，一个是行，一个是列。啊那第一个数字代表这个DataFrame当中有多少行，那第二个数字代表这个DataFrame当中有多少列。好的，那这是DataFrame这块常见的创建方式以及常见的属性。

好，那么简单介绍之后，接下来呢我们就来快速演示一下。来，我直接打开我的PyCharm。那么打开PyCharm之后大家会看到，在第05章这个目录下，那我这里呢又创建了这么一个Jupyter文件啊，03这个文件。好，那么在这里我也准备好了一段代码，因为这个代码比较简单啊，我就提前把数据准备好了，大家来看。那么创建DataFrame的方式，啊那这就是我们刚才介绍的几种方式啊。这是方式一，你看方式一创建DataFrame的时候里面是不是传递一个列表？列表当中呢啊一个一个的字典，那一个字典就代表其中的一行数据。那最终这个DataFrame当中到底有多少列，其实就是这个字典当中的这个key的名字，是不是就是它的列呀？好，那么这个时候呢我们可以在最下面啊在这块来df1直接将这个DataFrame输出一下。那这个其实和我们刚才的入门程序是一样的。好，我们在这块直接按Ctrl+回车。大家来看，那么一共有这么四列啊：姓名、语文、英语、数学。那么一共有三行，那索引分别为0 1 2。如果没有指定索引，索引默认就从0开始。

然后紧接着第二种方式呢，啊就是基于一个大的字典来维护这坨数据。好，然后里面那这个key指的就是DataFrame当中的有哪些列，那具体的值呢就封装在这个列表当中。好，我们在这块再来输出一下呃DataFrame2啊，df2来看一下效果，Ctrl+回车。大家会看到效果呢是一样的。

那么第三种方式呢，呃就是通过一个大的列表套着一个小列表来实现。那里面的每一个小列表就是一行数据。那么第一个数据代表什么意思呢？具体的列名叫什么呢？可以通过columns来指定。哎你看columns里面这一块指定一个列表，第一列是姓名，第二列呢是语文，第三列英语，第四列数学。好，我们在这块输出一下DataFrame3看一下效果啊。来，大家来看，效果呢是一样的。啊那如果说啊呃我在这块我换一下，比如这块稍微换一下啊。那这块我来一个name，姓名。呃这块来一个chinese。你这个列名想叫什么，你就在这个columns对应的这个列表当中来指定就行了。比如这块再来一个english，好。那数学呢，在这块再来一个math。然后紧接着大家再来看，我Ctrl+回车，你看列名是不是就发生变化了？哎通过columns来指定列名，你想叫什么，在这块指定就可以。

好，那么紧接着再往下，再来看一下DataFrame4啊。呃那么4呢其实和3这一块是比较类似的，只不过在这里呢多了一个属性叫index。那index呢是索引，好，就是我们可以指定索引。那前面默认没有指定索引，那默认就是从0开始的。那如果你想指定索引，你直接在这块指定就行了。你看我们指定index这个参数对应的值呢是一个列表，里面1 2 3。有多少行数据你就指定多少个索引，你看1 2 3。那这个时候你再看，我输出df4，走。大家会看到前面的这个index索引是不是就是我们指定的？哎当然这里呢我们也可以在这块指定，啊你比如我在这块指定一个a，这块再来一个b，啊还有一个是c。好，这个时候我Ctrl+回车，你看a b c。啊你指定的索引是什么，那这个DataFrame当中的索引啊就是什么样子的。

呃当然对于这块DataFrame的构造方式有很多啊，那这些构造方式呢大家可以不用去记啊。当你鼠标放在这个DataFrame这个上面，然后紧接着往下走，下面这块就有examples，有案例。你看我刚才写的代码其实都是来自于这个官方文档当中的说明，都有对应的样例，对吧？哎在这块怎么指定index，这块怎么指定，对吧？那么除此之外你看，除了这块可以传递这个列表以外，你看这个列表当中除了可以套列表以外，列表当中还可以套小括号括起来的这是什么？哎这是元组，对吧？哎多种多样，这一块都会有对应的样例。你比如，我把这一块换成什么？那我把这一块换成这个元组，换成小括号嘛。小括号是不是就是元组？来，我统一换一下。那后面这一块也换一下。这也是可以的。来，我Ctrl+回车。大家会发现效果是不是一样的？所以这个构建方式呢，多种多样，大家可以不用去记哈。鼠标往这一放，都会有对应的样例。

好，那么构建的方式我们清楚之后，那接下来呢，我们再来介绍一下关于DataFrame当中的常见属性哈。那么关于DataFrame当中的常见属性，我们主要介绍这么几个哈：第一个，index属性；然后第二个呢，是columns属性，是获取所有的列的列名；第三个，获取所有的值是values；然后还有一个获取这个单元格的数量，是size；那还有一个是dtypes，这是获取每一列的类型的；当然除此之外还有一个shape。好，那么接下来呢，我们就挨个来演示一下。

好。首先第一个，在这一块直接.index。注意了，它是属性哈。属性调用的时候你后面不用加括号啊，这是调方法，你看这一块马上是不是就有提示了？哎，那这里呢，将括号移除掉。这是获取属性。好，那么此时呢，我们在这块Ctrl+回车。大家来看。那此时就获取到了这个DataFrame当中的索引。有哪几个值啊？你看：a、b、c。其实它是封装到了这么一个Index的对象当中了。然后对象里面的值呢，就是a、b、c，是不是就拿到了？当然如果说你想将这三个值在这一块封装到一个列表当中，那这个里面其实它提供的有这样的方法哈。你看，有一个叫做tolist，是就转成一个列表。好，那这个时候我们在这块再Ctrl+回车。好，就拿到了这个列表了。你看，这是一个列表，里面是不是有三个元素？好，那这是这一块演示的第一个属性哈，就是index，这是获取索引的。

好，那这一块演示完了，我就将其注释起来。接下来再来演示一下第二个属性，就是columns。来，.columns。直接输出。大家来看。好，这个错误不用管哈。大家来看。那么.columns呢，在这一块拿到的就是里面所有的列的列名。你看有姓名、语文、英语、数学。当然如果说你想转为这个列表，也可以调用这一块的这个tolist，那么就转成了这么一个列表。好，将，这个比较简单哈，我这一块输一下，就是拿到每一列的列名。好，再将其注释起来。

然后，再往下呢，那么在这一块还有一个属性哈，叫values。那values这个属性就是来获取当前这个DataFrame当中所有的值。我们直接看效果。那运行之后，大家来看。那这一块是不是就拿到了所有的值了？哎，当然呢，它是给我们封装到了Pandas底层NumPy当中的这个数组当中了。这个数组是NumPy底层封装的一个数组。那么大家来看，那里面这是一行数据，这是一行数据，这是一行数据，是不是一共三行数据？哎，当然在这里呢，大家也可以去调用tolist，对吧？将其转成一个列表。你看大列表里面套了三个小的列表，那每个列表呢，其实就是其中的一行数据。好，那这是values，就是拿到里面具体的值。

好，那么再往下，那么在这一块还有一个是size哈。那么这个size呢，获取到的就是单元格的数量，元素的个数。你看，12。3行4列，3×4=12，对吧？这个比较简单哈。

好，那么在这一块再来最后两个哈。来，这两个快速看一下。一个叫做dtypes。那么dtypes就是来获取每一列的类型。大家来看。那么这一块类型，你看，姓名那个什么类型啊？object。那注意了，这个姓名的这个类型它是NumPy当中的类型哈，它不是我们Python当中的原始的类型。因为Pandas底层它是基于这个NumPy的。那么在NumPy当中呢，其实它在早期是没有字符串类型的。字符串其实也归到这个object，也就是对象类型了。然后对于数字类型呢，如果是大的整型类型的数字，那就是int64，就代表是整型类型的。好，这个呢大家知道一下哈。来，我在后面在这一块加上一个注释：这个是数据类型。

好，最后再演示一个，就是这个shape，获取数据的维度。好，后面写一下注释哈，这是获取数据的维度。那么最终它所输出的这个结果呢，就是这个DataFrame当中有多少行，有多少列。返回的是一个元组。第一个数代表的是行，第二个数代表的是列。那大家应该能猜到这个shape属性拿到结果是多少？来，在弹幕当中刷出来。哎，对了，是(3, 4)。来，我们这块回车。大家来看，(3, 4)，3行4列。好嘞，那这就是我们这块所演示的DataFrame当中常见的这些属性。

好，那到此呢，关于DataFrame这一块常见的构造方式以及常见的属性呢，我们就演示完毕了。那么下一小节呢，我们再来介绍一下另外一个核心对象：Series。黑马程序员，为千万学生少走弯路，我们一直在努力。

好的，那紧接着呢，我们再来剖析一下这个Series啊。那这个Series呢，前面我们提到这就像类似于DataFrame这个表格当中的一列，任何一列单独拎出来都是一个Series。那么其实呢，我们也可以自己来独立地构建这个Series对象，我们来看一下怎么创建。那么创建Series对象呢，我们就可以通过Pandas当中提供的Series这个类来创建。然后在创建Series对象的时候，里面在这一块就来传递那这个Series当中啊它存放的数据是什么样子的。那这个列表当中的这个数据呢，就是Series这一列当中的数据。好，当然对于Series来说呢，它也有索引，你如果没有指定索引，默认就是从0开始。

好，那紧接着第二种创建Series的方式呢，就是在这个里面啊我们传递数据之后也可以再指定一个index。那么传递数据我们可以通过列表传递，也可以通过一个元组来传递。那index当中指定的呢，那就是每一个数据对应的索引。那最终所构建出来的Series呢，它的结构就是这样子的：里面的数据呢就是10、20、30、40、50，对应的索引就是a、b、c、d、e，那就是我们在这一块手动指定的这个index对应的这个索引。好，那这是第二种构建方式。

那第三种构建方式呢，就是我们可以在这一块直接传递一个字典。那么字典大家都知道，里面存储的就是key:value键值对类型的数据。那这里的这个键呢，其实就是这个Series的索引值。那这个值呢，就是这个Series啊里面存储的这一个单元格里面的数据。你看，10对应的索引是a，20对应的索引是b，30对应的索引是c，40是d，50是e。好，最终就构建出来了这样一个Series结构。

好，当然除此之外呢，我们也可以通过DataFrame当中通过列名来获取出这一列的数据，那最终也构建出来一个Series。好，那么这是Series的创建方式。

然后紧接着呢，我们再来介绍一下Series当中常见的属性。那第一个属性呢，与刚才我们介绍的DataFrame是类似的哈，就是index，获取索引。那第二个呢，就是values，获取值。那紧接着第三个呢，是dtype，获取数据类型的。然后紧接着还有一个是size，是获取数据个数的。好，那其实对Series来说，它就只有一列，那获取的就是那这一列当中有多少个数据。好，最后一个叫shape，它是获取数据维度的。那么由于Series它只有一列，所以它所获取来的数据维度呢，里面就只有一个，就是这个行，对吧？具体里面有多少行数据。那逗号之后后面是没有值的，因为Series它里面就只有一列，所以获取到的就是多少行。好，那么这是Series它的构建方式以及常见属性。

那么接下来呢，我们还是打开PyCharm来演示一下。来，回到PyCharm，在PyCharm当中啊我这一块已经构造好了这样几个Series对象哈，s1、s2、s3。首先来看第一个。那么第一个s1，我们构建的这个Series，呃我们是不是就传递了一个列表？列表当中就传递了五个数啊？哎，那这就是这个Series这一列它里面的数据。那没有指定索引，默认就是从0开始。来，我们这一块直接输出一下这个s1看效果哈，大家来看。呃那么这个里面值，你看，这一列是值，前面就是索引，啊0、1、2、3、4。好，那这是第一种方式。

那么第二种方式呢，就是我们指定了这一列的值之后，再指定一个index，那index指定的就是索引的值。而前面这一块呢你可以使用一个元组也可以，使用列表也是OK的。好，我们在这一块再输出一下s2看效果哈。大家来看。你看，值是不是就这个？哎，那么对应的就是这一列。那前面这个就是索引，就是index这个参数所指定的这个列表当中的数据，就是索引。

那么紧接着再来看一下第三种方式。那么第三种方式其实是它把值和索引给我们封装到一个字典当中传递进来了。那这个key呢就是索引，这个value就是对应的值。来，输出s3看效果。你看，也是一样。好，前面的a、b、c、d、e就是这一块的这个key，那后面的这个值就是这个Series当中所存储的数据，这一列所存储的数据。

好，那么这是创建Series的三种方式。那么其实对于Series来说呢，它还有一种方式。就是我们前面提到 DataFrame当中的任意一列拎出来是不是都是一个Series啊？啊那大家来看，前面我们在这块是不是创建了这么多的DataFrame啊？那我们就从df4这个DataFrame当中啊来获取语文成绩这列哈。那这个我们刚才在入门程序当中演示过，就直接df4在这块来个中括号，那这个里面来个语文。啊就直接写列名就可以了。好，那么此时呢，在下面这块我再输出一下这个s4。来，回车。大家来看。那这块是不是就拿到了语文成绩这列啊？然后前面的这个索引怎么是a b c，怎么不是0 1 2呢？呃因为对于df4来说啊，你看它的索引我们手动设定了，是不是a b c啊？哎这是没问题的。

好，那么其实对于这个s4来说啊，那么这块其实还有一种方式，但这種方式呢呃大家了解一下哈，不怎么推荐。啊怎么写呢？就df4点，点后面写上列名，啊这块直接点语文就可以了。来，我们这块Ctrl+回车，你会发现效果一样。那么如果你在这块想获取到数学这一列对应的Series，点数学。你看，也是可以的。啊但是我们这块一般推荐啊，你在这个中括号里面写上这一列的名字。

好，那这是创建Series。然后紧接着呢，我们在这块再来介绍一下，那么Series当中的常见属性哈。呃那么Series当中的常见属性呢有这么几个，第一个index，第二个values，第三个呢就是这块的这个size，第四个呃shape。对吧？啊当然这个dtype也可以获取到，那么这一列它对应的这个类型，那还有一个就是shape。好，那我们快速演示一下哈。那其实和我们刚才所介绍的DataFrame当中的这几个属性类似哈，来，我们快速看一下。第一个index，那么index获取到的就是索引。来，我们这块换成s3啊，因为这里面数据会多一点。你看，获取到的就是索引。好，我们在这块加上一个注释。

然后紧接着呢，在这块再来看一下，那么values这个属性。那values这个属性它是来获取值的。来，我们这块Ctrl+回车。所有的，那么默认情况下，它是不是只会输出最后一个表达式对应的值啊？哎上面这个表达式的值它就不再输出了哈。好，你看，这就拿到了这个Series里面的值，是不是转换成了一个列表？

好，然后往下走就是size，元素的个数是五个，没问题。然后在往下走呢，dtype，那dtype它是用来获取对应的数据类型的，就是Series的数据类型。啊什么类型呢？看这，int64，这个其实指代的就是我们当前的类型是一个长整型，啊是一个整数类型。呃这个int64啊我再给大家说一下，呃这个是NumPy当中的类型哈，因为Pandas底层是基于NumPy的。

好，然后往下最后一个就是shape。那shape呢是获取数据的维度。对于Series来说，这个数据的维度呢其实就只有一个行，列都是固定的，1。啊所以最终它输出的结果，输出出来的就是行。来，我们这块直接回车，回车之后你看，5，逗号，它是个元组嘛，5，逗号，后面这一块没写，那这个5代表的就是行数。

好嘞，那这就是创建Series的方式以及Series当中的常见属性。好，那么这一块我们介绍完毕之后，最后呢我们就回到PPT，简单来做一个小结。那么主要明确一个问题就是DataFrame与Series它的创建方式以及常见属性。那么对于DataFrame和Series的创建方式呢，这个大家不用去记哈。那这里呢大家直接在我们的PyCharm这样的开发工具当中，你把鼠标悬浮在DataFrame或者是Series上，是不是就可以看到官方给的一些样例啊？哎在官方给的样例当中啊都有说明。然后对于这里的常见属性呢，其实大家会发现DataFrame和Series当中的常见属性，属性名基本都是一样的。啊包括像index获取索引，values获取值，dtype获取数据类型，那么size呢获取数据的个数，shape获取数据的维度。当然对于Series来说，它获取数据的维度返回的其实只有一个行，因为对于Series来说它只有一列，啊是固定的。而还有一个是columns，啊columns是获取列名的。啊那这个是DataFrame当中的属性，是获取DataFrame这个列表当中的所有的列的列名。

好的，那这就是Pandas当中的两大核心对象：DataFrame与Series。黑马程序员为千万学生少走弯路，我们一直在努力。

好的，那上一小节呢，我们完成了Pandas的入门程序，并详细地剖析了一下Pandas当中的两大核心对象：DataFrame与Series。那刚才呢我们在入门程序当中，我们在演示DataFrame以及Series的时候啊，那它里面所存数的数据呢，是我们在代码当中直接定义死的，直接写死的。但是在企业级的项目开发当中啊，那进行数据分析，那这块的数据来源一般都是来源于我们的文件或者是数据库，或者是从网上所抓取到的一些数据。好，那这个时候呢，我们在进行数据分析之前，我们就需要将文件或者是数据库当中的数据啊给它读取出来，然后呢再进行数据的处理、数据的清洗，最终如果需要的话，我们可以将数据清洗后的结果再存入到另外一份文件当中。

哎所以接下来这一小节呢，我们就来讲解一下Pandas当中数据的读取与写入。那么在Pandas当中啊给我们提供了对应的API，让我们可以在很方便的来灵活地读取各类数据文件，啊包括读取CSV文件，包括读取Excel文件、数据库的文件、网络数据的文件都是可以的。那我们来看一下具体怎么操作啊。

那么在Pandas当中呢就给我们提供了两类方法，那一类方法呢就是来读取文件的，另外一类方法就是写入文件的。呃那读取文件的方法呢，开头啊都是read开头。啊read_，后面在这块跟上对应的一些后缀。比如我们如果想读取这份CSV格式的文件，那就直接read_csv。那么此时呢就会将这份CSV文件当中的数据读取出来，然后封装到一个DataFrame当中。啊因为CSV啊Excel里面封装的是不是就是表格类型的数据啊？哎它是可以直接读取出来，然后封装到DataFrame当中的。然后接下来呢，就基于DataFrame来对我们的数据进行清洗、处理、分析。好，那么处理完毕之后的数据呢，如果说我们需要啊可以直接将其写入到另外一份文件当中。那这个时候如果说你需要将数据再写入到文件当中，那需要再去调用另外的一个方法，叫做to开头的方法。比如to_csv，那指的就是将我们处理完毕之后的数据再写到CSV文件当中。当然这块呢有read_csv也有read_excel，也有to_excel，都有对应的方法来操作不同类型的文件。

啊比如，大家来看。那这里呢，我们要想去读取某一个CSV文件，啊那这里呢就直接调用Pandas当中提供的这个read_csv这个函数就可以了。啊它就会将这个指定文件当中的这个数据读取出来，并且自动地给我们封装到DataFrame这个类型的对象当中。然后接下来呢，我们就可以基于DataFrame这个类型的对象对数据进行进一步的处理了。那处理完毕之后，如果说需要将处理后的结果保存起来，那这个时候我们就可以再去调用to_csv或者to_excel这样的方法，将处理之后的文件再保存到一个新的文件当中。好，那这就是Pandas当中进行数据的读取以及写入的方式。啊其实大家会发现是不是就是两类方法？一类是read开头的用来读取，另外一类是to开头的用来写入。

好，那么我们简单介绍之后，接下来呢我们就以CSV格式的文件为例来演示一下。来，这里呢我就直接打开我的课程资料啊。那么在给大家提供的课程资料当中，那么第五章这个文件夹下啊，那么这块有一个资料这么一个目录。资料目录下有一个素材，这里我提供了两份CSV文件。那现在呢我们可以将这两份CSV文件一并拷贝过来哈。来，我Ctrl+C复制一下。复制之后直接打开PyCharm。复制过来之后放哪儿呢？呃在第五章这个目录下我在这块再新建一个目录，啊那这个目录呢我们就叫data，就是用来存放我们的一些数据的。来，Ctrl+V直接粘贴进来。

好，那这两份CSV文件我是不是就复制过来了？啊当然这两份CSV文件里面啊有大量的数据哈。那一份呢是movies.csv，这份数据其实就是我们之前在上一章节，我们基于网络机器人是不是抓取下来的数据啊？啊当然这个抓取的数据我抓取的比较多哈，呃抓取了Top200我记得是。啊不只哈，是Top 300，啊也就是前300个高分电影的数据。那还有一个是sales.csv，那sales.csv呢这个里面大概有个1000条左右的数据。那这1000条数据呢是呃某商城它的一个订单的数据，啊当然这都是我构造的一些测试数据哈。那么接下来呢，我就以这个sales.csv为例哈来演示一下，啊看一下怎么读取这个文件。

好。呃那这里呢，我在这一个第五章这个目录下也创建好了一个新的 Jupyter 文件啊，就是04这个文件。那么接下来在这个里面首先 import 将 pandas 先给它导入进来，起一个名字啊就叫 pd。好，那么接下来呢，我们在这块儿来演示一下读取数据啊。好，那么这里我们读取数据就可以直接基于 pandas 当中所提供的一个函数，那这个函数就是 read 开头的函数。大家会发现 read 开头的函数是不是有很多呀？你看这块儿有 read_csv，有 read_excel，这块儿还有什么？read_html，read_json，还有这块儿 read_sql，是不是很多啊？哎，简单说就是 read 开头的这一类的方法它都是用来加载数据、读取数据的。那对应的文件的类型不一样，调用的方法也不一样。如果说大家要去读取 Excel，那就调这个函数，如果说你要读取的是 CSV 就调这个函数 read_csv。

然后里面有非常多的参数啊，你鼠标往这一放就会发现，read_csv 这个函数当中里面的参数是不是非常的多啊？啊，但是你会发现下面这些参数都是有对应的默认值的。那么这里呢，我们唯一需要传递的就是第一个参数，就是文件的路径。啊需要读取哪个文件啊？大家来看。那么当前呢我们是在第五章这个目录下，那我要读取的是不是 data 这个目录下的 sales.csv？所以这块怎么写啊？哎，直接来 data 这个目录下的 sales.csv，它是有提示的。好，这样呢，就可以了。

好，读取完毕之后，它最终啊会将我们的数据给我们封装到一个 DataFrame 当中。来，我这一块儿直接拿一个 df 来接收就可以了。然后紧接着我就将这个 df 直接输出出来，大家看一下效果哈。来，我 ctrl+回车。好，我们稍微等待一下，因为数据量比较大。大家来看，有多少条数据？1000 rows，然后乘以 8 columns，啥意思？一共有1000行，有几列？有8列。啊，所有的数据都加载出来了。那么由于这个 DataFrame 当中的数据量比较大，那所以呢在 Jupyter 当中啊你会发现，哎它只是展示了前面的5行，后面的5行，中间你会发现很智能哈，给你省略了，要不然你看一个数据一直往下滚一直往下滚一直滚到底。那这样呢，我们就将数据加载出来了。你看，输出的 DataFrame，这个0开始的这是不是索引？那后面一共是这么8列。

好，那接下来呢我们继续来看。那么如果说我们在读取数据的时候啊，那么我只需要加载其中的几项数据，比如这里呢我就只想去获取这里的订单号、产品类别、产品名称以及销售数量和单价，这个时候怎么做呢？后面这三列我不需要，如果你不指定你会发现默认这个 CSV 文件当中所有的数据是不是都给我们读取出来了？好，那你要用到哪几列，你可以在这块儿限定一下。在后面在这块儿加上这么一个关键字参数，叫做 usecols，这个 cols 其实是这个单词哈，是 columns 这个单词的简写。就是列的意思。好，use 用，你用哪几列？你在这块儿指定一下 usecols。好，那在这块儿指定一下，我要用哪几列啊？我这里呢要用订单号。啊指定一个列表哈，因为我们也可以指定多个列名嘛，所以这块儿来个列表。我要用订单号，我要用产品的类别还有产品的名称，对吧？好，然后，再往后走，还有一个是这里的销售的数量，然后再往后走还有什么？还有这里的单价。好，这就可以了。那这就代表我只需要这个 CSV 文件当中的这五列。好，我们再看一下效果。来，我们这块儿直接按 ctrl+回车，走。大家来看，这五列是不是就拿到了？

好，这就是读取数据。好，那么数据读取到了之后，接下来呢我们就可以进行数据的处理了哈。来，在这块儿，啊数据的处理。呃我在这块儿数据处理啊这个我们后面会详细讲哈，我在这块儿简单举个例子，大家来看。呃那么我现在如果想知道每个订单号对应的金额是多少？那怎么获取到呢？你看，那这个 DataFrame 当中每个订单号它销售了多少件这个商品对吧？比如销售了10个枕头，那每个枕头的单价是多少？我如果想获取到这个订单号的一个销售额，怎么做？这两个一乘是不是就完事儿了？好，那么接下来我们就看一下，那么在 DataFrame 当中我能不能为这个 DataFrame 动态的来增加一列呢？啊是可以的。来看一下怎么做啊。来，我在这块可以直接 df，那你现在要增加的列，列名是什么？直接在这个方括号里面去写就行了。那我们在这块儿要增加的列是什么呀？是销售金额哈，在这块儿写一下，销售金额。好，那么你要增加这一列，你就需要为这一列赋值。这一列的值是从哪儿来的？这一列的值是不是就是这一列销售数量乘以销售的这个单价呀？是不是就这两列相乘就完事儿了？所以你直接为它赋值就等于，好，AI 在这块儿已经提示出来了哈。就是让我们销售数量这一列，然后怎么着？再乘以单价这一列就可以了。来，我复制一下，把里面的这个列名换成单价。这样呢，就 OK 了。那么此时在我们的 df 也就是在这个 DataFrame 当中啊，它就会再增加一列，就是销售金额。那销售金额的值就是这两个相乘。来，我们试一下，看后面会不会再增加一列，列名就叫销售金额。看一下行，还是不行？来，ctrl+回车，大家看好了，销售金额有了没有？有了。大家会发现基于 pandas 进行数据处理是不是很方便啊？哎这两列相乘就得到了一个新的列，新的列在 DataFrame 当中就有了。

好，那现在最后一点，呃我现在需要写入数据。所谓写入数据指的就是数据处理完了对吧？我们这块儿只是模拟了一下数据处理。数据处理完了，我现在需要将新的这个 DataFrame 当中的数据，我要写到一个新的 CSV 文件当中，怎么做呢？好，那么这个时候大家来看，数据现在在哪儿存着？是不是在这个 df DataFrame 当中存着呢？来，大家告诉我，我刚才在这块儿介绍的时候我们说了，读取数据要用 read 开头的方法，操作 CSV 那就 read_csv。现在来告诉我，写入数据用什么方法？弹幕当中刷出来。哎，对了，写入数据，那这块儿要用的是 to_csv。你看 to 就是到，写到哪儿去啊？写到 CSV 文件当中。这个时候直接来 df.to，你看。我调一个 to 开头的这个方法，你会发现是不是有很多啊？哎你可以写到各种各样的文件当中，你是写到 Excel 当中还是写到 CSV 文件当中，我们这块儿就基于 CSV 来操作，to_csv。好，然后紧接着呢，我们在这块儿直接采纳 AI 给我们的建议哈。呃第一个写入的文件，文件名叫什么呀？叫 sales 我们在这块儿就叫 02 吧。啊 sales01，01 啊，那这块儿目前这个是 sales，那就从 01 来，sales01。这就可以了。

然后紧接着我在这块儿说一下这个参数哈，index=False。啊那么这个 index=False 这个参数它所表示的含义就是索引列我是不写入这个文件的。什么叫索引列？就是前面这块儿我不写入。我只写入后面的数据列，索引列不写入。好，这样就可以了。好，那么接下来呢，我们就可以来做一个测试。来，我直接 ctrl+Enter 回车。好，搞定。那这块儿报黄啊是因为你现在写的这个语句是不起作用的，它也不会输出，对吧？它只会让最后一个语句生效。好，我们在这块儿把这个就删掉了。那搞定之后大家会发现 data 目录下怎么没有出现这个 sales01.csv 这个文件呢？呃这个时候大家需要在这块儿点击一下右键哈，鼠标右键，要从磁盘当中加载，就刷新一下就有了。你看，sales01.csv。现在我双击直接打开，打开之后大家会发现这个里面它所存放的数据是不是就是刚才这个 DataFrame 当中的数据？又增加了一列销售金额，你看销售金额，是不是就有了？好勒。

那么这就是在 pandas 当中进行数据读取以及数据写入的方式。那么其实核心呢就是两个方法，一个是 read_csv，一个是 to_csv。当然呢这是两类方法哈。那么我们当前操作的是 CSV 文件，所以后缀都是 .csv。那如果大家要操作 Excel 文件，那么后缀都是 Excel。好，那么这样呢，关于数据的读取以及写入这块儿的基本操作呢，我们就演示完了。最后呢，我们再回到 PPT 简单做一个小结。啊那么这小节其实大家最核心的呢就是掌握两个方法。一个是数据读取的方法是 read 开头的，一个是数据写入的方法是 to 开头的。啊比如 read_csv，to_csv，read_excel，to_excel。

而这里呢有一个小小的注意事项需要给大家提一下。那如果大家要操作的是 Excel 或者是一些其他格式的文件，那么大家执行的时候，第一次执行的时候可能会报错。那么报错的信息啊就是告诉你，你要操作这个 Excel 或者操作其他的这个格式的文件，你可能需要去安装相应的这个软件报。啊那么这个时候第一次执行会报错，大家直接把你的这个控制台就是输出的这个结果往下拽，基本上都会见到有这么一个错，啊告诉你什么模块儿没有被发现，这个时候大家你要做的是什么呢？啊你就是点击一下这块。点一下，它就会自动下载对应的软件包。啊然后呢再次运行，就可以成功了。啊当然对CSV这一块来说，因为CSV它就是一个文本格式的文件，那么对于我们来说你是不用额外安装的。啊那么操作Excel啊以及其他文件，可能会有这样的情况。大家就点击一下这儿，让它自动下载对应的软件包，就可以了。好嘞，那么接下来呢，大家就可以暂停视频，抓紧时间练习一下吧。黑马程序员，为千万学生少走弯路，我们一直在努力。