符合 Unix 的设计。所以
你们猜这个 Git 的设计者是谁对吧？
是 Linus 对吧？是 是 Linux 的发明人。
你就觉这是一个非常 非常 非常
漂亮的设计。在这之前大家用的是 SVN，你们没有经历过那个时代，我经历过那个时代，就是都要 SVN check in、check out，然后呢 SVN 难用得像什么什么一样。
直到 Git 来了以后这个世界就 就完全变了。GitHub 现在就是这个事实的
标准的...他有一个非常干净的设计，但这个 这个干净的设计又能够承载
像未来更多的需求。比如说现在比如说 GitHub 这个 Git LFS 对吧？然后有 worktree，我一会儿会
会讲到这些机制解决 解决这样的一个简单模型，比如说对大文件处理不好的
这种 这种问题。但这些都是 都是小的问题。
这个模型对于代码来说简直就是太合适了对吧，小的细碎的
对象。
这 这是 这是分支和提交，然后 HEAD 是指针的指针。还有一个
有一个叫 Git 的工作区 stash。
你 你现在有可能会有一些
对吧？
你现在有一些就是 ongoing 的修改。
然后你可以把这些东西暂 暂 暂存到你的 stash。如果有有些这个
ongoing 的修改，
但是我不想把它 commit 到我的分支上，
对吧？因为他可能是 是 是是不对的，
做到一半的。那这时候我就可以
stash push，
我就可以看到这个 stash 列表，然后我可以不断地往里面 push。然后这个 stash 其实也是一个
commit object。
所以我 我可以做的是 echo，
他有一个 working on, working in progress on niuniu 对吧？
我可以找一个，
哎，找到了。
你看，
这是一个 stash object。
它其实是一个 commit。
对吧？你看它有一个 tree，然后它有两个 parent。
好，所有的，其实 这就是数据结构嘛。
只不过
这个数据结构的指针，你们在平时实现一个什么 tree、binary search tree 的时候，那个指针你都是一个什么 struct node 的星 left，星 right。
然后这里的所有指针都是一个
这个 hash，一模一样的。
就是一个数据结构。然后
数据结构有不同类型的节点，然后节点上面有你自己想要放进去的
data，对。
然后 stash commit 有两个 parent，一个是 HEAD，一个是 next stash，所以它变成一个 stash 的 stack，所以它可以 push 和 pop，这就把 commit 给串起来了。就这么简单。
就这么简单。然后你 也可以让 AI 去
直接啊，你 你任何一个，比如你们
比如你们自己的一个 commit 的 history，
你都可以直接让 AI 去帮你读，对吧？直接让你把它的所有 objects 都给
都给读出来。它 它会自己会用正确的工具的。
对，然后就可以 你可以让它画比如那种 Mermaid 的那种，就代码的图，也可以直接让它生成 SVG。
总之你可以得到一个很不错的
流程图或者原理图帮助你去理解你的
project 到底
到底发生了什么。
对，在这我在这我就不读了，但是你可以看到的是一个非常还不错的
对象图对吧？还有一些关键的
关键的
结构。
好。
这是 Git。我相信大家都对 Git 有兴趣，然后也
应该是倒在觉得哎呀这个东西是不是比较复杂。
对吧，然后你看看文档，就是那几个最基本的掌握了以后你发现基本上又可以工作了。
然后你就 可能不会再想要深深深入一步进去看一下。但是当你真的把这只脚在 AI 的帮助下踏进去，你就发现这个世界其实是如此的干净和 和简单，这就是好的 好的设计。
好。
当然了，Git 作为一个
如果问 问什么是 Git 的话，你还是给它的
定义会是更好。它是一个分布式的版本管理的一个系统对吧？它管理了很多目录树的快照。所以
刚才我们说它有 commit 对吧？它有 commit。
因为我可以做分支，
所以我可以在一个刚才知识实际上是一个 commit object 对吧？
commit object。当然 commit 是对应里有一个 tree，
commit 对应了一个 tree。
那我就有可能会在一个 tree 上面，比如说在一台机器上发展出来 A 和 B
两个 两个 commit 或者两个 tree。然后在另外一台机器上有 C 和 D 两个人开发的。
那这个时候，
我就不再是一个线性的历史了。
对吧？不再是线性的历史，那 Git 就会做 做合并。然后这是一个
基本上 基本上你 你用这个模型就可以理解 理解 Git 合并的全部了。它合并有三种。
最基础的
merge，
就是我添加一个 E 节点。
其实这也是你们在，比如说和我的 和我的那个代码合并的时候，对吧？你们本地已经 main 已经往前走了很 很远了，然后我 我在 我 我 我是一个很早期的 main，
对吧，你们看到我是一个非常早期，我的 main
是一个非常早期的提交。
然后我 我会在这上面做 M1，
对吧，一个 M1 的分支，M1 的提交，M2 的提交。
对吧，就是这个模型。你们 你们会遇到这个模型。然后你们会从远端拉取一个分支的时候，那，
OK，
你会创建一个节点 E，然后 E 它就会有两个 parent，B 和 D。然后这个时候他做的是一个三方的合并，有一个是公共的祖先是一个 tree，这是一个 tree，
B 是一个 tree，D 是一个 tree。中间的这些你都可以，如果有 100 10000 个提交还是 10 万个提交它都展平了。他我只看这个 common ancestor、B 和 D，然后合出一个 E 来。这个是 这是比较 比较直 直观的情况。
然后，
如果你的本地没有 A 和 B，
如果你的本地是长这样的，
就是在 在你的远端有 C 和 D，
你就可以直接把 C 和 D 拉过来。
哎，这两个就同步了。
你本地也是 也是这个。这个就叫 fast-forward。你们有的时候去别的 Git repo 拉东西的时候会看到它有一个 message 说这是 fast-forward。这就相当于说是
我 我 我 我这边的东西都没看见，我就把它放过来。
但是，
如果
你这边有 A 和 B，
你又不想创建一个 E，
你有办法吗？
对吧，就标准版做法当然是因为 因为已经打 merge 了，所以你要创建一个新的 新的提交不仅代表它的 merge，然后你 merge 完了以后他会...
对，你其实 其实我们也是可以在这里看到 看到的，比如说，
我 我可以特别的 no-fast-forward 对吧？我就合一个 niuniu。
然后这个时候他就要求我
有一个 创建一个新的 commit 提交的对象。
OK，然后就 merge 了。如果我看一个
看到这个
新的提交的话，哎，
它就有两个 parent 了。
对吧？他就 他就有两个 parent。它有一个 parent 是 531，
是这个。还有一个就是
远端的那个提交，就相当于做一个这样的
这样 这样的 merge。
当然也 我们也 也是可以在 在这里，
就是比如说，
这里，对吧？你可以看到
merge branch niuniu into new 的时候，你可以看到还有两个 parent。
就 就是 就是我刚才说的那个 那个创建个 E 的 E 的情况。
如果你不想创建这个 E，
也是有办法的。
这里就要用到一个叫 git cherry-pick
的功能。
git cherry-pick 是个非常好用的功能啊。比如说你 你有一段，它可以 cherry-pick 任何提交。
打比方，
你 你现在啊现在
你 你在
你在本地复制了一份 Linux 内核，然后这可能是你 10 年前复制的。
然后 10 年前复制的时候你已 在 在 10 年的过程中 Linux 内核已经发生了翻天覆地的变化。你已经不可能跟它合并了。
因为你也改了，他也改了，然后你要解决所有的冲突这 这是不可能的。
但是，
比如说 Linux 内核里面有一个地方给一个很老的代码打了一个安全补丁，
这个安全补丁跟你是没有冲突的，
你就把想把它拉过来，
你就可以用 git cherry-pick。
然后所以本质上你可以这样想，
你的任何一个提交，比如说一个从 A 到 B 的提交，
它
都是
都是一个 diff 对吧？
你从 A apply 一个 AB 的 diff 就到了 B。
你 你可以这样看对吧？你可以想 A 是一个 tree，B 是一个 tree，你也可以 你可以记录 A 和 AB 之间的 diff，然后你就可以把 AB 之间的 diff 给 cherry-pick 过来。
抄过来。
然后呢我们怎么做一个 rebase 呢？怎么做一个 rebase 呢？rebase 就这样的。
我 我的 rebase
如果我要在这儿啊，
我空间不够了，我画的不是 不是特别的准确啊。
我这样画啊。
A B，好。这个叫 $\Delta 1$，这个叫 $\Delta 2$。
那 rebase 做的事情是先把 C 和 D 复制 复制过来，
先把 C 和 D 复制过来。
然后
他在这个状态上，
试图去 cherry-pick 从 main 到 A 的这个 $\Delta$ diff。
然后它会得到一个 A'。
就好像是这个地方是一个补丁，
对吧？你 你修了一个 feature，如果他们都不冲突的话，哎，比如说 C 和 D 改了 改的文件和 main 和 A 之间改的文件就不是同一个，OK，那这个 $\Delta$ 就可以直接 patch 过来。
对吧？他就 他就 replay $\Delta 1$
就得到 A'，然后再 replay $\Delta 2$
就得到 B'。
然后它就得到了一个
那这个就没有了，这就没有了。得到一个什么假象呢？你在本地的时候，你 你开发的是 A 和 B，也就是你写的是 $\Delta 1$ 和 $\Delta 2$。
然后在远端的时候你看到了 C 和 D。然后他是把远端的插到你的前面去。
就好像是
虽然是你们两是并 并发并行开发的，
但是好像是 C 和 D 远 别人先做完，你是在他的那个基础上接着做的。
这样你就可以省掉一个
那个 merge 的 commit。
把这个东西接到 接到上来了。
但这个机制是 是你要慎用的。就如果你本地的那个提 提交和 和对面没有冲突，比如说改了不同的文件，基本上是没有...rebase 是没有问题的，也可以设置。一般来讲我

就是把rebase设成我的merge的default。但是，如果一旦cherry-pick的发生冲突，基本上你就要放弃rebase，你就还是得merge。而且你会发现的是，它每一个都是replay一个delta，所以你可能resolve了这个冲突以后，你下一个又冲突了。然后如果你在这两条线它diverge的时间比较长的话，你就会会会变得非常非常痛苦，极度痛苦，对。而且即便这个auto-rebase它成功了，也可能隐藏这个逻辑的冲突，你还得回过头来看这个的diff。最好你要review这个diff，再review这个diff，再review这个diff。这三个diff你最好都要，都要review一下，确保它没有在自动合并的时候发生你不想要的事情。所以rebase，rebase是一个非常危险的，危险的操作。好，很好，这就是Git。基本上你你你你就已经超过，就知道这些基本上已经超过90%的Git的用户了，应该。然后我在准备这个幻灯片的时候，我惊人的发现，历史总是在重演。然后Git刚才我们说Git是一个persistent data structure，它是为单线程设计的。也就是说它假设你的开发者就一次做一件事。还记得那个Unix吗？Unix我们说，最开始它就是进程模型，没有线程。然后那只有操作系统里面有共享的状态。一个进程里面它就是一个执行流。然后当我们一个进程变成线程，引入了pthread的时候，我就打开了一个魔鬼的盒子，对吧？我花了那么多节课讲并发，讲并发bug，讲并发bug应该怎么应对。OK，就是为了把进程模型变成线程模型。然后Git也是一个很像是Unix单进程的模型，它只有一个工作区，一个HEAD，你在本地工作的时候你是一个线性的commit。你们自己不自知，你们的在做实验的时候每一次make的时候都是在性地往后加一个，往后加一个，往后加一个。然后直到你比如说你需要跟别人同步的时候，你才会跟别人merge一下。大部分时候这个模型都工作得很好。但是，一旦你开始从单线程变成多线程的时候，事情就麻烦了。你你可以这样想，如果我现在正在正在一个分支上面工作，比如说我现在修到一半，修到一半突然老板给我打电话，说哎呀你你现在手手头的事情不要干了，你切换到另外一个feature分支上，去把那个上面一个高危漏洞给修了。好，那我我应该怎么做呢？我应该stash，对吧？我现在我现在做到一半的事情我不能commit，我得stash，stash变成一个stash。好，然后我再上做个上下文切换，我checkout到那个那个分支上。你有没有感觉到开始并发了，对吧？因为我做了一半的东西被保存下来了。然后我切换到另一个地方工作。然后这时候我又做了一半，老板又打电话来，说你有一个我有一个更高优先级的bug需要你修，那我就没办法，我再stash。然后你发现这个stash是一个stack，对吧，它是一个线性的stack。然后你发现它的设计在设计之初就没有让你多线程工作。所以当你一定要多线程工作的时候，你就会有一个stash hell。虽然还好，这个一般来从人道主义的角度考虑，老板一般不会做这么过分的事，所以你一般有个两三个stash，就同时multitask两三个事情，你可能还能管得过来。我觉得Linus当年在设计这些的时候应该是这样想的。但是但是但是，在Agentic AI的时代又变了。对吧？当我可以一下子放一大堆Agent在同一个项目的时候，在同一个机器上的时候，哎，又变麻烦了。对吧？当然我总是有个办法，就是我可以以进程的方式，我可以复制这个Git repository好多份。对吧？我可以复制Git repository，然后你们在分别工作，然后你在你去GitHub上面做同步。这这肯定是可以的。但是在2015年的时候，就为了解决这个对吧，有multitasking的需求，Git做了一个很有趣的机制叫做worktree。worktree。还记得吗？我们说是，哎我把它擦掉了，persistent data structure。所有的一切历史都是留着的。甚至比如说我在这里可以有一个，算了，甚至我在这里可以有个git，有个git reflog。就是除那就你的git比如说你git reset --hard，那你你那些没有add的那些东西就丢了。但是其他所有的东西，因为它是persistent data structure，所有的对象都留在文件系统里，只要你做git gc。你哪怕有一个commit被，就是你你commit是一个孤，没有任何引用的commit这种，你把它整个都弄弄坏了，因为它是persistent data structure，你的tree object，你的blob object都实际上都在文件系统，你只要你不GC，你都可以捞回来。然后你可以在这个git reflog里面找到所有的历史。所以既然这是一个persistent data structure，我们为什么不能把树直接复制一份呢？没有道理不能让我们把这个整个Git repository假装复制一份。因为复制是没有代价的，我是永远我是永远不修改的，所以我复制一个指，直接只是一个指针指在这个位置就可以了。所以就有了Git的worktree。所以你看，我如果现在啊，现在我在这里目录下，然后我现在我可以这样，做这样，git worktree add。当然我现在add的时候，我就得得再换一个目录了，比如我要换到它它点点下面的。我要起一个分支的名字，比如说hotfix。对吧，我我被老板要求要hotfix。然后在这个时候我就可以创建一个Git的worktree。好。这时候我就有一个hotfix。哎？我这有一个.git。然后但是这个.git，你有没有发现有点不太对劲吗？它不是一个目录哦。刚才那个.git是一个目录，就是那个原始的那个git是一个目录。对吧，目录目录里面有objects，就就那个persistent data structure就存在那个目录里。但worktree不是的，worktree我可以直接cat它。所以你看又是一样的设计，跟那个什么HEAD对吧，一样的。HEAD说是我是个reference，指向了某一个东西。好，他说这个.git说这是一个Git的目录，实际上是，是这个东西。那这个是什么呢？我也可以cd进来对吧？这时候你看到它是在这个worktrees下的，对吧，我的真实的.git的worktrees下的hotfix。啊！一堆指针。对吧？没有object，它不需要存object。因为是persistent data structure，所有的东西都是指针就可以了。所以我只要有一个HEAD，对吧，指向一个合法的HEAD，HEAD再指向一个合法的commit，这件事情就搞定了。这就是persistent data structure带来一个免，完全免费的feature。当然当时是为了解决这个老板不停地给我打电话的这样的这样的需求。但是今天worktree就成了这个Agent时代的一个非常标准的标准的特性。这时候你们会发现你们学那个SICP里面讲的那些functional programming的含金量还在上升，对吧？OK，历史总在重演。好，那我Git部分我就讲完了。你们用得到的feature基本上都都有一个基本概念了，然后你用不到的feature也可以在这个基本概念上再去向AI询问了。那你接下来想，如果Git是一个persistent data structure，persistent data structure这么好，我们能不能把文件系统整个就搞成persistent data structure呢？没有道理不能诶。对吧？所以就有一个，其实这个文件系统也有也有十来年的历史了，甚至更早。大概可能在十几年前的时候你基本上就可以在production里开始用了，它有bug。那时候我们在尝鲜用的时候，各种bug它会把你文件系统整个就搞搞挂了。但是这个idea就是一样的。你可以直接，这个文件系统叫Btrfs。B-tree就是你们在数结课课上面学的那个B-tree。就是一个一个大块一个什么4096叉树，对吧，它不是一个二叉树，它是一个非常多的多叉树。它多叉树这个索引结构它就可以把整个索引的层数压得压得非常浅。然后它就是一个和Git一样，everything is a B-tree。因为它是要在块设备上构建，所以它和Git还不一样。Git还因为有文件系统的抽象了，所以它可以直接往那个往那个.git Git下面的objects里面丢。但它但它不行，它要从零开始实现一个块设备上的persistent data structure。然后它所有的一切，它的volume，它的文件系统目录树，所有的东西都是以append-only，copy-on-write的方式做的persistent。这个文件系统就和Git一样，支持免费的快照。你需要，当然这是文件系统提供的这个功能啊，你只要持有了一个文件描述符，然后你你给它做一个io control，叫BTRFS_IOC_SNAP_CREATE，它就可以创建一个snapshot。而创建snapshot对于持久化的数据结构来说根本就不是什么困难的事，它只是把这个指针给它一个名字记下来。对吧？当所有的当前指向当前的一个root的指针，然后我把这个指针记下来，说我创建了一个快照，然后这个名字是什么，就和Git里面你创建一个新的这个tree object，然后它这个tree object指向了好多的blob object一样。恭喜你，又发明了一个这个改变世界，一项改变世界的技术。我就我就不再讲，因为我不讲操作系统内核，对吧，我向大家承诺我们只讲操作系统应用层的东西。但是你们依然可以看到，即使是说我们讨论一下操作系统的API，哪怕只是增删改查的API，对吧，增删改查的API，你都可以实现非常有趣的应用，像Git这样的应用。好。然后Git之后，我们还有一个你更更让你意想不到的操作。这个idea就比较神奇了，有最早追溯到一篇那个UnionFS的论文。他说，我们的文件系统既然是数据结构，数据结构那当然它是一个目录树对吧，我们还是还是尊重Unix的这个，对吧，/bin，/home，好，它就是一个一个这样的目录树。然后目录树除了最

增删改查监听以外， 你还能不能为他设计和实现一个 更不一样的机制？ 这个很神奇的idea， 它是这样想的： 我现在允许你在任何一个，比如什么刚才tmp里面 有两个目录，一个叫upper， 一个叫lower。 也可以有目录，也可以有文件。那我们简单起见，就比如说有一个a.txt， 有一个b.txt。 好， 呃，upper upper就一个a吧。 然后lower有一个a.txt， low- lower还有一个b.txt。 好。 然后UnionFS想要干一件什么事呢？ 它想要创建一个虚拟的目录， 然后这个虚拟目录随便随- 随- 随便放在哪，这比如这就叫merged。 这个虚拟的目录它 它在访问上，增删改查的意义上，就是一个普通的文件系统上的对象， 哎，就- 就是一个目录。 但是呢， 我可以通过一个系统调用， 在Linux里面是- 是通过mount这个系统调用来，来实行的。 呃，通过这个系统调用呢，我要把 upper 和lower 拼起来， 拼成一个叫merged。 然后你- 你可以从这个名字里面看到，一个叫upper，一个叫lower，所以upper拼在上面， 然后lower拼在下面。 然后这个merged， merged是一个假的， 是一个虚拟的。 然后每当你要问这个merged里面要文件的时候， 它就会先去upper里面看。 如果upper里面有这个文件， 它就会直接返回给merged。 然后如果 upper里面没有这个文件， 它就会去lower里面找， 再返回给merged。 如果lower里面也没有，那这个merged里面也没有。 就相当于它把这两个 目录给叠起来了。 然后叠起来以后就会有一个什么- 什么效果呢？ 我的lower里面可以有好多文件， 比如说是一个系统盘之类东西。 然后upper里面 可以对lower做一个修改。 在upper里面对lower做一个修改，然后修改了以后，lower本身 可以不变。 这样我可以创建好多好多这种merged目录，它就- 就可以有 有趣的应用了。有两个， 就你哪怕只能叠一层lower，叠一层upper， 你- 你也可以 有这样两个killer application。 你有一个刻盘的工具， 这个刻盘的工具可以burn，一次，一次性可以burn一个目录，比如说一个600M的目录， 然后写到一张光盘里。 然后现在 你，你的电脑上面有 16个光驱。 因为16倍你就可以，你有，你挂16个光驱你就可以刻16倍快的 就增加16倍的带宽嘛，对吧？因为假设你的内存和CPU的带宽是 是足够的，然后写光盘是相对，相对比较慢的。 但是， 你必须要用这个刻盘工具的同时，你又需要为每一个光盘写入一个不同的CDKey。 你会生成好多CDKey，然后把它打印出来贴在，比如说贴在光盘上面一起卖。 然后相当于是 我们在lower上面 有600M的文件， 然后这600M文件都是一样的。 但是在这个upper上， 你有一个1M的文件， 对每一个 光盘都是不一样的。 然后当我有这样的OverlayFS以后， 我就可以创建好多个upper， upper1、upper2、upper3。 然后我用upper1 + lower， 我就变成了第一张光盘。 upper2 + lower变成第二张光盘。 这样我就省下了空间对吧？因为我的刻盘工具必须要求是burn一个目录 和一个驱动器。 然后如果我希望并行， 如果没有这样的 UnionFS的机制， 那我就必须把这个目录拷贝多- 拷贝多少份，对吧？虽然可能操作系统 内部在拷贝的时候，呃做了优化。拷贝了多- 拷贝多少份？ 16份。然后为16份写入不同的 这个 呃 CDKey.exe， 然后再并行的做。但如果你有overlay的话， 或者说你有这UnionFS的话，你就可以 可以， 对吧，创建好多个同一个lower不同的upper， 然后去- 去写盘。 然后 还有一个呃 killer application，而且今天大家都在用的。 现在好多了，我觉得现在尤其是我现在用回这个Debian以后， 呃系统稳定很多。我以前用Ubuntu的时候， 如果有一段时间 你做- 你没有升级了， 然后你做一次apt dist-upgrade，你基本上电脑就一定要成砖。 就它有一个很大的概率电脑会成砖。 那怎么应对电脑成砖呢？ 你可以把整个文件系统的根当成一个lower。 对吧，听起来是有一点 对吧，刚才这个机制看起来不起眼，哦，我可以把两个目录拼起来拼成一个目录。 但当我说我把整个文件系统的根变成一个lower以后， lower，好。 然后我再把另外，再在另外一个地方，比如另外一个设备上，另外一个驱动器上或者whatsoever 呃做一个upper，一个空的upper。 然后这个upper也是挂在/上的。 对吧，相当于我的文件系统根就变成了，旧的根就变成了lower。 然后我创建一个新的目录，放在另外一块盘上，它就是upper。 然后我做一个apt dist-upgrade。 这就意味着 我所有的lower是不变的。 对吧，这个lower是不变，我所有的写入， apt没什么神奇的，apt本质上如果你看它的strace的话，它就是写文件， 写文件系统。所以所有的写入，因为这是根， 所有的写入都会写到upper里。 它改变的那个部分，比如说它会往什么usr share里面 改一个什么东西，甚至会在etc下面，写改一个配置文件，还有好多好多好多好多的东西对吧？在systemd里面， 它- 它会有很多triggers，比如它会在systemd里面加一个 什么东西。然后这都会写到比如说/etc下的systemd下的 什么， 对吧？所有你对系统的 持久状态的修改都是反映在文件系统上的，persistence。 OK。 然后，好， 这就意味着你的旧系统还在， 从来没有变过。 然后你下次系统重启的时候， 你挂载的时候也是的，你把lower和upper都挂上。 如果它成砖了， 不要紧，你就回到那个initramfs的那个模式下做一个配置， 把这个整个拿掉， 你的这个系统又回来了。 对吧？呃所以- 所以你看到这个机制看起来不起眼， 对吧？你说，哎我可以把两个目录拼凑起来，overlay起来。 呃但是 它的能力是，能力是非常大的。 像现在我们有这个overlayroot的工具就可以做这件事。 它- 它真的做到了这件事。你- 你可以在里面尝- 如果你想要对系统做任何 潜在摧毁性的 操作，你都可以- 可- 可以用它。 呃对。 然后，然后如果- 如果你试完了以后说这个OK， 很好，可以用， 没有- 没有出问题，你直接用一个rsync就可以。 相当于 把这里面的所有的数据同步搬到 搬到你的这个lower这一层上。 因为它是两个目录拼起来的嘛，你总是有办法访问这个lower的，rsync过来，然后你就 呃 你相当于就把这些 修改给commit到你的磁盘上了。 OK。 好。 呃所以Linux也给了这样的呃机制，叫OverlayFS。而且它做的更彻底， 它可以挂载不止一个 lower。 这就是Docker的 实现的一个非常关键的 关键地方。它会一层一层，Docker里面本身就有一个 概念叫layer。它会不停地创建layer。一会儿- 一会儿我会讲到，呃不停地创建layer。然后可以挂- 挂载好多个 lower overlay成一个。然后但是我只能挂一个upper。 一个，挂一个，挂一个upper。然后我还需要给它一个临时的目录叫- 叫workdir。 呃然后有些比较有趣的行为，我可以就着 就着例子来讲。 我右边会实时监控这个OverlayFS的 情况。首先我的demo.sh就是刚才的一个，一个mount，对吧。我有一个lower的目录，我有一个upper的目录，然后有一个workdir。这个workdir是- 是给操作系统用的，它需要一个临时的目录，必须是空的，然后不空的话它会 它应该会- 会报错。 然后只允许一个upper，可以有多个lower。 然后有意思的， 比如说， 比如说啊， 我现在可以开始干活了。我现在可以往 呃 upper里面 放一个 文件，比如说a.txt。 好。 我可以往upper里面放文件，你会看到在upper里面 呃是 呃是在的。然后同时 我对upper的修改会访问到，会反映到overlay里。 比如说我可以echo 哎， 你看到 upper和overlay会同时更新这个修改。 然后这个时候我可以进overlay， 对吧，比如说overlay。然后overlay 比如说我修改a.txt， 我把a.txt改成456。你看到 在overlay里的修改也会同样的反映到 upper里。 然后， 如果我试图修改一个lower.txt， 哎，你看，这里变了，lower.txt，对吧，overlay的改了，但是lower的没有改。 然后在upper里面多了一个lower .txt，是我改过的。 对吧，我可以再改一次。 我要改成new lower， 对吧？upper里面会多一个。 然后最有意思的是删除。 你看如果我试图删除这个lower.txt， 哎，删除的时候我怎么样再从 怎么样让我使我找不到这个文件呢？ 对吧？你在hash table里面删除的时候，你需要一个tombstone。 然后类似的， 你看到lower里面这个lower.txt永远不变的，lower是不会变的。lower的多少层，无论你overlay多少层，它都不会变。 但是它在upper这一层里面， 就产生了一个很有意思的对象。 一个字符设备文件。然后这个字符设备文件你当然是没有办法访问的，比如你可以 试着看，你看它会说Permission denied。这是一个，这是一个假装出来的， 不存在的 文件，对吧？这叫whiteout的 呃文件。 如果你删除就会在lower里面 创建这个文件。好。 然后这样你就可以实现一个，对吧， 一个网吧管理器。 你- 你想网吧里面的用户会任意地破坏你的电脑，你甚至可能愿意给它grant一个高的权限， 但不要紧，你只要

啊就像我刚才说的那种更新模式一样。你把你的系统做在lower里，然后给它一个upper。然后等到它下机了，你就直接把它upper删掉就，就可以了。所以我们在做这个大规模的考试、竞赛管理的时候，也是这么做的。这个，我，我们可以把考试机器的/home做成一个overlay，然后把机器做成overlay以后，我，我可以把这个overlay实际上的upper放在一个用户是访问不到的地方。然后，相当于我上午场考试的同学来了，他就在上午场那个upper里面工作。然后我直接把这个mount一个别的upper，下午场的考试又开始了。那下午场的考试的同学是看不到上午的人的工作目录的。但是，实际上的工作目录还是存在我的系统里的。当我最后把这个U盘回收的时候，我还是可以看到所有的所有的东西，对吧？网吧的这个系统也搞定了。当然用它解决网吧的问题就实在太是大材小用了。你们知道的Docker，对吧？Docker。这个Dockerfile是我的Online Judge的Dockerfile。然后Dockerfile里面有，有一些，有一些...就，就是一行一行的命令，其实它的设计也是非常的Unix。比如说它从一个，从一个已有的镜像开始。好，那我我们可以假设它有一个已有的已有的比如说Ubuntu 22.04，好，22.04。OK。在Docker里面这叫一个layer。接下来我会配置这些，比如说我要设置这个环境变量啊等等。以及这里有一个叫RUN。然后叫RUN的时候，每当...每当我运行一个RUN的时候，我都会在这个...相当于启动一个虚拟机，然后，当然这是容器，容器我不知道我这个学期有没有时间来讲了。然后它会创建一个upper，它要挂载一个upper，它挂载一个upper，然后底下是上面那层的Ubuntu 22.04。然后它现在把这个东西，就这批整个两层的文件系统放到虚拟机里，然后让虚拟机执行那个apt update。然后这个apt update不就写到这个upper里了吗？对吧？写到这个upper里。然后写到upper里面，等到虚拟机运行结束了，哎，它就不是upper了，它就是第一层。它相当于就是RUN 这个apt update 的以后的这个结果。然后它接下来再运行下一个，比如说我要装软件，apt get install -y。它会在这儿再放一个upper。就刚才装，执行那个上一条命令的那个apt-get update，它就会变成一个lower的层。然后这层就不会变了。然后在这里面，它就变成了upper，它再变成apt install。然后install完了以后，后面你要再执行下一个命令，它又变成了，对吧？它又变成了这个layer。它就一层一层一层堆上去了，一层一层堆上去。所以，如果比如说我发现我忘装了一个软件，漏，漏装了一个软件，我应该怎么办呢？一旦我这个地方改了，比如说我这条命令改了，我就需要退到上一个命令对应创建出来的layer，重新replay执行这条命令。所以我这条命令改了以后，我要装好多好多的包，对吧？我...我如果要，我如果改了这个，我就需要从这里开始执行所有后面的东西。如果我改了这个，比如说我把Ubuntu 22.04改了，我上面所有的东西都不对了，我都需要从这里开始再replay所有的操作。所以如果我想要再装一个包，我正确的方式是我在上面再装，加一个RUN。我，我，我再加一个RUN，这个apt install，比如说我忘记装一个什么东西了，我再，对吧？再加一个。这样它就可以在这一层上，所有底下都是lower，然后挂上去。然后就这一层一层一层叠了一个，又，又是这种persistent data structure。对吧？append only。然后在，在只有在这个地方是，是可变的，然后可变完了以后它又，它又append only。然后这个好处就是你的任何一个layer，当然实际上你创建的是一个镜像啊。那你，你创建完了以后，比如说这个安装完了以后，这个layer，或者说这个layer加上所有的东西，就变成了一个文件系统的快照。你就可以在创，在这个基础上，把所有的底下的这些东西全部当成是lower的，然后你再，再创建一个新的目录作为upper，你就得到了一个虚拟机。你可以创建好多个upper目录同时运行，但是它们共享一个lower。这就是容器，OK，Docker的容器。没，没有什么神奇的，确实没有什么神奇的。就是你想要在覆盖，对吧？你有一个动机是说我希望看到的目录可以是拼凑出来的，能够做到的事情。好，这个例子，你明显的已经感觉到我们已经开始做一些非常规的操作了。对吧？在前，在前面不管我们说是用inotify还是说做快照，这些都是可以用CRUD来实现的。本质上它都可以用增删改查来实现，对吧？快照你就把它拷贝一份，然后你，你，你的那个inotify你可以用，用这个遍历的方式来实现。但是overlay是不能，不能用增删改查来实现，它其实改变了文件系统的语义。对吧？你把两个目录合并，合并是一个新的语义的操作。所以你再往后想一步，文件系统就是一个数据结构。我们早就学过数据结构了，数据结构你是可以随便实现的，没有人规定数据结构一定要按照这个方式实现。所以为什么我不能直接写一个fs.c，我来实现一个文件系统？这个/proc就是内核里面的一个文件系统。所有的比如说它的getdents64的系统调用是它自己提供的，所以它就可以有些目录你就是能cd进去，但是你ls看不到。对吧？这个行为是由文件系统的实现确定的。所以我就应该能够直接写一个fs.c来实现文件系统。而你能想到的事情你就一定能做到。而最简单的方式，当然你可以在内核里面再实现，你实现一个内核模块，然后把这个内核模块装到内核里，然后这个内核就可以实现文件系统了。实际上还有更通用的办法，叫File System in User Space, FUSE。你的操作系统内核会把lookup啊、read、write这些转发到fuse driver。你所要做的所有的事情，就是实现一个这样的结构体。你去读这个手册，我，我不确定我的网络能不能打开它。一个struct fuse_operations，比如说读、写、打开目录、遍历目录，甚至还有就这个结构体里面有一个这个setxattr。FUSE是支持这个extended attribute的。它是一个Unix兼容的文件系统。然后从此以后，这个文件系统就再也不是一个CRUD，你，你可以用它来实现，可以用这个东西来实现overlay的语义。或者说你可以实现一个任何的文件系统，它背后没有任何的文件，但是你可以虚构出一个目录树来。然后每一个目录和文件的读写都是由你自己，自己来来决定的。对吧？你甚至可以直接在Windows上面实现一个/proc，理论上说没有，没有，没有任何问题。对。然后我这里给大家带来了一个我的例子。例子，还记得那个galgame吗？galgame是一个比较令人印象深刻的例子。我做了一个galgame文件系统，叫ggfs。我就用了FUSE，对吧？比如说我用了fuse version的26。我来找一下这个fuse_operations，对。我只要实现一个这样的struct fuse_operations，比如说open是什么，read是什么，打开比如readdir对吧？我只要实现这个readdir，我想要给它什么文件就给它什么文件。然后它问我要文件的时候，我想要给它什么数据就什么数据，完全是由我的，我的文件系统的实现来提供的。就是代码，代码是Turing-complete的，所以理论上说你可以做任何事。好，它的效果是什么呢？这次就不再是我建了一大堆虚拟链接了。好，现在现在的ggfs是空的，/tmp下的ggfs。好，我现在可以make fuse。好，我就进入了我这个galgame的模式。我这时候可以进入一个/tmp下的ggfs。然后如果我查看mount的话，你可以看到有一个galgamefs mount挂载到了/tmp下的ggfs。所以这个时候，我的这个目录，你看这个目录里面已经有文件了。这个文件完全是我虚拟出来的，它没有一个实际上对应的磁盘上的文件的数据。我的所有的行为，比如说我可以cat这个文件，我可以把这个文件的，对吧？我打开什么它会返回这个原样的数据，这是由程序生成的。所以理论上说它可以返回一个随机数给我，什么都是可以的。然后我还有一个行为是，比如说我，我往往一个文件里面写入任何的数据，我的文件系统的内部的状态就会感知到这一点，然后这个galgame就启动了。然后你看目录就变了。然后我可以只要往任何一个文件里面写入数据，所以写入数据也就是调用了、回调了我的那个FUSE实现里面的一个API，然后这样它就可以做任何的计算，所以它可以驱动这个galgame的运行，OK。好。这就是FUSE。然后你可以用FUSE做几乎任何事情，而且疯狂的事情人类已经干了很多了。比如说你可以把一个远端的ssh的目录变成一个本地的目录，就映射到本地。因为你是FUSE，所以你你你只要，只要把所...只要把所有的那个目录的操作代理到那个地方的目录操作就行了。是吧？我可以把一个远端的git仓库变成一个本地的目录。甚至有一个dbfs，我可以用数据库来存储文件系统。比如说有些文件，比如你的FAT文件系统是不安全的，它是不可...对吧？没有，就崩溃的时候可能会不一致的，但是数据库它可能是确实是做到了一致的，因为它是append-only的方式实现的。好，那我可以在这个上面用FUSE实现一个数据库的文件，一个对吧？比如说filesystem.db。然后我用一个SQLite这样的数据库的引擎来把数据，把你的文件系统保存在数据库里，然后你的文件系统的操作全部改成SQL的查询，这都是可以的。然后甚至你可以

你反过来干这件事故，这也是个很有意思的项目，叫，叫 F... FUSE，你，其实你也是马上就能想，想到的，你可以把一个不是文件系统的东西，像 Galgame，刚才我说 Galgame 可以，一个，这不是一个文件系统的东西，我就可以把一个不是文件系统的东西也挂载到文件系统里。比如说它可以，它可以把一个 JSON 文件，比如说一个 key-value，我就可以把 key 变成目录，然后 value 变成目录里的一个 value.txt，然后我写入那个 value.txt 我就可以更新那个 JSON。你每次读那个 JSON 的时候你就会读到那个更，最新版的 up to date 的那个 serialized version，对吧？就你可以把一个不是文件系统东西挂载成一个文件系统，然后你可以实现任何非常规的文件系统操作，就像 proc 一样对吧。你可以把一个，一个什么样的学习资料真正隐藏起来，你在，就你让这个 getdents64，readdir 系统调用看不到这个文件，但是你可以 cd 进去，这时候只有你知道那个名字你才能 cd 进去，没有任何人能 cd 进去，对吧？一个 Galgame 的文件系统，所以这是一个 everything is a file 的一个终极的实现。所以我觉得很有意思啊，你看，我们说文件系统是一个树结构，就这一句话，然后你从增删改查开始，你只要有想象力，借助操作系统机制你就可以做任何的事情。