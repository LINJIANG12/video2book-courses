上节课的时候我们讲了文件系统。什么是文件系统呢？文件系统就是一个给你提供目录树状的结构的任何的东西。对吧？我们其实在客场面从最开始的时候就反复的用，比如说procfs，比如说sysfs，这些它其实是虚拟出来的文件系统。只要你实现了一个，比如说如果甚至你是在用户态实现这个filesystem in userspace, FUSE operations, 你只要实现像我能遍历目录，能创建目录，删除目录，get attributes, open, read, write 这些基本的文件系统的功能，包括目录树的结构和文件的读写，好，它就是文件系统。然后你也看到很多例子了，对吧？我上次甚至做了一个galgame-fs，我可以维护一个自己的文件系统内部的状态。虽然我说它是文件系统，它其实不是，它是一个galgame。然后它可以在屏幕上显示出一个游戏。它可以向外暴露一个目录树，然后你可以读写那个目录树实现任何东西。所以理论上说你可以创建出任何的只要是一个这样的符合我们Unix反复使用的目录树结构的东西，它就是就是文件系统。那我今天来讲文件系统的实现，也就是我们要在block device上实现一个文件系统这样的数据结构，并且能够实现persistence。持久化还是我们操作系统很重要的主题。然后我们也知道mount这个系统调用，对吧？如果你看mount的系统调用的话，对吧？它要求一个block device。这个block device有三个，就有三个接口。你可以这样理解啊，实际上它会更复杂一点，它有一个队列，然后你可以往那个队列里面提交请求，并且能够知道这个请求请求完成。但简单起见，你你可以对比类比我们并发程序写程序的时候的共享内存。有点像啊，对吧？内存也是一个字节序列，只是内存它的寻址方式是按字节寻址的。你可以在memory hierarchy帮你藏起来了整个从L1、L2、L3，然后到memory，甚至还有更多层级的存储的存储栈、内存栈。然后你可以用load和store去按字节的方式去寻址。然后类似的，我们的block device也是，它是它是一个block array，一个数组，然后那数组可能是4K，或者比如说软盘上面是512个字节。你看成那个块去一起把它读出来写进去。以及并发程序里面我们有一个说说data race是很危险的，对吧？如果两个线程同时读写一个memory location就很危险，所以我们可能要用互斥锁这样的东西来实现互斥。然后我们的block device也有这样的一个类似于barrier的指令。它可以像并发程序里面你能够从并行变成串行一样，它有一个叫block sync。它可以等待ongoing的，所有正在再图当中的写落盘以后再返回。你可以你可以这样理解，就用这三个操作来实现，你可以就用它来实现任何数据结构，就像你可以用load和store来实现任何的内存里的数据结构一样。但因为当我打了并发的这个比方的时候，你们马上也有一个感觉是，我又再一次打开了魔鬼的盒子，对吧？我说多处理器编程从入门到放弃的时候，我们打开了一个魔鬼的盒子。然后我今天会再打开一次。我会讲文件系统怎么实现。相对来说实现其实是比较简单的，但是要实现一个crash safe的文件系统，你会发现你会遇到和并发编程里面一样的问题。很有意思啊。好，刚才讲了一个overly simplified的这样的一个块设备的存储设备的模型。它可以读一块、写一块，可以等待写入的块落盘。但实际上的设备会比这个要稍微复杂一点。比如说如果你在终端里面执行这个lsblk，你们确实可以看到一些设备，包括包括block device，包括它的分区，对吧？这是MMC是我的SD卡，block 0的p1第一个分区，第二个分区。你们可能会看到一个像nvme的这样的一个分区。实际上一个block device，比如说一个磁盘、SSD或者HDD，它本质上也是一个数据结构。然后这个数据结构也是有specification的。像我们今天主要用的是叫GPT，不是那个ChatGPT的GPT，是叫GUID partition table，叫GUID的分区表。它是UEFI的specification的一部分。UEFI是那个firmware，就固件的specification。你们看到为什么是由固件的厂商来制定这个标准？对吧？因为你的磁盘的数据结构的layout在系统一启动的时候，firmware就要看到。对吧？如果他没有办法解析这个分区，他就不知道比如操作系统到底装在哪，你的数据盘到底装在哪，它应该怎么样的挂载。所有的你可以启动的磁盘都遵从这个这个标准。有一系列的工具可以去读它，可以去写它。比如说去创建这样的系统盘，比如说创建一个可以启动的U盘。如果你希望做一个可以启动的U盘的话，你就需要把它格式化成这样的一个GUID partition table的分区。这是一个数据结构，然后它它告诉你OK，头是什么？然后数据结构在哪里？怎么识别这个文件系统是什么？然后以及，sorry，怎么识别这个分区有多大？然后以及每个分区所在的這個位置。它就是一个数据结构。然后我们通过读整个磁盘或者说存储设备的前面的一部分字节，你就可以知道第一个partition在哪，第二个partition在哪。所以如果说我是一个比如说是一个完全被我写变成了全0的一个存储设备我插上来的话，它是识别不出来这种p1、p2的。它只能看到一个可能只能看到一个mmcblk0。比如我现在打比方我可以干干这样一件事，zero，对吧？然后我直接把它重定向到这个mmcblk0，那我这个机器马上就重装了。对吧？我就把它全刷成0了，然后这时你就看不到p1、p2了，只能看到这个block。好。这是实际设备的情况。它也可以是一个虚拟的设备。甚至我们可以一层一层叠很多层的虚拟设备。如果你想要挂载一个文件系统里面的文件，就像我前面给大家展示，我可以mount一个文件镜像，对吧？.image，然后fat32我可以把它挂载到我的文件系统的目录里。实际上Linux系统里面有一整套虚拟化层，虚拟化层，它叫LVM，叫logical volume manager。既可以实现一对多，也可以实现多对一，也可以实现多对多。所以理论上说我可以干件什么事呢？我可以创建好多image，2.image。然后我可以通过loopback device，就回环设备，去把它变成一个比如说这个/dev/loop0，对吧？这个/dev/loop1。我可以变成好多个回环设备。然后它每一个都是一个看起来就像是一个block device的东西。把它虚拟成一个这个volume group。然后我再在这个上面又可以创建好多个logical volume。就其实可以随便虚拟化。因为本质上所谓的块设备，它就是一个block的array。原则上我甚至可以用多块磁盘来虚拟出一块更大的磁盘。对吧？你们如果读那个教科书的话，看RAID的话，我们也可以用多块磁盘去虚拟出一块更快的并且更可靠的磁盘。虽然我今年的课时可能没有办法讲那个RAID了，你们你们课后可以去读一下那个OSTEP的教科书上面的讲RAID那个章节，也很精彩。既然啊既然你知道可以做这件事，对吧？比如说我甚至可以，然后在这个上面我可以创建一个虚拟磁盘。然后把这个虚拟磁盘我可以变成p1和p2。对吧？比如说我甚至可以p1比如p1我可以给一个FAT，p2我可以给一个ext4。就像我们在lsblk里面看到的那样。然后所以我我也做了这样的一个就做了这样的一个脚本，而且完全是vibe coding的，我几乎没有没有管中间的过程。我让AI帮我实现了两个脚本，一个叫lvm-demo，一个叫lvm-cleanup。然后这个lvm-demo的功能，敲错了。我可以运行一下。哎，它就干了很多的事，我们先看一下日志啊。它创建了disk1 2 3 4 5 6 7 8，8个磁盘，每一个都是32兆。接下来它又创建了好多回环设备，就像就像这样。回环的设备。这样我就有好多块设备了。这些块设备就是一个一个的physical volume。对吧？它看起来就是一个一个的磁盘，对，看起来就是一个block device。然后再创建一个volume group，就它们虚拟成一个虚拟的磁盘，叫vg-demo。然后vg-demo创建了以后，你看现在一共有224兆。然后再把它格式化成一个GUID partition table。格式化以后就把它变成p1和p2，然后一个是FAT，一个是ext4。然后全部都全部都完成了。也就是说你们现在需要的是有概念。里面用到的很多的工具，LVM的工具以前都是要哎呀，查手查手册先查手册先把概念弄清楚，然后自己去执行那个命令。现在不需要了。现在你只要知道有这个概念，能这件事情是能做的。我现在就有一个叫p有一个东西叫p1，一个有一个东西叫p2。两个目录就在，对吧？虽然是root，在在我在这我这里。好。然后我在p1上面我比如说我就可以hello到a.txt。然后我可以p2下的b.txt。注意，这两这些东西都是虚拟的。最后它们都会走到回环设备的1.image、2.image这些文件上。所以你看我现在现在假设我做一个sync，或者我做一个sync就可以了。我我确保我确保这个系统已经落入磁盘当中，确保这个写入到文件里了。然后我可以看这样的一件事。你们觉得hello和world会在哪？刚才我写了两个文件，对吧？我在我在p1里面写了一个hello，在ext可能在p2里面我不记得是什么文件系统，写了一个world。然后这在就这在一个比如说a.txt里，这在一个b.txt里。然后你可以看到它们正确的被写到了路由到了两个我的文件当中。对吧？因为我是前一半和后一半的，我有一个GUID part...

partition table，然后前一半是一个文件系统，是P1，后一半是另一个文件系统P2，然后你看到这些数据真的就在这个磁盘的虚拟的文件上，很有意思。然后我还特意让它把所有的strace的log都打印出来了，这都是就真的是只要讲一句话就行了，我我就跟他说好你你能不能帮我把每一个LVM相关的命令里面的system call给挖出来，然后这样我就可以，呃，对吧，我我已经准备好这个命令了，我可以打印这个strace.log，然后我让AI帮我解释这些用来管理LVM的呃system calls。然后你看我因为我是用一个pipe，我就把整个strace的log就送给了大语言模型，然后让大语言模型稍微思考两秒钟。第一，建立loopback device。这这就是我前面上课的时候讲过的，对吧？它去/dev/loop-control的这个文件上面做ioctl，对吧？用loop control get free去得到一个空的loop device，创建这个好多个loopback device。主要用的是ioctl。然后在这个pvcreate的时候，就初始化这个物理卷的时候，相当于，呃，LVM已经开始起作用了。你看它用的是/dev下的mapper下的control，一个映射器。也是，你看操作系统里面就把所有的这些功能，LVM这些功能全部都丢给了ioctl。你看它找到了，确实找到了这个block device的大小，对吧？然后各种写入标签等等。然后创建一个组，创建一个组，好，创建逻辑卷。就和刚才其实loopback device用了一套API，对吧？我用一个ioctl去去查询这个device mapper的能力，然后创建一个device mapper的设备，这个是是关键的步骤。你看它用ioctl，然后有一个DM_DEV_CREATE，然后把name啊，UUID啊这些东西都都都写进去，也是通过ioctl实现的。呃，分区的映射，再次用这个每为每一个分区创建这个次DM的设备等等。然后基本上你看到这边总结下来就都是用用ioctl实现的。就我们可以把任何的软件系统都可以打开，然后可以看到里面实际执行的系统调用，它是怎么和内核交互的。所以这也是其实为什么说你想要再复制一份Linux是非常困难的。对吧？你你看起来OK我们想想要实现一个操作系统实现read、write、open这些就可以了，不是这样的。你当你碰到ioctl的时候，你发现内核里面的几乎每一个子系统，它的API都是用ioctl这样的东西暴露出来的，甚至有些是通过文件系统，对吧，sysfs暴露出来的。那那这些复杂性你都你都需要实现，所以想实现一个兼容的Linux就啊就非常困难。然后甚至是比如说我们现在的NVMe硬件上面它有NameSpace支持的时候，它就有点像一个硬件级的呃LVM，就是一个真实的块设备。但是不管怎么样，就块设备你还是可以这样理解，就虽然有有你知道有有很多的有趣的功能，你可以做层层的虚拟化，但最终一个block device就是一个read、write，呃可和和sync的呃操作。好，那在这个基础上，就在这样的一个比如说这样的东西上面实现一个文件系统，那面临的困难是什么呢？数因为本质上文件系统就是一个数据结构，你你直接实现它就可以了。你面临的主要的困难实际上是你在实现的时候如果要高效，还是很有挑战性的。因为你你读写数据都会造成读放大和写放大。因为你很多时候真的只想要一个字节，对吧？你只是想要这个文件的大小，或者只想要这个文件的stat，比如说什么时候修改的这些信息。但是你为了读到这个信息，你就需要把一整块数据全部都读进来。所以这个时候你你的数据在内在磁盘上面的排布也就也就很重要了。比如你更新一个字节，你想更新一个文件里面的一个字节，你不仅要更新这一个文件，你还会更新它的modification time，对吧？它的元数据。然后这就涉及到多个块的写入。涉及到多个块的写入，甚至一整个哪怕只是一个块的写入，你都需要首先把这个块读出来，把中间的部分写在改掉，改掉以后再写进去。而当你说写一个块的时候，你又会涉及到SS-SSDFTL里面的那个map table的修改，对吧？它有一个像page table，在你的SSD里面有一个像页表一样的东西做地址翻译。你你看到的比如说我要写第一块，实际上你写的不是第一块，你写的是第一块映射过以后的那个呃就因为是一个虚拟地址，映射过以后的那一块。好，因为你要维护这个虚拟地址，所以你又有一个数据结构，然后这个数据结构又是copy-on-write的。然后这个copy-on-write又会在SSD的内部产生写放大。它如果要拷贝，它都会拷贝根节点，然后再拷贝拷贝拷贝。所以这是一个，呃理论上你写一个字节可能会有几万倍甚至几十万倍的写放大。当然这个比较极端，你你真的是只写只写只写一个一个字节。呃写一块的话依然会有若干倍的读写放大。这个这个甚至可能到十倍都是有有可能的。所以你你们也知道要在文件系统这一层要做缓存，和memory hierarchy一样，对吧？我们memory hierarchy里面有一级、二级、三级的缓存。我们也需要在呃内存里面留一份数据。然后我们的block read和block write就看起来看起来这就是一个read disk，对吧？呃我可以对它做bread或者bwrite。但是操作系统内核会统一分配一个，当然Linux内核里面这个东西叫buffer cache，是一个很糟糕的名字，你就可以认为它是一个cache。这个buffer cache，这两个东西连起来，这是这是一个历史原因，呃不应该这么叫。它就是一个cache，文件系统的cache。然后你 你的bread你可以类比成是共享内存，就内存的load，然后bwrite是store，你就很好理解了，就跟你的cache一样。你如果load的话，你必须从cache里面load。如果cache miss的话，就说明这个cache里没有的话，它就要去这个设备里面找。当然，如果设备是这样的话，这个设备又会去这里面找，这个又会从去去loopback device里面，又会到文件里面找，对吧？经历了一层转化的过程。写的时候和memory hierarchy里面的不太一样，你因为呃我已经是一个block的写了，所以我这个block可以直接写到写到这个cache里面，把旧的东西给覆盖掉，然后就相当于是cache hit了，对吧？它存我写直接写到cache，下一次read的时候我就可以直接从cache里面给呃给读走了。这是呃这是你要做的一个呃一个优化。所以其实呃你看到很多操作系统里面做的事情和另外的系统，比如说存储系统、内存系统里面做的事情是一样的，它也用了这种locality的原理。如果你想要想要让这个文件系统高效的话，你就希望尽可能地把一起就我经常会一起访问的数据结构要放到一个块里。对吧？很自然的一个想法。这样你你cache hit以后，我这一次比如说我读到这个数据结构，我下一次要读到它临近的那个呃数据结构的时候，我的cache就会hit。这样可以减少访问速度，就一定程度上减少这个读写放大。但是有些读写放大还是还是不可避免的，对吧？比如说我就是要写一个字节，那那确实会造成比较显著的读写放大。好，呃这是一个呃block device的模型。然后现在在这个block device上我们就可以去做，就做文件系统的设计了。好，那文件系统我们要实现的是什么呢？我们要实现的就是一个目录树。呃反正任何东西都是根，对吧？比如说/bin、/home，它可以有目录，然后比如说它可以有文件，然后当然可能有链接，对吧？在链接的意义上这个图画的并不是特别好，但是这也不重要了。比如说list是一个byte array。好，你要实现一个这个东西。然后用block read、block write来进一步实现。呃那为了实现这个数据结构，其实就有好几个东西需要实现。比如说这是data，对吧？这是data。然后还有每一个目录或者文件的那个metadata，包括比如说它的大小，对吧，比如说它的名字，名字也是它metadata的一部分。比如说访问它的权限mode，然后比如说它的时间戳。上一次我也给大家展示了文件系统你每次修改以后，inotify的时候修改以后你是可以感知到，尤其是它的上层目录也会被modify time也会也会修改。以及这样的一个目录树状的结构，对吧？这个树结构。你们你们写实现一个树的时候你们OK，你可以实现一个struct，对吧？然后你有一个比如说vector of 什么什么的呃children。对吧？你们可能会可能会这样实现一个树结构。然后类似你在磁盘上面也需要这样一个树结构。然后那如果你要实现一个高效的文件系统，OK，那你可以观察一下现有的文件系统里面的一些呃统计。这个这就就是你先需要做一个study，然后去去去后厚着脸皮去找各种人，说哎你的文件系统能不能借我遍历一下，对吧？我不窃取你的隐私，但是我想看一下文件的分布。然后他去各种场景，比如说数据中心里，去这个个人用户里PC不同的人里面去把各种数据给拿出来。拿出来以后，OK，就可以得到一些结论。当然这个结论稍微有一点过时了。呃不过基本上还是符合我们现在的Unix系统里面的这个呃文件的分布，尤其是个人的一个电脑的分布。数据中心可能会稍微会很不很不一样的。因为数据中心里面大部分都是比如说是管理的数据块。好，如果只是我们说这个系统文件的话，那绝大部分的文件都是比较小的，也符合我们的预期。比如说我们生产的源代码，我们不可能在一个文件里面放巨大无比的源代码，这样会让你的维护变得很困难，我们还是会把它拆拆开，对吧？我们所以我们生产的内容，文档性的内容，它全部都是不算很大的。然后以及有一些像图片啊这些这种binary也不算特别大。然后但是a few big files use most space，对吧？比如说一个数据库、一个电影，它确实是会占用绝大部分的

空间。然后一般来说，一个目录里面的 entries 不会太多的，对吧？如果你把你组织你的一个项目里面有几千个文件，那对不起，这个你要浏览它也会也会比较比较困难。当然现在都是 AI 浏览啊，所以其实好像也没有那么大的关系了。然后还有一些有意思的发现，比如说是 file systems are roughly half-full，不管你的门限有多大，你你总会想办法把它填得比较满。有意思的一些 findings，然后这些 findings 就可以指导你去设计一个高效的文件系统。你你其实就是回答几个问题：第一个，这个 metadata 怎么存？对吧，metadata 是直接存在这儿吗？还是存在一个别的别的地方？对吧？然后 data 怎么存？然后一个一个文件是一个 byte array，那它就是好多具有好多块组成的，对吧？那它的第一块、第二块、第三块，所以这也需要一个数据结构。对吧？你需要知道文件，比如说这个文件可能很小，比如 2KB、4KB，它也可能很大，几十 GB。然后一个很大的文件，你怎么知道它的每一个 offset 所对应的那个块在哪？对吧，这也是一个非常现实的现实问题。你需要一个数据结构来来存储它。然后基本上你你你你可以做的是遍历这个世界上所有已有数据结构。比如说你你去写一个爬虫，然后把世上所有写的和数据结构相关的论文全部爬下来。然后爬完了以后你就给 AI 大语言模型一个 prompt 说：“哎，对于这个数据结构，我来实现这个东西，根据磁碟的这种特性对吧，”你可以把 prompt 拼起来，“对吧，我要实现一个什么样的文件系统，优化什么样的 workload，这是一个是不是一个好的 idea？”然后你就把所有不好的 idea 都删掉，然后你就可以得到一个新的文件系统的实现。对吧，基本上你你这是今天的我们的标准答案，如果你要做做文件系统的话。好，那其实就这么简单啊，然后我就可以下课了。我我剩下的内容都是相当于再把这些人类已经沉淀下来的知识跟大家 overview 一下。首先你要在一个 block device 上实现一个数据结构，对吧，实现一个数据结构。那它一般来说是一个 partition，对吧，比如说像刚才我们看的什么 /dev 下的 mmcblock p，block0p1 对吧，第一个 partition。好，它也是一个字节序列，是一个 block block 的序列。然后，那如果你要存储一个数据结构，所有的数据结构都要有一个入口。你们的链表一般会有一个 head，然后你们的一个二叉树会有一个 root 指针指着它，对吧？然后你你看各种各样的文件，它其实也是数据结构，比如说 Bitmap。比如说我们上课的时候，哎大家还有没有印象，我我为了输出一个图像，在做那个 Mandelbrot set 的时候，我我用 printf 输出的一个图片。那个叫一个 .ppm 的格式，对吧？我就一个 printf 我就可以输出输出一个最后你真的能在浏览器里面看到的图片。其实这个很好，这个对于对于我这种简单的想在上课的时候做 demo 的用途非常非常友好。像这些都是数据结构，ELF。它们也都有一个头，然后这个头上比如说 ELF 会有一个 magic number 说帮助像 file 啊以及加载器啊识别这是不是一个 ELF。然后以及这个 header 里面有一个结构体，这个结构体有 ELF 的 entry，对吧，它的这个文件的大小，program header 的数量，这些这些这些东西。所以，首先所有的数据结构都有这样的一个 root，指向某一个 block。一般来说就放在最就放在最头上。像像这种 GUID partition table 也也是数据结构，所以它也有一个 root 放在总分区的，对吧？这是一个 partition，这是一个 partition。它它不是从第一个 block 整个设备的第一个 block 开始的，第一个 block 一般来说是还是一个兼容的 MBR，然后后面有一个 GPT 的 data structure。这是这是 partition 的 root，相当于是整个分区表的，就整个磁碟的数据结构的 root。然后当然 GUID partition table 会在磁碟的尾巴上面再存一份，它会备份两份。因为如果这个分区信息丢了以后，你要恢复这个磁碟就会比较麻烦，虽然也不是做不到。它会把它备份一份，这样如果它损坏了还你顺着尾巴往前找还可以找到它。OK。然后在这个分区里面头上也有一个数据结构，然后这个数据结构叫做 superblock。就是这个名字，所以它就叫 superblock。然后它也是基本上是一个对整个数据结构来说最关键的 block。如果如果你要损坏，如果只允许你损坏一个 block，你就想要对文件系统的使用者造成最大的麻烦的话，你就应该把这个 superblock 给给抹掉。虽然这个信息还是可以恢复出来的，但是会给它造成一些一些麻烦，对。一般来说你你一个 superblock 已经有一个 magic number，然后有有这个文件系统意义上的，比如说这个 block 的 size。它比如说你的你的磁碟可能是 512 个字节一块，但是文件系统它可能会说，好，这个东西它可以按照 512 个字节的 block 给给所有的 block 编号 1 2 3 4。但这样的话，如果这个磁碟比较大，这个位数就会比较大。比如说 FAT32，32 位的 block number。如果你每一个 block 只有比如说 512 字节的话，那你也就只有 512 乘以 2 的 32 次方这么多个，最大的文件就只能这么大。但是如果它不按照 512 字节，比如按照 4KB，对吧？虽然磁碟是 512 字节，但我可以按照 4KB 作为我的一个逻辑的块，我我的文件就可以更大一点。这是 superblock，它里面有一些这个文件系统关键的信息，对吧？比如说这个文件系统还有还有多满，还有多少空间，然后还能够剩下多少个文件可以分配，数据结构的起始位置、大小这些。然后像 mkfs 的时候，如果你 strace 它的化，它就会看到你会看到它会在前面写上这个数据结构。然后我们的 mount 命令其实也是根据 superblock 去确定文件系统统类型的。好，这个没问题。OK，那既然你知道这是一个数据结构以后，你就可以开始开始设计它了。那第一个比较容易理解的、很简单的设计是 File Allocation Table。File Allocation Table 它可以在硬盘上面用，也可以在软盘，像比如说 5.25 英寸的软盘，单面是 160KB。也就是说 160KB 就意味着它软盘是一个 block 是 512 个字节，它总共只有 320 个 block，只有 320 个 sectors，只有 320 个块。你想只有 320 个块，你要在这个块上面构建文件系统，你的设计思想是什么？你的设计设计思想肯定不是什么 fancy 的数据结构去维护这个任何你在数据结构里面增加的复杂性都浪费了空间。某种程度上你你会牺牲一点点多余的空间去换换可能更高的查询效率，但是你不想做这个事，你想要的是极致的空间的节约。所以 File Allocation Table，FAT，它就做了它就是这样的一个设计啊。这就是一个非常简单的数据结构的设计。首先它有一个它的 superblock，我我还是看这个 specification。曾经我觉得操统课给大家带来的一个，就我自己的操统课给大家带来的一个很重要的学到 takeaway 就是你们可以去读这个手册。因为有一个我们有个实验去做那个 file system 的 recovery，然后你必须要对比如说 FAT 里面的各种东西感到以前不知道，因为你们从来没有做过这事，感到很清楚。然后你会发现读手册其实没有你们想象的那么困难。因为因为总是一点点来嘛，如果上来就给你一个什么 PCIE 的 specification 你就直接崩溃了。但是哎这个 specification 的长度适中，你可以对着这个 specification 写代码。然后最终你把那个代码调对的时候，你会，哎你会得到一些比较宝贵的宝贵的经验。那好像今天也不需要了，今天直接让 AI 读这个 specification 就行了，而且 AI 确实非常擅长这个处理这种手册。OK。3.1 有一个叫 BPB structure common to FAT12、16 和 32，我暂时先不说什么是 12、16 和 32。这是它的 superblock。然后 superblock 里面这有各种各样的字段：OEM name，对吧，它可以存储一些字符串的信息；bytes per sector，它可以存储这个存储设备每一个扇区有多少个字节，所以它你看它可以兼容 512、1024 的各种各样的，FAT 可以兼容各种各样的设备，不仅仅是一种磁碟；然后它还有一个概念叫 cluster。也就是我刚才说的，虽然我们的物理上就是那个磁碟你访问的时候是按照 512 个字节来来访问的，但是你可以把比如说任意的 2 的 k 次方个 512 个字节绑成一个 cluster。比如说 k 比如 k 等于 2 的话就是 4。那么 4 个 512 字节就是 2KB，这叫一个 cluster。Cluster，然后 cluster 点 number 1，number 2，number 3，cluster。好，这是它的核心的概念。等等等等等等等等等等等等一个一个 specification。好，这个网站我一会儿留着。然后为什么要叫 FAT 文件系统呢？它叫 FAT 就是因为它的核心的数据结构叫做 File Allocation Table，叫文件分配表。它是什么呢？它是一个这个东西：它是一个 cluster ID，FAT of 4096。不要把它叫 FAT，你想象成这个：

当然这个是针对比如说针对FAT12来的，4096，12个bit。12个bit可以可以存储只能从0到4096，一个很小的文件系统。如果FAT16的话，你就可以65536，对吧？它实际上它还会再小一点。你可以这样这样理解。32的话就是2的32次方。它就是一个next数组。

我们说这个磁盘在FAT里面已经被编成好多cluster了，就好多块块。它前面有一些是是不用的，就是刚才那个树结构的图上面有有一些其他的区域。好，cluster number 1, number 2, number 3，这个是一个cluster。

所以什么是next呢？next就是一个链表嘛。比如说如果next of 1等于3，这相当于是我把1说1是一个数据嘛，这就一个文件的数据。然后文件数据它的next是3，就说文件的钱比如说这个cluster size是4KB，那第一个4KB就在这儿，第二个4KB就在这儿。

如果next等于0，比如说next 2等于0，如果next 2等于0，那么这一块就是free的，它就可以分配。就说这个是没有用过的，可以分配。然后如果next等于-1，比如说next 3等于-1，-1就相当于说好，这个文件就到此结结束了。到这块就结束了。这个文件就只有两块，它最多只有8KB的大小。你只要再存一个文件的size你就可以知道到底这个第3第3块里面有多少是实际上属于这个这个文件的。这就是一个next数组，结束了。数据结构，对。

那这样的数据结构对于只有320个sectors这样的小的磁盘肯定是最合适的是吧？它非常紧凑。在在这个next数组，比如FAT12的时候，它的一个指针就是12个bit，对吧？12不是8的整数倍，它会把两个12bit打包成3个字节：byte 1, byte 2, byte 3。那我3个byte正好是24bit，然后相当于这个12bit是属于上一个指针的，这个12bit是属于下一个指针的，这样。它是按bit紧压的存储在这个next数组数组里面的，非常非常的省空间。而且因为你看啊，在一个320个扇区的文件系统里面，绝大部分都应该是很小的文件，对吧？都是很小的文件。所以你很小的文件，你你这样的一个next指针看起来是一个线性的链表，你觉得性能可能比较低，但是小文件完全没有问题，对吧？而且你可以把整个，因为整个FAT一个一个block或者一个，sorry，一个cluster是一起读出来的。这个cluster一起读出来读到内存里面，相当于你一下子把好多next指针都加载到内存里了，所以你等于是在内存里面，几乎是在内存里面遍历这个数据结构，所以性能也不是很大的问题。

但是如果你是一个很大的文件系统，就是FAT这文件系统在在小的存储设备上面是完全没有问题的。但是如果你是一个比如说到了几十个GB的文件系统，那里面可能有几GB的你的电影的大文件，而如果，如果这个next有两种情况：一种是我的next都是连续的，比如说next 3等于4，next 4等于5，next 5等于6，这这这就是最理想的，就是你你一访问一个文件的时候，它的它的这个next是连续的，意味着它在cluster里面存储的一大张也是连续的。但是呢如果你分配的时候，因为你你你的文件会不停地创建新的文件删除、创建新的文件删除，所以这个memory allocation一样的，你如果这有一个空的块，你可能哎分配走了，然后过会儿它又回收了，时间长了以后就会产生碎片。然后一旦产生碎片以后，它这个FAT就不连续了，不连续它可能会指成一个很远的地方，然后再指回来，又指到一个很远的地方，对吧？来回跳来跳去跳来跳去。那如果是一个很大的文件，并且碎片很多的话，就会造成很大的读放大。因为你的FAT本身也是也是很大的，对吧？FAT32，如果你的磁磁盘很大的话，你你的FAT entries也很多。然后FAT entries很多的话，你你就可能会会发生的是，这段的next你读出来了，它的下一个next在另一个在另一个这个block里，你就需要把这个block也读出来。然后再下一个next又在另一个block里，你又把next下一个next读出来。所以你就你就产生了很大得很严重的读放大。

对，我们其实给大家准备了一个实现去检查FAT文件系统的小工具，一个.c。这个代码对大家很有用，因为你们要做实验。你们要做实验解析FAT文件系统，然后你们的起点就是这个示例代码。你看到这是一个就是把文件系统真的是当成是数据数据结构来处理的。我们直接用mmap把磁盘一整个磁盘镜像映射到进程的地址空间里。好，然后你看，一旦我把superblock映射就把整个磁盘映射完以后，我的一个FAT32的header实际就是它的superblock，我直接就变成了一个指针了，所以它就是一个数据结构。对吧，然后我可以从这个指针里面读出各种各样的信息，什么bytes per sector啊，total sectors啊，各种各样的什么volume number, serial number。好，完了以后我就可以用一个遍历数据结构的方式，这个叫DFS scan。OK，去用DFS去扫描整个文件里面的整个的目录树。然后我可以用它来去读一下实验的要求里面的那个镜像。你可以看到目录的结构是可以正确的显示的，就跟你们的PS3一样。好，这是一个64兆的FAT32的文件系统，应该是FAT32。对，FAT32的file system，FS type是是FAT32。然后你可以看到好多的好多的文件。有一个叫DCIM，是一个假装是一个相机拍摄的那个文件。然后里面有好多的.bmp。

然后你看到有些图片，比如说这个83个cluster，从464到546，它就是连续的。它就是这种next都是连续的，next 3等于4，4等于5，5等于6，这样连续的。但是也有些文件是不连续的，我我创造这些不连续就是我我有些我会不停地往里面写文件，然后删掉，写文件删掉写文件删掉。就相当于我不停地allocate了一些随机大小的memory，然后然后时间长了以后你allocation和free以后，它就会产生碎片。产生了碎片以后，你看到的就是我的一个文件可能分散在连续的几个部分，对吧？从2407到2431，然后2511到2587。这些都是可以用程序把它当成数据结构来解析的，所以没有什么没有什么神秘的。

好，那文件的存储讲完了，接下来就是目录。目录是怎么怎么怎么弄的呢？在这个手册里面也有。在最早的FAT里面，它存储一个文件的文件名是用固定的11个字节。你看从0到0到11，当然现在叫short file file name，11 11个字节，是8.3的格式。所以比如比如说，比如说这里有一个我这里有一个比较长的文件名：AUTOEXEC.BAT，这个就是DOS里面如果它看到这个文件，它就会自动运行它。如果你的C盘，A盘的根目录，比如软盘的根目录下有一个AUTOEXEC.BAT，它就会自动去运行它。就跟你们的那个什么.bashrc这种这种差不多，系统启动的时候会会执行。然后这个时候它是8+3，就它只总共只存11个字节，然后它会默认有3个字节是扩展名，所以你看它这个非常有历史感，对吧？他说文件最多只能有11个字符，文件名最多只有8个字符。今天你根本就不能想象这个事情，对吧？你你随便就可以起一个很长很长的文件。

然后这也是刚才为什么我们那个工具里面看到它那有那个波浪线1，对吧？你看波浪线1、波浪线2、波浪线3，就是因为如果这个文件系统是是在今天创建的，它为了保持向前兼容，比如说在DOS里面你依然可以访问这个目录，所以它会把比如说你有，你有一个很长的文件，但是它的前缀又都是相同的，它就会把这个前缀截断，截断，然后波浪线1、波浪线2来区分这些这这些文件。OK，这是这是FAT的一个有意思的特性。

我们的目录项里面，就是目录实现也是一个文件，就它也是一个数据，它也是靠这个next。比如你你的目录里面有好多好多好多项，比如一万个文件，它这一万个文件就是不停next next next next。就它它这个数据结构也是一个线性的线性表，然后这个线性表第一它是一个好多好多个这种struct的这个directory的entry，对吧？叫dentry。我们讲目录项，directory entry。然后这个directory entry就是我刚才在这里展示的这个样子，对吧？它有它有一个名字，它有一个attribute。哎，这里告诉你它的最低位是代表是不是read-only，然后第二位代表是不是hidden，是不是system，volume ID，是不是目录，对吧？你看0x10，你可以知道这个文件到底是

不是目录，所以这个是文件的 metadata，它的名字、它的 attributes，还有一些别的，file size，这个是非常重要的呃数结构的问卷。然后然后这样每个目录里面就存了它目录下的所有的文件，所以你看到这个文件系统是不支持链接的。 就是一个最简单的呃实现，然后你你你怎么遍历目录呢，你就 你作为一个文件系统的实现者，你怎么遍历目录呢？你就把这个目录文件当普通的文件给读出来，对吧。 然后读完了以后他就变成变成好多好多个这个数据结构的拼接， 对吧，好多好多数据结构的拼接，然后他他去遍历这个数据结构，他就知道这个目录里面有哪些文件。然后以及 这个也非常重要， 呃是叫 first cluster， 就是这个文件的第一个 cluster 在哪，对吧，它有 high 和 low 两个两字节。 相当于是一个就砸这个 dentry 里面有一个指针，指向了这个文件的 第一块。 呃我这个图画的不是特别好啊， 呃指针指向了文件的第一个 cluster，然后这个 cluster 它的 next 就是它的第二块， 再 next 就是它的第三块，继续 next，就可以把整个文件的所有的内容都给 呃都给遍历出来。 好，这个就是 File File Allocation Table，呃没有任何神秘，它就是一个数据结构。 如果是 Unix 呢？ Unix 的设计者，他在设计文件系统的时候，想得比 FAT 要远一些，他会想如果我们的文件很大怎么办？ 如果我们的文件系统也很大， 怎么办？ 对吧，以及他想要支持链接，就是在 Unix 的文件系统里面，它和 FAT 不一样，它是一个图，它不是一个 它不是一个目录树。 Unix 是实际上是什么呢？就是比如说我的杠， 它是一个这样图状的结构，每一个目录里面，比如说这个是 bin，这这个叫 bin 目录， OK，bin 目录，然后它有一个点， 它一个点点是指向它上级的，它一个点是指向它自己的， 这是一个图的结构。 那如果你们要存储一个 graph， 然后这个每一个点是有它的 metadata 的，对吧。呃如果一个 bin 目录的话，首先我要知道它的，比如说 time， 它的一个 它的 modification 呃对，它的 time，然后它的比如说 inode 编号， 一些一些一些一些 metadata， 同时我要知道这些边， 我要知道这些边。那如果是你们，你们怎么会会怎么存储这个数据结构呢？ 如果你想要实现一个图， 那你最自然的想法就是你设一个叫 struct node， 对吧，节点，然后你把这个节点所有重要的信息，就是你在那个 在这个 stat 里面能看到的， 把这里面能看到的各种东西，什么呃你看什么 st_mode 对吧，st_mode， 然后什么 st_size， 把各种东西全部都都放进来，这就是一个节点所有的 metadata。 然后 边怎么存呢？边在这个目录文件里。 对吧，边 因为每一个 这个图的每一个节点 它要么是一个普就是这样一个数据文件，要么是一个目录文件。如果它是目录，不管它是目录还是数据，它都有一个 byte array 对吧，它我还拥有一个叫，数据叫 index。 然后这个 index， index 就相当于是一个