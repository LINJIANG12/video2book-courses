上节课我们讲了虚拟化的最后一次课。我们从最早的，对吧，从应用视角说，操作系统就是一组API，然后把这个API掰开来一点一点嗯讲了很多的内容。然后但如果我总结一下的话，你们应该能留下一个这样的概念，就是我们的整个计算机系统，对吧，你看到的这样的一台计算机，我的一台计算机，或者你们手上的笔记本电脑，它都是从CPU reset开始的。它有一个最早的那个状态。然后CPU reset开始以后，程序就跨始运行了。firmware会先把操作系统加载。然后加载以后，操作系统会干一些事，比如说我上节课的时候讲了有一个叫initramfs，它有一个文件系统的树。最早的操作系统就会把这个文件系统树加载到内存里，它变成一个文件系统，加载里面的init程序。然后在这个之前其实我们也讲了，到底什么是程序，对吧。execve的系统调用，它会接收一个可执行文件，它是怎么被加载到内存上到加载到CPU上执行的。然后等到这个init程序在加载了真正的文件系统，你们磁盘的呃时候，再会执行一个系统调用，切换到一个新的init程序，就是我们在pstree里面看到的第一个进程，这个systemd就被创建出来了。然后它再用呃各种各样的系统调用去创建你们看到的应用程的世界，就是你们在屏幕上面看到的这一切。然后在这一切系统调用，从操作系统来看，它就是一个system call的API。但是如果你想要构建这个应用生态的话，它还有一层一层的洋葱。对吧，我有libc，一个有ISO标准一个可以移植的基座。呃你写C程序就可以移植到很容易移植到其它的平台，甚至是那种微小的控制器。然后在此libc之上还有很多的库函数。呃你们想要在终端上显示字符，想要读取字符，对吧，想要访问屏幕，想要创建图形界面的都是有相应的底层的库函数。然后在这个底层的库函数上面又长出了很多应用程序的生态，对吧，JavaScript的生态，Python的生态。比如你们打开一个Visual Studio Code，那它是Electron框架里面，它是一个JavaScript的程序在运行，但它会一路依赖到最底下的libc底层的库函数。然后这样的一个操作系统的设计，对吧，它是一个OS的API，或者我们叫它系统调用。它它往上是整个应用程序的生态，也是越长越大的。它往下，其实可以有兼容多个体系结构。这个学期的课程从x86，因为以前我一般都是在讲台上的这台电脑上面直接展示的。然后我今年我把它换成了呃我的一台树莓派。那它两个就是不同的架构，对吧，一个是x86一个是ARM64。呃还可以有RISC-V等等等等等。呃但是不同的体系结构上，其实它跑的是同一个操作系统的API。你们也知道Linux内核是用C实现的，现在开始加入Rust的一部分，对吧，它用C语言实现的，那C本身也是具有一定的可移植性，它freestanding的环境可以在x86上也可以在ARM64上运行。那操作系统的这个API就是一个很好的中间层，对吧，我的底下的硬件不需要知道上面的应用程序是是干什么的，它只要机械的执行指令就行了。而上面的应用程序甚至都不需要知道它运行的是什么样的CPU，因为我们的代码是C代码都是可移植的，如果你写可移植代码的话。那它在x86、ARM上都可以正确的运行。而像Linux内核里面体系结构相关的代码是它其中相对比较少的一部分。呃总会有一些，但是相对比较少。这样我们就构建了这个计算机系统的世界，所以你看到一个很好的抽象。你们回头再去回顾一下，比如说在复习的时候，回想一下这个课程的内容，你会想到这个应用程序的世界是怎么样一点一点被构造出来的。好，那么今天我们就正式进入下一个部分的内容是并发编程的呃入门。当然我这个叫从入门到放弃，不是让你们放弃并发编程，是让你们知道并发编程，尤其是多处理器并发编程它会遇到什么样的困难。呃以及我们会在若干次课的时间里面告诉大家应该怎么样去写并发程序，它性能又好而且又不容易出错。在之前的世界里我们都假设，说我们的程序就是一个一个状态机，对吧，就是一个状态机。那我们的程序是顺序执行的，它可以执行指令，对吧，如果是一个binary二进制的文件，它就是一条一条指令执行的。它也可以执行语句，如果如果这是一个C的话，语句。编译器就是把高级的状态机编译成，就高级语言描述的状态机编译成低级语言描述的状态机。但是呢，这样一个顺序执行的模型，对吧，这叫这叫顺序sequential的模型，它在写一些程序的时候，你会感觉到有明显的小麻烦、小问题。比如说，如果你想要写一个这个HTTP的服务器，对吧，到处都有HTTP服务器。你们打开你们的手机，你们的淘宝、抖音所有的东西，在你请在你你想要从远端获取任何信息的时候，都是通过一个HTTP的协议，呃或者说HTTPS的协议来完成的。好，那如果你要写一个HTTP的server，比如说你今天想开一家开个公司，然后它就叫什么淘宝，对吧，假设你你回到200几年的时候你想你想开一家公司叫淘宝，你要上线一个信息系统。那你希望做一个网页，这个网页可以收到一个请求，收到一个比如说我要淘宝的首页，你想把这个首页返回给它。那如果是这样的顺序执行的模型，你就会写出这样的代码。对吧，我我有一个HTTP server，然后它有一个fd，比如说啊打比方有个fd，这个fd可以不断的持续的读取，读取来自别人的请求，假假设想的，实际上不是不完全是这样。然后你你会不断的先分配一个buffer，然后从某一个地方读到一些数据。然后读完了以后，你就去handle它。有的时候，这个读会等很长时间。有的时候，这个handle request可以等相对比较长的时间，比如说它去读磁盘或者呃或者怎么样。对吧，也就是一个程序里面可能会有好几个呃好几个部分。比如说我我既要读磁盘，我又要读这个网络上的请求，我还可以做计算。但是如果是顺序的模型的话，如果我执行syscall的时候，比如说我syscall是一个read，哪怕我现在手上还有一些别的事情可以做，我都不能去执行这个read。对吧，如果我执行这个read以后，我哪怕手上有事情做，read没有数据的时候，我就必须要等这个数据得到。它就其实浪费了一些呃时间。我的CPU可能在read的时候被卡住了，它就在空转，对吧，相当于不是在空转，它就是不在不在处理我可以处理的事情，这是一种浪费。这是第一个呃我们想要呃有一个共享内存的并发编程的一个理由，或者说只是一个想法：我能不能，能不能有一个什么样的办法，能够解决我能够同时做几件事情，如果它们真的能同时做的话。那第二个动机是：呃我们现在的系统都是多处理器的，对吧。哪怕我的树莓派，如果我用lscpu去看的话，哎，我的我的树莓派里面也有4个CPU，它编号是0 0到3。那你们自己看你们的CPU可能会有更多，可能有几十个处理器。这几十个处理器，不管是你们的我我的这个还是你们的，还是甚至是一个比如128或者256核的一个机架上的服务器，它们的模型，它提供的硬件的模型都是共享内存的。你不用白不用。也就是说，它的物理内存是一个地址空间，一个地址空间。然后两个CPU如果它说CPU1说我要访问呃1 2 3 4这个地址，CPU2说我也要访问1 2 3 4这个地址，那CPU1写进去的数据CPU2过一会读，它就能读到。它是一个共享内存的模型。那多处理系统其实系统它是共享内存的，那这个共享内存的信信性质不用白不用。我们说我们怎么样利用好系统里的多处理器呢？我举过一个例子，说你你现在写一个程序，那你可以在比如说啊，比如说你现在现在一个程序需要呃素数表，那你就先先把这个素数表算出来，对吧，算出来以后然后你可以fork 100份。乘100。好，这个时候，呃我就会得到100个100个进程，然后每一个进程它都可以并行的执行，会被放到一个CPU上，你们可以做一个实验，对吧，你们可以看到CPU的利用率变成了200%、300%，它超过了100%，就说明你的进程它们真的是在同时工作的。但是，但是，这样的程序有一个问题。如果你想要收回这个fork的结果，fork进程的结果就很难了。对吧，如果你想要把，比如说我我你你想想要算一个，把一个很大很大的区间分成好多少段，然后你每一段里面会算出一个值，最后你希望把它加起来。哎，如果它是独立的进程，你就必须要再用管道这样的机制，对吧。你可以创建一个命名管道或者匿名管道，呃把这个数值送回给它的父亲进程，最后把它加上。但这个就很麻烦，对吧，很麻烦。我我们能不能直接这是一个很很显然的动机。我们能不能直接，如果是共享内存的多个进程的话，我在主进程里面有一个sum，哎，我这里就能不能sum +=什么什么？对吧，我在fork完的子进程里面，直接就把这个内存给写了。那我的父亲的进程只需要等到所有的进程结束了，我把这个sum打出来就可以了。对吧，这样我不仅可以可以做求和这样的事情，我也可以做更复杂的事情。比如说我要同时可以我可以甚至可以同时处理多个网络请求，对吧。这是这是我们想要引入共享内存并发的动机。然后这个动机你你再往后想，这也是很自...

按呢，就是你想要实现一个共享内存的进程。 对吧，我们已经实现了，用 fork 已经可以实现，哎，我可以 我可以把一个成进程对吧，如果在，在我 fork 以后， 如果我执行了 fork 以后，它就变成了两份，对吧。 在操作系统里面它就，有两份，它们两份是完全一样的，一个是，除了一份返回了子进程的 PID，另一个返回了 0，其他都是完全一样的。 进程它们就 就，就往前执行。那我们能不能 让这两个进程不要是分开的， 或者说 我能不能让这两个进程 它程序是可以独立执行的，但是它的内存 是共享的呢？ 这个就叫线程，这就叫 叫线程。你，你，你的模型，我们的 我们的模型是这样的啊。 如果你考虑到 simple-C，对吧，你考虑到 simple-C。 simple-C simple-C 是什么样的呢？simple-C 是 你的一个程序有一个函数调用栈， 对吧，在最底下是 main， 然后你每一次函数调用的时候你都会 放一个 新的栈帧到 栈的顶上，然后每一个 栈帧里面都有一个 PC。 这个是，是 simple-C 的 执行模型。 然后当然， 除了这个栈帧以外，我们还有全局的变量。 对吧，globals。 那么 你就知道， 如果你想要让两个程序 或者说两个进程 共享内存，但是又能够独立地执行， 每一个进程，或者说每一个线程， 它就必须要有一份自己的堆栈。 对吧，栈必须是独享的。如果两个线程 它的栈，因为我们的程序 simple-C 执行的 执行一个语句， 它的含义就是从 top-most 的 frame，最顶上的那个栈帧的 PC 选一条语句出来，然后把它执行了。那如果两个 程序 两个独立的运行的实体，两个线程它 共享一份栈的话， 这个就全乱套了。 所以 你，你想的是 我们如果要将这样的一个模型 扩展成是一个两个线程，比如说两个线程的模型， 我们就可以这样： T1 有一个它自己的栈， T2 也有一个它的栈， 当然你也可以有 T3， 对吧，你，你可以有，有 T3， 也，也可以有自己的栈。 但是它们共享 一份 全局的数据。 这就很自然的一个扩展，对吧。就是完全是 是你可以，可以，可以想到的。好， 那在这个基础上， 如果它执行一条 语句， 就它的一次迁移， 它怎么迁移呢？ 你可以把 一个栈加上全局， 把一个栈 加上全局的 这个内存看成是一个 进程。 对吧，这就是一个进程。一个进程就是一个栈加上一个全局变量。当然你有代码，你假设 代码我这儿就不写了， 代码也在全局的这个地方。好，然后如果你把 线程 2 的这个栈 和全局放在一起，紫色的，我先叫它 T2 对吧， 它又是一个 我们之前讲过的进程， 一样的概念。 所以什么是迁移呢？ 迁移就是我可以选， 我可以选 T1 执行一条，把 T1 的栈和全局放在一起变成一个进程往前执行一条语句。 我也可以选 T2 执行一条语句，也可以选 T3。 好，所以我们就在 这个传统已有的这样的定义上， 对吧，什么是程序 上面定义出了什么是，是线程。 然后呢我们为了管理多线程对吧，我们还是可以，可以沿用旧的模型。 沿用旧的模型，我们在任何时候 都有好几个相当于 T1， T2， T3 的栈， 然后还有一个 globals。 然后我们现在 可以在这个系统里面增加一个 API 叫 spawn， 或者说我们增加在操作系统里面增加一个系统调用叫 spawn。 然后这个 spawn 的行为， 画小一点啊。 spawn 的行为就是在，这个 spawn 比如说可以有个 f，可以，可以跟一个函数， 然后就是在已有线程基础上不变的情况下创建一个新的线程。 这是，这是比如说现在创建一个新的 T4，然后它这个里面有一个 stack frame 是 f。 然后当然 globals 是不变的。 我还可以，除 spawn 以外，我还可以有一个 exit 对吧，thread exit，那我，我就是把这个线程从这个系统里面 抹掉。 和你们的 exit，和 fork、exit 很像， 只不过这个 fork 不是发生在操作系统层的， 这个 fork 是发生在进程里面的。 一个进程里面它有共享内存的 线程。这就是线程，这就是你，你们 如果让你们从零开始设计一个 线程，你们就会这样设计。 对吧。 然后在，在，如果在这 指令级的意义上，指令级，那它只有一个平坦的 内存，就一个地址空间， 顺便一个，一个 mmap 创建出来的地址空间。 然后那么， 我们需要的就是在地址空间里面为每一个进，每个线程，thread 1，thread 2 分配不同的 区域作为它的栈。 然后每一个线程有它自己的寄存器。 然后寄存器里面有一个 stack pointer，比如线程 1 的 stack pointer 就是必须指向这个 T1 的栈。线程 2 的 stack pointer 就必须指向 T2 的线程栈。 因为程序一旦被加载到 CPU 上运行，它就是一个无情的直接执行指令对吧，它只能 load、store 和 system call。 那 它 load、store 的就是这个地址空间里面的东西。 只是 呃第一个线程它的 呃寄存器 里面的 SP 就要指向自己的堆栈的空间，然后 T2 就要指向自己的栈的空间。 这就是，如果，如果你们想要 设计一套，设计一套这个 呃共享内存的 进程的话，你们就会实现 spawn 和 exit 两个 呃两个系统调用。 好。 所以这就是你，这就是并发编程的全部啊。我就已经可以下课了，这个 concurrency 整个部分我就讲完了。因为 所有的 concurrency 剩下的部分都是从这个模型上面推出来的。 然后这里也就知道你打开了一个魔鬼的盒子， 呃对魔鬼的盒子。然后我，我，我昨天在准备的时候，我把这一页换成 slides 呃发给 AI， 然后让 AI 评阅一下， 然后 AI 直接这个让我 呃顺着补几刀把这个魔鬼具体化， 然后 AI 完美的预言了我后面要讲什么。 就我后面要讲，今天我要讲的所有东西，AI 都是知道的， 所以我真的不知道我还要上课，就为什么需要我，我还来上课对吧，我可能只要上到这儿，然后剩下你们就可以 跟 AI 对话了。 好，现在我继续，进入继续之前， 跟大家澄清一个概念，你们经常会看到这个概念叫并发和并行。 对吧，因为当我们说 这个一旦 我们的线程进入这个模型以后， 它就是并发的， 或者是并行的。 然后并发和并行有一个比较微妙的呃差别。并发意义上是逻辑上的同时执行，你可能只有一个 CPU， 你只有一个 CPU 那你每次就真的只能执行一条指令。 对吧，嗯一个单周期的 CPU，每个时钟周期 它就真的只能执行一条指令。 但是呢，如果你有两个程序，你可以让它一会儿， 一会儿是 A 程序在 CPU 上运行，一会儿是 B 程序在 CPU 上运行，那 A B A B B 交错执行， 在一个比较长的时间尺度上， 你看起来它们两个都被执行了， 这个就叫 并发。 它逻辑上是在一个时间段上面它 都有往前的 呃进展，但实际上有可能是操作系统和运行时库模拟出来的那个轮流的执行。 当然，如果它们真的是同时执行的，比如说 就是两个 CPU， 两个线程被加载到两个 CPU 上，它同时运行， 这个，这个就叫并行。 并行那是真正意义上的同时执行。所以 呃如果 你的程序是并发的， 它可能是并行的，也可能不是。 但如果你的程序是并行的， 那么它一定是并发的。 对吧，它已经，已经 在物理时间上面就同时发生了，所以它一定也是 呃也是并发的。 然后在这里有，有挺，呃一个挺有意思的理解啊， 就是在刚才那个概念模型上， 我有两种表示方法， 一种是 你看我现在有 T1 和 T2， 然后我可以选 T1 执行一步，我也可以选 T2 执行一步。 这是我，我刚才定义的，对吧，因为我现在有两个线程。 然后比如说我现在 T2， 呃 T1，T2，然后我现在如果执行 spawn， 比如说啊， 比如说 T2， 选 T1 执行 spawn， 它就到一状态 T1，T2，T3， 对吧，就会到这样一个 呃状态。 你的状态看起来是 是这样的，它这样它就可以选 T1，选 T2， 选 T3。 然后还有一个 模型， 更接近于实际 CPU 实现的模型， 是 呃你有 T1 和 T2， 然后你走一步的时候相当于 T1 和 T2 就同时， 同时往前 呃往前走，走了一步。 但是你们也知道实际的处理器处理每条指令的速度是不一样的。 对吧，你，你有，有的指令你，你这条指令你 cache hit 了，它就执行得比较快。 有的指令 cache miss 了，你就执行得比较慢。 然后你有的 load、store 的指令，它就天生就比你寄存器的指令要慢一点，你的乘法和除法可能就要好几个周期。 所以 呃所以这个模型在，我说选两个 呃线程同时执行一条语句， 这个就有点过度简化了。 在实际的系统当中，就真实的 CPU 上你是很难理解并行程序执行到底发生了什么的，因为你不知道一条指令到底执行得多快。 执行指令，这条指令执行的快和慢是取决于你当前整个系统里面 呃各种各样复杂的 呃状态的。这里面我给魔鬼埋了一个伏笔。当然你们可以，可以，可以理解这两种 对吧，在概念上可以理解这两种 呃 理解线程的 方式。 好。 那既然你已经在理论上把这件事情 呃想清楚了，那我们就可以 呃做实验了。 做实验那我给大家提供了一个 方便大家理解的最小的线程库。这个库非常好用，呃它就实现了我刚才说的这个 呃这个 spawn 的 呃功能。 我们看一下，这个线程库，首先 操作系统给我们提供的 功能比 我说的这个简化的模型要多。这里有一个手册叫

pthreads。你们可以看这个man 7的pthreads。当然，我们现在也在这，也不需要，也不需要看了，这个我可以管道给AI让它帮，帮我看。对吧，这是我，我把操作系统给我们提供的线程库做了一些减法，这个只适用于最简单的，它就两个API。第一个是spawn。它创建一个入口函数是function的线程，它可以传一个函数指针进去。然后这个，这个函数是可以长这样。每一个function有一个整数的线程号。线程号就从1开始编号，所以，所以我这function你可以，我可以spawn一个T，对吧，我可以spawn比如说一个T，然后我可以再spawn一个T。这样，两个是同一个函数，但是它们的参数不一样，第一个spawn会得到1，第二个，相当于会调，会启动一个线程调用T(1)，第二个会启动一个线程调用T(2)。然后它们就，就是共享内存，而且是独立，独立运行的。这是spawn。然后除了spawn以外呢，我还有一个功能叫join。join就是等待所有我放出去的那个线程，它们结束。因为你的函数是可以返回的，对吧，你任何一个函数都可以返回，然后返回以后，这个线程就，就结束了。然后我现在可以等，等所有的线程返回。然后我的，我的线程库里main会默认返回join所有的线程，但是你的pthreads不会，就是操作系统给你提供的库不会，所以我为了简化，为了简化让你，你让代码看起来容易理解一点，做了这样的简化。它的行为就是，我在每次创建一个新的线程的时候，我都会记个数字。你可以这么理解啊，比如说我创建一，调用两次spawn，我就会记个2。然后我每一次线程结束，我也都会记一个数字，哎我有一个结束了，两个结束了。那我就不断的等，等到直到等到所有的线程都结束，我，我的main函数才能真正的结束。有兴趣的同学你可以去看一下线程库的实现，当然这个线程库的实现也是比较比较straightforward。好，它就是调用了我们POSIX线程，POSIX标准的线程，所以这是标准规定的，也就意味着各种各样的平台上都会支持的功能。一旦成为标准的好处就是你可以放心的在绝大部分足够好的平台上使用它，比如说你们的鸿蒙系统之类的，对吧。可能是一个和Linux不兼容的系统，但是它一般说都会兼容POSIX thread，以能够移植更多的第三方的库函数，第三方的应用程序到它的系统上。对，有兴趣的同学可以看。那这个线程库怎么用呢？看一个程序就知道了。我有一个hello.c。啊，这就是一个普通的C程序。一旦碰到spawn的时候，就会，这个系统里面就多了一个，相当于就多了一个函数，然后这个函数是和其他函数独立运行的。函数是while (1) print A，第二个函数是while (1) print B，那你就会预期，你不会看到AAAA打到底，BBBB打到底，你会看到交替出现的A和B。然后这个能力是之前我们在不并发的世界里面，当然你多个进程理论上说可以实现这一点啊，但是在一个进程里面，一个程序里面以前你是实现不了这个功能的。那我们当然可以运行一下，当然你可以假设我实现的是对的，对吧。你可以看到一个随机的，交替出现的A和B。符合预期，对吧，因为，因为你不知道，首先你不知道我们的操作系统什么时候会把A和B放出来执行，然后你也不知道这个他们执行到底谁快一点谁慢一点，所以你看到的就是一会儿A一会儿B一会儿A一会儿B。这个结果是符合，符合我们对并发和并行的预期的，它实际上是并行执行的。实际上是并行执行的。好，这个就是并发程序的，线程库的运行。好，那么下，下面一个问题。如果你有了这样的一个线程库，你马上就可以写代码了。那你作为第一次接触多线程编程，你可以问出什么问题呢？或者你想改一改这个TA和TB来实现哪些有趣的功能来加深你对线程这件事情的理解呢？我们至少可以干几件事，比如说，我们可以证明这几个线程确实是并，并行的，而且它们确实是共享内存的。对吧，我怎么证明它是共享内存的呢？我怎么证明它的栈是独立的呢？如果它的栈是独立的，每一个线程它的栈都在地址空间里，那这个地址空间应该有一个头，有一个尾，这空间到底有多大呢？诶，对吧。你就可以问出很多这样的问题帮助你理解我刚才说的这个概念的模型，对吧，是不是我，我刚才随口提了一，好像随口提了一句啊，在指令的意义上这个地址空间是平的，对吧，程序从线程和进程的意义上，地址空间就是从0开始到一个最大很大数字的一个字节的数组。那所有的一切都是由寄存器里的地址控制的，栈有个栈顶的指针，代码我也可以对main取地址，甚至可以把这个地址给打印出来。然后，然后如果要作这些证明，我们就可以自己，自己写程序，对吧，可以自己写写程序。比如说，如果你想要证明我的线程确实是共享内存的，我在这里做了两个线程，一个是x++的，一个是y++的。然后x++的线程，我x每次x+1，然后我sleep 1秒钟。然后我，我y++的线程每次y++以后我sleep 2秒钟。我是在main函数里面读x和y的，对吧，我每隔一小段时间，我就会去读x和y，然后把它打印出来。那我在一个线程里面写，在另外一个线程里面读，如果我能读到，就说明确实是共享内存的。是这个道理，对吧。那我运行这个程序，你可以确实看到，哎，x走的是比y快的。那就非常确信的知道，噢，我的至少数据的部分它是共享的。类似地你也可以证明代码的部分是共享的，然后你也可以知道线程的栈也是位于同一个地址空间里的。为什么呢？你可以在，你可以在这里比如说可以在这里打印一个，你可以定义一个全局变量，局部变量，x_local，你可以取它的地址。你取它的地址你会发现，噢，所有的东西也都是在同一个地址空间里，你甚至可以把一个指向局部变量的指针传递给另外一个线程，然后另外一个线程就可以访问这个另外一个线程的局部变量，这都是可以的，因为它们都在同一个地址空间里。通过这些实验的程序，你对线程就可以有一个比较好的理解。以及，线程是否具有独立的栈？它的栈有多大？如果你想要知道线程的栈有多大，你们有什么办法呢？当然你可以直接问AI说线程栈有多大，它马上就，马上就能告诉你了。那你能够写一个程序来验证这一点确实是真的吗？也可以的。我这给大家准备了一个test-stack.c。它是这样工作的，我有一个无穷递归，我有无穷递归，这个这个叫，叫probe。然后我probe是一个不断往往后，你看我会定定义一个scratch，就无所谓你我也不用的，每次栈帧都会有一个64字节的这样的一个不用的数组被分配。然后我就无穷递归，总有一天这个stack会overflow，然后这个系统就crash了。线程就，就crash了。但是呢，我一边，我一边在这个递归的时候，我都会把我局部变量，这个，我当前这个局部变量的这个范围更新到一个high和low里。你看一个叫，我有两个指针，一个叫high一个叫low。然后这个update_range就会把我看到的，我看到的最小的和最大的地址都给，都给更新了。所以我，我干一件什么事呢？我现在在上课的时候，老师声称这个线程栈是在地址空间里的，好。那我现也不知道它在哪，但是呢我知道的是，在我这个函数刚刚调用的时候，它应该栈是在一个可用空间比较，还比较，就没浪费掉绝大部分的空间，它只用了一点点。然后这个栈会从高地址往低地址生长，或者低，也可能是低地址往高地址生长，实际上是高地址往低地址生长的。然后每次函数调用呢我都会加一个新的栈帧，这里面就会有一些比如说64字节的一个局部变量，64字节的局部变量。然后我直到走走走走走，总有一天呢时候它会overflow。然后我不断的在overflow之前把我已知的最小和最大的地址给打印出来。诶，那我不就，对吧，那我不就可以实现观察到我的栈的大、大小了吗？所以也可以，test-stack。诶，你确实看到了这个栈越长越大越长越大的过程，对吧，你看，stack 2，T1，T2，T3，我们通过不断的去探测、试探这个栈，我们发现了T1的stack最后打印的是T1的stack大于等于8183，所以你们认为这个栈有多大？这是个离某一个整的数字非常近的，对吧，它离8192非常近。它有8192 KB，就意味着每一个线程，我创建出来的线程都有一个8兆的堆栈。这个和你们平时，就当你启动线程的时候就跟你平时，比如说用程序的时候，它以可以有一个几乎无限的栈，就不一样了。对吧，你们在Linux里面默认的时候，程序可以用的栈是几乎无限的，它可以一直往后用。但是，因为我们的每一个线程，我们说每一个线程的栈都在地址空间里，所以你必须在创

创建线程的时候，就决定好它的栈有多大。因为它必须在地址空间里面分配，分配一段，额，一段内存。好。至少你现在知道它是，它是8兆的。那我也可以让这个AI来帮我，运，运行一下。那我也可以让它帮我运行，然后它可以，它可以读这个代码，然后它理解这个代码以后，它还可以帮我，去解读这个结果是，为什么。我今天换到GLM 4.1了。在非高峰的时段，它还是运行得很好的，出token速度也很快，但是到现在，比如说码农们都开始上班了，对吧，10:45正好是这个时间，它的，你看它的这个token per second就非常，非常地慢，它已经serving，serving不过来了。啊，所以在某种意义上我就翻，直播就翻车了，因为它的这个serving速度已经，已经完全没有办法，办法容忍了。我，我昨天在试的时候其实还可以的。好，我知道了，我今天，今天我今天就不在这个地方做live，live coding了。但至少我们刚才看到的那些，那些结果都是，都是正，正确的。好。以及啊，刚才我们说，我们至少可以很便宜地写一些程序，或者让AI写一些程序来帮我们验证关于共享内存，多线程的这样的模型。与此同时，我们，也可以问自己，多线程的程序能不能用GDB调试呢？哎，答案肯定也是，也是肯定的。我就直接展示一下，比如说，我加一个-g的编译选项。好。哎？直接运行这个GDB，它就说好全部都，全部都结束了，exited。如果你想要调试一个多线程的程序，首先我可以打断点。我可以在两个线程的入口的地方，比如说都，都，都打上断点。然后我可以，这这AI都可以告诉你，因为以前我们上这个课的时候，我的第一句话就是好，你去read the friendly manual吧。这是GDB的文档。你看GDB里面的threads，它告诉你怎么调试多线程的程序，去读吧。但现在AI帮你读过了，所以你，你，你相对来说我就，就展示一下就可以了。这个时候，我可以看，GDB还有一个功能叫info threads。你们可能没有用过这个功能。info里面可以看好多好多的东西的，对吧。你看我info Tab补全一下，可以看到好多有用的信息，有些你，你知道，有些你可能不太知道。你花点时间，去，学一下，还是很有好处的。好。info threads，你现在可以看到，现在的线程，只有一个，对吧，ID是，是1。然后呢，一般来如果我们要调试多线程的程序，我们会用这样的一个叫set scheduler-locking on。它做的是什么呢？我们刚才说线程有两种模型，一种是你可以选一个T1，选一个T2，选一个T3执行一步。相当于是我选T1的时候所有其它的线程都是不执行的，都被暂停了。还有一种就是我说我，我每一次所有的线程都是往前推进的。然后如果我，我默认的时候它的这个scheduler-locking是off的时候，那我线程1执行一步的时候，线程2和线程3还在往前执行。那可能会影响我们调试，所以我这个set scheduler-locking on是把我们的调试器退回到这个，这个模式。我们每一次，比如说我现在可以正常调试它，对吧，我可以正常调试它，spawn，spawn。但是如果我continue的话，你看我continue的话，我的main函数是在，实际上是在join的。main函数后面返回的时候它有一个join，join会等待所有的，其它的线程结束，但是其它线程，它都卡住了，对吧，它不会执行的，所以我现在我要打断它，对吧，打断它。然后，我可以，我可以info threads，这个时候，你就可以看到三个线程了，1 2 3。那我回到thread，thread 2上。我到thread 2上continue，我就在Ta。我到thread 3上continue，它就在Tb。哎？对吧，我可以在线程之间切换，这样我就完美地把，我们一个实际的程序，对吧，这是一个真正的程序，你看我可以，可以看assembly的，对吧，它是，它是有assembly的，一个真实地在CPU上运行的程序，对应到了这个模型上。我们有多，多个线程，然后每个线程都有自己的栈，有它的PC，然后每一次，我都可以在GDB里面选一个线程执行一个单步，对吧，我可以thread 1就选T1，thread 2选T2，thread 3选，选T3。最终，你就可以把这个多线程的模型给调试好，理解好。当然，调试多线程的程序是一个很大的麻烦，你会花很多的时间在线程之间切来切去切来切去切来切去。现在AI做得还不是特别好，但是我们可以期待一下也许明年的时候，AI就可以很好地帮你调试，这个并发程序了。OK。好。这就是多处理器编程的入门。我们，你已经入门了，对吧，入门了，而且，而且你用了这个线程库，写出了一些有趣的代码，确保你对多处理器编程是有，还是比较好的，理解的。OK，很好。那就该到放弃的部分了。我们放弃讲多处理器编程放弃，你们第一个放弃的是状态迁移的确定性，也就是这个魔鬼来自这个模型。说到你们要放弃什么呢，就要说一说，一个概念叫做确定性。这个词有点，对吧，有点拗口，你们，你们有接触过这个概念吗？有没有说过什么，你们在哪一门课学哪一个概念的时候学过这个确定性这个词？算法？计算复杂性，OK你学过计算复杂性。确定性，所谓的确定性，这个字面的含义还是我很喜欢的。数学就是一种确定性，具有绝对明确，没有，没有分歧，没有别的可能性，只有一种可能性，这叫确定性。所以数学的确定性来自什么呢？数学确定性来自于函数的定义，你们在学离散数学的时候都觉得函数的定义是一个，为什么要这样定义对吧？你们花了，至少花了一些时间去适应函数的定义。然后你回头想数学里面的函数，这个对吧，f(x)=x^2+1，这个东西就是有确定性的。它就是一个存，existence，它存在在那，然后它确定下来了，你，你给一个f(1)，它永远是同样的值，这个就是确定的。你不管在什么时间什么地点，调用这个函数f，它总是，一定产生唯一的，而且是确定的输出，这就是数学的，确定性。然后这个确定性，使得我们研究数，全世界的人，研究数学的时候可以都说同样的数学语言，大家达成共识，这个数学才能发展，计算机科学才能发展。然后程序的确定性和数学的确定性其实是，是类似的。不管我们是说Simple C，对吧，是一个C程序，还是一个，比如说NEMU的模拟器，一旦程序的状态确定了，对吧，我们说Simple C，我们有看到它，一旦程序的状态确定了，那我们的迁移，它也确定了。迁移是什么？迁移是一个函数。迁移是一个f，你们，你们有没有想过，你们实现的NEMU本质上实现了一个函数f(s)=s'。对于一个状态，对于s的一个状态，你可以确定的算出它的下一个状态。虽然你可能会从外面，比如说从外面再读一个随机数什么的，那也是状态的一部分，对吧，也是状态的一部分。它是一个确定的，至少绝大部分确定的数学意义上的函数，它可以把一个状态变成下一个，下一个状态。好，这意味着什么呢？这意味着，给定相同的初始状态，也就是说给定相同的初始状态，你这个程序运行多少次，无论运行多少次，它的输出一定是一样的。对吧，因为你的每一步都是确定的。你的每一个单步是确定的，根据数学归纳法，你的整个程序的执行也是确定的。就假设，假设这是，假设这是确定，确定的。所以虽然我把这个数学视角的这个操作系统砍掉了，但是你在理解并发程序的时候你去回到这个视角，我就有的时候还，还很很有用。那确定性就给我们一种可控的感觉，对吧，这个程序，你不会今天运行它是对的，明天它就不对了。至少这个测试用例，你这代码不改，你今天是pass的，你明天这个作业交上去，它还是pass的，对吧，总是，总是可以重复的。而且这也是我们喜欢functional programming的原因，如果你们学过SICP的话。functional programming它，它就要求你把所有的东西都写成是function，是这样的数学意义上的function，pure function。pure function就，就它就是一个绝对的deterministically reproducible的东西。这是我们喜欢的原因。但是，引入了并发，彻底摧毁了这一点。我觉得完全是引入了一个魔鬼，对吧，完全引入了魔鬼。在实际上，刚才我，我再补充一下，实际的程序实际上也是某种程度上确定的。因为假设你不调用什么随机数什么的，程序所有的不确定性都来自于系统调用。当这个程序执行execve的时候，它的初始状态里面确实是有一些东西，比如说，比如说它地址空间是随机的，对吧，比如说这个main函数的地址是随机的，然后它里面有auxv，有一些系统的信息，我们

讲链接和加载的时候，加载器呃可以 操作系统的内核里的加载器会把栈上面放上一些比如说甚至是随机数种子这样的信息在在这个地方。但是， 一旦你的进程的初始状态确定了， 实际上这个函 迁移的函数也是确定的， 除了系统调用， 对吧？read是不确定的，你不知道我这次键盘按的是a还是b， read是不确定的，但是程序的执行是确定的，程序是可以 可以理解的。 但是共享内存的并发其实打破了这一点。 因为线程执行的速度是没有保证的。 对吧？刚才刚才我有一个例子， 我有两个线程，一个在执行x++，一个线程在执行y++，还有第三个线程在读x和y。那 我每一次x读的时候， 对吧？我们回到 回到这个程序，你们还没有 在看到那个程序的时候， 你们还没有理解这件事情的可怕之处。 你们在看到这个 看到这个例子的时候，欸觉得啊我们很好证明了呃 确实线程之间是共享内存的，你们没有意识到 它带来的可怕之处。 我们中间使用了sleep，使用了系统调用。 线程执行的速度是没有任何保证的。 哪怕我是 哪怕我就是x=x+1, x=x+1， 我的处两个处理器它的频率可能不一样， 对吧？它执行的它当时的缓存也不一样，它执行的速度就是不一样的。