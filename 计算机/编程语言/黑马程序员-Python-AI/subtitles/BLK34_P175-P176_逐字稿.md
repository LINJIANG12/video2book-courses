# BLK34_P175-P176 逐字稿：Web应用-AI汉字谜盒-开发规范与基础环境准备

好的，那学习完了面向对象高级以及FastAPI的基础知识之后，那接下来呢我们就一起来完成一个Web案例：汉字迷盒。那通过案例驱动的方式啊，让大家体验并感受一下Python在Web开发当中的应用，并掌握FastAPI在Web应用开发当中所涉及到的一些核心知识。

那么关于汉字迷盒这个案例呢，我们在开发的时候啊将会分为以下四个部分来进行。那么首先第一个部分呢，先和大家聊一聊啊，就是我们当前这个案例的一个开发的规范；啊然后第二个部分呢，再来搭建一下当前案例的开发环境；那么第三个部分就要来开发这个汉字迷盒项目当中所涉及到的一些核心的功能了；啊那么最后一个部分呢，再对我们所开发的功能进行一个程序的优化。

好，那么接下来这一小节呢，我们就先来介绍一下当前这个案例在开发的时候对应的开发规范是什么样子的。

那我们当前呢开发的是一个Web应用，那前面我们也介绍了，那一个Web应用程序呢，它的三个组成部分分别是：前端程序、服务端程序和数据库。那么当前呢我们是一套Python基础课程，那里面不涉及到数据库的知识，所以呢关于数据的存储，我们依然会采用文件来进行存储。那接下来呢，我们所讲解的开发规范主要指的就是，那么前端在与服务端交互的时候，对应的规范是什么样子的。

那我们提到前端呢，它主要是有HTML、CSS以及JS组成的。那么这些个前端技术呢，它主要负责将数据以好看的样式呈现出来。那么界面当中所呈现的数据是从哪来的？是不是需要去请求服务端，服务端给它返回的？哎所以呢，那么在服务端呢，我们在这一块在基于FastAPI再去开发的时候，那么就要去开发一个一个的功能接口，哎就是一个一个的API接口。然后功能接口开发好了之后，那么接下来前端程序是不是就可以发送请求来访问我们这一块的功能了？哎那么访问完这一块的功能之后，那服务端再给前端响应对应的数据。那最终我们的前端程序呢，再将服务端响应回来的数据将其呈现出来，哎以好看的样式将其呈现出来。

哎所以在这个工作流程当中啊，那么最终前端是需要去访问我们所开发的服务端的功能接口的。啊那么在访问服务端的时候，只需要发送对应的HTTP请求就可以了。而前后端在交互的时候呢，那这里我们所使用的接口规范是RESTful风格的接口。

那么这里呢，我们就需要来介绍一下，那什么是RESTful呢？那么我们来介绍一下。那RESTful呢指的是遵循REST架构风格的API接口服务。

好，那么这块又提到了一种架构风格啊，叫REST风格，那什么又是REST风格呢？那REST呢，它其实是这三个单词的简写。啊那这三个单词呢，翻译过来啊叫表述性状态转换。那么它呢其实就是一种软件架构的风格。

但是大家会发现，这个REST好像涛哥你翻译了和没翻译好像都差不太多，为什么呢？因为这个英文啊我好像不太认识，对吧？这个中文你翻译过来叫表述性状态转换，翻译过来我好像也不太认识。

哎是的，这个REST呢，其实它直接翻译过来你会发现是比较晦涩难懂的。啊那么接下来呢，为了大家理解到底什么是REST风格的API接口，那么接下来呢，我就以一组示例为例啊，给大家做一个对比，大家就明白了。

大家来看。那么在没有这个RESTful之前啊，那我们在进行项目开发时，我们所使用的这个API接口的风格，那其实就是传统的API接口。那么传统的API接口对应的请求路径长什么样子的呢？大家来看。

呃那么对于传统的API接口啊，它的请求的路径，啊也就是请求的URL呢，大概的形式是这样子的。好，当然呃这对于一个URL来说，前面这个http部分，这块是不是就协议啊？哎然后呢，localhost，这是不是就是请求的IP地址啊？访问的IP地址是谁。那么8000，这个是不是就是对应的端口号？哎那么我们在前面讲到，那每一个服务启动起来，是不是都会占用一个端口号？那8000呢，就是我们的端口号。

好，那前面这一块是一样的，我们主要来看一下8000之后，也就是我们访问的这个后面这部分，我们叫访问的资源路径，啊主要看后面的这一块的路径。那么在传统风格的URL当中啊，那后面这一块的路径定义的相对来说是比较随意的。比如呃那这里呢，我要去查询id为1的这个用户，好，那么这块的路径就定义为/user/get_by_id。那查询id为1啊，id为1我就在问号之后，啊你看，GET请求的请求参数是不是就在问号之后携带过来的？你看，问号id=1，哎那这就代表我要查询id为1的用户信息。

好，那么紧接着再来看。那么如果是要新增用户呢，哎那我这块路径就定义为/user/save_user。啊那么前面这个/user呢就代表，那我要操作的是用户的信息，而/save_user，那这一块你会发现save就是保存，啊那我要保存用户的信息。好，那么这里的请求方式呢，POST。

然后紧接着如果是修改用户，你看，/user/update_user。啊然后紧接着删除id为1的用户，那就是这一块的user/delete_user。那删除id为1的，所以在问号后面把id为1传递过来，请求方式依然是GET。

那么大家会发现，在传统风格的URL当中啊，那么其实呃是喜欢在我们的URI，也就是后面这部分资源访问路径当中，会加上一些动词来区分那么这个URL，这个接口具体是做什么用的。你看，get，啊那是获取，是查询用的；那save呢，哎就是新增、保存用的；那update呢，啊就是更新用的；那么delete呢，就是删除用的。

好，当然大家这么一看呢，好像也没问题啊，哎你通过这些呃动词来描述我们执行什么样的操作，看起来很直观啊。但是大家别忘了一件事，什么事呢？就是在企业项目开发当中啊，那么一个项目组少则几个人，多则几十号人，再多点几百号人，而且一个项目组它经手的人，它不是说就这一两个人经手。项目组的开发人员就是来的来、走的走，这一个项目可能经历了几十手，哎也就说经历过几十号开发人员来开发，对吧？有的人离职的，啊有入职的。那这个时候大家再想，那么这些开发人员他们的这个开发规范、他们的技术水平、他们的英文水平是一致的吗？

哎是不一致的。每一个人都有自己的开发习惯，有自己的命名习惯。那也就意味着针对于同一个需求，针对于同一个功能，那么他所定义的这个URL这一块的这个访问路径可能各式各样。就比如新增用户这一块的这个路径来说，那么有的开发人员习惯用的是什么？/user/add_user，对吧？有的呢叫/user/add，有的呢可能喜欢用insert，/user/insert_user，那有的呢就/user/insert，还有/save_user的，还有/save的，当然还有很多。

那每一个人是不是都有自己的开发习惯啊？哎如果说都按照自己的习惯来命名这个API接口的访问路径的话，那么最终你会发现我们这个项目在开发的时候，它是没有一套统一的规范的。那这就会造成时间长了，我们这个项目最后你维护起来会很繁琐，因为没有一套统一的规范。这就是传统风格的URL它的弊端：不规范，后期难以维护。

那么现在呢，在企业项目开发当中啊，在进行这些接口开发的时候，基本上都会遵守REST架构的风格。那么接下来呢，我们就来看一下，那REST架构的风格的这个URL地址到底长什么样呢？来，我们一起来看。

那么在REST架构的风格当中啊，大家会看到这个资源访问路径后面这一块的这个路径的定义啊，会非常的简洁。你看，/users，这就代表好那我们要操作的是用户的信息，你看这块是不是都是/users。然后到底对这个用户的信息进行什么样的操作呢？哎那这里呢是通过请求方式来决定的。

那这里呢会用到四种请求方式，因为请求方式它不是自定义的，请求方式它是固定的。这是HTTP协议当中它是固定的东西。那么这里呢，用到四种请求方式，分别是GET，那GET呢，啊就是查询。啊一旦我见到这个请求方式是GET，那我就知道他要执行查询操作。那如果是DELETE，那我不用说，大家应该知道，啊是删除的，啊是删除信息的。那POST呢，它是新增的；那PUT呢，哎它是修改的。

哎也就是说啊，那么现在这个URL这个接口它到底执行的是增删改查什么样的操作，不再取决于开发人员在这个路径当中所定义的这个动词了，而是取决于什么？而是取决于对应的请求方式。通过四种请求方式分别来表示增删改查的操作。

好，那么紧接着我们继续来看。那么在进行操作时，那如果说我现在我要去查询id为1的这个用户信息，怎么做呢？那查询，请求方式是不是就GET？哎然后在请求路径，啊你看在请求路径当中，这一块来了一个/1，那么这就代表啊，那这里呢我要查询的是id为1的用户。好，那么紧接着大家再来看，那下面这一块是不是也有一个id为1的这个用户啊？你看/users/1是不是也是id为1的？那么到底对这个资源id为1的这个用户信息到底去进行什么操作呢？你看什么？看请求方式。哎，请求方式如果是DELETE，那就是删除；请求方式如果是GET，那就是啊查询。

好，那么紧接着呢，大家再来看下面这两个URL。你看下面这两个URL，它的URL地址是不是一样的？都是/users。那么/users这个/users到底是执行什么操作呢？诶，你看请求方式就行了。啊请求方式如果是POST，那就是新增；那如果请求方式为PUT，那就是修改。

好，那这就是REST风格的URL。那么大家会发现在REST风格的URL当中啊，这个呃资源访问路径后面的这部分路径定义的特别的简洁啊。哎在这里呢，我们并没有去通过动词来描述这个URL到底是进行的是什么样的操作，只是在这一块里面指定了那我们要访问的是哪一个资源，来定位我们要访问的资源的。你看/users就知道我要访问的是用户，那访问的是哪个用户？/1那我就知道我要访问的是1号用户。所以在这个REST风格的URL当中是不是通过路径来定位要访问的资源？

那么具体对这个资源进行什么样的操作，是增加、修改还是删除还是查询，那这里呢就得看请求方式了。啊那么对应于这四种请求方式，GET是查询，DELETE是删除，POST是新增，PUT是修改。

所以呢，对于REST风格的URL来说呢，呃其实两点啊，核心就是两点。第一点，通过URL来定位资源，哎就是通过这个URL只是来定位资源，来定位到我们要操作的是哪一个资源。那具体要对这个资源进行什么样的操作，得看什么呢？得看请求方式。啊那这个请求方式呢，我们也叫HTTP的动词。啊这个HTTP的动词指的就是这个请求方式啊。就是通过请求方式来描述具体的动作，啊你到底是查询、删除、新增还是修改。

好，那这就是REST风格的URL。啊那么这种风格的URL呢会更加的简洁，更加的规范，也更加的优雅。那这也是现在企业项目开发当中啊，那无论是什么样的项目，无论你是Python的项目还是Java的项目，在进行接口开发的时候，啊那现在基本上都是REST风格的URL。所有的开发人员都按照这个规范来开发对应的接口就可以了。

好，那么经过这么一组对比之后啊，我们简单认识了什么是REST之后啊，接下来呢，我们再来介绍一下REST当中涉及到的两点注意事项。

第一点，REST是风格，是约定方式，约定它不是规定，是可以被打破的。啊也就是说，是可以不按照这个规则来来的。也就意味着我即使请求方式是GET，啊那我也可以在对应的这个逻辑代码当中，我来执行删除操作，是可以的。啊所以呢，这个REST它是约定，约定它不是规定。啊但是呢，呃我们作为一名这个高素质的开发人员，啊那我强烈建议大家啊，那么在进行今后的项目开发当中，尽量都按照规范来。啊你不要在这一块，你的请求方式设置的是GET，哦别人一看，哦这执行的是查询操作，结果你在代码当中你却给我执行的是删除操作。啊那这样呢，就很不规范了，啊会显得你很不专业。

行，那么这是第一个注意事项啊。那第二个注意事项呢，就是在RESTful风格的这个接口当中啊，那么在去描述功能模块的时候，我们经常会使用复数形式，也就是加s的形式，以表示我们操作的是此类资源，而非单个资源。比如，大家来看，我们上面的这个示例大家会发现，我这一块都是什么？叫/users。啊，users就代表我要操作的是用户这一类的资源，啊不是说操作的是单个资源。然后紧接着后面这一块/1就代表那我要操作的是这一类资源当中的哪一个呀？噢是这个id为1的。那这个id呢，其实就是我们资源的唯一标识。啊通过这个id，我就可以唯一的定位到这个资源。

好的，那这是我们这一小节给大家介绍了一下，那我们当前这个案例的开发规范。那这也是现在企业级项目开发当中啊它的一个接口的开发规范，啊开发的是RESTful风格的接口。

好，那么我们讲解完毕之后啊，最后呢，我们就简单做一个小结，主要明确两个问题。第一个问题，REST风格的特点是什么？啊，简单说一句话，啊就是，通过URL来定位资源，就是定位我们要操作的是哪个资源。那么具体要对这个资源进行什么样的操作，得看执行的请求方式。啊，那么这个请求方式呢，我们也叫HTTP的动词。好，那比如这里大家所看到的这个URL，那么这个URL呢，我们就知道，啊它是用来操作啊用户的资源的，那么操作是哪个用户呢？啊是这个id为5的这个用户。然后那对id为5的这个用户进行什么样的操作呢？那我们通过这个URL你是看出来的，所以URL它仅仅是定位我们要操作的资源的。那么具体要对这个资源进行什么样的操作，得通过HTTP的请求方式来描述。那如果说我们HTTP的请求方式啊为DELETE，啊那我这就知道了，它是要删除id为5的用户。那如果是GET，那我就知道它要执行的是查询操作。好，那么这就是REST风格的特点。

好，那么紧接着再来明确第二个问题，就是REST风格当中涉及到的四种请求方式以及对应的操作是什么样子的。好，那么如果是GET那代表的是查询；如果是DELETE那就是删除；如果是POST就是新增，也就是添加；那如果是PUT那就是修改。啊所以这四种请求方式呢，分别对应于增、删、改、查的操作。

好的，那这是我们这小节在这一块介绍一下我们当前这个案例的开发规范。当然这个开发规范呢，不仅是针对于我们当前案例，大家以后再去开发其它的一些项目的时候，基本上也都是按照这个规范来开发的。

黑马程序员为千万学生少走弯路，我们一直在努力。

---

好的，那上一小节呢我们介绍了一下当前这个案例的开发规范。那接下来这小节呢，我们再来完成案例基础环境的搭建。

那首先呢我们先来看一下，就是汉字谜盒到底是一个什么样的一个项目。那汉字谜盒呢其实它是一款基于AI大模型的一个字谜互动的个游戏，那主要呢是为汉字爱好者设计的。啊那我们来看一下这个项目的一个介绍啊。

那么在这里呢我们可以与AI大模型进行交互，那AI大模型呢会随机的出一套经典的字谜，哎比如你看这里AI大模型出了一个字谜为“一箭穿心”，打一字。好，那么接下来呢，用户哎就可以在这一块给AI大模型进行交互了来猜这个谜语，比如这个“一箭穿心”哎那就是“必”字，对吧？哎那我们在这一块回复一下我们所猜的这个谜底，那接下来AI大模型就会自动的判定哎你当前猜的这个谜底到底是对还是不对。那如果是不对的，那接下来它会给出提示；如果是对的，它就会告诉你啊答对了，要不要再来一题？如果你说来，好，那么接下来继续出下一个谜语。啊“半部春秋”，哎那接下来继续进行猜谜。啊那如果说这个谜语比较难，你可以让AI大模型给你提示一下。比如你给我提示一下，AI大模型就会给你相应的提示，然后你再来猜这个谜语。

哎所以大家会发现这个汉字谜盒呢其实它也算是一个小的一个AI应用。而大家会发现这个汉字谜盒这个应用啊它的界面呢比我们之前所完成的AI智能伴侣这个AI应用它的界面呢复杂度要高一些。啊因为你会发现它的界面呢第一个分为了三个部分，左侧这一块是控制台，就是一些会话信息的控制。那中间这一块呢就是一个主交互区域，与AI大模型交互的信息就展示在中间的区域。那右侧你会发现是不是又有一栏？哎右侧呢是这个游戏的一个详细的介绍，包括游戏的介绍、核心的玩法以及特色功能、适用人群等等。

这个界面呢是比较复杂的，而且大家会发现这个界面比我们之前所开发的AI智能伴侣这个界面呢要好看很多。啊包括页面的一些样式，包括消息这一块的展示，包括用户回复的消息这一块这个展示，界面效果是不是要好看很多啊？哎因为这个项目的页面并不是基于Streamlit构建的，而是基于专用的前端技术基于HTML、基于CSS、基于JS来构建的。通过HTML来控制这个页面当中的内容、元素；通过CSS来对页面的样式进行美化；那通过JS来完成页面的一些交互动作。啊所以这个项目呢它的这个页面是基于前端程序开发出来的。

啊当然这个前端程序啊它不是我们课程讲解的重点，我们是一门Python基础课程。所以呢，关于这个项目的前端代码其实在给大家提供的资料当中已经准备好了。那我们在开发这个案例的时候，我们的核心是放在服务器端，基于Python当中的FastAPI如何来开发服务器端的相关接口。

好，那么接下来呢，我们可以来打开我们的课程资料来看一下那给大家准备好的前端页面啊。来找到我们的课程资料，找到资料当中这一块一个文件夹叫 static。那么 static 目录下大家会发现是不是有三个文件？分别是 html、css 和 js。那这些前端页面以及前端页面的代码都是我用 AI 生成的，一个字都没写。好，那是我们这一块给大家准备的这个前端页面哈，就放在 static 这个目录下。

那么接下来呢我们再回过来说一下，那我们接下来在去做环境准备工作的时候，那具体要完成哪些准备工作呢？其实第一步准备工作就是要去创建一个项目的文件夹。然后呢将刚才大家所见到的 static 这个目录下的这些个资源文件拷贝到我们的项目当中。这个比较简单。然后紧接着第二步呢，就可以来编写 Python 程序来定义路径操作函数来访问这个 html 页面了。

那么刚才在 FastAPI 的入门程序当中呢，我们定义了一个路径操作函数访问 / 这个根路径的时候是不是返回了一个字典？那么最终返回给前端的其实是一个 JSON 格式的数据啊。但是现在不了，现在我们是要去访问这个游戏也就是汉字谜盒这个游戏的前端的 html 页面的。来看一下怎么做。

那么这里呢首先第一步，创建 FastAPI 的实例依然是需要的。然后紧接着第二步呢，就是定义这么一个路径操作函数。那么在这个路径操作函数当中大家需要注意的就是它的返回值。不再是返回一个字典啊或者是列表的数据了，那要返回的是一个文件。那这个文件在哪放着呢？就在 static 这个目录下放着，文件的名字就叫 index.html。那由于要返回的是一个文件，所以这里呢，我们这里需要将这个文件的名字哈在这一块封装到这么一个类当中哈叫 FileResponse。在这一块去构造这么一个对象返回。而这个 FileResponse 呢其实是 FastAPI 当中提供的。我们需要将这个 FileResponse 导入进来。那这样的话，我们在浏览器地址栏当中我们访问当前这个项目，如果你访问的路径就是根路径，那最终你就会访问到 static 目录下的 index.html 这个文件。好勒，那这就是我们准备工作当中所需要完成的这两步操作。

好，那么简单介绍之后啊，接下来呢我们就来操作一下。来，我们直接打开我们的 PyCharm。那么打开 PyCharm 之后啊接下来呢，我就在第六章这个目录下我再创建一个目录啊，因为之前就是一些演示，那这里这个项目呢我就放在我的目录下。这个项目呢就叫汉字迷盒。谜语的谜啊，迷盒。好。然后接下来呢我们就可以将我们刚才在资料当中所看到的这个 static 这个目录，对吧，里面是不是有三个文件啊，然后将这个目录 Ctrl+C 复制一下，复制之后直接放在汉字迷盒这个项目的目录下，你看。这个 static 是不是就拷贝进来了。那么这个 static 目录下呢其实存放的就是一些这个前端页面相关的一些文件：html、css 和 js。好，那么这些文件我们也叫静态文件。为什么叫静态文件呢？因为这些文件当中的这些内容都是固定的，你无论你什么时候去访问，里面的内容都是一样的，不会变化的，所以我们也叫静态文件哈。

那接下来呢我们在汉字迷盒这个项目的目录下，我再创建一个文件。那这个文件呢其实就是我们后端的 Python 文件，那这一块我就叫 main。因为这是我们的主文件哈，就叫 main 就可以了。那接下来呢，我们就要来编写 Python 代码了，是不是要去创建 FastAPI 的实例啊？来，从 FastAPI 当中去导入一下这个 FastAPI 这个类，然后接下来上来第一步创建 FastAPI 的实例，直接拿过来。那这里我在创建 FastAPI 实例的时候啊，那这里我可以给它传入这么一个参数哈：title。title 呢就是我们当前这个项目的一个标题。那标题如果你不指定默认就是 FastAPI。那我在这里指定一下哈。title，那这里呢，我们就叫汉字迷盒。好勒，那这样呢这个实例就创建出来了。

那接下来呢，我们在这块再来定义路径操作函数哈，路径操作函数。那定义路径操作函数我们这一块定义的這個路径就是根路径。app.get 是不是发起 get 方式的请求，请求路径：根路径。那如果说你请求的路径为根路径，那这里我要给你返回什么样的信息呢？AI 给我提示返回一个字典，那当然这个字典最终啊它会转为 JSON 格式的数据，然后给前端返回回去。因为在前端开发当中啊没有字典这一说，那都是 JSON 格式的数据。但是这里呢我们就不能再返回 JSON 格式的数据了。我们的目标最终啊是，你访问这个根路径，我最终将这个 static 目录下的这个 index.html 这个前端页面给你返回回去，然后浏览器端是不是再将这个 html 页面解析并渲染出来？然后展示在浏览器当中就行了。所以这里呢你就不要再去响应这么一个字典啦，那这一块我们要响应的是这么一个类哈，刚才我们介绍过，叫 FileResponse。那这个 FileResponse 呢大家可以使用这个 Starlette 当中的这个模块当中提供的哈，starlette.responses 当中提供的这个 FileResponse。因为 FastAPI 呢其实里面底层就集成了这个 Starlette。那 FastAPI 当中像 HTTP 请求的处理、请求的分发，这些工作其实都是它所集成的 Starlette 当中提供的功能。所以这块你直接导入这个就可以。来，我们这块直接双击一下，你看，是不是就导入进来了。哎，然后在这块指定。那么我要给你响应回去的是哪个文件呢？是 static 这个目录下的 index.html。那么这一块就直接写：static 下面的 index.html，这就 OK 了。好，那这里呢，我在这块输出一句日志哈，那么在这块访问项目首页。好，那这个呢其实就是我们项目的首页页面。

好勒，那这样呢这个路径操作函数我们也就定义好了。那接下来我们是不是就可以将这个服务启动起来了？哎，那么启动服务，我在这块再来编写一个项目运行的入口哈，那么在这块直接基于 uvicorn.run 将这个项目启动起来。那这里呢将这个 app 传入进来，host 指定 0.0.0.0 没问题，端口号 8000。也就意味着当前我们这个项目启动起来之后占用的端口是 8000 啊。如果你不想让占用 8000 占用 9000 你自己改就行了。好勒，那这个呢就是我们项目运行的入口，启动项目用的。

好，那这一块的代码我们编写完成之后，接下来呢我们直接右键来运行这个项目。好，那这样呢这个项目就启动起来了。那启动起来之后占用端口是 8000 啊，那接下来呢我们就可以打开浏览器来访问一下。那这里呢我们直接访问 localhost:8000。那这里这个 8000 呢指定的是端口号，那端口号之后你就要来指定我们访问的是哪个路径了。那这个路径如果你不写，默认访问的就是根路径，它会自动地在后面拼接上这个 /。

好，那我们来看啊。一旦在后面拼接上了这个 / 之后，来我们回到 PyCharm。来，我将这块的日志先清空哈。来，访问的路径是这个，那么一旦访问的是根路径，那么最终它就会调用这个函数来处理这次请求。那么调用这个函数之后，首先是不是会输出这句话：访问项目首页，然后再将 static 目录下的 index.html 这个文件是不是就响应给浏览器了？那浏览器呢就会将这个文件解析并渲染展示出来。

好勒，那么接下来呢我们回到浏览器，接下来直接回车。那回车之后，大家来看，这个页面展示出来没有？汉字迷盒控制台，汉字迷盒，然后紧接着你看这块是不是可以与 AI 大模型进行消息交互啊？哎这块可以输入你的答案，然后下面这块有游戏介绍。那大家会发现这个页面确实是在浏览器当中呈现出来了，但是好像和我们在 PPT 当中所见到的页面不太一样。为什么呢？

来，我们回到 PyCharm 给大伙介绍一下哈。那么其实这里呢我们给浏览器响应的是这么一个文件：static 目录下的 index.html。那么最终它访问到的就是这个文件。但是在 static 目录下的 index.html 当中啊，它又去访问了另外两个文件：一个是 /static 下面的 style.css；那还有呢再往下走，在这里还访问了一个 static 下面的 app.js。也就意味着一旦浏览器去解析完这个 html 这个文件之后，它就会再发起两个请求：一个请求请求的是这个 static 目录下的 style.css；另外一个请求请求的是 static 目录下的 app.js。但是呢，它访问到了没有？没有。为什么呢？你看，在控制台当中是不是已经输出日志了？告诉你啊，这个 style.css 和这个 app.js 都返回的是 404 状态码。404 就是请求的资源没找到，不存在。因为在你当前的这个 Python 代码当中并没有一个函数能够来处理这两个路径。因为你这块是不是只定义了一个 / 啊，并没有一个函数能够处理这两个路径。

那这个时候怎么做？怎么解决这个问题呢？大家想，那如果说我在这块我再来定义两个函数分别来处理这两个路径，是不是就很麻烦了？如果 index.html 这个文件当中涉及到的 css 以及 js 文件很多，你是不是要定义很多的函数？那就很繁琐了。

那么这个时候怎么办呢？好，那这里呢，其实在 FastAPI 当中啊，它给我们提供了一种方式，什么方式呢？叫做挂载静态资源的目录。我这一块写下哈，叫挂载静态文件的存放目录。那具体怎么做呢？那当然AI刚才都给我们提示出来了，那这里呢其实我们可以去看一下官方文档。来，我们直接打开浏览器。打开浏览器之后找到官方文档，呃之前我们是不是查看了这个入门程序的写法。那么在这个用户指南哈，大家往下走往下走，去找，找什么呢？找静态文件，这儿，点一下。那打开之后大家来看，那么在这里呢就会告诉我们啊，你可以使用 StaticFiles 从目录当中自动提供静态文件。那具体怎么做呢？就下面这一块你看，有阴影的这两行代码已经给我们提供了。

第一步，要导入 FastAPI 当中的 StaticFiles 这样一个类。然后接下来呢，调用 app.mount 去挂载指定的这个目录。那这里呢其实我们要做的很简单，把这两行代码复制过来。Ctrl+C 复制一下，复制之后，来，直接放在最上面哈。呃我们在这一块导入的这个代码都放在最上面，然后紧接着再把下面这行代码复制过来。那复制过来之后，那我就放在这个位置。好，这就就可以了。

来，我给大家介绍一下这行代码什么意思。app.mount 就是挂载的意思，啊要在 FastAPI 当中再挂载一个目录。挂载哪个目录呢？挂载的是这个目录，这一块指定的 static 目录，就指的是挂载这个目录。那么这个时候如果说你的请求的资源路径哈，看这儿，如果是以 /static 开头，好，那么这个时候它就会匹配到这儿，你看。如果你的这个路径是 /static 开头，啊你看，是不是就是 /static？好，那么这个时候呢，我就会去到这个目录下查找这个文件。到哪个目录？到 static 这个目录下来查找文件。那如果说在我们的 HTML 当中，你在去访问这个 CSS，访问 JS 的时候，你前面这一块叫 abc，那你这块这个路径就写 abc 就可以了。你看，这个 path 代表的就是你的静态文件的访问路径是什么。你看，我们的访问路径是不是 /static？哎那如果说你静态资源的这个访问路径是 /static，好，那你把这一块的这个请求就交给我当前这一块来处理，然后呢你就到 static 目录下来找这个资源就可以了。所以大家要注意对应关系哈。这个 static，/static 是路径，要和这一块对应。这个 static 是目录名，和这个目录名对应。那后面这个 name=static，这是 FastAPI 内部使用的名字，你随便写都行，啊你叫 abc 都可以。

好勒，那接下来呢我们再来试一下，看一下行还是不行。呃那么这里的代码发生变更了，我们这里呢需要去重新启动一下，加载最新的代码。然后接下来再次打开我们的浏览器，来，这个时候我直接回车。那回车之后，现在大家就见到了我们汉字谜盒这个项目的页面了。那此时你会发现，这个页面和刚才大家第一次访问的那个页面的展示形式是不是有翻天覆地的变化？哎因为现在已经有了 CSS 的样式了，页面展示的就会更加美观一些。那么只要大家能够看到这个前端页面，那就说明我们基础的环境准备工作呢就已经完成了。

好，那我们可以来看一下这个前端页面哈。那么这里呢其实分为三个部分，哈左侧这一块就是汉字谜盒的控制台，其实就是控制这个呃游戏会话信息的。那中间这一块就是与大模型交互的这个界面，那右侧这里呢其实就是这个游戏的一个简介。当然呢，刚才大家会看到我们在这一块刷新之后啊，那这里是不是会提示我们加载会话列表失败，啊以及这里会告诉我们创建会话失败。因为我们服务端 Python 的代码是不是还没开始写呢？哎服务器端的功能还没有开发呢，所以这里呢，那么前端发起请求，服务器端没有响应，这一块就会提示失败。好，那这些功能呢是我们下一小节要来开发的功能。

好的，那这是我们这一小节所完成的基础环境的搭建。那么在基础环境搭建的时候呢，大家核心需要去关注的就是关于静态资源目录的挂载。那需要调用 app，也就是 FastAPI 实例当中的这个 mount 这个方法来挂载这个静态文件的存放目录。啊然后指定那如果说我们访问的路径哈是以 /static 开头，那你就把这个请求交给我当前这个静态资源目录来处理，啊就去对应的这个 static 目录下来查找对应的静态文件就可以了。

而这里呢再次强调一下这三个 static 具体的含义哈。呃这三个 static，那么第一个 static 它代表的是任何以 static 开头的路径都交给这块来处理，啊它指的是路径。然后第二个 static 呢代表的是，那交给这一块来处理，最终要到哪个目录下查找这个文件呢？哎就是到这个 static 目录下来查找文件。那这个 static 目录下放的就是 HTML、CSS、JS 这样的静态资源文件。那么最后一个 static，那这个是 FastAPI 内部使用的名字，啊大家是可以自由定义的。

好的，那这是我们这一小节所完成的基础环境的搭建。那么接下来呢大家就可以暂停视频，先完成这一块基础环境的准备。那么准备好了之后，下一小节呢我们再一起来完成这个项目当中核心功能的开发。黑马程序员，为千万学生少走弯路，我们一直在努力。
