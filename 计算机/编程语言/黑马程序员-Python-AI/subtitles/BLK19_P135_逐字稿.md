好的，各位同学。那通过上小节的学习呢，我们已经知道了什么是正则表达式，以及如何基于正则表达式来匹配并提取出我们所需要的数据。那接下来这一小节呢，我们就来详细地剖析一下正则表达式的详细写法。那正则表达式的写法呢各式各样。那接下来呢，我们就一起来看一下，那在编写这个正则表达式的时候，涉及到的一些具体的语法。那我们一起来看。

那首先，我们先来看一下左侧的这个表格。第一类呢，就是在编写正则表达式的时候，我们所使用的这个普通字符。比如在这个正则表达式当中啊，这个1，那表示的是不是就是数字1本身的含义啊？那么这类字符呢，我们就称之为普通字符。那么其实像我们常见的字母、数字、汉字以及绝大部分字符呢，它就是直接匹配自身，表示的就是本身的含义。那这类字符就叫普通字符。比如我这块写个1，那它表示的就是1。如果我这块写个a，那它表示的就是a本身，字母a本身。这个就叫普通字符。

然后紧接着再来看一下表达式当中的第二类写法。就这块这个字符我写个点。那一个点呢，它是用来匹配任意一个字符的，除了这个换行符之外。这个\n呢代表的是换行符。那一个点呢就匹配任意一个字符。那如果是两个点呢？它匹配的就是任意的两个字符。

好，然后紧接着再往下走呢，这个刚才我们在上小节见过啊，就这个\d。那\d呢是用来匹配数字的，0~9之间。那数字呢也就是0 1 2 3 4 5 6 7 8 9这10个数字。

然后还有一个是\D。那\D它是用来匹配非数字的。你看\d呢是用来匹配数字的，那\D它就是取反，匹配的是非数字。那它就能够匹配像字母啊、汉字啊，\D就可以匹配到了，匹配的是非数字。

然后紧接着再往下走，还有一对，你看，\w、\W。先看\w。那\w呢是用来匹配单词字符的。那它匹配的内容呢比较多，比如像a~z，A~Z，0~9，你看，包含了大小写字母，包含数字，还包括下划线，还有其他的语言字符。比如像中文当中的汉字以及日文、韩文都是可以匹配到的。这就是这个\w，小w。那如果是\W呢？它匹配的是非单词字符。什么叫非单词字符呢？其实指的就是对\w取反。那比如像逗号啊、句号啊、引号啊、感叹号啊、加号、等号这些符号，那其实通过\W就可以匹配到了。

好，那么这是前面这块的这个表达式的写法。然后紧接着再往下走，下面这3个大家会看到是不是都使用的是方括号将其括起来的？那方括号括起来的呀，代表的是匹配其中的任意单个元素。比如你看这块方括号里面写了一个a e i o u，这块是不是写了这么5个字母啊？它就是用来匹配这5个字母当中的任意单个字符，任意一个字母的。那如果说在方括号当中，我们在方括号的前面，也就是在这个字符列表的前面，我这块加上了这么一个符号啊。那这个符号呢大家可以认为就是一个尖角号。那这个是怎么按出来的呢？是我们键盘上的数字6上面的这个符号。那这个符号呢，如果出现在方括号当中，代表的是取反的意思。就是取它的反义。那你看，上面这一块写了一个a e i o u，代表的是要匹配其中的任意单个字符。那取反呢，指的就是匹配不在这个字符列表当中的任意单个字符，就是排除了a e i o u之后的任意单个字符。好，那这就是取反的含义。好，所以方括号当中如果出现了这个尖角号，就代表取反。

好，那么最后一个，这个我们刚才在这块介绍过，那如果说在方括号当中啊出现了这个横杠，代表的是范围。那0-5就代表的是数字范围在0~5之间，0 1 2 3 4 5。比如刚才你看这个正则表达式当中的3-9，那这个是不是表达的是范围啊？3~9之间。好，那这是在正则表达式当中啊所涉及到的这些字符的写法。

然后紧接着再来看一下右侧这张表。那右侧这张表呢，其实里面就会涉及到一些符号。那这些符号呢，像上面这些符号，它是用来描述量词的，表达量词的。我们一起来看一下。

那首先先来看第一组啊，就是前面这3个：星号、加号和问号。那首先先看星号。星号代表的是出现任意次，表示的是0次或无数次。比如我这块写了这样一个表达式，大家来看。第一位这块写了一个1，就代表第一个数字必须得是1，然后第二个数字呢得在3~9之间。然后后面呢这块反写了一个\d，代表的是那后面第三位之后呢在这块得是0~9之间的数字。那具体这个数字出现多少次呢？这块写了一个星。那星呢表达的是任意次，也就是说第三位之后的这个数字可以出现任意次，0次或多次。这就是这个星号。

那如果说将这个星号在这块换成加号，那它所表达的意思是啊，那第三位之后的这个数字呢，你至少得出现一次。加号代表至少出现一次，也就是说明一次或多次。

然后紧接着再往下走，还有一个是问号。那问号代表的是至多出现一次，最多出现一次，那也就是说明0次或一次。那如果后面这块写了一个\d?，那就代表啊，那第三位这块的这个数字呢最多出现一次。好，那这是这3个符号的区别，用来表达量词的。

然后紧接着再往下走呢，还有一类表达量词的方式，就是使用大括号将其括起来。那么大括号括起来，如果里面直接写个m，就代表那这块这个字符出现m次。比如我们在上一小节这块写到，这个\d后面写了一个大括号9，是不是就代表那后面的这个数字要出现几次啊？出现9次。那再加上第一位的1和第二位的3~9之间，一共是不是就是11个数字啊？好，那这是这个语法，代表的就是出现m次。

那如果是第二种写法，m逗号，你看逗号之后就完了，没有写第二个数字，那这代表的是至少出现m次，上不封顶。比如你看，\d后面写了一个9逗号，那就代表后面的这个数字至少得出现9次。那出现10次、11次、12次、120次都是可以的，至少9次。

好，那么还有一种写法呢就是m逗号n，代表的是出现m次到n次。那比如我这一块的这个表达式写了一个\d{9,11}，那就代表这个数字得至少出现9次，最多出现多少次啊？11次。好，这就是这个量词的写法。这三种写法是用大括号括起来的。

那么再往下走呢，在这块还会涉及到这么几个符号。首先第一个符号呢就是一个竖线。那这个竖线呢代表的是或的意思。代表的是要匹配这个竖线左右任意一个表达式。比如大家来看，那这块我写了这样一个表达式啊。那这块呢你看我使用了一个小括号括起来，然后里面写了一个3 5 7 8 9，然后两个数字之间你会发现我使用了一个什么符号？竖线。那这个代表的就是或的意思。那这块使用这个小括号将其括起来，表示那这个里面这是一个整体。然后紧接着，那3竖线5竖线7，那就代表那第二位是3或者是5，或者是7，或者是8，或者是9。而整体呢我使用一个小括号括起来表示是分组，代表的是这是一个独立的单元，这是一个整体。那所以这个小括号代表的是分组符号，就是把分组后的内容呢我们可以看作成一个单元，看作成一个独立的整体。

比如大家来看，这个表达式代表什么意思，大家可以自己想一想。代表的是第一位得是a，然后b呢，至少得出现几次啊？你看加号嘛，至少得出现一次，也就意味着第一位得是a，然后第二位这个b呢至少得出现一次，也就是说出现一次或多次，对吧？那如果说我想表达的含义是ab这两个它作为一个组合，然后至少得出现一次呢，这个时候怎么写呢？就是ab这个组合至少得出现一次，要么是ab，要么是abab，要么是ababab。你看，a和b这是不是就是一个整体啊？我要将a和b作为一个整体来看待，那这个时候其实我就可以使用一个小括号将其括起来，你看，(ab)+，括号括起来，后面写一个加号，那这就代表ab这个组合至少得出现几次啊？至少得出现一次。这就是这个括号分组它的一个作用，可以将括号里面的多个字符视作为一个独立的单元。

好，然后紧接着最后呢在这块再介绍两个符号。一个呢就是我们刚才所介绍的这个尖角号。这个尖角号如果出现在这个方括号之内呀，它是代表取反的意思。那如果说没有出现在方括号之内，它代表的是匹配字符串的开头，而dollar符（$）是用来匹配字符串的结尾。比如大家来看。那如果说啊，我在这个正则表达式的前面加上了这么一个尖角号，后面加上了这么一个dollar符，那就代表啊这个正则表达式它所匹配的文本啊必须得是1开头的，然后9位数字之后到这儿就结束了，必须得是9位数字结尾的，要匹配的是这样的文本。

好的，那这是我们这块介绍了一下这个正则表达式的基本写法。那基本的语法我们介绍之后啊，那接下来呢我们就直接来打开PyCharm来演示一下。

那么在PyCharm当中啊，我这块已经准备好了这么一个Python文件，正则表达式语法演示的Python文件。那这里呢，我已经编写好了一些演示的代码，那我们一起来看一下。

那首先大家来看，在上面这里呢，我定义了这样一个字符串s1。s1呢就是这么一长串字符串，那里面呢涉及到很多内容啊，我们来简单读一下，你看188这是一个手机号，是我的手机号，188开头的，100结尾的。我的另外一个手机号是什么，两个QQ号分别是什么，那邮箱是什么。好。

那现在呢我们就基于这个字符串来演示一下，那基于正则表达式如何从字符串中匹配并提取出对应的内容。

那么首先在这块我们就先来演示一个符号，就是这个点。那这个点我们刚才提到它是用来匹配任意一个字符的。那这个点呢我们可以配合着量词来操作，大家还记得这个星号代表什么意思吗？那这里呢我们在后面在这块可以写一下，我们刚才介绍啊这个星号它是用来匹配多少个字符的？匹配任意个字符。好，然后紧接着第二个呢问号还记得吗？问号是匹配0个或一个。我们刚才提到这个叫最多出现几个啊？最多一个哈，最多出现一次。好，然后紧接着这个加号呢是最少出现一次，匹配一个或多个，是最少出现一次，我们叫最少出现一次。

好，那么接下来呢我们就来看一下那它们匹配到的结果是什么样子的。首先大家来看，在这里呢我调用的是re模块当中的findall这个函数。那findall这个函数呢我们在上小节都讲到过，它是从任意位置开始去搜索所有的匹配项，返回的是不是一个列表啊？大家来看，从任意位置开始那就开匹配188开头的，然后后面这块呢是任意字符，然后出现任意次。来我们看一下匹配到的结果是什么样子的，来我们在这块右键直接运行，运行之后看效果，大家来看。那第一个它是不是将整个字符串都匹配到了？你看嘛，188开头然后任意字符出现任意次，那么这个点它是用来匹配任意字符的，那后面这块的这个数字包括后面这块的这个汉字，包括后面这块这个符号是不是都会匹配到呀？所以最终呢就输出到了这个控制台当中，封装在这个列表当中。

然后紧接着再来看下第二个符号，就是第二行输出的这个问号。那这个问号代表的是最多出现一次，匹配0个或一个。那我们一起来看一下，188开头的后面任意一个，那1880是不是就能匹配到了？然后紧接着再往后走你看，1880后面这块就不能再匹配了，因为它匹配最多出现一次嘛，最多一个，那就匹配到这个0。那除此之外，那还会继续往后匹配，来找188开头的，走往后走走到这188开头的你看，188开，那这个“开”是不是就匹配到了这个点？所以呢它也能匹配成功，再往后走呢就没有188了，往后走就没有了。所以这里匹配到的是两个，一个是1880，一个是188开。这是这个问号，最多出现一次。

那加号呢代表的是最少出现一次。好，那最少出现一次，那么对于这块的这个效果来说呢，它也就匹配到了整个符号你看，最少出现一次那后面这块出现多次，因为我们使用的是点，它是用来匹配任意字符的。

好，那这是这块给大家演示了一下这个点以及这个星号、问号和加号。好，那这里呢演示完了我将其注释掉，注释掉之后接下来再来演示一下大括号括起来的这个量词。那大家还记得我们刚才在这块介绍这个大括号括起来量词的含义吗？

那么如果说在这块写了一个大括号这块写了一个8，这个表示什么含义呀？匹配的是8个。8个什么？你看前面这块是反斜杠小d匹配的是8个数字。那如果说在这块写了一个6逗号10呢？匹配的是6个到10个。然后这块如果是6逗号，那匹配的是6个或更多，那也就说至少得出现几次？6次。

好，那接下来呢我们就来看一下输出的效果。那这块输出的效果你看第一个得是188开头的，然后后面这块得是几位数字啊？8位的数字。然后第二位呢是155开头的，然后后面是6到10位的数字。那第三位也是155开头的，那后面这块呢至少6位数字。

好，来我们在这一块右键直接运行，运行之后再来瞧瞧匹配到的效果。你看第一个，188开头的，然后呢8位数字没毛病，是不是匹配到了这个第一个手机号啊？那再往后走还有没有188开头的啦？没有。然后第二个你看155开头的，那155开头的后面出现6到10位，那155在这啊6到10位那后面这块是几位？是8位，没问题。然后第三位是155开头的至少几位？至少6位。那也将这个结果匹配出来了，这也是没有问题的。好，那这就是这个大括号括起来的这个量词的表示形式，好这么三种表示形式。

然后紧接着呢我们将其注释掉。然后再往下走，再来看一下下面这组语法。

那么下面这组语法呢其实它主要演示的是这里的这个方括号还有这块这个开头和结尾的符号，我们一个一个看。你看1，方括号里面写了一个3 8什么意思？我们提到方括号当中的这块的这个字符呢代表的就是要匹配方括号当中的任意一个字符，也就是说第一位得是1，第二位可以是3或者是几？8，任意一个。然后后面在这块呢是几位数字呀？后面这块写的是8，那就是8位数字。当然大家注意了我这块就是来演示这个正则表达式的写法，并不是匹配手机号。所以大家来看这块的写法，第二位得是3或者是8那后面这块8位数字就可以了。

好，那我在这一块写一下。那方括号我这块要给大家说的就是[38]，它表示的是什么意思啊？匹配的是3或者是8，这个AI生成的有误哈。那这个1呢它是第一位，匹配的就是3或者是8。

那你再看第二个呢前面加了这么一个尖角号，这个又代表什么意思呢？那这个代表的是取反的意思，匹配只要不是3或者是8都会匹配到。这个匹配的是非3或者是8，就是取反的意思。

然后紧接着再往下走你看这一块3-9，这个我们刚才在入门程序当中我们是不是都演示过呀？这个3-9呢代表的是范围，代表的是匹配3到9，它描述的是一个范围。

好，那么再往下走呢在这个里面你看最前面在这块加上了这么一个尖角号，注意喽。那么在方括号当中加这个尖角号它是取反的意思，那么在这个表达式的前面加了一个尖角号这个又代表什么意思呢？这个代表的是匹配开头，然后这个dollar符代表的是匹配结尾。尖角号匹配的是开头，dollar符匹配的是结尾。

好，那么接下来呢我们就直接右键执行看一下效果。好，执行完毕我们一个一个看。那首先大家来看，那么在这块在进行这个表达式匹配的时候啊你看第一位得是1，第二位得是3或者是8你看，18几、13几是不是都匹配到了？然后这个15几有没有匹配到？没有，因为并不仅不在这个范围之内。然后紧接着你再看第二个你看，第一位得是1，第二位只要不是3或者是8就行，所以你看它匹配到的结果是什么？15几，这块12几是不是就匹配到了？好，然后再往下走呢，那么再往下走第三个是3到9之间，那么3到9之间的话你看18几、15几、13几是不是都匹配到了？然后这个12几的呢并没有匹配到。你看125的这个它就没有匹配到。

好，然后紧接着我们再往下，那么再往下的话那这块你看尖角号后面这块写了一个1，[3-9]，然后这块后面写了一个\d九位的数字。那也就意味着它要匹配的是以1开头的，然后第二位得在3到9之间，后面是9位数字。那匹配到的是谁呢？你看，这个字符串首先你看它是不是以1开头的？是。第二位是不是在3到9之间？是。后面是不是9位数字，有没有9位数字？有。那最终它就将这个手机号匹配到了。然后紧接着再往后走，即使你用的是findall，那由于它就是从开头这块开始匹配的，第一位必须得是1，所以后面的这块这个内容啊，它就不会再进行匹配了，它就是匹配开头，从开头开始匹配。你用了这个尖角号的话，匹配开头。好，所以这块你看，匹配到的是18809090000这个手机号。

好，那么最后再来看一下，那最后这块呢它匹配的是以1开头的，第二位在3到9之间，后面9位数字，然后到这儿就结束了。那最后你会发现匹配到了没？没有。因为你会发现这个手机号到这儿它没有结束，后面是不是还有内容啊？所以它并没有匹配到。那如果说你想让它去匹配到，那你得这个字符串呢，就是一个手机号，后面没有别的内容。你看，1开头，后面9位数字结束，是不是就匹配到了？这个时候你再执行，你会发现这就有数据了。

好嘞，那这就是我们这块所演示的这个正则表达式常见符号的这些写法。

好，那紧接着这块演示完了之后，再来看一个，就是这个反斜杠小w（\w）。大家还记得这个反斜杠小w它所表示的含义吗？反斜杠小w是匹配任何的单词字符，那这里的单词字符注意了，是小a到小z，还包括什么？我这块就直接来写一下啊，还包括大A到大Z，还包括什么呢？还包括0到9，还包括下划线。好，那么除此之外，那还包括其他的一些语言字符，我这块写一下，还包括其他语言字符，比如像中文、日文、韩文，都会匹配到。这是这个反斜杠小w。

我们来看一下，那这个正则表达式它表达的含义是什么呢？我们一起来看一下。那首先这块写了一个反斜杠小w，在这块写了一个加号。那这个加号呢，大家都知道是出现至少一次，就是一次或多次。也就是说，那这块至少得有一个单词字符。这个单词字符至少得出现一次或者是多次。然后后面这块你看固定地写了一个@符号，那就是一个@这个符号表示它本身的含义。也就是说这个@符之前的这个单词字符至少得出现一次，一次或多次，然后后面这块又是单词字符出现一次或多次，然后这块又有一个点。那注意咯，这个点前面加了一个反斜杠进行了一个转义。那转义之后这个点代表的就是不是任意字符了，而表示的就是这个点本身。这个点之后呢，这个反斜杠w又出现了至少一次，一次或多次。

那最终匹配到的是谁呢？这个时候我们就去找谁？找@符就行了。那么在这个字符串当中有没有@符呢？往后走，这块有个@符，那也就意味着，它匹配到的这个字符串到底是哪一部分呢？你看，@符前面这块的这个单词字符至少得出现一次，你看，这个@符前面的这个Python666，它能不能匹配成功这个反斜杠w呢？可以匹配成功，那“邮箱为”这几个汉字能不能匹配成功呢？可以，因为反斜杠w它默认是能够匹配其他的这些语言字符的，那这个逗号能不能匹配成功呢？匹配不了。你看，这个范围内并没有包含逗号句号感叹号引号之类的这些符号，所以它只能匹配到前面的这部分。然后@符往后，163能不能匹配到呢？你看@符之后是反斜杠w，就是这些单词字符，163能不能匹配到？可以。然后后面这块是不是有个点？那这块有个点。然后再往后走，.com，那这块是不是也能够匹配这个反斜杠w？出现一次或多次嘛，你看，com。那com之后是一个逗号，是不是又匹配不了了？所以最终它能匹配到的是哪部分呢？就是“邮箱为”这部分。

好，那么接下来呢，我们右键直接运行，运行之后大家看效果，你看，匹配到的就是这部分。

好，那这就是这个\w它的作用。是用来匹配任何单词字符的。当然这是它的默认行为，我在这块写一下，这是这个反斜杠w它的默认行为。

那么其实对于这块的这个写法呢，我们还可以这么去做。如果说你不想匹配其他的语言字符，不想匹配中文、韩文、日文这些语言字符，那这个时候我只想匹配什么呢？那我就想匹配数字、英文和下划线。也就是说我只想匹配后面这部分。

那么此时呢，其实我们在进行匹配的时候，可以在后面在这块再加上一个可选项。那这块呢可以再加上这么一个参数，就是re.后面在这块来一个ASCII（re.ASCII或者re.A）。那这个ASCII呢代表的就是它只能够匹配ASCII码当中的这些个字符，那ASCII码当中呢就不包括像中文、日文、韩文这些语言字符。这么做就可以了。那么此时它能够匹配的范围呢就是小a到小z，大A到大Z，0到9，还有一个下划线。

好，我们一起来看一下效果。来，我在这块右键在这块直接运行，运行之后你看，这个中文它是不是就匹配不了了？默认你在这一块写反斜杠w它是能够匹配其他的这些语言符号的。那如果说你不想匹配其他语言符号，只想匹配字母、数字和下划线，你这块再加上这么一个选项，就是ASCII。

好，那这是我们这块所演示的这个反斜杠w。好，演示完毕之后，我们再将其注释掉。那注释掉之后，最后呢，我在这块再来带大家看一下下面这组样例，大家来看。那这块呢我又定义了这么一个字符串s2，那这个字符串的内容呢，“现在的时间是2026年2月6号啊，10点05分25秒，然后今天的天气还可以，气温是28度”。好，那么接下来呢大家来看，那下面这块呢我写了这样一个表达式，大家来看一下这个表达式的含义是什么。

你看，反斜杠d是不是数字啊？数字出现几次啊？4次。然后呢，这块横杠，那横杠表示的就是它本身的含义，然后反斜杠d数字出现两次，横杠表示本身的含义，反斜杠d数字出现两次。那这块它要匹配的是哪一部分，能匹配出来的是哪一部分呢？那能匹配的，你看横杠哪有横杠吗？这块是不是横杠？那最终呢它就可以匹配到这个2026年2月6号。这是没问题的，是可以匹配成功的。大家来看，我将下面这个注释掉，只将这个放开，我们测试一下，大家来看效果，你看，2026-02-06这个日期我们是不是就提取出来了？

那现在如果说我就想单独地提取出年份、月份以及具体的日期，那现在怎么做呢？那这个时候大家想，我们既然想单独地提取出年份、月份以及日期，那我的是不是就可以把年份看作成一个单元？月份看作成一个单元，日期看作一个单元啊？

那这个时候呢，我们就可以使用正则表达式当中的分组操作。你看，我们只需要在原来的这个表达式当中你看，这块表达的是不是就是年份啊？我只需要将这部分看作成一个单元，使用一个括号括起来，然后这个表达的是不是就是月份啊？我也使用小括号括起来，那这个表达的是不是就是日期啊？我也使用小括号将其括起来。

那这样的话，你看，那整体在进行匹配的时候呢，它就会匹配，首先呢是4位数字，横杠之后是两位数字，横杠之后又是两位数字，那最终它就会将这块文本匹配出来。那匹配出来之后，由于我们这里进行了分组操作，那其实是不是就相当于是3个组啊？这是第一组，这是第二组，这是第三组，那么第一组匹配的就是2026，第二组匹配的就是02，第三组匹配的就是06。

好，我们看一下效果。来，右键在这块直接运行。运行之后大家来看，那最终呢它就拿到了这个分组之后的效果，你看它不是作为一个整体给我们返回的，而是将每一个组的内容匹配到的内容都给我们封装到这么一个元组当中返回了。你看，2026就是第一组匹配的效果，然后第二个呢就是02，这是第二组匹配的效果，那第三个呢就是06，就是第三组匹配的效果。首先呢findall返回的就是一个列表，在列表当中呢它将多个组匹配到的内容给我们封装到了一个元组当中。那这样呢如果说我们想去取出这个年份，那你通过这种方式呢你去获取到第1个元素就可以了，还是很容易的。

好的，那这是我们这小节所讲解的正则表达式的基本语法。

那么演示完毕之后，那最后呢我们再回到PPT，简单来做一个小结，那我们一起来看一下下面的这些正则表达式的含义。

首先，我们刚才讲到这个点表示的是什么含义？代表的是任意字符，当然除了换行符以外。然后紧接着呢就是反斜杠d和反斜杠w。那反斜杠小d呢代表的就是匹配的是0到9之间的数字，而反斜杠大D呢代表的是非数字。然后这个反斜杠小w呢匹配的是单词字符，表示的就是大A到大Z、小a到小z、0到9、下划线以及任意语言字符的。而反斜杠大W呢代表的是非单词字符，就是取反的含义。

然后再往下走呢，就是这个方括号这一块的这个表达式的写法，常见的这么三种。第一种呢，代表的就是这个aeiou匹配其中任意一个字符就可以了。然后第二个呢，就是在方括号当中如果用到了这个尖角号代表的是取反的意思，代表的是只要不在aeiou之内，其他的符号都能匹配成功。然后第三个呢就是匹配范围，代表的是小a到小z这个范围之间的任意符号。

好，那么再往下走，那么下面这一块呢都是用来表述量词的。那么星号代表的是任意一个，0个或多个；加号是至少一个，就是一个或多个；而问号是至多一个，0个或一个。而下面这一块的这个大括号m代表的是出现m次，那m逗号代表的是至少出现m次，m逗号n代表的是至少出现m次，最多出现n次。而这个竖线呢表示的是或的意思，那小括号呢是用来进行分组的，而这个尖角号和dollar符，这个是用来匹配开头和结尾的。

好的，那这就是常见的正则表达式的写法以及它的含义。那么下一小节呢，我们就要通过我们所学习的正则表达式来对我们之前案例当中的数据来进行进一步的清洗、处理，然后最终再将我们清洗之后的数据呢存储起来。那我们下一小节再见！黑马程序员为千万学生少走弯路，我们一直在努力。
