好的，各位同学，那接下来呢，我们就来学习数据容器当中的第一类——列表，也就是 list 类型。 那关于列表，也就是 list 类型呢，我们将会从以下三个部分来进行讲解。首先第一个部分呢，我们先认识一下什么是列表，列表该怎么定义，以及列表的特点。 然后第二个部分呢，再来讲解列表当中的常见操作，也就是它里面提供的一些常见的方法。那第三个部分，我们再通过一个案例来加深大家对于列表的理解和掌握。 那这小节呢，我们就先来认识一下什么是列表，以及列表该如何定义。 那列表呢，其实它就是数据容器当中的一种啊。那数据容器呢有很多，列表只是其中的一种。那它呢可以一次性存储多个数据，而里面的每一项数据呢，我们都称之为一个元素。 那接下来呢，我们就来看一下，那这个列表我们该怎么定义？怎么让列表一次性存储多个元素的？ 大家来看一下它的定义语法。 呃它的定义语法是这样子的啊，首先前面这一块就是列表名称啊，其实就是一个变量名。然后后面这一块呢，就是为这个变量赋值。那这块定义的就是一个列表，大家来看。 那列表呢是拿方括号括起来的，注意哦，两侧是方括号括起来的。那么在方括号当中呢，我们就可以来罗列，那这个列表当中要存储哪些数据呀？每一项数据之间逗号分割。 你看，元素一，逗号，元素二，逗号，元素三。两个元素之间是不是逗号分割？ 那就比如右侧这里呢，我们定义了这么一个列表。列表的名称呢叫 s，其实就是一个变量名。然后列表的值呢，就是右侧这一块所定义的这个值。 那是用方括号括起来，里面呢就存出来一项一项的元素。那在这个列表当中呢，里面存出了七个元素。你看，54、15、75、108、23、78、75，哎，一共七个元素，每两个元素之间逗号分割。那这就是列表的定义方式。 那么紧接着呢，我们再来看一下，那么列表的特点。列表的特点呢，总结起来啊，大的方面来说就这么两点，第一点： 它是可以存储不同类型的元素的。当然在给大家罗列的这个案例当中哈，这个存储的是相同类型的元素，都是 int 类型。但其实呢对于列表来说，它是可以存储不同类型的元素的。也就是说在一个列表当中，你可以存储数值类型、int 类型，可以存储 float 类型、浮点数，可以存储字符串类型、string，可以存储 bool 类型，这都是可以的，是没有限制的。哎，但其实在项目当中呢，我们在列表当中在存储的时候呢，基本上存储的都是同一类型的数据。但是大家要知道，在语法层面，它是可以存储不同类型的数据的。 然后紧接着第二个特点呢，就是列表它的元素是有序的，是可以重复的，元素也是可以修改的。这就意味着， 我们所定义的这个列表，里面的这个元素它是有顺序的。我们在定义的时候顺序是什么样子的，那么在存储的时候，它就是按照什么顺序来进行存储的。那最终我们在操作这个列表当中的元素的时候，也可以按照指定的顺序来进行操作。 然后第二点呢，就是它也是可以重复的。就是在列表当中，我们是可以存在重复性的元素的。比如大家来看，这个 75 是不是就重复了？哎，那么列表当中是允许重复的元素存在的。 那么第三点呢，就是元素可以修改。哎，我们可以指定我们要修改的是列表当中的哪个元素。比如我要将这一块的 108 我改为 208，那是可以直接去修改的。 那问题来了，列表当中这么多元素，我怎么知道我要操作的是哪个元素？我要修改哪个元素呢？ 哎，那么其实啊，在列表当中呢，它会给我们每一个元素都去定义一个索引或者叫下标。 那这个索引或下标呢，它是从 0 开始的。那列表头部的第一个元素，它的下标呢就是 0；第二个元素，它的下标就是 1；第三个元素下标就是 2，依次类推。 那最终我们在去操作这个列表当中的元素的时候，我们就可以通过这个下标或者说索引来操作。比如 我想获取出这个列表当中的第一个元素，那就可以写 s 方括号 0，这就代表我们要获取的是第一个元素。那如果要获取的是第二个元素，那就是 s 方括号 1，这就代表我要获取的是第二个元素。那如果我要操作的是第三个元素，那就是 s 方括号 2。 那大家注意喽，这个索引呢它是从 0 开始的。而在 Python 当中呢，针对于这个列表的索引，它又给我们提供了另外的一种编写方式，还提供了一种叫反向索引。那反向索引呢它是从列表的最后一个元素开始的，那最后一个元素的這個反向索引呢就是 -1。哎，倒数第二个这个元素呢，它的反向索引就是 -2，倒数第三个就是 -3，倒数第四个就是 -4。所以呢我们也可以通过这种反向索引来操作列表当中的指定元素。比如 我要操作列表当中的倒数第一个元素，也就是最后一个元素，我就可以写一个 s -1。 哎，注意哦，一旦这个索引是负数，就代表它是一个反向索引，那 -1 呢就代表最后一个。如果是倒数第二个元素，那就是 s 方括号 -2，这就代表操作的是倒数第二个元素；s 方括号 -3 就代表操作的是倒数第三个元素。 那么这两种操作方式都可以。那这两种方式呢，一种是从前到后的这种叫正向索引。正向索引呢，这个索引值是从 0 开始。另外一种呢就是从后向前的，那这种叫反向索引。那反向索引的这个下标呢它是从 -1 开始的。 好，那这是列表当中所支持的两种索引：正向索引和反向索引。 而在 Python 当中呢，它把这种元素有序并且可以通过索引访问元素的这种容器呢，称之为序列类型。 哎，所以对于这个列表来说呢，其实它就是一个序列。哎，因为里面的元素呢，首先是有顺序的，然后第二个呢，它里面的元素是可以通过索引访问的。那这样的容器类型呢，我们也称之为序列。 好，这个呢大家知道一下。好，那这是我们这块介绍一下什么是列表，以及列表的定义和列表的访问。 好，那介绍完毕之后，接下来我们就直接打开 PyCharm 来演示一下。 来，我们直接打开 PyCharm。那打开 PyCharm 之后啊，我们在这块来写一下，我们这块来演示一下 列表的基础操作。 呃那么首先我在这块先来说一下啊，就是列表该怎么去定义。 呃那么定义列表，那我们刚才提到是不是要使用一个方括号这样去括起来？ 那方括号当中呢，我们就可以来写列表当中的一个一个的元素或者说一项一项的数据。比如我这块随便来几个啊，56，啊 90，然后 88，啊 65，然后来一个 90。 后面在这块我再来一个呃字符串类型的 a，然后这块再来一个字符串类型的 hello。那我这块再来一个布尔类型的 true。 可不可以存储不同的数据类型呢？我们刚才提到是可以的。所以你这么写呢是没问题的。那为什么 PyCharm 这个工具在这块提示有一个黄色背景色呢？哎你鼠标往这一放啊，一悬浮它告诉你啊，这个语句似乎不起任何作用。为什么呢？因为这个语句定义到这儿它并没有任何的意义。哎那如果说你想操作这个列表，你是不是可以通过一个变量来接收啊？ 哎所以在前面呢，我就可以通过一个变量来接收啊。啊定义一个变量就叫 s，然后呢为它赋一个值，值呢就是这个列表类型。 那这就是定义列表。 那接下来呢，我们就可以来看一下，那这个列表的类型它到底是什么类型呢？哎我们在下面这块我就可以直接输出一下。来，所有的小伙伴大家告诉我，我想查看这个变量现在是什么类型的，我怎么操作？在弹幕当中刷出来。 哎那我想查看这个变量的类型，我们是不是需要去调用这么一个语句啊？叫做 type，然后将 s 传递进来。然后右键我们这块直接运行，运行之后大家来看，什么类型？ list。 哎，所以列表的这个数据类型呢就是 list 类型。好，那么紧接着我们再来说一下，那我怎么样去访问这个列表当中的元素？我这块写下，访问列表的元素。 呃那么访问列表的元素呢，首先第一个呢就是获取哈。 啊然后第二个呢就是我获取到了之后，如果我想修改该怎么做？那第三个，那我能不能对列表当中的元素进行删除操作呢？ 好，我们就来演示一下这三个部分。先来说一下获取。 那么我们要获取列表当中的元素，我们就可以通过我们刚才所介绍的 索引来获取。 来，大家来看。 那现在我要获取列表当中的第一个元素，大家告诉我，我有几种方式来获取啊？ 是不是两种方式？ 哎一种呢就是正向索引，一种是不是反向索引？好，那正向索引的话，我们这块的写法就是 s，我直接获取到之后直接将它输出出来啊。s 这块写一个方括号，那正向索引第一个元素是不是就是 0 啊？哎所以这块直接 s 0。 好，那如果说我要通过反向索引来操作，那这块该怎么写呢？方括号里面写几呢？ 哎那这个时候我们是不是就得数一下了？你看，反向索引的话最后一个是 -1，那这是 -1、-2、-3、-4、-5、-6、-7、-8。好，那这块应该是 -8，我们写一个 -8。 好，那这就 ok 了。 那接下来呢，我们就来做一个测试，看一下行还是不行。来，右键直接运行，运行之后大家来看，56 是不是拿到了？哎我们通过正向索引、通过反向索引获取到了第一个元素，也就是这个 56。 好，后面我这块标注一下啊。那这种呢我们叫正向索引。 那正向索引是从 0 开始的。然后第二种呢负数的这种我们叫反向索引。那反向索引呢是从 -1 开始的。我这块写一下啊，呃正向索引是从 0 开始，那反向索引呢是从呃 -1 开始。 好，那么紧接着我们再来看。假设我现在要去获取的是第三个元素呢？哎如果获取第三个元素啊，那么我们来看一下，那这个里面该怎么写？ 呃获取第三个元素的话你想，这块是 0，这个是 1，这个是 2，对吧？那这块应该是 s 2。 好，那我们来看一下，那反向索引里面应该写几呢？来，大家在弹幕当中给我刷出来。如果我要获取第三个元素，反向索引里面写几？ 哎大家来看，那这个是 -8，这个是 -7，这块应该是负 6。好，我们写一个 -6。来，右键再次运行，大家来看，88 是不是就获取到了？ 那这就是我们获取列表元素的方式，直接通过这个索引来获取就可以了。两种方式：正向索引和反向索引。 然后紧接着我们在这一块再来说一下，那现在假设我想去修改这个列表当中的元素，我想将列表元素当中的这个 a，我想将它修改为 abc，大写的 ABC，看一下怎么做呢？ 呃那么要想去修改列表当中指定的元素，那你先得去根据索引获取到这个元素。 那大家来看一下，a 这个元素我们如果按照正向索引来获取的话，它的索引应该是几啊？你看前面是五个元素，这是第六个元素。那么第六个元素它的索引应该是 5，因为索引是从 0 开始的， 对吧？所以这块直接 s 方括号 5，这就可以了。这获取到的就是第六个元素。 那么刚才我们是获取元素，我们直接将这个元素拿到就行了。那现在我们是要去修改这个元素，那这个时候我就需要为 s 这个列表当中的第五个位置这个元素进行一个赋值就行了。所以后面加上等于。好，后面这块来一个 ABC。那这就是修改。哎因为我们刚才提到列表当中的元素是可以修改的。啊我们直接为指定索引位置这个元素赋一个值，那么它就会将这个位置的元素修改掉。 好，那修改完毕之后呢，呃接下来我在这块我就可以直接将 s 输出出来。那 s 呢其实代表的是整个列表，我可以将整个列表输出出来，看一下这个 ABC 有没有把这个 a 给它修改掉。 好，那接下来呢我右键直接运行，运行之后大家来看，ABC 有没有将这个 a 给它修改掉？ 有。哎第六个位置的元素是不是就变成了 ABC 了？ 好，这就是修改元素。 但是这块需要提醒大家一点啊，如果说我这块来一个 s，s 几呢？s 10。大家来看，这是 5 对吧？这是 6，这是 7。那么对于这个列表来说，它有没有索引为 10 的元素啊？ 没有。那如果这个时候我再为它赋一个值为 DEF，那此时会发生什么现象呢？ 哎这个时候我们右键直接运行看效果就可以了。大家来看，怎么着了？ 呃 error，这代表的是发生错误了，报错了。什么错误呢？ 呃大家来看一下啊，这块告诉我们 list index 代表的是索引，呃 list 分配的这个索引 out of range，已经超出范围了。 啊所以这一块大家需要注意哦。 我把这一块的这个代码注释了，然后在上面在这块给大家加上一个呃注意事项啊。 那么如果指定的索引 超出 范围 将会 报错。 啊那报的这个错呢，哎就是这个错误。告诉我们索引超出范围了。啊这个点要注意一下。 好，那么这一块演示完毕之后，最后再来说一下，那我们如何来删除这个列表当中指定的元素呢？ 呃如果我们要删除列表当中指定的元素啊，那么这个时候我们需要用到一个关键字啊。那这个关键字叫 del。 然后我们再去指定我们要删除哪个元素。那比如我现在想去将这个 hello 给它删除了，那这个时候我就需要去指定 hello 这个元素。呃那你看前面是六个元素，这是第七个，那这块应该是 del 这块方括号写几啊？写 6。 这就代表我要删除的是 s 这个列表当中的第 7 个元素，因为索引为 6 就是第 7 个元素。删除掉之后，我再将整个列表输出出来看一下有没有成功删掉。好，我们直接右键运行，运行之后大家来看，hello 还有没有？ hello 就没有了，是不是删掉了？ 哎这就是删除指定位置的元素。通过一个关键字 del。其实这个 del 呢就是 delete 的简写，删除那个单词的简写。 好嘞，那么通过我们这块的这个演示呢，大家会发现啊，我们所定义的这个 list 列表里面确实是可以存储不同类型的元素的。并且呢，这个列表当中的元素它是可以重复的，也是可以被修改的，而且呢也是有序的。 那怎么验证它是有序的呢？ 呃其实我们刚才在这块输出的时候大家已经看到了，我们存的时候顺序是什么样子的，那么这块输出出来的顺序是不是就什么样子的？当然呢，我们也可以将这个列表的元素挨个取出来来看一下。 那如果说我们想去将列表当中的元素每一个元素挨个地取出来，然后一个一个地输出，那这个时候呢，呃我们就可以通过我们前面所学习的一种语法将其循环遍历出来。 哎就是 for 循环。 哎我们前面提到，for 循环是不是就是用来遍历数据集的？ 哎那其实对于我们的 list 来说呢，我们就可以使用 for 循环来进行遍历。来看一下怎么做。 呃直接写一个 for，然后我在这块写一个 item in s。大家来看哦，这个 in 关键字后面跟的 s 是不是就是我们要遍历的数据集啊？哎就是我们所定义的这个列表。然后这个 item 其实就是一个变量，啊这个变量名你叫什么都可以。那这个就代表的是遍历出来的元素，我将元素直接输出出来，看一下，看一下遍历出来的顺序跟我们这块的这个顺序是否一致。 来，我右键直接运行，运行之后大家来看， 56、 90、88、65、90、ABC、True，是不是一致的？ 哎大家会发现，跟我们存储的顺序呢是一致的。那这就表明啊列表 list 它确实是有序的。我们要想去遍历列表当中的每一个元素，哎直接使用 for 循环就可以遍历输出了。 好的，那这是我们这小节啊先认识一下列表 list 以及它的特点。 那演示完毕之后，最后呢我们回到 PPT，简单来做一个小结。主要明确这么三个问题。第一个问题： 列表的定义方式。怎么定义一个列表啊？多个元素使用什么将其括起来？ 哎，是中括号将其括起来。然后多个元素之间呢，使用逗号分割。 而对于它的特点来说呢，首先它是可以存储不同类型的元素的，而且是可以重复的、是有序的，元素也是可以修改的。 然后第二个部分呢，就是关于列表的索引。那我们刚才讲到是不是有两类索引啊？一种呢是从列表的头部到尾部正向索引，那正向索引是从 0 开始的。另外一种是从尾部到头部叫反向索引，反向索引是从 -1 开始的。 然后紧接着第三点呢，就是关于呃我如何去获取或者说查看列表当中的元素，怎么修改，怎么删除。那如果说你要查看的话呢，呃那在这块呢你就直接写这个列表啊，list1 就代表的是列表的名字，方括号里面写上对应的元素的索引就可以了。啊这就是查看。那如果是要修改，那你为指定的这个元素重新赋一个值，那就会将列表当中指定位置的这个元素修改掉了。那如果要删除，用到一个关键字 delete，就可以删除掉列表当中指定位置的元素。 但是这里有一个注意事项大家需要关注一下。 就是我们在指定这个列表的索引或者说列表的下标的时候呢，不能超出列表的索引范围。如果超出范围，将会报错。 好的，那这是我们这小节呢，先认识一下什么是列表，列表的定义方式，以及列表的特点。那下一小节呢，我们再来讲解列表当中的常见操作。 那我在下一小节等您哦。 黑马程序员为千万学生少走弯路，我们一直在努力。
