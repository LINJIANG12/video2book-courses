好的，那讲解完了面向对象高级之后，接下来呢，我们再来讲解 FastAPI 基础。那 FastAPI 呢，它是 Python 当中的一个 Web 框架。那基于 FastAPI 我们就可以快速地构建出属于自己的 Web 应用。那由于这里呢是大家构建的第一个 Web 应用，啊所以呢，在讲解 FastAPI 之前，我们首先先来认识一下什么是 Web 以及什么是 Web 应用，以及 Web 应用它的一个工作的流程是什么样子的。然后第二个部分呢，我们再通过一个入门程序让大家快速感受一下，那么基于 FastAPI 这样的 Web 框架，它是如何来构建 Web 应用的。好，那么接下来这一小节呢，我们就先来讲解第一部分：Web 初识。那首先我们先认识一下什么是 Web。那 Web 呢叫全球广域网，也称为万维网，其实就是我们熟悉的那个 3W，啊那全称呢就是 World Wide Web。那说白了，Web 指的就是能够通过浏览器所访问到的网站。那么大家可以思考一下，大家在平时日常的学习、日常的生活以及日常工作当中，那么通过浏览器所访问到的网站多不多呀？哎是不是很多呀？哎因为大家在 PC 端，大家在电脑上去上网的时候，基本上主要的途径是不是就是通过浏览器来访问的？哎那么大家想，大家当前所看到的这个视频，是不是就是你访问 B站，然后看到的这个课程视频啊？哎那这个 B站 呢其实就是一个 Web 网站，我们也叫 Web 应用。那么除此之外，再比如大家所访问到的一些电商网站，比如像京东、淘宝、天猫、唯品会这样子的一些电商网站，还有像一些新闻网站，啊还有一些官网的一些门户网站。那除此之外，还有呢就是我们自己所构建的一些网站应用，那这些都算是一些 Web 网站，我们也叫 Web 应用。那总而言之一句话，大家通过浏览器地址栏你所能访问到的这些网站，那都是 Web 网站，也称之为 Web 应用。

好，那么接下来呢，我们再来聊一下，那么这一个 Web 网站它是由哪些部分组成的呢？哎其实一个 Web 网站它的三大核心组成部分分别是：前端程序、服务端程序以及数据库。那么紧接着我们就来聊一下，那么前端程序负责什么事，服务端程序和数据库又负责什么事呢？那首先先来说一下前端程序啊。那前端程序呢主要负责就是将数据以好看的样式呈现出来，就是负责界面展示的。那么大家打开你的浏览器，在你的浏览器地址栏在这一块输入指定的域名之后一敲回车，是不是就访问到了这样的 Web 网站了？那么大家在 Web 网站当中所看到的这个界面展示的这个形式，包括这里怎么是一个轮播图，左侧这一块怎么是商品的分类的这个菜单信息，那右侧这一块是怎么展示出对应的这些商品的卡片信息的。那这些呢，其实都是由前端程序负责的。前端程序主要就负责将数据以好看的样式呈现出来，而这里的这些数据是在前端页面当中写死的吗？哎比如大家这里所看到的每一个商品的数据，包括商品的名字，包括商品的单价、商品的销量以及商品的原来的单价是多少、商品的折扣是什么样子的，这些是在前端的页面前端代码当中直接写死的吗？哎其实并不是。那这些呢，都是要去请求服务端端的。所以大家可以来看，那这里呢，我在这块我打开这个 F12，找到这里的 Network 哈，大家来看。那么当我在这一块我去搜索一个关键字，搜索一下手机。当我搜索手机的时候，我一点搜索，大家会发现在 Network 网络这栏是不是发送了大量的网络请求啊？哎其实这就是前端网页再去发送请求，请求服务端来获取数据。啊因为前端页面呢，它主要就负责将数据展示出来，那么这些数据其实都是来自于服务端，服务端给前端返回的。啊所以这里呢，那么前端它还需要去访问服务端。那服务端呢，它主要就负责业务逻辑的处理。哎比如我刚才在这一块输入手机之后，它是如何根据这个关键字去搜索对应的商品信息的？它是如何去解决高并发问题的？如何去解决权限问题的？数据量大了具体怎么去处理的？那么这些都是由服务端来处理的。哎所以服务端呢，它主要就负责业务逻辑的处理。而这些数据最终也不是在服务端的代码当中存储的，而是要去访问数据库的。所以服务端还需要去访问数据库。那数据库呢，它就是专门来存储数据的仓库，啊就负责数据的存储和管理。那么最终服务端访问数据库，数据库再将这些符合条件的数据再返回给服务端，那服务端再对数据的格式再进行进一步的处理，最终再将符合条件的这批数据再返回给前端。那前端呢，再将这些数据以好看的样式呈现出来。

好，那么接下来呢，我要问一下大家，那么在这个流程当中啊，那现在假设我打开浏览器，我去访问，到底是访问的是前端呢？访问的是服务端？还是直接访问的数据库？来，大家告诉我，访问的是 1 还是 2 还是 3？来，所有的小伙伴在弹幕当中刷出来。哎对了，它访问的其实是 1。当我们在浏览器地址栏输入地址之后，那么一敲回车，其实它访问到的其实就是我们的前端程序。那么前端程序再去访问服务端获取数据，服务端再访问数据库，数据库将数据返回给服务端，服务端将数据进行进一步的处理之后再返回给前端，最终前端再将数据以好看的样式呈现在浏览器当中。哎这就是一个 Web 网站它的请求访问流程。哎主要是有三个部分组成的：前端程序、服务端程序和数据库。那么对于这里的这个数据库来说哈，那么在真实的项目开发当中，数据都是存储在数据库当中的，而只不过当前呢，我们这套课程它是一套 Python 基础课程，那么里面不涉及到数据库。所以这里呢，我们等会儿再去开发这个 Web 应用的时候，我们会将数据存储在文件当中。那文件当中是不是也可以存储和管理数据啊？哎那我们等会儿就将数据直接存放在文件当中就可以了。

好嘞，那么 Web 网站的工作流程我们大概了解之后，接下来呢，我们再来聊一下，那么前端程序和服务端程序那具体是怎么开发出来的？那具体有哪些技术构建的？好，那么首先先说一下前端。那么前端呢主要负责的是网页的展示，啊就是将数据以好看的样式呈现出来。前端网页那通常是由三个部分组成的：分别是 HTML、CSS 和 JavaScript，简称 JS。那么 HTML 负责的是网页的结构，啊也就是网页当中有哪些内容是由 HTML 来负责的。那么 CSS 它负责的是网页的表现，哎也就是网页展示出来的这个样式到底好不好看，是由 CSS 来控制的。那么 JS 呢，控制的是网页的动作、行为，啊也就是网页的一些交互效果。好，那这就是网页的三个组成部分。好，当然为了大家能够更加深刻地理解前端网页的这三个组成部分啊，呃那这里呢，我在给大家提供的资料当中也准备了一个 Web 网站的演示项目。那基于这个演示项目啊，我就给大家来演示一下，那么 HTML、CSS、JS 分别控制的是什么，来加深大家的理解和印象。那么首先大家会看到，在这个 Web 网站当中啊，那么这一块是不是有一个 HTML 的一个文件啊？那这个 HTML 呢，呃里面其实编写的就是 HTML 的代码，啊其实就是一堆的标签。那这个我们在前面讲解网络爬虫的时候大家是见过的，你看里面是一堆的标签啊，哎其实就是前端代码。好，那它呢主要负责的就是哎网页的结构，也就是网页的内容。然后紧接着大家再来看，那么在这一块是不是还有一个文件夹叫 CSS 啊？那么 CSS 里面呢存放的是一堆的 CSS 文件。那么 CSS 文件呢里面其实存放的就是 CSS 的代码。来，我打开这个 CSS 代码，大家来瞅一眼。你看里面是一堆的代码。那 CSS 负责的就是网页的表现，也就是网页的样式。那还有一个文件夹是 JS，那么 JS 呢，它控制的是网页的交互效果，网页的动作和网页的行为。

好，那么接下来呢，在演示的时候，我将这里的 JS 删掉，CSS 文件夹也删掉。那么这里呢，就只有一个 HTML 了，当然还有一些图片啊、字体啊这些文件就不用去删了。好，那么接下来我直接双击打开这个 HTML 这个文件。那这个 HTML 我双击打开之后，它是不是会自动地被浏览器识别的？那浏览器就会将我们刚才大家所看到的页面的源代码，也就是 HTML 的标签，解析并渲染出来，然后呈现在浏览器上。哎大家所看到这个效果就是解析渲染之后的效果。但是大家会发现，当前这个 HTML 当中呢，其实并没有任何的 CSS 和 JS，你会发现这个网页是不是比较丑啊？哎只有相关的一些字，还有一些超链接，还有一些呃图片，图片堆放的呢其实也比较凌乱，啊并没有一个有效的组织。这就是纯 HTML 的效果，因为 HTML 控制的是网页的内容、网页的结构。如果你想让这个网页美观一点、好看一点，来大家告诉我，需要把哪块的代码加入进来？来大家在弹幕当中刷出来。哎对了，需要将 CSS 加入进来。来我们再回到文件夹当中啊，我 Ctrl+Z 撤销一下，你看这个删除掉的 CSS 文件夹是不是就回来了？那这个时候我再次来打开我们的这个页面，然后刷新，大家会发现，那么现在这个网页它的样式是不是有了一个翻天覆地的变化呀。哎你会发现这个页面明显美观了很多，好像跟刚才的页面都不是同一个页面了一样，因为这里面加入了 CSS 样式之后，让这个网页的布局以及网页的样式进行了一个美化。这就是 CSS 的效果，控制的是网页的表现、网页的样式。

好，那么紧接着再来看，现在你看啊，我鼠标放在这个官网游戏资料攻略中心上，其实是没有什么反应的，只是 CSS 给它添加了一个背景色。这里并没有将那攻略中心里面具体有哪些功能，对吧，具体有哪些菜单并没有给我展示出来。那如果说你要去增加页面的交互效果，增加页面的动作行为，那这里呢需要再将 JS 引入进来。来 Ctrl+Z 再撤销一下，JS 是不是就回来了？那么 JS 回来之后，再次回到浏览器，来我刷新一下。刷新之后，来你看我鼠标往这一放，下面的这些菜单是不是都展示出来了？这就是 JS 的作用。啊控制的是网页的交互效果和动作行为。而且你会发现下面的这个轮播图是不是也已经开始轮播了？哎已经开始播放了。好的，那这就是这里啊通过一个案例给大家演示一下 HTML、CSS、JS 它们之间的职责是什么。

好，那这是前端程序。那么紧接着再来说一下，那这个服务器端程序又是基于什么技术构建的呢？那么服务器端呢我们现在学的是 Python，那这里呢我们就可以基于 Python 当中的 Django、Flask 或者是 FastAPI 来进行构建。那么在当前的企业项目开发当中啊，那么对于一些新项目来说，基本上选择的都是 FastAPI。因为 FastAPI 在进行 Web 应用构建的时候会更加快捷、更加高效。哎所以呢我们当前啊在讲解这个项目实战之 Web 开发的时候，那我们选择的就是 FastAPI。基于 FastAPI 来构建服务器端的程序。好的，那这是我们这一块啊认识一下那么前端程序的组成部分，以及服务器端程序那么稍后呢我们如何来构建，我们就会基于 FastAPI 来构建。

好的，那这是我们这小节啊先认识一下什么是 Web，Web 网站的工作流程。那么最后呢，还是一样简单做一个小结，主要明确两个问题。第一个问题，那一个 Web 网站通常是由哪几个部分组成的呀？哎大家可以回想一下，我们刚才在讲解 Web 网站工作流程的时候提到它是由几个部分组成的？是不是三个部分？第一个部分是前端程序，那它呢主要负责将数据以好看的样式呈现出来。然后第二个呢是服务器端程序，那我们也叫后端程序。那它主要负责的是逻辑的处理。那紧接着第三个部分呢就是数据库程序，那它主要负责的是数据的存储和管理。而当我们在打开浏览器，在浏览器地址栏输入域名之后，一敲回车首先访问到的是不是前端程序？然后前端程序是不是再来访问后端程序？那么后端程序是不是再访问数据库？数据库再将对应的数据返回给后端程序，返回给服务器端程序。服务器端程序再对数据进行进一步的逻辑处理，处理完毕之后再返回前端。最终前端程序是不是再将返回回来的数据以好看的样式呈现出来？哎这就是 Web 网站的组成部分以及工作流程。那么紧接着再来明确下第二个问题哈，那么就是对于前端程序来说，一个前端网页它是由几个部分组成的以及它们的职责是什么？哎是不是还是有三个部分组成的？分别是 HTML、CSS、JS。HTML 控制的是网页的内容，啊就是网页的结构，网页当中有哪些内容。那么 CSS 负责的是网页的表现、网页的样式，而 JS 负责的是网页的动作行为以及交互效果。好，那么这是前端网页的三个组成部分以及它们各自的作用。

那最后呢说明一点啊，由于我们当前是 Python 基础课程并不是前端课程啊，所以呢在我们的课程当中我们并不会详细的去讲解 HTML、CSS、JS 这一块的代码该怎么写。啊因为现在我们已经有了 AI 了，那么这些个前端页面的一些代码其实我们是完全可以借助于 AI 来帮我们生成的。AI 生成之后基本上都不用做任何的改动。哎所以呢稍后我们在开发 Web 应用的时候，那么涉及到前端页面的部分，那这里呢我们直接让 AI 来帮我们生成就可以了。那我们的重心呢是放在 Python 上，那在 Python 当中我们如何基于 FastAPI 来编写服务器端的代码。好的，那这是我们这小节所讲解的 Web 初识。那么下一小节呢我们就来讲讲那么基于 FastAPI 我们如何来构建 Web 应用。那我们下一小节再见。黑马程序员为千万学生少走弯路，我们一直在努力。

好的，那我们大概了解了什么是 Web 以及 Web 网站的工作流程之后，接下来这小节呢我们就通过一个入门程序让大家快速感受一下 FastAPI，看一下基于 FastAPI 这样的 Web 框架我们是如何来构建一个 Web 应用的。那首先我们先来认识一下什么是 FastAPI。那 FastAPI 呢它是一个现代、快速、高性能的 Web 框架。哎它就是一个 Web 框架。那么通过 FastAPI 呢它可以基于标准化的 Python 类型提示来构建 API 接口服务。好，那么这是官方网站当中啊对于这个 FastAPI 的一个介绍。那下面这个就是它的官网，啊我们可以直接打开官网，打开官网之后大家来看。那么在官网当中啊那么在这一块就去详细的介绍了一下这个 FastAPI。哎这一块是不是介绍它是一个现代的、快速的高性能的 Web 框架？哎然后呢在这一块用于基于标准的 Python 类型提示来构建 API。

好，那么这一块我再说一下这个什么是 API 啊？那 API 呢是这三个单词的首字母缩写哈，叫 Application Programming Interface。那翻译过来呢叫应用程序编程接口。啊简单说这个 API 接口指的就是我们程序对外提供的功能的入口，啊供别人来调用的。那就比如，那么其实在现在的互联网上有很多很多的这个 API 接口服务。啊那这个是别人写好的来供我们使用的，比如大家来看，那么这一块有一个叫做天气查询的 API 接口。这个就是别人开发好的，啊我们可以直接去调用人家给我们的这个接口服务。那这个接口呢其实我鼠标往这一放，大家就能看到啊其实就是一个 HTTP 的一个 URL 地址。好，那我们在这块你看，我现在点击访问这个 URL 地址，你看这。那么这个访问的过程当中是不是跟上了很多的一些参数啊？啊那这个呢是查询天气的哈，包括这一块的这个经纬度，啊包括我们当前要查询的是当前的天气以及我们的时区。那这些个参数我们传递过去之后，那么对于这个 API 接口服务来说啊，它就会根据这一块的这个条件来查询出对应的天气信息。好，那这里呢就查询出来了，那么当前啊这个经纬度其实我传递的是北京的这个经纬度哈，那么当前北京的这个温度呢是 21.4 度，对吧？哎以及它的风速是什么样子的，那么这些呢在这一块都会给我们返回回来。那这个呢就是别人给我们提供好的这样一个 API 接口服务。啊我们拿到这个 API 接口服务的访问地址之后，就可以直接去调用了。而我们现在学习的就是 Web 应用的开发，那也就是说啊现在我们学习的不是怎么去调别人提供好的，而是如何来开发一个 API 接口服务来供别人调用。哎所以呢 FastAPI 啊它的作用就是一个 Web 框架，那么基于 FastAPI 这个 Web 框架，那我就可以开发出一个一个的 API 接口服务了。那这些 API 接口服务开发好了之后，别人只要知道了那我这个服务调用的地址，那他就可以来调用了。比如我们可以在浏览器端来调用，我们也可以在其他的系统当中来调用。你只需要在浏览器端或者其他的这个项目当中啊发起 HTTP 请求来访问我们当前开发的这个 Web 应用是不是就行了？哎然后 Web 应用接收到请求之后，然后再给它响应对应的数据就可以了。哎所以呢 FastAPI 这个框架啊其实它就是用来开发这样的 Web 应用的。那开发好了这些 Web 应用之后，你就可以把我们 Web 应用当中所开发的这些 API 接口是不是就可以提供给别人了？哎别人就可以来调用了。

好，那接下来呢我们就来看一下那基于 FastAPI 如何来开发这样一个 Web 应用。那接下来呢我们就进入到 FastAPI 的入门。那我们要想使用 FastAPI，那它并不是 Python 官方标准库当中的提供的。那么它是第三方的，啊所以要想使用，第一步你先得安装 FastAPI。那么安装完 FastAPI 之后，接下来呢，在我们的代码当中你就可以直接去导入 FastAPI 这个类了，啊就是从 FastAPI 这个模块当中啊来导入 FastAPI 这个类。那接下来第二步呢，我们就可以基于导入进来的 FastAPI 这个类来创建 FastAPI 实例了。然后紧接着第三步呢，就可以来创建路径操作函数。啊简单来说就是在 python 文件当中，我们可以来定义函数，然后在函数上面加上这么一个装饰器，哈。那这个装饰器呢，呃其实就是 FastAPI 当中提供的。那你看不看这个装饰器叫什么？叫 app.get。啊那这个 app 指的就是 FastAPI 的实例对象。啊然后这一块的这个 get 呢，其实就代表啊，那么将来你要想访问我当前这一块的这个代码，那你得发起的请求方式得是 get。然后请求路径是什么呢？哎请求路径在这一块就是斜杠。哎访问根路径就会访问到这个函数。那么最终这个 return 返回的这个字典啊，那其实就是我们要响应的数据。谁调用我们，我们就响应给谁。如果是前端程序调我们，我们就响应给前端；如果浏览器地址栏直接调用，那我们就响应给浏览器；如果是别的系统调用，我们就响应给对应的系统。啊那下面这一块呢也是类似的哈，那我们可以定义多个功能接口，啊也叫 API 接口。好，那么这就是第三步：来创建路径操作函数，定义访问路径。啊那我这个访问路径呢，就是在这个装饰器当中来定义的。

那么最后一步呢，就是将这个 FastAPI 服务要将其运行起来。那么运行 FastAPI 服务呢有多种方式，啊那第一种方式呢就是我们可以在命令行当中运行，啊直接基于 fastapi dev，后面在这一块跟上这个 Python 文件就可以直接运行了。啊那么第二种命令行当中的运行方式呢，就是借助于这样子的一个服务器啊叫做 uvicorn。啊借助于这样一个服务器来运行，然后指定。那我们要运行的是哪一个文件当中的哪一个实例呀？哎，这个 app 指的就是 FastAPI 的实例，然后后面在这一块跟上一个选项 --reload 就可以了。那这两种方式呢就是通过命令行来运行。当然如果说你觉得通过命令行方式比较繁琐的话，那么也可以在我们的 Python 文件当中啊来指定我们要基于 uvicorn 这个服务器来运行当前 FastAPI 的实例。好，那这个实话呢，我们直接在这一块呃去导入这个 uvicorn，然后调用 uvicorn 这个服务器当中的 run 方法，那么就可以将我们的 FastAPI 服务运行起来。啊你看这个 app 是不是就是 FastAPI 的实例？然后这个 host 就代表，呃那当前所运行的这个 FastAPI 的这个应用，那么你在哪些主机上可以访问呀？如果这一块写的是 0.0.0.0 就代表你在任意的主机上都是可以访问的，IP 地址不做限制。那 port 就代表的是端口号，那你要访问的端口是 8000 这个端口。好的，那这就是我们使用 FastAPI 来开发 web 应用的操作步骤。

好，那么接下来呢，我们就将这个操作步骤啊我在这一块我截个图贴在屏幕的右上方，接下来呢我们就直接来按照这个步骤完成这一块的程序开发。来，我们直接打开 PyCharm。呃那么打开 PyCharm 之后啊，首先第一步，由于这个 FastAPI 它不是 Python 标准库当中提供的，所以要想使用先得安装。那这里呢我们就直接找到这一块的这个终端，找到终端之后，那么这里呢我们直接来 pip install，然后这一块写上 fastapi。那先来安装这个 FastAPI。好的，那很快呢这个 FastAPI 已经安装好了。那么安装好之后将这个命令行就可以直接关掉了。那接下来呢我们就在第六章这个目录下，在这一块来创建一个 Python 文件啊。呃那这个 Python 文件呢我就叫 08_这一块来一个叫 fastapi入门，好，就叫这个。

那首先第一步呢我们需要导入 FastAPI，啊那要导入的是 FastAPI 这个类哈，所以在这块呢我们就从 fastapi 这个模块当中来导入 FastAPI 这个类。然后紧接着第二步呢，就是来创建 FastAPI 的实例对象。啊那么创建实例对象啊在这一块写一下。呃写下注释啊：创建 FastAPI 的实例。那创建 FastAPI 的实例呢就直接基于这个类来创建就可以了，这个 app 现在就是 FastAPI 的实例。

好，然后紧接着第三步呢，那我们就可以来创建路径操作函数来定义访问路径了。那这步其实它也叫定义路由。啊路由呢指的就是根据访问路径来找对应的操作函数。好，那这一块我们就来写一下哈，呃我们也叫定义接口，啊定义 API 接口。好，那这里呢首先我在这块定义一个函数，呃这个函数名你随便定义啊，我们这一块就叫 root。然后紧接着在这个函数当中呢，我就直接返回这么一个字典，啊那字典的 key：message，值：helloworld。那注意，我们所定义的这个函数的返回值就是最终我们要响应的数据。来，我在这块写一下哈。那该函数的返回值哈，该函数的返回值表示那么就是这个接口最终要返回的数据，啊 API 接口返回的数据。那么紧接着那再来看，那最终你想我们所定义的这个 API 接口是给别人使用的，别人使用是要通过这个访问路径来访问的。那这个访问路径是什么呢？啊那么这个时候呢可以在上面再去加上一个装饰器哈。呃那这里大家来看装饰器的写法就是 @app.get，这个 app 与这儿是对应的。啊那么 .get 就代表那么将来你想访问当前这个函数，那你必须要以 get 方式来请求。好，我们在这一块再写一下哈，这个哈接口的访问路径为什么呀？为呃斜杠，然后请求方式为 get。好，这就就可以了。

好，那么如果说当前的这个 web 应用当中啊还有其他的这个接口，你继续在这块定义哈，再定一个 API 接口。呃那比如我们按照相同的方式哈在这一块先来定义一个函数，呃那么这个函数呢我这里就叫做 get_users，啊获取所有的用户数据。然后紧接着呢下面这一块我就让 AI 助手给我提示一下哈，生成了这么三个用户信息，啊当然是基于列表来封装的，里面就是一个一个的字典，有 id 有 name。OK，这就是我们最终要返回的数据。然后紧接着在上面在这一块再加上一个呃这个装饰器哈，@app.get 代表请求路径是什么？请求路径是 /users。好嘞，那这样呢第三步我们也搞定了。啊我们定义了这个操作的函数，然后在上面定义了访问的路径。

那么接下来最后一步呢，我们就需要将当前的这个 FastAPI 的服务要启动起来。那如何运行这个 FastAPI 的服务呢？方式有多种。来，先操作第一种方式啊：fastapi dev，后面跟上具体的文件名。来，我这一块直接打开终端。呃那么打开终端之后哈，呃那这里呢我们要找到第六章哈，在这个所以在这块要切换一下目录哈。第，哎第六章，啊 6，走。切换到第六章。那我们要运行的是第六章这个目录下的 08 这个文件，对吧？哎所以在这块直接来哈，就是 fastapi dev，然后呢在这个里面我们就要来指定我们要运行的是哪个文件，是这个文件，直接把文件名放进来，这就可以了。好，那么接下来呢我们就可以直接回车。好，那么回车之后大家来看，这块报错了。那原因呢是因为当我们基于这种方式在运行的时候，那你需要去安装一下这个 fastapi-standard。啊并且这一块已经告诉我们了，请安装这个 fastapi-standard。那这个时候其实操作很简单哈，就是第一次运行可能会报错哈。所以这个时候大家只需要把这条命令怎么着？原封不动复制，然后右键在这一块粘贴，然后回车。好，让它去安装一下就行了。好嘞，那么安装完毕之后哈，那接下来呢我们直接再次运行刚才的命令。这个时候大家只需要按键盘上的向上的箭头找到之前的这个命令，然后再次回车。那回车之后大家来看，那最终大家会发现这一块运行呢又报错了。那这里呢要给大家说一个问题哈。就是当我们在终端在命令行当中在运行的时候啊，那么这一块如果说你的文件当中，比如 08 后面这一块有个点，它会认为哎 08 是一个模块，然后呢会认为是一个层级分隔符，然后认为这个 08 它是一个模块，然后这个 fastapi入门 呢它是 08 这个模块下的一个文件。但是你会发现这个 08 是不是本身是我们文件当中的一部分，是文件名的一部分啊？哎但是它这一块识别成了一个模块，你看没有这个模块命名为 08。啊所以这个时候呢大家要注意哈。呃那这里呢如果说你在命令行当中运行，那这个时候你要规避这个问题其实也比较简单，你重命名一下，把这一块这个 08 这个点改为下划线。然后接下来呢在这一块再次来运行一下，来，这一块改为下划线，走。好，那么运行之后大家会发现，这次呢就正常运行起来了。

那么运行之后大家只要你在命令行当中能够看到这块访问的这个路径。你看，HTTP://127.0.0.1，这是不是本机的 IP 地址啊？:8000。这就代表我们的这个 FastAPI 服务已经启动起来了。那接下来呢，你就可以直接访问这个链接了。好，那在这块呢，我们可以直接复制一下。复制之后，来打开浏览器，然后在浏览器地址栏直接粘贴进来。好，因为浏览器地址栏是不是就可以发送 HTTP 的请求啊，而且浏览器地址栏所发送的 HTTP 的请求都是 GET 方式的请求。好，所以呢，和我们刚才在这块所编写的这个代码是完全契合的，GET 方式的请求，对吧？哎所以接下来呢，我们打开浏览器，然后输入这个地址之后，一敲回车，如果说能够出现刚才大家在这块所编写的这个返回值，也就是 message hello world，那就说明我们当前这个入门程序是不是就跑通了？好，那么接下来呢，来，我们直接打开浏览器，在这块直接回车。回车之后，大家来看。那这里呢，“message”：“hello world”，这块是不是就输出了？哎这就是我们刚才在 Python 代码当中所定义的这个函数的返回值。

好，那么此时大家会发现：“哎，涛哥，你刚访问的时候这个后面怎么没有带那个访问路径呢？”哎注意了，在浏览器当中如果你访问的是根路径，后面什么都没带，默认就有一个斜杠啊。大家来看，我复制一下这个地址。复制之后，来，我们这块直接找到我们 Windows 上的记事本，直接粘贴进来。我问大家，后面的这个斜杠有没有？有。那这个斜杠啊，其实就是我们的访问路径，那么与这块这个路径是一致的。那如果说你想访问下面的这个函数，那么这个时候你要访问的这个路径是 /users。来，我们在这块再次打开浏览器，来，这块加上一个 /users 啊，是一个斜杠。走。大家来看，id 1 张三，2 李四，3 王五，这一块是不是就已经返回回来了？哎这就是我们刚才在 Python 代码当中所定义的这个函数的返回值。好，那么这也说明啊，我们在浏览器端访问这个 /users 这个路径，最终是不是就访问到了这个函数了？好嘞，那这样呢，你看，我们是不是就在我们的这个 Web 应用当中对外提供了两个 API 接口了？哎一个访问路径是 /，是根路径，另外一个访问路径是 /users。好嘞，那这样呢，我们这个测试呢就通过了。

而我们刚才在来进行运行的时候啊，我们是基于 fastapi dev 这条命令来启动的 FastAPI 服务。那其实也可以通过下面这条命令来启动。好，那么接下来呢，我再来演示一下啊。大家如果说想将这个服务结束了，你直接按下 Ctrl+C，啊它就结束了。啊直接按 Ctrl+C 就结束了。那么此时这个服务器你看，shutting down 是不是就关闭了？好，那么接下来呢，大家也可以通过另外一条命令啊，叫做 uvicorn。然后呢，后面在这块就要跟上对应的文件名。啊那文件名呢就是这个啊。呃来，我在这块 Ctrl+C 复制一下。复制之后直接粘贴进来。那后面的 .py 啊，.py 这个后缀是不需要的，然后冒号后面在这块指定 FastAPI 的实例名，就是这个 app，然后后面在这块跟上 --reload。好，再次重新运行。那么重新运行之后，大家来看，那么这里是不是就已经启动起来了？哎启动起来之后你再次打开浏览器，又可以进行访问了。啊当然这块我美化一下输出，你看，又可以正常访问了。好嘞，那么这是两种启动 FastAPI 的方式。啊当然大家访问的时候你会看到，这个控制台这块是不是还有访问的一些日志信息啊？哎在这块告诉你访问的 GET 请求，请求路径是哪个，对吧？响应的状态码是什么，200 就代表成功了。

那么这两种通过命令行来启动 FastAPI 的方式大家觉得方便不方便？来，所有的小伙伴，大家认为方便的话来扣个 1，认为不方便的话来扣个 2。哎是不是并不方便啊。好，那么接下来再来给大家说第三种方式，我们推荐的方式是什么样子的。来，我 Ctrl+C 啊，先将其结束了。那第三种方式呢，就是我们直接在这个 Python 文件当中来定义当前服务运行的程序就可以了。好，那这个时候呢，来，上面是我们 FastAPI 的应用程序的代码。啊那么在这里呢，我们来写一个程序运行的入口。啊当然 AI 已经给我提示出来了。那这里呢，我们直接在这块 import uvicorn，啊就是导入这个服务器的依赖。那么在这块我要给大家介绍一下这个 uvicorn 啊。来，我在后面这一块来介绍一下。呃那么这个 uvicorn 呢，它是 Python 中的一个轻量级的 Web 服务器。啊它就是一个 Web 服务器。那么在这块呢，我们基于这个服务器就可以将我们的项目部署进去并运行起来。好，那这里呢，大家来看，我们导入这个 uvicorn 之后，直接调用 uvicorn 当中的 run 方法，就可以将当前服务启动起来了。然后这个 run 方法当中传递了这么三个参数，第一个参数就代表的是你当前这个 uvicorn 这个服务器你要启动运行的是哪一个 FastAPI 的实例啊？哎是 app 这个 FastAPI 的实例。然后紧接着第二点呢，就是你当前所启动的这个服务，那么将来在哪些主机上可以访问？如果 host 指定为 127.0.0.1，就代表只能在本机上访问。如果说你想让别人也能够来访问你所开发的 Web 应用啊这些 API 接口的话，这块改为 0.0.0.0，就说这个 IP 地址不做限制。那端口号呢，在这块我们设置为 8000。那么启动起来之后它占用的端口号就是 8000 这个端口。啊当然对这段代码来说啊，这是固定的，啊就是来启动这个 FastAPI 服务的。

好，那么接下来呢，我们要想运行就很简单了，直接右键，在这块直接运行就可以了。那么运行起来之后，占用的端口是 8000。那么接下来呢，我们再次打开浏览器，然后呢，在这块先来访问一下根路径啊。来，回车。是不是可以正常访问？来，这块来一个 /users。来，我回车，是不是依然可以正常访问？哎那就说明我们刚才这一块所编写的启动 FastAPI 服务这一块的这个代码是没问题的。好，那么后面呢，我们在进行 Web 程序开发的时候，在启动的时候，那我们都会基于这种方式来启动，会更加简洁、更加高效，而不用每一次都在命令行当中来敲这两条命令了。好的，那这是我们这小节所讲解的 FastAPI 的入门程序。

好，那么讲解完毕之后，那最后呢，我们再回到 PPT 简单来做一个小结。啊那主要明确这么两个问题，第一个问题，先得知道什么是 FastAPI。那 FastAPI 呢，我们说它是一个现代快速高性能的一个 Web 框架。那通过 FastAPI 这个 Web 框架，我们是不是就可以来开发这些 API 接口服务了？哎就可以来开发 Web 应用了。好，然后紧接着第二个点呢，就是基于 FastAPI 我们是如何来开发服务端的这些接口的。那我们刚才提到一共几个步骤啊？哎四个步骤。那首先第一步，要导入 FastAPI，要从 fastapi 这个模块当中导入 FastAPI 这个类。然后第二步，要基于 FastAPI 这个类来构建 FastAPI 实例，来创建实例对象。哎那么接下来第三步呢，就是来开发对应的接口了，啊要来创建对应的操作函数来定义对应的访问路径，啊就是开发 API 接口。然后最后一步呢，就是运行 FastAPI 的服务。那运行服务呢，我们讲到过三种方式，两种方式是基于命令行来运行的。那么最后一种方式是不是直接在我们的代码当中来启动这个 FastAPI 服务啊？哎那么命令行当中运行的命令呢是这么两条啊，fastapi dev，后面在这块跟上我们要运行的这个文件是谁。那第二条呢就是 uvicorn，哎基于 uvicorn 当中所提供的这个命令 uvicorn 来运行这个FastAPI 服务。那我们推荐的呢，其实啊就是在我们的代码当中导入 uvicorn 这个服务器的相关库，然后调用 uvicorn 当中的 run 方法，将当前的 FastAPI 这个实例传入进来，然后启动 FastAPI 的服务。啊我们推荐啊在项目开发当中用这种方式。那这种方式是不是一劳永逸啊？哎这段代码是固定的，我只需要编写这么一次，以后你再去运行当前的这个 Web 应用的时候，直接右键是不是直接将这段代码运行，那么这个服务器就启动起来了？哎然后就可以打开浏览器来访问了。好的，那这是我们这小节所完成的 FastAPI 的入门程序。

哎那么其实我们刚才所编写的这个入门程序啊，在官方文档当中都有。那最后呢，我再带着大家去看一下。那如果说大家想去查看一下那官方文档当中的一些详细信息，这个时候大家直接找到官方文档。找到官方文档之后，找到什么呢？找到这里的学习。找到学习之后，呃你可以从头到尾去看一遍啊。那如果说大家想快速入门的话，从哪去看呢？找到教程。啊找到教程之后这块有个入门指南。那么在入门指南当中啊，大家来看，这里呢就有第一步做什么。你看，这是不是就是我们刚才写的代码？你看这儿，是不是就我们刚才写的代码？哎以及下面这块怎么去运行、怎么打开浏览器去访问，那么其实在这个里面都有说明。而且这个文档呢是 FastAPI 所有的文档，里面包含了 FastAPI 所有的详细的用法。那这个呢，大家有兴趣的话可以自己去翻阅一下官方文档。好的，那这是我们这小节所讲解的 FastAPI 的入门。那接下来呢，大家可以暂停视频，完成这一块的入门程序。那入门程序完成之后，下一小节呢，我们就要进入到我们实战案例的开发了。黑马程序员，为千万学生少走弯路，我们一直在努力。
