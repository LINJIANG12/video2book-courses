好的，那在上一小節呢，我們介紹了一下字符串這種數據容器以及它的特點，那接下來這一小節呢，我們再來介紹一下字符串的常用方法。 那字符串的方法呢比較多，我們這一塊主要介紹一些常用的一些方法，而在介紹這些方法之前，強調一點， 我們剛才講到字符串它是不可變的，也就意味著它是無法修改的，那我們在調用這些常用方法的時候，調用所有這些方法都不會對字符串的內容做任何的修改。好，明確這點之後，接下來呢，我們再來看一下那常用的方法以及方法的含義。 呃，我們簡單來看一下哈，首先第一個是 find，第二個是 count，這兩個方法呢都是用來查找統計的。 那首先第一個呢就是 find，find 呢是用來查找的哈，在字符串當中查找指定的子串，子串指的是子字符串，所謂子字符串指的就是字符串的一部分。 那麼 find 方法它會返回第一次出現這個子字符串的索引位置。注意喔，是從前往後查找，它會返回第一次出現這個子字符串的索引位置，如果找不到，返回 -1。 好，這是 find 這個方法的作用。 然後第二個呢是 count，那 count 呢是用來做統計的，統計這個子串在字符串當中出現的次數。 然後再往下走呢，這兩個是對應的，一個叫 upper，一個叫 lower，那 upper 呢是將字符串轉為大寫，lower 呢是將字符串轉為小寫。注意喔， 它並不會修改字符串本身，而是轉大寫、轉小寫之後，它最終會再給我們返回轉大寫、轉小寫之後的結果，而不會修改字符串本身。 好，那麼再往下走呢，就是這個 split，那 split 呢是進行字符串切割的。 啊，可以按照指定的字符串將字符串切割為多個部分，比如大家來看， 這一塊我們 s.split，這一塊寫了一個，那注意喔，這一塊不是空字符串，而是一個空格，這就代表我要按照空格來將指定的字符串切割為多個部分，那最終它會返回一個列表。 啊，那多個部分的元素它就會放在一個列表當中。 然後接下來再往下呢，就是 strip， 呃，那 strip 呢它是去除字符串左右兩端的這個空白字符啊，也就是空格，或者是指定字符。如果說你直接調 strip，那默認它是去除空格的，如果你寫 strip 裡面傳遞了一個參數星，那這就代表我要去除字符串左右兩側的這個星號。 好，那麼再往下呢，就是 replace，replace 它是用來替換字符串的，呃，我們可以將指定的字符串替換為一個新的字符串，比如後面這樣，這就代表我要將這個字符串當中的這個 H 替換為 C。 好，那麼這是替換字符串。還有一個呢，叫 startswith，那麼 startswith 呢，它是用來判定這個字符串是否是以指定的子串開頭的，比如 s.startswith('p')，這就代表我要判斷 s 這個字符串是不是以 p 開頭的，如果是，它將會返回 True，如果不是，將會返回 False。 好，那麼這就是字符串當中常見的這些方法以及它的作用。 那簡單介紹之後啊，接下來呢我們就快速來演示一下，來，我直接打開 PyCharm，那麼在 PyCharm 當中啊，我這一塊已經準備好了，那接下來呢我們就一塊來看一下那具體的作用啊。 呃，我們一個一個來看啊，我這一塊打一個斷點哈，大家來看， 呃，首先這是我們定義的一個字符串， 那麼 s.find 它是用來查找指定字符串第一次出現的位置的， 那你看我要查找誰？ 查找橫槓這個字符串， 那這就代表我要查找橫槓這個字符串在這個 s 這個字符串當中出現的位置，注意是第一次出現的位置， 那最終會返回一個索引值，你看， 這是 0，這是 1，這是 2，這是 3，這是 4，這是 5， 第一次出現的位置索引是不是為 5 啊？ 哎，那這個 index 呢其實就是 5。 好，我們來看一下 find 查找之後的效果啊，那我這一塊右鍵 Debug 運行哈。 好，我們在這一塊呢看一下控制台啊，往下走，走， 大家來看，這，看這看這， 是不是 5 啊？ 哎，獲取到的就是第一次出現的索引位置。 然後第二個呢是 count，count 是統計指定的這一個子字符串出現的次數的， 你看，count('o')，那這就代表我要統計 o 在這個字符串當中出現的次數。 來，我們這一塊往下走一步， 那最終輸出 4 就代表出現了 4 次，呃，是沒問題的。 好，那麼 upper 和 lower 呢是轉小寫和轉大寫，upper 是轉大寫，lower 是轉小寫，那注意喔，轉大寫、轉小寫，它會將轉大寫和轉小寫之後的結果呢，再賦值給一個新的字符串， 啊，所以這一塊我們通過一個新的字符串來接收一下，那麼此時我們輸出出來，大家就可以看到轉大寫和轉小寫之後的效果，你看， 這是全部的字母都大寫，這全部的字母是不是都是小寫？ 好。 那麼再往下呢，就是這個 split，那 split 呢是切割字符串。 啊，我們可以按照指定的一個字符串將字符串切割為多個部分，比如大家來看， 那麼 s 這個字符串它裡面是不是有這麼三個橫槓啊？ 哎，那這裡呢我們就按照橫槓將字符串進行一個切割，那切割完了之後，它會將多個部分的字符串給我們封裝到一個列表當中。 來，我們往下走一步， 大家來看， 那多個字符串切割之後的多個字符串是不是封裝到了一個列表當中了？ 好， 那這就是字符串的切割啊，split 是切割字符串的。 然後緊接著再往下走呢，我們在這一塊呢來演示一下這個 strip，strip 是去除字符串兩端的空格， 啊，如果說你後面什麼參數都沒跟啊，就是 strip 這個括號裡面什麼參數都沒寫，那默認它是去除空格的。 啊，當然對於我們當前來說呢，呃，對於我們這個字符串來說，左右也沒有空格，啊，所以大家並沒有看到什麼效果，那這個呢，我們等會兒將字符串重新做一個改造之後再來測試一下。 好，那麼再往下呢，就是 replace，是替代字符串的，替換字符串。大家來看，這裡面傳遞了兩個參數， 第一個參數叫 old，第二個參數叫 new，就代表的是我要將哪個老的字符串替換為這個新的字符串， 你看我這一塊是兩個槓，但注意了一，個是橫槓，一個是下劃線，這就代表我要使用下劃線來將字符串當中的橫槓替除掉，那替換掉的這個結果呢，哎，我再賦值給一個新的字符串。 來，我在這一塊再次執行， 走，大家來看， 現在橫槓是不是就替換為了下劃線了？ 好。 呃，那麼最後呢，在這一塊再演示兩個方法，一個呢叫做 startswith，一個叫做 endswith， 那麼這兩個方法執行的結果都是一個布爾值，就是來判斷 那麼這個字符串是否以指定的這個子串結尾和開始，那 startswith 呢就是開始，endswith 就是結束。 對於當前 s 這個字符串來說呢，它是確實是以 hello 開始的， 所以呢，它的結果就是 True。 但是它並不是以 Python 結束的，所以呢，它的結果應該是 False。 哎，大家來看，False，沒問題。 好嘞， 那這就是這個 startswith 和 endswith，就是判斷這個字符串是否以指定的字符串開始和結束。 好，那麼這一塊演示完了之後啊，最後我們回過來再給大家演示一下這個 strip，呃，那 strip 呢，它是用來去除我們字符串兩端的空格的，來，我在上面這一塊呢，在字符串兩端我這一塊加一些空格。 好，那麼大家來看效果喔，呃，我就在這個 strip 這一塊打個斷點哈， 來，我右鍵這一塊 Debug 直接運行， 那麼運行之後，來，我們在這一塊往下走一步， 大家來看， 現在看我們的這個字符串左右兩側的空格，你看這，現在就沒有了， 而上面在這一塊輸出的時候你看， 左邊、右邊是不是都有空格呀？ 而調用了 strip 方法之後，那麼就將這個空格左右兩側的空格呢都去除掉了。 好，我們在這一塊放行。 那這樣呢，我們這一塊呢就演示完畢了。 那最後我還要再給大家強調一個點。 上面我進行了一系列的操作，什麼轉大寫、轉小寫，然後這一塊又切割、又去除空格，完事之後最下面這一塊，注意喔，我這一塊加上一串橫線，對吧？ 然後緊接著呢我再這一塊再輸出一個 s，我將原始的字符串輸出出來。 我要讓大家看一下，經過一系列的操作之後，原始的這個字符串的內容我讓大家去看一下發生變化了沒有。 來，看好，我右鍵直接運行看輸出的結果，來，我問大家， 原始的字符串經過一系列的操作之後，原始的字符串變了沒有？ 沒有， 因為字符串是不可變的。 好的，那這是我們這一小節給大家演示了一下字符串當中常用的這些方法。 那麼這些方法呢大家不用去死記硬背啊，那大家下來呢自己去練習一下，然後混個臉熟，那後面呢我們再去集中做案例的時候啊，那前面的這些基礎知識我們都還會再用到，那麼大家用得多了，自然而然也就熟悉了，而且呢即使說這些方法大家過了一段時間忘了也沒關係，因為這些方法都是固定的，那其實這些東西我們是完全可以讓 AI 來幫我們提示的，那麼在後面我們再去作案例的過程當中呢，我們也會藉助於 AI 來提高我們的開發效率。 好，那這是我們這一小節所講解的字符串的常用方法。 那接下來呢大家就可以打開自己的 PyCharm 練習一下字符串當中的常用方法，並熟悉一下它的作用。 黑馬程序員為千萬學生少走彎路，我們一直在努力。
