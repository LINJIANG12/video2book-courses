好的，那大概了解了什么是模块之后，接下来呢，我们就来讲解模块操作的语法。

那我们要想在项目当中能够使用Python官方提供的这些内置模块，或者使用别人已经开发好的模块功能，那我们必须在使用之前，先要导入该模块。

哎，所以这小节呢，我们就先来讲解导入模块的语法。

那在Python当中我们要导入模块呢，语法还是比较灵活的，因为它可以导入完整的一整个模块，那也可以导入这个模块当中我们所要用到的部分功能。而这里所提到的功能呢，可以是一个变量，可以是一个函数，也可以是一个类等等，都是可以的。那接下来呢，我们就来看一下具体的导入语法。

那么在导入模块的时候啊，那这一块的语法细分为这么五类，其实分为两大类。那么第一大类呢，就是前面这两种，就是导入整个模块的；第二类呢，就是下面这一块，是从模块当中导入指定的功能的。

好，我们先来看第一种语法哈，就是直接导入整个模块，也就意味着那整个模块我就可以直接导入进来。那它的语法是 import，后面跟上模块名。那这个 import 呢就是导入的意思，啊后面跟上对应的模块名。那如果说我们一次性想导入多个模块，那多个模块之间使用逗号分割。

哎比如这里呢，我们要想导入 random 以及 os 也就是操作系统的这个模块，那这一块直接写 import random, os 就可以了。那这样呢，我们就可以将整个模块直接导入进来。

那模块导入进来之后，接下来如果我们使用模块当中的功能，那它的调用方式呢就是：模块名.功能名。啊因为我们导入进来的是一个整个模块，那我们要调用的是这个模块当中的哪个功能呢？那么在点后面跟上具体的功能名。

好，然后紧接着再来看一下第二种语法。那么第二种语法呢，其实就是给这个模块名起了一个别名。你看，import 模块名 as 后面跟上一个别名。那为什么要起别名呢？哎因为我们在导入某一些模块名的时候啊，这个模块名可能会比较长，我们接下来再去编写代码的时候可能并不方便，这个时候呢，我们就可以在 as 后面给这个模块名起个别名。那比如大家来看，import random as rd，那是不是就相当于通过这个别名对这个模块名进行一个简化呀？那么后面我们在去调用这个模块当中的功能的时候，那就可以通过别名.功能名的方式来调用。你看，前面这一块写的是别名，点之后写的是调用的具体的功能名。

好，那这是导入整个模块。好，那么在导入整个模块之后啊，我们在调用的时候呢，就要使用模块名.功能名的形式来调用。然后紧接着下面这三种语法呢，就是来导入模块当中具体的功能。哎比如我只想用到这个模块当中提到的这个功能1和功能2，那我就可以只导入这两个功能就可以了。来看一下具体的语法。

那它的语法是这样子的：关键字是 from，那这个 from 翻译过来是不是就是从哪儿来呀？哎来自于哪儿，从哪儿来。那大家来看它的语法哈，from 模块名 import 后面跟上功能名。那这就代表啊我所导入的这个功能是从哪儿来的呀？哎我是从前面这个模块当中导入的这个功能。哎你比如我们这一块 from random import randint, choice，那这就代表我要从 random 这个模块当中要导入两个功能，一个功能呢就是 randint 这个功能，还有一个是 choice 这个功能。哎这就代表我要从 random 这个模块当中导入这两个功能。那如果说我们在 import 后面直接导入的是具体的功能的名字，那么我们在调用的时候，你就不用在前面去加上这个模块名了。啊直接通过功能名来调用就可以了。你比如我这一块从 random 当中导入了一个功能叫 randint，那这一块我们就直接去调用 randint 就可以了，直接写功能名就可以了。

然后紧接着再往下走呢，我们也可以对这个功能名起个别名。啊如果说这个功能名比较长、比较复杂、不便于操作，那这个时候我就可以在功能名后面加上一个 as 这个关键字，在 as 后面跟上别名。那么一旦为这个功能名起了一个别名，那以后我们在调用的时候啊，那我就直接基于别名的方式来调用。比如你看，我们为 randint 这个功能是不是起了一个别名叫 rint 啊？哎那以后你调用就使用别名 rint 来调用就可以了。

好，那这是导入具体的功能名。啊当然了，在 Python 当中我们在导入功能的时候，我们一次性的也可以将这整个模块当中的所有功能都导入进来。啊那如果说你要导入的是这个模块当中的所有功能，那么在这一块直接在 import 后面写个星就可以了。那这就代表啊，我要导入的是这个模块名当中的所有的功能。好，那么所有的功能导入进来之后，你在调用的时候直接使用功能名来调用就可以了。

而最终大家会看到啊，那么我们导入的方式不同，那最终我们在调用模块当中的这个功能的时候调用的方式也不同。那这里呢，大家只需要记住一个点哈：就是如果你导入的是整个模块，那你在调用里面具体功能的时候啊，那你需要通过模块名.功能名这种方式来调用；那如果说我们导入的是模块当中具体的功能，你看 import 后面跟上的是具体的功能，哎那么这个时候我们在调用的时候，就直接基于功能名的方式来调用就可以了。那么再次强调呦，这一块的功能可以是一个变量，可以是个函数，也可以是一个方法，当然也可以是一个类。啊当然这个类呢我们在后面面向对象的时候，我们会详细讲到。

好，那基本的语法我们介绍完毕之后，接下来呢我们就直接打开 PyCharm 来演示一下。

那么在 PyCharm 的这个第二章这个目录下哈，我再来新建一个 Python 文件，啊这一块直接新建一个 Python 文件。那这个 Python 文件呢我要将其放在一个目录下哈。呃那这个目录呢我就叫 module01 啊，module 就是模块的意思。然后在这个目录下我要来新建一个 Python 文件。那目录下要放一个文件，目录与目录之间的分隔符是斜杠，所以在 module01 后面加上一个斜杠，这一块再来一个 01_ 呃这个是演示导入模块哈，这一块写一下文件名字叫导入模块。后缀可以不用加，它会自动加上 .py 的后缀。而此时大家来看，我们在创建文件的时候，我在斜杠前面写的这个其实就是目录的名字，那最终我们一敲回车之后，它会自动创建这么一个目录，然后在目录下给我们生成一个新的文件。

好，这个时候我直接回车。大家来看，目录是不是给我们创建好了？哎目录下的文件也创建好了。那接下来呢，在这个 Python 文件当中我就来演示一下导入模块的语法。

那我们刚才讲到，导入模块呢分为两类，一类呢就是直接导入整个模块哈，我这一块写一下，导入整个模块。那另外一种呢叫做导入模块当中的功能，导入模块中的功能。

好，那我们这一块先来演示一下第一种，导入模块。那导入模块呢，我们就以这个 random 这个模块为例啊，生成随机数的这个模块为例来演示一下。呃那这个模块名字呢叫 random，那导入对应的这个关键字呢叫 import，你直接敲一个 im 就可以了，这一块是不是一个 import 啊？哎那你双击一下，双击之后呢后面这一块是不是要指定我们要导入的是哪个模块呀？哎是随机数的那个模块，这个模块名叫 random。

那这个是 Python 标准库当中所提供的这个模块，你可以理解为就是 Python 官方的内置模块，直接导入进来就能使用。那这一块我们直接双击一下这个就可以了，就这个 random。那么导入进来之后大家会发现，这句代码怎么没有生效呢？怎么是灰色呢？哎原因呢是因为这个模块导入进来之后，我们并没有使用，所以显示为灰色。

好，那接下来呢我们在下面这一块就来使用一下 random 这个模块。那这一块呢，我可以使用 random 来给我们生成一组随机数。呃那这一块呢我就进行一个循环哈，我循环个100次，然后生成随机数。那注意哦，循环100次我们要基于 random 来生成随机数，那这一块就要调用 random 这个模块当中的功能了。啊那它里面的这个方法呢比较多，我们这一块就调这个哈，叫 randint。啊就是生成一个 int 类型的随机数。然后在里面要指定这个 start 和 end。啊那这一块你看有个 a，有个 b，这个 a 代表的就是 start，那 b 呢代表的就是 end。我们生成的随机数介于多少到多少之间呀？哎介于1到100之间。啊在循环当中输出100个随机数。啊那注意哦，这一块的这个随机数呢，它是既包含1，也包含100的。也就说在生成的结果当中啊，呃那么是包含1，也包含100的。

好，那接下来呢我们就直接右键运行。运行之后大家来看，那么这一块就给我们生成了100个随机数。你看，是不是包含1了？哎当然它也是包含100的。好，那这就是我们导入整个 random 这个模块。

那如果说导入整个模块的话，那我这一块给大家写一下哦，调用的方式呢就是通过模块名.功能名的方式来调用。你看 random 是不是就模块名啊？哎这个 randint 其实就是其中的一个功能，啊当然它本质就是一个方法，就是一个方法名。

好，那么这是我们第一种导入方式。那紧接着我们在这一块再来演示一下第二种导入方式哈。那么第二种导入方式呢，我们在导入的时候，如果说这个模块名比较复杂，我们可以在后面加上一个 as 为这个模块名起个别名，比如我给它起个别名叫 rd。那这就代表啊，我导入进来的是 random 这个模块，但是导入进来之后 random 这个模块的名字就变了，变成什么了？变成 rd 了。这个 rd 就是我们给 random 这个模块起的一个别名。一旦起了别名之后，你再去调用这个模块当中的功能的话，你就得通过别名的方式来调用了。啊所以这一块的调用方式啊，要么是模块名.功能名，要么是别名.功能名。好，那这一块我们就换成 rd，这就就可以了。好，那么接下来呢，我们右键再次运行，效果呢是一样的。好勒，那这是我们这一块所演示的第一种导入方式，导入的是整个模块。

好，那么接下来呢再来演示一下第二种方式，我们可以只导入模块当中的特定功能。来看一下具体该怎么做哈。那如果说我们要导入的只是其中的某一个功能哈，那这个时候我把上面这一块导入的代码就先给它注释掉哈，呃那这段代码呢我也先注释掉。然后接下来呢，我们在这一块来执行导入操作。那这个时候如果说你导入的是模块当中特定的功能的话，它的语法是这样子的：from 后面在这一块跟上模块名，import 后面在这一块跟上功能名。来看一下怎么写哦，from random，random 是不是就模块名啊？然后在 import 后面跟上功能名。那我们只想用到 random 这个模块当中的 randint 这个功能，那这一块直接写 randint 就可以了。

好，那么一旦这么导入之后啊，那接下来我们在调用的时候，那调用方式呢又发生变化了，我们直接通过功能名就可以调用了。当然如果说你为这个功能名起了别名，那就通过别名的方式来调用。好，大家来看，这群是不是就这个功能名啊，randint。好，这个时候在调用的时候，你就不用在前面加上模块名了，直接 randint，后面在这一块写 1, 100，这就可以了。那这就代表我要调用的是 random 这个模块当中的 randint 这个功能。

呃那这一块呢我在输出的时候我也是在循环当中输出哈，我把这段代码直接复制下来。前面这一块呢，这个模块名就不用了，直接通过功能名的方式来调用。来，右键，再次运行，效果是一样的。

好，那我们在导入这个模块当中的功能的时候啊，那如果说这个功能的名字比较长、比较复杂，啊那我们也是可以为它起别名的。啊那我们来看一下起别名的方式哈。那起别名的方式和我们刚才上面这一块起别名的方式类似哈，直接在功能名后面加上 as，比如我这一块来一个叫做 rint。那这就代表这个功能我已经给它起了一个别名叫 rint 了。那么你在调用的时候你就得使用别名来调用了，就不能再使用原始的功能名来调用了。哎所以这一块也得换成 rint 就可以了。好，来，右键我们在这一块再次运行，效果是一样的，没问题。

好，那么在这一块导入的时候啊，我们刚才在这一块导入的是某一个具体的功能。啊那其实呢我们也可以将 random 当中所有的功能一次性全部导入进来。呃那这个时候如果说想导入这个模块当中的所有功能，那此时呢，你在 import 后面直接写一个通配符就可以了。啊这个星，我们叫通配符，就代表的是这个模块当中的所有功能。那么此时在这一块在调用的时候该怎么调用呢？哎那这个时候你就要使用 random 这个模块当中具体的功能来调用了。比如你看 random 当中是不是有 randint 啊，哎那这一块你就需要通过 randint 这个功能名的方式来调用了。好嘞，那接下来呢我们再次右键，再次运行，效果是一样的。好勒。

那么这就是我们在这一块所演示的导入模块的两类语法。那第一类语法呢，就是导入模块，将整个模块导入进来。那导入整个模块的话直接在 import 后面跟上具体的模块名就可以了，而对于这个模块名我们是可以起别名的。然后第二种方式呢，就是我要导入指定模块当中的功能，哎只导入其中的功能，那具体的语法就是 from 后面跟上模块名，在 import 后面跟上具体的功能名就可以。

而最后呢在这一块需要提示一下大家哈。呃那么其实对于我们 import 导入模块这样的语句啊，那么在我们的 Python 文件当中，我们一般把导入模块这一块这个语句一般会放在整个 Python 文件的最上面。哎我们具体的这些可执行的代码我们会放在 import 导入语句的下面。哎也就是导入语句会放在整个 Python 文件的最上方的这个位置。那这样呢，我们打开这个 Python 文件一看我就知道那这个 Python 文件当中到底用到了哪些模块。

好的，那这是我们这小节所讲解的导入模块的基本语法。那讲解完毕之后，最后呢我们就回到 PPT 简单来做一个小结，主要明确两个问题。第一个问题：啊什么是模块？有什么作用？哎那这个我们刚才讲到哈，模块其实就是一个 Python 文件，啊就是一个以 .py 结尾的一个文件。那里面呢可以包含像变量、函数、类以及其他可执行的代码。啊其实我们之前所定义的每一个以 .py 结尾的 Python 文件都可以看作一个模块。那模块存在的意义呢，就是为了提高代码的复用性，降低我们程序开发的门槛。别人开发好了一个模块我想要用，直接导入这个模块就可以使用了。

然后紧接着我们刚才在这一块演示了一下导入模块的常见语法。那么常见语法呢分为两大类。第一类呢，就是直接 import 后面跟上具体的模块名，直接导入模块。我们可以通过 as 这个关键字为模块起别名，当然起别名的这部分也就是中括号当中的这部分呢是可选的，可有可无。那这是导入模块的第一类语法。那第二类语法呢，我们可以导入指定模块当中的特定功能。那它的语法呢就是 from 模块名 import 后面跟上具体的功能名。那如果我们一次性想导入多个功能，多个功能之间逗号分割。那我们也可以为具体的功能名起个别名。那如果说我们要导入的是这个模块当中的所有功能，那么这一块也支持通配的写法。import 后面这一块可以写一个星，星代表的就是这个模块当中的所有的功能。

好，那这是我们导入模块这一块的基本语法。而这里面呢还涉及到一些注意事项，就是关于 import 导入模块的代码呢，我们一般会写在 Python 文件的开头。这么做有两个好处：那一个好处呢就是我们打开 Python 文件一看就能够知道这个 Python 文件当中到底导入了哪些模块；然后第二个点呢，就是我们模块的操作一定是先导入，再使用。那么你导入的代码写在最上面，接下来你是不是就可以直接使用了？

好的，那这是我们这小节所讲解的导入模块的语法。

好，那接下来呢大家就可以暂停视频，打开自己的 PyCharm 练习一下这一块的基本语法。

黑马程序员，为千万学生少走弯路，我们一直在努力！
