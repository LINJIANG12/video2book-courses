好的，那我们知道了什么是类型注解以及类型注解的语法之后，接下来这小节呢，我们再来讲解函数的类型注解。

那所谓函数的类型注解呢，指的就是我们可以在定义函数的时候为函数的参数以及函数的返回值来添加类型注解。

那我们来看一下是怎么添加的。

那我们要为函数的参数来添加类型注解呢，我们就可以在定义这个函数的时候，在形参名称的后面，在这一块来指定这个参数对应的类型。那写法呢，和我们刚才讲解变量的类型注解是一样的，直接在形参名称后面加上冒号，后面指定类型就可以了。比如这个 scores 呢它是 list 列表类型，这一块直接冒号 list。那中括号里面写的就是这个列表当中每一个元素的类型。那这就是为函数的参数来指定类型。

那如果我们来指定这个函数的返回值的类型呢，在这个参数列表后面这一块加上这么一个箭头啊，就是通过这个横杠加上大于号组成的一个箭头，然后在后面这一块指定返回值是什么类型的。那你比如下面的这个，返回了多个值，多个值是不是会封装到一个元组当中啊？哎，所以在这一块写了一个 tuple，然后后面方括号里面写的就是元组当中的三个元素每一个元素分别是什么类型。你看，第一个元素 int 类型，第二个元素 int 类型，第三个呢是 float 类型，直接这么写就可以了。

好，那这就是关于返回值类型的指定。

好，那这就是关于我们在定义函数的时候如何来指定参数及返回值的类型。

那接下来呢，我们就直接打开 PyCharm 来演示一下，大家来看。那么在 PyCharm 当中啊，我已经准备好了这样一个函数。那这个函数呢就是根据圆的半径来计算圆的面积以及圆的周长，最终是不是返回了两项数据啊？哎，然后下面这一块调用了这个函数，传递的半径是 10，来，我们跑一把看一下效果啊。

好，那么这一块就计算出来了这个圆的面积和圆的周长。那最终这两项数据是封装到一个元组当中了。那这是我们之前定义的函数，我们并没有指定这个参数的类型，也没有指定返回值的类型。那我们提到 Python 当中是有类型推断机制的，那通过 Python 的类型推断，它能不能推断出这个 r 是什么类型呢？

哎，那大家想看到到底有没有推断出来，可以把你的鼠标放在函数调用这个位置，放在这个上面，然后紧接着你来看一下有没有推断出来啊？r 冒号这一块写了个 Any，代表的是任意类型。有没有推断出来？没有。因为啊我们在定义函数的时候，你这块就写了一个 r。我们前面讲到 Python 的类型推断它是根据什么去推断类型的？是不是根据我们在定义变量或者是在进行一些数学运算的时候，是根据它赋的这个值来推断类型的？这个值是 596，哎，一看是 int 类型，那么这个 a 呢，它就推断出来它的类型就是 int。一看到这一块的这个 flag 值是 True，那就推断出来它的类型呢就是 bool 类型。它是根据赋的值推断出来的。而在我们函数定义的时候，你这一块赋值了吗？没有，所以呢就推断不出来了。那如果说你在这一块给它赋了一个值，那这个时候你鼠标往这一放，大家再来看，推断出来没有？推断出来了，你看，是 int 类型。那这是不是就相当于是给这个参数设置了一个默认值啊？如果你设置了默认值的话，那这个参数就可以推断出来了，你看，是 int 类型。

好，那后面这一块的返回值呢，它也是可以推断出来的，你看这儿，tuple，里面两个元素，每一个元素都是 float 类型，因为这一块会涉及到这个浮点数的运算嘛，所以推断出来就是 float 类型，是没问题的。

所以呢，在函数调用过程当中啊，这个类型推断依然是生效的，但是前提呢，你要为这个参数有默认值赋值了，它才会推断出来。如果没有默认值，它就推断不出来了。那这个时候呢，我们就可以手动的给它去加上类型注解。冒号，我这一块指定一下。呃，那么这个半径呢，那可能是 float 类型哈，也可能是 int 类型，我这一块直接写个 float 就可以了。那写一个 float 之后，你这一块传递 int 也是没问题的，因为 float 它是能够兼容 int 类型的，因为它是可以转为 float 的。当然呢，如果说你在这一块写一个浮点数，那当然没问题了。但是如果说你在这一块写一个字符串，马上就会提示出来，这一块应该要给我传递的是 float，而不是 str 字符串。

那么此时大家想，一旦我在定义函数的时候我指定了这个参数的类型，你在调用的时候如果传递的类型不对，我是不是马上就会提示出来了？哎，那这个时候呢就可以减少我们代码出错的概率。一旦提示出来了，噢，那我就是知道这一块要的是一个 float，我在这块再去换一下就可以了。

好，那么这是指定参数的类型。那我们再指定一下返回值的类型啊，当然这个返回值类型大家也能够看到，它其实是可以推断出来的。好，你可以加，也可以不加。那我这一块呢既然讲解语法，我这一块就加上啊，在后面这一块加上一个箭头，然后它是一个元组，这一块直接来一个 tuple，然后里面写上这两个元素各自的类型。那他们各自的类型都是一个 float，这一块直接写 float, float。好，这样就可以了。那这样呢，我们就为这个函数的参数以及函数的返回值指定了类型注解。

好，那这就是函数添加类型注解的方式。

那最后呢，我们再来解决我们之前所定义的这个订单金额计算器的这个函数，来解决一下这一块的这个问题，大家来看。那这里我们传递的这个价格，它是一个字符串，那最终运行的时候报错了，但是我们写代码的过程当中，是不是并没有任何的提示啊？哎，那接下来我们就来解决这个问题，那通过类型注解就可以解决这个问题。

来，我将这个函数 Ctrl C 复制一下，将这个文件关掉，直接将这个函数复制过来。那么上面的这些注释的测试代码我直接将其删掉。接下来，我就要为这个函数来添加类型注解，大家来看。

那么这个函数当中呢，一共有这么四个参数，那么其实后面的这几个参数啊，这三个参数我不用给它添加类型注解都可以。为什么？因为它是可以通过这一块我们所传如进来的这个默认值参数是不是就可以推断出这个类型的？哎，那这个时候大家来看，我鼠标放在函数调用上面，鼠标往这一放，大家会看到，那这几个参数的类型是不是都推断出来了？哎，只有谁推断不出来？只有这个 args 推断不出来，是 Any。因为 args 你有没有为它设置默认值啊？没有。那这个时候呢，我们就可以为 args 来加上一个类型注解，大家来看。

呃，那这个 args 呢，它接收的是不定长参数，是基于位置传递的不定长参数，那它呢其实就是一个元组类型啊。你看我鼠标往这个上面一放，提示这就是个元组。那么接下来呢，我们在后面在这一块写上一个冒号。那这是一个元组，这一块怎么写呢？那这一块提示一下哈。呃，如果说这一块是一个不定长参数，那你再冒号后面写的就是这个元组当中每一个元素的类型就可以了，因为它本身就是一个元组。那在后面你要写的就是里面的每一个元素的类型。那里面的每一个元素呢都是一个 tuple 元组类型。大家来看，这不就是里面的每一个元素的类型啊，又是一个 tuple。那这个 tuple 当中，那里面三个元素分别是：第一个是字符串；第二个价格，那它就是一个 float，是一个浮点数；第三个是一个数量，那就是 int。那我们这一块直接写：str，第二个是 float，第三个是 int。好，这就可以了。

那注意哦，我强调一下，那么由于这一块写的是 *args，它会收集对应的位置参数，然后封装到这个大的元组当中。那后面要写的就是里面的每一个元素的类型，那每一个元素又是一个元组。

好，那这个时候大家来看，我鼠标往下面的这个 args 上面一放，大家来看，这个 args 这个参数，它什么类型啊？整个是一个元组，在元组当中它每一个元素又是一个元组，而内层的这个元组当中呢，里面有三个元素，分别是 str、float 和 int。好。

那么接下来再往下走，大家来看。那么一旦我们在上面这一块指定了这个 args 里面元素的类型之后，下面这一块是不是马上就提示有错误了？哎，这一块告诉我们啊，那第二个参数应该是什么呀？应该是 float，结果你给我传递的是什么？传递的是 str。那这个时候我们在写代码的时候，这一块是不是就出错啦？哎，那我们就能够知道，那接下来第二个参数我要传递的就是一个浮点数。好，这就 OK 了。当然我们提到啊，这个 float 是可以兼容这个 int 的，所以后面这一块是没问题的。那最后呢，我们在这一块运行，大家会发现也是可以正常运行的。

好，那这样呢我们就为这个 args 加上了这个类型注解。那后面的这几个由于它有默认值啊，这个类型注解你加或者不加都可以。呃，那我们在这一块演示这个语法，我还是把它加上哈。呃，那优惠券呢它都是整数的优惠券，我这一块就直接写 int 了。哈，那积分呢也都是整数的，我们这一块也直接写啊 int。那么对于这个邮费的话，那它是一个浮点数，我们这一块来一个 float。后面如果你要写完整了，那我们在这一块也可以指定一下这个函数的返回值。那返回值大家来看，呃，这一块给我们提示出来的是一个 tuple，那但实际并不是一个 tuple 啊，实际是一个什么呢？实际是一个 float 啊，在这一块直接写 float，这就就可以了。

那这样呢，我们就为这个函数加上了对应的类型注解，以后你在调用这个函数的时候，你就不怕写错了，写错它就会有对应的语法提示了。你比如这一块写个 100，这一块写个 99，后面写个 2，你会发现是不是马上就会有语法提示了？哎，告诉你第一位应该是什么？应该是 str，啊，应该是一个字符串。这不就没问题了？

好，这也即是类型注解的作用。

好，那这是我们这一块所演示的这个函数的类型注解。

那最后呢，我们再回到 PPT，简单做一个小结。

呃，主要明确一下函数当中类型注解的语法。那么其实呢，就是为函数的参数以及函数的返回值来指定类型注解。那如果是为函数的参数加类型注解，我们就直接在这个形参名称后面加上冒号，后面指定类型就可以了。那如果是为函数的返回值来指定类型，那么需要在形参列表的后面这一块加上一个箭头，后面再指定返回值的类型。

那最后呢再强调一点，就是关于函数的类型注解可以加也可以不加。你不加也不会影响函数的正常运行，只不过呢就是有一些错误可能到运行的时候我们才能够发现，代码的可维护性不高。那如果你加了类型注解，那我们在编写代码的时候有一些错误可能马上就会暴露出来，哈，从而降低我们代码出错的概率。

所以呢，对于我们在实际项目开发当中啊，对于一些呃需要团队协作，并且长期维护的项目，啊，为了便于我们项目的后期维护，推荐使用类型注解。但是如果说基于 Python 的解释器它可以推断出这个变量的类型、这个参数的类型，那这个呢大家加不加都是可以的。那如果推断不出来的情况下，就比如这个 scores，你不加由于它没有赋值，它肯定是推断不出来的，那这个时候呢我们建议大家可以加上类型注解，让我们的代码呢更便于后期的维护。

好的，那这是我们这小节所讲解的关于 Python 当中类型注解的语法。

那么到此呢，关于函数这一章节的内容呢，我们就全部讲解完毕了。那接下来呢，大家可以打开自己的 PyCharm 完成这一块的练习。练习完毕之后，再开启下一章节的学习。我在下一章节等你哦。

黑马程序员为千万学生少走弯路，我们一直在努力。
