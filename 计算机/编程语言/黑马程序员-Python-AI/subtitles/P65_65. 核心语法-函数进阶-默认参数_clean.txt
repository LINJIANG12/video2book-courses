好的，那我们清楚了函数的传参方式之后，接下来这一小节呢，我们再来讲解一下函数的默认参数。那什么是默认参数呢？哎，我们一起来看。那默认参数呢，也叫缺省参数，指的是我们在定义函数的时候啊，我们是可以为函数当中的参数来提供默认值的。那么在函数调用的时候呢，如果说这个参数有默认值，那这个参数我是可以不传递的。而这个默认参数呢，也叫缺省参数。那这个缺省呢其实就是字面意思，缺呢就是缺少，省呢就是省略，啊代表的就是这个参数呢是可以缺少或者是省略的，啊当然前提呢是你给它设置了默认值。我们来看一下怎么样为参数设置啊默认值。比如大家来看，我在定义这个函数的时候呢，如果说我们要为这个参数设置一个默认值，它的语法是这样子的。呃在定义这个形参列表的时候呢，我们就可以使用这个等号为这个形参设置一个默认值，比如你看，city=北京，这就相当于我们为 city 这个参数设置了一个默认值为北京。那么一旦设置了默认值呢，我们在调用这个函数的时候啊，那这个参数呢我们就可以不指定了。你比如，你看，下面我们在调用这个函数的时候，我们是不是就指定了三个参数啊？哎，第一个是 name，第二个是 age，第三个是 gender，city 这个参数我没指定，因为它有默认值。我们没指定，它就会使用默认值。那如果说我们在调用这个函数的时候啊，我指定了这个参数，哎比如我指定了四个参数，最后一个参数是深圳，你看这四个是不是都是位置参数啊？哎，那最后一个参数就会传递给这个 city。那么一旦我们指定了这个参数，那么就会使用我们指定的这个参数值，默认值呢就不再生效了。好，那这就是我们这块所设置的这个默认参数的含义。啊那通过这个参数名等于参数值的形式就可以来设置啊默认参数。而这里呢也涉及到两个注意事项啊，强调一下。首先第一个呢，就是呃我们设置的这个默认参数啊，它需要放在我们正常参数之后，哎也就是放在我们没有设置默认值的这些参数列表的后面。而且呢，在一个函数当中，我们是可以为多个参数来设置默认值的，你只需要将多个默认值都放在我们没有设置默认值的这个参数列表的后面就可以了，形式呢就是参数名等于参数值。那这是第一个注意事项。那第二个注意事项呢，就是在函数调用的时候啊，那如果说我们为这个默认参数我们传递了值，那么它就会去修改这个默认值，就会以我们传递的这个值为准。那如果说我们没有传递这个参数值，那么此时呢默认参数就生效了。好，那这是我们这一块所提到的默认参数。那接下来呢，还是一样，我们直接打开 PyCharm 来演示一下。那么在 PyCharm 当中啊，我这块也定义好了这么一个函数啊，就是我们刚才使用的这个函数。那么在这个函数当中呢，有这么四个参数。那如果说我们想为某一个参数设置一个默认值，哎那么此时呢我们就可以在定义这个函数的时候啊，在这块来指定，啊指定参数名等于参数值。那接下来呢，我们就来为这个函数当中的参数来指定一下默认值啊。比如现在我想为两个参数指定默认值，一个是 gender 一个是 city。哎那这个时候呢我们就来 gender 等于，啊比如性别呢默认就是男，啊我这块写，gender 等于男，然后 city 呢默认就是北京，那我这块就来一个北京。但是大家会发现，我刚才为 gender 设置了一个默认值之后，city 是不是马上报错了呀？哎这就是我们刚才所说的，那么默认参数必须要跟随在非默认参数之后。哎也就是说是吧，你看这个参数是不是没有默认值啊？哎那么它应该放在前面，默认参数应该放在后面。好，那现在我再为它加上一个默认值就行了，这块我写一个北京。好，那你看，现在这两个参数是不是都有默认值了？哎那么接下来我们在调用这个函数的时候呢，那其实这两个参数我们就可以不用指定了。啊比如我这块写一下，调用函数。呃来我在这一块直接调用 add_stu 这个函数，那在这块指定一个王林啊，继续来吧，啊王麻子是吧？好，在这块来一个 20。好，后面我就不用再指定参数了。那这个时候大家会发现好像也没有报错，原因是什么呢？原因是因为我后面两个参数是不是都有默认值啊？你没指定，它就会用默认值。那如果说你这块的这个 gender 没有默认值，那么此时鼠标往这一放，它就告诉你啊，你这个形参 gender 还没填呢。啊因为 gender 没有默认值，对吧？那有了默认值呢，我们就可以不用指定了。好，那么下面这块我们输出一下这个 stu，看一下效果。来，右键直接执行，执行之后大家来看。你看，王林这个学生，那姓名和年龄有值没问题，那 gender 和 city 是不是也有值啊？而这两个值呢，其实就是这块指定的默认值。好，那么紧接着呢，我们再来调用一次啊。来，我将上面这块代码呢复制下来，复制下来之后姓名我这块换一下，换成李木婉。好，李木婉呢这块写一下，18 岁。好，那李木婉呢性别是女，那此时呢，我就不要使用默认值了。如果你不想使用默认值，你就为它传参就行了。啊你使用关键字传参，使用位置参数都可以，那我这块就使用位置参数写个女。好，接下来我右键再次执行，大家来看，那李木婉 18，gender 为多少？女。那我们是不是传入了这个参数值之后，就会将这个默认值给它覆盖掉了？而对于 city 来说，我没有传递，那使用的就是默认值北京。好，那么紧接着我们继续来看。呃那么接下来呢，我在这块再来。呃这块换一下哈，那我这块就换成韩立。啊那换成韩立之后，这块换成 22。那性别呢，我们不想传递，我想使用默认值，默认是不是就男啊？好，你这么写可以。但是这个时候我还想传递 city，那 city 他这个韩立呢是上海的，我这块直接写个上海，我问大家，我这么写对还是不对呢？哎我可以直接运行让大家看一下效果啊。大家来看。现在我在这块传递的是韩立的 city 上海，但是大家会发现，它把上海作为什么了？作为 gender 性别了。为什么呢？因为你这块用的是位置参数。那位置参数的话，第一个参数就会传递给 name，第二个参数传递给 age，第三个参数是不是就传递给了 gender 了？而现在你还想使用默认值，这个 gender 的默认值，那这个时候我怎么把 city 传递过去呢？这个时候你就不要再用位置参数了，位置参数是一个一个传递，对吧？要一一对应。那这个时候呢，我们就可以使用关键字参数来传递就可以了。写个 city=上海，那这样就可以了。好，那接下来呢，我们右键再次执行，执行之后大家来看，你看，gender 是不是男啊？哎使用的是默认值。然后我们指定了 city 为上海，就会将默认值这个北京给替换掉。好勒，那这就是我们这小节所讲解的关于函数的默认参数。啊那这个默认参数呢，大家需要知道是可以设置多个的，但是默认参数需要放在非默认参数的后面。好，那这是我们这小节所讲解的函数当中的默认参数。那这个语法呢相对比较简单，那接下来呢大家就可以打开自己的 PyCharm 来练习一下。黑马程序员，为千万学生少走弯路，我们一直在努力！
