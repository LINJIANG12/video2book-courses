好，到目前为止，关于我们的 Transformer 整个的组件部分，我们其实就已经完全搭建完毕了。那比如说像输入部分，编码器、解码器、输出部分，我们已经全部构建完毕。那接下来我们要做的具体就是把这四大块融和到一起，然后我们来搭建一个 Transformer 这个模型出来。其实也就是编码解码模型。

好，我们先来看，首先模型构建的内部函数，其实就是 encoder-decoder。这个 encoder-decoder 你可以把它理解为就是咱自己的 Transformer。然后，能够输出模型构建内部函数的测试，make_model。那也就是说，接下来就是摘果子的时候。刚才是不是把 Transformer 架构组件、子层、编码器层、解码器层、编码器、解码器，我们都已经被实现了。那现在是往一块最终的组装的一个过程。

好，我们先来看。已经完成了所有组成部分，接下来直接把这个整体给它拼装一下就可以了。

好，那么现在呢，第一个就是 encoder-decoder。往这里边塞，你看，这是你的什么器？编码器、解码器，source 的什么什么，什么什么怎么样，这个就不多说了。封装有没有发现也很简单？但这个测试是不是稍微写的有一丢丢想多？因为你的参数是不是有点多呀。来，前首句啊，先开始切到 PyCharm 里面直接做。告诉我该第几个了？demo 几？

demo 09。好了，09 代码就模拟出来了。那 Transformer，t-r-a-n-s-f-o-r-m-e-r Transformer。好，那接下来，来吧。啊那案、摘桃子的时候来了。这个叫演示 Transformer，然后整体、整体模型架构。好，开始啊，开始啊。首先导包。哎我觉得你会。把谁导进来？

demo 几？把包一导，那前几个是不是全都能玩？好，全都能玩。来来来来来，todo 1，先写。第一个操作叫干嘛？定义整体的 Transformer 模型架构。然后初始化，叫 trans- Transformer 模型的主要组件。来了啊，class 你可以叫 My t-r-a-n-s-f-o-r-m-e-r My Transformer。这啥意思？是不是就是我的 Transformer 啊？但是实际开发中，如果老大真的让你写代码模拟，你上来就整一个 My Transformer，你看就 low 爆了。你绝对没有把它理解到位。

其实这个，You Transformer，你的 Transformer 是吧？啥脑回路呀？啊不不不。这个 Transformer 大家要知道，咱之前做过一个全球人名分类那个案例，它其实包括后面做的那个机器翻译。那机器翻译，英译法这个案例，它是不是就是一个 sequence to sequence 那个架构？所以 sequence to sequence 还记不记得，它的核心是三部分：第一个是编码；第二个是解码；第三个是中间语义张量 C，是不是中间那个东西啊？所以你的 Transformer 其实叫 Transformer，但这个哥们的底层其实也是一个编码解码模型。跟上思路了吗？叫编码器解码器模型。所以说一般啊不会写 My Transformer，一般我们会去写，对，叫 e-n-c-o-d-e-r encoder d-e-c-o-d-e-r encoder-decoder。所以你写了个 encoder-decoder，其实就知道，哦，你写的是编码解码模型，那就是 Transformer 这一堆。是这意思啊。

好了，那接下第一，初始化函数。来了啊，def __init__。__init__ 里面写谁呢？self 里面卧槽那一堆都得写啊。source_embed。原的 embedding。来吧，看这个图写啊，你看你要啥。

看这个图写吧。第一个，原的 embedding，是不是源头的词嵌入和位置编码呀？第二个要不要编码器？要不要编码器？要 encoder。来，要不要解码器？当然要，但是别着急啊。那你看你这里有个词嵌入，你这是不是还有个词嵌入啊？这个是谁？这个是目标的，叫 t-r-g-t target _embed。词嵌入。那是把这个词嵌入也存起来了。好，那接下来再往下边，就是你的什么？decoder。再来最后一个叫 g-e-n-e-r-a-t-o-r generator。也就是说我在这里边给你换颜色啊，换绿色的，其实我只要这几个。

第一个就是，哎 inputs 的词嵌入，就是 source，就是它。第二个我要输出的词嵌入，就是这个，叫 target embed，就这。然后呢接下来我还需要一个 encoder，encoder 是编码，decoder 是解码。你看，这不就完事了吗？那最后就是上面那一对，上面那一对就是那个 generator 生成的，那就是你的 linear 即可。看懂了不？这样的话整体公式我们就齐了啊。来吧，那接下来，说，怎么写？

初始化 Transformer 框架的一个基本参数。来 source_embed 啥意思？编码器的，说，输入嵌入层。包含，注意，编码器的输入啊，是这个位置。这个位置，就这，这个位置。能听懂不？包含啥？说。

说，encoder 啥意思？你看，那这样的话，哎我有一个在这儿，然后我有编码器，那你的结果是不是瞬间就标到了，是不是就可以标到右边这个位置了？对。听懂了吗？好，那那继续往下干呗。在这个 target 的 embed 是啥？包括，这啥意思？哪个？是不是就是到这个蓝点了？到蓝点以后经过它的处理，唰一下是不是就跑到这里了？好，那接下来这个是什么？什么模块？解码器模块。好，解码器模块就是到这儿处理处理处理，到解码器处理。处理处理处理之后，到这，到这，到这，这个操作是到最上面那个位置，要注、要注意。好了，那到这儿是不是就变成解码器的输出是不是就到这里了？好，那接下来再来最后，generator 是啥意思？输出层。你可以把 generator 理解为就是这里的 linear。它就是最后一步了，因为要把你的最后的一个蓝色的变成一个红色的，是不是变成我要的就行了？好，来。那接下来它是输出层，接着说，干啥的？将解码器的输出转换为词汇表的概率分布。

来吧，写吧，怎么写？第一个，初始化父类成员。叫 super()。

又来。

第二个动作，说。第二个动作干嘛？是不是叫定义参数？记录上述的一个变量。来，怎么写？source_embed，encoder。跟上来了吗？好，那下个呢？前向传播。

source_x，source_y。不是 source_x，target_y 吧。那再来，source_mask，还有一个叫 target_写完后呢，这参数够不够啊？然后，说。编码解码器前向传播函数，不是编码，是编码解码器，它是个整体。好，Transformer 干嘛？接着说。

来，source_x 是什么？编码器的输入，是啥？2 4 4 512。跟上啊。target_y 是啥？嗯？解码器的输入。比如跟上面是一样的。所以说这就意味着你在这里边输的是，你看啊，你这里边输的是一个 2 4，你在右边输的它也是一个多少？能跟上来吗？好，来，下一个。source_mask 是什么？它是 padding mask 机制。这是刚才题上没有出现的，就是这个你要去记记了。你看啊，下边这个是注意力，上面这个是不是也是注意力啊？那大家都要用掩码，那你用什么掩码呢？比如说 source 它用的是 padding mask。好，padding mask 是在哪儿演示呢？咱们接着说。再往下走，padding mask，来，那 padding mask 机制，这个机制做的是这样一件事：防止填充填充填充的它的值然后呢，填充就是这个 padding 啊，padding 是填充的意思。叫填充的 pad 值，然后干嘛呢？影响注意力计算。好，影响注意力计算。不是人，是影响注意力计算。那例如，例如2 4 4。好，那它用的是 padding。好，那接下来告诉我，下面还有个操作叫，哎 target_mask，target_mask 用的是什么？它叫 sentence，叫句子。来，说，这哥们啥意思？防止防止未来的信息要干嘛？提前被使用。好，所以说这哥们是做掩码的，但是上面那个哥们不是。是保持跟左边同名了。好，这个怎么做？来，接下来我们继续，我们接着说。

来，该干嘛了？注释写完是不是该写代码了？代码怎么写？return 也说一下吧，return 是什么？刚看过了。模型啊，概率分布。好，模型预测，模型预测的词汇表的概率分布。好，即多少？2 4 1000。2 4 1000。好了，来吧。那我们开始看啊，怎么说？这代码怎么写？先得到编码器的输出结果。编码器输出结果是不是就是它？先拿到它呀。来，怎么说？self.encoder。encoder 是谁？source_x。好，source_x，然后 source_mask。好来，第二个动作，说。在再，说。decoder 这一堆，把 decoder 弄进去，target_y，然后后边扔放你的什么输出，然后扔放你的 source，对吧，target 就行。来，继续。将怎么说？将解码器的结果进行输出层处理。什么意思？output 等于 generator，把你看刚才那个 decoder 那个制造的整进来是不是就行了？来吧，接下来最后第四个就是干嘛？返回结果。哎呦我的天，终于写完了。return output。

好，但是你别忘了一件事，还差一点。你现在是不是只是把初始化和前向链接是不是、前向传播是不是写了？还少一个呢。编码解码动作你是不是还没写呢？所以还有一个操作叫得到得到，得到什么？编码器的输出结果。编码器的前向、前向传播。好，来吧，怎么说？啊？你这里边是不是在这里边算，算谁？你这里边是不是就换了一下编码器，还有一个解码器操作啊？哎这块肯定下面还得写啊。那来吧，接着往下写。得到编码器的输出结果，然后呢？也就是编码器的前向传播。来，第四个，还得得到什么？解码器的输出结果，然后呢？解码器的前向传播过程。然后呢再往下边还得来，再来接下来就是具体的什么？todo 2。todo 2 该干嘛了？叫测试。测试 Transformer 模型架构。def 叫 make_model。能大概看明白吗？make_model，这是我要讲的思路啊，我要讲的思路。来个 pass。

好了，那现在有个问题就是，我们现在要干嘛咧？要得到编码器的输出结果，就是编码器的前向传播；解码器的输出结果，是解码器的前向传播。现在我有个问题就是，你看，你在上面做了啥？你在上面只是在这里边写了你到底去用哪一个解码，啊，你倒底用哪个解码，所以这里边我得把编码跟解码的动作得糅进去。然后去做了件事。好，编码是编码，解码是解码，我们得把 encoder decoder 它的具体的动作给它揉进去。啊就这个工作，要注意。好，就这。行吗？那关于我们的 Transformer 它的架构搭建我们先聊到这。稍后还差两个函数和一个测试，那我们整完，然后这个结果就结束了。啊就到这儿了，其实还是比较简单一些啊。行，那关于这个整体的 Transformer 架构，然后搭建我们先聊到这，先保存一下啊。
