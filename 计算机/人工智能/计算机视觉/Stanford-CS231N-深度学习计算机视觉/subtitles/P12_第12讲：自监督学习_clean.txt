上周二我们上了一节关于GPU的课，讲了怎么训练，怎么使用，还有怎么用多张GPU做更大规模的训练，拓展训练规模等等。今天我们给这门课新加了一个很有意思的主题。我觉得它很及时也很重要，因为现在的模型规模和应用都在不断变大，如今的AI模型就是这样。 在那之前， 我们讲完了计算机视觉里所有的关键任务，包括分类、语义分割、目标检测、实例分割等等。我们会回头再看其中一些主题，还有今天讲过的模型结果。这些任务现在还是很重要的。然后我们讲了怎么可视化和理解模型， 看看模型到底在学什么。我们讨论过的一点是， 比如早些课上讲到的最近邻和像素空间， 当时聊了具体该怎么做。我们讨论了仅靠像素空间的距离来给图像分类，发现这其实效率并不高。还有一点我们也聊过，如果用嵌入层，或者说特征空间、特征层，也就是特征图里那些全连接层，它来自卷积神经网络，或者我们用的其他网络架构，这能很好地表示图像。我们还讨论了如果使用L2距离，这就是这些模型在特征空间里做最近邻的度量标准，对吧？所以这说明这些特征对我们手头的具体任务很有意义，也就是说，好的，我们跑一个神经网络，比如CNN、ResNet甚至Transformer模型，然后来看看这些表示。 也就是学习到的表示。不同语境下大家可能用不同名字称呼它们：学习到的表示、特征、嵌入、潜在空间等。不过，这些学习到的表示或特征非常能代表图像。 要是我们能要提取它们，我们可以直接从这些特征里得出类别标签。而且你看最后这里，用一个简单的线性模型就行。但主要难点在于，大规模训练 或构建这些网络一直很有挑战。你知道这挑战在哪儿吗？主要是在大规模下需要大量标注数据，因为网络从图像开始训练，最后要出类别标签。只要训练这个网络，这些特征确实能帮我们很好地获取类别标签。对，但在大规模场景下，得靠大量人工逐张标注图像，工作量很大。比如做分割，你得把每张图里的像素都标出来，这挑战可不小。所以问题来了， 有没有办法不用海量人工标注数据，也能训练神经网络？所以人工标注是个难点，咱们看看能不能绕过它来训练，这样就能训练出一个能提取高质量特征的神经网络。于是，自监督学习这个概念就登场了，这也是咱们今天要讲的主题。比如 你手头 有一大堆没打标签的图像数据，我们假设可以用一个目标函数，也就是pretext任务来训练神经网络，这能帮我们从图像中提取出好特征。 接下来，面对数据量较小的特定数据集，对于带标签的数据，我们可以训练模型，或者迁移这个训练好的编码器，用来提取下游任务所需的特征。所以我们要定义一个pretext任务，它得足够通用，以便从图像里学到好特征，然后利用这个编码器——咱们就叫它编码器，去解决另一个问题，也就是我们说的下游任务或目标，即你关心的应用场景。比如你有很多从网上下载的自然图像，我们可以从中训练出模型，然后我们有一个小规模数据集，比如某种工业或医疗应用，里面只有少量带标签的图像。现在我们可以利用这种知识迁移来提取特征，然后进行分类或执行我们感兴趣的任务。所以我们要深入探讨这个话题，更深入地理解各个组件。简单说，自监督学习正如我所讲， 就是在无标签数据集上定义pretext任务，通常用一个编码器，这样我们就能得到一些学习到的表示。同一个神经网络的另一个模块，会把学到的表示转换到输出空间，也就是标签，或者是从数据中自动生成的输出，而不是人工标注。只要能做到这一点，我们就有目标函数、损失函数，以及一个要用该损失函数训练的神经网络。大家看这里第二部分有时叫解码器、分类器或回归器，取决于我们怎么定义pretext任务。我会举几个例子，不过这其实可以是任何形式的框架。但如果是编码器加解码器，这就更偏向自编码框架了。我稍后简单讲一下。好，所以做完pretext任务的训练之后，我们就能用编码器和学到的表示来处理下游任务。 只需要加一层或者一个能预测标签的全连接网络，这些标签现在直接来自数据集。所以这就是自监督学习的核心概念。它的pretext任务部分训练时不需要任何标注数据。但具体怎么定义pretext任务本身，其实没那么简单。定义方式有很多种， 比如，只要记住我们想要定义的是pretext任务，首先要足够通用，既能帮我们学到好特征，又不需要人工标注。标签应来自数据本身，比如图像补全，遮住图像的一半或部分，任务是根据未遮住的部分预测被遮住的部分，预测被遮住的部分。或者把图像旋转一个特定角度，任务是以图像为输入，预测它被旋转了多少度。还有一种是拼图，图像块是打乱无序的，任务是输出这些图像块的正确顺序。还有，色彩化也是常用的一种。今天我们快速过一下这四种。给定图像的黑白版本，预测每个像素的颜色。 解决pretext任务能让模型学到好的特征，这正是我们想要的。自动为pretext任务生成标签。刚才说的两点，是判断一个任务能不能作为自监督学习pretext任务的关键。这里有几点需要时刻记在心里。怎么评估一个自监督学习框架，其实有很多方面和角度值得深入考察。先看pretext任务本身，毕竟标签是我们生成的，这就让我们能做一些事，比如评估一下模型解决这个pretext任务的表现有多好，这是一个因素。另外，表示质量本身有时也很关键。比如直接看那些没微调过的表示，或者把表示聚类一下，看看里面有没有规律。 有时候我们也能用一些不错的降维算法。这里提一下t-SNE，之前没细讲，它是一种降维框架，能把学到的表示降维后在2D或3D里可视化，看看是否存在，看看你能不能发现某种模式。 总之，鲁棒性、泛化能力和计算效率，这些都很关键。但最重要的、我们真正追求的是 下游任务的表现。我们做自监督学习、定义pretext任务等，都是为了提升我们关心的那个任务，我们关心的事。来看几个快速示例，看看具体怎么做。 举个例子，比如旋转图像，然后预测旋转的角度作为输出。这样就能自监督训练，不需要图像中物体的标签。 这个例子用了一堆卷积层，最后接一个全连接神经网络来完成。不管是回归还是分类，这都说明我们得到了一组不错的特征，这是个很好的特征提取器。最后我们可以把全连接层去掉。 pretext任务里有特定的全连接层，可能是一层，也可能是多层，把特征分到对应的物体标签里。这次我们就用这些标签来做预测，然后训练这个线性函数本身。所以我们常找浅层网络，因为特征够好的话，就不用费太多功夫， 不用花太多精力去训练怎么输出类别标签。这就是自监督学习。虽然咱们聊的是计算机视觉，但这种范式其实是什么推动了GPT-4等这些大语言模型及其框架的训练？主要是原始数据无需人工标注。这不仅是语言模型、语音，甚至如今的机器人领域也是如此，还有强化学习。因为无需标注数据，我们就能直接开始采集数据，直接用这些无需人工标注的原始数据进行训练。这就是为什么你在湾区能看到那么多自动驾驶汽车，通过收集数据来获取训练素材，无需人工标注即可直接训练模型。 今天我们将先讲讲基于图像变换的pretext任务，接着介绍一组算法，这些算法基于对比表示学习，虽与图像变换任务不同，但前景看好。那么 我们先从第一部分开始。我们将逐一介绍这些任务。 我之前讲了不少关于旋转、预测旋转角度的内容，咱们来看看能不能真的把图像旋转起来。把图像随机旋转任意角度， 再用模型预测它的旋转角度。我们的假设是，只有当模型具备物体应有的视觉常识时，它才能识别出物体的正确旋转方向，也就是关于物体本该长什么样的视觉常识。所以，这些模型大多围绕视觉常识这个概念来设计。如果模型能捕捉到这一点，说明它也能把整张图像概括成一组有用的特征，一组好特征。 这是2018年发表的一篇论文。他们通过探索0、90、180和270度这四个角度，对图像进行旋转来实现这一点。旋转图像后，用卷积神经网络预测它属于哪种旋转。因为输出只有四种，所以这是一个分类任务，对吧？它不用预测具体度数，只需预测这四个类别之一。类别是0、 1、2或3等。这样一来，作者们就能学习好的表示，然后利用这些表示，他们开始在下游任务上训练神经网络，本质上就是对网络做微调。基于对编码器的微调， 还有分类器，实际上在这个例子里他们冻结了前两层冻结，只微调最后一个卷积层和线性层。这并不是对整个网络做微调，但他们依然能够取得非常好的结果。这是在CIFAR-10数据集上，这是我们之前提到过的数据集。可以看到，模型经过预训练后，一开始就有很高的准确率。这说明模型状态不错，哪怕刚开始迭代，它也能很好地理解物体。不过任务要是够简单，CIFAR-10其实不难，任务够简单的话，直接监督和先预训练再训练，最终准确率通常是一样的。但在很难的任务里，如果不做大规模预训练，监督学习的效果通常没那么好。好。 谢谢。 他们还在Pascal VOC 2007数据集上做了些实验，这里面有分类、检测和分割好几个任务。针对这三类任务，他们用了不同设置，比如只训几个全连接层或者全训。以分割任务为例， 如果你看ImageNet的标签，假设我们有一个庞大的标注数据集，并基于它进行预训练，准确率会非常高。但要注意，这是基于包含全部标签的ImageNet做的预训练。而如果我们不做任何监督预训练，预训练完全基于自监督学习，这说明这个旋转框架， 它的表现远胜于其他许多方法。虽然这里没有细讲其他那些方法的细节，但这证明了旋转pretext任务确实有效。再看看区别有多大。如果从权重的随机初始化开始，效果会提升多少？可见，随机初始化和用旋转pretext任务预训练，两者效果差异巨大。这个任务确实管用，结果相当接近在完整ImageNet上预训练的效果。 论文还研究了 特征， 看它们是如何，学到的特征为何有意义。我之前提过，评估pretext任务或自监督学习框架，通常看特征，对吧？你可以直接利用全连接层的特征。我们讲过Grad-CAM等注意力框架，如何从特征还原到图像空间。评估是将这些特征投影到图像空间，看模型关注什么。看监督模型的注意力图，通常更集中，因为它只解决分类这一单一任务。只要模型抓住了眼睛和周围形状，它对其他部分就太不在意了。比如帕丁，在自监督学习中，模型通常会学习更多特征和区域，因为它需要对图像有整体理解，毕竟我们不知道下游任务是什么，所以它它的目标是在这些任务中都表现出色。 这就是其中一个任务。 嗯，大家如果有问题先记下来。我讲完这些任务就停。如果问题没解决，我很乐意解答。好。 另一个流行的pretext任务是建个3x3网格， 然后用网络 来预测每个给定补丁相对于中心补丁的位置。比如这个来自这里的补丁，输出应该是3，因为我们总共只有8个，这是3x3的网格中心图块作为参考，所以这是一个8分类任务。给定这些图块中的任意一个，模型试图输出它相对于输入传感器的位置，图块，抱歉。所以 这是个例子。但另一篇后续论文把把它变成拼图框架，不再让模型猜它是8个图块中的哪一个，而是尝试预测确切的排列顺序，也就是正确的某种排列，排列顺序。所以他们用了同样的3x3网格，他们把所有图块随机打乱，然后交给神经网络去处理，指出正确的排列顺序，也就是哪个才是对的。所以他们基本上是在预测正确的排列顺序。大家能算出这里有多少种排列组合吗？ 再说一遍， 这个设置。9的阶乘，没错。这数很大，对吧？我记得是30多万。但他们的做法是，只建了一个含64项的查找表，合理的、可能的旋转，抱歉，是排列，然后他们只考虑64种排列。打乱的时候，就按照64种里的某一种来打乱。而输出也同样是一个大小为64的向量。所以这又变成了一个只有64个输出类别的简单分类任务。他们还证明，把它定义为pretext任务同样是个好主意，用在同一数据集同类型的数据上。 在我讲过的那些任务和监督方式上，他们证明自己的方法优于其中一些方法，以及之前的其他模型和框架。再次提醒，这项研究发表于2016年。 下一个pretext任务是图像修复， 也就是预测缺失的部分。他们这里用的是一个简单的掩码策略，把图像的一部分遮住，然后让模型去修复这些被遮住的区域。虽然只是对输入图像做了简单掩码，但因为我们有所有图像，所以其实知道想要的输出。于是编码器把它转成特征空间，然后在这个特征空间里还有一些其他成分，中间是全连接层，随后接一个解码器，用来还原缺失的部分。损失函数会把输出结果和真实情况做对比。 这本质上就是在学习重建缺失的像素。咱们之前聊过几次自编码器，这也算一种。它先把输入图像编码成一种表示，再去解码。不过这种自编码器是用掩码策略、掩码目标来训练的。 咱们再看几个例子，看看图像修复的效果。这有点意思，也挺棘手，因为做图像修复时，补全这张图的方法太多了，对吧？所以在这种情况下，不能说只有唯一一种正确的结果。早期的重构框架其实会生成一堆很模糊的图的，还有向外做平滑处理。 所以 我这里提到的这篇论文用了一个额外的对抗目标函数， 细节下节课讲生成模型时再说。这类框架的原理是有个重构损失，它主要就是在计算重构的误差，就是原图x和过了一遍编码器后的图之间的差别，然后呢，这里做个逐元素相乘，还有个掩码，因为我们只想计算损失函数目标函数只在掩码区域算，所以我们也拿掩码做个逐元素相乘，这基本上就给出了我们已有掩码那部分的重建损失。 所以，前面提过我们还引入了对抗学习损失函数作为补充，这一点稍微有些复杂，这样能保证生成的图像看起来够逼真，对吧？借助于这一点，他们成功优化了那些原本效果不佳的部分，重建出来的部分会更好看一些。不过细节还是会有所欠缺，留到下次课再详细讨论。 这个重建框架，我们的方案也一样，确实能带来额外收益。在相同的分类、检测和分割任务上，相同的数据集上也是如此。对，我待会儿再讲基于重建的框架和掩码，毕竟这招最常用。这些模型或pretext任务是现在做预训练常用的。好的，我回头再说，不过在此之前， 先介绍另一种pretext任务：图像着色。 这是另一个很简单的设置。我们把彩色图像作为输入，毕竟数据集里基本都是彩色的，对吧？把彩色图像拆分成通道，把亮度和光照分离出来，和颜色本身分开。如果你上过计算机图形学或CS131这类课，肯定知道色彩空间有很多种，对吧？不过做计算机视觉时我们主要用RGB，想把明度和颜色分开， 咱们可以用其他色彩空间，比如Lab色彩空间。Lab就是把明度和颜色分开的那类空间。所以咱们用一个通道管明度，另外两个通道定义实际颜色。把L、a、b这三个通道加在一起，就能得到彩色图像。 这里的pretext任务很简单，已知L通道，去预测a和b通道，对吧？所以咱们完全不用做人工标注， 数据里本来就有。后来这思路还扩展到了其他框架里。咱们干嘛只盯着给定L预测a和b这一种做法呢？反过来也行。 这就叫分裂脑自编码器。把输入图拆成亮度通道和颜色通道，这是单通道，这是两个颜色通道。我们训练两组网络层，互相预测，最后合并两者生成原图，算L2损失，用任意距离函数都行。训练网络更通用的框架里，思路是给定一组通道，预测剩下的。对X2也这么干。 所以通道组X1和X2，给定一个，就能预测另一个对应的网络，就是这样子。把它们合在一起， 就能得到这个值。这样图像和损失函数就简单了，对吧？有了这个框架，咱们就能把它用在各种地方，不只是颜色和光照，对吧？咱们可以用一些数据，比如那些有RGB和深度通道的RGB-D传感器，像Kinect，以及机器人用的其他传感器。咱们可以用RGB通道去预测深度， 反过来也行。这是个很成功的下游任务，大家一直在用，各种应用场景。你看，这个模型和论文就是分裂大脑，还有那个给图像上色的模型。这些特征本身预测类别标签的准确率其实很高。你看，这里还有很多其他方法用的框架。 另外， 咱们来做个比较。再说一遍，这效果不如监督学习，因为这儿没用到标签。它是基于学习到的特征，把F1和F2的特征拼接起来。 好，所以图像着色这个pretext任务挺有意思，因为咱们现在能，它不仅能用来预训练神经网络，本身也挺有用，因为咱们能给那些没彩色版的图上色。所以咱们能给图片上色，还有那些没彩色版的视频，这些。而且论文里还展示了另一个有趣结果，就是这张优胜美地的图，他们给半圆顶上了色。有意思的地方在于，这张图里能看到半圆顶、树木或桥梁这些实物，和它们在水里的倒影，颜色是一致的。模型也能理解倒影也得保留物体的颜色。这得益于海量数据的训练。记住，这些都是大型语言或视觉模型，只针对特定任务训练过，所以它们并不是万能的。 这其实可以扩展到比如视频场景。有了视频，我们就能用带颜色的参考帧给后面的帧上色。做法很简单，给视频后续帧上色，其实我们做的就是在尝试追踪视频里的像素和物体。模型会隐式地学会该怎样追踪。假设是学会给视频帧上色， 就能让模型在没有标签的情况下学会追踪区域或物体。另外，学习给视频上色本身就挺有意思，因为里面有大量对应关系。建议大家看看具体细节。我简单讲一下。有了参考帧，给输入帧上色时，得先找到特定对象或像素的对应位置，看看那儿是什么颜色，然后复制过来，把它作为输出，也就是目标颜色。 这做法跟我们之前聊的注意力机制很像，也就是给每个像素建立注意力。对每个参考帧和目标帧，我们一般跑一个CNN，看看像素周围有什么特征。有了这些特征，我们就能为每个目标像素算出注意力或距离或距离，应用到参考帧中的所有帧， 所有像素。接着我们定义了这种注意力机制。即我们把目标帧里的目标像素与参考帧的所有像素关联，然后取平均值，根据这些注意力模块来计算颜色。 注意力其实就是两者的相似度。有了这个，我们就能直接得到输出颜色，以注意力加权求平均，最终得到结果。接着计算损失函数，因为数据里已经有了这些像素的正确颜色值。利用这个参考帧，我们就实现了图像上色。你看，在着色上这种一致性有多稳定。如果分开着色，缺乏时间上的一致性，你会发现比如人物的衣服颜色会变色，因为没有约束 来保持这种一致性。另外，因为你在计算对参考帧的注意力，就能跟踪视频里的物体和分割区域。所以有了些很有趣的应用，甚至还能识别视频里的关键点。好，问题。 你问的基本上就是关于这张幻灯片的内容，以及编码器是怎么了解数据的，首先，然后帮我们学到了好的表示。刚才提到的这些任务，目的都是为了做点事，比如解码、分类，或者用回归生成输出，好去训练这个编码器。比如分类，或者用回归生成一些输出，这样就能训练这个编码器了。假设你的原始图像都是网图或ImageNet里的自然图像，那你学到的编码器就能从这类图里提取特征，对吧？借助于pretext任务，接着你把解码器去掉， 在末尾加上分类器，这时候你只需要训练分类器，去训练这部分就行，因为这个编码器已经用我刚才说的所有预训练任务训练过了。你是想问预训练编码器时，标签是不是从解码器来的？答案是肯定的。我们定义pretext任务就是为了获得某种标签或输出，对吧？然后基于这些输出，我们去训练整个网络，以及其余的部分。预测标签时，这个编码器也会一起训练。好，问题。你是想问编码器和解码器是不是同一个大网络，还是不同论文里的做法不一样？完全不一样。有些时候编码器，而不只是解码器，才是分类器。刚才讲预测度数那个例子里，这就是个简单的神经网络，对吧？解码器就是这些全连接层，所以这算是一个完整的网络。之后你会把它替换成下游任务的模型。但像自编码那样，先编码图像再解码，通常会有两个端到端训练的网络，因为你想利用中间那个表示空间。接下来我想说的是，掩码编码器基本上大体没有，编码器和解码器是对称的，但它们也可以完全不同。我们可以为不同的神经网络搭建框架，即便没有对称性，也能进行训练。这很看具体任务和pretext任务，但它们可能属于同一种架构，它们可能属于我们熟知的同一架构，比如CNN或ResNet，也可以是两种不同的架构。即便没有对称性， 大家记得这可是自监督学习最早jp期的的方法之一，所以它们没法解决所有问题。先做个简短说明。不过，假设是如果模型能认出这是旋转了90度，说明它隐晦地理解了正确的旋转方向。对，抱歉，我指的是方向，然后如果给它一张未旋转的图，它就能正确处理，它能识别右边那东西。不过这任务本身挺局限的，同意。 你问的是为啥这儿选64？对，好问题。但这基本是随便选的。我说过了，这儿有很多排列组合，9的阶乘，数字很大。预测所有这些没意义，作者挑了一些变化大的扰动，因为很多扰动其实只像一块补丁只是换了位置，对吧？所以他们挑了64个差异最大的。选64是因为要解决分类问题，不是别的类型的任务。谢谢。 好。我刚才一直在讲这些，这些框架通常会对图像或视频做些变换。这就引出了2021年发布的一个新框架，之后大家跟进了很多人研究。这是个很棒的预训练框架，能用于很多任务。 不过现在大家用原始数据预训练时，通常都用MAE，它叫掩码自编码器。这也是个基于重建的框架，就像我说的绘画像掩码策略，但细节要丰富得多。大家可以看到这个框架不只是选一个掩码，有很多不同的图块和位置，他们会用更激进的采样率来做掩码，掩码率能达到50%，甚至75%。大规模训练表明，他们不仅能重建所有被掩码的区域， 他们还得到了能总结图像的出色编码器，从而获得优质特征。具体做法是定义一个解码器，定义一个解码器，这就是我之前提到的非对称性示例之一。编码器与解码器，大部分输入补丁被掩蔽，未被掩蔽的部分送入编码器，编码为特征，再经由解码器处理，以生成完整图像。咱们具体看看这意味这什么。关于训练细节我稍后详述。 先简单讲讲这些模型的工作原理。此处的编码器与ViT非常相似，它们都基于Transformer，也就是我们之前聊过的ViT。图像被切成图块，再对这些图块进行采样。他们用的是均匀采样，并且表明75%的采样率是合适的好，在实验中相当高效，而且他们用了很高的掩码比例。这让预测任务变得很有挑战性。在自监督学习的pretext任务里，有挑战性说明任务是有意义的，这任务很好，因为模型必须学到好的特征才能重构出来。所以采用这么高的采样率采样，还能大幅增强数据，因为每次你都掩码了75%的数据。所以训练时你能把同一张图反复复用很多次，这样就有海量数据来训练这个编码器。所以他们才用了个巨大的编码器，一个超大的ViT作为编码器。这个编码器本身只看到25%的样本，也就是图像块，然后对它们做嵌入，先做一次线性投影，映射到嵌入空间，再加上位置嵌入，跟我们之前讲ViT时完全一样。这些全是Transformer块，编码器非常大，这就是我刚才说的。 然后到解码部分，这时我们手里有所有现存图像块的嵌入。对于被掩码或缺失的图像块，有一个可训练参数，跟我们之前共享的那个掩码类标记很像。这是个掩码标记，基本上算某种平均。咱们可以把它当成一种平均补丁，也就是针对那些缺失或被掩码部分的平均表示。接着，解码器得把它们还原成图像补丁，这些图像补丁拼成了整张图，最后输出的就是整张图像。那咱们怎么训练它呢？这里用的是基于均方误差MSE的损失函数。咱们在原始图像和重建图像之间算损失时，只针对那些被掩码的补丁来算，就跟刚才说的那样。 论文里提到，训练的时候你可以选线性探测，或者做全微调，这样就能把它用到你想做的那些下游任务里了。做线性探测的时候，通常会把编码器冻结住，接着利用学到的表示，只针对最终任务去学一个线性函数，这个标记说明模型已经训练好了。不过在全参数微调里，预训练编码器也会跟着调，要么全调，要么只调几个Transformer块。这就是微调框架，也就是线性探测，这能衡量表示质量，看看特征表现如何。

微调就是利用模型接近真实潜力的能力去适应新任务，任务。好，如果你对这方面感兴趣，打算用这篇论文，我强烈建议看看它后续的跟进研究。大家讨论了很多方面，比如模型怎么选，超参数怎么设，掩码比例就是其中一个。他们发现掩码比例设为75%时准确率其实非常高，这就是选75%的原因。还有解码器的深度、宽度，以及掩码token、重构目标、数据增强，还有具体是怎么做的，还有辅助方法和掩码采样方法，这里再展示一下结果。掩码采样方法主要看用随机掩码块还是网格式掩码，大家看这些例子，他们得出的结论是随机掩码效果最好。最后，他们证明MAE比当时其他很多方法都要好的多，比如另外几种很先进的方法，就是DINO和MoCoV3，时间够的话我简单过一下，这个框架表现确实更好，比当时那些先进的对比学习框架都要强。我先停一下，大家有什么问题吗？最后，咱们简单回顾一下刚才聊的内容。Pretext任务其实很关键，就像我说的，它主要用来培养对视觉常识的理解。另外，结合大家问到的问题，我们发现单独设计一个pretext任务挺难的，因为学到的表示可能不够时长15.03s，译文小于等于75字，不够通用，这跟你定义的任务类型有关，对吧？比如要是用补全、旋转、预测、拼图或者着色这些任务，学到的表示只擅长解决这类特定问题，它们可能不太适合通用的pretext任务。所以问题来了，在分裂脑自编码器里，模型是怎么知道的？怎么根据已有的通道去预测另一个通道呢？比如L通道，也就是光照通道或明度通道。咱们换个方式，我用一个问题来回答你：训练模型预测图像物体类别时，编码器怎么知道该提取哪些特征来预测“母亲”这个类别？你有标签数据，你做的是反向传播，利用基于这些标签算出的损失值，对吧？道理一样，我们定义一个网络，输入是其中一个通道，然后输出另一个通道。训练时我们通过反向传播来调整，使输出符合预期。输出就是那个通道，因为数据里本来就有它。所以咱们不定义任务去预测物体类别，而是定义为预测像素的颜色。像素颜色数据里都有，所以损失函数照样能算，也能反向传播。关键是怎么把这些输出喂给解码器？这又是ViT Transformer那一套框架，也就是编码器，它把每一个输入变成token代表那个特定的输入块，咱们聊过这个。但不是所有块都在列表里，有些被遮住了。对那些被遮住的块，我们让编码器输出一个共享的掩码token。这个token基本就是个平均token，比如它是可学习的参数，咱们不一定能解释它，但可以说它可能就像个掩码，比如平均token。共享的mask token填补了缺失处，由此生成了这个长序列。解码器是另一种Transformer框架，处理这些长序列，一组token，并输出投影为像素值的结果。很棒！咱们只剩数分钟了，而且嗯……你还有很多内容要讲。但我想让你通过这次课理解什么是pretext任务及其定义。现在最常用的框架之一是掩码自编码器，我们其实已经讲得挺细了。不管怎样，我们看了这些变换，知道它们其实都指向同一个物体，原始图像只是换了种形式，对吧？而且我们还有相关的背景知识，数据集中还有其他看起来完全不同的对象，对吧？咱们定义一个任务，把属于同一对象、同一像素的样本在表示空间里拉近，而那些不属于同一对象的样本在潜在空间里尽量拉大距离，也就是排斥这两张图像的表示，这通常被称为对比学习，或者叫对比表示学习。这里有很多很有意思的方法值得看。这个方向论文特别多，特别是围绕2018年到2020年代这段时间。此后几年出现了SimCLR、MoCo、CPC，最终是DINO。DINO借鉴了对比学习的概念，但并非严格意义上的对比学习框架。嗯，我们这么做是为了定义吸引和排斥函数，吸引和排斥函数，谢谢。我们基于这些特性或正则化项来定义，把参考图像记作x，同一图像的所有变换则视为作为正样本，数据集中或批次里的其他对象则作为负样本。这些正负样本基本决定了我们如何计算损失函数。具体怎么做？假设我们有一个评分函数，我们要设计一个评分函数，让参考图及其编码特征与正样本的得分高于另一组对比的得分，也就是参考图与负样本的对比得分。所以有了这个评分函数，我们据此定义损失函数，并在训练它之后训练这个评分函数。这里的s就是上一页里提到的得分。有了这个评分函数，现在为了实现吸引和排斥，我们可以用softmax框架把指数化的得分转化为概率值。得分转成概率值，分母里则包含了所有用到的其他负样本，这些都会被考虑进去。实际上我们是用批量学习框架来实现的，所有属于其他物体的负样本，批次里的其他样本作为负样本，其中一个Transformer作为正样本。所以我们这样定义损失函数：正样本对的得分以及所有其他负样本对的得分。这个函数和我们之前讨论过的很相似，大家有想法吗？这是多分类的交叉熵。所以假设我们有n个样本，抱歉，N个样本。对，这种情况下我们有N个样本。所以softmax就是假如输出10个类别，它要最大化其中一类的得分，同时最小化其他类的，对吧？这里也是同样的逻辑，我们要最大化这个分数，同时最小化负样本和参考样本之间的分数，对吧？概念是一样的，就是咱们之前讲的多类分类，只不过这里用对比学习损失函数的形式来表达。这个函数叫InfoNCE，也就是信息噪声对比估计损失，是这篇论文里提出的。论文里有很多理论探讨，说明这个目标函数也就是这个损失函数，衡量的是变量之间的依赖性。抱歉，它其实是互信息的一个下界。而互信息衡量的是，比如你有两张图，算它们之间的互信息其实就是在看这两张图有多少共享信息或依赖性，对吧？所以我们要做的就是最大化x和x+之间的共享信息，同时最小化x和x-之间的共享信息，对吧？论文里说这部分展开讲要半小时，感兴趣的话一定要去读读原文。InfoNCE损失函数的负值就是x和x之间互信息的下界，它是互信息的下界取负号后，就是互信息的下界。最小化InfoNCE其实就是在最大化，也就是最大化x和x的互信息，这才是真正想要的。所以我们就把它当损失函数开始最小化它。InfoNCE论文还有一个理论点，负样本越多，这个界限就越紧，所以它是靠负样本数量来收紧这个界限的。所以训练这种损失函数时，我们需要用很大的批次大小，负样本越多效果越好，谢谢。而且训练收敛快得多。这个损失函数用在了好几个框架里，接下来几分钟我给大家讲讲这些框架。比如SimCLR，这是一个简单的对比学习框架。它主要是对每张图片做两次变换，把它转到表示空间里。它算的是嵌入表示间的余弦相似度，不过算之前会先线性或非线性投影到一组特征z，用来算这个空间里这些特征间的距离。这就是生成正样本的做法。生成正样本时可以使用多种变换。细节基本都讲过了，采样数据增强以生成正样本。对，我们采样几个函数，然后在样本对上计算InfoNCE损失。我们不断迭代，因为每个样本对应n个我们创建的样本。对，具体来说，我们先取一个列表，把当前批次里的图像都过一遍编码器。同一张图的不同变体，每张图基本都有它的变换版本。接下来我们有两个N阶，也就是批次里的样本，也就是说对于每个样本，它的下一个样本，这两个是正样本，其他都是负样本。所以第一个样本，第二张图是，是正样本，其他都是负样本。第二个样本的话，第一个是正样本，其他都是负样本。所有样本都这样重复，这就是SimCLR的高层定义。注意，作业三里有一个关于SimCLR的问题，你们会咱们再深入看看这个框架。不过要注意，这里的定义和我之前讲的标准定义有点不一样，大家记得按作业要求来做。SimCLR挺成功的，它不用标签，只要在特征上训练一个线性分类器，就能取得不错的效果。它超越了之前的所有工作，结果甚至能和完全监督学习框架媲美。虽然咱们得用更大的网络，因为学的是更通用的特征，但准确率其实没那么高，跟监督学习差不多。SimCLR和相关结果有意思的地方在于其实有好几种选择，咱们重点聊聊主要的几种。你可能在想，为啥要把特征投影到新变量，而不是直接用原来的表示？这是SimCLR的一个设计选择。他们这么判断是有道理的。目标函数在样本间做对比时，往往会丢掉一些对对比学习框架没用的额外信息。为了保留这些额外特征，我们把表示定义为h，但接着会有一个线性或非线性投影。在他们的论文里，他们用非线性投影来得到那些能用来计算InfoNCE的z值。这是其中一个重要的设计选择。另一个是之前提到过的大批量，你需要超大的批量、更大的批量才能获得更好、更简单的效果。至于性能，咱们聊过它为什么会这样，又是怎么实现的。但手头很多任务我们没法总是用大批量，因为内存等方面的限制，这就是原因。后续有不少跟进工作，比如MoCo提出了动量对比学习，不再使用批量中的所有负样本，也就是利用批量里的所有负样本。它的做法是建立一个队列并保存那些负样本的历史记录。这种关联是跨越所有图像块并随时间变化的，所以它不只依赖批次里的负样本。模型有个独立队列，专门存负样本，还会随时间不断更新。咱们用对比损失在这儿训练模型，去算InfoNCE损失。但有了这个队列就没法反向传播了，因为这些样本不在批次里，所以没法对负样本反向传播。这就是为啥正样本得分开用编码器，正样本现在叫query，负样本现在叫key。训练只影响query编码器，然后用动量机制随时间更新key编码器。它更新key编码器，也就是动量编码器，对吧？这套框架落地效果很好，后续版本也很成功，结果挺有意思的。他们主要尝试了混合方案，比如用了非线性投影投影，结合了SimCLR的数据增强，还有MoCo里小批量和负样本队列的做法。他们发现把这些结合起来，也就是MoCoV2，效果会更好，能大幅提升性能。我就讲到这儿。另外还有个CPC对比预测编码的例子，大家看幻灯片就行。还有MoCo的改版，也就是MoCoV3。DINO也是常用框架之一，架构和MoCo类似，就像MoCo，但不一定是对比学习，因为现在有了学生网络和教师网络。这部分留在单独讨论，感兴趣的话我们以后在课件或讲座里再聊。总之，这也是从图像中提取特征的常用框架，有时也用于视频。