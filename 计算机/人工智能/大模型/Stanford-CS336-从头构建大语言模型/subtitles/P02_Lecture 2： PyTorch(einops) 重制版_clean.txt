um，希望大家都没淋雨，我倒是淋湿了。上次说过，我们好像有一次 1e23 浮点运算次数的训练，之前在跑，现在跑完了。结果其实和预测的一样。大家记得我们跑的这些每条曲线代表的是，um，本质上是一条等浮点运算次数曲线，也就是一批小规模模型运行，你要从中找到计算最优的那个点。拟合一个缩放定律，这就是我们预测损失，然后实际跑出损失的那个点。模型跑出来的损失在 0.01 以内，我觉得这挺酷的。如果外推到 GPT-5 的性能水平，这就是你会得到的损失值。当然，具体结果可能因这些缩放定律的具体情况而异。好的，简单分享一下这个消息。在上一讲中，我概述了整门课程，我们谈到了分词，这将会是第一次作业的内容。今天我想谈谈资源核算。这部分会更偏向系统层面。各位回顾一下，我们的核心目标是在有限资源下，训练出尽可能好的模型。资源包括算力、内存，有时也包括数据。不过数据在本课中并不会成为限制因素。我们的目标就是单纯地最大化训练的效率。所以在优化计算效率之前，我们需要理解某一具体计算的效率。为此，我们需要理解算力和内存特征。先让大家大致感受一下，到课程结束时你们有望能回答哪类问题。多少 H100s，用 1024 张 H100，在 15 万亿个 token 上，训练一个 700 亿参数的模型需要多长时间？其实这里指的还是 H100。那我们要怎么回答这个问题呢？这里有个公式，能算出浮点运算次数是参数量的 6 倍再乘以 token 数。我们可以去查一下 H100 的规格表，看看它有多快。我们有个叫 MFU 的指标，取值为 0.5。然后你能估算出需要多少浮点运算次数，硬件每天能提供多少，最后就能算出需要多少天。所以总共需要大概 40 天。好，接下来看另一个问题。如果用 8 块 H100 配合 AdamW，你能训练的最大模型有多大？你可以看一下，H100 配有 80 GB 的 HBM。内存，也就是每个参数占用的字节数，加起来是 2 + 2 + 4 + 4。我们稍后解释这些数字是怎么来的。参数总数大约是 530 亿。这里有个注意点，我们不计算激活值，因为它取决于批次大小和序列长度。所以这些都是非常粗略的估算，但希望上完这门课，你们能明白这些数字是从哪来的。重点不是要把每个数字都算得精确，而是大概搞清楚整体情况。谢谢。好，上次我讲了知识，以及你们能从这门课里学到什么。机制，也就是系统是怎么运行的，所以今天的内容会很简单、直接。原理就是 PyTorch 和张量是怎么工作的，没什么花哨的，很直接。我想让大家建立一种思维：资源核算非常关键，希望大家养成习惯，每写一行代码都想想性能。特性，最后是直觉。我们要大致了解一下资源有多少，以及它们是怎么被消耗的。我们今天不会讲机器学习里的“魔法”，这部分留给 Tatsun 下次课讲。So，咱们开始吧。从底层入手，层层往上搭。底层是什么？是张量。张量是存储一切的基础单元。参数、梯度、优化器状态、数据、激活值，本质上都是张量。比如大家可以看看 DeepSeek 模型，你会看到模型本身其实是一堆不同的张量，每个张量都有各自的形状和精度，我后面会讲到。你知道的，张量包含了向量和矩阵，并且能推广到任意维度。好，那我们来聊聊存储张量需要占用多少空间。这得看张量的类型。所以一般来说我们处理的是存储浮点数的张量，不过张量也能存整数或其他类型。说到浮点数，通常你提到 float，我想大家指的都是标准的 float32。所以 float32 拆开来看，有 32 位。其中 1 位是符号位，8 位是指数位，用来表示动态范围；剩下的是尾数或小数部分，用来表示数值的细微变化。这也被称为 FP32 或单精度。单精度这个词的由来是：早期做科学计算时，float32 算是一种基准。就好比你预期的那样，如果有人给你一个浮点数，你会默认它至少是单精度。如果想要更高的精度，可以用双精度，也就是 float64。但在深度学习中，我们走的方向恰恰相反。因为即便 32 位精度也已经很高了，我们要做的这类计算，并不需要像某些数值模拟那样的高精度。好，在介绍其他类型之前，我们先来看 float32。好的，那么我们来构建一个 4×8 的矩阵。默认情况下，你创建的张量类型是 float32，所以如果你需要其他类型，应当显式声明。内存占用就是元素数量乘以元素大小。这里 32 位数元素大小为 4 字节，因此总共是 128 字节。为了给你一个直观的概念，在 GPT-3 这个相对较旧的模型中，前馈层中的一个矩阵，trying, like 16 and uh, can we get hands who are preferring transformers? 因此这些张量可能变得相当大，而这甚至还不是人们能想象到的最大尺寸。好的，既然我们关注效率，通常我们希望减少存储量。我们将看到，随着精度的降低，你实际上既节省了内存，也节省了时间。因为在 16 位上进行运算会更快，假设速度能快一倍，不过这不总是成立，具体还得看情况。还有，通过降低内存需求，我们后面会看到，这其实也能节省时间。这点可能没那么明显，但希望后面能讲清楚。所以最直观的做法就是：行，我们把位数砍掉一半。现在你用的是 float16，它的格式是一位符号位、五位指数，剩下的全是尾数。所以，好吧，float16 还不错，除了一点：它的动态范围比较差。所以就算你试着构造一个 1e-5 的张量，结果实际上就是 0。所以你没法表示特别大的数，也没法表示特别小的数。原因在于这怎么都不对，就只有这么多次。你知道指数位只有 5 位，相比之下 FP32 是 8 位。所以如果你用 FP16 训练，以前大家确实这么做，就会遇到不稳定性。出现下溢、上溢和 NaN，这相当让人头疼。谢谢。于是 bfloat16 被发明出来。它实际上是在 2018 年开发的，就是为了解决这个问题。当时它的思路是：好，我们不在位数上做妥协，总位数和 FP16 保持一致，但我们会把一部分位从尾数挪到指数。也就是说，它的动态范围比浮点数更大，比如 16 位。它的动态范围其实和 float32 是一样的，但分辨率确实更差了，毕竟天下没有免费的午餐。但事实证明，在很多深度学习应用里，这种取舍是值得的。你需要确保动态范围不会溢出或下溢，而且因为数据本身就比较粗糙且带有随机性，你并不需要那么高的分辨率。好，让我直接跳过这部分。好，总结一下这堆训练有什么影响。所以用 32 位精度训练完全没问题。要是你训练的是个小模型，也不想操心这事，直接用 float32 就行。不过每个参数得占 4 个字节的内存，这挺占内存的。要是用 float16 训练，风险有点大。所以 BF16 算是个比较平衡的选择。哪怕 BF16 也有点风险，我们待会儿就知道了。谢谢。现在大家普遍的做法是用混合精度训练。让我整理一下这些点。好，我再重新编译一次。这些看起来是旧版本。所谓混合精度训练，就是让一部分计算用一种精度，另一部分计算用另一种精度。一般来说，BF16 用于参数、激活值和梯度，而优化器状态则使用 FP32，这点我们后面再详细讲。要启用混合精度训练，PyTorch 提供了一个 AMP 库。我们不会过多讨论这一点。你基本上只需把代码包装一下，PyTorch 里的库会自动处理。在安全的情况下，它会把数据转成 BF16。例如矩阵乘法通常是安全的，但如果你做指数运算，它会把数据保留为 FP32。好，我们可以用 BF16。我想这门课大概就讲到这里。但如果你想更深入探索，可以再往下走。所以 FP8 是 4 年前推出的，并且目前已经实现了标准化。所以 FP8 其实有两个版本，具体取决于你是更需要动态范围还是更高的精度。我们就不深入讨论这两个版本了，不过 NVIDIA 的某些 Transformer Engine 是支持 FP8 的。最近甚至可以用到 FP4。去年 NVIDIA 推出了 NVFP4，每个数值仅占 4 位。我们达成基本共识：4 位的数据量很少，我可以在这里的一行里列出从 -6 到 +6 的所有值。所以这精度并不高，不过这里其实有个技巧，对吧？因为如果你只是机械地直接使用这些值，模型是没法训练好的。也就是说，虽然每个值只有 4 位，但数据是按块组织的，每个块可以相应地向上或向下缩放。所以虽然能表示的数值变多了，但每个数值都无法覆盖完整的动态范围。今年刚发布了一个叫 Nemotron-3-Super 的模型，它是用 FP4 训练的，我觉得挺酷的。好的，有些内容你大概了解一下就行。还有些东西你根本碰不到，它不像你建一个张量，调用接口说“这是 FP4”，它就真的变成 FP4 了。这些底层工作大多是由 NVIDIA 的软件栈完成的。好，刚才稍微扯远了一点，但这或许有助于你理解精度这里面的门道。好，在我继续之前，有什么问题吗？请说。不好意思，这是个提问。比如当你处理一个块时，你会对该块涉及的所有数值进行同样的缩放，所以这些数值之间的比例关系在 FP4 下依然保持不变？对，所以这么做就是为了填入最大值，从而获得更高的精度？对，那这个问题问得好。那我再解释一下这个块。假设你有一个块，在这个块里，你可以在 4 位的范围内变化。另外所有这些值都可以根据缩放因子的位数进行上下缩放。所以如果你看单个数值，实际得到的动态范围超过了 4 位。但问题是你不能让这个值极大，而相邻的值极小。好的，那关于 1 位的问题，那如果是 1 位呢？训练显然有一条单独的路径，对，对。所以这可能正好是个切入点。所以问题在于 1 位精度，因为实际上无法再低于这个了。这里需要区分训练和推理。所以很多低位精度的内容，如果我们谈到量化，我想我们之后会讲到量化，量化是指你先以某个精度，甚至是 BF16 训练模型，然后将其量化到比如 1 位或 2 位。这比训练一个 1 位的语言模型要容易得多，而后者我甚至认为还没有人做过。也许这是可能的，但我认为还没有人在这方面训练出可信的成果。好，那我们继续。我们讨论了张量内存计算相当简单，就是元素数量乘以每个元素占用的内存。所以默认情况下，你在 PyTorch 中创建的张量会位于 CPU 上。还有，当然，如果你希望运行速度快，就需要把张量移到 GPU 上。其实这里有个小问题，这些幻灯片是在我的笔记本电脑上跑的，所以没有 GPU，因此有些代码我只展示，不实际运行。好，我们来看看……这部分可能没那么有意思，但我想大家都知道怎么操作，把张量移到 GPU 上，但一定要记得这么做，否则就拿不到加速效果。好，刚才我们讨论了张量的内存，这部分很直接。现在我们来谈谈用张量做计算。在讨论张量操作的浮点运算次数之前，我们先稍微岔开一下，聊聊 einops。在座有多少人熟悉或用过 einops？好，大概三分之二。好的，einops 的初衷是，对于那些还没用过它的人，原生操作很容易搞错，或者我觉得这种代码很难看懂。看到 x 和 y，接着是转置里的 -2 和 -1，你根本搞不清这两个负数代表什么。所以这大概就是使用变量名而非索引的动机。所以 einops 是一个用来处理维度带有名称的张量的库。这灵感来自爱因斯坦求和约定。有个不错的教程，大家可以看看。我只讲一些基础。基本上可以把 einsum 理解为一种带有良好索引管理的广义矩阵乘法。举个例子，我有一个 3×4 的矩阵，我有一个 4×3 的矩阵，如果做矩阵乘法，这其实是相当不错，这很容易理解。在 einops 中，基本上你可以说 x 有两个维度，行和列，我将它们命名为 seq_1 和 hidden。然后我会有个 y，它也是一个矩阵，包含所有行和列，我将它们命名为 hidden 和 seq_2。我将生成一个张量或矩阵，其维度由 seq_1 和 seq_2 索引。任何未在此处提及的维度，比如 hidden，将被求和消去。所以其工作原理是，我会遍历此处出现的所有变量，即 seq_1、hidden 和 seq_2 的所有可能取值并求和。我基本上会对 x 和 y 进行索引，将它们相乘，然后累加到结果 z 的 (seq_1, seq_2) 上。好的。好，所以你说这是……这可比刚才简单多了，对吧？来，我们看个更复杂的例子。还是刚才那个例子。现在我们有一个张量，它的形状是 2×3×4，另一个张量也是 2×3×4。如果我采用旧式做法，基本上我是在转置最后两个维度。然后由于隐式地对非矩阵乘法维度进行了批处理，你就得到了结果。懂了吗？你得稍微推理一下。einops 让这一点变得非常清晰。它明确标出了批处理维度：b seq_1 hidden, b seq_2 hidden，而我只需要输出 batch, seq_1, seq_2。懂了吗？注意，这里没有转置操作，因为某种意义上我通过命名就实现了转置。假如命名里写的是 hidden 和 seq_2，那也无需转置。我老是被转置搞糊涂，所以不用考虑转置这件事让我很开心。行，那如果你想写得精简点，可以这么说：对于 batch，我就用省略号代替。也就是说，假设我有个秩为浅的张量，有 8 个不同的批处理维度，我只要写省略号就行，不用一个个列出来。这在语言建模里很常见，因为你可能有批处理维度、序列维度、头维度，然后你想对这些维度都做矩阵运算。你肯定不想光为此发愁。好在你可以写模块化代码，用省略号表示，连输入张量的形状都不用管。好了，这就是 einsum，它属于 einops 库。还有一个 reduce 操作。简单来说，它是均值、最大值和最小值这些操作的通用形式。比如假设你有这个张量，想对它求和，依据 dim=-1 这个维度，也就是说在这里沿着最后一个维度求和。所以，我再次强调我不太喜欢这种写法，但你可以使用一种叫做 reduce 的操作。简单来说，这里指的是存在某些批处理维度，在这个例子中，就是前两个维度，然后还有一些隐藏维度，它们不会出现在等式右边，这意味着它们会被求和掉。这里我用了 sum 表示聚合归约操作是求和，但你也可以换成 mean, max 或 min。这样做能带来加速吗？这本质上归约为了同一种基本操作，你可以把它仅仅看作一种语法糖，所以性能应该是一样的。谢谢。最后我要讲的操作是 rearrange。我觉得这个工具相当强大。作业里应该会考到这一点。有时候某个维度其实包含了两个维度的信息，你需要针对其中某一个进行处理。之所以会出现这种情况，是因为我们有时会把矩阵展平，之后你可能需要将其还原或再次展平。它的工作原理是这样的：假设我有一个 3×8 的矩阵，但这里的这个维度，那个大小为 8 的维度，其实代表的是一个 2×4 的矩阵。所以，我想把这个 2×4 的矩阵和那个 4×4 的矩阵做乘法。所以我接下来要做的其实是调用 rearrange。我在这里的操作是声明：看，这是若干批次维度，在这里就对应第一个元素。然后我用括号来标记这一点。这里的 h 其实是 head 和 hidden 维度的乘积。显然分解方式有很多种，比如 2×4 或者 4×2。所以我设 head 数量为 2，这意味着 hidden 维度是 4。然后我可以把它拆分成 head 和 hidden 这两个维度。好的，所以这就形成了最终结果。在此之前，x 的形状是这样的，而现在 x 变成了这样。所以我想这可能会有点难看清。然后你就可以对 w 进行变换操作了。这就是我们之前见过的情况，执行标准矩阵乘法，其中 x 带有一些批处理维度。也就是隐藏层乘以隐藏层 2。接着，完成变换后，你就能把它重新排回去。这很直接，你主要看两个维度，然后把它们合并成一个维度。它是能把二维的东西转成一维，还是说有两种做法？你可以按行优先或列优先来做。对，所以问题是，你有一个一维的东西并转换它，抱歉，是你有一个二维的东西并转换为一维，你按哪种方式做。执行顺序由这里的 order 指定。好的，我是说刚开始适应它确实要花点时间，但很值得，因为一旦用上 einops，你的思维方式就会变得不一样。还有那些转置和规约操作，所有这些处理起来都会变得更顺畅，对吧？你必须深入思考这类更为定制的基元。好的，那么好，我们接下来要用 einops。我们稍后再细看。现在让我们回到资源核算的问题上。所以我们有了张量，我们已经讨论过它们如何占用内存，那么它们需要多少计算量呢？好的，那么我们将用来衡量计算成本的指标是浮点运算次数。一次浮点运算就是一次浮点操作，我们假设它是加法或乘法这样的基本运算。当然 GPU 还能做其他一些操作，但在大多数情况下我们将忽略它们，因为加法和乘法才是核心，会占用你大部分的时间。所以有件让我有点介意的事是，如果我说 flops 这个词，它的含义其实是有歧义的。一种是 FLOPs，指浮点运算次数，通常写作小写 s 的 FLOPs，这是衡量计算量的指标；另一种也是 FLOPS，指每秒浮点运算次数。有时它也会很容易混淆地写成大写 S 的 FLOPS，但我会一律写成 FLOP/s，以明确表示这是在衡量硬件速度。所以如果你去查，会发现 H100 有 989……每秒 Teraflops。而当我说到 GPT-3 消耗了 3e25 或者类似数量的 FLOPs 时，指的是前者 FLOPs。先把这个讲清楚。给你一个数量级的概念，当我提到 1e22、23 或 25 的 FLOPs 时，这些隐含指的是计算量，或者这些模型的规模。好的，所以如果你看 H100，如果你查看这份精美的规格书……

明书。实际上它并不在这一页。
好吧, 算了。有一份规格说明书, 它会告诉你BF16下每秒的浮点运算次数是多少。
1979, 然后你去进行基准测试, 结果发现, 等等, 这和我实际得到的数据不符。然后, 你去阅读细则会看到一条脚注, 说这是基于稀疏性的, 即稀疏矩阵, 而稠密矩阵则要除以2。
好的, 所以你必须总是将这些数值除以2。这就是为什么你会看到这些除以2的情况。
好的, 所以
所以, 这有助于我们建立一种
直觉。所以如果你有8张H100, 这就是一个节点, 持续两周,
那就是你所知道的8乘以两周内的秒数。
实际上, 这看起来是一周。行, 没问题。也就是一周乘以每秒的浮点运算次数。
所以, 大约是5e21。
这主要是为了帮你建立一种直觉, 了解特定硬件能提供多少浮点运算次数, 以及特定模型需要消耗多少浮点运算次数。
没什么复杂的, 就是数学, 粗略估算一下。好, 那现在我们来做一些更机械的计算。假设你有一个线性模型, 事实证明, 很多关于浮点运算次数的计算, 核心上其实就是线性矩阵乘法。所以这其实并不是一般性。这里有若干端点, 每个点都是d维的, 我们要进行映射。
把这些d维向量中的每一个映射成k维的输出。
好, B是点的数量, d是输入维度数, k是输出维度数。
好, 我们来构建一个X, 也就是数据矩阵, 维度是B乘d。
权重矩阵是d乘k。
当你做矩阵乘法时, 关键问题是, 这涉及多少浮点运算次数？
结果发现, 这其实是两倍那么多。
基本上就是这三个维度相乘。要理解这一点, 我们可以这样看:
每个三元组都要做一次乘法, 然后再做一次加法。
好的, 这里有-1, 因为如果被提问, 你就得实际加上类似的东西。
这种情况下是d-1乘以, 不过咱们先忽略它。好的, 如果觉得讲得太快, 我会再回来讲。我觉得还有另一种推导方式。谢谢。
那其他操作的浮点运算次数是多少呢？
逐元素操作的浮点运算次数就等于矩阵的大小。
我想这很清楚, 所以加法也需要nn次浮点运算。
所以一般来所, 对于足够大的矩阵, 你遇到的任何其他操作的开销, 都不如矩阵乘法大。
所以一般来所, 我们只关注矩阵乘法在做什么, 但在讨论内存时有一个重要的例外。
就像是纯粹出于兴趣, 其实还有一些其他算法可以用来做矩阵乘法。
这是否只适用于你所知道的常规做法, 也就是说, 存在其他进行矩阵乘法的算法？
是是是, 像Strassen算法那样的。所以一般来所, 人们从矩阵乘法中探索的优化算法像更多关乎如何与系统进行协同设计, 而不是这些更偏渐进性的算法。
算法, 对对。
如果把加法和乘法同等看待, 难道加法不能比乘法执行得更高效吗？
对, 所以我认为从硬件构建的方式来看, 两者基本上是一样的。但确实直觉上感觉加法应该比乘法做得更快。
从硬件构建的方式来看, 它们的计算开销是一样的。
对。
好, 你可以把这一次矩阵乘法理解为, B是数据点的数量。
而dk就是参数的数量, 对吧？记住, X的维度是B乘d, W的维度是d乘k。
所以理解这个公式的另一种方式是, 完成这个线性矩阵前向传播所需的浮点运算次数, 实际上是token数量, 或者说
数据点数量乘以参数数量。
好, 实际上这一结论可以推广到Transformer。如果你还记得6nd公式, 就能看出其结构是如何形成的。
好, 遗憾的是, 这些计算结果意义不大, 因为我是在CPU上跑的。不过我还是在这里把代码过一遍。
到目前为止, 我们测量的是浮点运算次数。
这与硬件无关, 它就是你的模型需要完成的计算量。
谢谢。那么现在的问题是, 在硬件上实际跑一次需要多长时间？所以, 一种办法就是直接计时。好, 在这门课里我想我们会用几讲来深入讨论基准测试。
不过先做个小预告。通常来说, 计时时, 尤其是在GPU上, 你得调用cuda.synchronize, 因为GPU是异步运行的。你要确保有这种同步点, 然后你执行操作, 操作之后你也必须设置同步屏障。如果你省略这一步, 你会发现, 哇, 你的计时结果非常快。这是因为这是一个非阻塞调用, 它直接返回了。通常来说, 多跑几次取平均值是个好习惯。
好, 所以, 所以你所以实际的每秒浮点运算次数
是
基本上是你执行的浮点运算次数, 再除以你在硬件上
记录的时间。
对吧？所以别忘了, 还有一个数据在GPU的规格表里。我试试能不能把这个链接调出来。
我好像放错链接了。
我得课后改一下。它本来会给出这个数字的。
一个浮点运算次数是989 TFLOPs, 所以实际每秒浮点运算次数通常和标称的每秒浮点运算次数不一样。
好,
还有, 行, 嗯。
哎呀, 抱歉。理解这种差异的一个概念叫做模型浮点运算利用率, 简称MFU。
MFU的定义就是实际每秒浮点运算次数除以标称的每秒浮点运算次数。
对, 我们暂且忽略了通信和其他开销。
好, 简单来说就是用实际值除以标称值。一般来说你很少能超过标称值, 我是说, 就是这样。没错, 你不可能超过标称值, 通常结果会更低。一般来说, 如果现代模型的MFU能达到0.5左右, 你就该挺满意了。如果你只是做纯粹的矩阵乘法, 可能能达到0.8, 甚至更高。
但通常达不到那么高。有时候如果真的出了大问题, 你只能拿到0.1左右, 这就说明你得做点什么了。
好, 谢谢。好, 所以每当你编写模型时, 你都可以计算。现在你已经知道如何通过统计模型所需的逻辑浮点运算次数来计算MFU, 然后查看实际耗时, 本质上就是做个除法。
Let me just go to next slide.
是的。
那边有人提问吗？我要翻到下一页幻灯片。所谓的承诺浮点运算次数是多少？
在规格表里, 它已经是1989这个数除以2之后的结果了。
此外, 通常你也只能拿到其中约一半, 具体取决于你的计算。
谢谢。你得到的是50%的吧？是的, 那么为什么你只能得到50%的MFU？这确实是个好问题。可能要引到内存瓶颈时我会再提这点。好, 总结一下。矩阵乘法通常是计算的主力, 这其实是特意设计的。
所以,
每秒能跑多少浮点运算次数取决于硬件, 所以硬件越好, 浮点运算次数越高, 但这还取决于数据类型。也就是说, 如果你看规格表会发现不同数据类型的浮点运算次数是不一样的。要是现在还用Float32, 速度会非常慢, 因为厂商根本没针对这种负载做优化, 而现在用BF16或FP8会快得多。好, 现在你该知道MFU是什么了。它就是实际浮点运算次数除以理论承诺的浮点运算次数。
Thank you.
谢谢。
好, 我们回到MFU为什么是0.5这个问题。要理解这一点, 我得先引入算术强度这个概念。
原因在于事情没那么简单,
不只是做一堆矩阵乘法就完事, 然后看看矩阵乘法花了多长时间。这是我画的一个非常简化的硬件示意图。你有HBM, 然后是计算核心所在的位置, 也就是加速器芯片的位置。那么计算是怎么进行的呢？你得自己动手算一下。三元组, 你的输入矩阵和张量都待在这里, 从内存流向加速器。你做完计算, 再把结果传回去。
好的, 那么如果你想衡量这要花多久, 这主要取决于两点: 第一是加速器的速度, 也就是我们刚才聊过的, 你知道具体指什么。
但另一个关键因素是硬件的内存带宽, 这点我们还没聊过。
如果你看规格表, 我们之前提过, 每秒浮点运算次数是多少？
1.979 * 10^12
除以2。再看每秒字节数, 也就是内存带宽, 这是每秒3.34。
好的, 还要记住, 我们为什么要关注内存？
存储这些数据需要多少空间？最明显的一点是, 如果你的模型太大, 它就放不下你的内存。
那可不是什么愉快的体验。而且事实证明, 内存这边也有讲究。你需要移动这些数据, 这需要时间, 因此实际上数据规模的大小也会影响速度。好的, 接下来我来讲讲一些运算。
我基本上会计算所需的时间, 并引入算术强度这个概念。好的, 假设我有一个100万维的BF16向量, 我将对其执行ReLU计算。请记住, ReLU就是对整个向量逐元素进行x与0的最大值运算。所以我要算两件事: 第一是移动了多少字节。
所以, 我得先读取x, 然后把它拷贝到加速器里。加速器, 每一份都是2字节, 因为BF16每个浮点数占两个字节, 再乘以N个浮点数。
所以这部分是2N。接下来我要把y写回去, 所以这里又是2N。
好, 这就是需要移动的字节数量。那么完成了多少浮点运算次数？
对于其中的每个元素, 我只是把它和0做个比较, 就这些。
所以这是N次比较。
好的, 所以现在来看通信时间, 也就是字节数量,
也就是我移动的距离除以移动速度。
这样算出来时间是1微秒。那计算时间呢, 用浮点运算次数除以每秒浮点运算次数,
所以是1纳秒。
好。
此外还有一个重要假设, 我们通常会尽量满足, 那就是我们让通信和计算重叠进行, 等后面深入讲GPU时再详细讨论。
其核心思想是, 在这种情况下, 我们不会做等数据传输完成, 数据一到就开发计算, 紧接着继续往下走。
再把数据传回去, 这样数据传输和计算就同时进行了。
所以从数学上讲, 我们就假设总时间是取两者的最大值。因为我们假设这两部分可以完美重叠, 但在实际中重叠不可能那么完美,
会有一些开销, 但目前这样已经够用了。好, 所以总时间,
大家看, 这里是1e-6。
好。
那如果问瓶颈在哪？当通信时间大于计算时间时, 我们就称之为算法内存受限。
因为你大部分时间都在等数据过来。而当计算时间大于通信时间时, 那就是计算受限。因为这时候你的瓶颈其实就在于做计算。那么在这种情况下, ReLU是什么？
谢谢。
抱歉。Memory bound, 是内存受限还是计算受限？内存受限。极度内存受限, 对, 这很明显, 对吧？
因为计算量远小于通信量。
好, 换个角度来看。这里我要定义强度, 那么加速器的强度是什么？它本质上是指加速器每传输一个字节能完成多少工作量。
对于任何加速器, 从规格表上你基本上能拿到每秒浮点运算次数除以每秒的字节数。
好,
那么每搬运一个字节, 你能做多少有用的计算？对于H100,
这个数值是295。
也就是说, 每传输一个字节的数据, 可以执行295次,
就能进行295次浮点运算。好的, 这是个让你心里有数的直观数字, 大概300左右。好的, 接下来讲算术强度。
它衡量的是算法在处理这向任务时, 每字节数据背后实际做了多少计算。
以ReLU计算为例, 它的算术强度就是浮点运算次数除以字节数。
实际上这个数值我想是1/4, 而不是1/2。所以关键在于它非常小, 只有0.25。好的, 现在我们可以用强度这个概念来讨论瓶颈问题。如果算术强度低于加速器的临界值, 就是内存受限；如果高于这个值, 就是计算受限。
所以这两者是等价的, 你看一下代数推导其实就是两个分数通过乘除把项的位置换一下。
好。
那这种情况下呢,
你知道,
我们是显存受限的。
所以一般来所, 我们会发现自己处于显存受限的状态, 因为数据搬运的代价很高。
所以如果能提高算术强度, 那就更好了。
好,
所以0.2, 如果你看到这个数, 或者别人告诉你你的算术强度是0.25,
你就该意识到这真的很糟糕。
对。那典型的算术强度大概是多少呢？对, 我们呆会儿会讲到。好, 那么提升算术强度的一种思路是, 我们试着在每搬运一个字节的数据时多做一些计算。
好, GELU是另一种激活函数。它的公式大概长这样, 看起来更复杂一些。里面没有0, 要是算一下的话, 来回搬运的字节数还是2N+2N。再算浮点运算次数, 对于大规模的逐元素유形运算, 每个元素大概有20次浮点运算, 所以总共是20N。
所以算术强度大概是5, 这只是一个粗略的估算。
那在这种情况下, 我们是内存受限还是计算受限？
对吧？因为5还是比295小, 小得多。所以虽然GELU做的计算比ReLU多得多, 但按目前的结构来看, 它仍然是内存受限的。
对, 也就是说如果你只看ReLU和GELU, 你可能会觉得GELU那么复杂, 开销一定很大, 但实际上两者开销完全一样。
Thank you.
对, 因为瓶颈并不在那里。
谢谢。好, 接下来我们看几个线性运算, 比如点积。好, 点积, 假设你有一个向量x和一个大小为N的向量w, 然后计算它们的点积。
那么数据移动量是多少？读取x用2N字节, 读取w用2N字节, 然后写入y一个2字节的标量。
那浮点运算次数呢, 你做了N次乘法和N-1次加法, 总共是2N-1次。好, 那算术强度是多少？
这个大约是几秒？5。
这也相当低, 希望你明白了, 它属于内存受限型计算。
谢谢。
好, 那矩阵向量乘法呢？
好, y = x与W的乘积,
其中x是向量,
W是一个m乘n的矩阵, 然后你计算这个乘积。
大家觉得这会是计算瓶颈还是内存瓶颈？举手示意一下。
好, 我们来看。我要读取x_tot大小2N, 读取W_BR大小2N^2, 然后写入y大小2N。
浮点运算次数基本上就是N次点积的开销。
所以总成本是N乘以一次点积的开销。
算术强度也只是略高一点。
所以这同样是内存瓶颈。好, 现在我们来讨论矩阵乘法, 这才是有意思的地方。我有一个N乘N的矩阵, 再乘以另一个N乘N的矩阵。传输的数据量大约是2N^2 + 2N^2字节。
然后我必须把2N^2字节写回y。
这里的浮点运算次数是N^2次点积。
那算术强度是多少？是300？太棒了。好的, 所以是7.4。一般来说它大约是3/3。
直觉上这很合理, 因为你传输的是N^2量级的数据, 但运算量却是N^3。
所以计算量相对于传输量的比例是N的量级。
谢谢, 而且矩阵越大效果越好。
这也就是为什么推荐后面增大批量或使用更大的矩阵, 原因就在这里。
如果你低于加速器的算术强度, 那么缩小规模实际上并不能加速运算。
情况都差不多, 但一旦你超过这个点, 你实际上就已经处于饱和状态了。
G pass GP。好, 所以到这步终于算密集型了。
好, 所以只要有大型矩阵, 我们的效率其实挺高的。
我们处于计算密集型状态, 加速器已经满载。那之前提到的Transformer问题呢？事实证明你们在作业和下一讲中都会看到, Transformer实际上是, 本质上就是大型矩阵乘法中间穿插了一些其他操作。所以从算术强度来看这是个好消息。
这是经过刻意设计的, Transformer的结构经过特定设计以实现高算术强度。这里提一点, 为接下来的推理讲座做个铺垫。矩阵向量乘法本质上大致就是你做Transformer推理时发生的事情。因为推理时你是一次生成一个token, 所以这就像是用一个向量与矩阵做点积。
正如我们所见, 这受限于内存带宽。而在训练时, 你会获得整个序列并一次性进行处理。
好的, 此外请注意, 计算强度取决于精度。因此这里我们默认使用的都是BF16。
你
好的, 那么把这一点和之前关于MFU的问题联系起来, 你MFU较低的原因可能是虽然你看似在做, 是, MFU是承诺浮点运算次数, 抱歉, 是实际值除以承诺值。因此如果你存在严重的内存带宽瓶颈, 那么实际上你无法获得理想的吞吐量, 尽管承诺的前提是如果没有内存瓶颈, 你本来能一路把所有这些都做完。
Thank you. Thank you.
模型的运算。谢谢, 谢谢。
好, 关于算术强度最后讲一下屋顶线图。所以有个挺好的办法, 对吧？我再重复一遍。
一般来说, 正如大家所说, 这些模型在大部分计算中都是受内存限制的。不过,
如果加速器利用率只有50%, 那就不太理想。要是能到70%, 甚至80%, 那就相当不错了。
这意味加速器的规模相对于内存来说过大了。我呆会儿会再讲这个问题。
为什么会这样？为什么这些加速器做得这么大, 结果只是在那闲置或等待内存？
所以问题是我们能不能设计出特性更好的加速器。对, 也许等我们讨论完GPU, 对它们的工作原理有更多了解之后, 就能解释为什么会这样了。
不过如果你有答案不妨告诉Jensen, 说不定你还能设计出更好的硬件。
好, 我们来直观地看看算术强度和性能之间的关系。这个图基本上是把算术强度画在了x轴上。所以这里每一个垂直切片都对应着比如说某种算法。
然后我们有这些线,
每条线对应一种特定的加速器, 比如H100或B200。因此, 它展示的是在y轴上实际达到的每秒浮点运算次数。
所以如果你的算法算术强度较低, 比如ReLU或点积, 你就处在这个区域。这意味着实际

达到的每秒浮点运算次数会低于加速器的峰值性能。而当你提升内存带宽，增加算术强度时，情况会逐步好转。在达到某个临界点之前，你都能充分利用硬件性能，超过那个点之后，计算就成为瓶颈。显然你无法超过峰值浮点运算次数。好的，那我们继续往下讲。好，谢谢。接下来我要回过头来谈谈内存和计算，也就是我们在训练中需要的那些操作。到目前为止，我们做的是张量操作，基本上就是矩阵乘法。我们看到了它需要多少内存，多少计算，以及两者之间的相互作用。现在我们来具体看看训练到底需要什么，就拿我们刚才那个例子来说吧。其实这不是线性网络，而是深度网络，所以我设想这样一个场景：输入是 B 乘 D 的矩阵，然后经过若干层，每一层都做 D 乘 D 的矩阵乘法路径，然后生成一些一组预激活值，接着做逐元素的 ReLU 运算，得到第一层的激活值，然后这个过程不断重复。好的，输出尺寸保持不变。好的，所以这个深度网络，为了看看它在 PyTorch 里长什么样，基本上就是一组模块，每个模块都有一个与之关联的权重向量，抱歉是权重矩阵。好的，所以这里的参数量是 D 的平方乘以层数 L。当你在一个数据批次上运行模型时，我们做的是逐层处理，每一层我们基本上先做线性变换，然后再做一个逐点运算，使用 ReLU 激活函数，然后对所有层都这么做。好，希望这里很清楚。这是一个非常简单的模型。好，现在我们来谈谈梯度，所以我们用一个更简单的例子，这就是一个简单的线性回归模型。我们有一个向量 1 2 3，一个权重向量 1 1 1，我们计算点积，然后构建这个均方误差 MSE 损失。当我们执行反向传播时，计算图中涉及的每个变量，它的梯度要么被赋值，要么没被赋值，所以 w.grad 被赋值为 1 2 3。好，这只是 PyTorch 的基本机制，这对大家来说应该都很熟悉。那么现在的问题是计算梯度需要多少浮点运算次数？好，我们来计算梯度所需的浮点运算次数。我们考虑一个简化模型，其中你有一个 B 乘 D 的 X 乘以 W1 矩阵，为简化起见，我们暂时忽略这个 ReLU，再乘以 W2，它是一个形状相同的 D 乘 D 矩阵。好，出于后面会讲清楚的原因，这里我使用 einsum。在这个线性网络中，你做的第一件事是取出 X 和 W1，X 是批次大小乘输入维度，W1 是输入维度乘输出维度，得到的是批次大小乘输出维度。H2 取 H1 和 W2，情况大致相同。这些只是矩阵乘法，然后你形成一个损失，我只是把它传给某个任意操作，以便得到一个数值。好的，那么在反向传播过程中会发生什么？为了调试方便，我把这些叫做保留梯度，你后面会看到。你对损失执行反向传播，问题是：那做了多少工作，也就是说那是多少浮点运算次数？好的，让我们放大查看一层，让我们专注于这里的第二层，即这一层。第二层接受 H1，然后直接乘以矩阵 W2 得到 H2，就这样。所以如果你看前向传播里的浮点运算次数，这仅仅是个矩阵乘法，我们只需要把这三个维度乘在一起。BF16 占两个字节，抱歉，这个细节不重要。这里算两次，因为包含一次加法和一次乘法。好的，那反向传播阶段是怎样的呢？好的，在反向传播中，如果你还记得链式法则和反向传播算法，你需要计算两个量。你需要计算反向信号，也就是损失函数相对于输入的梯度，同时你还需要计算相对于参数的梯度。清楚吗？这些梯度的具体形式是什么？这直接由定义得出。这其实就是用爱因斯坦求和符号写出的链式法则。你计算损失对 H2 的导数，DH2，也就是 H2 的梯度。这是一个批大小乘以输出维度的矩阵，然后你把它乘以 W2。W2 是输入维度乘以输出维度。接着，这就得到了 H1 的梯度，也就是损失对 H1 的导数，DH1。维度是批大小乘输入维度。好，我每次看到这个总会搞混。如果你学过微积分，里面会出现一个转置，我总是记不清它们该按什么顺序排。我觉得爱因斯坦的记法把这件事讲得很清楚，因为只要你记得标量情形下它应该是什么样子，就知道它必须长成那样。H1 的梯度就是 H2 的梯度乘以 W2，唯一的问题是你怎么给这些元素加索引。你基本上是通过看这些张量的形状和命名维度来给它们加索引的。这种情况下，它就是一个矩阵乘法，你在输出维度上做求和。行，照这么操作的话，我们就能验证一下。所以 h1.grad 是 loss.backward 算出来的，这跟我在这儿手写的结果是一样的，就当做个 sanity check 吧。好，那你还得算另一件事：对参数的梯度，也就是 d_w2 乘 H1。所以这其实是同一种反向传播信号，只不过它是乘上了你没求导的那部分。你知道就是对应的那个梯度，你直接把维度列出来就行，这里你要对 batch 维度求和。谢谢。好，那么好处在于如果你只看这里，我们就知道它的计算成本了。浮点运算次数基本上就是所有维度相乘的结果。不管你有没有做 batch，这都不影响，本质上你就是在遍历 i j k 并进行聚合。聚合方式不同，但浮点运算次数是相同的。请注意，反向传播的开销恰好是前向传播的两倍。原因在于你需要计算两类梯度，一类是针对每个参数的梯度，另一类是关于输入的梯度，也就是那个非参数的部分。好。也许我该停一下看看有没有问题。听得，谢谢。谢谢，嗯，好。好，那么刚才讲的只是针对 W2 的情况，你只需要把这套方法应用到网络中的所有参数上，然后汇总起来就行。你会发现，对于该网络，前向传播的计算量是两倍，即数据点数量 B 乘以参数量的浮点运算次数。反向传播的计算量是前向传播的两倍，即数据点数量乘以参数数量的四倍浮点运算次数。因此，总计算量为 2 + 4 = 6，即数据点数量与参数数量乘积的 6 倍。这就是你在各种地方可能见过的 6ND 这一说法的来源。这仅仅是通过统计前向传播和反向传播得出的。每一次我们仅针对这些深层网络进行了上述计算，这是可行的。但事实证明，只要上下文长度不太大，这对 Transformer 来说也是一个良好的近似。如果上下文长度过大，就会出现上下文长度的平方项，这属于此类估算中未包含的额外浮点运算次数。好，那么我们就稍微聊一聊吧。现在我们有了梯度，接下来在训练过程中另一项必要工作是执行优化。因此，这里展示的是我们的深度网络。为了避免只是重复作业一里的内容，我打算使用 AdaGrad 优化器。该优化器来自 2011 年，比 Adam 还早。你可以把它理解为介于 SGD 和 Adam 之间的方法。基本上它是在 SGD 基础上考察梯度的二阶矩。动量 momentum 考察的是梯度的一阶矩，而 Adam 则是将两者结合起来。由于我们主要关注使用方法，因此这里不展开优化器的具体细节。因此我们可以定义一个优化器，然后当你计算梯度时，接下来计算梯度，然后进行优化器的一步更新。如果你要自定义优化器，比如在第一次作业里你需要实现 Adam，针对每个参数组，比如这里的 W1 或 W2，我们要看一个叫优化器状态的东西，它本质上是一种存储。优化器在运行时就会用到这种存储。在 AdaGrad 里我们算的是梯度的平方，也就是所有梯度平方的总和。那这里我们从优化器状态里取出这个值，我们用当前的梯度去更新它，然后再存回去。接着更新完 g2 之后，你就去更新参数。懂了吗？在 AdaGrad 中，你基本上是用梯度平方的均值或总和的平方根来做除法。Thank you. 你，哎好，谢谢。让我看看，我可能得跳过前面那部分了。你知道的就是训练那块儿，看，其实，等等，等一下。我想我好像漏讲了一点，不是故意的。好，刚才说的是优化器状态，现在来看看优化器占用了多少显存。占用了多少或者更一般的说内存用量是多少？以参数为例，在这个网络中，L 层，每层有 D 的平方个参数。如果我们用 BF16 存储，每个参数占两个字节，这就是参数的总量。激活值，这个嘛，你知道的。好，两倍，这是针对 BF16 的，即批次大小乘以 D 再乘以层数。每一层都有激活值以及梯度，它基本上是所有参数的副本，这些都是 BF16。优化器状态则是针对每一项保存的。实际上这应该是，我想这里写错了，抱歉，我会修正这个笔误。这里指的应该是参数数量，而不是参数内存。嗯，是参数数量。所以是参数数量的两倍，然后是参数数量的四倍。原因在于为了保持稳定，惯例上会使用 FP32 来存储优化器状态。显然有人试过用 BF16，结果发现因为涉及平方运算和跨步平均，这样并不太稳定。并且以 AdaGrad 为例，这里每个参数需要四个字节来存储优化器状态，Adam 优化器需要存储一阶矩和二阶矩，因此占用八个字节。因此优化器状态实际上占用了大量内存。这里有一个注意事项：请记住，内存主要承担两种功能：一是你需要在 HBM 中存储这些数据，二是这些数据必须传输到加速器。总体而言，优化器状态通常不是计算的瓶颈。因此这里的内存量对于速度方面的性能并非至关重要，但它意味着你无法在内存中容纳大型模型。好的，综上所述，如前所述，参数数量为 D 乘以 D 乘以 L，浮点运算次数为 D 乘以 token 数量或数据点数量再乘以参数数量。因此，对于 Transformer 架构，情况会稍显复杂，但这将在作业一中涉及，且你会进行更细致的推导。好的，咱们跳过训练循环，这算是个总体回顾。Thank you. 结束前我想快速提两点。第一点，谢谢。正如所见，显存不仅影响存储大模型的能力，有时还影响速度。所以一般要尽量减少显存占用。大家常做的两件事里，一项叫梯度累积。一般来说批次大小要足够大以提高稳定性，直到达到临界批次大小。Tatsu 后面会讲这个。正如我们看到的，激活显存会随批次大小增加。所以在某些情况下，你可能会显存不够。如果批次大小太大，梯度累积的做法就是把一个批次拆成若干个微批次，在每个微批次上计算梯度，然后把梯度累加起来。不用把梯度清零，每隔批次大小除以微批次大小步更新参数并清零梯度。因此，这其实是一个非常简单的代码改动，就能节省算力。另外，我快速提一下激活检查点 activation checkpointing。在训练时，我们需要存储，一般来是存储所有层的激活值，默认就是这么做的。有意思的是在推理时我们不需要计算梯度，所以只需存储当前层的激活值。但在训练时，内存占用是 B 乘以 D 乘以 2 乘以 L。那么问题是能不能把这个降下来？所以激活检查点也叫梯度检查点或重计算 rematerialization，其核心思想是在在前向传播中，你只保留其中一部分层的激活值。反向传播时，你从最近的检查点重新计算缺失的激活值。这就是它被称为检查点的原因。这是系统中常见的通用技巧，本质就是一种权衡。如果你想减少内存，重新计算就行，所以我们要把这个想法具体实现出来。这其实相当，其实很简单，我没，好，可能是我没看到那一部分，好。基本上你在这里做的是用同一个模型，只是有处不同。你只需在某一层上加上 torch.utils.checkpoint，意思就是执行这个计算，但不存储任何中间的激活值，只存储需要的部分。所以这意味着，如果你存储所有激活值，记住在深度网络里有 ReLU 之前的值和 ReLU 之后的值，这就占用了大量存储。换个做法，在这些由线性层和 ReLU 组成的块上使用激活检查点，不保存 ReLU 之前的激活值，基本上就能省下一半内存。这样内存就能省下一半，然后做反向传播时，虽然需要 G3，但你可以很容易地从 H2 算出 G3。谢谢。好，你还可以做得更彻底。你可以说：“好吧，我可以把所有层都存下来，但在极端情况下，我也可以一层都不存。”这样内存利用率就达到最高，唯一的问题是计算量会变成 L 的平方，因为每一层都得从头算起。也许一个折中方案是在根号 L 层储存检查点，这样激活值占用的内存是根号 L，重新计算的开销也是根号 L。这样就平衡了。谢谢。好，总结一下这节课。所有操作都是在张量上进行的：参数、梯度、激活值、优化状态数据。我们介绍了 einops，希望大家能把它当做理解张量运算的一种思路。数据点数乘参数数再乘 6，这个公式我们现在已经搞清楚它代表的是每步的浮点运算次数。如果是训练步骤的话，这里的变量应该是指批次大小。然后我们讲了算术强度和屋顶线分析，借此我们可以判断计算瓶颈是在内存上还是在算力上。矩阵微批处理属于计算受限，而基本上其他情况都是内存受限。最后，梯度累加和激活检查点技术都是用来节省内存的方法。节省了内存，你就能能够使用更大的批次大小。好，今天的课就讲到这里，下周 Tatsu 会讲架构相关的内容。
