原本的計劃是深入探討強化學習,但考慮到課時有限,如果不介紹多模態,這門課就顯得不夠完整。畢竟你看如今的主流模型幾乎都具備多模態能力。所以今天,咱們就大致overview一下多模態模型。當然,這部分內容單開一門課都綽綽有餘,但我盡量在這一節課里把能講的核心內容都涵蓋進去。好,咱們開始深入多模態模型。到目前為止,這門課一直只關注語言模型。語言模型其實已經非常通用,它能實現從任意文本到任意文本的轉換。比如,它可以處理各種語言、代碼、詩歌,或者任何你喜歡的文本形式,甚至像DNA序列這樣的數據。但現實世界是多模態的。我們面對的數據包括文本、 圖像、音頻和視頻。咱們想想看,最終想要達到北極星目標,也就是大家所說的全模態模型, 它能接收任意模態組合的輸入,也能輸出任意模態組合的結果。打個比方,我給你一張圖像和一個視頻,然後問你關於它們的問題,我能生成圖像,甚至能把音頻轉換成圖像。有了全模態模型,你就能想到各種各樣的應用場景。這門課我不會帶大家從頭搭建一個全模態模型,但我會給你一些關鍵組件,讓你至少能上手處理一些數據,以圖像數據為例, 這也是今天討論的重點。 那麼如何構建這樣的系統呢?這聽起來可能有些抽象,但事實是Transformer確實行之有效。儘管人們嘗試過許多其他方法,但在處理多模態大規模任務時,它依然是我們目前的最優選擇。 因此我們需要找到充分發揮其優勢的方法。 需要注意的是,Transformer最初是為文本任務設計的。它具備處理token的特性,即輸入是一組token,輸出也是若干token。 在此,我將token的概念進行了擴展,它不再局限於文本中離散的token,還可能包含連續的token。 你可以將這些視為token的嵌入形式, 核心在於token必須代表某種語意信息單元。例如在自然語言中,token是詞,詞具有一定的意義,但像像素這樣的元素,單獨來看是沒有意義的。 因此,我們需要將音頻、圖像等數據轉化為離散或連續的token。 值得注意的是,對於文本數據,我們早已採用這種做法。 在第一講中我們討論過分詞。當時的情況尚可,因為我們使用了BPE分詞器,並且大家也親手實現過。這雖然勉強可用,但大家心中或許都期待有更優秀的分詞器出現,或者乾脆不用分詞器,這倒也不是什麼大問題。處理非文本模態時,咱們得費點腦筋,想清楚怎麼把BPE分詞器的邏輯遷移過來,讓圖像能變成Transformer能處理的形式。好, 這裡咱們得解決兩個問題。第一,面對圖像、視頻或音頻這些非文本數據,咱們該怎麼把它們餵給Transformer?第二是怎麼把它們生成出來。咱們這節課主要先講第一個問題。好, 咱們往後倒帶,回到2021年。CLIP模型, 大家可能聽說過它是現代VLMs的許多基礎, 所以咱們可以稍微深入一點來看這個例子。CLIP的全稱是“對比語言-圖像預訓練”。稍微回顧一下歷史背景,那時候GPT-3已經問世,GPT-4以及所有語言模型都已宣進入了基礎模型時代。不過視覺領域當時還融絡有些嘗試, 但傳統做法主要依賴大量標註的數據集,比如ImageNet,研究人員在這些數據集上訓練ResNet等模型,配合數據增強等手段,最終取得了非常出色的性能。因此,OpenAI的研究人員提出一個問題,能否利用海量的圖像及其文本描述,受語言領域這一思路的啟發,只需抓取互聯網上的文本,儘管數據噪聲很大,但只要訓練足夠大的模型, 就能從中提取意義,並生成有用的內容。 那麼圖像領域的等效方案是什麼? 於是, CLIP應運而生。CLIP的核心思路其實很簡單、很直接。那麼針對圖像對應的做法是什麼呢?於是CLIP就這樣誕生了。CLIP的核心思路其實很簡單、很直接。這樣一來我就得到了32000個圖像編碼,對應的文本我也會做同樣的操作,從而得到T1到T,這些是文本嵌入,那些則是圖像嵌入。接下來我要設定一個目標。 以圖像I1為例, I1和T1是配對的, 所以我要讓這兩個嵌入向量的點積遠遠大於I1與其他所有文本嵌入的點積。 同理,對於這段文本我也希望如此,也就是說這個點積的值要大於T1與其餘所有圖像點積的值。簡單總結一下,這就是CLIP的目標,你可以把它想像成執行了2n次不同的softmax分類。來看代碼, 這裡你會得到一個m×d的矩陣來表示圖像編碼,另一個m×d的矩陣表示文本嵌入,先做歸一化,再計算點積,這裡會用到溫度係數,接著計算交叉熵。好的, 所以這基本上就是一個多類分類問題, 樣本被組織成了m×n的矩陣形式。 好。 大家有什麼問題嗎? 好。 那咱們繼續。 接下來講講CLIP構建的一些細節。數據是從哪來的?論文裡關於數據部分的細節並不多,簡單來說就是先收集一批查詢詞,從某個來源獲取這些查詢詞後去網上搜索,從中挖掘出大量的圖文, 謝謝,最終得到了4億個圖文。該數據集並未公開,不過有人嘗試過復現,有一篇名為 OpenCLIP的論文進行了復現,並進一步擴展了CLIP的思路。例如,發佈了一個名為LAION-5B的數據集,包含50億張帶文本描述的圖像。期間還進行了一些數據處理,有趣的是,他們先用CLIP進行數據篩選,再訓練OpenCLIP,這裡存在一種自舉過程,但至少這是一個能明確指明訓練數據集的模型。期間,還進行了一些數據處理。 有趣的是, 他們先用CLIP進行數據篩選, 再訓練OpenCLIP,這裡存在一種自舉過程, 但至少這是一個能明確指明訓練數據集的模型。 還有代碼。 好,咱們再補充幾個細節。呃圖像分辨率五花八門,對吧?網上隨便找張圖,長寬比可能很不固定,要麼細長,要麼瘦長,尺寸完全隨意。另外,學神經網絡時,你會知道它們不喜歡動態尺寸,偏好固定大小的輸入, 所以通常會做個啟髮式的預處理,把圖像縮放,讓短邊變成336或者224這類目標尺寸,接著做中心裁剪,把它變成正方形。通常目標位於畫面中央,因此只需裁剪掉背景即可,所以這倒不是問題。 好。 那視覺編碼器到底是什麼? 這是通用算法,但我尚未詳細解釋圖像編碼器和文本編碼器具體含義。 對, 文本 會 輸出 序列的 末尾 其實它大同小異,因為它要推測某黑種的鍵,甚至要考慮到背後都有相應的經驗過程。這這是他們使用圖對進行訓練,而不是單獨使用圖像的原因。 我認為核心問題在於為何他們採用圖文對訓練,而不是單獨使用圖片,謝謝。 有一類研究致力於將圖像編碼為表示形式。謝謝。有一類研究致力於將圖像編碼為表示形式,它們被視為同一張圖像,因此應具有相似的嵌入表示。不過我們接著往下說,這種方法對捕捉低級細節很有用,但問題是僅靠數據增強,你無法將一隻狗變成另一種狗, 比如從一種狗變成另一種狗。引入文本就能提供更高階的語義信息用於表徵圖像。 這是個很好的問題,稍後我們會看到,結合具體任務類型,這些語義表徵能發揮多大作用。那麼CLIP使用的視覺編碼器是什麼?他們嘗試過多種架構,例如ResNet和當時剛發佈的視覺Transformer。實驗結果表明,視覺Transformer的效果最佳,因此人們提到CLIP時,通常指的就是基於ViT的那個版本。視覺Transformer的處理流程如下, 我們先取一張圖像, 然後將其切分為小塊。原始論文中採用的是16×16的塊大小,而CLIP使用的是14×14。無論尺寸如何,這些小塊本質上都是向量,因此每個小塊在某種程度上就是它的token。對於視覺Transformer,你會像訓練語言模型那樣加入位置嵌入,然後將其輸入到一個標準的Transformer中。如果你要進行分類任務,通常的做法是取出序列的第一個token作為分類表示。Transformer編碼器會輸出一組向量序列。以Transformer為例, 如果你要做分類任務,通常會取出序列的第一個token作為分類表示。通常的做法是Transformer編碼器會輸出一個向量序列,具體來說,先對所有激活值做全局平均,再用這個查詢向量與每個位置的鍵和值進行一輪注意力計算,這樣得到的向量可能比直接求平均包含更多信息。 謝謝。 好,這就是ViT。CLIP文發現效果最好的模型是ViT-L14。這個名稱的意思是這是一個大型ViT,我估計它大概有24層,不太確定,使用的是14×14的patch。每個patch都是RGB的,所以有三個通道。他們使用336×336分辨率的圖片進行訓練。實際上他們先用低分辨率圖片訓練,然後再upscale,這主要是為了提速,畢竟高分辨率圖片太耗時,在後續訓練階段他們才使用高分辨率圖片。 對。 那麼位置編碼在文本處理中是否會更複雜一些? 比如橫坐標是一維的,而圖像則包含上下、左右等方向信息。 沒錯。 所以核心問題在於,

能否设计更智能的位置嵌入方法？因为目前采用的只是0到9的线性编码。我认为CLIP这篇论文其实尝试过该方法的二维和一维版本，结果显示两者差异并不显著。在解读这些结果时，需要意识到它们主要属于有趣的分类实验，因此，对于分类任务而言，这一点或许并不关键。稍后我们会介绍更高级的位置嵌入方法，这些方法会考虑空间结构。接下来，

我们来看文本编码器，它采用标准的Transformer架构。由于开发团队相同，这里使用的是GPT-2风格的Transformer。为了从序列中提取一个向量，他们在序列开头添加BOS，结尾添加EOS，然后取最后一层中EOS位置的激活值。现在，我们拥有了视觉编码器和文本编码器。随后，即可利用这一流程来训练模型，它代表了整个序列。现在我们有了视觉编码器和文本编码器，明白了吗？

因此，CLIP在2021年引发广泛关注的关键成果是，在ImageNet基准测试中实现了零样本分类。它的表现超越了在120万张图像上训练的模型。注意，是图像（images），而非图片。我们可以想一下，处理120万张ImageNet图像，需要亚马逊机械托尔克（Amazon Mechanical Turk）的工人投入大量工时进行数据清洗。而现在你使用的是在更自然的网络数据上训练的CLIP。诚然，这确实利用了网络上众人的劳动，但既然能直接利用这些现成数据，就能直接进行零样本推理。

零样本技术的基本原理是，虽然这里没有幻灯片展示，但逻辑很简单：你拿一张图，再准备一堆文本，然后计算它们与各个标签的点积，看谁的得分最高。我们继续。

对，确实是这样。抱歉，这部分在侧面。好的，好问题，所以这是零样本预测。嗯……就是你拿一张图片，然后你……对，就像我之前说的。

有问题吗？是的。就像我们遇到了一个问题，比如，就比如一张狗的图片，对应四个标题，也许不只是一个简单的标题说是狗，也许其他标题也和狗有关。这会让模型产生混淆吗？没错。所以问题是，如果你有一只狗，也许还有其他标题也提到狗，这会混淆模型吗？

嗯，一般来说，这个过程是有噪声的。嗯，如果有另一只狗也没关系，因为平均来看，嗯，不太可能总是出现狗，有时会是苹果或猫之类得东西。还有一点是，这些图片是……嗯，如果有一只狗也没关系，因为平均来看，不太可能总是出现狗。还有一点是这些图片是，嗯，并不一定只是如实描述图像内容。没错，因为如果图像是一只狗，你就不需要特意说明这是一只狗，因此这一过程噪声很大。也许令人有些意外，但有趣的是，它确实有效。我的意思是，需要进行大量的数据过滤。因此，如果随意选取网络上的图像和文本，噪声可能会过大。

还有一点需要注意，这与我将在最后回到的一点有关。他们还尝试了另一种替代方案，即不进行这种排序，而是从图像中预测文本。因此，他们可以设定一个目标，尝试从图像中预测文本，既可以作为词袋（Bag-of-Words）形式。他们还尝试了另一种思路：放弃排序，改为让模型根据图像直接预测文本。于是，他们设定了一个目标，即让模型根据图像预测文本，比如把文本当做词袋（Bag-of-Words）来处理。这和CLIP使用的词袋方法不同。所以我的观点是，对于你真正想学习的表示，或者说看ImageNet的准确率，也就是分类任务的表现，实际上，精确建模字幕的token序列，对于获取图像的粗略表示来说，并没有那么关键。好，我们稍后再回到这一点。

那么，到目前为止我们学到了什么？我们已经对图像进行了编码，使用CLIP，它能捕捉图像的语义，因为它与文本配对，而文本通常描述的正视语义。需要指出的是，这里的设计决策是基于图像分类的，因此，它的粒度并不十分精细。但正如我们将要提到的，它仍然为我们后续要做得所有事情提供了一个稳健的起点。

CLIP在技术上的一个缺点是它需要较大的批量大小，比如30000。如果你的批量大小仅为1，显然它无法工作，即使是10也不行。此外，Softmax操作是在整个批量上进行的，因此它缺乏良好的可分解性。相比之下，在常规语言模型训练中，一批样本中的所有序列会被并行处理，最后只需进行聚合。

这一问题将由谷歌发布的另一篇名为SigLIP的论文来解决。因此，你可以将其基本视为CLIP的改进版本。它代表用于语言图像预训练的Sigmoid损失。这里的主要区别在于，CLIP执行的是多类别分类，其逻辑是既然拥有匹配的文本和图像对，就将这一对视为正样本，与其他所有不匹配的图像或文本进行对比。从某种意义上说，SigLIP要简单得多，它基本只问：对于任意给定的图像、文本对，它们是否对齐？没错，大家看上面这张图，这其实是个二分类问题。对角线上的元素是正样本，对角线外的就是负样本，就这么简单。

咱们来看算法流程。拿到嵌入向量后，先做归一化，然后照常计算点积。不过现在咱们是在计算相似度，咱们来设定标签，非对角线位置标为-1，对角线位置标为+1，接着执行交叉熵损失计算，也就是对数Sigmoid。所以这个目标函数非常简单。

（咱们来定义一下标签，非对角线位置标记为-1，对角线位置标记为+1，然后实行交叉熵损失计算，也就是这个sigmoid，对吧？因此，这个目标函数非常简单。）

对，那问题来了，这需要采用复杂的采样策略吗？在最早的那篇论文里，他们直接操作的是同一种类型的矩阵。我猜你大概在想一般情况下在进行这种对比学习时，负样本该怎么处理，比如是否需要平衡正负样本，或者让负样本选得更紧一些，以避免模型学到错误的关联产生偏差。不过在最初版本里，这其实挺简单的。

所以，咱们来聊聊数据。嗯，你知道，这是谷歌做的一个数据集，他们参考了另一篇论文，即2022年左右出现的一款语言图像模型。该模型使用了WebLI数据集，其中包含11亿级的图像-文本对。他们还做了一些额外处理，比如针对图片中包含文字的情况，他们使用了OCR技术，这也是构建图像-文本对的一种方法。他们还进行了一些过滤。这个数据集是多语言的。这篇论文的核心观点是其训练效率远高于CLIP。

CLIP是在256个TPU v3上训练了10天，而SigLIP仅用了32块v4显卡训练了5天。你可能会觉得v4按理说应该比v3更快，单看每秒浮点运算次数，它们其实并不更快。优势在于你可以在一个pod中部署更多设备，且互联性能更好。但在这个规模下，实际情况是效果非常显著。你知道，并没有更快，实际上速度慢了大约60%。所以，这种方式之所以更快，我认为CLIP可能并未针对最大吞吐量优化代码，当时只是想先跑通模型。不过SigLIP论文展示了我们可以这样进行并行化。

所以，大家可以回想一下之前的系统课，把它看做，也就是DDP。每个设备各自存储一部分图像、文本。对，不过这里样本之间存在交互，不像语言模型训练那样各项完全独立。所以在第一轮中，每个设备只计算它手头那些图像-文本对的损失，接着输入文本，这样设备1就能拿到T5到T8，并据此计算这里的负样本。接着获取T9到T12，以此类推，不断轮换，直到覆盖所有非对角线块中的条目。

好的，SigLIP的一个优势在于它有效的将批量大小与损失解耦了。相比之下，在CLIP中，损失是依赖于批量大小的。如果你改变了批量大小，损失函数也就变了。所以他们得以用更小的批量大小做实验，比如小于16k。在这种设置下，它的表现远优于CLIP。因为在CLIP中，如果批量大小过小，损失函数就会退化。而对于SigLIP，即使批量较小会导致方差较大，但其期望值与标准损失是一致的。他们还提到虽然可以使用更大的批量大小，但这其实并没有带来实质性的帮助。谢谢。

好，接下来我们看看CLIP和SigLIP。这些是图像编码器，负责接收图像。在这个版本中是固定尺寸，比如336×336的图像，并将其映射为向量。好的，这个向量里包含了一些语义信息。好的，这个向量里包含了一些语义信息。

好，接下来我们开始构建视觉语言模型，也就是VLMs。我要介绍两类模型：LLaVA和Qwen。这两类模型在整体架构模板上非常相似，具体细节上会有些不同，我们稍后展开。其基本思路是将这些嵌入向量注入到语言模型中，而不是从零开始训练。谢谢。好的。

LLaVA论文发表于2023年。这让大家很兴奋，因为那时候像GPT这样的闭源模型，我觉得是GPT-4已经能做视觉推理了，而且他们展示了自己确实能做视觉推理。当然，效果不如GPT-4，但是这是个开源模型，大家得以看到它的内部运作机制。所以……四个VLMs这里展示了多模态能力的提升。本质上，这里主要分几块：一个是视觉编码器……

器，他们用的是CLIP。文本解码器部分，他们用了一个叫Vicuna的语言模型，这是首个在共享GPT对话数据上微调过的LLaMA模型。他们用的是CLIP，文本解码器部分，他们用了一个叫Vicuna的语言模型，这是首个在共享GPT对话数据上微调过的LLaMA模型。本质上它主要由几个部分构成。首先是视觉编码器，他们使用的是CLIP；对于文本解码器，他们采用了一个名为Vicuna的语言模型，这是首个在共享GPT对话数据上进行微调的LLaMA模型。数据集是MS COCO，这是一个经过标注的数据集。研究人员雇佣了Mechanical Turk上的工人，对大量图像进行标注，包括边界框以及描述图像内容的标题。他们基本上为模型合成了一套数据集。因此，他们将标题或检测到的物体作为prompt输入给GPT-4，并要求GPT-4生成问题或对话。例如，这是MS COCO数据集中一张图像，这是由人工标注的标题。此外，这里也是由人工标注的。然后你让GPT-4生成一段对话，你可能会得到一个问题及其对应的答案。你可以向GPT-4提问，让它生成一段详细描述，它大概会生成类似caption的内容。然后你可以让GPT-4做复杂推理，它也能搞定。好的，这就是合成数据，共158,000个样本。呃，然后他们拿这些数据训练了一个模型。所以这个模型用的是CLIP，具体来说是表现最好的那个版本，ViT-L/14，也就是14×14的patch。咱们直接看这张图，把图像输入视觉编码器CLIP，就能得到一个向量。也就是14×14的patch。咱们直接看这张图，把图像输入视觉编码器CLIP，就能得到一个向量。对于文本，直接用标准的嵌入生成向量即可。简单来说，文本和图像都被编码成向量，然后这一串向量序列直接送入标准的Transformer，然后输出结果。从某种意义上说，我们把这些图像转换成了文本token，因此我们可以利用预训练的语言模型。为了训练这个模型，他们分两个阶段进行。第一阶段冻结视觉编码器和语言模型，只训练这个矩阵W。他们称之为对齐阶段，本质上就是确保图像被映射到大致相同的空间。大家好，欢迎来到本系列课程。如果直接使用随机的W，这些图像根本无法生成。也就是说，它们的输出肯定不是能表示自然语言token的嵌入向量。因此，训练W的目的就是让它的输出尽可能接近自然语言token的嵌入向量。第二阶段则是冻结视觉编码器，随后训练W和语言模型。也就是说，他们是在用自己的数据对语言模型进行微调。大家还记得吧，他们的数据主要是图像，并配有对话、描述或复杂的推理示例。因此，这完全属于图像加文本到文本的任务。这是他们论文中的一个例子，很有意思。他们还特别指出，即便用户的提示词不够清晰，该模型也能表现出色。即使你没有直接问哪里出了问题，它也能给出合理的解释。GPT-4显然能做到这一点，但当时的其他模型却不行。关于LLaVA，还有问题吗？好，那我们先回到2023年。接下来我们直接看2024年的LLaVA-OneVision。LLaVA论文发表后，又涌现出许多后续工作，比如LLaVA-1.5、LLaVA-NeXT等。不过我认为我接下来要讲的内容基本涵盖了这些创新，其核心思路并未改变，仍沿用那套技术架构，但他们旨在拓展更多种类的多模态应用。因此，该系统现在能够处理多张图片及视频。我们来看下面的例子。请看这张示意图，系统现已支持图像输入，例如单张图片。输入也可以是多张图片或者是视频，而视频本质上就是一系列连续的图像帧，这对应于对视频帧进行采样。在此，视觉编码器被替换为SigLIP。在文本解码器方面，他们采用了Qwen2，这大概是当时性能最强的语言模型。接着是投影器，也就是适配器，负责将视觉编码器的输出转换为语言模型的输入。这里使用了一个两层的MLP（多层感知机），而非简单的线性投影。综上所述，这些细微改动就好比你升级了系统内部的组件，但整体架构仍保持大致相同。好了，所以这点小改动，这就好比你是个系统，只是升级了里面的零件，以支持更复杂的数据处理，但整体架构还是那种大致相同的系统。嗯，OCR的关键在于保留细粒度的信息，不然的话，j看着就像i，这就麻烦了。大家记得CLIP会把图缩放到336×336。你看，这是一份文档，要是直接裁成336×336，你就没法读了。所以他们的解决办法叫AnyRes，这其实是LLaVA-1.5论文里提过的。这个思路其实挺简单的，我是说很直接，你把一张图拆成好几块，简单来说，每一块的大小就是视觉编码器要处理的那个分辨率，比如336×336。所以咱们要把所有这些块都编码成向量，然后把它们拼在一起。说白了，这就是为了解决视觉编码器搞不定高分辨率的问题，所以咱们不直接做下采样，而是直接裁剪图像去观察不同的局部区域。如果输入的是超高分辨率的图片或视频，生成的token数量会非常多，因此需要对这些token进行下采样处理。不过，无论采用哪种分辨率调整方法，核心思路都是自适应，这一点非常关键。这很好，因为Transformer本身就具备这种灵活性，无论序列长度如何，Transformer都能很好地处理。这意味着图像也能支持任意分辨率，再次体现了这种动态适应能力。这就是他们在处理图像时实际采用的方法。假设你有一张论文截图，系统会先对其进行下采样，同时保留一条路径。这本质上是对整篇论文进行下采样后的编码。接着将其分割成若干块，每块的分辨率都符合视觉编码器的要求，然后分别对每个块进行编码，最后将所有图像块拼接起来。如果数量过多，则通过插值方法减少图像块的数量。这样一来，他们就能处理单张图像、多张图像以及视频了。你可以这样理解，虽然从技术上讲所有这些都可以还原为图像，但他们还是稍微调整了权重。因为视频数据有其特殊性，他们希望确保所有模态之间大致具有可比性。你可以这样理解，虽然从技术上讲这些内容最终都能还原为图像，但系统还是对它们进行了权重调整，因为视频数据量巨大。目的是确保不同模态之间的数据大致具备可比性。好，如果有多张图片，就直接用基础分辨率处理，也就是说只有一张图时我们能看得更细致，图多了就只能粗略浏览。处理视频时，我们会用更低的分辨率或更少的token来表示每一帧，毕竟视频数据量很大，需要节省计算资源。他们最多只取32帧，但这样做会面临上下文长度限制的问题。稍后我们会看到，解决多模态问题的关键之一就在于如何处理长上下文。好，那么，关于这组数据，我认为他们阐述的核心理念是既要保证数据的高质量，也要保证数据的数量。换个角度说，这些数据具有极强的针对性，你会发现它们大多是为特定任务定制的，比如视觉问答，或是回答关于表格的问题。因此，这显然属于后训练阶段，其目标是让模型更好地对齐人的意图。为了让模型执行这些任务，你会专门构建一批对应的任务数据。这项工作也毫不掩饰其目的，本质上就是在蒸馏GPT-4模型以获取最佳性能。虽然这或许并非最理想的做法，但现实如此，如果没有标注预算，你只能这么做。此外，他们使用的数据涵盖单张图片、多张图片以及视频。如果没有标注预算，你只能这么做。此外，他们使用的数据涵盖单张图片、多张图片以及视频。训练思路与之前类似。第一阶段主要进行对齐，具体来说你只训练投影器，也就是那个适配器，接着进行第二阶段训练。第三阶段则训练整个模型。以前是两个阶段，现在变成了三个。我不确定这背后有什么特别的原则，除了第二阶段似乎是想进一步提升模型的推理能力，使用高质量数据，但更侧重知识。最后阶段则侧重那些类似下游任务的示例。这篇论文发现了一个有趣的现象，即不同模态之间可以迁移。虽然使用高质量数据，但更侧重知识；在最后阶段则侧重那些类似下游任务的例子。好，这篇论文里发现个挺有意思的事，就是不同模态之间可以迁移。你会问它一些问题，但在测试时你会看到一张图里既有表格又有图表，然后就能跟它聊聊天。训练的时候你只用单张图上的OCR数据，而且只在多张图之间做关系推理，但这能帮你搞定那些很有用的情况，比如用来驱动GUI agent，给它看一堆截图，也就是多张图，它就能生成描述，这些截图的描述。再举个例子，有个叫视觉提示的想法，就是图里有个圆圈，这个圆圈是图的一部分，它的作用是标出你需要对它做点什么的那块区域。再举个例子，有一种叫视觉提示的方法，就是在图中画一个圆圈，这个圆圈是图像的一部分，它的作用是标出你需要对其执行操作的那块区域。所以我第一次看到这儿心想，哎哟你这是在针对每一个任务下手，这有点像监督学习。不过如果你有足够多的任务，这些模型似乎能实现某种迁移。呃，这算是个让人安心的消息。好，所以再说一次，这是一个相当标准的VLM架构，

视觉编码器加上投影层，再进入语言模型。数据策展工作投入了大量精力，他们很大程度上依赖于合成的特定任务数据。LLaVA系列的一个优势在于它是少数几个能做到这点的模型之一。谢谢。好，接下来咱们聊聊Qwen模型。所以Qwen也是从2023年开始训练多模态模型的，首个版本是Qwen-VL。Qwen也从2023年开始训练多模态模型，视觉编码器方面，他们用了OpenCLIP，记得吧？OpenCLIP就是CLIP模型的开源复现版，所以它本质上就是个CLIP编码器。至于适配器，他们使用了OpenCLIP作为视觉编码器。他们用了一层交叉注意力结合2D位置嵌入，将特征映射到一个固定尺寸。所以它本质上就是个CLIP编码器。至于适配器，他们通过一层交叉注意力机制将视觉特征映射到固定大小。他们用了一层交叉注意力结合2D位置嵌入，将特征映射到一个固定尺寸。他们采用了一层交叉注意力机制，结合二维位置嵌入，将特征映射到固定尺寸，比如256。这部分可能被略过了，但特殊token包含了图像标签、边界框标签box tag，以及参考标签ref tag。谢谢。这就是幻灯片导出为HTML格式时的效果。训练分为三个阶段，这与LLaVA模型的做法类似。第一阶段，也就是他们所谓的前预训练，其实我们并非从零开始预训练。这里使用的是大规模但质量较低的数据。具体做法是冻结语言模型，然后训练视觉编码器和适配器。这与LLaVA模型略有不同，因为LLaVA模型在此阶段并不训练视觉编码器和适配器。谢谢。大家可以看到他们训练所用的一部分数据集规模达到了14亿个样本。到了第二阶段，他们换用了质量更高、针对特定任务的数据。这里能看到一些VQA数据集中常见的来源，比如图表问答之类。接着，他们在这里训练所有参数。最后是第三阶段，使用的是指令微调数据。我们冻结视觉编码器，然后训练适配器和语言模型。这里举几个他们能做到的例子。当然因为这是Qwen，所以有些例子是中文的。我们还希望打造一个能涵盖语言模型能力的照片。既然语言模型能写代码，我们也希望视觉模型在代码方面同样出色。咱们看个例子。如果你能看见的话，这是一张包含金霸王、蜘蛛侠和绿巨人的图像，它还能输出边界框。它其实不生成图像，只是输出边界框。接着咱们可以做OCR，也就是文字识别，然后对比一下。所以咱们就用咱们自己的东西。好，那Qwen2算是又一次升级。这里有些新点子，我接下来会过一遍，比如用更大的视觉编码器。他们还开始搞动态分辨率了，我觉得这是最关键的一点。大家还记得咱们从LLaVA到LLaVA-OneVision的时候吧？那时候他们意识到：“哦，得能处理不同尺寸的图像。”你要是想搞视频，那就很明显了，你得需要某种动态分辨率。大家还记得我们从LLaVA到LLaVA-OneVision的演进过程吗？当时他们意识到必须能够处理不同尺寸的图像。如果要处理视频，那就很明显了，你需要一种动态分辨率机制。这个模型的基本逻辑是这样的，假设你有一张这样的图片，它可能会被编码为11000个token，而这张包含公式的小图可能只被映射为8个token，以此类推。那么他们具体是如何实现这一点的呢？基本思路是这与LLaVA中的任意分辨率概念是一致的。具体来说，每个224×224的图像块都会通过ViT进行编码。此外，他们最初使用的是OpenCLIP的ViT，但随后对其进行了微调。接着，他们尝试压缩上下文长度，将每两个图像块合并为一个，从而减少每个图像的token数量。嗯，所以Qwen2在这里采取了一些不同的做法。因此，他们引入了一种称为多模态旋转位置编码（Multimodal RoPE）的方法。大家还记得RoPE吗？它的核心思想是嵌入向量具备这样的特性：向量间的内积仅取决于相对距离。在一维情况下，距离即为两个token之间的间隔数。推广到多维版本时，原理基本一致，只是维度扩展到了三维。这三个维度分别是高度、宽度和时间。因此，每个位置或图像块现在由一个三元组来表示。推广到多维版本时，原理基本一致，只是维度扩展到了三维。这三个维度分别是高度、宽度和时间。因此，每个位置或图像块现在由一个三元组来表示。不过后来当我们审视Qwen3时，会发现这其实并非最优解。于是，他们以此来初始化语言模型。这是Qwen-VL模型，自然是以Qwen为基础进行初始化的。接着，他们进行了三阶段训练，这与我们之前的做法非常相似。最后，他们展示了一系列不同能力，比如视频理解。它既能处理数学和代码，也能进行函数调用等等。好的，那我快速介绍一下Qwen3。嗯，所以再说一遍，这里有一些改动，我不认为这是结构上的重大变动，但它们确实会影响模型的质量。这是去年Qwen3-VL报告里的图，所以这里的图表基本没变。我提一下，也许后面我会再回到这张图。我来梳理一下这些变化。首先，现在他们用的是Qwen3模型。他们有一系列Dense和MoE模型。Qwen3模型效果非常出色，这对提升最终模型的质量帮助很大。他们重点投入了长上下文理解能力，上下文长度最高可达256k，这一点非常重要，尤其是当你试图处理长视频时。咱们来谈谈视觉编码器。他们用的是SigLIP-2，这基本上是对SigLIP的改版版本。实际上这是完全相同的架构，其设计旨在向后兼容。随后，他们改进了RoPE。咱们先回顾一下，以前只有三个维度：时间、宽度和高度。简单来说，他们把第一块维度空间留给了时间，简单来说，他们把第一块维度空间留给了时间，接着给宽度维度，再给高度维度。如果你记得RoPE，问题就出在这儿，每个组件其实代表不同的频率。这就导致一个问题，时间维度可能全是低频，而高度维度全是高频。所以，他们把这些维度交错排列了。意识到这点后，现在的每个轴都能同时接触到低频和高频信息。另外，他们还做了一件事：直接引入显式的视频时间戳。所以，他们将这些维度交错排列。意识到这一点后，现在每个轴都能同时接触到低频和高频信息。另外，他们还做了一件事，直接引入显式的时间戳。哎呀，嗯，你看，这个可能有点小。不过之前我们讨论过位置嵌入的问题。时间戳在位置编码里是隐含的，视频里的每一帧天生就带着不同的时间信息。之所以有时间概念，纯粹是因为它们位置不同。呃，但在这儿，他们把时间显式化了，所以这些其实是token。0s就是一个token。现在你有了一个代表时间的token。呃他们发现这挺有用的，大概是因为你可以直接引用它，比如两秒后发生了什么，从而增强时间感知。0s对应一个token，现在你拥有了一个代表时间的token。嗯，他们发现这挺有用的，大概是因为你可以直接引用它，比如两秒后发生了什么。咱们得这么想：但是视频的例子通常很长，但是单张图片的例子就很短。通常的做法是把每个token一视同仁。这样一来，视频样本就会占据主导地位。所以嘛，他们大概是——具体细节我不太确定，但我猜他们是用示例长度的平方根做了归一化，主要是为了压低那些特别长样本的影响力。最后还有个挺有意思的点，就是他们在adapter上搞了点新花样。咱们回想一下，adapter就是负责把视觉编码器和语言模型连起来的那个部件。LLaVA最简单，就是个线性投影。后来咱们又加了MLP，还搞了种交叉注意力机制。咱们回想一下，adapter就是负责把视觉编码器和语言模型连起来的那个部件。LLaVA最简单，就是个线性投影。后来咱们又加了MLP，还搞了种交叉注意力机制。咱们基本上就是把它们直接加到语言模型的残差流里。所以，这算是把视觉编码器和语言模型做了种深度融合，即将视觉编码器与语言模型进行融合。这与仅输出向量序列的黑盒视觉编码器不同。训练过程分为两个阶段：预训练包含四个阶段，后训练包含三个阶段。训练过程分为两个阶段，预训练包含四个阶段，后训练包含三个阶段。随后进入三个阶段，逐步在更长的序列上进行训练。序列长度从8k增加至32k，再增至256k。大部分token集中在第二和第三阶段。之后进入后训练阶段，团队会使用长链思维数据进行监督微调SFT和知识蒸馏，并辅以强化学习。严格来说，这是一篇侧重系统实现的论文。我标注了一些核心创新点，但文中仍存在不少细节差异。从最终结果来看，这是一个相当出色的模型。也就是说，若将这些基准测试成绩与Gemini、GPT-4o、Opus 4.1等闭源模型进行对比，从最终结果来看，这是一个相当不错的模型。所以你看，Qwen模型的实力其实很强。嗯，对，有同学提问？好，下一位。谢谢，所以问题是……

在视频生成方面，你怎么判断输出应该是视频还是文本？首先得明确，这些模型其实并不生成视频或图片。视频或者说所有多模态内容都只存在于输入端，你们生成的始终是文本。所以，首先得明确，这些模型其实并不生成视频或图片。视频或者说所有多模态内容都只存在于输入端。在这些阶段里，除了RL，你基本上是对每一个token进行监督。所以不存在，比如用语言模型当裁判或者评估描述质量的情况。这主要取决于数据集中的内容。不过有了强化学习，你可以尝试不同的反馈机制。

没错。我想确认一下，这是否属于多模态模型的训练？在那些...系统工程系统角度来看...所以，从系统角度来看，我能够...你可以在某些家族可以在目的中使用...录制的视频比一个人用以测试，从算法方面我想，英语我们只是说我更专注于那是完美的。从系统角度来看，我认为可以，某些家族可以在目的中使用...录制的视频比一个人用以测试。从算法方面我想，英语我们只是说我更专注于那是完美的。我的意思是，肯定没那么简单。就像你提到的，数据集规模更大了。视频数据嘛，你知道的。光是加载它就可能让人头大。通常我们没怎么特意讨论语言模型的数据加载问题，因为，它通常不是主要的瓶颈，成本非常低。所以这点得考虑进去，同时确保数据加载与实际计算异步进行的那些原则也都适用，得考虑进去。第二部分嘛，抱歉，刚才说到哪儿了？第二个问题，咱们知道了。哦对，视频和图像包含的token更多，尤其是视频。第二个问题，咱们清楚了。对啦，视频和图像包含的token更多，尤其是视频，主要是为了防止视频在模型中占比过大。你看你可以直接调低权重。如果token数量太多觉得不合适，就把权重降下来。咱们上次或者说是两周前聊过数据混合的问题，所以具体怎么加权，只要合理都可以灵活调整。不过文本token的数量其实也非常庞大，所以总的来说这些模型都是在数万亿个token上训练出来的。我觉得多模态token的数量并没有远远超过文本token。

对。好，你先问吧。关于X-Space API的适配器变更，我也有个问题。我觉得多模态token的数量并没有远远超过文本token。对。好，你先问吧。关于X-Space API的适配器变更，我也有个问题。它怎么识别什么是语言？语言模型必须经过预训练，不然就没法，对齐也就没意义了。所以语言模型是冻结的。我们只训练适配器，把给定的视觉编码器和给定的语言模型连接起来。训练方法很简单，你先设定一个token预算，比如671。对，就是token，然后开始训练，这里没有自适应阈值。

没错。平台间的差异也就是平台数量...平台数量...平台的...完整的设计...对，所以问题来了。视觉编码器中的参数量要小得多。答案通常是肯定的。我在想还有什么不同之处。我...嗯，不在这儿。好的。原因在于从某种意义上说，视觉编码器执行的是非常局部的操作。操作针对的是一个个小块patch。小块非常小，它只是试图理解这些小块。由于缺乏足够的支持，我们无法对这些小块进行推理。因此，该模型的核心能力主要仍源于语言模型。我确认一下。找到了。大致位置在这里。我们来看一下。对了，你看这个LLaVA模型，也就是完整版，它拥有720亿参数，是一个语言模型。还有，抱歉你刚才说什么？好的。我不太确定这个数字是...可能是一个笔误。啊抱歉，这是7200万。好吧，那确实是少得多。投影仪参数量小得多，外体通常不到10亿参数。嗯，对，我想主要是因为它是局部操作，作用范围有限。收回内容，我们继续。

所以Qwen3是我接下来要讲的最后一个视觉语言模型。它提出了一种名为Save Our Performance的方法。这背后涉及大量数据方面的工作。在后续的Qwen模型中，关于数据和数据混合的细节就不再那么详细了。所以建议大家去读一下LLaVA的论文，或者发部的那篇关于移动端的论文，我之前没来得及细讲的那篇，里面的细节更丰富。从Qwen到Qwen2，再到Qwen3，这些迭代中的许多变化都旨在提升模型性能。主要是发生了一些种类上的转变，但整体框架保持不变。主要是规模上的扩展，整理更多的数据集，意识到必须处理好长上下文以及可能优化图像处理，诸如此类。

好，接下来咱们聊点不一样的，差别很大。咱们来看一篇Meta在2024年发布的论文，叫Chameleon。到目前为止，VLM的做法是把图像编码成向量再输入到语言模型里。毕竟它是语言模型，所以只能生成文本，没法生成图像。要解决这个问题，其实有很多路径。你可以直接拿一个VLM再挂载一个扩散头，这样就能生成图像了。但我想聊聊这个思路。Chameleon的想法是要是我们把所有内容都映射成离散的token呢？某种程度上从审美角度看，这，好吧，也许这说明我是个搞语言的，更关注文本部分，这很有吸引力。因为现在你可以用同一种方式分析和生成图像，一切都被离散化为token。打个比方，假设你想生成图像，你可以输入一个文本提示，然后系统会生成更多的token。或者你也可以反过来，把语言和文本的顺序调换一下。这里有个来自Chameleon论文的示例，你可以试着给它发个提示。比如输入“我无聊了，给我看点鸟的照片”，接着它会输出文本，再输出图像，然后再是文本和图像。所以文本和图像是交替出现的。某种意义上Omni-model的设计理念正是如此，文本和图像真正处于同一个空间。这是通过让所有数据都表现为文本形式来实现的。这是一种可行的方法，谢谢。

好，接下来我简要讲讲具体是如何实现的。那么你现在需要做的事情是，嗯，是将图像映射为离散的token。这个想法源自2017年Oord等人的研究。虽然由来看已久，它叫矢量量化变分自编码器。核心思路是把图像映射成离散码字。你需要学的是一个映射，把它转成连续向量，然后把它舍入到最近的码字。所以你会得到一个码本，假设里面有8000个码字。这些就像原型向量，对应着图像块，然后将其舍入到最近的码字。因此，你会拥有一个码本，假设其中包含8000个码字。这些就像是原型向量，对应于图像块，把它舍入到最近的码字。接下来解码器的工作就是拿到这个码字，试着重建图像。然后你训练这个VQ-VAE，基本就是这么回事。主要是最小化重建损失。因为这里不可微，所以还得加一些其他项，不过我没时间细讲。好了。总的最后的结果是512×512的图像被转换成了1024个token。每个token都来自一个大小为8000的词汇表。谢谢。

好，现在你手头基本上既有文本也有图像了。这些基本上就是相关代码。接下来我们需要训练一个新的token分词器。因为情况发生了变化，现在的数据与以往仅处理纯文本时有所不同，即自然语言数据。

接下来进入训练阶段。这一步其实非常直观，就是标准的语言模型训练。这里不需要适配器，也无需调整视觉编码器，仅使用语言模型即可。这部分设计颇具吸引力。尽管仍分为两个阶段，但即便是语言模型训练，通常也包含两个或更多阶段。第一阶段是训练的主体，规模庞大且为无监督学习，模型中同时包含文本和图像数据。随后进入第二阶段，我们引入高质量数据。这一设计确实十分优雅，但其中存在几个问题。首先，他们发现训练过程不太稳定。原因在于，尽管文本和图像处于同一空间，但两者的表现差异显著。所以，仅仅把图像数据也称作离散的token，并不能掩盖图像本身依然存在的本质。尤其是文本token，你想想看，要预测下一个词，它的熵值其实是比较低的。大部分词多少都能猜到。但图像token的熵非常高，我根本没法确定这个token具体会呈现什么蓝色。结果就是这种混合训练引发了一系列范数方面的问题，参数会不断增大，进而导致损失值不稳定。他们利用QK范数和z-loss正则化，在一定程度上缓解了这一问题。其中z-loss正则化用于控制范数的增长。具体实验结果我就不展示了，我想重点介绍这项工作，因为它有一种简洁之美。它仅用一个模型就同等对待所有模态。但是缺点在于事实证明该模型的性能并不理想，

另外，离散化过程必然会导致信息丢失。我们可以以 OCR 为例，如果对极小的印刷文字进行离散化，你将无法再识别它们。最后，多模态训练也颇具挑战性。我们在 CoCa 模型中已略见端倪，当时需要反复调整权重，而在此处这一问题更为显著。VQ-VAE 曾经非常流行，当时许多人将其用于图像生成。主要原因在于如果你手头有一个 Transformer，该如何让它生成内容？你需要生成离散数据，即将所有数据转换为离散形式。后来扩散模型兴起，成为了一种可行的生成方案，因此这类方法如今已不如从前流行。我们在此简要回顾并总结：当前的前沿模型预计都将具备多模态能力，尤其是原生多模态或全模态模型。遗憾的是，Gemini 和 GPT 发布时被宣传为原生多模态，且能处理所有模态，事实也确实如此，但具体的架构细节并未公开。我推测可能采用了某种，比如为保留信息不丢失，可能使用了连续编码器，生成部分则采用了扩散模型。当然这仅是推测。我认为多模态最大的难点在于如何处理非文本模态。还有一个有趣的现象：理解模态和生成模态之间存在一种对称关系，而且具体情况各不相同，也就是说并不存在一种通用的编码器。以 CLIP 为例，你主要关注分类，目的是提取高层语义。因此这些向量可以做得很小，同时仍能捕捉到高层语义。相反，如果你要做光学字符识别或图像生成，就需要非常精细的细节，这正是扩散模型表现优异的原因，因为它能对高低频信息进行微调。谢谢。我觉得在处理多模态问题时，我们需要仔细思考，要把它们平衡好。打个比方，视频的信息密度通常低于文本，所以你不想让视频淹没文本。就像我之前提到的，也许目前最好的做法是使用连续编码器。看起来即使 CLIP 已经是五年前的技术，类似的思路现在仍是捕捉图像语义的首选方案。Transformer 仍在这广泛使用。至于扩散模型，我刚才没有详细展开，它在生成方面非常出色。好，我就讲到这里。关于多模态的讲座就讲到这里。这部分没有实操手作业，但如果你感兴趣，我鼓励你去动手试试，比如训练这些模型。
