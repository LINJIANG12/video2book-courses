好，我们开始吧。欢迎大家回来。
今天我们要讨论 parallelism。
还记得吗？上周我们介绍了如何通过编写 kernels 让单个 GPU 跑得更快，并且深入看了 GPU 的内部结构。
而这周我们要讨论如何利用多个 GPU 让你的代码跑得更快。
你脑海中应该有这样一幅画面：上周我们聚焦于这些方框中的一个，也就是你的 GPU。
还记得吧，里面有 high bandwidth memory, HBM, L2 cache, L1 cache, registers, 以及一大堆 streaming multiprocessors, a stack of streaming multiprocessors。
现在这幅画面要扩展了，因为你不再只有一个 GPU，而是可能有4个甚至1000个 GPU，而且它们是相互连接的。
稍后我会讲这些 GPU 是如何连接起来的。
然后你就得想办法利用所有这些 compute 来训练模型。
所以无论是单 GPU 还是多 GPU，这也是我们今天要讨论的内容。
这两种情况下，如果把视角拉远看，情况大致相似，那就是 compute，像 arithmetic logic units, tensor cores 等等，离你的数据很远。在单 GPU 场景下，这意味着数据远在 HBM 里。
而在多 GPU 场景下，你需要的数据可能在另一块 GPU 上。the problems。你不得不想法把它搬过来。
当然核心原则不变，因为关键在于编排计算，尽量避免数据转移带来的瓶颈问题。
瓶颈。用上一大堆 GPU 很容易，但要高效地用好它们却很难。
所以我们可以稍微灵活地看，考虑一个广义的层次结构。在最局部的层面，紧邻的是单节点单 GPU，其中你有……你有 L1 cache, shared memory, 这是最快的，然后是 HBM。
我们之前曾抱怨它太慢，但在这节课里，HBM 会被当作高速存储来看待。
现在我们将考虑单节点多 GPU 的场景，其中 GPU 通过 NVLink 和 NVSwitch 连接。
最后是多节点多 GPU 的场景。这时我们必须根据你所拥有的网络，选择使用 InfiniBand 或 Ethernet。
好，那么上周我们讨论了减少内存访问的各种技巧，也就是 fusion 和 tiling。读入 shared memory，尽可能在其中完成所有操作，然后再写回。
而本周我们将讨论如何减少通信量，也就是通过在 GPU 之间进行适当的复制和分片来实现。
那么为什么要使用多 GPU？显而易见的答案是：你想要扩展规模。不过说得更具体一点，其实有两个原因。
首先，你的参数，activations, gradients 和 optimizer state 装不进单张 GPU 的 HBM 内存。
比如 B200 有 192GB 显存，如果你训练一个 1 trillion 参数的模型，单张 GPU 根本装不下。
其次，即便模型能装进单张 GPU，你可能也想通过拆分任务来利用更多的 GPU，为了加快训练速度。
所以有时候你得做个权衡，因为虽然能把所有东西放进 GPU 但核心数不够多，但如果你把任务分散开，就得承担通信带宽的代价。
所以你得算一笔账，看看该怎么做并行化。
好，这里提一点，到目前为止这门课用的是 Python，你执行代码就能把所有结果展示出来。
这一讲里如果你直接运行这段代码，会用到 multiprocessing。不过我一步步演示的时候，用的是某种特殊模式，也就是 single process mode。
如果你想看这一讲的代码在 multiprocessing 模式下的 stdout，点这里就行，我会边讲边演示。
不过大家记住，我们一步步讲解的时候，其实并没有真的在跑 multiprocessing，因为只是在逐行执行代码。
好。
这一讲分为两个部分。第一部分我们来学习分布式通信与计算的基本构建模块。
先从编程模型讲起，简单说说硬件，然后开始在 Torch 里实现一些东西，这也是你在作业里要做的。
第二部分我们来看看实际的训练过程。我们会看三种 parallelism: data parallelism, tensor parallelism 和 pipeline parallelism。
每一种都会以不同的方式切分我们的模型。我们拿 MLP 而不是完整的 Transformer 来举例，但这里展示的其实就是核心的计算过程。
好，咱们深入看看。
首先得了解这些叫集合操作 collective operations 的东西。
集合操作是分布式编程里的一些基础原语。
这得追溯到80年代了，所以并行编程的想法其实非常古老。
它们当初并不是为了训练大语言模型才发明的，而且直到今天我们用的还是这些原语。
这里的集合意思是，你指定的是跨多个设备的通用通信模式或模板，而不是去管理点对点通信，也就是不用管这块 GPU 具体怎么跟另一块 GPU 通信。
抽象会简单得多，系统也能帮你做更多事情。
所以这是一种非常成熟可靠的并行编程接口。
好的，总体设置如下。这里的术语我觉得有点奇怪，但这是并行编程里的标准说法。
核心思想是，你有一组 ranks，每个 rank 对应一个特定的设备。
在我们的例子中可以是 GPU，也可以是 TPU，但重点是你这里有4个 ranks。
The world size 对应于设备的数量，所以这里的 world size 是4。
好的，我们将来介绍几种操作：broadcast, scatter, gather, reduce, all gather, reduce scatter, all reduce 和 all-to-all。
这些操作中的每一个都将指定这一组 ranks 或设备如何与另一组设备传输一定量的数据/compute。
那么前三个操作 broadcast, scatter, reduce，我认为它们其实只是热身，它们让你能够大致理解这些集合通信是如何工作的。
集合操作确实有效，但它们并非驱动大部分训练任务的主力。
训练，在语言模型的分布式训练中，all-gather, reduce-scatter 和 all-reduce 这些操作会反复出现。
最后是 all-to-all，我简单提一下，它对 MoE 很重要，但这节课我们不会花太多时间讲它。
好，那我们就从最简单的操作开始，深入看看。
也就是 broadcast，广播。在广播操作中，你有一个 rank 0。
它可以是任何 rank，但为了举例，我们假设 rank 0 有一个 tensor 0, 1, 2, 3，然后把它广播给所有的 ranks。
所以在这个操作结束后，每个 rank 都拥有相同的数据，它上面的张量。
这应该相当直接明了。
而且同样的，这其实并不会出现在训练的核心路径中。
一般来说，广播用于初始化阶段，比如你加载一个初始检查点，然后把它广播给所有的 ranks。所以这类操作大概只做一次。
好，第二个操作是散射 scatter。散射的基本意思是，我有一个张量，在 rank 0 上，这个张量被切分成 world size 份，
然后我会把我的张量散射到其他各个 rank 上。
所以 rank 0 得到第0个分量，rank 1 得到这个，rank 2 得到这个，rank 3 得到那个。
好的，再说一遍，scatter 本身并不直接使用，但它是理解 reduce-scatter 的重要基石。
顾名思义，scatter 就是把一个大张量从一处分散到多处。
你可以看出这为什么有用，因为你希望所有参与 scatter 的 GPU 对各自的部分做局部计算。
谢谢，谢谢。
好，scatter 逆操作是 gather。这应该不难猜到。
输入是一堆数据片段，每个片段都存放在特定的 rank 上。
然后当你执行 gather 时，这是针对特定的 rank，也就是 rank 0 而言的。
它只是把所有片段拼接在一起。
好，再说一遍，gather 本身并不直接使用，但它是理解 all-gather 的基石。
接下来是 reduce。做过函数式编程的人可能对 reduce 很熟悉。
完全一样的思路是从和 gather 一样的起点开始。当数据分散到多个设备上时，你在每个不同的 rank 上都有部分数据。接着你对这些数据执行规约操作，并把结果放到 rank 0 上。
所以如果你执行某种规约，把所有数加起来，结果就是6。
所以你可以把 gather 看作是一种规约，它的操作是，也就是所谓的拼接。
好的，当然 reduce 对于理解 all-reduce 很重要。
好，先停一下。这些只是热身，以防大家对什么是集体操作还有疑问。
广播、散射、收集、规约。
Castle? 对。
城堡。
谢天谢地。但真是这样吗？
那么，问题是这与广播 broadcasting 和 NumPy 有关吗？
我是说，我认为它们在概念上是一样的，思路也相同。
就是一个东西分发给多个东西，比如在 NumPy 里如果你有一个标量，它会被广播成张量。
在实例化这方面，因为这是用于集体通信，所以略有不同。
好，那我们继续聊点更有趣的。
那么，我来做收集 gather，你所做的基本上就是对所有 rank 执行收集操作，不只是 rank 0。
还记得 gather 是做什么的吗？它基本上就是把所有不同的部分都收集起来。
它把数据放到一个 rank 上，也就是 rank 0。
接下来就是 all-gather。它对每个 rank 都这么做，这就是 all-gather 的意义所在。

意思是把所有输出发到所有rank上。而gather就是你对所有rank所做的操作。 好的，这个概念后面会频繁出现。你不需要完全搞懂这句话，但之后我们会看到每个rank只持有部分参数。 接下来，你需要做的是用all-gather把参数收集起来，从而得到完整前向传播所需要的完整参数。 所以在训练过程中，我们通常会看到这样的模式：先gather来做点事，然后scatter，再gather，再scatter。 好的，reduce-scatter就是在每个维度上执行reduce操作， 然后把结果scatter出去。好的，假设你有 嗯，比如四个设备，每个设备里都有一些向量。 所以 当我们这么做的时候， 之前做reduce的时候，我们只有0、1、2、3，结果被reduce成了6。但现在reduce-scatter的意思是，对于这个张量的每个分量，我都会做一次reduce。 然后，我会把它们放到不同的，你知道，然后 is 对于第一个维度，我把这些加起来得到6。is 现在 对于第二个维度，我把这些加起来得到10。对于第三个维度，我来处理这些。 K? 对于第四个维度，我处理那些。 好的。 好，先做个铺垫，说一下这个会出现在什么地方。 反向传播之后，当你做求和时，你要做的是这样的，每个微大GPU会处理不同的数据。对，你要做的是把所有的梯度加起来， 来自不同分片的梯度，然后你将重新分配这些存储。好的，最后是all-reduce。 如果你理解all-reduce、scatter和all-gather，本质上就是先做一个，再做另一个。 好的，所以这个操作执行的是reduce-scatter。输入和我们之前的一样。还有， 回想一下，在reduce-scatter中，索引6、10、14和18，这些元素分散在不同的rank上。 接着，all-reduce里的all-gather步骤会把它们全都收集到同一个节点上。 所有元素都在同一个节点上，所以从这个角度看，all-reduce其实是最容易理解的。 你有一堆张量，对它们做归约。 比如这里是求和，然后把结果复制到所有节点上。 好的，我们在做数据并行时会先看到这个操作，对梯度求和，然后复制完整的参数。 谢谢。所以我们实际上要从这里开始讲，大家不妨先把注意力集中在reduce上。之后我们会看看如何进阶到更复杂的技术，比如ZeRO或FSDP。 我们需要把all-reduce拆解成reduce-scatter和all-gather这几个步骤。 因为这样你就能介入并更好地掌控局面。不过对于基础版来说，用all-reduces就够了。 谢谢。好，最后讲一下all-to-all。 这在某种程度上是最通用的，你基本上要指定每个rank如何向另一个rank发送特定的消息。所以这里有个简单的例子。输入和之前一样。 它的意思是，我想把位置0的元素发给rank 0。意思是这个元素我自己留着。然后把位置1的元素发给rank 1，把位置2的元素发给rank 2，把位置3的发给rank 3。 然后，假如我是rank 1，我想把位置4的元素发给rank 0，位置5的发给rank 1。 把位置6的元素发给rank 2，位置7的发给rank 3。所以基本上，这里的位置表示哪个rank是最终接收方。 Um, all these 所以，看看输出结果你就明白了。第一个rank会收到所有在列0发送了数据的rank，因为所有这些rank都在向rank 0发送数据。 同理，对rank 1，所有rank都在向它发送这一列，以此类推。 哎，好的。这在训练中会很有用。 直观的理解是，每个rank既持有数据的一个切片，也持有一组专家子集。 嘛，基本上MoE的核心思想是某种动态路由。 你需要查看自己的数据，才能确定要把哪些activations路由到哪些专家。 所以，它最终就变成了一种all-to-all通信。 goes 假设一切都是平衡的，也就是每个rank向其他每个rank发送相同数量的字节，那么自始至终你都可以把它本质上看作一次转置。 如果你把它看作一个矩阵，你所做的无非就是转置这个矩阵。 不过一般来说，all-to-all也能处理负载不均的情况。虽然这里没展示，但你完全可以配置它，向任意其他rank发送任意数量的字节。 不过总的来说，最好还是让划分尽可能均衡。所以，回想一下top-k加MoE的那些课，里面提到了负载均衡，目的就是确保各部分尽可能均衡。 所以说到底，理想目标是让all-to-all的形态大致像这样。 咱们来总结一下，我刚才讲了不少不同的概念。这里有些小贴士帮您记住这些术语。 操作 首先是reduce。 它做的就是reduce，执行某种满足结合律和交换律的运算。可以是sum、max，也可以是min。 scatter是gather的逆操作。 scatter 负责分散数据，gather负责汇聚数据。而all则表示目标是所有节点，也就是所有设备。这就解释了all-reduce和all-gather。好的，我先停一下。大家有什么关于集合通信的问题吗？ 对，比如在gather这类操作中，我们通常希望 我们基本上是想把所有rank的数据汇聚到rank 0。这个rank 0是固定的吗？ 也就是说，每次都是同一个特定的GPU，还是说这个rank 0可以变动？ 是的，你的问题是当你在执行gather或者对目标做reduce的时候， 你 把输出写在哪里。我刚才说是rank 0。 稍后看代码你就会明白，你只需指定GPU ID或rank，数据就会发往对应位置。et cetera。因此它不必提前很久就定死，但必须确定。So 基本上在执行那个调用时才确定。 好。 还有别的吗？对，这些只是概念层面的。 他们并不是，实际上你问的是否存在像 它才是实际的交付者。 对，所以问题是这些只是概念性的构件块，还是代码？所以我现在展示的这些只是概念构件块，但我们很快会看到它们在代码中如何实现。 好的。所以在进入代码之前，我想先谈谈硬件。尤其是GPU之间是如何连接的，因为我们已经知道GPU内部的结构了。 嗯，那么来，我们来谈谈网络。 大致上是这样。所以这算是一张非常经典如图。从这张看起来很旧的图片中，你可以看出计算机过去大致就是这样工作的。 Yes. 所以你有这个，你知道服务器，然后你有一堆CPU。 有一个PCIe总线，你用它来连接各种设备。 或者过去用来连接诸如鼠标和键盘之类的东西。然后有一堆GPU挂接在上面。你还有一些RAM。然后，这台计算机通过以太网连接到另一台计算机，如此等等。 Oh, 好的。所以这是一种特定的配置，一种特定的拓扑结构。同一节点上的GPU用PCIe通信，而不同节点上的GPU就得另走一条路了。 一路通过以太网。好，这就好比你买了游戏GPU，然后跟朋友把它连了起来，说：“我要训练个大模型。” 那你就只能这么干。但如果你真的认真想做训练，那情况就更像这样了。 I think 就是我一开始展示的那张图，上面有GPU还有一些东西。 Yes. 叫做NVLink，NVSwitch和InfiniBand。所以 典型的配置是这样的。这些数字里，8是典型值，但这个256是我随便举的例子。所以通常每个节点配备8个GPU。这些GPU通过NVIDIA的NVLink连接到交换机。 为了有个概念，如果你用NVLink 5，总带宽能达到每秒1.8 TB。 别忘了，B200的HBM带宽是每秒8 TB，所以NVLink 5大约是它的1/4。 慢了大约四倍。不过我的意思是，如果考虑到这是在设备之间传输数据，这个速度其实已经相当快了，只是显然比不上HBM，HBM要快得多。K? 比共享内存或L1 Cache慢得多。 好的。 所以简单来说，NVLink连接到交换机。这意味着从编程的角度看，你可以把任意一个GPU都当做直接连到了其他任何GPU上。 你从一个GPU发送数据到任意另一个GPU，由硬件负责把数据传送到交换机，再由交换机进行路由。好的，所以通常你还会遇到这样的情况，到某个时候你没法只靠NVSwitch和NVLink，你必须这样做。因为随着集群里GPU数量 增长，你就不得不把这些节点放进Pods里。它们之间通过InfiniBand连接。 而InfiniBand的工作方式是，现在情况有点不一样。 也就是说现在GPU不再直接连接到另一个GPU，而是必须经过PCIe还有其他环节。然后通过这种专用的InfiniBand线缆连接，你会发现速度要慢得多。 要低得多。最后，如果你的InfiniBand也用完了，而你拥有这些庞大的Pods， 那么你就需要

跑。通过 Ethernet 把它们连接起来。而在 Ethernet 中，数据必须经过 PCIe，还要实际经过 CPU，正如我们后面会看到的，这甚至更慢。好的，所以这有点类似于内存的情况。呃节点越多，你没法让一个 NV-Switch 去处理大约 10 万个 CPU，谢谢。还有一点我要说明，我之前暗示过绕过 CPU 这件事，从硬件角度来看，这将会是一个重要的方面。所以，如果你用的是传统 Ethernet，情况是 GPU 必须与 CPU 通信，才能完成它的数据复制。呃 this 所以，它基本上得把数据复制到这，CPU 里有个内核套接字缓冲区，这里的 kernel 是指内核，不是 GPU kernel，而是 CPU 传统概念里的内核。接着，它得组装一些网络数据包，复制到网络接口，再发出去。3-year 所以，这通常会带来很大的延迟，于是有了远程直接内存访问 RDMA 这项技术。它让 GPU 能直接读写，直接读写另一个 GPU 的内存，整个过程完全不需要 CPU 参与。所以，显然如果你用的是 NVLink 和 NV-Switch，那就具备 RDMA 功能。啊 InfiniBand 也支持 RDMA，所以如果你通过 InfiniBand 连接，GPU 就能直接互联，无需 CPU 介入，但标准 Ethernet 就不行了。我要提两个值得注意的进展。这就是说，NVIDIA 一直在大力突破用越来越大的集群能实现什么的极限。所以针对 B200 和 B300，他们推出了一种叫 NVL72 的产品。也就是说，他们用的是装 8 个 GPU 的托盘，但一共有 9 个这样的托盘。因此，归根结底你就有 72 个 GPU，它们全部通过 NV-Switch 连接到同一个 NVLink 域中。如果你还记得，NVLink 的速度非常快。所以，通常来说，如果你只是个普通用户，你会想，好吧，我有 8 个 GPU，它们之间以极快的速度互联，而在这个范围之外，速度就会大幅下降。但如果你有足够的钱，就可以购买这种非常高端的硬件，你就能在多达 72 个 GPU 之间实现极快的互联。另外我要提到的一点是，我之前说标准以太网不支持 RDMA，但在以太网方面也有进展。所以有一种叫 RoCE 的技术，也就是基于融合以太网的 RDMA，它能让以太网直接绕过 CPU。And 这算是他们针对 InfiniBand 给出的解决方案。InfiniBand 通常价格不菲，NVIDIA 的许多产品也是如此。呃但你可以获得相当不错的性能。用的就是基于融合以太网的 RDMA。有比如 Meta 发过一些论文，显示他们正在研究这个方向。Llama 的训练可能用了融合以太网，也可能没用到。Like 好了，以上就是对硬件大致情况的简要概览。GPU 之间通过 NVLink 连接到 NV-Switch，这个连接范围可能跨越 8 个或 72 个节点，随后再通过 InfiniBand 互联。谢谢。接下来，我们谈谈如何对它进行编程。在最底层，有一个名为 NVIDIA Collective Communications Library，NCCL，读作 nickle 的库，它负责转换集合通信操作。把 all-reduce、reduce、broadcast 等操作转换为在 GPU 之间实际传输的底层数据包。当你使用 NCCL 时，它本质上就像是在说，我想要执行 all-reduce。然后 NCCL 会分析硬件的拓扑结构，确定不同 GPU 之间的通信路径，并实际启动 GPU kernels 来发送和接收数据。因为归根结底，请记住，在 GPU 上运行的所有内容本质上都是一个 kernel。另外还有一些专门负责与其他 GPU 通信的通信 kernels。好，我们就不过多讲 Nickle 了，只要知道它存在就行。接下来我们要讲 PyTorch。在这之前，大家有什么问题吗？对，就是关于硬件的，你能具体描述一下吗？它看起来像机架和托盘那样吗？我能从物理结构上描述一下机架和托盘吗？就是机架和托盘分别指什么？关于 NVL72，我不是硬件专家，但机架说白了，我见过数据中心，就是这么一个机架，而每个托盘大概就是这样。里面装得一些东西，G 代表 Grace，所以里面有两个每个 CPU 连接 4 块 GPU，因此每个托盘上有 8 块 GPU，它们堆叠在一起，所有设备都连接到这个 NV-Switch。谢谢。是的，所以问题是区别究竟在哪里，在 RDMA 和 RDMA 之间？是的，你可以把 RDMA 理解为更像是一种，一种期望特性。RDMA 意味着一个 GPU 可以读取和写入另一个 GPU 的内存。实现 RDMA 有多种方式，一种是使用 NVLink 和 NV-Switch，另一种是使用 InfiniBand。因此，InfiniBand、NV-Switch 和 NVLink 更多是指硬件，也就是有哪些组件，比如有哪些线缆和交换机。而 RDMA 更多是指操作层面，也就是通信时发生了什么。也就是你通信的时候，所以总希望你能在网上读到那些事件，就像一周一样。所以还有其他途径，我们其实不需要深究这些细节，比如基于融合以太网的 RDMA 就是另一种实现 RDMA 的方式。用的人多吗？好的，好的。对。那 NCCL 是专门针对什么优化的？是多节点集群，虽然这听起来很直观，但它其实适用于更通用的场景。但如果是针对 RDMA，NCCL 是可选的吗？It's it's 所以问题是 NCCL 是不是针对多节点集群？谢谢。所以我的，我不太清楚他们具体有没有针对这一点做优化，细节我不了解。我只能说英伟达基本上一直在优化其整个软件栈，用于这些大模型的推理和训练，因为他们的主要客户都是那些大公司。云模型提供商，所以如果他们没想过针对这类工作负载进行优化，我反倒会感到意外。谢谢。是的。你遇到的工作负载里，最常见的，你能完成大约 90% 工作的，是哪种情况？谢谢。你正对着镜头说话。问题是，如果你有 9 块 GPU 会怎样？你如何在这些 GPU 之间分配工作负载？我想这取决于这 9 块 GPU 在你的配置里是如何分布的。比如很多时候你会有，假设每个节点有 8 块 GPU，那第 9 个就会在另一个节点上，要是你没，哎遭了，要是没有 NVLink 把它们连起来，那情况就会变成另一个样子。那就太糟糕了，因为那个节点提供的 compute 很少，而且跟它通信的成本极高。但如果你比方说所有东西都有 NV-Switch 硬件，那情况就会是另一个样子了。那就合理多了。好，再问一个问题，然后我接着讲。好的，那么这跟 TPUs 有什么区别？让陶述来多讲讲 TPUs。呃，所以我，我是说，呃我们看看，我该怎么快速解释这个？所以，TPU 通常是结构简单的多的硬件。我对这些组件具体如何对应的细节不太熟悉，但我们可以在课后讨论。谢谢。好的，让我们实际编写一些代码来利用这些硬件。PyTorch 很方便地提供了一个 torch.distributed 库，该库为这些集合操作提供了简洁的接口，因此你无需显式地考虑 NCCL。事实上，该库还支持针对不同硬件的不同后端。因此，如果你使用的是 GPU，就使用 NCCL 后端；如果你使用的是 CPU，则有一个名为 Gloo 的工具。正如我所说的，并行处理在 GPU 出现之前就已经存在很久了。你也可以在 CPU 上执行这些集合操作。这个库还支持更高级的模型和算法，比如不过我们这门课不用那些，因为我们要从头开始搭建。好了，我们来过几个集合操作的基本例子。这里有个叫 spawn 的函数，它接收另一个函数，意思是把这个函数在 world_size 为 4 的情况下复制运行 4 次。我们来看看它做了什么。这其实是我写的一个封装函数，主要是为了绕过我在本次讲座中无法进行 multiprocessing 的限制。正常情况下，你会调用 torch.multiprocessing.spawn，然后传入那个函数。好了，我会用这个分支，它禁用了分布式功能。我们过一遍流程。现在，我在这个函数里，它本该为每个进程异步执行。请记住，world_size 是进程的数量。好吧，rank 则是 0, 1, 2 一直到 world_size - 1。所以，会有和 world_size 一样多的这些函数同时在各自的进程上运行。好，我现在是 rank 0，那我该怎么做？先有个 setup 步骤，基本上你要配置 master 的 TCP 端口。注意，这其实不是 GPU 之间通信的方式。这更多是为了通用的元数据和协调。实际数据走 NCCL，否则速度会非常慢。如果你有 CUDA 就可以用 NCCL 后端，我在笔记本上，所以打算用 Gloo 后端。好，现在我在这里运行，假设我有 4 个这样的进程在跑。这里有个屏障函数，它可以用来做同步屏障。简单来说，

一旦遇到这个，
他就会等所有进程都走到这一步。
基本上你可以把所有进程看作是异步运行的，我没法保证某个进程一定比另一个先跑完，它们的完成顺序可能是任意交错的。
所以为了确保某些代码先于其他代码执行，我会加上这些同步屏障。所以多加屏障的坏处是，你可能会陷入不必要的等待。
好，我们来试一下 all-reduce。好，接下来我会创建这个张量，从 0 到 width 3。
我设置了 rank，只是为了让例子更有趣一些。每个 rank 将拥有不同的张量。
我会在 all-reduce 之前把我手上的内容打印出来。那现在我跳到这里。
看看会打印出什么。
那么，
到 all-reduce 之前，rank 0 是全 0, 1, 2, 3，rank 1 是 1, 2, 3, 4，以此类推。
这和我之前展示的示例一样。
注意，打印语句是按硬件决定的任意顺序出现的，因为他在异步运行，但所有数据都在那里。
好，那么现在如果我执行 all-reduce，
你将这些作为参数传入，这是一个 PyTorch 函数，它接收该张量，
你传入规约操作，这里是求和，我要说明不要进行异步操作。
它的作用是调用在这种情况是 Gloo，但也可能是 NCCL，此时它会启动 CUDA kernels 执行通信，并为你处理好一切。
然后它基本上就地写入数据。
因此在 all-reduce 之后，我得到了 all-reduce 操作的结果。
记住，
这是每一列的和，但复制到了所有 rank 上。Thank you.
谢谢。
好的。
这就是 all-reduce。如果你想更高级一些，执行异步操作，可以设置 async_op=True。
但那样会搞乱所有这些 print 语句，所以我试着插入比平时更多的 barriers。
对，有个问题：你在骗人吗？
谢谢。
那么，
问题是 rank 到 GPU 的对应关系？在这门课里 rank 就是 GPU。
是的。那我们在这里是另一个例子。
我要做一个 reduce-scatter 操作。那么在这里我要创建一个输入，从 0 到 world size。
然后我要，嗯，把输出处理好，我要为输出分配好空间。
那么在我执行 reduce-scatter 之前，它看起来是什么样？它看起来像
我看看。
这个其中
每一块的情况都差不多，输入和输出碰巧都是 0，但其实可以是任何值。
然后
我执行一下 reduce-scatter 张量操作。这里我不做原地写入，而是分别用了一个输出张量和一个输入张量。我指定要做求和。
然后接下来，
我得到的结果是输入基本没有被改动，但在输出里我把每个分量的规约结果写进了对应的 rank。
当然。
你，你谢谢。
还有问题，谢谢。
其实本来是不需要它的，谢谢。好的，问题是这样：异步执行 all-reduce 是怎么做到的？也就是说这在某种程度上就像是一个整体式的操作。你说去执行 all-reduce，它就启动 CUDA kernels 去执行通信。啊，而且要记住，
CUDA 本身相对于进程就已经是一步的。现在我们让所有进程也都异步了。关键在于这段代码执行后会立即返回。
然后，然后你就可以去做其他事情了。
所以有一种典型的做法，这门课我不会展开讲，就是让计算和通信相互重叠。
比如说你可以执行这个操作，然后接着去加载下一步所需的其他数据，因为这些数据和这个操作是相互独立的。然后当你想要确认
当这些确实都完成时，你就可以调用一个 wait 等待或者 barrier 屏障。
好，我们来处理最后一个，也就是 all-gather。
至此，我想你已经大致理解了其中的思路。在这里我把 reduce-scatter 的输出设为输入。我会先分配一个输出缓冲区分。所以在之前的 all-gather 的过程是这样的。这里我有来自 reduce-scatter 的结果，这个输出缓冲区刚刚分配好，里面碰巧带有一些值，但不用担心。
这一点。然后在我把数据 all-gather 到 tensor 之后，
然后我会
得到，
你知道
所有不同的输入，
你知道，
数据被聚集到所有不同的 rank 上。
好的，在这里你可以看到确实如此。
我们通过一个例子来证明：
all-reduce 其实就等于 reduce-scatter 加上 all-gather。
好的，最后做个收尾。
就像我开头先做了设置那样，然后我把环境清理掉。
你知道的，就是
清理环境是个好习惯。
命令，好的。所以这就是你第一个 torch.distributed 程序的例子。
你知道的程序。好的，那么我们来做一些
做个基准测试。这会很快，因为我其实想继续进入第二部分。那么通信速度有多快呢？我们来做一次 all-reduce。
这里我要对一亿个元素做 all-reduce。
哎呀，好吧。
抱歉，我弄错了。好吧，那我做一下 reduce。所以我要创建一个你知道的包含这么多元数的张量。记得跟之前一样，做基准测试前先预热一下。这里我会调用 cuda.synchronize 和 barrier。
只是为了确保，因为这里存在两种异步情况：CUDA kernels 和不同的 processes，我想确保一切都同步到位。计时开始前，有些操作其实没在跑，已经完成了。接下来我要做 all-reduce，
然后再次通过 barrier 同步等待，
停止计时。
好。
所以记住，这是在每个 rank 上运行的。所以，如果看一下这里的输出，针对 rank 0, 2, 1, 3，
我得到的时间可能不同，因为它们都是独立的进程，每个进程都会报告某个你知道的测量值。
如果你想报告一个数值，可以取平均值，比如
所以，
现在有一件有用的事要做，就像我们算 MFU 时那样，就是计算。我们来测一下有效带宽。
思路是这样的：
刚才花了 1.6 毫秒，具体是多少？这算好还是不好？
所以要算有效带宽，我们得先算出实际发送了多少字节，
以及理论上应该在这段计算期间传输的数据量。
然后用这个数据量除以总耗时，就能得到有效带宽。
好，那我发送的数据量大小是
每个元素的大小乘以元素个数，这基本上就是这个数据张量的字节总数。
实际发送了多少字节？这里需要稍微拆解一下。
所以对于 all-reduce 操作，为了简化起见，假设我们要把 rank 0, rank 1, rank 2 和 rank 3 的值相加，由于总共涉及 world size - 1 次加法运算，因此需要迭代 world size - 1 步。这就是这个因子的来源，之所以乘以 2 是因为既需要发送，也需要规约。
然后乘以负载的大小，这就得到了发送的总字节数。总持续时间指的是墙钟时间，也就是实际消耗的时间。
然后乘以 world size，因为这是所有 rank 加权后的总带宽，即发送的字节数除以总持续时间。
好的，所以在这种情况下，结果大约是每秒 400 GB。好的，那么这里有几点要说明：
第一点，看这个表达式：
有效带宽等于数据大小乘以 2，再乘以世界大小减 1，然后除以世界大小与持续时间的乘积。
所以随着世界大小增加，世界大小减 1 除以世界大小这一项基本趋近于 1。
所以实际上就只剩下数据大小（字节）乘以 2 再除以持续时间。
所以这本质上就是带宽。注意，它与世界大小无关。
这是好事，也就是说当你增加 GPU 数量时，带宽保持不变。它也独立于拓扑结构，而拓扑结构是环形还是树形由 NCCL 来确定。所以就是这样。好，这就是 all-reduce。
reduce-scatter 的操作也非常类似。
所以我会先创建输入和输出，做个预热，执行操作并计时。啊，注意看，reduce-scatter 有这些，
这些计时数据。
你也可以在这里测量有效带宽，
也就是输入数据的字节数。
你统计的是发送出去的字节数，注意这里没有乘以 2。
然后用总耗时去除，就得到了带宽。所以这里的带宽就是这么算出来的。
应该非常相似，我想有时会有一些随机性，但结果大致在 400 多。所以
这里说明几点：
请记住，all-reduce 正如我们所说，由 reduce-scatter 加上 all-gather 组成。
因此 all-reduce 自然要移动两倍的数据量，因为 reduce-scatter 会有开销，all-gather 也有开销，而 all-reduce 执行了双倍的工作量。
但它花费的时间也是双倍，不过两者相互抵消。

好，因此你得到的带宽是相同的。OK？好吧？你，嗯，你好，第一部分到此结束。也许在我进入第二部分之前，大家有什么问题吗？为什么你必须同步执行那个操作？对其他人来说，谢谢，谢谢，谢谢，谢谢。那么为什么我们必须为 CUDA kernel 做同步？所以归根结底，我们仍然是在执行 CUDA 操作。对，我们只是有多个 process，每个 process 都配有一个 GPU 正在执行一些 CUDA 操作。然后如果你在执行 CUDA 操作，请记住默认情况下它是异步的。所以当你执行到 Python 中的下一行时，那个 CUDA 操作可能尚未完成。所以我们需要始终等待，通过同步来确保它已完成。是的，所以反过来，那么你是必须先做 barrier，然后再做 synchronize 吗？呃，不是，不太确定吧。所有设备读取代码的设计，然后等待所有，呃，对。我觉得有个问题，如果你先调用 barrier，队列可能还没跑完，正在运行，而你却立刻去执行 barrier，这样你只是在各自独立的同步不同的 CUDA kernels。这意味着并没有真正同步。比如 barrier 如果那些操作都返回了，barrier 其实并没有起到作用。好，接着往下讲。现在我们来谈谈如何训练模型。好，我们接下来会过一遍一个非常基础的 MLP 训练实现，多层 MLP。嗯，我想这有点重复，因为 MLP 本身就已经是多层感知机了。别忘了，MLP 才是 Transformer 里真正的 compute 瓶颈，所以这个例子很能说明问题。好，数据 parallelism，张量 parallelism，流水线 parallelism。请在脑海里想象这张图。这是个示意图，不用想得太复杂，它主要是帮你理解如何切分数据和参数的一种方式。数据 parallelism 的做法是把数据拆开，分成几块，然后每个 GPU 负责一部分数据，我照常，我会维护所有参数，进行正常的模型训练。接下来我需要同步，我来解释一下这是怎么运作的。所以我要生成一些样本数据，批量大小是 128，维度数是 1024。所以这是一个批量大小乘以维度数的矩阵。数据矩阵，咱们直接开始。这就是数据 parallelism。明白吗？它的工作方式是你拥有这个数据矩阵，我会把行拆开。分成数量等于 world size 的若干份，比如这里就是四份，每个 rank 会分到一份。啊，好的，这就是维度数和批量大小切了，所以每一份我基本上就管它叫本地批量大小。你知道，就是批量大小除以 world size，也就是每一个 GPU 看到的会有 32 行，32 个数据点。嗯，这里指的是索引的起始位置。嗯，用从起始到结束的索引，就能取出该数据的一个切片。他们会把它放到哪里？啊，放在那个 rank 上。好，至此，每个 GPU 都持有一个独立的数据 tensor，即它负责处理那部分数据。实际上，每个 rank 通常会加载自己的数据，以避免这个瓶颈，不过这里只是为了演示方便。好，接下来我们实例化一个 MLP，我现在开始操作。假设我们设定了层数 num，嗯，以及层数 num。对于每一层，我将使用一个尺寸为 num_dim * num_dim 的矩阵。嗯，所以我只是初始化一组随机参数，然后我会将其输入优化器。好的，这就是训练循环。在前向传播中，我获取数据。请记住，数据并非全部数据，如果我是 rank 2，那么我只获取 beta，也就是数据的一部分。我会遍历这些层，执行前向传播。那么我会执行反向传播。通常到这里就结束了，但是现在请记住，每个 rank 拥有不同的数据，因此 gradients 也会不同。同样地，所以这正是让数据并行（data parallelism）生效的关键步骤。我们要在所有工作节点之间同步梯度，这是标准训练和 DDP 之间唯一的区别。其实这挺优雅简洁的，基本上对所有参数我都要做一次 All-Reduce，也就是对 param.grad 做，然后取平均。这个 All-Reduce 做完之后，此时每个 rank 拿到的梯度就完全一样了。然后我就直接更新参数。好的，我觉得这挺优雅的，因为它本质上就是标准训练，也就是把它用在你本地的 batch 上，不过你只要把它插在反向传播后面就行。我们就用这个把所有梯度平均一下，我做一次 reduce，就一行代码的事。代码，改完后参数就更新了。所以训练时每个 rank 看起来像是在用全部数据做参数更新，但其实它只处理了其中一部分。基本数据，好，这基本上就是 DDP，也就是第一种数据并行方法。啊，行。大家对此有什么问题吗？有，谢谢。问题是，是不是只有 batch size 大于 1 才能这么做？对，所以为了让这招真正有意义，你的 batch size 至少得等于 world size，而且通常应该更大一些。好，对。谢谢。对，问题是 batch size 是不是得是 world size 的倍数？要是倍数当然更好。对，不是说如果不是的话你可以补零或者做点填充，所以确实是有办法的，不过如果是倍数的话对大家都会更省事。What would be the possibility for silver? 能聊聊针对 Transformer 的 DDP 吗？还有做成银级方案的可能性有多大？那基于 Transformer 的情况会怎样？嗯，不是，对于 Transformer 里具体是什么样子？其实基本是一样的。DDP 有个好处就是它的模块化做得很好。你做前向传播，我的意思是 DDP 在这里只是对参数做平均。OK？它并不在意你的前向传播具体是什么样子。好，我接着往下讲。好，这就是 DDP。所以总结一下，各个 rank 之间的损失值是不一样的，梯度在一开始也是不一样的，但他们都会经过归约在各个 rank 之间变得一致。因此，参数在各个 rank 之间也保持相同。好的，下一节课 Tatsu 将讲解更高级的数据并行技术，也就是 FSDP 和 ZeRO。那里的思路正如我刚才提到的，这里我们使用 All-Reduce，它就像一个非常简单的单体操作，但它确实需要把模型的所有参数都保留在内存中。但如果模型参数太大，内存装不下怎么办？那你就得想些更巧妙的办法，这正是下节课的主题。好，哇，下面我来讲讲 tensor parallelism。所以这里的思路是我们沿这个方向切。我的意思是，我们不切数据，我们切的是，这其实每一层，所以每个 rank 都会分到每一层的一部分数据。是的。好的。通常来说，这意味着我们需要传输的数据量会大得多。我们稍后会再讨论一下。嗯。那 tensor parallel 具体长什么样呢？好，那么我们先做个简化假设，每个 rank 都持有全部数据，这里记住数据维度是 batch size * num_dim，我来定义一个 local num_dim，对于这个 rank 我只负责其中一部分，负责一部分维度。所以这里的示意图是说每个模型仍然包含所有层，但对于每一层，现在参数的数量就是 num_dim 乘以一个局部数值。好，假如这是某一层的参数矩阵，我会沿着列方向来做切分。这也叫作列式张量并行，其实也可以按行切分，但我们现在先不讨论。谢谢。好，那前向传播具体是怎样进行的呢？我应该会遍历所有层并计算 activations。所以，呃，我是从，啊，X 也就是数据开始，然后去访问第 layer 层的参数。呃，第 layer 层。注意看，这仅仅是参数的一部分，对吧？所以如果我是 rank 1，我就只能拿到矩阵的这一部分。但我仍然可以继续。我可以应用这个非线性函数，因为它本来就是逐元素进行的。啊，但现在我要做的是，也就是把 activations 通信出去。所以如果我有一个数据矩阵，rank 1 拥有这部分矩阵对应的部分 activations，然后 rank 1 拥有这个矩阵的部分 activations，以此类推，而我基本上需要把所有 activations 都放到所有 rank 上。而且我们知道该怎么做，我们之前介绍过 All-Gather，呃，作为一种集合通信原语，所以我拿到了，这就是所有的 activations？所以这就是 passes 大小乘以局部维度数，即 activations 的形状。然后我执行一次 All-Gather，抱歉，这里是在为 activations 分配内存，X 是 activation 的实际部分。所以这是批次大小乘以局部维度数。All-Gather 的意思是每个 rank 都持有 X，每个 rank 都会分配 activations，它是一个列表，元素数量与 world size 相等。然后在 All-Gather 之后，X 会被复制到各

给出对应的 activations 中。谢谢。好，一旦我收集了所有 activations，我就把它们拼接起来，形成全维度的 X，其形状是批次大小乘以总维度数。好，关于列张量并行，大家有什么疑问吗？所以每一层都要这么做？嗯，我们现在大家注意，数据并行和这里有个区别，我们现在得对模型本身动点手脚。数据并行非常优雅，因为它是按数据切分的，模型被当做一个模块来处理，但现在我们必须改动模型。嗯，这很大程度上利用了这样一个事实：如果你要做矩阵乘法，可以把它拆成一组小矩阵乘法。我们可以在不同的 rank 上分别执行这些运算，然后把结果汇总起来。而等到了反向传播的时候呢，猜猜看，因为你的 radiance sock 还是... 谢谢。对，那现在问题是反向传播时会发生什么？现在，就，在反向传播里，你有 activations，你需要做 reduce-scatter，分发到所有不同的部分。那就，从某种意义上说，all-gather 和 reduce-scatter 具有这种双重性。在前向传播中，如果你执行的是 all-gather，那么在反向传播中，你执行的就是 reduce-scatter。那么问题是，这些是由 autograd 自动完成的吗？啊，这些都不行，没办法。如果你只是调用 .backward()，它不会这样做，因为其中没有 parallelism。就，但是你知道的，PyTorch 自带了所有这些功能，很多事情都会自动为你完成。嗯，如果我换成另一种做法，我大概需要添加什么或者做些什么呢？需要在多大程度上修改我的代码？又在多大程度上可以保持原样直接沿用？那么，在这里哪些是自动为你完成的，哪些不是呢？嗯，在这里我们相当明确地手动管理这些事务，这意味着我不去做反向传播，而你必须自己管理并调用 reduce-scatter。好的，是的，谢谢。这就是特意这样设计的，因为这门课是 336 从零开始构建语言模型嘛。虽然你通常不必这么做。好，那我们快速讲一下流水线并行。流水线并行的思路是，我们把网络这样拆分，这样每个 rank 会分到一部分层。每层内部它会拿到所有的维度，它还会拿到所有的，呃，算了。其中一个 rank 会持有全部数据，每个 rank 都会以某种形式看到所有数据。OK，so... 所以这就是它的工作方式。好，那么，我们来看看，我们拥有所有数据，其大小还是批次大小乘以 num_dim。所以我要把层拆开，这样 local_num_layers 就是每个 rank 负责处理的层数。接下来会有 local_params，啊，本质上就是那些局部层对应的参数。不过在每一层内部我会做 num_dim 乘 num_dim 的计算。好，所以有件事我，也许 Tatsu 周三会再细说这个。这是 micro-batches 的概念吗？嗯，那我就直接展示一下，并解释我为什么要这样安排。所以除了拆分成之外，我还要把 batch 拆成许多 micro-batches。那么如果我是 rank 0，我就获取数据，我会把它拆分成若干个 micro-batches，每组四个，呃... 对，不是 micro-batch，我要做的是，呃，接收，嗯，从上一个 rank 接收，然后执行前向传播，只对分配给这个 rank 的那些层，然后再发送给下一个，你知道的，rank。好，所以这里我其实用的是接收和发送操作，它们是点对点通信。所以我之前没细讲这些，但它们的意思其实挺直观的。这基本上是说我是 rank，我会把这个 tensor 发给，呃，呃，我会从 rank-1 那里接收这个 tensor。嗯，这表示我会把 tensor X 发给 rank+1。好吧，所以所以基本上原因是，嗯，我在讨论 micro-batches，Tatsu 周三会更多地讲这个。啊，这不是 pipeline parallelism 吗？所以你有一个 rank 获取数据，它处理一部分层，然后把它传给下一个，嗯，GPU 它处理一部分层，然后把它传给下一个 GPU。你知道的。So，嗯，这是一种拆分深度网络得很自然的方法，但问题是会产生所谓的 pipeline bubbles，也就是在等待其它张量处理时，你其实是在空等，并没有在计算。这最终导致效率相当低下。所以微批次的想法是把数据拆成更小的批次，这样你就能快速处理并传给下一步，从而减少那些 pipeline bubbles。另外我要提一下，在这个很初级的版本里没处理的一个概念是，计算和通信的重叠。这其实对 pipeline parallelism 非常重要。这基本上给了你正确的结构，如果你在这些前面加一个 "I"，它就变成异步的了。你得加些东西来管理代码。这里的想法是，当你在这儿计算的时候，你应该，你可以接收或发送数据。所以计算和通信应该重叠起来，这样就能减少你实际花在等待上的时间。好，这里有些内容没讲到，希望下次能补上。即通信与计算的重叠，这在 pipeline parallelism 中尤为关键。我之前没提 data parallelism 的情况其实也一样，因为我只做了一次前向传播，最后再做所有这些 all-reduces。但如果你处理得当，在后向传播中梯度一算完就可以开始发送。这样你就能更充分地把通信和计算重叠起来。啊，有人问到通用模型的问题。同样，我觉得这个 MLP 基本上涵盖了你所需要的一切。也就是理解基础知识所需的大部分内容。嗯，一些规模更大的模型需要做更多的簿记工作，因此更难看清核心的核心算法。还有一些其它类型的 parallelism 我们还没讲到，比如 sequence parallelism，它把整个序列拆分成小块，从而实现 attention 计算的并行化。还有 expert parallelism，它可以并行处理 MoE 中的 experts。这正是我之前提到的 all-to-all 通信派上用场的地方。啊，此外，不同 parallelization 技术的各种组合也会出现，这些内容在作业中也会涉及。吧，嗯，所以要注意一点，你选择哪种 parallelism 技术，很大程度上取决于取决于硬件。例如 tensor parallelism 涉及大量的通信，因为对于每一层你都需要发送这些相当庞大的 activations。所以一般来讲 tensor parallelism 是在节点内部进行的，也就是在拥有高带宽的 NVLink 连接处。相反，你不会在超出 NVLink 域的范围使用 tensor parallelism。嗯，至于 pipeline parallelism，你会看到人们在使用它，而且它通常能容忍慢得多的啊，互联网络。所以一些去中心化训练工作会使用 pipeline parallelism，因为你的节点 GPU 实际上分布在世界各地，但你不会想在那样的网络情况下使用 tensor parallelism。就样。所以当你看到这些组合时，通常是节点内部的 tensor parallelism，嗯，然后是接着是 pipeline data parallel 或者 FSDP，如果需要的话，最后是 pipeline parallelism。还有其他因素，比如如果你做 data parallel，你可能能进行大量的 data parallel，但随后你会遇到所谓的 critical batch size。如果你把 batch size 增加得太多，但三者中对你没有帮助，在这种情况下你只是在浪费 compute，那么使用 tensor parallelism 会更好。所以这里有很多需要考虑的因素，我们之后在课上会详细展开。啊，最后补充一下刚才稍微提到的 TPU。有一点要注意，我们特意选用 PyTorch，而且不只是用 PyTorch，更是用一种很基础的方式去调用 collective operations，这样你们能看得更清楚。另一种做法，尤其是如果你在用 JAX TPU，你可以直接定义模型以及分片策略，编译器实际上会处理很多决策，比如基本确定你需要什么样的通信操作。你基本上就是说，这块数据得放在这儿、这儿和这儿，然后编译器就会通过一些魔法来搞定其余部分。这确实很吸引人，但显然这样会少了很多从零开始构建东西的乐趣。好，简单总结一下，并行化的方法有很多种，你可以按数据、张量或专家来切分，也可以按流水线或序列来切分。我们之前讨论了数据 parallelism，当时只是涉及 DDP。下次我们会讲 FSDP 和 ZeRO。正如我提到的，tensor parallelism 对互联速度要求极高，而 pipeline 要求稍低，但你得下功夫减少 pipeline 气泡。从更宏观的角度看，这种模式很常见，对吧？所以你可以选择重新计算或者存储在内存中。当我们讨论 activation checkpointing，啊，或者在使用 GPU 时做某些操作时，或者在这种情况下去

可以把它看作一种扩展，即你可以存储，呃存在不同的 GPU 上。从这个角度看，在数据 parallel 中你做的是冗余工作，从某种意义上说，因为每一个 rank 实际上都在更新其参数并跟踪所有参数。之所以这么做是因为你无需将 optimizer state 在不同设备间传输。呃，所以一方面硬件变得更快了，但从某种意义上说，我们总是需要更大的模型。所以采用分层结构这一思路将一直适用。好，今天的内容就这些。下周三 Todd 会深入讲解更多的 parallelism 机制。
