> 来源：3小时前端入门教程_03_定位与JavaScript基础：变量、函数、事件与DOM(P13-P19).m4a｜块时长 50.7 分钟｜转录端点 gemini-proxy-asr｜续读 1 卷｜读完=True

这节课我们来讲定位。这个浮动布局虽然灵活，但是呢不容易控制。而定位布局呢，就可以让用户精准地定位页面中的某一个元素。那反过来，我们是不是也可以说，这定位元素它缺乏灵活性，虽然容易控制，但是也给空间大小和位置不确定的版面布局带来了麻烦。所以我们要辩证地看待这个问题啊。这个定位方式呢有三种。第一种是相对定位，相对定位呢，就是相对于元素在文档流中的正常位置进行定位。第二种呢是绝对定位，它是相对于与它最近的已经定位的父级元素进行定位的，而且不占据文档流的位置。最后一种呢是固定定位。固定定位是相对于浏览器窗口进行定位的，不占据文档流，固定在屏幕上的位置，并且呢不随滚动而发生移动。那我们就先来看这个相对定位。那就 h1 相对定位。咱们先来画几个盒子。那先画一个大的盒子，就叫 box1。那有 box1，一会儿就还得有 box2 对不对？box1 里面呢有几个子元素。这普通的元素呢我们叫 box-normal。把它给复制上三遍。再把中间这个 box-normal 呢给改成是我们要练习这个相对定位，那就改成 relative。relative 就相对嘛。一会儿就把咱们中间这个子元素呢给它变成是相对定位。那盒子建好了，接着就是添加样式，style。先给咱们这 box1 添加样式。那给它一个，宽度不需要给了，给它高度吧，高度给它一个 350。再给它添加个颜色，就 aqua。接着咱们给这个 box-normal 给它添加一个什么呢？嗯让它宽度和高度都变成 100。再添加个背景颜色，背景颜色就 purple 吧。那最后就是咱们这个相对定位的标签，box-relative。它的宽高也都是 100。颜色咱们给它改成是 pink 吧。那现在如果我想把这个元素改成相对定位的话，我们只需要使用这个 position 属性，然后呢后面的属性值就写成 relative 就可以了。那现在还没有任何变化，因为我们虽然把它改变成这个相对定位了，但是使用相对定位我们还没有改变它的位置。而且呢，相对定位的元素并不会脱离我们正常这个文档流。如果我想改变它的位置的话，就可以使用 left 或者 right、top 和 bottom。left 呢就是距咱们这个元素原本位置最左边偏移多少距离。right 就是距离它最右边偏移多少距离。top 就最上边，bottom 呢就最下边。就比如现在我想让咱们这个元素呢向右移 20px，向下移 40px。咱们就可以写成是 left 向右移 20px，那就是去左边 20px 呗。咱们看它是不是向右移了？然后就是 top 向下移 40px。是不是也就向下移了？我们把这个左边这个值给它改成 120，再看一下啊。现在我们注意啊，虽然我们这个元素它移动了这个位置，但是咱们下面这个 box-normal 这个元素呢，它依然保持在原位。这就是我们刚才说的，相对定位呢它的元素并不会脱离正常的文档流。那接下来我们再来讲这个绝对定位和固定定位。绝对定位和固定定位就不一样了，就比如绝对定位，我们再创建一个盒子二，那就是.box2。那里面的内容呢，咱们就把上面的给它复制过来。中间之前是这个 relative，现在是绝对定位了，我们改个名字，那就 absolute。先给咱们这个 box2 添加一些样式。那也是给它添加个高度和这个背景颜色就行了。背景颜色给它变一下，改成 yellow 吧，黄色。那接着就是给咱们这个 box-absolute 这个类呢设置一下属性。那宽高都还是 100。颜色给它一个 yellowgreen。现在有点太靠下了啊，咱们给咱们这个 box2 添加一个 margin 外边距，让它距离最下侧呢添加一个 400px。给它移动一下，400 多了点，300。那接着呢我们给这个 box-absolute 这个类，把这个类的元素呢修改成绝对定位。那就是 position，这个值呢就变成 absolute 了。我们看第一个这个 absolute 就是。注意啊，我们一旦添加了这个 position-absolute，咱们这个下面的紫色元素是不是就消失了？这是因为我们这个相对定位的元素呢就脱离了正常的文档流，所以呢下面这个紫色元素就自动地上浮了。这里看得不明显，我们给它添加个 left，那还是 120px 的距离。咱们看下面这个紫色元素是不是就显示出来了？那还差最后一个固定定位。那固定定位与绝对定位唯一相同的地方就是，固定定位呢它也是脱离了正常文档流。但是不同的地方就是，绝对定位呢它是相对于元素最近的已经定位的父级元素进行定位的。如果没有已经定位的父级元素呢，它就相对于我们这个 HTML 标签定位。而固定定位它是相对于浏览器窗口进行定位的，即使我们这个页面滚动，元素呢依然会保持在这个窗口的相同位置。比如我们现在就直接创建一个固定定位的盒子，就叫 box-fixed。再设置一下它的样式，那就.box-fixed。宽高各自 100。颜色给它添加一个 brown 吧，棕色，棕色也挺好看的。我们看现在咱们这个元素是不是就在我们页面的最下方？现在我们给它添加一个固定定位，position: fixed。哎现在我们给它添加一个固定定位，是不是我们在页面中就看不到它的位置了？那是因为我们还没有定义。比如我们想让它出现在咱们这个屏幕的最右边中间的位置吧。那就 right: 0，据右边是 0，然后再据上面 300px。咱们看，我们这个棕色盒子是不是就出现在了我们这个页面的右侧中间的一个位置？并且不论我们怎么滚动这个鼠标的滚轮，这个页面咱们这个棕色盒子依然出现在这个位置。是不是就很像很多页面出现的这个侧边栏，还有顶部的导航栏？不管你怎么滚动的话，咱们这一块总会有一些小图标都出现，还有就是顶部这个导航栏也会一直出现。这个呢就是固定定位的常用方法。当然啊，选择使用哪一种定位方式取决于具体的设计需求。通常相对定位就用于微调元素的位置，而绝对定位和固定定位呢，用于创建更复杂的布局。

这节课开始呢，我们学习前端三剑客的最后一部分内容，JavaScript。首先你要明白的是，JavaScript 与 Java 呢没有任何的关系。JavaScript 又简称 JS，是一种轻量级、解释型、面向对象的脚本语言。它呢主要是被设计用在网页上实现动态效果，增加用户与页面的交互性。起初呢它被设计的时候，就是想定义一种简单的脚本语言，用于处理用户在浏览器中的输入。但随着浏览器技术的不断发展，JS 呢也逐渐演变成一种功能强大的编程语言。当它作为客户端的脚本语言时候呢，JS 可以直接嵌入在 HTML 文档中，并且呢在浏览器中执行。和 HTML 与 CSS 不同的事啊，JS 的出现就使得网页不再是静态的了，而是可以根据用户的操作动态变化。JS 语法也比较灵活，易于学习，是前端开发的核心技术之一。咱们呢就可以用 JS 来处理用户输入啦，修改页面内容啦，与服务器进行交互等方式，为用户呢提供更富有体验的页面。JS 它在前端开发中扮演着相当重要的角色。它的应用领域呢包括但不不仅限于以下三种。首先呢就是客户端脚本，用于在用户浏览器中执行，实现动态效果和用户交互。第二种就是网页开发，与 HTML、CSS 协同工作，使得网页具有更强的交互性和动态性。第三点呢是这个后端开发。如果我们想使用这个 JS 做后端开发的话，就需要使用这个 Node.js，这样我们的 JS 呢也可以在服务器端运行，实现服务器端应用的开发。总的来说呢，JS 作为一门语言，在前端开发中的重要性肯定是不限上升的。而且在前几年，JS 也是全球最热门的编程语言。因为后端语言像 Python 啦、C、C++、Java，在软件开发中都占据一定的比重。但是前端开发就肯定要使用到 JS。OK，在我们写第一行 JS 代码之前啊，我们还需要了解一下 JS 的导入方式。主要常用的呢就两种，第一种是内联式，也就是在 HTML 文件中呢直接嵌入 JS 代码。咱们的 CSS 代码是放在这个 style 标签内对不对？而 JS 代码是放在 script 标签内。而这个 script 标签呢，可以在这个 head 标签内，也可以是在这个 body 部分。比如我们就可以现在这个 body 标签内写一个 h1 标签。那咱们现在就在 body 部分呢创建一个 script 标签。其中呢我们就可以来写我们的 JS 代码了。而第二种呢是外部引入，也就是把咱们的 JS 代码呢保存在单独的外部文件中，通过 script 标签的 src 属性引入。比如我们在这里创建一个文件夹，就叫它 js。并且在这个文件夹内创建一个文件，就叫 myscript.js。名字什么的都无所谓的。那我们先来写一行简单的代码吧。无论学习什么编程语言，第一堂课永远是打印 Hello, World!。那咱们就用这两种打印方式来打印一下 Hello, World!。但是我们这打印的内容在哪里显示呢？我们看我们的浏览器啊。我们在浏览器中按这个快捷键 F12，这里我用的是谷歌浏览器啊，并且选择这个 Console，也就是我们的控制台。那之后呢，我现在想在这个 body 表现中使用这个内联样式，打印一下内容。这里呢我们用到的函数是 console.log。console.log 呢就表示我们要控制台里面打印一些日志内容。比如在这里我们让它打印 “你好，Hello，body 标签的内联样式”。刚才我们也说了，我们可以把 script 标签呢放在 body 里面，也可以放在我们的这个 header 里面。那还是写一个 script 标签，也是打印同样的内容，那就 console.log “Hello，header 标签内联样式”。那最后就是我们这个外联样式了。那外联样式我们让它打印 console。如果你觉得写 console.log 比较麻烦的话，咱们看啊，我是不是可以直接写个 log，然后给它一个 Tab 键，就可以老直接创建了。那我就在这里就让它 “Hello，外联样式”。现在咱们看还没有显示对不对？那是因为咱们还没有导入。导入方法呢也很简单，我就写在咱们这个写在咱们这个 header 标签最后了啊。那就是 script src。这个 src 呢就解释路径。我们就使用相对路径，那就点开头，点就表示当前目录，然后斜杠，再选中我们这个 js 文件夹，然后再一个斜杠。现在我们看 js 文件夹中呢只有一个这个 JS 文件对不对？我们就选中它，然后保存。咱们看这个外联样式是不是也出现了？我们注意一下顺序啊，是不是它有一个加载的顺序？这 script 在 header 中呢是最前面的，这个 header 表现内的内联样式先打印。接着呢，我们把这个外联样式呢放在了这个 header 内联样式的后面，所以它第二个打印。最后打印的是我们这个 body 标签内的内联样式。除了在控制台打印之外，我们也可以让它弹出来一个弹窗，也就是使用的就是 alert 函数。比如我们让它打印 “你好，内联样式弹窗”。我们给它刷新一下。咱们看是不是就打印出来内容了？同理，我们也可以把这行代码复制到我们的这个头部的内联样式中，或者是我们这个外联样式中，都是可以的。咱们小伙伴可以自己去尝试一下。

这节课我们来学习 JS 的基本语法。当学习 JS 基本语法的时候呢，我们首先要了解的就是变量和数据类型。在 JS 中首先就是变量，我们还是打开咱们的这个浏览器的控制台，F12，选择 Console。在 JS 中呢，我们使用三个关键字来声明变量。第一个就是 var，那全名就是 variable，也就是变量的一个简称。它呢就是声明一个正常的变量。比如我们 var x，不赋值。或者呢我们也可以使用 let，比如我们让 let y，并给它一个初始值是 5。还有一个叫 const，这个呢是声明一个常量。比如这个圆周率 π，我们就可以声明一个 π，它的值呢为 3.14。这 const 与 let 和 var 的区别我们用不用说了，因为 const 就表示它是常量，一般就不会轻易改变的。而 var 和 let 呢，它们声明的都是变量。但是呢，var 声明的变量是具有函数作用域，而 let 声明的变量呢是具有块级作用域。总的来说呢，就是 let 更安全、更灵活。因为呢它避免了 var 可能引发的一些问题，特别是关于变量提升和块级作用域的方面。在现代 JS 中，一般推荐就是使用 let 和 const，而避免使用 var。那具体什么是变量提升和作用域呢？这个咱们可以自己去了解一下。基本上啊在咱们前端开发的初级阶段是涉及不到这么深入的内容的。接着我们就可以打印一下这三个变量，那就 log x, y, π。咱们看，它显示的内容就是 undefined、5 和 3.14。那 undefined 的意思就是一个空值、未定义的状态。这个 undefined 呢是 JS 中的一种数据类型，就表示咱们这个变量声明，但是没有初始化。而这个 y 和这个 π 呢，它们两个都是数值，只不过一个是整形，一个是浮点型。除了这几种呢，我们也可以创建一个字符串，也就是 text 文本。就比如 name 吧，就叫如花。我们也可以打印一下 log，选择 name。还有就是空值，那就 let 一个变量名，我们叫 empty_value。空值嘛就等于一个 null。那我们这现在呢再使用 log 打印一下 empty-value，它要给我们显示 null 了。这里我们也需要注意一下啊，这个 null 和 undefined 它们两个的区别。虽然它们都是用于表示缺失值的特殊值，但是呢它们在涵义上还是有一定区别的。咱们先说这个 undefined，undefined 表示一个变量已被声明，但尚未被赋值，或者呢是一个对象属性不存在。当函数没有返回值的时候呢，默认返回的也是 undefined。那接着是这个 null。null 表示一个变量被明确赋值为空，或者不存在了。它是一个被赋予的值，表示一个空对象引用，或者没有对象值。它们的使用场景呢，就是 undefined 表示变量可能在将来被赋值，或者表示某个值还没有被计算出来。而 null 呢就明确地表示没有对象值。所以在实际开发中呢，还需要根据语境选择使用哪个值。

那我们接着来看 JS 的控制语句。控制语句呢包括条件语句和循环语句。咱们呢先来看条件语句。条件语句是编程中常用的结构，咱们就可以基于不同的条件呢，执行不同的代码块。在 JS 中呢，这个条件语句和其他的编程语言呢差不多，都是使用 if、else if 和 else 三个关键字。if 语句呢就是用于执行一个代码块的，当咱们这个指定的条件，也就是 condition，它的判断逻辑呢是 true 的时候，就会执行这个 if 大括号里面的代码。else 呢则是用于判断如果我们之前的这个 if 语句为假，还有这个 else if 语句呢都为假的时候，才会进入我们这个 else 的代码块。那最后就是 else if。else if 呢是用在上一 if 语句条件为假，检查第二个条件用的。并且呢，if 和 else 只能有一个，而 else if 呢可以有多个。那了解了这些之后，我们就可以来先写一些简单的代码练习了。比如我们可以先写一个变量，那就 let age，写一个咱们这个年纪的变量，age 等于 18。然后呢我们判断，如果你的年龄大于 18，就会输出你已经成年了。那就 if age 大于 18，那就会输出 log “你已经成年了”。但是我们这里并没有打印出来内容对不对？因为我们的年龄是等于 18，并没有大于 18。比如我们改成 19 的话，咱们看，是不是就打印出来了？或者呢，我们是不是也可以在这个 if 后面加一个 else？也就是如果你不大于 18 岁，就说明你是未成年。那咱们就让它显示一个未成年。接着我把这个年纪改成 9 岁。咱们看，这里原本是这个你已经成年了，现在变成了未成年对不对？那我们就再来写一个例子。就比如我们在不同的时间弹出不同的问候窗口。那这现在我们就需要重新声明一个变量，那就是 let time 等于一个随便先给一个时间吧，12。那如果时间是小于 12 点，那我们就让它弹出一个 alert “上午好”。注意啊，一定要是英文的标点。“上午好”。并且再写一个 else if。那这里我们的判断条件就是 time 大于 12 的情况，那大于 12 并且呢小于 18，也就是下午 6 点，这时候我们就可以让它打印这个 alert “中午好”。“下午好”吧。那除了上午和下午是不是就只剩下了晚上？那咱们就可以用一个 else，alert “晚上好”。OK，我们保存一下，刷新。咱们看，它是不是给我们显示了一个下午好？因为我们现在是 12 点。那我们再改一下，改成 9 点，也就是早上。它就会弹出一个上午好。那再改成一个 22 点，它就会显示一个晚上好。这个呢就是条件语句了。条件语句讲完，我们再来看循环语句。循环语句用于重复执行一段代码，直到呢指定的条件不再满足为止。在 JS 中常见的循环语句呢，就是 for 循环、while 循环，还有 do-while 循环。下面呢我们来详细讲解一下这个 for 循环和 while 循环。for 循环是一种常见的循环结构，就是按照我们这个指定的条件重复执行代码块。咱们看这个 for 关键字后面呢，这个括号内有三段表达式。第一个表示我们初始化表达式，也就是我们这个 for 循环中的一个循环的变量。第二个呢是判断我们这个循环的一个条件，满足就执行循环，不满足呢就跳出循环。最后就表示我们这个迭代器，用于计算我们这个循环的次数的。我们可以先写一个简单的代码，先吧之前这个弹窗给它注释掉，不然它会一直给我们弹。比如我们就可以使用 for 循环，让它呢在我们的这个控制台打印 1 到 10 这几个数字。那首先我们先给它分隔开，那就 log 就是 for 循环。接着呢咱们就使用一个 for 循环。第一个是我们的这个初始化表达式，比如我们就使用 let i 等于 0。循环条件就是 i 小于 10。那最后这个迭代器呢就是我们的 i++。这句话的意思非常容易理解啊。这个 let i=0 意思就是初始化我们这个循环变量为 i，并且它的初始值是 0。其次就是我们的循环条件，当 i 小于 10 的时候呢，我们就执行我们这个 for 循环循环体的内容。那每执行完一次呢，我们就执行一下这个 i++。i++ 呢是 i=i+1 的简写，咱们也可以写成是 i+=1，都是可以的啊。那接下来我们就可以在循环体中打印一下 log i 就可以了。我们现在看一下我们的这个控制台啊，是不是打印出来了 0 到 9？而我们想打印的是 1 到 10 对不对？那咱们就可以把咱们的初始变量声明为 1，循环条件呢可以改成是 i 小于 11，或者是 i 小于等于 10，都是可以的对不对？咱们看，现在是不是就变成了 1 到 10？这个就是 for 循环。接着呢，我们来看看 while 循环。while 循环呢会在指定条件为真的时候执行代码块。它与 for 循环最大区别就是没有这个迭代器。当我们执行这种无限循环的时候，就会使用 while 循环。而有限循环的时候呢，一般都会使用这个 for 循环。当然啊，还要看具体的语境来判断哪一个更合适。比如啊，我们就可以使用 while 循环来打印一次这个 1 到 10。那咱们在这里也 log 先打印一下 while 循环。while 条件呢，这里我们是不是没有初始化变量的表达式？所以呢我们这个循环变量需要写在外面，那就 let。为了不产生歧义啊，我们来叫 index 等于一个 1。index 是不是没有 count 好理解？count 吧。计数，count。如果我们这个 count 小于等于 10，那么我们就打印一下这个 count 这个值。咱们看啊，刚才我页面是不是就卡死了？这是因为我们这个 while 循环啊，它是一个无限循环，所以呢它就会不停地一直在这里打印这个 count 的值，而 count 的值呢一直是一，并没有增加。所以呢，如果我们想要使用 while 循环来实现打印 1 到 10 这 10 个数字的话，就需要给它添加一个迭代条件。比如我们每执行一次循环之后呢，给 count 写一个 count++。或者我们也可以写成是 count+=1，count 等于一个 count+1，都是可以的。现在咱们的这个页面就应该可以执行了。我们打开看一下。我们看现在是不是咱们这个 while 循环就可以正常打印了？不像刚才一打开就卡死，然后我的这个电脑就在这轰鸣。最后一个需要讲的呢，是循环的关键字 break 和 continue。如果你了解其他的编程语言的话，像是 C、Python 还有 Java 等等的话，你就会发现它们的这些基本语法都是大差不差的。因为编程嘛，万变不离其宗。不管你换什么语言，也就是一个入门快慢的一个差别，还有就是后来工作面向的一个岗位，面向的一个领域不同。那我们先来看这个 break 关键字。break 就是用于跳出循环，结束循环的执行。而 continue 呢是表示跳过本次循环，执行下一次循环。它只会跳过咱们这个当前循环中的剩余的代码，在它之前的代码还是可以执行的。我们就可以来举一个简单的例子。比如我使用一个 for 循环，for var i 等于一个 0，并且呢 i 小于 5。这次我们只让它打印 0 到 4，i++。然后呢我们在 for 循环中嵌套一个 if 关键字，判断一下，如果 i 等等于 2，那么我们就跳过本次循环，continue 跳过。那另一种情况，我们把这个 5 改大一点，改成 7。那另一种，如果我们的 i 等等于 4，那么我们就结束本次循环。其他情况呢，我们就正常打印 log i，这就可以了。那咱们就执行一下看看结果。先在这里打印一个分行，log 循环关键字。咱们来看啊，我现在是不是只打印了 0、1、3？为什么没有 2？因为 i 等于 2 的时候，就跳过了这段代码。而 i 等于 4 的时候呢，我们就终止了这个循环。所以呢，i 等于 5、等于 6、等于 7 这几行代码呢都不会执行。那我们把这个 4 改成 5 的话，就会打印是 0、1、3、4，5 之后的代码就不会再执行了。

那接着呢我们来讲讲函数。函数是一段可重复使用的代码块。它呢就接收这个输入，也就是参数，执行特定的任务，并返回输出。在 JS 中呢，我们使用 function 这个关键字来声明一个函数。语法呢就是 function 关键字开头，后面紧跟着的就是我们的这个函数名。函数名后面是括号，括号里面填写我们这个函数要使用的一些参数，这参数 1、参数 2、参数 3。不同的参数呢，用逗号隔开。当然啊，如果没有任何参数，我们就可以不写，就表示不传参。后面紧跟着的就是一对大括号。大括号里面呢就是我们的函数体了，也就是我们这个函数要执行的一段代码集合。最后呢就是一个 return。return 就表示返回值。如果你这个函数没有返回值的话呢，也可以不写这 return。那接着我们就来看一个简单的例子。比如我们在 body 标签内写一个 script 标签。然后呢我们就先写一个函数，这个函数的作用呢，我们就让它在控制台打印一个 “Hello, World!” 这个字符。那咱们就可以 function 开头，并且呢给它一个函数名。就比如打招呼嘛对不对？那就叫 greet，或者我们直接写 hello 都可以。这次我们先不传参，接着写我们的函数体。我们这个函数呢，它既没有参数也没有返回值，所以呢我们直接写一个函数体就可以了。比如那在控制台打印 Hello, World! 是不是就可以直接是 log “Hello, World!”，对不对？当然啊，我们这个函数现在写完了，但是呢没有任何内容打印。因为我们还没有调用这个函数对不对？所以呢我们还需要再调用一下。比如在这里直接写一个 hello()。咱们看我们的控制台是不是就多了一行这打印出来的文本？其实呢我们也可以给它添加一个返回值。比如我们就叫 function hello_with_return，也就是有返回值的 hello 函数。这次我们不让它打印这个 Hello, World! 了，而是让它返回一个 “Hello, World!” 这个字符串。为了区分一下，返回值。咱们看一下区别啊。现在如果我调用 hello_with_return 的话，它什么也不为显示。这里咱们需要想一下啊，因为咱们在这里第一次调用这个 hello 的时候，它呢就会执行 hello 这段函数体的代码，于是呢就在我们的函数体内打印出来了 “Hello, World!” 这个字符串。而我们的第二个函数，如果我们调用这函数呢，它会给我们返回一个这个 Hello, World! 这项返回值，这个字符串，而不是打印出来。那如果我们想通过它打印出来这个字符串的话，就需要用一个变量来接收一下。就比如 a 就等于一个咱们这个函数，并且呢 console.log 一下 a 这个变量。咱们看是不是就打印出来了这个返回值？或者我们也可以直接就 console.log 一下这个 hello_with_return 这个函数也是可以的，都一样对不对？那现在呢，我们不想让它说 Hello, World! 了，想让它接收一个参数，接收一个人名。那作用呢就是对这个人啊打个招呼，就比如 “Hello，如花”，“Hello，李华”。这时候我们的这个 function hello_with_params，return 表返回值，params 就就是这个 parameter 的缩写，也就是参数。这时候我们这个括号里呢就不能为空了，而是需要填写一个这个参数名，比如就是 name。那我们就让它打印一下 log “Hello, ” 逗号空格，加上一个 name。那这次我们在调用这个 hello_with_params 这个函数的时候呢，这个括号里面我们就要传入一个字符串。就比如叫如花吧。咱们看，“Hello, 如花” 是不是就打印出来了？当然你也可以打印别的，“Hello, Alice”，也都是可以的。那会传参了，也会添加返回值了，咱们是不是也可以写一个有参数有返回值的一个函数？大家就可以去试一下。在函数这一块内容呢，JS 还有一个比较重要的知识点叫作用域。作用域分为全局作用域和局部作用域。在函数内部声明的变量呢，就具有这个局部作用域，也就是函数作用域。而在函数外部声明的变量呢，就具有全局作用域。我们可以举一个例子，比如 let 一下 global_var。我们这个变量它是在函数外对不对？所以呢它就是一个全局变量。名字我们就叫它“全局变量”。接着呢我们再写一个函数，这函数名呢我们就叫它这个局部作用域吧，local，local_var。local_var 不行，local_var_function 好吧。参数不传参了，没有必要传参。那函数体的内容呢，我们就是在这个函数内打印一下这个 local_var。先声明一下这个 local_var，也就是这个局部作用域，局部变量。并且呢在内部我们使用 log 函数，先打印全局变量，“函数内打印全局变量” global_var。把它复制一份，并且呢在函数内再打印一下局部变量，后面就改成这个 local_var。然后呢我们现在就可以先调用一下这个 local_var_function。我们看它显示什么啊。咱们看，这个函数内打印了出来这个全局变量，函数内呢也打印出来了这个局部变量。那如果我现在在外部再调用一下这两个代码，全局打印全局变量和全局打印局部变量，咱们就会发现它会有报错。当然打印全局变量的时候没有问题，而打印局部变量的时候呢，它就会给我们报错说这个 local_var is not defined。意思就是我们这个 local_var 呢这个变量没有定义，因为它是在我们这个函数的局部作用域里面，在外部呢是访不到的。

那我们接着来讲讲 JS 中的事件处理。在 JS 中呢，事件处理是前端开发中至关重要的一部分。它就允许你对用户的交互呢做出了响应。也就是通过事件呢，我们可以让 HTML、CSS 与 JS 建立起来关系。那什么是事件呢？事件呢就是文档或者浏览器窗口中发生的特定瞬间。就比如用户的点击了、键盘按下了、页面的加载等等。常见的事件呢就由差不多以下几种，当然还有很多啊。常用的就是比如这个 onclick。onclick 就表示点击事件。像是这个 onmouseover 鼠标经过了，还有 onmouseout 鼠标移出，onchange 文本改变事件，onselect 文本框选中，onfocus 光标聚集，还有 onblur 移开光标，也叫失去焦点。那我们该如何把事件给它绑定到我们的 HTML 元素中呢？其实啊这个事件它就是一个函数触发的条件。就当事件发生的时候呢，相关联的函数呢就会被调用。在 HTML 中我们可以通过这三种方法来添加事件。首先呢第一种就是 HTML 属性，也就是在 HTML 标签中添加咱们这个事件属性。其次呢就是 DOM 属性。第三种是使用 addEventListener 方法。后面这两种呢都与 DOM 相关，我们等学完事件之后再讲。所以呢我们这节课先使用咱们这个 HTML 属性。比如我们就可以先来几个简单的练习。那我们就先练习一下这个 onclick 方法吧。onclick 就是当我们这个元素被点击的时候触发。那我们是不是就可以先写一个 button 按钮？其实不一定非要是 button 按钮啊，你随便写个 div 标签也是可以的。这是一个点击事件按钮。那我们如果想给它添加这个事件的话，就需要使用这个 onclick 这个属性。然后呢这个属性后面对应的属性值呢，就是我们的函数名。那我们就在这个 body 标签里面先写一个 script 标签。那咱们现在还没给它绑定事件对不对？而且呢咱们这个事件的函数都还没有写，所以呢我们先吧这个函数写出来。那咱们这个既然是个按钮，就给它绑定一个点击事件嘛。点击事件，那函数就是 function 打头。事件名呢我们就叫它 click_event。函数体的内容呢就简单一点，我们就让它弹出一个弹窗，alert “点击事件触发了”。然后呢我们就在这个 button 内写一个 onclick，并且把咱们这个 click_event 给它写进去，不要忘记加这括号。那现在我们点击一下这个按钮啊，是不是就出现了我们这个点击事件触发了的一个弹窗？那这就说明我们这个事件就绑定成功了。那除了这个点击事件，我们还可以再写一下这个聚焦和失焦事件。就比如聚焦和失焦我们一般用在这个文本框。那文本框那就是一个输入框嘛对不对？这标签啊一个标签它可以绑定不止一个属性。就比如聚焦就是 onfocus，失焦呢就是 onblur。我们再写两个这个事件，先是聚焦事件，那就 function 开头，名字就叫 focus_event，参数没有。函数体呢这次我们使用 log，console.log “获取焦点”。接着呢我们再写一个失去焦点事件，那就是 function blur_event。函数体也是一句话，console.log “失去焦点”。最后我们把这两个函数名呢给它放进来，那就 focus_event 和这个 blur_event，不要漏加括号。OK，最后我们就打开我们这个浏览器的 F12，打开它的控制台。我点击一下咱们这个文本框，就获取了焦点对不对？再点击一下其他的地方就失去了焦点。点击这里获取焦点，点击其他地方失去，点击获取，失去，获取，失去。当然啊，在 JS 中啊有几十上百种这个事件，我们呢没有必要一个一个去学习，大家可以自己去了解一下。

这节课呢我们来讲讲 DOM。在网页被加载时，浏览器会创建页面的文档对象模型，也就是 DOM。DOM 的全称呢就是 Document Object Model。它呢就允许开发者通过 JS 与 HTML 文档进行交互，动态地改变文档的结构、样式还有内容。如果你不理解什么叫对象，那也没有关系，你只需要知道对象的作用呢是为了让函数和变量之间建立联系。对象中的属性就是变量，方法呢就是函数。与普通的变量函数相比，区别就是在调用的时候呢，我们需要在这个对象的属性和方法前加上变量名。在 Web 开发中呢，DOM 通常与 JS 一起使用。每个 HTML 文档或者是 XML 文档呢，都可以视为是一个文档树。那文档树呢大概就长这个样子。文档树呢就是这整个文档的层次结构表示。每个节点都有父节点、子节点还有同级节点。这个文档节点也就是咱们这 DOM 节点，是整个文档树的根节点。而其他的节点呢就分布在咱们这树的不同层次上。DOM 的作用就是给这个文档树呢提供一个编程的接口，也就是 DOM API。开发者就可以通过 JS 来操作这个树状的结构。可以这么说啊，DOM 中的一切都是节点，而文档的本身呢也是一个文档节点。而文档中的元素，就比如我们这个 HTML 标签、head、body、h1、a、div、link、title 等等，这些呢都可以称之为是元素节点，就表示我们这个 HTML 文档中的元素。那像咱们 a 标签就有自己的属性，或者这其他标签也有自己的这些属性，它们的属性呢也可以被称之为是属性节点。就比如像是类啦 class、id 等等。还有呢就是像是文本节点，比如我们这 title 啦，a 标签也有文本节点，h1 双标签呢基本上都可以有文本节点，表示我们这元素的文本内容。要在 JS 中获取元素节点啊，我们就需要使用一些 DOM API 提供的方法来获取文档中的元素。这常见的方法呢有以下几种，我们可以来看一下啊。那我们首先就是先输一个 document.get，element。现在我们看这里有五个方法，也就是五个函数。分别是 getElementById、className、Name、TagName、TagNameNS。名字很好理解，get 就获取嘛，element 就元素，by 就通过，最后就是我们通过什么方式。id 呢就通过咱们的 id，className 呢就通过我们的类名，TagName 呢就是通过我们的标签名。但是需要注意的是，id 通常是唯一的，所以呢我们获取到的这个元素就是唯一的。像是 className、name、TagName 它们呢都是不唯一的。比如这一页面可能有不止一个 div 标签，所以它们获取的都是数组，名字呢也是从 element 变成 elements。如果我们想获取这数组中的某一个元素，就需要给它一个索引值。我们就可以来示范一下。先创建一个带有 id 的这个 div 标签吧。那就是 box1。这是一个 id 选择器标签。接着呢我们再创建一个类选择器标签。这是一个类选择器标签。最后呢我们再创建一个普通的 div 标签。普通的 div 标签。那如果我想选中咱们这个 id 这个 div 标签呢，我们就可以通过这个 document.getElementById。括号里面内容呢填的就是我们这个 id 名。注意啊是字符串，就 box1。然后呢我们就把这个获取到的节点啊存放在一个变量中。就比如我们叫 element_id 就等于一个我们这个对象来接收一下。然后使用 console.log 打印一下我们这个 element_id。接着呢我们就在我们的浏览器窗口中查看一下控制台。咱们看我们的这控制台是不是就打印出来了我们这一个节点？那接着我们就通过咱们的这个类名来获取元素。那就还是 document.getElementsByClassName。那这里类名是 box2 对不对？然后咱们打印一下 console.log element_class。这里注意啊，咱们看之前我们这个 div box 获取到的是一个元素对吧？而现在我们这获取到的这是一个 HTMLCollection。这个呢就是一个数组，但是呢长度是一。所以呢我们需要给它加一个索引值，也就是中括号 0。现在就获取到了咱们这个 box2 这个标签。最后如果我想获取第三个标签的话，咱们就得通过这个 var element_tag 等于一个 document.getElementsByTagName。后面跟着的就是我们的这个标签名 div。那不用说，我们这里应该会获取三个元素的集合对吧？咱们看这个 div#box1、div.box2、div，这是不是三个？所以我们想获取咱们这第三个的话，就需要通过这个索引值，那就是 2。OK，那咱们这三个节点呢就获取到了。接着我们就可以给咱们这三个节点啊修改一下它们的文本内容。修改方法呢很简单，就是 element_id，比如我先给这个 id 选择器这个标签修改一下，innerHTML。修改 id 选择器标签文本内容。这里注意啊它报错了，说的是什么？他说 innerHTML 不是一个函数。所以这里我们需要注意一下，innerHTML 它不是函数啊，它是一个属性。所以你不可能把一个变量当成方法使用。所以我们要让这个 innerHTML 等于一个，给它赋上这个值，咱们现在就更改成功了。除了 innerHTML 之外，还有一个是 innerText。那我们给类名修改一下 innerText 吧。修改后的这个类选择器文本。咱们看这里我们是不是这个类选择器文本也修改过了？需要注意的是啊，这个 innerHTML 和 innerText 它们两个是有区别的。innerHTML 呢它不仅会返回一个纯文本，还可以解析一下我们这个文本中的语义。就比如我们把第一个名字给它改成一个超链接 a 标签，当然跳转一个锚点吧，就井号，“跳转链接”。然后把它给复制一下，放到我们这 innerHTML 中。咱们第一个这个是不是就变成了个跳转链接？而如果我们把它放在这个 innerText 中呢，我们就会得到一个完整的文本。因为呢 innerText 它会忽略掉 HTML 标记，把咱们这些内容呢当做纯文本来处理，不会解析。而 innerHTML 呢，它就会解析这个 HTML 标记，并且呢更新这个元素的内容。那我们呢除了可以修改咱们这个 HTML 标签之外，还可以给它新加一些属性。那属性就是样式对吧？我们先给这个 tag 标签吧，比如给它的设置一个颜色。那样式就 style. 先给它改一个字体颜色，字体颜色就 color 吧，就等于一个 red。咱们看字体是不是变成了红色？我们也可以修改一下字体大小，那还是 style.fontSize 等于一个 20px。咱们看字体是不是也变大了？最后就是我们上节课说的，那如何通过 DOM 来绑定事件。首先就通过 DOM 属性绑定事件。比如我们先写一个最简单的按键标签。之前我们绑定事件是使用的 button 标签的 HTML 属性对不对？这里呢我们就直接写一个简单的 button 标签。那给它来绑定事件的话，我们是不是首先得先获取这个 button 对象？那就是 var button 先找一个变量来接收一下，element 就等于一个 document.get，element。没有设置 id 也没有类名，那就通过 TagName 吧，button。因为 TagName 呢获取的是一个数组，所以我们还需要通过索引值来获取到我们这个 document。如果你不知道自己有没有获取到的话，就可以通过 log 函数打印一下 button_element。这里我们就获取到了啊。然后呢我们就来绑定一下事件。首先就通过 DOM 属性，那就是 button_element. 咱们这个 onclick 的属性。给这个属性赋一个函数值，也就是 function。注意啊这里我们的 function 它是一个匿名函数。匿名函数的意思就是没有函数名，因为呢不需要在其他地方调用，所以就可以写一个简单的匿名函数即可。那函数体的主要部分就是 alert “DOM 属性按键触发”。那我们点击下按钮，咱们看是不是 DOM 属性按键就触发了？第二种方法呢就是我们这个 button_element 这个对象，它有一个 addEventListener 这个方法。这个方法我们需要传递两个参数。第一个参数呢就是我们的出发方式，也就是 click 点击事件。那第二个呢就是我们的这个触发事件之后执行的程序，function，还是一个匿名函数。这次我们函数体的内容稍微变一下，打印不同的内容。“通过 addEventListener 触发按键”。咱们看，点击一下，是不是就通过 addEventListener 触发了这个按键？当然啊我们没有必要完全使用这个匿名函数。比如我把这个函数给它复制过来，给它剪切掉。然后再下面重新写一个函数，那就叫 function 吧。函数名呢就随便，那就叫 click_event，没有参数。函数体给它复制过来。现在也是一样的对不对？当然啊我们把这个事件给它绑定，也就是 click_event。咱们看，我这点击一下是不是就触发了？点击就触发。其实啊这个 DOM 对象它的方法有很多。比如这里我们就列举几个。除了刚才我们讲过的这个获取元素，通过 id、类名还有标签名获取元素之外，我们也可以对这个子节点啊进行增删改查，或者创建这个属性节点、创建元素节点、创建文本节点，以及设置和获取指定的这个属性值等等，还有很多。更多的属性和方法呢，我们可以等到用的时候再学也不迟。
