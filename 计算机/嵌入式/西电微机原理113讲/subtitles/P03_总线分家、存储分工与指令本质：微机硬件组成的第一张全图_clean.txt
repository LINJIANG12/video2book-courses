CPU要给存储器或者某I/O要写数据，就应该由CPU的写控制引脚发送一个写控制命令。

清楚吧？

或者要读数据就要发一个读控制命令。那这些个命令信息都要由地址总线传送。

所以地址总线走专用的地址总线通道，CB。如果地……控制总线通道，CB。如果控制总线上的命令是CPU发送出来的，叫命令信息。

当然控制总线上还有一些信息是设备反馈给CPU的状态信息。

状态。

我们把CPU发送出来的命令信息和设备反馈给CPU一方的状态信息统称为控制信息。

控制信息走专用的控制总线。

啊。

那么来看，

把微处理器这个引脚，由于好多是复用的，通过这部分电路就转化成专用的三种线了：数据总线、地址总线、控制总线。那么转化的这部分电路，我们把它称为系统总线形成电路。

啊，系统总线形成电路。

那系统总线形成电路怎么设计？

一，是不是你要掌握，要了解，要掌握CPU那些个引脚，每个引脚的功能是什么？还要了解引脚与引脚，这就一个信号与另一个信号之间的时序关系。

然后用我们数电课上学过的那个锁存器、缓冲器等等芯片来设计这部分电路。

而这部分知识点涉及的是我们教材第五章的内容。

啊，教材第五章的内容。

还有，我们再来看，

这一部分，这一部分是不是也是总线？但是这个总线缩缩缩缩是不是到引脚上了？显然微处理器的引脚是不是也是总线？是的。

我们把微处理器引脚构成的总线称为微处理器级总线。

由于微处理器级总线上部分引脚采用了复用引脚，再加之微处理器级总线上的每个引脚驱动负载能力比较弱，微型机里面微处理器芯片是不是体积很小？那这个芯片让某个信号通过引脚输出时，不可能在微处理器内部设计一个放大电路再输出。

所以呢，每个引脚驱动负载的能力都比较弱。鉴于这两个问题，我们必须把微处理器级总线在系统设计当中第一步通过系统总线形成电路转化成这三种线形式。转化后的这三种线，我们把它称为系统总线，也是系统级的总线。“级”字可以不说，就叫系统总线。

那么在微型计算机系统当中，任何要和CPU通讯的部件，存储器也好，I/O也好，都是挂接在系统总线上来完成和CPU之间的通信的。那挂接在系统总线上的任何器件或者部件，对总线而言是不是都是一个负载？

但是微处理器级总线它驱动负载能力弱，我们只让它驱动了这一个负载，然后系统中大量的负载的驱动是不是交给了系统级总线了？这样通过系统级总线形成电路，不光解决了引脚复用分开的问题，也解决了总线驱动能力的问题。

那么在微型计算机系统组成当中是不是也离不开存储器的设计，也离不开I/O的设计。那显然，任何存储器芯片，任何I/O设备，是不是都是挂接在系统级总线上的？把这个负载的驱动任务交给系统级总线。

啊，这是这一块。

刚才通过讲解我们已经剖析出来了，微型计算机硬件组成是以微处理器为核心，采用三总线形式来组成的。那么系统中需要的其他部件是不是都挂接在三总线上了？

那么首要部件就是存储器。那么不管以什么型号的CPU组成什么功能的电子信息系统，存储器部分都分两大部分。

第一大部分就是我们常说的ROM。

第二大部分就是RAM。

这个听到过没有？

两大部分组成了这个系统的存储器子系统。那么这两部分存储器的区别应该清楚吧？

ROM呢？

ROM里面的信息，我们里面是不是每个地址单元的信息，掉电后不丢失。那既然掉电后不丢失，那么ROM里面存放的肯定是这个系统的管理程序。

如，就我们常用的计算机来说，主板上也有一片ROM，是用EPROM芯片组成的。EPROM是不是紫外线下可擦除的只读存储器？

那主板上的ROM里面放的是什么呢？BIOS。

听过没有？BIOS。就系统管理程序。计算机一加电，CPU先从BIOS里面取程序代码执行。

那取程序代码执行的任务有哪些呢？

一，对内存进行自检。这不就是内存吗？买内存条嘛。RAM，能读能写的存储器。掉电后信息是不是丢失了？

啊。

一加电，CPU从ROM里面读取BIOS程序，一，对RAM进行自检。自检的目的就是看RAM每个地址单元的每个bit位都能写能读吗？它保证每个bit位是好的。

RAM自检完了，内存自检完了，执行BIOS是不是要对主板上的I/O接口电路是不是要初始化？要初始化。

三，执行BIOS，RAM自检了，电路初始化了。三，干什么去了？从外部设备磁盘上是不是读取操作系统到内存了？然后内存才有操作系统。我们用户在计算机上干活，是不是在操作系统界面上干活呢？

啊。

那么那个BIOS就是这个系统的管理程序。你用户以某一种型号的CPU来设计完成你自己功能的系统，是不是也要设计这系统加电后的管理程序？

所以管理程序都在ROM里面放着，加电后不丢失，所以我们把ROM存储器称为程序存储器。

程序存储器。

我们把RAM存储器称为数据存储器。

啊。数据存储器是用来存放程序在运行期间产生的数据等等。

有些型号的CPU内部自带ROM和RAM存储器，如51系列的单片机、MSP430单片机内部都有一定容量的程序存储器和数据存储器。在实际应用当中，如果内部的存储器容量不够用，你用户就要在外部总线上来扩充、扩展设计存储器了。

我们这门课用到的CPU是8086。8086 CPU内部没有存储器电路，所以以8086 CPU组成的电子信息系统，系统中要用到的存储器都必须在三总线上去扩展设计。

啊。

那么来看，对于RAM数据存储器而言，CPU和RAM当中的某个地址单元写数据，就发送过来的数据写到某地址单元去，或者从那个地址单元把数据读出来，CPU接收回去。

那么你要读写数据的那个单元的地址，是不是CPU事先在数——地址总线上发送出来的？

啊？

那么地址总线上有了地址码，就找到了这个单元。然后对这个单元进行写还是读操作，CPU根据现行执行的指令不同，通过控制总线会发出不同的，如读命令或者写命令来完成读写操作。所以呢读写命令呢是由控制总线上，CPU通过控制总线发送出来的。假如说这是一个读命令，那对于这个芯片而言，这个引脚一般叫OE非。

O是输出，E是允许。输出允许了，非表明低电平有效。当给这个引脚上加低电平时，这个地址码对应单元的数据就允许输出了，是不是读出来了？

啊。

如果给这个地址码对应单元要写数据，就要通过控制总线给RAM存储器发写命令。写命令呢，一般RAM存储器写控制引脚符号，有些器件叫WE非，写允许，低电平有效，有些器件叫WR非。

Write，写。

明白了吧？那在看存储器芯片时，看到这样的引脚就知道是写控制引脚，看到这个引脚是不是读控制引脚。它地址引脚上肯定是A0到A几，数据引脚如果是8位存储器，是不是D0到D7？

啊。

那么对于ROM存储器而言，程序存储器而言，也是CPU通过地址线给它发送地址信息，发送地址信息。对这个地址信息对应的地址单元的数据是不是要给它发一个读命令OE非，把这个单元的地址对应单元的数据读出来放在数据线上，CPU是不是读走了？

只读。

当然程序存储器内部每个地址单元的信息是怎么放进去的呢？就怎么写进去的呢？是你用户把程序代码编好了，也调试通了，是不是你程序的二进制代码要通过专用的写入器写进去？写进去以后呢从专用的写入器上拿下来，是不是插在你的电路板的ROM器件去？

听懂了没有？一旦插在你用户电路板上了，你这个芯片只能读。

啊。所以CPU和存储器之间信息的传送，我们来看，是通过地址线上输出的地址码来示范要操作的具体地址单元的。对于该单元进行读操作还是写操作，是根据CPU现行执行的指令。如果是对存储器写，是不是有存储器写的指令？对存储器读操作，有存储器读操作的指令。对于I/O读还是写，是不是有专用的I/O读写指令？

指令，我在这儿引出了指令两……一个概念。什么叫指令？

指令就是说用你们C语言来说就是一行语句，明白了吧？用汇编语言来说，当然也是一行语句。当然，结合到CPU能认识这条语句，是不是只能认识二进制代码？

那么显然指令是一组二进制代码。

你不管用什么语言编写的程序，程序中的每行语句都要翻译成CPU能认识的二进制代码。一组代码是不是代表一行语句？一行语句告诉CPU做一件事情。

理解了没有？

所以指令是一组二进制代码。当然，不是随随便便的一组二进制代码。这一组二进制代码它肯定能告诉CPU做什么，而且能告诉CPU怎么去做，那么一组二进制代码才叫一条指令。

那用这样的二进制代码是不是也可以编写程序啊？那么用二进制代码编写程序采用的这种语言就叫机器语言。

显然用机器语言编写的程序放到存储器里面去，CPU是不是能直接认识，能直接执行？

但是用机器语言来编写程序势必给程序员造成极复杂，程序设计非常困难。因此人们才把能代表一条指令的一组二进制代码用符号描述了。用符号描述后采用的语言就叫汇编语言。

比如说我说存储器里面，这是存储器，一个单元，一个单元。假如说这个单元放了个B0H，16进制数。B0H化下来是不是8位二进制数？后面02H，把这两组，这两个8位二进制代码就是一组，这一组就是一条指令。

但作为程序员来说，一下子就不知道这条指令让CPU做什么。这不就是机器语言代码？人们后来呢用这样一组代码，一组符号来代替这一组代码，用MOV AL, 02H，这一看一目了然，就知道MOV是数据传送，就相当于高级语言里面的赋值语句。

它告诉CPU执行这一组代码，给CPU内部有一个8位寄存器叫AL，给AL赋值02，赋初值。

那么这一种符号采用的语言就叫汇编语言。当然用汇编语言编写的程序叫汇编语言程序。汇编语言编写的这个程序是不是也是符号化的？CPU也不认识。

啊，CPU也不认识。所以让CPU执行之前是不是要把这个符号要翻译成代码？在高级语言里面，把符号语句翻译成CPU能认识的代码是不是叫编译？在汇编语言当中，把一组符号语句翻译成CPU能认识的一组二进制代码，也就是指令代码的过程叫汇编。

叫汇编。实际上汇编就是翻译。明白了吧？

汇编后的二进制代码是不是最后你要用专用设备写到ROM里面，程序存储器里面？然后系统一加电，CPU从ROM里面读取用户程序代码去执行了。

啊，这是存储器系统的设计。二，这个存储器程序存储器电路怎么样和总线接口起来，数据存储器怎么样和系统总线接口起来，是不是一个接口电路的设计？而存储器接口电路的设计是我们教材第六章讲的内容。

啊，教材第六章讲的。教材第七章往后设计的是I/O接口电路的设计。

那么在一个微型计算机系统当中，除了CPU、存储器以外，是不是还挂接了好多I/O设备？

啊，比如这是一台I/O设备。

啊，I/O设备1号设备，可能还有2号设备，3号设备，4号设备，设备很多。由于设备很多，设备和设备之间信息接收的格式要求是不是不同？刚才我讲过，有些设备要接收的是数字量信息，好比说这是个输出设备；有些设备要接收的是模拟量信息；有些设备接收数字量信息时要并行接收，有些要求串行接收等等。设备很复杂，因此设备和CPU之间要通信的数据的格式就很复杂。

因此我们可以总结出一句话：任何I/O设备与CPU之间数据通信或信息通信都不能直接通信。它不像存储器这样，存储器和CPU之间数据是不是沿数据总线直接通信的？而设备不行。
