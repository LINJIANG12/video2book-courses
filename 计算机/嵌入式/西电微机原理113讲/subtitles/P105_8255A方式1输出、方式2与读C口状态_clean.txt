怎么样让 INTEA 置1呢？A口处于中断允许状态呢，是不是和B口一样？是不是也是借助 PC 位置1达到的？那就当 A 口工作在方式1输入时，紧跟着对 PC4，是哪儿看是不是 PC4 啊？有一个表，有一个表，表10.2，302页。当 A 口工作在方式1输入时，初始化完了，紧跟着对 PC4 位按位管理置1，是不是让它的 INTEA 置1了？但当 A 口工作在方式1输出时，要让 A 口的 INTEA 置1，是在初始化后对 PC6 位按位管理置1完成的。而 B 口工作在方式1不管是输入还是输出，对它的 INTEB 置1，不管是输入还是输出，都是对 PC2 位按位管理置1达到的。明白了没有？看看那个表，A口B口不一样。啊。

这是……呃……8255工作在方式1做输入时的情况。既可以用户应用时编程与时，是不是既可以用查询法也可以用中断法？都可以，啊。

下面我们看……呃……8255的 A 口和 B 口工作在方式1输出方式时的情况。以 PA 口为例，来看。

这是 8255 的 PA 口，它初始化工作在方式1输出。这是和 CPU 一方连接的数据线。啊，数据线。当做输出时，我们感兴趣的是不是写控制信号？刚才那是读嘛。8255 的……呃……PA0 到 PA7 这八个 I/O 线现在做输出，做输出，方式1输出。那这边是不是接的是一个输出设备？啊，输出设备，是一个智能设备。啊。

当 8255 PA 口初始化成方式1输出了，那么有一个 8255 向设备发的一个这个通知信号，叫 $\overline{\text{OBFA}}$ 就变低，就变低……啊就变高，说明输出缓冲器是空的。空的。当 CPU OUT 朝 PA 口写了输出的数据，输出缓冲器是不是满了？输出缓冲器一满，$\overline{\text{OBF}}$ 就变低。啊，$\overline{\text{OBF}}$ 你看刚才高电平是空的，现在缓冲器满了，它是不是变低？啊变低。这代表输出缓冲器满了。那么输出缓冲器满低电平可以作为外部这个输出设备的数据选通信号。这是一个输出设备，你看，这是设备的 D0 到 D7。当 CPU 把数据输出到 PA 口，是不是锁存到这儿了？表明输出缓冲器满。但是输出缓冲器满表明输出的数据有效了，是不是设备不知道？设备不知道，8255 一方通知设备，用什么通知呢？用 $\overline{\text{OBFA}}$ 低电平通知。那 $\overline{\text{OBFA}}$ 低电平是不是可以做设备的 $\overline{\text{STB}}$ 数据选通信号？看到了没有？就在这个选通信号作用下，把数据有效的数据是不是选通到设备的输入缓冲器中？设备就收到。设备收到了，设备就向就向 8255 通知。怎么通知呢？给 8255 发一个 $\overline{\text{ACKA}}$ 回答信号，回答信号。假如说这个回答信号是这个时候发的，发了个负脉冲回答。当这个回答信号一到来，一到来，我们看，8255 就使 $\overline{\text{OBFA}}$ 变高，就变高。这个高电平又通知设备输出缓冲器空了，线上的数据是无效的。看到这儿没有？是这样吧。

那么我们用户用查询法，什么时候给 PA 口方式1输出时输出数据？应该查询什么呢？是不应该查询 ACK 吧？应该查询 $\overline{\text{OBFA}}$。$\overline{\text{OBFA}}$ 等于1吗？等于1了输出缓冲器是空的，替它就输出去，应该查询 OBF。

那么当 8255 PA 口工作在方式1输出时，PA 组 A 组中的 PC 口的高四位，哪两根线充当这一对联络线呢？看书上。在书上的 301 页，是 PC7 和 PC6。啊。这个是 PC7，这是 PC6。同样，PC 口的第四位的 PC3 是不是还可以做 PA 口方式1输出时的 INTRA 中断请求输出？那 PC3 INTRA 要能向 8255 发出中断请求信号，8255 PA 口必须处在中断允许状态。8255 PA 口要处在中断允许状态，那就要在 PA 口方式1输出初始化完成后，紧跟着对 PC6 位按位管理置1，使 INTEA 是不是置1了？一旦 INTEA 置1了，8255的 PA 就处于中断允许状态。当 PA 口处于中断允许状态时，我们看，这是内相当于里面，从这儿来看，它属于中断允许状态是不是1？当 OBF 是1的时候，也就是输出输出缓冲器是空的时候，这地方是不是变变高了？由低变高。就促使 INTRA 由低变高，向 8255 就请求中断。刚才 PA 口做方式1输入时，INTEA 置1是不是借助的是 PC4 位按位管理置1完成的？而 PA 口方式1输出时 A 口的 INTEA 中断允许触发器置1，是初始化完成后借助 PC6 位按位管理置1完成的。

那么当 PA 口工作在方式1输出时，A 组中的 PC5 和 PC4 是不是还没充当联络信号？那么这两根线还可以作为方式0的基本的输出、输入 I/O 线用。做输出还是做输入，是不是要初始化？啊。

这是方式1的输出。当然，PB 口是不是也可以工作在方式1的输出？我就不讲了，大家自己看下。啊。

而我们再看第三种工作方式，是不是方式2？双向传输方式。这种工作方式只有 PA 口有。啊，只有 PA 口有。由于 PA 口工作在方式2时，中断请求信号线是不是用了 PC3 了？所以 PB 口再不能工作在方式2。联络线不够用。那么 PA 口工作在方式2双向方式时，实质上是 PA 口工作在方式1的输入和输出的综合。理解了没有？那你输出时可以查询 PC7 $\overline{\text{OBFA}}$，做方式2输入时是不是要查询 $\overline{\text{IBFA}}$？中断方式时，INTEA 置1的时候，能向 8255 发出 INTRA 中断请求信号，是不是黑板这种情况或者方式1输入时如书上在 303 页有一个图 10.9。PA 口工作在方式2，方式2中断方式时，我们看，向 8255 发出中断请求信号 INTRA，输出是一个或门，输入时的中断条件和方式1输出时的中断条件，任一个发生时，都能向 8255 提出中断请求信号吧？啊。那么在 PA 口工作在方式2时，PC 口是不是还剩 PC0、PC1 和 PC2 三根线没用？这三根线还可以作为基本的 I/O 线用户来使用。

这是 8255 的工作方式。

第四，看读端口 C 状态。读端口 C 状态。就是说在 8255 的 PA 口工作在方式1或者方式2，PB 口工作在方式1时，你编程序期间读端口 C，理解了没有？读端口 C，读出来的也是状态。如 304 页，图 10.11。图 10.11 你看第一行，方式1做输入时，把 A 口 B 口都工作在方式1做输入时，读端口 C 读过来的八位，高两位没用，是 I/O 线，D7、D6，D5 位读过来是不是 A 口的 IBFA 状态？读过来的 D4 位是不是 A 口的 INTEA 状态？中断允许触发器状态嘛。读过来的 D3 位是不是 A 口的 INTRA 中断请求信号状态？读过来的 D2 位是不是 B 口的 INTEB 中断允许状态？读过来的 D1 位是不是 B 口的 IBFB 状态？D0 位是不是 B 口的 INTRB 中断请求信号状态？C 口的八位在 A 口 B 口工作在方式1或者方式2时，读 C 口八位，反映出不同的状态。这个大家一看就知道了。

那么 10.4 节，也就是这一章我们要掌握的第五个知识点，是 8255A 与系统总线的接口方法。也就是与系统总线的连接。在我们这门课当中，任何一个外部 I/O 接口或者存储器芯片和系统总线相联相接，按照我们这门课用到的系统总线可能有几种？五种。理解了没有？有五种。8088 最小、最大，8086 最小、最大，PC 总线。啊。书上分别在什么系统下配置 8255A，怎么接，分别做了以图的形式画出来了很多例子。那我们来看，我在这儿画一个，大家跟着我的图来看 8255 与系统总线怎么接。

8255A。8255A 和设备相联的是不是有 PA 口八根，是做输出输入要初始化的；有 PB 口八根；有 PC 口八根。这是和设备相联的。

再看，和 CPU 一方，也就是和系统总线一方相联的是不是有 8255 的 D0 到 D7 引脚？数据引脚。和系统数据总线相接口。如果系统总线是 8088 最小或者最大，或者是 PC-XT 总线，这样的总线的数据线是不是只有八根？那么 8255 的 D0 到 D7 和系统数据总线 D0 到 D7 对连，啊，对连。

8255 的 RESET 和系统总线提供的 RESET 相连。如果系统总线没有提供 RESET 信号，自己就用 RC 电路设计上电 RESET。8255 复位后才能工作。啊。

还有 8255 的 $\overline{\text{RD}}$ 和 $\overline{\text{WR}}$ 读写控制引脚。如果是 8088 或者 8086 最大工作方式系统总线，或者是 PC-XT 系统总线，那 $\overline{\text{RD}}$ 是不是应该和 $\overline{\text{IOR}}$ 相连？$\overline{\text{WR}}$ 和 $\overline{\text{IOW}}$ 相连。如果是 8088 或者 8086 最小方式，$\overline{\text{RD}}$ 是不是应该连 $\overline{\text{RD}}$？$\overline{\text{WR}}$ 连 $\overline{\text{WR}}$。啊。

如果是 8086 系统，不管 8086 工作在最小方式还是最大方式，那么 8255 的 D0 到 D7 和数据总线相联时，由于 8086 系统是不是有16位数据线？那就和哪八根相联呢？就要看分配给 8255 的端口地址是四个连续的偶地址时，是不是连低八位数据线？四个连续的奇地址时，要连高八位数据线。

再看，8255 是不是还有 A1、A0 片内片内端口 0、1、2、3，和方式控制寄存器端口四个端口片内寻址线？还有 $\overline{\text{CS}}$ 片选信号。啊，片选。那么在 8088 系统内，或者在 PC-XT 系统中，如果分配给 8255 的四个端口地址是连续的，那么 A1 就接 A1，A0 接 A0。A2 到 A15，或者在 PC 系统中 A2 到 A9 是不是参加译码做片选？如果是 8086 系统，那么 A1 是不是应该接 A2？A0 接 A1。在片选译码当中，如果是四个连续的偶地址，A0 等于0参加译码；如果是四个连续的奇地址，$\overline{\text{BHE}}$ 等于0参加译码。这地方特别要注意的是，在 8086 系统中，A1 接 A2，A0 接 A1。系统总线的 A0 等于0是不是取四个偶？$\overline{\text{BHE}}$ 等于0取四个奇地址。
