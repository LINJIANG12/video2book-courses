那么首先看 DOS 操作系统的有些功能调用。
在书的后面有一个附录 B，381 页，
列出了 DOS 操作系统功能调用的一个功能表。
列得也不全。
应该再列一个附录，BIOS 调用列个附录，就比较好了。
当然就是说 DOS 操作系统功能调用当中的表上没列的功能，和 BIOS 功能调用表上书上没列，你要用到，
现在查资料很方便嘛，你在网上去搜索一下，把那表调出来，自己就可以看，很方便。
那么从这个表上我们看，表的最左侧这一列是功能号，
就是 DOS 操作系统里面把每完成一个功能都编了一个号，有 00 号、01 号、02 号，编号了。
那么这个功能号对应完成的功能在第二列是不是给你叙述了？
第三列是有一些 DOS 功能调用在 INT 21H 调用之前需要有入口参数。
那你就要入口参数准备好，用 MOV 指令该往哪个寄存器写，入口参数要准备好。
有些 DOS 功能调用不需要入口参数。
然后呢，表的最后一列是出口参数。就是有些 DOS 功能调用你调用成功了没有，是不是可以查看出口参数？
那么作为我们这门课上机要用到的 DOS 功能调用我说一下。
第一个，我们说 DOS 功能调用的 01 号 DOS 功能调用。
01 号 DOS 功能调用，也可以看表，是带回显的控制台输入。写得也不明确，因为表地方比较小。我把它展开：
01 号功能调用是等待用户从键盘输入一个字符。
等待用户从键盘输入一个字符。
其调用方法如下：比如说是 4.39 题，
它让你从键盘输入 1 到 5，如果键盘按的是 1，完成什么什么功能；如果按的是 2，完成什么什么功能。是不是这样子的？
那你前面程序设计设计设计，设计到这儿了，那我就要从键盘输入 1 到 5，怎么输入？
我 MOV，先把功能号要给 AH，01 号。功能号要给 AH。你看，381 页的表的最左侧这一列：功能号在 AH 中，写得很清楚。
然后 01 号 DOS 功能调用不需要入口参数，直接调用 INT 21H，DOS 功能调用。
那么 CPU 执行 INT 21H 1 号 DOS 功能调用，就等待到这儿了，就等在这儿了。下面程序不执行了，等着。
一直等到什么时候？等到用户按了一次键。用户如果一直没按键，是不是一直等在这儿？
一旦用户按键了，被按键字符，你看书上就写叫回显，把被按键字符显示在屏幕上了。
显示在如果是 DOS 界面的话，应该显示在屏幕的当前光标处了。
这是一个。第二个，
被按键，
被按键的字符是不是 ASCII 码了？就在，放在了 AL 中了。
那你是按的哪个键？退出程序在这儿是不是可以测 AL？理解了没有？
当然，它也检测，在等待用户按键时也检测按的是 Ctrl+Break 组合键吗？
如果是的，就退出用户程序到 DOS 系统去。
理解了没有？这是一号功能调用。
那你，在这儿可以做 CMP，就 4.39 题，AL 中输入的这个字符，数字，是一字符吗？和一字符比较一下。
如果相等是不是跳到一功能去了？
如果不相等是不是和二字符比较？那就太麻烦了。明白了吧？
我可以这样做：
如果按了个 0 呢？不小心按到 0 上去了，是不是错了？
那是是不是应该 JB？两量比较低于一字符，是不是按到 0 上、按到 8 上去了？
重来。我跳到 AGAIN，
重新输入。
否则，CMP，AL 中的被按键字符和 5 字符比较一下。如果大于 5 字符，你按到 6 上去了，是不是按错了？
重来。
AGAIN。
否则，程序直接到这儿了，那肯定按的是 1 到 5。那是 1 该咋、2 该咋去处理。
明白了吧？但是如果是 1，和 1 比较相等了，跳到 1 上，1 功能去；不是，和 2 比较；那我由 99 样功能呢？那是不是 CMP 要 99 次？那太复杂了。
那，是 1 吗？不是。那和 2 比较，是吗？不是。和 3 比较，是吗？不是。一直到和 9...和 98 那个字符差比较相...怎么怎么，相等？不是。那和 99 比较相...相等！哎，找到了。那前面是不是做了好多无用功？程序的效率很低。
所以在这种地方你就要找一种不管按的是几，都能转到相应功能入口处，且花的时间一样多的一种程序设计方法。
理解吧？那个方法怎么设计呢？
假如，
假如我是这样子。你看，
这不程序一直朝下设计嘛？设计到这儿就是什么功能一是不是编了一段程序？
功能二，如果按的是 2 是不是执行这一段程序？
如果按的是 3 就执行这一段程序。
如果按的是 4，就执行这一段程序。
如果按的是 5，是不是执行这一段程序？那这一段程序你看，按 5 在这儿程序结束。程序结束，这一段程序简单吗？两行嘛。
MOV AH, 4CH，INT 21H，结束了没有？结束了。明白了吧？那你看，这是你的功能。
那么前面我们讲的时候，这几个是不是都叫每个完成每个功能片段的程序的首址、入口？这是标号。
标号一旦定义了，就具有了三个属性嘛，其中有段地址和偏移地址属性。
那你，
那你
在数据段，
我写到这儿，这几个是不是在代码段编写程序？
那你在前面的数据段，假若这是数据段，DATA SEGMENT。
我设计了一个 TABLE 表，
用 DW 定义的。在 TABLE 字单元放了个 J1。
J1 是不是这个标号？那相当于把 J1 标号的什么属性放在这个 TABLE 字单元了？OFFSET。
听懂了没有？16 位的偏移地址。comma J2，
在 TABLE+2 字单元放置 J2 的 OFFSET。
依次类推，J3, J4, J5。这样，这个数据段，DATA ENDS。
那这样，
我在数据段创建了一个地址表。
地址表。
这个地址表汇编后我画到这儿，我上面没黑板。
这是数据段的 TABLE 单元。
在 TABLE 的这个字单元，
存放的是
OFFSET J1 嘛。
在下一个字单元
放的是
J2 的 OFFSET。
这下一个字单元放的是
J3 的 OFFSET。
这下一个字单元，我把它画完。
我把每个功能入口的
地址的偏移地址是不是创建在这个 TABLE 表里面了？
那么 J1 的偏移地址相对于 TABLE 表的首址的向下的位移量是不是 0？
这个位移量是不是 2？这个位移量是不是 4？位移量是 6，
位移量是 8。是不是这样子的？
看到了吧？把这个位移量想办法。
那么
我
从键盘输入了 1 到 5 了。
我根据输入是 1 还是 2 是 3 等等，是不是有一个 n？
我根据这个 n
要跳转到相应程序入口去。
而相应程序入口的偏移地址是不是在这个 TABLE 表里面？
那就是说，我实现到相应、根据这个 n，实现相应程序到入口的跳转的时候，是不是要在 TABLE 表里面获取目的地的偏移地址给 IP？
如果是段内的话，是不是段内间接寻址？转移地址寻址方式。
那我们来看，我怎么处理。
我从这儿，我 A...
先表 A 呢。
你看，
从键盘输入了一个 n，
把 n 看成数、数字。n 与这个 TABLE 表的位移量之间有什么关系？
是不是有括号，
有括号 (n - 1) * 2 的关系？
当 n 等于 1 时，位移量是不是 0？当 n 等于 2 时，位移量等于 2；当 n 等于 3 时，3 - 1 = 2, 2 * 2 = 4。
看到了没有？把这个关系找到。
那么有了这么一个关系，
我在这儿
AND 把 AL 和一个 0FH 得到数字。
原来输入的是不是字符？最高位清零，最高四位清零，低四位保持不变，就保留了 1 到 5 的数字。
有了这个数字以后，
我写到这儿。
有了数字了，我
怎么算？
我先做 n - 1 嘛。行不行啊？
我先做 n 乘 2 行吗？最后减 2。我按减 1 吧。我 SUB AL, 1，是不是实现 n - 1？
在这儿再做 n - 1，然后我 SHL，对 AL 的内容右移...左移嘛，左移一位，是不是实现了 (n - 1) * 2？
看到了没有？
因为 n 等于 1 到 5，所以我 CBW，把 AL 中的 8 位字节数扩展到 AX 中。
AH 高 8 位是不是 0？
然后我
LEA BX, 什么呢？
TABLE。
把 TABLE 的偏移地址给 BX。
BX 的内容是不是指向了 TABLE 字表的首址？
我要用 BX 间接寻址在它里边找一个字，默认段地址存在 DS。好。
那我们再来看。
我...
我接着往下写了吧。我 ADD，把 BX 加了个 AX。
BX 现在是指向这儿，加了个 AX，是不是加了个 (n - 1) * 2？
看到了没有？
然后我 JMP WORD PTR [BX] 间接寻址。
BX 间接寻址，如果刚才按的是 3，是不是加了个 4？BX 间接寻址是不是找到这个单元？
从这个单元取一个字，
把取出来的这个字不当数据用，当地址用，给 IP。
IP 从这个大件...这个地方是不是一下子跳到这儿来了？
实现了转移。
那你看，我这地方用的转移地址的寻址方式是不是段内间接寻址？
按照我以前讲的那个传称的话，是以 BX 间接寻址方式得...是把数据寻址方式找到这个字单元，
间接寻址。那就转移目的地的地址间接地在这个字单元放着。
那么我们来看，这样，我如果程序设计时不管从键盘按的是 1 还是 2 还是 3 还是 4 还是 5，最后通过这个运算，这个当做跳过相应功能入口跳花的时间是不是一样多？
理解了没有？
把这种，这是不是一个多分支程序设计？这种多分支程序设计称为
运用跳转表法实现多分支程序设计。
我把多分支转移到每个分支对应目的地的偏移地址都创建一个跳转表。
在程序实现跳转时，在这列表里面找目的地地址给 IP。
这种方法显然提高了程序运行的效率。
理解了没有？
也是我们提倡的一种方法。
我们的 4.39 题一开始就要发这么一个架子。
听懂了没有？
利用跳转表法。当然，这个跳转表是不是定义在数据段了？
当然，跳转表也可以定义在代码段。
那么当跳转表定义在代码段的时候，怎么实现跳转呢？
我们来看。
我们来看。
在代码段 MOV AH, 1，INT 21H，等待用户按一个键。
当然是不是和这一样，按的是 1 到 5 吗？不是是不是重按？是了就下来了。
那就是前面这段话还要写上：CMP AL 内容和 1 比较，如果低于 1，跳到 AGAIN。
不低于 1，CMP，AL 中输入的字符和 5 字符比较。如果高于 5 字符，
我这地方应该用 JA。是不是啊？
这地方应该用 JA，字符是无符号数。我刚才用 JG 的话是不是从带符号数转移了？好。
跳到 AGAIN。
好。否则，我们来看：AND AL 内容与 0FH，CBW，是不是和刚才一样？
写到这儿。我这个地方是 A，
我接到 A。
这地方我设计一个标号。
标号名字自己起了嘛。假如我标号名字叫 TABLE，这是个标号名。
在 TABLE 这个表一开始，我 JMP SHORT J1。
下面是，我 JMP SHORT J2，JMP SHORT J3，JMP SHORT J4，JMP SHORT J5。你看，我在代码段是不是创建了一个转移地址表？
但是这个转移表当中放的并不是转移目的地的地址，
而是无条件转移指令。
由这个无条件转移指令，好比说你按的是 1，我程序在这儿想办法就跳到这儿，从这儿无条件搭个桥是不是跳到 J1 去了？那显然 J1 是不是在这儿？J2 是不是在这儿？J3 是不是以此下去了？理解了没有？
我这个跳转表里面是用无条件转移指令二次转移再转到目的地去。
