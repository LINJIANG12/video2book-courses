今天我们讲第九章，8253芯片的第三个问题，就是工作方式控制字及工作方式。

上节课已经把8253内部，这个芯片内部的工作方式控制寄存器每位的含义已经介绍给大家了。

其中前两位是计数器选择，也就是8253内部有三个16位的定时器或者计数器，每个计数器有六种工作方式。每个计数器工作在哪一种工作方式，在8253内部，每个计数器都有一个方式控制寄存器这个端口。而且这三个方式控制寄存器端口占用的是同一个端口地址，也就是$A_1$、$A_0$等于00的……等于11的端口地址。

那么，给哪个计数器的方式控制器……控制寄存器写方式字来设置这个计数器的工作方式，是靠工作方式控制寄存器的最高两位来选择的。

中间$D_5$、$D_4$位是不是确定读写规则？来确定计数器0或者计数器1或者计数器2是8位计数还是16位计数器？是8位计数器是高8位还是用低8位做计数器？还是用这两位向输出锁存器发锁存命令？是00是吧？发锁存命令。

$D_3$、$D_2$、$D_1$是用类确定方式的。有六种方式嘛。

最后这一位是用来确定计数模式是以二进制计数写0的时候，还是以BCD码计数写1的时候。

那么今天我们来看六种工作方式。先看方式一……先看方式0，叫做计数达到终值而中断的工作方式。

如果我们假定系统配置的这一片8253占用的端口地址是300到303H。地址先拿过来。那303H是不是8253的方式控制寄存器端口地址？300H是计数器0的端口地址，301是计数器1的端口地址，302H是计数器2的端口地址。

那我下面讲工作方式，以计数器0为例来讲。是不是计数器1、计数器2都有六种方式嘛，我以计数器0为例来讲。

假如要把计数器0设置成方式0，初始化编程如下：那就 MOV DX, 303方式控制寄存器端口地址给DX，然后 MOV 给AL里面是不是要算？计数器0工作在方式0的方式命令字，高两位是不是00选计数器0？如果我们选16位计数是不是11？方式0是000，我以二进制计数是0，这么一个命令字，30H。

然后让CPU执行 OUT DX, AL。CPU执行这一条OUT指令就把AL中的30H……这地方是不是来个写信号？把30H就写到计数器0的方式控制寄存器去了。

那么对于……我这样加一个信号吧，写。你看，CPU执行OUT指令是不是来个写信号？写信号嘛。这个写信号，就是这个OUT写，就给计数器0的方式控制寄存器里面写了一个控制命令字，这个控制命令字等于30H，30H。

就是在这个写信号一到来，给计数器0的方式控制寄存器就写个30H，计数器0是不是工作在方式0了？

那么对于计数器0而言，在方式0没有确定之前，OUT0输出端是高电平还是低电平，咱们不知道，不知道。但是方式0一旦确定，OUT就变低，就变低了。就是……就是说计数器0方式0一初始化，OUT0输出引脚是不是也初始化了？引脚也初始化。

紧跟着是不是要给计数器0写方……写计数初值？那就把计数器0的端口地址拿出来：MOV DX, 300H。

然后它是16位计数，是不是要把初值要给AX，16位初值嘛。初值一般说是16位计数，肯定初值大于255了。但是我……我不能在这写大于255。我大于255我在这来个脉冲减1，来个脉冲减1，我讲到六时很长了。是不是啊？好比那我给一个小的值得来说明问题。比方5。

然后我 OUT DX, AL。那这一条指令是不是写CRL0呢？初值低8位嘛。给CRL0是不是写了一个5？那紧接着CPU执行这一条OUT指令，是不是在这又来了一个写信号？是不是又来了一个写信号？这个写信号给CRL0里面写了个初值5，初值5。

然后CPU继续往下执行：XCHG AH, AL; OUT DX, AL。执行这一次OUT，IO写，是不是写的CRH0，高8位。写的是0。那叫CPU执行这一条OUT指令，是不是在这又来了一个写信号？又来了一个写信号。

那这个写信号，就是这个写信号，给CRH0是不是写高8位初值？是00。写的是00。

那么当初值，我们再来看，初值在这个时候，是不是写完了？写低8位，写高8位，是不是写完了？在初值写完……初值写完之前，OUT0输出一直是低电平，一直是低电平。保持低电平。

然后我们再来看，当方式0确定了，初值写完了，如果GATE等于1，是不是允许计数啊？下面就要计数了。下面计数的时候，是不是以……我上节课讲以使外部提供的这个时钟下降沿要计数啊，下降沿计数。那么这个下降沿能不能计数，是这个时钟的上升沿时刻采集GATE。如果GATE等于1，那这个时钟就起作用；如果上升沿采集的GATE等于0，是不是停止计数？这个时钟就不起作用。明白了这意思吧？

那么你看，现在GATE等于1，在这个时钟下降沿到来时，你看，写初值写完了后，外部计数时钟的第一个时钟下降沿，这个是OUT等于0，是不是把你刚才写到CR0里面是不是有一个初值，是5，就打入到CE里面了。打到CE。

CR叫初值寄存器，CE是不是叫减法计数器啊？这上节课讲的嘛。把初值就打到CE了。减法计数器里面就有初值了。

随后如果GATE等于1，外部计数时钟每来一个时钟，CE就减1，刚才CE是不是等于5？现在CE减1等于4。这个时候没减到0之前，OUT0输出一直是低电平。

外部时钟再来一个时钟，CE减1。再来一个时钟，CE减1。呃，我这时钟还不够用。外部再来一个时钟，CE就减1。啊。

再来一个外部计数时钟，当然这个时GATE是不是一直是高电平啊，CE就减1，CE现在是不是减到0了？减到0以后呢，OUT自动变高。OUT0输出引脚自动变高，是不是表明时间到？计数时间到嘛，或者叫定时时间到嘛。看到了没有？

那这个定时宽度，是不是与初值有关？哎，与初值有关。我们改变初值，就能改变定时宽度，能改变定时宽度。

那么这种工作方式，你看，叫计数达到终值而中断，那就是意思这个……当CE减到0，OUT0由低变高的这个上升沿，可以向8259提出中断请求。就我定时时间到，中断，向CPU提出中断请求，CPU在中断服务程序里面是不是要做一件事情去了？定时时间到。啊。

那么，下面我要说的是，当CE减到0，OUT0输出端由低变高后，如果用户没有再次给CR里面写新的初值，那么OUT0就一直是高。就下一次，什么时候下一次定时时间开始？是不是要给CR重新写初值？啊，那就是这种工作方式是要给CR重新写初值，下一周期的定时才开始。才开始，就是OUT0才变低。如果没有重新写初值，它不会变低。当然，怎么要……就我记的是，我说的意思是下一个定时周期才开始。当然OUT0要变低是不是重新设置方式0也能变低？哎。

但是我还要说的是，当CE减到0，OUT0变高后，CE并没有停止工作。听懂这意思吧？就是说CE还在减1。外部计数时钟来一个，CE是不是减1了？一减1等于多少了？FFFFH嘛。再来一个计数时钟，CE还在减1，等于FFFEH，是不是这样一直减下去了？啊。

OUT0输出端的电平保持高电平不变了，但CE并没有停止计数。

那么，我就要问你了，你要重新开始定时，是不是必须在CE减到0后，要重新写初值是不是开始？你怎么知道CE减到0了？是不是要把CE减1的值要锁在OL里面读走？理解这意思吗？

那我叫看锁存，我 MOV DX, 303, 303H。我 MOV AL, 逗号，什么呢？高两位是00，计数器0。这了第5位、第4位是00，是不是发锁存命令？这几位不用，不用，写0写1都行。然后我 OUT DX, AL。

那么，这条OUT指令是不是在发OL命令，锁存命令嘛，把CE的值锁在OL中。锁了是不是才能读？

读。那我们现在下面读。我这写不下了，我写到这来。写到这。我 MOV DX，计数器0端口地址，300H。我 IN AL, DX，这副不是在读OLL0？计数器0的输出锁存器低8位。

要读两次，16位计数嘛。先读的是不是低8位？紧跟着是不是要读高8位？高8位是不是还要读到AL？所以 MOV，把低8位先暂存在高8……AH中。然后我再 IN AL, DX。这个时候读的是不是OLH0？

然后我 XCHG AH, AL。这个时候，XCHG指令一执行后的AX值，是不是读出来的OL锁存到的值？两次读完了，第二次IN读高8位输出锁存器值后，是不是开锁了？OL是不是开锁了？一开锁OL是不是跟着CE又在变了？这是上节课讲的嘛。啊。

那么现在我们来看，我把输出锁存器的值读到AX，那你就要测AX，你减到0了吗？啊？测AX读回来的OL值，你减到0了吗？没减到0是不是还要发锁存命令再读？直至读到它减到0了，是不是可以重新写初值啊？

那……那我们再来看，CPU执行这段程序，给OL发锁存命令和读OL的值，是CPU按沿着自己的时序来工作的。而CE减1减1一直减到0，是外部设备提供的计数时钟这样一个时序来工作的。两个是不是异步的？

那假如我发锁存命令的时候在这，然后读回去的时候没减到0，当我判断完了，我发现它没减到0，我再次发锁存命令的时候已经跑到这来了。这我读过去是不是还是不是0？那就是说，你测锁存输出锁存器的值，你读回去的值，看CE是否减到0了，这个0你不一定能抓住。

怎么办？怎么办？我认为应该这样做。在方式0下，CMP AX的值和初值比较一下。按无符号数比较，你高于吗？理解我意思吗？高于了是不是到FFFF、FFFE，高于了没有？高于了那肯定减到0了嘛。理解了没有？啊。我说如果……不高于……不高于，哎，是不是还没减到0呢？没减到0我跳到L1。

L1在哪呢？发锁存命令。是不是应该跳到L1，端口地址给DX，再发锁存命令，再读，再读低8位、高8位，交换，再和初值比较，如果还不高于，是不是还没减到0啊？再锁存，再读。直至它高于了是不是减到0了？减到0以后呢，我可以……我最简单的时候，我如果初值不变的话，我JMP L2。我JMP L2，我重新写初值。

是不是下来了？我一重新写初值，我一重新写初值，假若在这个地方重新写初值了，是不是写了两次啊？啊。重新写初值，这个写信号一来，OUT是不是马上就变低了？理解了没有？然后第二……高位初值写完后的第一个时钟下降沿，是不是把初值又打到CE？再来一个时钟下降沿是不是减1减1下去了？啊，是这么一种工作方式。这就是方式0。
