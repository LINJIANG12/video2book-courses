我们看通用寄存器的第二类，叫地址指针与变址寄存器。这共四个。其中地址指针寄存器有两个，变址寄存器有两个，一共四个。地址指针寄存器最好理解的第一个，16位，都是16位的啊。下面讲的全是16位的，8位的就这几个。SP，叫堆栈指针寄存器。这个已经理解了嘛。用来指示 PUSH、POP 堆栈操作时所操作单元的段内16位地址的。明白了吧？标准的术语是指示段内16位偏移地址的。我先把偏移那两个字不说出来，不理解，只能说段内的16位地址。而段地址，堆栈操作段地址刚才说了，是由谁提供的？SS，16位的堆栈段寄存器提供的。第二，地址指针寄存器叫 BP，BP 叫基址指针寄存器。基址指针寄存器，16位的。这个寄存器的作用和用 BX 做基址寄存器是一样的，但是是有区别的。我举个例子，举个例子。如：比如说，这是存储器，这是存储器。假如说存储器的这个区域，这个区域我们把它定义成堆栈区了，那现在我们把名词换回来，不叫堆栈区，而叫堆栈段，逻辑段嘛，堆栈段。又假如存储器的这个区域我们定义时，定义成了数据段。数据段是专门用来存放数据的，当然数据有字节型的，有字型的。那么一般情况下，一个逻辑段一开始，这个当然的，好比这个单元的段地址，堆栈段段地址是不是在 SS？堆栈段寄存器里面嘛。那这个单元相对这个堆栈段开始点向下的偏移量，叫偏移地址，就是位移量，用16位描述。这是不是0个位移量，1个位移量，2个位移量？把位移量用16位描述，那这个段最大是多大？是不是64K？2的16次方嘛？啊？2的16次方。一般这个逻辑段一开始这个单元的这个位移地址，叫偏移地址，是不是0？是不是这个意思？那这个单元地址是1，这个单元假如是2，是2。那对数据段来说，这个单元的段地址是不是在 DS？数据段段地址嘛。那这个单元的在段在这个逻辑段从开始点向下的位移量是不是2？那它在这个逻辑段段内的偏移地址是不是16位0002？是不是这个意思？现在我编个程序我们来看：如果我 MOV BX, 0002H，是不是这个2单元的地址？是段内的16位偏移地址？啊。那我还 MOV BP, 0002H，是不是两个值相等了？它的值都是2。啊。那我现在的然后我 MOV，好比 AL, 34H，是不是给 AL 赋了个值，这个字节值 34H。那我现在的有这样的操作：我 MOV，我把 AL 内容做源，写到目的去。目的写到目的什么地方去？是不是这个地方要给写的那个存储单元地址了。那我地址用间接寻址的话，我用 BX 寄存器间接寻址，那是不是把 AL 的内容写到 BX 的内容做偏移地址的单元去了？那 CPU 执行这一条指令，这一条指令是不是把 AL 的内容写到这个单元来了？写到数据段来了。这假如我后边还有这么一条指令：MOV BP, AL，把 AL 内容写到这个地址单元去。但这单元的地址是不是由 BP 间接提供的？而由 BP 间接提供的单元的16位偏移地址，CPU 默认是 SS 段的。显然，显然，这一条指令执行以后，这一条指令执行以后，把 AL 内容是不是写到这个单元来了？34H。听懂了没有？就是说 BX 基址寄存器，BP 叫基址指针寄存器，都可以给存储器提供16位段内偏移地址。但是提供的这16位偏移地址是不在同一个逻辑段。由 BX 提供的偏移地址，默认，CPU 默认在数据段；由 BP 提供的16位偏移地址，CPU 默认在堆栈段。这要特别注意。听明白了没有？当然，当然你由 BP 提供的偏移地址，你认为是给数据段那个单元写，你这一条指令就要改了。就是说我这样做：MOV，我 AL, 56H，我 MOV，好比我 BP, 0003H，偏移地址3嘛。我然后我 MOV，把 AL 的内容写到 BP 中括号间接的单元去。如果我这样写是不是和它一样？这个偏移地址是不是堆栈段的3单元？但是我要找到的是数据段的3单元。我找的是数据段的偏移地址是3的单元，怎么办？你程序设计时在这个操作数的左边必须贯穿一个前缀 DS: 特别指明一下。理解了没有？我指明你把这个寻址方式找到的单元是数据段的，如果你没有指明，默认在堆栈段。当然，用 BX 间接寻址指明的单元是不是左边也可以加这个前缀来指明操作数所在的逻辑段段地址？听懂了没有？那加的这部分呢，加的这部分就称为段前缀，标准术语叫段超越前缀。段超越前缀。可以简单地说就叫段前缀。当然，增加了段前缀以后，汇编程序汇编这一条指令时肯定多了一个字节代码，叫前缀码。听懂了没有？啊。这提出了一个段前缀的概念。那么现在讲的重点叫你理解的是：BP 也能间接寻址，BX 也叫基址寄存器，提供存储器间接寻址单元的地址，但是是不是有含义区别呢？啊。这是两个地址指针寄存器。再来看两个变址寄存器：一个叫 SI，一个叫 DI，都是16位的地址寄存器。其中 SI 叫源变址寄存器，16位的；DI 叫目的变址寄存器，变址寄存器。你不管源，不管目的，也不管变，只看址，是不是地址的意思？因此 SI 和 DI 都是地址寄存器。所以，你要对某存储器地址单元写或者读操作，刚才我举例子用 BX 做间接寻址能找到这个单元，用 BP 是不是也可以？用 SI 和 DI 中括号也行。但是用 BX 基址寄存器和用 SI 源变址寄存器和用 DI 目的变址寄存器中括号间接寻址找到的单元都是 DS 段的单元。只有用 BP 中括号间接寻址找到的单元，CPU 默认是 SS 段的。听懂了没有？啊。那么这个概念已经有了。其次你没有没有的概念，是不是一个变的概念？变。那这个变的概念来自于 8086 CPU 对字符串的操作。字符串。现在讲不讲？想理解吗？想。这么个意思，我再举个例子，再举个例子。假如这是存储器，这是存储器。现在存储器这个单元，假如说这四个单元我存放了一个 31H，是个什么？1字符。行不行？我说得对不对？ASCII码嘛。啊，当然你也可以 31H 字节数据，是不是也行？啊。下个单元假如我放的是 32H，再下个单元我假如放 33H，再下个单元我放 34H。那显然，这个地方是不是放了一个单引号 '1234' 字符串？看到了没有？啊。那我现在的想想把这个字符串搬到这个地方来，搬家。像字符串传送、字符串移动、字符串搬家，是不是这么个概念？啊。那你把它叫字符串移动也好，叫数据块移动是不是也行？显然被移动的这个字符串是不是叫源串？源字符串。搬到目的地的字符串是不是叫目的串？目的串。那么在 8086 汇编语言程序设计当中，这么一种功能的操作，用数据操作的方法也行，但是为了提高操作的效率，8086 指令系统专门开辟了一类指令，称为字符串操作指令，适合于字符串操作的指令。那个在什么地方讲呢？你看看目录第三章，3.13节专门有一类指令叫字符串操作指令。理解了没有？当然，8086 没有字符串操作指令也能对字符串操作，用我们学的这叫数据传送，你看这不叫数据传送的意思？用数据传送的方法也能实现数据块的搬家，但是为了提高这个串搬家呀、串干什么呀、串搜索呀等等、串比较等等的效率，8086 CPU 专门开辟了一类指令叫字符串操作指令。啊。那么用字符串操作指令来实现这个搬家的时候，首先它规定源串，源串在这，必须定义在数据段，DS 段。源串的首址必须，就段内的这个偏移地址必须由 SI 提供。这个源字是不是出来了？目的串必须定义在附加数据段，ES 段。程序设计时目的串的操作单元的偏移地址必须由 DI 指明。DI 中的目的，是不是出来了？现在就差一个什么？变。变的概念。怎么变？我们看。我不编程序了吧，编程序太突然了。啊。然后我们用这样做了初始化了，是不是做了初始化了？做了初始化以后呢，你用这样的指令：MOVS，S 是不是 string？字符串搬家。刚才的 MOV 数据传送，MOVS 字符串传送。理解了没有？好比我后边写个 B，byte，字节。一次搬一个字节。啊。如果我写 W，是不是一次搬一个字？理解了没有？那么 CPU 执行这一条指令，后边没操作数，目的和源都没有。无操作数指令。但 CPU 执行这一条指令它知道源在这，指针在这；目的的指针在这。它是知道的。那么 CPU 就自动地把 SI 指的单元的内容按字节是不是搬到 DI 指的单元去了？这是第一步操作。第二步，第二步，如果你再按首址往下搬，CPU 执行这一条指令后会自动地让 SI 和 DI 变。按增大的方向变是不是加1？加1。如果一次搬一个字节是不是加2？如果你原来的这个指针指的是末址，它会自动地减。自动地加或者减，是不是自动地在变？所以在字符串操作当中，用 SI 和 DI 做地址指针是不是叫变址寄存器？变的概念来自于这个地方。明白了吧？啊。但是你把 SI 和 DI 变址寄存器用来做数据传送操作，它不会变的。理解了没有？只有在字符串操作指令中才会自动地变。啊。这是变址寄存器。那么第一类通用寄存器8个我是不是讲完了？啊。第二大类寄存器是段寄存器。一共有四个，分别是 CS 叫代码段寄存器。CS 的值指明的是程序代码所在的那一个逻辑段的段地址。听懂了没有？显然在你编的程序汇编后放到存储器里面，是不是应该有堆栈段、数据段、代码段？如果是字符串操作是不是还有附加数据段？因此你用户原程序编完了，让汇编程序一翻译，放到存储器里面的代码、程序码是由几个逻辑段组成的。听懂了没有？因此 8086 汇编语言程序设计，现在要建立的概念：你就用符号这样的汇编语言符号来编写程序，也是定义一个逻辑段、定义一个逻辑段，一个源程序由许许多多逻辑段组成的。你定义的那个逻辑段可能是代码段，定义那个逻辑段是数据段，再定义后那个段可能是堆栈段，理解了没有？那后边要说，要八零八六汇编语言程序要会设计，是不是马上就会学先学那个段怎么定义。有了段定义，有了那么多指令，你还不会编程序啊？听懂了没有？那这样说你感到 8086 汇编语言程序也不难。是不是？任何机型的 CPU 的汇编语言程序思想都是一样的。但是 51 系列那个单片机没有段的概念。啊。DS 是数据段寄存器，寄存器。ES 是附加数据段寄存器。SS 是堆栈段寄存器。啊，概念已经建了，分别放的是各自逻辑段的段地址。在 8086 汇编语言程序设计当中，如果这些个逻辑段程序中都出现了，都定义用到了，那么你要注意，你在代码段一开始要对这些个寄存器初始化。使这些个段寄存器的值指向你定义的那个逻辑段的段地址。而 CS 代码段寄存器的值、初始值，用户不能初始化，由操作系统完成。我这句话有个意思呢，假如考试时，假如考试时叫你问这一条指令的语法格式书写正确吗？逗号，好比 AX。对不对？不对，大家现在很肯定地说错了，为什么？就是我刚说了一句话：它的初始化由操作系统完成的，就意味着用户无权初始化它，用户不能给 CS 写。更确切地些说，CS 不能做目的操作数。做源是可以的，读出来是可以的。理解了没有？啊。这是四个段寄存器。
