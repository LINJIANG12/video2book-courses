上节课我们讲到第七章的第四点，常用芯片的接口技术。上节课我们做了例 7.1。例 7.1 讲到的是无条件输出端口设计。那么今天我们先看例 7.2，是一个无条件的输入端口设计。看例 7.2，书上的啊。看题目要求：题目要求说，在 8086 CPU 工作在最小方式构成的微机系统中，扩充设计一个数据输入端口。那么按照这一句话，首先我们要抓住的是，系统总线是 8086 工作在最小方式的一个系统。那就用最小方式系统总线为基础来扩充设计一个输入端口，也就是 I/O 设计。那么 I/O 设计的时候，我们再来看，在这样的系统总线上，扩展设计 I/O 电路用到的系统总线有：有数据总线低 8 位和数据总线高 8 位。那么你扩充设计的输入端口读到的输入端口的数据，是用低 8 位数据线读到 CPU 内部，还是沿高 8 位数据线读到 CPU 内部？用 IN 读到 AL 了吗？那就要看分配给输入端口的地址是偶数走低 8 位了，还是奇地址走高 8 位，啊。当然，是奇地址的时候，是不是还要用到 $\overline{\text{BHE}}$ 等于 0 表明是奇地址吧？啊。地址线要用到 A0 到 A15。啊。还要用到 $\text{M}/\overline{\text{IO}}$ 控制信号。输入端口是不是要用到 $\overline{\text{RD}}$ 读控制信号。当然总线上是不是还有 $\overline{\text{WR}}$ 写控制信号。当然在设计，光设计输入端口电路的话，写控制信号就用不着。啊。在这样的总线上设计一个输入端口，这是我们端口选到的器件。来看题目要求，输入端口芯片是不是用 74LS245？用 74LS245 作为输入端口芯片。245 是不是一个数据双向缓冲器？但是现在这个题设计的是一个输入端口，是不是单向的？那你就要通过控制信号把它改造成单向的。那么控制信号是不是有 DIR 方向控制端，还有一个 $\overline{\text{E}}$ 数据有效控制端。当数据有效端 $\overline{\text{E}}$ 等于 0 时，245 的，245 是不是可以工作了？就是 B0、B1 一直到 B7，这是 B 边是不是有八个输入或者输出引脚，啊。A 边有 A0 到 A7 八个输出或者输入引脚。当然我现在是做单向缓冲器用，做单向缓冲器用。就当 $\overline{\text{E}}$ 端等于 0 此，数据有效端等于 0 时，245 可以工作了。可以工作就意味着 B 边和 A 边各八个引脚是不是处于二态？啊。那么工作了是沿哪个方向来传输数据？从 A 到 B 端传输还是从 B 到 A 端传输，是不是受 DIR 端方向控制端控制？当 $\overline{\text{E}}$ 等于 0，DIR 端也等于 0 时，你看我接地，是不是数据就从 B 边传送到 A 边？传送到 A 边。那显然，这样控制 $\overline{\text{E}}$ 就相当于这个输入端口芯片的片选。片选。这个片选信号从哪儿来的？题目说分配给这个输入端口的地址是 8001H。那显然，是要对 8001H 这个地址译码得到这个片选。啊。既然地址是 8001H，是奇地址，那么数据传输显然是不是走高 8 位数据线传输？啊，走高 8 位数据线传输。啊。那下面我们看这个译码电路怎么设计。译码电路怎么设计。这是地址线 A15、A14，题目说是 8000 你看是不是？A13、A12 嘛，是 8，啊。0，0，8001 嘛，这是 1，8001H。啊。这是 A0 地址，A1，一直到 A15。在译码时，A0 等于 1 的这个地址，是不是我们要用 $\overline{\text{BHE}}$ 等于 0 来替代？啊，来替代。而且题目说，没有说这个译码电路用什么形式，是用门电路还是用专用的译码器等等，没有特别指明，那么你设计这个译码电路时，用什么形式都行。啊。那我们用门电路来译码。用门电路译码的话，最简单的，是不是全 0 相或得 0？啊。那 A15 反向或 A14 等于 0 或，一直到 A1 等于 0 或。这里边是不是还有 $\overline{\text{BHE}}$ 等于 0 或？那么当这个或门输出低电平时，是不是 A15 到 A0 上地址线上的地址，是不是 8001H？啊。而且这个地址呢，是不是 I/O 地址？分配给 I/O 的地址。所以我们来看，假如我这地方再用一个或门，这个有效地址或，我再用 $\text{M}/\overline{\text{IO}}$ 等于 0 或，那你看当这个或门输出低电平的时候，这地方对应的地址是不是 8001H，对应的是 I/O 地址。I/O 地址。而且这个 I/O 是不是一个输入端口？只有当 CPU 对这个端口 IN 读的时候，这个端口是不是才片选有效？当对这个地址 OUT 写时，这个端口就不能选中。所以在译码电路设计当中，这个信号，就是 $\overline{\text{RD}}$，是不是也要在这儿参加或？那这个时候，当这个或门输出端是低电平时，对应的 8001H 地址是不是一个 I/O 读地址？I/O 读地址。啊。那么假如说这个地址信号叫 X，这个 X 是不是应该接到这儿？啊。这是译码电路。啊。然后题目又继续说，输入设备是八个乒乓开关。第一问，画出此输入端口与系统总线的连接和与输入设备的连接图。输入设备是八个开关，乒乓开关。那乒乓开关，我在这儿先画一位啊。我画一位。假如说，这一位，这个引脚是乒乓开关的中心抽头，输出端。这个引脚，假如我接地，这个引脚我通过电阻上拉到电源。那你看，当这个开关，乒乓开关打上去，闭合了，开关在这儿输出是不是一个 1？当这个开关扳下来，是不是输出一个 0？这样一个开关量。那么在实际工程设计当中，最好在电源这个地方呢，接一个电容到地。这个电容呢一般选 103 到 104 的电容就可以，对电源滤波。啊。那 B1 到 B7 引脚是不是还有八个开关？可以在画图时说明这叫 K0，下面开关是 K1，那个我不画了。啊。这样我们把八个开关组成了一个字节输入设备，是不是接到输入端口 245 的 B0 到 B7 上去了？这个连接图也画完了。啊。然后题目的第二问，让你编写程序检测 K0 开关。如果 K0 开关断开了，断开了我们现在统一起了，指的是不是拨下来了？叫断开。打上去是 1，是不是叫闭合了？啊。如果开关断开了，程序转向 PROG1 处执行那一段程序去。如果 K0 开关闭合了，程序就转向 PROG2 那段程序。那你看这个程序怎么设计？首先，程序设计 MOV 把端口地址给 DX，是 8001H，然后 IN AL, DX。那么看，CPU 执行 IN 这一条指令的总线周期的 T1 状态，是不是把 DX 内容作地址送到地址线上来了？送到地址线上这地方是不是变 0 了？在执行这一条指令的 T1 状态，这个总线周期 T1 状态一开始，$\text{M}/\overline{\text{IO}}$ 是不是也变 0 了？啊。然后呢，在这个总线周期的 T2 后半周期后，开始读，$\overline{\text{RD}}$ 变低。那低、低、低变低，245 的 $\overline{\text{E}}$ 端变低，B 边到 A 边这个方向是不是导通了？CPU 就沿着，导通以后开关的状态就输出到 A0 到 A7 边输出，是不是接到高 8 位数据线？CPU 读回到 AL 去了。读回到 AL 里面，AL 的 D0 位到 D7 位，其中 AL 的 D0 位反映的是不是 K0 状态？看，K0，K0 状态通过 B0 输入，A0 输出，沿 D8 位数据线读入到 AL 的 D0 位。听懂没有？啊。那 K7，K7 这一位状态是不是通过 A7 输出，沿 D15 数据线读回到 AL 的 D7 位去了？啊。那我现要测的 K0 状态是不是应该反映在 AL 的 D0 位？那我们去测。测的时候怎么测呢？先用 TEST 与，给 AL 与一个，要测的位与 0，与 1，不测的位与 0，是不是与一个 01H？啊。如果与下来的结果为 0，就 ZF 置 1 了，结果为 0，JZ。啊。那就是说，AL 的 D0 位是 0，开关是不是打下来了？那就相当于开关断开了。按照开关断开了题目要求，是转向 PROG1 处执行去了。那我 JZ PROG1。假如说 PROG1 在这儿，就执行这一段程序去。哎。否则，如果 TEST 完了以后结果不为 0，是不是开关打上去了？开关打上去，题目说执行 PROG2。那我假如说 PROG2 就在这儿，啊，就执行接着接着执行下来了。啊。那如果这段程序执行完了，或者这段程序执行完了，都要执行下面这一段程序，那这地方是不是还要加叫无条件转移到这儿来？理解了没有？啊。这是程序设计的思路。这是书上的例 7.2，是一个无条件的输入端口设计，很简单。啊。那么关于无条件输入、输出，输出上节课讲了例 7.1，我们已经讲完了。那么本章还要掌握的是，查询法输入输出方法。啊，查询法的输入输出。查询法的输入输出，书上给了一个例 7.3，留给大家下去自己消化。我今天重出一道题。啊，重出一道题。例：若某 A/D 转换器，这个芯片啊，A/D 变换器的电路原理图与主要工作时序如下表示。啊。有一个 A/D 变换器。这是 A/D 变换器。这是模拟输入，我用 VA 表示，这是模拟输入。模拟信号，啊。是电压模拟输入。这个 A/D 变换器能对 VA 输入端采集的模拟电压信号进行 A/D 变换。变换后的数字量是 8 位的，通过 D0 到 D7 引脚输出。啊，是个 8 位 A/D。那这个 A/D 变换器能否对 VA 输入端的模拟电压采集并做 A/D 变换，受控制信号的控制。一个控制信号叫 START。好。当给 START 端加一个正脉冲时，由正脉冲的下降沿启动 A/D 变换，开始采集变换。那么开始采集变换，变换是不是需要一定的时间？在这个时间内，变换时间内，A/D 变换器输出了一个状态叫 EOC。叫 EOC。啊。那我们看时序。这是 START，啊。这是 START。这个下降沿到来时，你看 EOC 就变低。表明在变换变换变换变换变换。当 A/D 变换完毕，EOC 变高。变高了是不是变换完毕了？变换完毕 CPU 从这把数据是不是可以取走？啊。如果再次要 A/D 变换，是不是又要再次加一个 START 启动信号？在这个启动信号下降沿到来时，EOC 是不是又变低了？变换完后又变高了。显然这个时间是不是叫变换时间？也就是变换速度。变换速度。当然，你还要看器件手册。有些 A/D 变换器对这个启动信号 START 还有一个宽度的要求。还有一个宽度的要求。有些好比说它要求是 1 微秒或者是多少，啊。就是脉冲不能太窄。这是它的工作时序。那显然，只有变换完毕后，是不是？这个时候，A/D 变换器的 D0 到 D7 引脚上才有变换后的有效数据，CPU 才能取走。理解了这意思吧？啊。这是它的主要时序。那么题目还说，分配给你看，你要从这取数据，是不是要设计一个数据输入端口？理解了吧？设计，分配给这个数据输入端口的端口地址假如是 8001H。这个一开始启动 A/D 后，你是不是要不断地测 EOC 状态？直至测到 EOC 变高了，是不是才变换完毕？那这个状态信号要测试，是不是在这要设计一个状态输入端口？啊。状态输入端口的地址是 8003H。啊。8003H。那么要启动 A/D，CPU 就要给 A/D 发一个 START 命令，是个命令。那这地方是不是要设计一个命令输出端口？命令输出端口的地址是 8005H。啊，这是题目的已知条件。
