那我们来看一下这个3.2节，这个多路复用解复用。啊。

那么，IP向上层提供的服务啊，是主机到主机的，从一个IP传到另外一个IP，是这样吗？

那么到了传输层，不管是TCP还是UDP，怎么样把它区分成不同的进程到进程呢？

增加了一个什么机制啊？

端口号，对吧？

TCP有TCP的端口号，对吧？UDP有UDP的这个端口号。TCP端口号也是16比特，UDP也是一样。

那么上面有6万多个端口号，啊，6万多个端口号。

当然了，这个TCP使用端口的方式和UDP使用端口的方式不一样。

那么，为了使得穿过层间的这个信息最少啊，TCP的socket和四元组相捆绑。

代表的是这两个进程之间的会话关系，对吧？这个端节点和这个端节点，这个主机上面的这个端端port，这个主机上的这个TCP的port，它们之间的通信关系，我们用一个整数来代表，用一个本地的整数来代表。在这里呢，用一个整数来代表我这个IP、这上面的TCP端口和对方的这个IP、对方TCP端口的一个通信关系。

当然这个socket跟哪一个PID，就是这个进程相捆绑，它也把它记录下来。

对吧？一旦建立完之后，我在源端往下交的是两个东西，一个是socket，整数，另外一个呢就是这message本身，对吧？这啊这应该写错，message本身，对吧？

然后呢，我们把message如果太大，分解成很多报文段的这个细节，我们把它忽略掉。假如一个message正好能够被封装在一个segment当中。

那这样的话呢，它交往下去交两个东西，一个是socket，那么另外一个呢，就是一个message本身，mess- message本身。

然后呢socket是不是代表了本地的IP、本地的端口、对方的IP、对方的端口这四个信息啊？

那这样的话呢，这个TCP知不知道我把这个message拿下来之后，message拿下来之后，我封装成TCP的段，知不知道？

我的源端口、目标端口，我知不知道怎么封装啊？

当然了。然后之后呢，我这个TCP的段再往下交，同时交的还有什么呢？还有源IP、目标IP，是这样吗？

所以说呢，这个TCP的段往下放来了之后，然后呢再加上IP的头部，形成一个IP的数据报。

因此往下交的，除了这个TCP的段之外，还有什么呢？源IP、目标IP。那么这样的话呢，在TCP的这个在IP的数据报的头部怎么样封装IP的信息是不是拿得到了？拿到，当-当然，它知道怎么样封装这个IP的数据报。那这样的话呢就可以借助网卡把它打出去，是这样吗？

这就是在源端做复用。所以说呢往下传的是什么呢？socket和这个message。

message呢是携带着数据部分。

socket对应了源端口、目标端口，知道它怎么样去封装，是吧，这个segment，TCP segment。

这TCP segment当然还有其他的头部字段啊。往下交之后，同时往下交的还有什么？IP的地址，源IP、目标IP。那之后，这个IP的协议实体知不知道怎么样封装成一个IP的数据报啊？

当然，源IP、目标IP会封装在它的头部，形成一个IP报打出去。

到了目标端怎么呢？

IP分组拿到了，IP的datagram拿到了，往上交，往上交。然后呢把IP的这个body的部分，就载荷部分，啊，或者是报文体部分拿出来，是不是一个TCP的段吗？

是吧？

对方的IP实体能不能收到这数据报？当然。

你说怎么收到的？对不起，我们讲到网络层的时候再讲，暂时不讲，我们递归地讲啊。我们假设这个借助于这个IP能够把这个分组啊能够传到另外一个IP。

它把这个分组的数据部分取出来，头部去掉。

啊，当然了，这个分组一定是那个IP发过来的。我的这个目标IP跟这个分组的目标IP是对上的，一定到达了正确的主机。

好吧？

然后呢把IP数据部分取出来是不是一个TCP segment？

TCP的segment，那么包不包括TCP的头部和TCP的数据部分、载荷部分？TCP的头部里面有没有这个TCP的源port和目标port？

然后呢往上交的还包括什么呢？IP，源IP和目标IP。这四个信息拿到之后，在这个表格当中能不能查到对应的哪一个socket？

然后呢能不能找到相应的PID，能够把发给相应的进程？谁创建了这个socket？从而发给相应的应用进程，是吧？

这就是在TCP当中啊，在源端怎么样做这个复用，到目标端怎样做解复用。靠的是什么呢？靠的源IP、源端口，目标IP、目标端口。实际上靠的是引入了新的信息，什么新的信息啊？

port，端口号，来完成源端的复用，到了目标端再做个解复用，采用这种方式来完成了这样一个情况。

好，我不知道有没有讲明白啊，就是说这块，就是说复用解复用前面我们通过这个应用进程借助于socket API创建了socket。创建完之后呢，就操作系统当中就形成这样的一个表格，这socket对应的源IP、源端口、目标IP、目标端口的信息又拿到。往下交，不仅仅是交socket。

那么我们大家在传在用socket API进行编程的时候，我们发送，在发送的这个socket API当中要不要增加要不要填入两个参数啊？哪两个参数？socket和什么？

和要传的东西本身嘛。

message嘛，对吧？这东西拿到之后，这socket对应源端口，源IP、目标IP、源端口、目标端口这基本上搞定了。

那么源端口、目标端口用于封装TCP的头部信息。

源源源port和目标port。

然后往下交的，封装成一个TCP segment往下交，同时往下交的是源IP、目标IP。那这样的话呢源IP、目标IP，这个IP协议实体知不知道怎么样去封装IP的datagram？

知道，然后呢传到对方。对方往上交的是IP的数据部分，就TCP的segment。

同时还要往上交的是什么呢？源IP、目标IP。

TCP的segment当中有源端口、目标端口，再加上往上交的源IP、目标IP，就能够找到相应的socket，从而找到相应的port，从而把这个message的，这个TCP的segment的数据部分交给正确的应用进程。

好吧，这就是复用的这种情况。

然后呢在这个UDP当中啊是不一样的。我们知道这个UDP的socket和什么相捆绑？

本地的IP和本地的端口相捆绑。所以说呢，这个应用进程往下交的，交的是什么？交的是数据部分，啊，假如我们用message来代表，这是第一个。

第二个，是不是要socket？UDP的socket。

第三个，你们还记得吗？是对方的那个IP的和端口号的一个什么？这个地址吧，这样一个结构体啊，sockaddr的话。

对，这个地址相当于是对方的IP和对方的UDP端口，相当于交这四个信息。

对吗？

大家还记得吗？sendto，sendto什么？里面有哪三个参数？第一，socket，UDP socket。第二，要交的东西。第三个，是什么？

对方的那个IP和UDP端口的一个捆绑关系所在的一个结构体的指针。交的就是这一堆东西。

这东西拿到之后呢，UDP知不知道怎么样封装它的UDP的源port和目标port？

源port、目标port在哪？源port在哪？这个socket对应的。

对吗？

目标port在哪？这里面有啊。

然后呢再往下交的是UDP的报，UDP的datagram往下交，同时还要交什么？IP地址，我的IP、对方的IP，是这样吗？对吧？

然后呢这个IP拿到这个东西之后，UDP的datagram来了，源IP、目标IP，我知不知道怎么样封装成一个IP的分组啊？知道吗？知道。好，那这样的话呢能够正确地传到对方的IP实体。

对方的IP实体往上交的是什么呢？一个UDP datagram，这是一个吧？

第二还交什么呢？源IP和目标IP，这俩这几个信息。

对吧？在UDP的datagram当中有没有源port和目标port？

目标port和目标IP，这两个信息拿到一起，可不可以找到相应的socket谁创建的？

然后呢交给相应的PID，交给相应应用进程，是这意思吗？对吗？对吧？相当于是这样的一个情况。

所以说呢在UDP当中呢它只是说目标IP和目标端口最终决定是哪一个socket，从而交给哪一个创建这个socket或者守护这个socket的应用进程。这样情况。那这样的话，就是完成了目标端的这个复用和这个解复用。完成目标端的一个解复用，这样的一种情况。

实际上本质上啊就是靠引入了什么？TCP的port和UDP的port。

只不过引入了一个socket的这样的一个概念呢，它就是为了使得穿过层间的信息啊来得最少、最方便而已。因为两个应用进程在使用TCP通信的时候要不要握手啊？

握手通-通信关系是不是搞定了？通信关系搞定了，我用一个整数值来代表我和你的这种会话关系，我一个socket的值和我，我的IP对对方IP，我的端口对对方端口相捆绑，用起来是不是方便？就这个概念。而UDP是不需要握手的。不需要握手，我在创建这个通信的这个，我在创建这个实体的时候，我知不知道要跟谁通信啊？谁要和我通信我都不知道。我只能知道我的IP和相应的端口，就这个概念。所以说，我的socket只是我和我的IP和相应的端口相捆绑。对吧？所以说我发的时候呢，不仅要把socket要，要告诉我的下层，同时还要告诉我发什么，还要告诉我的下面的IP，我下面的这个东西是什么呢？对方的IP和对方的端口号，是这意思吗？UDP端口号。所以说呢是这样的一种情况。这个本质上来说就是这样。这里的幻灯片应该描述得比较清楚，啊，搞得都比较清楚。那么，这个发送方怎么样做这个多路复用呢？在接收方怎么样做多路解复用呢？在发送方是从多个套接字接收过来的多个应用进，进程的报文。怎么叫复用呢？就是一个TCP，一个UDP实体上面有很多的应用进程它借助于它来发送。然后根据这个socket所对应的IP地址和端口号，当然TCP的时候要联合源端口目标端口，源IP目标IP来联合使用，来封装这样的一个TCP的报文段。然后呢，这个UDP要借助于什么呢？这个情况跟TCP呢稍微有点不一样。到对方之后呢怎么样呢？再根据这个协议数据单元当中的这个IP地址和端口号，把它发送到正确的这个socket。当然TCP使用socket的这种方式和UDP使用啊使用端口的方式是不太一样的，啊不太一样的。这是这个多路复用解复用的这个，这个情况。解复用，TCP和UDP采用哪些信息将报文段的数据部取出来交给正确的socket，从而交给正确的应用进程。因为在目标端，上面的很多进程都要借助于TCP或者UDP来收，收这个报文，或者收这个UDP datagram。它实际上是根据什么呢？根据一些IP地址，一些端口号来找到相应的socket，找到相应的PID，是这样的一种情况。整个呢这两个板书呢，两个黑板呢应该描述得比较清楚，大家回去再思考一下，思考一下，有这样一种情况。这个呢，我们就不再耽误太多的这个时间。但是，这是无连接UDP多路解复用的时候，你看服务器在创建UDP socket的时候，只是和本地的IP和端口相捆绑吧？是吧？跟本地的IP和端口相，相捆绑。然后呢，客户端在创建socket的时候呢，那么它也返回一个整数值，虽然说没有bind，但是呢，隐含的就客户端，它创建的这个整数和我这个IP和相应的端口也做了一个捆绑。只不过你没有指定这个端口号而已，但是也是做了一个捆绑。所以说呢，不管在UDP当中啊，不管客户端还是服务器啊，一个整数，一个UDP的整数，socket的值只是和IP和端口这个相捆绑，啊相捆绑。和对方的IP，对方端口没有关系，明白我意思吗？这样的情况。然后呢，这边呢是介绍了这个UDP套接字的二元组表示。我收到一个UDP报文段，然后根据目标IP，目标端口，找到相应的socket，从而找到相应的应用进程，是这样一种情况。这个呢，我就不再细讲，因为我们在这上面花了不少的时间，相信大家对这个应该是比较理解了，比较理解。那么UDP呢是无连接的。那么你看啊，在这个例子当中，你看，P3跟P1通信，采用UDP。P4也跟P1在通信。那么P4发过来的这个UDP的datagram，它的目标端口是什么？是6428。源端口是5775。它发过来的这个UDP datagram目标端口也是6428。当然它的目标IP肯定是这个主机B的IP了。然后呢，你不管是谁发的，是你这个IP发的，还是你这个IP发的，不管是你这个IP这个端口发的，还是你这个IP的这个端口发的，到了目标端都定位到这个主机B IP上的6428端口，是不是都到了同一个应用进程啊？对吧？因为我们知道这个，呃，就是这样，UDP呢只是跟本机的IP和相应端口相关联。你不管是你发的还是它发的，你的，你上面的那个端口发的，还是另外一个UDP端口发的，只要目标端口是我这个IP的这个相应的端口，都发给同一个应用进程。好吧？就是这样一种情况。你看，这个P1发给P3的是不是这个46428？然后P2发给P3的也是46428。你两个发的虽然他们的源IP这个东西不一样，源端口也不一样，但是他们的目标IP都是这个机器的IP，目标端口都是6428，6这写应该写错了啊，46428。最后是不是都到了这个P3这个应用进程？是吧？都到了P3这个应用进程。而这个TCP的这个多路复用呢，哎，一个socket和四元组相捆绑，所以说呢，这个TCP实体要联合四个信息找到相应的socket，从而找到相对应的应用进程。那你看我们在例子当中啊，P3跟P4在通信，P5跟P3在通信，P2跟P6在通信。对吧？P2在通信。P4、P5、P6都守候在这台机器上，假如我们它地址是B，它的目标端口都是80，就是它的端口号都是80。也就是说P4、P5、P6实际上都在这个中间这个机器的B上面的80号端口。但是呢，它跟它的通信，源端口，如果它发过来的，发给它的这个报文，源端口是不是主机A，它的端口号是不是主机的这个9157这个端口？就主机A的9157发送给主机B的80号端口，实际上定位到P4。然后呢我们看一下P3。P3呢是源IP是不是C啊？目标IP是不是C，B啊？源端口是这个9157，目标端口是它的80。但是呢，它是9157，它也9157，不错，但是呢它的IP是A，它的IP是什么？C。虽然说它们的目标IP都是B，端口都是80，但是它们的源IP是不一样，虽然它们源端口是一样，但是来自于不同主机的，因此定位到不同的socket，交给了不同的应用进程，是这意思吗？而这个P6和P3之间，P2之间的这种通信，P6仍然守候在这个B的80号端口，但是P2来自于C的这个5775这样一个端口，虽然它跟P3发出来的虽然是来自于同一个IP地址C，但是呢它们的源端口号是不是不一样？它们的端口号是不是不一样？因此在这里定位到不同的应用进程。也就是说一个socket的四元组当中，只要任何一个不一样，它就定位到不同的socket，从而发给不同的应用进程。也就是说TCP的socket实际上是四元组的这样一种捆绑关系。那么如果是一个多线程的话呢，它可能是发给同一个进程下面的不同的线程，啊，不同的线程，这个我们细节我们就不管了。好，这个是，这个多路复用解复用的就讲完了。TCP和UDP不管怎么样，都是靠什么呢？靠着这个端口号这样的信息来区分不同的应用进程。但TCP所使用的端口号的这种形式和UDP使用端口号的形式是不一样的。那么，TCP要联合源端口、源IP、目标端口、目标IP这四信息来找到相应的应用进程，发给相应的这个正确的应用进程。而这个UDP呢是联合什么呢？两个。第一个是目标端口和目标IP这两个信息找到相应的socket，交给相对应的应用进程，是这样的一种情况。所以说呢这个是多路复用解复用的这样一个讲解。之后我们再看一下这个无连接呃这样的一个传输层协议，就UDP协议。就UDP协议它的缩写呢是什么的缩写呢？用户数据报协议。那么我们来看一下，用户数据报协议呢，它实际上是在IP所提供的服务基础上没有增加太多的额外的服务，只是增加了一个多路复用解复用。那么UDP能够提供进程到进程的，IP只能提供什么呢？主机到主机的这种服务，啊，这个是不一样的。除此之外，除此之外，这UDP没有在IP基础之上增加一些额外的一些东西，一些服务的品质，啊。所以说呢，它向上层提供的服务跟IP一样也是尽力而为的。所以说报文有可能怎么呢？丢，有可能乱序。你看，先打出一个UDP datagram数据报。当然UDP数据报当然也被封装在IP数据报往外打，是这样吧？先打了一个，又打了一个。但前面那一个IP啊，这个UDP的数据报所在的那个IP啊，怎么样？它可能经历了比较大的延迟。而第二个数据报怎么样？延迟比较小。那不是不是后传的先到了？有可能是乱序。啊，有可能是乱序。那么因此呢，UDP所提供的这种服务啊，有可能是乱序的。而且呢，两个应用进程在使用UDP在通信之前，不需要握手。啊，不需要握手。你UDP端口，那么一个socket跟UDP端口相捆绑，当然跟本地的IP和UDP端口相捆绑。然后呢，你都不知道跟谁要通信，也不知道是谁发送给我的。跟谁要通信的时候怎么样？因为我前面没握手嘛，我也不知道跟谁要通信，所以上面告诉我跟谁要通信。告诉我什么呢？第一，socket，socket只是告诉我从哪个我的源IP、源端口发出去。同时还告诉我第二件事，哪件事呢？你发的东西是什么？第三件事是什么呢？参数是什么呢？就对方的IP、对方的端口，究竟是什么。因此呢，基于这些信息才可以封装UDP的数据报。所以说呢，之前没有握手。但收的时候也是一样，收的时候也是一样，对吧？也是一样。所以说呢，这个两个应用进程使用UDP在通信的时候，无需握手。啊。

然后呢，UDP这种协议呢，经常被用于什么呢？流媒体的数据传输、域名解析、SNMP，就是简单网管协议，啊，网络管理协议。

那么基本上呢，这个UDP上面的应用分为两类。第一个呢是流媒体的应用，特别是实时流媒体的应用。第二种呢是这种事务性的应用，一次往返搞定的应用。啊，比较适合于在UDP上使用。那么一次往返搞定，如果UDP要连接的话，连接请求、连接确认不是很麻烦？啊，所以说呢，这种事务性的应用啊，一般来说也基本上采用的是UDP的这种方式来通信。

那么你说我要利用了UDP的一些好处，啊，没握手的麻烦事，对吧？整个的效率也比较高，因为它的头部比较小嘛，效率比较高。但是呢，我还希望可靠。那你说怎么办？因为UDP提供服务不可靠。对吧？你要么在TCP上做，要么在UDP上做。你在TCP上去做，必须要忍受它的这种负担重啊，这种坏处。对吧？你要在UDP上做的话呢，它又提供得不可靠。你必须要接受它不可靠。但是呢，我又希望应用要可靠，怎么办？你在UDP上去跑应用，但是在应用进程自己内部实现可靠性。只能是这么做，对吧？那你说了，为什么没有第三个协议呢？对吧，因为TCP和UDP这两种类型的协议能够支撑85%以上的这些应用。啊，整个协议栈不再安排第三个协议。因为再再安排第三个协议，他们之间的协调啊，整个协议栈比较复杂。另外一方面，他们之间的协调也存在很大的一些问题。所以说这是一个架构性的问题。啊，架构性问题就提供两个，你要么TCP，要么UDP。要么A，要么B，没有其他可选。你说我要在UDP上去这个利用它的这个高效率，同时呢，我又希望可靠，怎么办？你在UDP上跑啊，跑应用啊。但是在编制你应用程序的时候呢，你自己再应用层面上就实现可靠性，只能是这样，好吧？没有，没有可选。你说这个为什么没有第三个？因为第三个太麻烦了，对吧？整个协议栈的这种情况已经就像一个制度一样。

我们来讲一下这个接着上节课的内容，UDP用户数据报协议。那么用户数据报协议呢，为什么叫数据报呢？因为它是无连接的。每一个这个UDP的协议数据单元都独立发送，所以说呢叫数据报。IP它也叫数据报，所以说呢，你讲到数据报这个词啊，可能要结合上下文来看，到底是UDP的数据报，还是IP的数据报。另外一方面呢，就是说我们来看一下UDP数据报的格式。啊。8个字节的头部，然后呢是载荷部分。载荷当然是应用进程交给UDP，让他传的这个报文的内容。是UDP的SDU，就服务数据单元，好吧？然后呢，前面8个字节的头部呢，包括源端口号，2个字节。2个字节的目标端口号，啊，2个字节，也就是16个比特。然后呢长度，以字节为单位，包括头部整个的长度。啊。代表了这个一共多长的这个字段，多长的这个UDP的报文，包括头部。校验和，就是我们前面学的是什么呢？叫EDC，叫差错检测码。它属于差错检测码。那么携带的数据部分当然包括头部的一些信息，跟校验和之间呢是符合约定的、差错控制编码的这种关系，啊，差错控制编码的关系。然后呢在远端做校验和的计算。到目标端呢，再判断一下校验的范围和校验和本身是不是还符合约定的差错控制编码的关系？如果符合的话呢，怎么样？就不出错，认为它不出错。如果不符合的话肯定出错。所以说呢这个校验和的主要目的是什么呢？判断这个UDP的数据报包括头部，包括载荷的部分，在传输的这个过程当中啊，有没有出错。如果出错，它没有通过校验，这个UDP就会被扔掉，好吧？就会被扔掉。就是说呢，最终出错，它的表现是什么呢？UDP数据数据报被扔掉。那么应用进程收不到这样的一个message，收不到这个数据报。所以说呢错就是丢，就是丢失。表现为丢失，因为在某些级别，在UDP这个级别，可能在IP这个级别等等，没有通过校验，然后呢把它扔掉。

然后呢UDP呢，它的存在的必要性，因为他不提供可靠性嘛，它为什么要有UDP呢？它的理由是不建立连接，那当然没有连接的损耗。然后呢，这个是不是比较简单啊？就是发送方、接收方，那么就服务器不要维护客户端的状态，对吧？那么非常的简单。那么客户端也不需要维护我有没有跟哪一个远端的应用进程在通信的一个状态，简单。头部的开销很小，我们知道它的头部呢是8个字节的固定长度的。而TCP的话呢20个字节，所以它的载荷呢这个占比比较大，这是头部，然后呢，这是载荷的这个部分。头部、载荷。有效的传输部分是什么呢？就载荷，叫payload的部分，对吧？这是载荷。你付费要求这个货车去传货物的这个东西，对吧？这个叫什么呢？报文的头部。啊，这个头部呢，我们一般呢叫开销。那么有时候呢，啊我们把它直接就叫头部。所以说头部越大是不是开销越大？对吧？头部越大是不是开销越大？当然，一个卡车5吨重，4吨9是头，你只能拉多少？有效载荷只能拉100公斤。是不是？头。如果这个开销比较大，头部比较大，叫overhead，头大了嘛，对吧？马上要考试了一堆overhead，对吧？就是这样。就是开销它比较小，因为它只有8个字节，TCP 20个字节。然后呢，因为他这个UDP啊是没有流量控制，没有拥塞控制。应用进程啊往下来交的速度有多快，他往IP的这个协议层次啊这个发送的有多快，主机的话呢，往网络当中吐出去的速度就有多快。这个速度和这个速度是一致。如果我们把头部的这个增加啊，增加，你到了这个传输层是不是要加UDP的8字节头部啊？到了IP要加20字节的这个头部啊。如果我们把这东西忽略不计的话，这个速度和这个速度是一致的。这种特性呢对某些应用非常有用。哪些应用呢？像这个实时多媒体的应用，非常有用，对吧？你这边应用是一个web camera，是一个网络摄像头，对吧？你一边采集一边生成一边发。如果这个不是UDP而是TCP的话呢，哎呀你考虑比较多啊，就是这是一个忧虑的人，一方面考虑对方有没有堵啊，另外考虑网络有没有堵。所以说这个速度和这个速度是不一致的。但采用UDP呢基本上是一个直肠子，对吧？你上面来多快管他三七二十一，我也不考虑你的感受，我也不考虑网络的感受，你上面来多快我就往外发多快，是这样的一个情况，对吧？所以说比较简单的人他的这个这个这个思虑会少一点，思虑会少一点。那么这样的话呢，就是说比较适合于某些应用，特别是网络实时多媒体的一些应用。

那么我们来看一下具体校验和的一个计算的一些细节。在发送方做什么呢？做校验。使得校验范围啊和校验和，这个EDC部分符合某种约定差错控制编码的关系。这个差错控制编码的关系具体到这儿，就是什么？就是一个和的关系。什么意思呢？就是这个EDC啊，校验和，是校验范围的一个和。就是一个和的关系，你可以认为是它的一个和，对吧？到了目标端怎么样做校验呢？我同样计算校验和，对吧？然后呢，我计算的校验和和携带过来的校验和是不是一样？如果一样的话呢，我认为它通过了校验。如果不一样呢，没有通过校验，这个数据报错了，把它扔，明白我意思吗？那么后面一段话是对的。就就说如果没有通过校验它是肯定错的，对吗？它是肯定错的，对吗？有点晕乎啊。对吗对吗？呃对啊。

然后呢，通过了校验会怎么样？它一定是不错了的吗？不见得吧，为什么？我们知道，校验的范围，假如我们用D，实际上这个D不仅包括传输数据部分，还包括一些头部啊，和EDC。如果在传的过程当中，D变成了D一撇，EDC变成了EDC一撇。但是呢正好错的还挺离奇，离奇到什么程度呢？以至于D一撇跟EDC仍然符合约定的差错控制编码关系，可以吗？当然有这个概率，虽然非常小，对吧？那这种错误呢是差错控制编码检查不出来的错误，是这意思吧？我们把这个错误呢称之为是什么呢？叫残留错误。啊这也就是我们再打开一个通过网络下载的一个大文件的时候，有时候打不开的一个原因。为什么呢？中间部分出错了。啊出错了还比较离奇，正好这个D跟EDC一撇啊都错了。错的还离奇，这个正好错的还对上，对吧？就是数据部分和EDC部分正好还是仍然符合约定的差错控制编码关系。我们把这种错误称之为残留错误。

所以说它的目的就是什么呢？就发送方做差错控制编码，接收方做差错控制解码、检码。如果在传输的过程当中在保护的范围，如果出了任何一个比特的错误，它都有可能检查出来。啊这样的话，我们把这个这个EDC的主要作用呢是做差错控制的一个作用，和差错检测的作用，啊不是差错控制作用，是一个差错检测的作用。

然后呢它的加的细节是这样，就说我们首先看一下这个D。这个D啊，我们刚才讲了，它不仅仅是UDP UDP的数据部分，啊不仅仅是UDP的头部。不仅是头部，还且包括不仅是UDP的数据部分，还包括一些头部，甚至包括了一些IP一些伪头部。明白我意思吗？我讲的这个D啊，不仅是UDP的数据部分。是指它的整个的保护的范围。那么当然有很多的细节，大家可以看RFC，就是UDP呢这个差错控制编码所保护的范围啊，它实际上更广。数据部分、头部，实际上还包括一些所谓的伪头部，咱不管。这个呢我们把它称之为D，好吧？我们假设这就是D。

D怎么办呢？怎么样做这个checksum呢？checksum。sum是求出来的和，对吧？check是它的目的。对吧？求sum的为目的就是为了check它有check它有没有出错，是吧？然后呢比较大的这个数据部分这是D，可不可以把它分解成一个个的16个16比特，啊一个个的16比特。一个个的16比特。然后呢把这16比特16比特可不可以作为一个整数啊？把它加起来。加起来求得的和，我们把它称之为这个checksum，可以吗？就相当于很长的一个保护范围，咔咔咔咔切，切成一个个的16比特。当然不足的补零，对吧？切完了之后呢加起来，加起来之后呢算出来这个checksum。checksum，可以吗？然后呢把checksum到对方。对方怎么检验啊？然后我也加嘛。加完之后得这个和跟携带过来的这个checksum是不是一样，对吧？来判断它有没有出错。

当然它这个求和的这个细节，我们稍微讲一讲。求和呢有两个，一个呢叫进位回滚。什么叫进位回滚？这个16比特和另外一个16比特加有可能有一些进位啊。对吧？进位的话呢它把它拉到最低位，可以吗？你看最后三位是011，加上1的话变成几啊？变成100了。是这样吗？就凡是的进位把进位拉到底下再往上加。明白我意思吗？这第一个细节叫进位回滚。然后呢最后得出来的这个和，假如是前面是一堆的16比特，加完了和，在加和的过程当中啊进位回滚，最后得到了这个和。真正的这个EDC是什么？校验和是什么？是求得和的一个反码。什么叫反码？0变成1，1变成0呗。哎，我不知道有没有讲明白。这是进位回滚的和，这是最后的checksum。对吧？你看0是不是变成1啊？然后呢这个这个1是不是变成0了？

所以说在接收方它做的一个校验是什么呢？就是D跟EDC管它三七二十一，加起来按照同样的方法加起来。把把EDC也加起来啊，不仅是D加起来啊，EDC也加，最后得出的和是什么？16位全1。对吧？大家可以想一想，其实很简单的个道理。如果把D不仅是这个D加起来，而且把EDC以16比特为单位加起来，进位回滚，最后得出来的和是16比特全1。如果16比特全1，FFFF，啊两个字节。那么我就在接收端可以判定这个数据在传输的过程当中没有出错。是这意思吧？道理很简单嘛。进位回滚求和之后呢再做一个反码嘛。然后呢接收端是这样，做了一个非常简化的一个运算，不要再做进位回滚然后判断是不是一样。还干嘛呢？就把D和EDC以16比特为单位，这个加起来，当然要进位回滚。最后得到的是16比特全1。如果是的话呢，就认为这个数据在传输过程当中通过了校验，没有出错。好吧？这很简单的一个事情，大家回去想一想就可以。这是一个简化的算法。
