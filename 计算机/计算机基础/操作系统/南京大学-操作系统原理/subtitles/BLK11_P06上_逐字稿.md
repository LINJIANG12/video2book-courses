星期上一次课的时候，我们介绍了三个和进程相关的系统调用，对吧？我们说从上星期，从这个星期开始，我们就进入学习操作系统的 API，然后使用它们去做一些有趣的程序。那我介绍了 fork、execve 和 exit 三个系统调用，这是 Unix 的设计，像 Windows 的话，它有 spawn 这样的操作系统的 API。实际上 Linux 里面也有一个类似于 spawn 的 API，它比这个 fork 更轻量，但是这可能是后面我要讲的故事。这个 Unix 的设计很有趣，比如说 fork 我讲了一些有趣的应用，比如说 zygote process。比如说你可以在 DFS 的时候，对吧，你有多个分……当你有多个分支要探索的时候，你可以瞬间创建好几个状态的复制，然后每一个状态因为都知道自己要走哪一个分支，所以他们可以并行地在一个多处理器的系统上面运行下去。这边有一个典型的代码，讲解了我们怎么样用 fork、execve 和 exit 来实现我创建一个子进程，然后让子进程运行另外一个程序，然后我父进程可以和它分离，就我就直接父进程就可以继续做剩下的事情，也可以等待，用一个 waitpid 去等待子进程来完成。那大家只要有这个概念，知道我们可以做程序运行时状态的复制、重置和删除，那剩下其实你如果想要开发一个多进程并行的程序，那完全你就可以让 AI 来帮……帮……帮你完成了。然后我们今天继续上一次课讲的，这个简单的模型，对吧？我们来看一眼，上节课我做了一个 CrazyOS。也许啊以后上操作系统课就可以直接从这个 CrazyOS 开始，把整个学期的课程都都给讲出来。在 CrazyOS 里，我们观察到进程是有内存的，里面有几个比较关键的变量，比如说这有一个 memory size，进程的内存大小是固定的，然后 memory 的 offset 是 80000000。因为因为 CrazyOS 可以直接运行一个二进制文件对里面的指令序列，就好像这个指令序列被加载到了 0x80000000 里面。有一个这样的内存镜像，相当于是 80000000，然后这里有一个这是 mem size，栈顶的指针是指向这里的。所以如果我们要往栈里面 push 东西的话，它会从从上往往下 push。这是一个一个进程的内存镜像，然后这这个进程的内存镜像是直接写在 struct proc 里的。所以你每当你分配一个新的进程的时候，无论你是用 fork 创建一个新的进程，还是像我们在这里面用 proc_init 直接就直接无中生有生成一个进程，那它都会生成一个 struct proc，或者说都会分配一个 struct proc，里面有一份内存的副本。我内存有多大，那我这个数组就就有多大，它会有一份拷贝。这是进程自己状态的一部分。然后但是呢，对于实际的程序，比如说你看到的我终端里面的这些程序，或者浏览器啊这些更复杂的程序来说，它不像是我们 CrazyOS 那么简单，它只有一小段指令序列，然后这一小段指令序列直接就就跑起来，它只用栈上的数据。对吧，我们的整个进程的内存会比这个模型复杂一点。在计算机系统基础课上面，其实大家已经学过一次了。大家都知道像代码也就是指令序列是在内存里的，然后数据，你你定义的各种各样的全局变量，对吧，如果你写 int x = 1，那这个 x = 1 的 1 是会出现在二进制文件里啊。你们可以直接去编辑那个二进制文件，看到初始化的那个变量的值。然后如果你 int x = 0 的话，或者你定义一个 int x 数组是 100 万的话，那这个它就不出现在二进制文件里，它在一个叫 BSS 的 section 里。对吧？那内存里面有有比大家想象的要多一些的内容，比 CrazyOS 要复杂一些的。所以这就是今天我们要讲的进程的地址空间，也就是我们要弄清楚进程的内存里面到底有有什么东西。好，那我们就先看一下什么是进程的地址空间。那 AI 给了我一个标准答案啊。什么是进程地址空间？address space 是操作系统为每个进程提供的一套独立的、连续的虚拟内存地址范围，因此也叫虚拟地址空间。OK，这是大家能够接受的定义啊。如果我上课的时候讲这个，其实其实在计算机系统基础课上大家已经听过一遍这个了。然后我们为什么要有地址空间呢？它也说得很好，我们需要隔离和保护。因为我们讲到上上次课，有一个用 fork 实现 DFS 的例子。那我们每分叉出来的、fork 出来的进程，它们的内存都是独立的一份拷贝，就像我们在这个概念模型里面，对吧？每一个进程都有一次一份自己独立的 memory，它互相不干扰、互相独立。那我我我两个进程可以写同一个内存地址，但是它们其实实际上写到不同的，真正写到不同的实际的内存上嘛。然后这样就便于扩展和管理。然后程序可以甚至程序可以以为自己占有一大片连续的内存。我今天也会讲到这个例子，我给一下子分配一个很大很大的内存。然后它一瞬间就分配完了，然后实际上它没有给我，就像我给你画一个大饼，对吧，然后我慢慢慢慢一点一点，直到你真正真正吃的时候，我才把这个饼给你变出来，以及在支持共享，在隔离的前提下允许有限的共享。这是一个非常教科书式的定义，这个 GPT-5.4 给我的。但其实我们可以从一个更直观的角度来理理解。所以我觉得这个可能就需需要一些 prompt，如果以后 AI 要成为我们的好老师的话，它需要一个从从，比如因为我们写教科书的时候都是反向总结，你写书的人其实已经对知识有很好的理解了，然后很好理解后它会会把它总结好，然后然后给你讲。但其实我们反过来应该是带大家学习，或者说你们学习任何东西都应该是一个知识正向构建从无到有，对吧？你可以回到历史的早期，或者回到一个最早的一个简单的模型，然后我们能不能把一个现代的系统给构建或者理解理解好。OK。所以我们换一个方式来理解刚才那句话，其实是一个等价的理解。我们都知道，所谓的地址空间就是这个东西。我们的进程是由程序组成的，程序里面有指令，指令里面有 load 和 store。然后 load 和 store 的时候，这两条指令都需要一个地址，对吧？都需要一个地址和一个位宽，当然可能有些 mask 之类别的这个不重要的东西。它 load、store 本质上就是从一个内存地址上面取出一个值来读或者写进去一个一个数数据。所以内存是一个按字节寻址的字节字节序列。所以所谓的地址空间就是这样的进程所有你能够访问的那个字节序列。然后我们通过什么东西在编程语言里面访问进程的地址空间呢？哎，这个答案也很明显，就是指针。对吧？如果我有一个指针，这个指针我可以随便给它任何数值。就像你看我我这里定义了，我代码里面有一个 volatile char pointer，然后这个 pointer 我可以可以从外面就我从键盘上输入一个整数，对吧，输入一个 64 位整数，任何一个 64 位整数，它都是一个合法的指针。对吧？我这是一个一个按着字节寻址的大的数组，那我就可以 load 它，从这个 pointer 里面读一个值出来，然后可以往这个 pointer 里面写一个数据。都是这两个操作在编程语言意义上都是合理的，但是在操作系统意义上它未必允许。如果是空指针的话，你的程序就会发生 segmentation fault，对吧？那程序就 crash 了，这是你们在编程课的时候经常会遇到，哪怕是我觉得现在 AI 写代码，它经常都会写出能让你 segmentation fault 的代码。好，那么下一个问题就是如果，如果，如果我们有一个 C 程序，它的地址空间如果我们是 64 位机，那就是从 0 到 2 的 63 次方减 1。每一个格子我都可以有一个指针，对吧，有个有个 pointer 指向它。那我如果试着试着 load 它，在 pointer 指向某任何一个地址的时候，load 它会有什么会有什么结果？store 它会有什么结果？如果你能回答清楚这个问题，对吧？因为你知道所有的操作系统课上告诉你，所有的代码啊、数据啊、堆啊、栈啊，它都在同一个地址空间里，对吧？64 位机的二从 0 到 2 的 64 次 3 次方减 1 这个可以寻址的数组。能读出什么？对吧？这就是什么叫进程的地址空间。如果你能把这个问题解释清楚，那你才真正理解所谓的什么是进程的地址空间了，对吧？所以那很简单，我们就让 AI 来帮我们写一个代码来理解。好，在在这之前我做一个小 quiz 啊，大家知道为什么这个地方需要 volatile 吗？volatile 和 C++ 里面另外一个，C 和 C++ 里面另外一个 keyword 是有点像的，它可以用来修饰一个具有内存地址的东西，和 const 是一样的。const 说的是这个东西只能读不能写，然后 volatile 说的是所有的对这个内存的访问都不能优化。就就比如说你看我我这里这里有一个 load，实际上这个 load 不产生任何效果，因为我没有把它赋给任何一个值。对吧，如果我要把它赋值给一个别的变量，把那个变量打出来，哎，那它就不能优化了，因为最终是传递到了 system call。但是单我就这一行代码的话，它是可以被编译器直接删掉的。因为因为这个数据读出来它没有任何用。对吧，所以我用了这个 volatile 修饰这个字字符所对应的内存，所以它就不会被优化了。那如果我要修饰这个指针的话，我就需要写这个 volatile char * volatile pointer。这样我可以把这个 pointer 也变成 volatile。这和 const 一样的，你可以加加两个 const。就你能够出现 volatile 的地方都可以都可以出现 const。这是一个小小 trick。以前好像大家学编程的时候经常要把这些事情给搞清楚啊，现在也也不太需要把这些事搞清楚了。OK。

那么我们来看一看，如果让 AI 帮我们做一个例子会会怎么样？那我成就...就这...就这个 prompt，我让它写一个 C 程序。我们为了理解地址空间，写一个 C 程序用指针指向不同的位置：代码、数据、栈。然后把这些地址打印出来，并且从中读取一些数据。额外的，我们有一个比较有意思的呃发现，有意思的发现。我们可以干这样...这样的事情。任何一个符号，也就是任何一个指...能够合法的指针，比如说 main。main 我是可以把它赋值给一个函数指针的。对吧，函数指针也是一个指针。所以函数指针理论上说也是一个这样的指针，它指向了内存里面的一个位置。你用你用 printf 是可以把这个指针的值，指针到底是什么给打印出来的，对吧，你用 %p 就可以把那个 pointer 的 value 给打印出来。所有这些都是在在地址空间里的。那么我就可以把 main 函数指向的那个位置的二进制代码给打印出来，然后甚至我可以去试图去反汇编它。那反汇编它，我就可以知道它是不是真的和我们在那个二进制文件里面看到的是是一样的。如果我们...如果我们运行这个程序，它会告诉你各个，比如说 main，main 的 address 是多少，什么 555 呃什么什么什么。然后数据，比如说呃这个应该是数据它打印的是 global variable 的地址。对吧，它用 %p 把 global variable 的地址给打印出来。于是你就大概知道，噢，大概知道比如说 555 的地方是代码，然后比 555 大一点的地方，对吧，你看这是 CA0048，这是 C80A08，所以在比它高一点的地方是数据。在在某一个很高的位置上，它是栈。然后栈上的变量的地址我也可以可以可以得到。以及我可以去 verify main 函数的 binary code。比如说这里面它打出了 main 函数的前 20 个字节，fd 7b bd a9，我我就可以看一下，对吧。诶，是对的，这是一个呃 ARM64 和 x86 一样是小端的，所以所以你看是 fd 7b bd a9。啊，有一些有一些汇编代码。然后你就可以各种交叉验证，你可以通过，对吧，可以通过去去玩这个 pointer。然后你也可以可以去写一些让 AI 去写一些程序，比如说，要不要试试看扫一扫这个进程的地址空间？比如说随机地去去找一些指针，对吧，找一些指针看看有还有还有没有有没有一些地方是是它比如说我 load 其实可以的，对吧，是不是是不是可以的，呃但我还不知道的。OK。所以在在理解了，就是看到了我们确实呃进程的地址空间是符合我们教科书上那个定义以后，你就可以问出今天呃我们这节课最重要的一个问题。是这样问的：因为计算机世界里面没有魔法。我以前跟大家做教计...还教计算机系统基础的时候，我就会说有两个呃有两个公理。第一个是“机器永远是对的”。因为因为机器它的指令集的实现是遵照 specification 的，它有一个手册，那这个手册的行为就是就是应该被严格执行的。就算这个处理器的实现有一个 bug，就像你们自己实现的 NEMU 是有 bug 的，那这个 bug 它也是呃你可以认为电路的制造还是比较相对来说比较稳定，它不是那种突然发生的 bug。呃你也知道它在什么样的情况下执行哪一条指令会出现什么样的错误。所以机器永远是对的，它哪怕是是有 bug 它也是它也是 somehow 它遵守了那个一个呃 specification。所以你你你需要知道的是把把事情搞清楚到底为什么。然后第二个就是你没有测测过代码都是呃都是错的。所以这两个加起来，其实计算机世界里面是没有任何魔法的。它的推论就是你的进程的地址空间里面的每一个字节应该是什么，它都应该是规定好的，或者说你应该都是可以理解的。它不是一个像我前面这页里面，比如说讲讲讲进程的地址空间，啊对，他说，噢这是一套独立的、连续的虚拟内存地址范围，叫虚拟地址空间。这句话说得对，但它不蕴含它每一个字节到底是怎么来的，怎么定定义的。然后如果你去问 AI 的话，好那我今天就可以下课了，对吧，你们自己课后今天自习，然后大家去把这个这个问题搞搞清楚。AI 其实给了一个非常好的回答，因为以前这些知识都是非常碎片，对吧，你想每一个字节，每一个字节，它它有有些是 Linux 的，有些是长久以来这个 ABI 演化累积下来的很复杂的一个 specification。但 AI 就告诉你，OK，呃你首先搞清楚字节是什么，字节本身不带类型，对吧，就是我说的，呃它就是一个大数组。呃取决于你的指令把它解读成什么。如果我的 program counter 在这儿，它就会被译码、执行。然后如果我的呃指令是 load 或者 store，然后我要 load 一个这个地址的值，那它就会被当成是一个字节或者四个字节的数据给给 load 或者或者 store。呃它本身不带类型，由 CPU 的指令集体系结构来决定。然后字节放在哪？对吧，比如说为什么刚才刚才那个例子里呃为什么是这个什么 55555555？而且它每次还不太一样。如果我运行好几次，你看我在不停地运行它，它不一样。然后如果我用 gdb 运行它，诶每次它又一样了。你看我在 gdb 每次运行，你看那个 5 都要多一点，为什么？对吧，你你一旦观察到这些现象，人工智能马上就能给你一个回答。嗯它告诉你我们的 Linux 内核有一种叫 address space layout randomization，它是一个地址随机化。所谓的随机化就是每次程序会被加载到一个稍微有点不一样的地址，但都在那个 55555 的附近。啊这样它可以让你的呃系统更安...有些攻击会更困难。然后你可以继续跟它聊，然后它会给你讲那种什么什么那种缓冲区溢出的攻击，你们应该是在计算机系统基础课上面呃学过的。OK，但这不重要，这不是我今天今天要说的。这个字节放在哪，实际上是由链接与可执行文件的格式来决定的。所以大家如果用 readelf -a 去看去看一个二进制文件的话，这个也是以前应该是计算机系统基础课上面要讲的。这里有很多，program header 里面有很多叫 load 的，PT_LOAD 的部分。然后这个 PT_LOAD 的部分就是应该要被加载到地址空间里面的东西。然后如果是一个静态链接的 binary 的话，它它 load 的地址是就是完全确定的，你必须 load 到那个正确的地址。但如果我是一个 PIE，是一个 position-independent code，有位置无关代码，那我就可以把它加载到地址空间里面的任何一个地方，由由操作系统来决定，只要把它加载了呃就可就可以。这也是这也是反正也是也是规定好的。由链接与可执行文件格式其实再加上那个操作系统的那个地址随机化共同决定所有的节，比如说你的 text，对吧，你的二进制文件里面的源代码的部分，那些初始化的数据，未初始化的数据，只读的数据等等放在放在地址空间的呃什么地方。啊当然，呃真正当你程序运行起来以后，呃这个每个字节是什么还是由操作系统的加载器和 ABI 呃共同决定。它会告诉你呃进程的初始状态里面呃是什么。曾经都是很复杂的内容，我有时候也会带着大家去读一下那个手册，但我现在现在已经认为读手册已经没有那个必要了。呃你需要的是建立一个正确的概念，在进程的初始化好了以后，它有一个初始的堆栈，堆栈里面会有一些呃重要的初始的信息，比如说所有的参数和环境变量，呃以及一些可能比如说随机数种子啊这些呃更重要一些其它地方会用的，这个叫 auxv，在 System V 的 ABI 里面也有。大家只要有这个概念，有一天你用到的时候能想起来就呃就可以。你当然可以理解，对吧，因为你们回过头来想，噢，确实需要需要有一个人来规定这个进程在加载了以后，整个地址空间里面每一个字节字节是什么。但是同时 GPT 确实是告诉我正确的概念，但还是我那句话：概念是概念，实现是实现。呃我们除了机器永远是对的以外，还有一个计算机系统里面的公理，呃也是我希望在这个课上面大家能记得的公理，就是你但凡能想到一件有意义的事情，就这件事情对人类来说是有意义的，这真一定能做到。因为 computer science 是一个人造科学，它和自然科学比如说物理学不一样。我们不知道这个为什么呃有没有上帝啊，这个这个也不谈，为什么创造出了我们这样的世界，对吧？为什么为什么粒子会以这样的方式来运动？我们不知道，我们我们想要把它弄清楚。但计算机不一样，计算机从逻辑门开始就是由人类世界设计的。对吧，逻辑门的那公理其实就是人类设计，是布尔代数。那么我们就可以，就或者人会在你有需要的时候会尽可能地满足，用计算机能够满足任何实际中可能的呃需求。比如说，当我们讲这个进程的地址空间是由什么什么什么什么规定以后，呃那么我们一定有一个办法可以可以看到呃一个真实的呃进程的地址空间到底是什么样。所以我也给大家准备了一个小的一个小代码。

这个叫 Process Address Space Explorer。我看我的 prompt 放在这儿啊。就这样的。我说我在课堂上学习了 Linux 进程的地址空间，也就是这其实你已经理解了这些概念。然后请你帮我做一个命令行工具，从命令行接受一个进程的进程号。欸，这是上节课的内容。因为你知道我们通过一个编号就可以去直接定位到我们的一系统中运行的程序。然后可以解析它的地址空间，输出每个部分是什么。如果是可执行的区域，再把第一个 kilobyte 的内容给反汇编。然后我为了上课演示，我还特意让它兼容 x86-64 和 ARM64，免得它产生幻觉认为我我在 x86 上工作。比如说啊，比如说我现在就可以做一个对吧，一个这样的 .start。这个 a.out 就是可以可以可以运行的。我这样我就在一个终端里面我运行这个 a.out，然后我可以看一看有没有 a.out。当然如果你用 top 来看的话，你会看到有一个 a.out，这里有个 a.out 占用了 100% 的 CPU，它正在运行。好，我现在这个 a.out 正在正在运行。我用 ps 我可以看到它的进程号。你们看我在这儿操作其实不要担心，因为你可以让 AI 帮你们操作。我只是只是觉得可能 AI 出 token 的速度现在还比我要慢一点，所以我因为我是一个熟练工，我可能会比大模型推理要快一些，我上课的时候我就显得快一些。但是如果未来比如说如果 AI 有一个脑机接口的话，它比它比我熟练工要快得多。对吧？就没有任何意义了。我只要我只要告诉 AI 我现在想要做这件事情对吧，我想要运行这个 address space 这个程序，然后我就可以对吧，我就可以去对，我就可以去看到看到它。好。于是啊，我就真的这个程序，我先不看这个程序是怎么实现的，我就真的把整个进程一个真实的进程的地址空间全部都给打印出来了。对吧？你会看到，哦，这是一个静态链接的二进制文件，所以从 400000 到 400400 的地方，它的它有一段可读、可执行的一个区域。然后它映射到了，就是它说它叫叫 file mapping 是 a.out。也就是说我有这样的一段，这次就是来真的，它从 400000 到到某一个，这就是这就是我们的 a.out。然后甚至我还可以把它反汇编。当然如果不相信的话，就我就先选择相信它。如果不相信的话，你可以把所有的这个二进制都核对一下，应该确实是看到我们二进制文件里面对应的那个那个文件。好。好。然后除此之外，你们还看到它有一个地址空间高位就在栈附近。它那个这个地方应该是在栈下面，应该是在栈下面，它是从从小到大的。89000000 到 89800000 有一个叫 vvar，有一个叫 vdso，然后在上面才是栈，然后到第四个才是才是栈。我看一下啊，它是 11890000，它是 c5 什么什么，对，在在它的在它的在它的下面。vvar、vdso，然后在它上面才是 stack。那都在这个比较比较比较高的地方高的地方。所以你看到进程的地址空间里面有一些和教科书或者说和理论稍微有一点点不一样的地方。教科书上说的都对，你们计算机系统基础课上说的都对。但是在 Linux 里，除了我因为我这个 a.out 是我直接用 ld 链接的，对吧？它只有一个一个节，它甚至都没有什么只读数据啊数据啊，它就是一个一个只读、可执行的，就只有这一段只读、可执行，它连数据都没有。但即使是这样的一个最小的应用程序，我的地址空间里面都不只是有栈和 a.out，还有两个东西。这个是 Linux 内核提供的一个非常有趣的机制，可以让你在不陷入内核的情况下完成系统调用。实际上是我以前上操作系统课的时候会真的去调这段代码。你你如果有一个比如说像 gettimeofday 这样的系统调用，它其实是如果你要调得非常频繁的话，比如说你要不停地问当前的时间，那如果你每次都进入内核，它其实会浪费很多的时钟周期。然后操作系统就会干一件很有趣的事，操作系统会为每一个这个进程里面在它的地址空间里面都放一段代码，然后这个代码就是系统调用的实现。然后这个系统调用的实现是可以允许访问这段数据的，所以叫 vdso 和 vvar。一个是数据一个是代码，这个是代码，这个是数据。Anyway，这个不重要啊。然后我们的代码比如说我现在要要知道现在的时间是什么，我的 CPU 里面是有一个相对比较精确的定时器的，然后我就可以通过一个比如说秒级的操作系统维护的一个秒级的，比如说这里有一个 seconds，有一个秒级的计时器，年月日秒，加上 CPU 的那个那个非常精密精密的那个小的那个时间，我就可以可以在不进入系统操作系统内核的时候完成这个得到时间的系统调用。然后再比如说如果我要 get 得到当前的 CPU 的编号，对吧？甚至如果我可以为每一个进程如果都分配一页的话，那我可以直接从这个地方得到它的进程号。对吧？我可以把进程号直接放在内存里，这样我 getpid 的系统调用我就不需要进入操作系统内核，我直接从这里读取数据就行了。所以所以操作系统还加载了一小部分代码和和数据来更快速地实现系统调用。但这个也不是大家需要知道的，大家需要知道的就是计算机系统工程里你有意义的事情就一定能做到，对吧？然后那我们现在可以看一看它是它是怎么做到的。它是怎么做到的。那我们也不要不要用传统的方式，对吧？我我只要问就行了。好，那等于说是豆包现在在阅读，现在在思考，对吧？所以我作为熟练工或者说我备过课了，可能会比临时的 AI 要更快一点。好，一个用来探索进程地址空间的命令行工具。它到底是怎样实现的呢？接收进程的 PID 作为输入，它做的事情是解析 /proc/[pid]/maps 文件，分析进程的内存区域，识别每个内存区域的类型，然后对可执行的区域使用 Capstone 反汇编得到 1KB 的内容。好，所以你看我其实要告诉大家的是，你们更重要的，尤其是现在的时代，你们更重要的是要知道我们的计算机操作系统能给大家提供什么样的能力。然后这个能力你看，我只要我我的那个 prompt 里面，我的那个 prompt 里面没有说应该怎么做对吧？你看我只是说因为我学习了 Linux 进程的地址空间，所以应该有办法，有一个强烈的信念，应该有办法从命令行接受一个 PID 解析它的地址空间。但是你是不需要知道这个地址空间是怎样解析的。虽然你多花一点点时间你去了解一下它的实现，你就知道，哦，它有一个叫 proc 下的 PID 下的 maps 文件，这个文件可以帮你得到进程的内存区域。然后我们来看一下是不是啊。我可以到比如说一个真正的，我的当前的 fish 的进程这边。欸，这里有一个文件叫 maps，我也可以编试图编辑它。我们的操作系统其实给大家在文件系统里就 procfs，其实我之前好像提到过，在 procfs 里面有一些和这个进程相关的信息，然后这就包括了这个进程完整的地址空间的镜像。从哪里开始到哪里结束，每一块访问的权限是什么等等。以及比如说如果它是映射到了一个文件，就是你会看到 libc 里面的一一些文件里面的一些部分其实是被被放到了进程的地址空间里，libm 对吧，libgcc 等等。以及这里有 vvar 和 vdso。所以以前以前我还会大家去去看比如说 proc 的手册。曾经这个手册里面是有比如说像像刚才我们说的那个 maps 文件里面有的所有的域到底是什么呢，这样你通过看手册你就可以知道你应该怎么解析一个操作系统里面描述了进程地址空间的那个文件。但是现在都不需要了，你直接问问 AI 就可以，就可以。你只需要知道这件事情能做，你就可以把它做好。OK。现在我们就知道了进程的地址空间。一个最小的程序对吧，一个 minimal 最小的程序，它里面至少也有这么多的东西。那你下面就可以问出一个问题：如果我们要真真的我们不是一个 minimal，就这个 a.out 不是我刚才那个死循环，while 1 的死循环，而是我真的是一个复杂的程序，我要是我想用它实现很多功能，比如说我在这里能不能有没有可能啊要用一个 malloc，对吧？我可以调用一个 malloc，而且我可以调用 malloc 甚至要很大的申请一个很大的内存区域。理论上说这件事情应该是可以做的。所以还是那个道理，只要这件是可以做，应该就一定有一个办法给做到。OK，那下面下一个问题就是：我们现在初始的时候，地址空间里面就什么也没有，对吧？只有栈，只有 vdso、vvar，当然这两个我

大家不需要知道，所以我就，就把擦掉了。假设，假装它没有。那我现在的地址空间里就只有 a.out 和，和栈。那如果我要分配一个那么大的一个 GB 或者 10 个 GB 的数组，现在任何地址空间里面的指针，如果指向，如果大家回去做实验的话，指向任何一个我这里是空着的区域，访问它都会导致 segmentation fault。那我这个 malloc 应该怎么实现呢？下一句话就是应该是有一个系统调用可以帮我们来实现这个功能，对吧？应，进程但凡它自己做不了的事情都必须请求操作系统。所以应该有一个操作系统可以帮我们去配置或者修改这个进程的地址空间。打个比方，我应该有一个系统内部可以可以问操作系统要内存。对吧，要，因为这个地方现在所有我没有画的地方都是 dead zone。你只要，你只要任何一个指针只要碰到它，比如说一个 0 对吧，所有的进程的 0 这一页，默认都是不可访问的。所以你如果读或者写的话，你，你程序就 crash。这样你空指针，如果有空指针，它成，就知道你的程序其实是有个 bug。如果我们有一个系统调用可以帮我们，比如说就，就比如说就叫 malloc 系统调用，对吧？我 malloc 一个 1 兆、100 兆、一个 GB 的内存，然后它就在这个地址空间里面给我，dead zone 里面给我分配一块，然后这一块就我访问它，我就可以读、可以写，并且比如说它的初始值是 0。那我只要有一个这样的系统调用，我就，可以实现 malloc 了，对吧？我就可以实现，而且可以，我既然能实现 malloc，我理论上说就可以布置出任何一种地址空间的样子，就像我刚才看到那个一个真实的进程的地址空间一样。这是怎么办到的呢？是你们在计算机系统基础课上面学过的叫 MMU。我们知道我们的系统，计算机系统是用虚拟内存的，这解释了对吧，我这节课刚开始的时候讲的那个虚拟内存。所谓 MMU，你就理解成我们的物理世界就是 memory，就是一个内存，然后我就是一台计算机。对吧，然后那我，我，我是我是 CPU internal state。这个，这个地方其实还挺好用的，因为我后面讲到并发的时候还会，还会就把自己想象成是，是一个线程或者想象成是一个计算机。然后我们的物理世界就是整个共享的内存，相当于整个物理的空间。但实际上我并不是直接看到这个，虽然看起来我，我是我是看到这个实际的物理空间的，但没有人知道我到底是生活在一个真实的世界里，还是生活在一个脑子生活在一个水缸里。对吧？没有人能证明自己不是生活在一个虚拟的环境里。然后我们的 MMU 它的功效就是给，相当于是操作系统给进程，就用户态的进程，强制套上了一个摘不下来的 VR 眼镜。然后我的进程其实，因为进程它能看到的指，它能用的指令是比较少的，它不能随便去配置 MMU，对吧，它不能执行系统的指令。它进程只能执行自己地址空间里面的计算，load、store 和计算。进程无非就，还有系统调用啊，它自己能够在自己的地址空间范畴内做的就只有 load 和 store。所以它根本就不知道世界，世界之外还有，是不是还有一个世界，但实际上世界之外还有一个世界。操作系统给进程带了一个 VR 眼镜。然后这个所谓的 VR 眼镜就意味着你想看到什么，实际上操作系统系统给你编出来的。所以操作系统想让你能访问这一页，就可以访问这一页，因为因为所有的 load 和 store，所有的访，访存都是通过 load、store。当然我们的取指，取指令译码也，也，也可以理解成是一个，一个 load。然后所有的 load 和 store 都会经过 MMU。然后经过 MMU 以后，操作系统可以配置这个 MMU，让它这个 load 和 store 可以使产生一个异常，也可以是反，把它路由到任何一个物理页面上，任何一个操作系统可以配置的物理页面上。所以就是因为有这样的 VR 眼镜，所以我们的操作系统是可以实现这个分配内存这样的系统调用的。那如果让你来设计，就是修改，一定会有个系统调用来修改进程的地址空间，那你就会怎么设计呢？每个同学都可以花那么几秒钟想一想。然后 UNIX 的答案就是一个叫 brk 或者叫 sbrk 的系统调用，我们也可以看一下。这个手册叫 system calls manual，就叫 brk 和 sbrk，就叫 change data segment size，非常遗留，历史遗留的 system call。这为什么呢？是因为以前的 a.out 除了代码，就 code，a.out 就是 code、data 和 bss。code，code 就是代码；data 就是初始化的数据，你那 x=1 的那种全局变量；然后 bss 就是那些你没有初始化过的数据。然后这个地方就叫 break。对吧？它相当于在这里就 break 了，后面，后面的东西就内存就不合法了。然后什么叫 set break 呢？对吧，set break 就是我要更改这个 break。所以如果我要更多内存的时候，我就可以把这个 break 往上移。然后如果我要更少内存的时候，比如说我，我内存不需要了，我上面都释放掉的时候，我就可以把这个 break 往下移。这样我不就实现了内存地址空间的更改嘛。这是 UNIX 最早的最早的一个可以改变进程地址空间的系统调用。当然今天这个系统调用显得稍微有点不够用了，我们有一个更，更先进的、更好用，把所有各种和地址空间相关的功能都集合在一起的系统调用叫 mmap。mmap 也非常直观，所谓的地址空间，进程的地址空间是什么？就是一块一块可以访问的内存。对吧？就初始的时候，比如说刚才那个最简单的例子里面有一个 a.out。所谓的进程地址空间就是里面一块一块，a.out 有一个什么 8KB 的东西是，是可读、可写、可执行的。那 mmap 所做的功能就是可以从地址空间里面找一块，它可以指定一个大小，然后可以指定一个用途或者说权限。然后这个用途意味着有可能我可以把这一段地址空间可以映射到一个文件，可以是一个文件，也可以是一个新分配的内存，可以是很多，反正，反正它集成了很多功能。在 mmap 在地址空间上面添一段，添一段合法的地址空间，并且我可以配置它是什么。然后还有一个，有，那有分配就有回收，所以 mmap 和它有一个对应的叫 munmap。我，我可以，对吧，我分配一段，我可以把其中的，甚至我可以把其中的一部分给 unmap 了。比如说我，我 mmap 我可以用一个叫，叫 MAP_ANONYMOUS，叫匿名，我想，我想匿，映射一个叫匿名的页，所谓的匿名就是我要分配一段内存，这段内存是可用的，然后初始值是 0。然后我可以分配比如说一个 GB 的内存，我只要说给我分配 1G 吧，它就会给我分配一个 G gigabyte。然后那我还可以把前面的比如说 512 兆给 munmap 了，这都是，这都是可以的。我可以随心所欲地修改进程地址空间里面的，这整个的映射。然后以及我还可以把一，一，一段我已经持有的内存给它用 mprotect 修改它的访问权限。比如说原来这段内存是只读的，然后我现在把它变成只，可读可写的。然后有一段内存可能是我自己的数据，比如说我用 malloc 分配出来的内存，然后我现在因为在上面填上了代码，我想把它变得可执行，那我也可以需要用这个 mprotect 的系统调用。然后所以我就有了一个非常疯狂的想法，虽然我今天没有实现。还记得那个 CrazyOS 吗？那个 CrazyOS 现在有一个系统调用，就是 putchar。然后我上一次 live coding 给它加了一个系统调用叫 fork。然后其实我也可以把这个 mmap 这些，就我可以把 Linux 世界里面所有的系统调用都放到 CrazyOS 里，然后 CrazyOS 就可以直接直运行 Linux 上的进程。对吧，一个 Linux，一个 RISC-V 的 Linux 的程序。只不过它所有的系统调用都是我在，我在用户态模拟出来的。这件事情理论上说完全是可以，可以做到的。然后也许明年还要开操作系统课的话就可以，就可以这么玩了。我可以在这那个，你们可以回去试试看啊，可以让他直接在 CrazyOS 上实现 mmap。它应该，如果你用一个够好的模型，它应该可以一把手就做对的。OK，这是，这是 mmap。然后 mmap 呢有很多的应用，看起来，就看起来是一个说，啊，我就是修改进程的地址空间嘛，在概念上很简单，但其实它的用途很多。比如说我可以一次性生成，申，申请很极大的一段内存空间。你们现在知道程序的运行一秒钟，你的，你的 CPU 一秒钟也就是运行可能十亿或者十亿到一百亿条指令，最多就这么多了。或者一个，一个，一个 CPU 核心，对吧，一个处理器，它就运行，运行这么多了。那如果啊，如果，如果我现在问操作系统直接要 8GB 的内存，对我的是，我的，我的树莓派整个就总共就 8G 的内存。我现在可以很开心地问操作系统要 8G 的内存，已经超过我的物理内存的，就和我们物理，物理内存一样大，但显然因为我自己的操作系统本身是要消耗内存的，然后我系统上面其他的程序，对吧，我的 VS Code