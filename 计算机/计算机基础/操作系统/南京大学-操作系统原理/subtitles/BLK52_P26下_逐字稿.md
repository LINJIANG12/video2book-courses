到所有的所有的 blocks。然后对于目录 文件来说，我只需要像这个 struct inode 我就叫给它一个名字叫 inode。inode 是有编号的对吧？ inode 是有编号的。 呃每一个 inode 有一个 编号。然后 我们就在一个目录的 这个数据里面，目录源存一个 map 从文件名到 inode 的编号的映射就可以了。 当然你不需要用一个呃比较 fancy 的数据结构，你就你就顺序的对吧，你就把文件名 比如比如文件 文件名，然后一个 inode，然后比如一个 \0，然后一个文件名，一个 inode， 一个 \0，你就顺序的存在这个里面，对吧，一个 一个目录文件。然后这就是边。 就跟你们平时说在算数据结构或者算法时候 OJ 要写一个图，要实现一个 graph，你们也是这样实现的。 对吧？呃如果你要在在边上面放一个 label 的话。 对吧，目标节点在哪，然后它的名字是什么，这样数据结构整个就 就实现了。 但是 Unix FS 里面 它比较有趣的一个设计是这个 index。 然后这个 index 它是怎么做的呢？ 还记得我们说 大部分文件都是小文件。 对吧？ 大部分文件都是小文件，而且绝大部分文件你都是读一个头， 它的次数会比读整个文件要多得多。 对，因为你很多时候你需要知道这个文件是什么，然后稍微看一下就行了。你比如说我知道这是个 binary，是个什么 binary，那后面的东西可能我不在意它。 所以， Unix 就希望 这个文件在访问头部的时候可以非常快， 而又能够支持很大的文件，所以它做了一个联合的数据结构。 但是它是做这样的，它你可以这样 int 叫比如说啊，这 first block， second block， 它直接把它的这个文件的第一块直接就写在这个 inode 里。 第二块也写在 inode 里， 但是它只写 10 块， 应该是 10 块，那可能 13 个指针还是 12 个指针，OK，我我我不确定，还是 9 块，doesn't matter，它先把一些就是文件的头部的那些块直接放在 inode 里， 所以你直接放在 inode 里。 然后， 如果这个文件超大了， 我就会拿一个块来做间接索引， 相当于是1层的间接索引， 像一个1级页表。 然后如果这个1级页表还不够大， 再用一个2级页表， 一个指针，再用一个2级页表。然后如果这个还不够大，再用一个3级页表，这样就可以索引足够的非常非常非常大的文件了。对吧，比如说一个 4KB 的页表项页表它一有 1024 个页表项， 所以你1级索引就可以 1024 * 1024， 然后这个就可以 1024 * 1024 * 1024，对吧？ 呃 3 3级表。所以这是一个非常漂亮的数据结构的联合，它用了 locality。 然后然后你你你只要有读这个 inode， 比如说你要读文件头，你直接从 inode 里面知道这个文件头是什么。 然后， 还有一个设计， 它把所有的 inode 都放在一起。 这样它就有 locality 了。 你在 访问这些 inode 的时候，一般来说， 像一个目录里的文件它都是 在 inode 的区域里面存储也是近乎连续的。因为你分配的时候嘛，你你在这个目录里面写 10 个文件，你就连续分配 10 个，分配 10 个你就大概率会得到连续的分配。 然后这就意味着如果它把 inode 全部放到一起的话， 你一次把这一块给读出来，你就会 把相邻的 对吧，和它有相关关系的文件一起都载入到 内存里，所以 在有缓存配合的情况下，呃这个 inode 也是一个合理的可以 justified 的 实现。 而且这个实现更有意思了，比如说在呃 比如说你要实现符号链接的时候， 你要实现符号链接的时候，它就可以偷这个部分的内存。 如果你是一个小于等于 60 字节的符号链接， 那这些索引块是不是就不需要了，对吧？索引块你不需要再为它分配一块，而是而是你可以直接把 小于等于 60，比如说 我现在要把 a 把这个把这个文件 把这个文件链接到什么 ../a。 ../a 总共只有 4 个字节。 总共就 4 个字节，我就可以直接偷这一块的内存 去存一个，把这块内存当成是一个字符串来存 ../a。这样我我甚至都读不需要为它分配任何一个数据块。 就说我在这里面有一些冗余的空间， 可以让我有很多 好玩的事情可以 呃好玩的事情可以干。 然后我们甚至是真的可以在文件系统里面 呃看到这个呃这个行为的，我也做了一个 这样的工具。 你不需要真的去读代码， 你让 AI 去看一下代码的结构，然后它对 Unix 世界里面有的工具其实是非常 呃熟悉的。 对吧，这是一个 ext 文件的大可持化解剖 呃工具。 核心用到的是一个叫 debugfs 的 东西。 甚至你不需要，就是在你做这些工具之前，你只要有一个概念说我认为操作系统应该有一个办法把 文件系统的那些底层的那些信息 呃 暴露出来， 呃而不需要而不需要这个 呃去我我们用上层的那些 API 呃是可以的，它有一个专门的叫 debugfs，然后我甚至可以可以输出一个 这是我我的我的 demo 里面的那个 呃目录树的结构，这个看起来 看起来比较简单对吧，我可以 cd 进去，哎， 然后我可以现在可以做一个链接，比如说 呃 对吧，然后我可以木创创建一个目录， 呃当然我可以写一个文件， 也可以做一个链接，然后做完了以后我可以再次 哎呀， 打卡它。 呃你你想看更多信息也没有问题，你就让 AI 去去去帮你写，你可以看到 呃这里的包括 link 的 count， 对吧，然后你看到 这个图结构正确的被被展被显示出来，这 a.txt 和 b.txt，然后它的 它的 block 的 ID 对吧，你看这些 每一个文件 README 的那个 block 的 ID， 然后 这个 dump_ext4.py 这个这个文件一共有 10 16.1KB， 呃然后它一共占用了 5 个 block，然后这 5 个 block 在 磁盘里面是连续的。 对吧，然后这是一个符号链接，这个符号链接是 inline 在 i_block 里面， 这主要告诉大家的是这些事情都是可以做到的，你 你只要有兴趣试着构建一个工具，你就能够 呃得到很不一样 的 呃更深入的理解， 对。 OK，这是这是 Unix 的实现，所以 本质上文件系统就是一个数据结构， 你完全是应该可以在数据结构课里面把所有的内容都讲好了，对吧，我们用数据结构的知识 呃就把 呃 就把文件系统系统给讲了。 但是操作系统 里面的文件系统 还有一个 呃非常重要的职责，这个是你们数据结构课没有的， 就是应对崩溃。 实际上你们的系统随时随地都有可能 crash， 对吧，可能是 power loss， 你你就就没电了，没电了你你一个没有电池的设备它 瞬间就所有的内存里的数据就都没有了。 你们是已经接受了 crash、 掉电、 内存数据丢失这样的一个事实了，就已经习以为常了。 但是对于 persistent storage 来说， 它要非常小心的确保 即便你有可能 power loss， 你的文件系统里面的数据也不能丢。 这就带来一个叫 crash consistency 问题，他说 crash consistency 就是需要把 file system 从一个 consistent 的 state， 它下一步必须要挪到另一个 consistent state。 然后为什么 crash consistency 的实现 是很困难的呢？ 就是因为我们说 这是一个数据结构， 对吧，不管你是这个 Unix 的文件系统还是 FAT 还是任何一个数据结构，哪怕你 你写一个链表对吧，你要 insert 一个 insert 一个把将 y insert 到 x 里，那比如说你说好， OK，那你 n = x 的 next。 好，那你就要做好多个指针的操作。 比如你就说 x 的 next = y， y 的 previous = x，然后 next 的那 previous = y， 然后 y 的 next = n，啊没关系，呃 doesn't matter。 你们已经习惯 这样写了，对吧，更新一个数据结构，插入插入一个节点。那 在并发的时候， 为了防止 data race， 你会 在这里加一个 lock，在这里加一个 unlock。 对吧，这也 OK。但你们从来没有想过 如果 这个时候这个线程突然被杀掉的话会怎么样？ 也就说对吧，crash consistency 有点像你这个线程随时随地都有可能突然消失。 那突然消失意味着什么呢？比如说在这， 你这样想吧对吧，这个这数结构 呃 我有一个 x，有一个 n，然后有一个 y，好，那我第一步是 x 的 next = y，好，这个对了。 然后 y 的 previous = x，好，这对了。 然后呃一开始是这样的， 呃好。 这时候 n 的 previous 是在这，然后 n 的 previous 我要改成 y， 好。 然后这时候 y 的 next 还是空的， 突然 在这个时候线程消失了。 我写了 对吧，我写了 1 2 3 4，我写了 1 2 3， 4 没了。 因为这个时候系统 crash 了， 现场 crash 了。 那这个时候这数据结构就在一个 inconsistent 的状态了。 对吧，y 的 next 是没有的。 y 的 next 漂浮漂到 漂到 0 了， 然后你如果遍历这个数据结构，这个数据结构就 crash 了。 就是说你你必须要把所有的操作都执行完，这个数据结构才是 consistent 的。 对吧？ 然后你马上就发现这个事情 没有你想想的那么简单。 比如说 如果我想要 呃去往数据结构里面写一

的内容。打比方，你要你要 append 一个，对，你要 append 一个 4 4KB，就说一个，就一个 block，恰好一个 block 最简单的。append 一个 block。那你需要需要什么呢？你首先你需要 allocate 一个新的 block。对吧，比如说等于 40，好。然后你需要写 data。以及你需要更新那个元数据。好，对吧？这是 1 2 3。然后真正的麻烦在在什么呢？还记得那个 SSD 上面，我拿放大镜给大家看过，看过上面有一个计算机系统。你的一个 I/O 的请求，比如说这个 write 的请求，或者 update inode 的请求，它只是你当你执行这个 block write，bwrite 的时候，对吧？这两个这三个其实都是涉及到 bwrite。这个会把 40 的这个 bitmap 给更新，对吧？因为你说你把第 40 块给分配出去了，你就需要把那个那个 40 bitmap 的对应的 40 块给置成 0。然后你这个也需要一个 bwrite 的话，把这个数据写进去。然后你这也需要一个 bwrite 会把这个 inode 的那个块写进去。然后你想，这些 bwrite 执行以后，它都是到一个队列里的。然后这个队列是进了你的计算你的存储设备上面的计算机系统。你想想这是一个，如果你这是一个旋转的一个硬盘，那它有一个读写头，这个读写头会怎么样处理这个请求呢？当然是离我先进我先处理谁。对吧，如果我马上马上这个盘片转过来以后，我这二二号请求就在我底下，我肯定是先把它做了。因为反正 1 2 3 是不同的 block，我按照 1 2 3 的顺序，按着 3 2 1 的顺序，eventually 它都是一样的。但就是因为这个 eventually 一样，导致什么呢？导致实际上它是任意的顺序都有可能的。任意顺顺序都有可能，如果随时随地发生一个 crash，就意味着我这里有三个 bwrite，它任何一个 subset 都是有可能的。所以可能会发生什么问题呢？可能会发生我现在 inode 也更新了，数据也写进去了，好，但是 allocation 的 bitmap 忘记更新了。这会发生什么呢？好，我现在这个文件，inode，对吧？好，我说我现在知道 inode 里面有一个第 40 块。然后这第 40 块里面有一个文件，就说写了 hello。这个也是对的。但是这个 40 块在文件系统的视角上还是 free 的。对吧，如果这一块丢了，你去运行这个程序没有问题，hello 是在的。就意味着未来有一天，这个 40 块分配给别人了，别人又写了一个文件，然后他把这个 hello 给覆盖掉了。结果你这个文件好好的，第二天过来发现文件的内容变了。没有想到过还会有这种问题，对吧？所以说我说打开了一个魔鬼的盒子，和并发一样的问题，它会 reorder，它会乱序。它和并发里面的那个是是一样的思考方式。然后这就麻烦了。你想呢？光这个情况我有三个 bwrite，就有 2 的 3 次方等于 8 种情况。有的情况是安全的，那我这个 data 写到写到第 40 块了，但 40 块还是 free 的，然后 inode 也没更新，所以相当于我就往一个 free 的内存里面写了一些值，这个是安全的。对吧，但是有些情况像我刚才说那种是是不安全的。你必须非常小心地处理所有的所有的这些情况。好。所以呀确实 crash 是会导致文件系统的不一致的。那会有怎么样的后果呢？然后甚至是人类在几十年的时间里面其实都是在一种裸奔的状态。就是文件系统的实现者他可能知道这个 crash consistency 问题，但是因为想要实现 crash-safe 的数据结构，可能在 1970 年的时候显得太太过复杂了，所以就干脆我们不管了。我们不管。然后不管不管就带来一个什么什么后果呢？就做 systems 的人都是这样的，都是先挣钱后还债。然后你你你系统会 crash，系统会 crash，然后 crash 了以后你的数据可能会丢失，目录可能会损坏。对吧，比如说 super 如果你真的写入的时候是一个打打比方写一个 inode region 给他给他写挂了，对吧，它 crash 的时候有可能甚至就 crash 的时候有可能是，我要写这个 2，然后正好我我读写头已经到这了，我已经开始写了，结果这个 RAM 掉电了。RAM 掉电以后，哗，就把那个 block 给抹掉了。对吧，甚至都有这种可能性。它会得到一个一个 corrupted file system，完全有可能的。我们在我当时在美国的 lab 他们做了这样一个很有意思的小面包板。然后这个面包板就可以从 VGA 里面送一个信号，相对我就从 VGA 送一个信号就可以编程了嘛。然后我从 VGA 里送一个信号以后，可以可以物理性地把 SSD 的电源给关断。然后关断以后，发现很多 SSD 的计算机系统其实有很大问题的。它 crash 以后整个这个盘就可能会死掉，就是你再也读不出任何数据了。而且你知道 SSD 的修复是非常复杂的，它和 HDD 不一样，你还可以拆盘。对吧，SSD 基本上没有办法修复。然后这个工作后来促成了厂商的一一大批固件的修复，反正在后来就再再也找不到找不到这样的问题了。然后做 systems 的人的思路很简单粗暴，反正我的文件系统是能用的，我文文件系统是好用的，这种小问题我先不管。然后如果出了问题怎么办呢？你可以事后送去修电脑，做个 file system check。当然 file system check 的这个后果也是很严重的。你们都知道快速格式化的文件系统，你是可以把数据给捞回来的。对吧，比如说那个摄影师事件里面，修电脑的最后就背叛了 8 个月的有期徒刑。当然当然这个是可能是在你们出生是不是在你们出生以前的事情了。这个 inspired 了我们的那个 fs recovery 的实验。然后你你实际上我们在 Unix 的很长的一段时间里面做的当 crash 以后，都需要做一次 file system checking。你们有没有在小的时候，在你们的机房里面如果还是还是比较老的 Windows 系统的话，会看到它在启动的时候会有一个扫描文件系统，就正在扫描文件系统，找到多少坏簇，然后把它们都修复了。对吧，本质上就是你的数据结构有可能，比如说刚才你的 40 块是 free 的，40 块是 free 的，在 bitmap 里面，第 40 块是是 free 的，但是在 inode 里面有一个 40 块指向了一个内容。哎，这时候如果你扫描这个磁道上所有的 inode，你就可以遍历出所有已用的，相当于是已经使用的存储空间。然后如果已经使用的存储空间的话，那那你发现比如说 40 已经使用了，那这个 40 就不应该是 free 的，你应该 fix 这个这个 40。对吧，你应该 fix 这个 bitmap。然后很长一段时间里面，甚至大家还在做做这种 rule-based file system checking。然后你就可以，你可以看到它它有，他说如果在你有些时候是当目录发生移动的时候，对吧，你会得到一个很奇怪的，对吧，对吧，一个一个很奇怪的，比如说第 1 指着第 4，第 2 指着第 4，然后第 3 的父亲是第 1，第 4 的父亲是第 2。一个，就你看起来也看不懂的文件系统结构，你必须要猜一个，对吧，你必须要猜一个原，到底是什么样的前面的的状态，导致了现在这个不一致的状态。当然像 Unix 格式，默认的 FSCK，他就不会说把它丢到一个叫 lost+found 的目录里。所以你们看到吗？有的时候你会在有的时候会在你们的文件系统里看到一个叫 lost+found find 的目录。这个目录就是在你的文件系统崩溃以后，它重新扫描以后，扫描这个一致性的数据结构以后，它有些文件比如说已经没有这个 inode，比如说从已经没有任何的目录指向它了，可能是那个目录文件损坏了，它依然可以把这个文件，如果那个 inode 还在的话，inode 还在的话，可以把这个文件给捞回来，会放到那个 lost+found 里面。很有意思的一个 file file system check。但是这不是我们想要的，其实不是我们想要的。我们真正想要的是实现一个 crash-safe 的文件系统。它在任何时候它 crash 都可以，它不会影响我们的一致性。那这是怎么实现的呢？这还是要回到我们的数据结构上。好，打比方，我举一个二叉树的例子。比如说我们现在有个二叉树，一开始是空的，行，一开始，对，一开始是空的。就什么没有，一个二叉树。然后那我们第一次比如说 insert 3，我们就会得到一个 3 在根上。然后再比如说 insert 一个 6，那 6 就会在这儿，对吧？6。然后再 insert 一个 比如说 5，5 会在这儿。好，那这个时候比如说数据结构可能是有 parent 指针的，对吧？有 parent 指针。那就跟刚才链表一样，你会面临 multiple write 更新的问题。也就是你说你你会你会说在我写这个 5 的时候，我说 5 的 parent 等于 6，然后 6 的 left 等于 5。打比方，你有你你有多个这样的写入。这时候它是 crash unsafe 的。也就是说你对数据结构的写是 crash unsafe 的。但是什么东西你可以容易做到 crash-consistent 呢？append-only。所以数据结构其实有两种视角，第一种视角这就是实际的数据结构。对吧，你说好，这就是一棵树，然后我我的树上每一个节点有 left、right，有 parent。这是这是这这个就叫数据结构，然后它有一个比如说 O(log n) 的查询时间，我很喜欢它。还有一种方式，

记录这个数据结构，是记住它所有的操作。 如果你能记住所有的操作， 就算是这个数据结构不一致了， 对吧，丢了一个，比如说 5.parent = 6 丢了。 对，你也不知道这这个 1 和 2 谁谁谁被 persisted 了， 不重要。 甚至整个文件系统，整整个数据结构丢了都可以。 你只要 replay 这个操-插入的序列， 你就可以 把这个数据结构再恢复回来。 甚至你可以做一件什么事呢？ 我可以做一个 memory trace。 也就是说， 我对所有的数据结构共享内存的修改和分配， 我全都比如说 mem 的 3 比如说 5.left 6.left = 5 然后 mem of 5.parent = 6 你实际上可以记录的是所有的对 share memory 的 write。 然后你 replay 这个所有对 share memory 的 write，就可以 replay 任何数据结构。 因为数据结构的本质，本质上你都是通过 memory write 实现的。 你只要把所有的 memory write 按顺序记下来， 你 replay 这个 memory write，你就可以得到一个一模一样的数据结构。 就这么简单。 对，所以你 实现 crash consistency 它真正做的事情就是一个 1 + 1 > 2。 我们同时要在 block 上面存储一个这样的就是 这个 所有的 呃这就是个 Unix 文件系统。 这我们刚才看到的，对吧，说有什么有 inode bitmap，然后有 inode， 这这 struct node 的一个数组，然后全部都在这。 然后 我在 磁性上面单独开放一个区域， 叫做 journaling。我看我有没有 slides 讲这个。 我单独在文件系统里面开辟一块区域叫 journal。然后我每当我执行一个 bwrite 的时候， 我会干什么呢？ 我会先把这个 bwrite 以 append-only 的方式写到 journal 里。 然后 等我的这个确保我这个 我的 bwrite 任何数据， 这个 bwrite 写到这个 journal 以后，我再 去把 这个 bwrite 再去写到这个数据结构里。 然后如果我这个数据结构写进去了，我这个东西就可以丢掉了。 但是 只要我的这个数据结构比如说这里还没有写完， 那我我我这里始终是 on-disk 的，就把他搬到磁性上这个操作还没有执行完，那么这个东西一直存在。 所以如果在这个时候发生 crash， 磁卡上的数据结构是不一致的。 但是我 re-reboot 以后，我可以把 这些 bwrite 重新再 replay 一遍。 这个叫 write-ahead log 或者叫 redo log。 我我更喜欢 redo log，就是说你你你这个 log 是为了从做是為了 redo 的。 这叫 write-ahead，在写之前的一个 log，也叫 re-redo log。 呃 redo log。 然后这样我们就实现了就就我可以实现一个任意的数据结构的呃 crash consistency。 好。 那这里面就有个非常呃经典的 数据结构叫呃 LSM-Tree，叫 Log-Structured Merge-tree。 呃大概是大概原理是这样的，你们可以可以回去可以在 AI 的帮助下下学习一下。这是现在现在的这种像 K-key-value 啊甚至数据库啊都是都是以它为根基的。 它基本上是这样的， 如果你要实现一个高效的在磁盘上面的数据结构， 你你你首先会在内存里面 放这个比较热的数据。 然后你你如果要写，比如说你要写一个数据的话，你会直接写到内存里。 然后这个内存里面有一个，而且有一个并发比较友好的像 skip list 这样的呃数据结构。然后你对这个数据结构所有的更新都会以 write-ahead log 的形式 呃写到写到这样的一个日志 日志里面。 然后直到 直到这个 memtable 写满了以后， 写满后，当然如果随时这个 memory uh 这个系统发生 crash，那因为我有这个 write-ahead log，所以我可以把内存里面的这个数据结构也再也在重建回来。然后到 memory table 写满了以后，我就会触发一个 flush，向磁盘的持久化。 然后磁-磁卡上面存储的一颗一颗树全部都是不可变的。 全部都是不可变的，它都是那种靠均摊分析。有-有点像什么呢？我我现在 我有一个很大的数据结构，然后它一层一层的，每一层都比下-下面的一层总比上一层大，然后最上面的一层是 memory，它很小。 然后 memory 写满了以后就会触发下层的合并，比如说它发现 这个东西写满了以后，我要插入到这一层。 然后插入到这一层以后， 它可以把这层的两个节点又发现又太大了，又太大又会合并，它又会到下一层。 然后这层如果又太大了，它又会把它在合并到下一层。然后就通过这样均摊的方式得到一个接近于 O(1) 的呃时间复杂度。哎我没有时间来展开讲这个 LSM-Tree，但是它就是，它的核心就是一个内存的层次的存储加上一个 write-ahead 呃 write-ahead log，对。 呃然后在文件系统里面，在文件系统里面如果你要实现这样的呃 journaling 呃你你其实不需要像我说的说得这样直接把所有的 bwrite 就就对于任意的数据结构我都呃 都成立的这样的 呃 这样的日志。而是你可以呃你可以这样，你可以这样，你你在任何时候 你在想要写入一些数据，比如说 呃刚才我们说我们要 update 一个 inode，对吧？ 我想要 update 一个 inode，然后我想要 update 一个 bitmap， 就说我的一块数据块被分配了，然后我希望往某个地方写 data。 我只需要把这个实际的需要写入的东西直接往里写，然后在这里我做一个标记。 这个叫 transaction begin。 说 我的 transaction，事物要开始了，transaction 开始了。好， 在这个时候如果 crash 如果 crash，那那那这个数据我认为就随时都可能丢嘛，对吧？随时都可能丢，所以所以这个数据都是不可靠的。 所以在 这个 transaction begin 写完了以后， 我们会执行一个 flush。 会告诉磁盘说我要等待 等待所有的这些数据要落盘， 等它落盘。 然后等它落盘完了以后， 我会再写一个特殊的标记。 这个叫 transaction end， 代表事物结束了。所以 我会先我会先 flush， 然后再 写这个 transaction end。 然后我完了以后可以再 flush 一次，OK。 呃可以再 flush 一次。 这就意味着什么呢？这就意味着 是这些数据，所有的这些数据都 happens-before flush。 flush happen before 我写 transaction end。 所以说这就反过来就是 当我在磁盘上面如果看到了这个 transaction end 标记的时候， 对吧？说明 transaction end 已经发生了。 就 transaction end 必须发生 我才能看到它。 因为 transaction end 发生了，所以 flush 一定也发生了，而且完成了。 flush 发生了而且完成了，就意味着 这些数据一定已经 落盘了。 一定落盘了以后就相当于是我对数据结构操作已经被记下来了， 我只要 replay 它就可以了。 所以我我做的就是 这个叫 checkpointing。 就是我 我当我把这个东西写进去，并且确保它落盘以后， 我就可以把这个操作 replay 到，现在是 redo 嘛， replay 到 实际的数据结构上。 然后 replay 完了以后我就可以把这个 journal 呃给给消掉了。然后在这个时候如果发生崩溃， 在这个时候发生崩溃，如果在在 flush 之前发生崩溃，那这个事情就好像没有做过，因为我不能保证所有的数据都落盘。 但是如果在 flush 之后或者 transaction end 之后， 我看到了这个 transaction end， 那这些落盘的操作我就可以保证 在重启以后可以正确的反映到文件系统上，我就实现了 呃 crash consistency。 然后为了实现这样高效的 journal，其实 trick 还挺多的。 呃比如说 在哪怕是 xv6 这样非常非常小的教学用的操作系统，你们不知道有没有听同学有兴趣去看一下，啊我今年不讲了。 呃它会把多次系统调用的 transaction 合并成一个。 就说我我可能有好多好多 ongoing 的 write。 但是 呃我可以把这好多好多 ongoing write，比如说我写了这个文件，写了这个文件，写了这个文件，做成一个 transaction。 这样我就只要 flush flush 一次。 相当于是我 crash 的时候可能会稍微丢多一点的东西，但是 呃我减少了对磁盘的这个等待它同步的呃的次数。 然后 Linux 也是的，Linux 里面有一个 呃 有个叫 Journaling Block Device。 叫 JBD， 呃 JBD2 是第二个实现。 呃你会看到对于每一个设备都有一个这样的 呃 journaling 的 呃 daemon 在后台。它每隔一段时间，比如说 30 秒钟的时候会会把 这个东西做一个 snapshot，而不是你你你写一个文件以后马上就 马上就会呃落盘。在在系统里面会稍微 稍微要要慢一点。 呃然后 ext4 很有意思。 刚才我们说 我们有，我们的核心是一个 happens-before，对吧？ 我们要要做这个 flush。 然后因为有这个 flush 落盘，所以我们就可以可以确保 先数据到达磁盘，我才能看到 呃 transaction end， 有这个 happens-before 的 呃关系。 然后 ext4 做了做了一个 呃非常精高明的操作。 他说我我不需要这个 transaction end。 我也不需要这个 呃就不用真的甚至都 可以不需要去真的去 flush，等它 flush。你你还是可以可以把这个日志推落盘，这样这样比较安全。 而是 我在头上直接写一个 checksum。 加一个长度。 这样 我在 crash 以后， 我不是直接扫描这个 journal 的区域，然后我看下哦这有个 checksum，这有个长度，然后我来读一下这个长度是多少， 长度是这么多，它 checksum 对不对。 如果 checksum 是对的，就说明它正确落盘了。

我就 replay 它。如果 checksum 不对，我就把后面的全扔了。 对吧？就，就这么简单。这样它就可以省掉，它用 checksum 的机制来省掉，这种落盘的 synchronization。 然后以及， 像 ext4，它会做这种 metadata journaling。 因为我们知道数据 占磁盘写入的绝大部分，对吧？你绝大部分写磁盘实际上是写数据的。而数据其实丢不丢， 通常对你来说都是不要紧的。 因为你的程序，应用程序一定程度上有义务要保证你， 你，你不会丢掉数据，对吧？你不能在保存一个文件的时候丢掉一些数据。 所以， 呃，ext4 default 的时候会采用一种 metadata journaling。它把所有的 inode 啊，bitmap 啊，这些东西的， 呃，更新全部都会写到日志里。但是你对文件内容的， 呃，修改它不会，不会反映到日志里。因为无非 丢也就是 crash consistency 意味着你 crash 了，它会丢一小段时间内的内容。 所以它更多的是要确保一个目录树的完整性，它不能因为你的那个目录节点，发生问题，然后把你的目录整个移到 lost+found 去了，这就， 这对你来说是一件比较糟糕的事，对吧？呃，它要保证，呃，文件系统的目录结构是一致的，但是数据可能是丢的，这样可以提高性能。 真的是这样的吗？ 答案是不是。 实现 crash consistency 比你们想象的， 呃，要 麻烦一点。 假设啊，假设你写了一个文本编辑器，就像你们在数据结构课上面，呃，做了一个 Vim 的 clone。 然后你，你，你做了这样的一件事，你，你现在，你现在在 buffer 里面，你自己的 buffer 里面有一个 a.txt。 这是 memory。 那你，怎么保存文件呢？ 你肯定不能直接 首先你肯定不能直接 open 这个 a.txt， 然后带一个 O_TRUNC。 如果你用 O_TRUNC 打开它，在 open 的那一瞬间， 你，你那个文件内容就没有了。 对吧？它已经就，tra-打-用 O_TRUNC 打开的时候，这个文件就直接没有了。然后这时候假设你的程序，程序被杀掉了，进程被别人杀掉了，你这个文件也就没了。 对吧？文件没了。所以，所以你肯定不能用 O_TRUNC 去打开一个文件来保存它。所以你会怎么样呢？你会先 open 一个 a.txt.tmp。 然后，你会写入这个，写入这个新的 新的数据。好。 然后一旦写入新的数据以后呢，就可以 close。 OK，close 以后，你就觉得，哦，我已经把临时数据写进去了，我已经安全了。 然后， 好，你说你 remo-unlink a.txt。 好，unlink 完了以后，你，你在 rename a.txt.tmp 对吧？然后到 a.txt。 哎，这个实际上是不行的。 为什么呢？ 因为我们的操作系统里面做的是 metadata journaling。 它对 这个东西不负责。 也就是说，他在写 journal 的时候， 说，好， 呃，这个文件创建了，对吧？这个有一个 O_CREAT，其实隐瞒了一个。 说我现在的要创建这个文件。 文件创建了，好，这个文件要创建，所以我知道了，在这是个 metadata，对吧？在目录里面要创建这个文件，这是一个 metadata。然后， 要删除一个文件， 这个也是目录里的操作，也会被 journal，也会被放到刚才那个日志区里。然后这个时候发生了 system crash。 Data 是不 journal 的。 所以， crash 完了以后， 系统 redo log 里看到什么呢？哦，我知道了。第一个，我要创建一个 空的 a.txt.tmp。 第二个，我要把 a.txt 给删掉。 好， 删完了以后 a.txt 没了。 你回来以后，你重启回来以后，你，你的那个保存文件他就， 呃，呵，它就它就没有了。 实际上，这个现象是非常普遍的。就打个比方， 呃，比如说 Git。 Git 是不管 文件系统的一致性的。 因为它是 append-only 的 data structure。 它只要确保所有东西都，就它所有的 object 都在。所以理论上说，如果你的文件系统 leave 了 inconsistency， 它还是可以把 把 Git 回到一个过去的 正、一致的状态上。因为所有的 commit object、tree object 都是不会丢的，它都在文件系统里。所以你扫描一遍，你总是可以恢复到 就你没有如何东西可以丢掉。 所以 Git 就说，好，那 他绝对不会 管，他为了性能他就不管这个 crash consistency。然后这个就带来了一个很有意思的后果。 呃，我们很长时间 OS lab 都是在 呃，虚拟机里做的。 然后在虚拟机里做的时候意味着什么呢？意味着同学们啊在在编完程以后，比如说今天编程然后 make，编译通过，然后 make submit。然后在 make 的时候我会做一次 git commit，对吧？你们知道我会做 git commit。然后 commit 完了以后就就反正文件系统里面都写进去了，就执行了一大堆这样的文件和目录的操作。 然后，然后 make submit 交上去，一看 OJ 过了，或者交上去了， 好， 他马上就立即点击关闭虚拟机。 然后关闭虚拟机的时候有几种选项，一个是安全的关闭，一种是快速的关闭。那对于同学们来说，我们一定是选择最快的方式关闭它。 然后快， 快速关闭意味着直接给那个虚拟机一个 power off， 直接那个，相当于拔线。 线拔了。拔了以后，你看， 我们的文件系统是有日志的。 那个 journaling block device 的哪怕是 metadata 可能都还没 replay。 对吧？journal 的一部分。然后这个时候 Git 就会进入一个不一致的状态，因为有些数据就不对了，对吧？data 没有了。 Git 就会进入一个不一致的状态，你需要重新扫描它。 然后所以， 呃， 对，所以 我们在用户空间是可以控制这个行为的。 用户空间为了实现，对吧？如果你想要在用户空间实现 crash consistency， 有一个叫 sync 系列的系统调用。 呃， 有一个 sync 命令。 如果你们去看 呃，我们的 Makefile 的话，在你的 git commit 以后， 我会加一个这个 sync。 做一个全局的文件系统的同步，确保数据都落盘。所以， 我们那个 make 会比较慢。 尤其是如果你们也像我一样是在一个树莓派这样的比较慢的存储设备上，你会明显感到一个肉眼可见的延时。 这就是我为了实现 crash consistency 说， 我告诉操作系统说 sync 说，请你把所有东西都落盘，对吧？请把所有东西落盘，确保数据的一致性。 然后这是一个 family of system calls。 你也可以带一个文件描述符。 比如说有 syncfs 可以可以去同步这个文件描述符所在的整个文件系统。 也可以就同步这个文件 的 data 和 metadata，就这一个文件描述符。 甚至 还有一个非常有用的是系统叫 fdatasync(fd)。 它同步文件的 data， 并且只有它的 metadata 变了 只有 metadata 变了， 这个 metadata 才会，才会同步。 这个是很有用的，为什么呢？ 你如果要在磁盘上面实现一个数据库， 你很有可能会分配一个固定大小的，比如说对吧？你有一个 4GB 的 database。 那这个 4GB 的 database 所有的空间都已经被分配出去了。也就是说它每任何一个 block 都已经映射到了磁盘上的一个，一个 block 了。 然后这个时候，你对它的任何修改， 实际上都不会改变这个数据结构的映射关系，只有数据 和那个 modification time。 然后但是 fdatasync， fdatasync 它会更新这个 data，就是如果你写了一个，比如在这写了一个 1 2 3， 它会确保这个 1 2 3 落盘。 但是它不会去动那个 inode。 因为动 inode 的时候，你你就要又多涉及到一个 呃，一个块的写入，对吧？它不去动那个 inode，因为，呃，因为它的 size 和它的这个索引都没有变。 一个固定大小的，大小的文件，所以你做 fdatasync 的时候这是没有 就是没有额外的 inode 上面落盘的，呃，开销的。所以你你如果你要在应用程序里面，比如说你要实现一个数据库，你要做个这个 redo log 的话，你一般来说，呃，用 fdatasync 的性能是 性能是最好的。 对。 然后， 一个有趣的故事。 如果你们 都不知道 应该怎么保存文件， 说明地球上的程序员应该都不知道应该怎么保存文件。 对吧？你们知道正确的方式是什么？ 有，有同学知道正确的，就怎么样保存这个文件是安全的？ 首先你可以再再这里面加，首先你可以加 sync，对吧？可以加 sync family 的系统调用。哪怕是 fdatasync，你把这个数据，确保这个数据写进去，然后再执行这个 后面的操作都是可以的。 实际上， 正确的方式是， 只留一个 rename 就可以了。 因为 rename 能保证它是，它能保证 atomicity。 rename 的系统调用会保证你换过去要么要么他就换过去了， 要么他就完全没有换。 它是有这个 atomicity 保证的。 呃，这是 Linux 的设计。 但是开发者怎么可能知道这一点呢？所以我，我当时我在读 PhD 的时候我就想，除了真正好的 systems programmer 写的软件之外， 所有的业余程序员写的软件都应该有 crash consistency 的问题。然后答案就是 yes，当时我们找到了包括，呃， 就因为很多是 legacy 就老得很老的系统，它它在那个设计时候根本就没有 crash consistency 的概念。 呃，像 GNU 的 make， make 可能会 leave 一个 incorrect 的 build。 然后 sort，sort 如果你 in-place 的 sort 的话，那个文件可能会丢掉。然后那个 Perl，呃，Perl 也是。Perl，Perl 本身它是支持那种像 sed 一样做正则表达式替换的。然后如果他这专门有个 -i 那 in-place 的选项，然后那个也会把你的文件整个给丢掉。然后

甚至我们发现的是，2016年那个时候VS Code刚开始，刚开始有。这个整个JS的世界，Node.js的世界 没，不存在一个API可以安全的保存文件。 也就是所有的Node.js的、Electron的应用全部没有，就全部都会有crash-consistency问题。 然后那个那个时候那个GitHub还是Atom嘛，那个Atom现在已经死了。然后Atom哎有人在那个GitHub issue上面说，呃说我要保存一个很大的文件， 然后这个时候磁盘空间满了，然后呢我的文件就消失了。然后他就， 对吧，非常frustrated。 呃很有意思。 所以操作系统课我觉得就到底教会了 我们什么？ 对吧，比如我今天讲了呃日志，讲了crash-consistency，你们有个概念。然后你们学了一组API，fsync, fdatasync，肯定是留下了一些印象，然后下次你知道，哦，要处理这些数据的时候要非常的 小心。 但其实我真正教你们的是，就我反复的，比如说我我我要开一个trace啊，我要wrap code一个什么东西啊， 你们 真正应该相信的是 所有的什么东西都是可以做的。 所以我昨天就让 我的agent，我把我那个paper，2016年，10年， 对，10年以前的paper，呃丢，直接把paper丢给大语言模型， 然后让它帮我实现一个crash-consistency的 checker。当然是一个简化版的用户态的版本，你看，有将近1000行的 代码。反正是修了几轮基本上就， 就对了。 比如说， 呃我可以展示一下这个sort这个命令的 对吧，比如说我可以sort a.txt。 然后但是，我sort又是，它sort又是支持in-place的。 sort本身是支持in-place的。 就是它是可以 呃有些有些程序是不支持的，因为你如果用O_TRUNC打开它的话，那它直接挂了。 但sort是支持的。 但支持的时候它是不支持crash-consistency的。所以我可以 呃需要重新搞吗？ 好。 我可以去呃观测 这个sort保存文件的过程。 它执行的所有系统调用，和文件相关的系统调用。然后我可以生成很多模拟出来的 可能的crash的 现场。 然后我可以让大语言模型直接帮我 去解读这个result.json，然后它就可以复现。相当于我是让大语言模型直接 直接复现了呃我当当时那个论文里面的研究结果，所以我们的paper是可以是reproducible的。但 呃然后我就发现了一个 洗paper的流量密码，对吧？你可以找到一个还不错的paper， 然后你用你的token和超能力 去扩大100倍的实验规模。 因为理论上说因为有现在有LLM了，你可以 对吧，你你你可以去世界上所有的开源软件里面都做一遍这个实验。 然后而且你可以用LLM去替代所有的heuristics，我那个时候没有LLM。所以所以很多时候我需要依赖heuristics来判断这个 呃到底有没有这个crash-consistency的 呃这个 问题。所以你看这个AI已经 它知道这个sort会在截断以后发生一个崩溃。 然后它在写入中断的时候也会发生部分的数据的 丢失。它能够 它能够正确的找到这个呃这个bug。然后当然coreutils为了portability它不打算修修这个。呃Perl最后修了这个bug。我们完全可以用LLM替代heuristics，然后去洗一个，就做一个非常好的实验。但是， 但是大家不要这样做。你们你们学操作系统，你们要去干那个 呃对吧，地球上没有人做过的事情。因为现在有有你有知识了，然后你有模型了，你就可以去做世界上 没有人做过的事情。