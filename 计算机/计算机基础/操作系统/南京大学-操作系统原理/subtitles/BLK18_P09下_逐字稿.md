对吧，你可以试图去引用它的member。虽然，虽然这个这个指针只是把一个指针挪过去，它不会实际地dereference，不会解引用这个指针，然后我再把这个东西的地址给取出来，然后我再把它转换成整数，就是y相比于对象头的offset。对吧，因为我相当于0，在0的位置有一个这个对象，然后我把它的地址取出来就是它的，它的offset。当然，呃编译器有一些，可能有一些这种builtin的方式可以更好地，呃更好地实现它。呃但，但这个，这个操作在C语言里面是完全，完全合法的。呃里面有很多这样有意思的宏，你们呃有兴趣的时候，在课后的时候，比如说我像你们这么大的时候，我就喜欢呃看这些稀奇古怪的东西，然后有些东西记住了，有些东西记不住，但是久而久之呃就，就有一些概念和和和积淀。呃这个freestanding环境里面提供的各种库函数里面，还有一个非常特别的库函数，叫做呃 standard standard-argument.h。这个是用来处理呃变参数的。我看看我有没有，有没有代码啊。我们在printf的时候，对吧，你看我这里可以printf，我我我实际上可以写任何多个参数，1 2 3 4 5 6 7 8的。对吧？你们在上计算机系统基础的时候，有没有讲过这个这个机制是怎么实现的？在32位的机器上还呃比较简单，因为32位的calling convention是所有的参数都往往往栈里压。32位是如果你要call，你要call一个呃call一个函数的话，它会把它会把所有的参数都都放到栈上，然后这样栈上就会有一个数组。它有一个数组。啊比如这是第一个参数，这是第二个参数，等等等等的。所以，所所以在x86上你只需要给第一个参数取一个指针，取一个取一个引取一个地址，比如说呃假设啊如果它是int printf (const char *fmt, ...)，对吧，如果我我这样写。那呃我我如果有正确类型的arguments就是对吧，我只要把第一个参数的地址取出来就行了。但是，但是，在现代的体系结构上，这个hack是不管用的。不管用的原因是，我们的参数大部分是由寄存器传递的，函数调用的时候，对吧？rdi，rsi，然后这个参数所有的参数也都不在内存里。我们的标准库会生成正确的汇编代码来，呃至少包括和编译器一起协作，把你在变参数的情况下把需要的东西放到内存里，然后使得我们可以正确地呃用一套宏，那个va_list、va_arg这些宏来访问呃多个参数。然后除此之外，你们在freestanding的环境上面也很容易实现一些随手可以当做练习的函数，比如像memcpy、memmove、strcpy，这些我想都是大家在刚开始学C或者C++的时候都做过的呃想客户习题这样子的东西，比如说像rand。呃rand其实并不随机。呃C的rand你你需要给一个rand的seed。给一个rand seed，然后它如果你rand seed给的相同，它每一次运行程序生成的随机数序列又完全是相同的，所以这完全就是一个数学上的呃函数，一个数学上的呃随机数生成器，pseudorandom generator。然后还有这种atoi对吧，呃可以把整整数和字符串之间呃互相转换，以及呃这个qsort。有没有人用过QC语言的qsort吗？谁用谁知道，一点也不好用。你看它那个函数的参数，因为这就是难为C语言了，C语言是一个高级的汇编语言，它想要呃就如果你在在写实现真正程序的时候想要呃做quicksort，你一般而言会做一个结构体。对吧，你可以，呃如果你想要排序什么东西的话，你可以你可以这样。然后你这个element里面可以有一个呃compare的function。你可以在这个element里面带一个compare的function，当然你你这里就可以定义任何你想，想要的东西。你就不会需要，就你可以你总是可以写出来很好的代码。但是，C语言给我们提供的语法的机制很有限，如果它想要实现，在标准库里面实现一个通用的quicksort的话，那只能干这件事。所以你几乎不会用，呃反正这个这个有有一点难用。呃不是说绝对用不起来，但是但是确实是是你最后写成那个代码的可读性就会比像C++就要差一点，比比Python就更呃更差一点。然后再再比如说，呃math，数学库。甚至数学库像什么sin、cos这些你也也都，你也都是可以可以实现的。但数学库的水也很深。比如说，有一个数满足既不是a大于a，也不是a小于a，也不是a等于a，这世界上存在这样的数吗？当然在整数域和能够合法的表示的数域里面肯定是，肯定是不存在的对吧？因为呃比如说我1，或者一个IEEE 754能够合法表示的浮点数，那它就应该，应该自己等于自己嘛，对吧？1就等于1，2就等于2。那什么东西自己不等于自己呢？数学里面专门有一个Not a Number。所以浮点数，就数学，整个数学也是呃也是一个非常大的大的坑。我刚才的那个代码示例里面有。我Not a Number，我可以定义三个变量x, y, z，呃然后我也可以把它打印出来，然后我可以确定确实是看到呃既不是a大于a，也不是a小于a，也不是a等于a，我可以运行一下啊。我可以运行Not a Number，然后你你确实看到它它等于1。行，呃这是呃这是各种各样的的的库函数，那我们也不用多说了对吧？你可以用循环来实现memcpy、memmove、呃strcpy这些呃这些这些操作。在在这个指令集和计算的封装上，还有一个非常非常特殊的呃函数。我我今天会会着重讲一讲。你们还记得有一个叫叫simple C的模拟器吗？我们第一，刚开始上课的时候我们就说，好，呃C语言的程序，它是，它执行的时候是有状态的。然后我举一个例子是那个非递归的Tower of Hanoi，然后我相信大家在看过那个代码示例以后，应该在座的同学都会写一个呃非递归的Tower of Hanoi了。然后你还记得C，我们说simple C，simple C，simple C的计算的模型是，我们有一个stack frame，就有一个一个一个的栈帧，对吧？假假设比如说这是，在最，最底下是main函数的调用，然后你可以调用f1，f2，对吧？你可以随随便的函数调用。然后那每一个每一个栈帧上面都有一个自己的好像是PC或者是一个当前执行到哪里的位置。然后所谓的函数调用就是在这里在在上面再放一个新的栈帧，然后在呃所谓的函数返回就是把top frame的这个给pop出来。然后每次执行任何语句都是从，对吧？从这个top frame，从这个top framePC取指令执行，取一条语句，应该这么说。到它的下一个状态。好，这个。然后setjmp和longjmp，就做了一个非常有趣的呃有趣的hack。呃这个hack是hack是这样的。它把setjmp和longjmp的语义稍微改了一改。它是这样的。setjmp允许你放一个buffer，当然我现在可以说好buffer是一个全局变量。对吧？当然如果我有一个全局变量x，x也是在这在这边，那我也可以有这个指针，对吧？我在我在执行的时候我可以访问这些这些全局变量。好，我现在在全局的地方有一个buffer。然后我现在如果执行一条setjmp，然后它就会在buffer里面记住当前栈帧栈的高度，比如说高度h等于3，打比方。3和PC都会记下来。在我执行完setjmp以后，setjmp会正常返回。就setjmp我说setjmp以后，setjmp就执行完了，它就返回，我就可以继续继续执行随便什么代码。但是我会悄悄地把这个栈的深度给记下来。在这里我只要记3就可以了。在计算的意义上这是可以办到的，但是在C里面其实办不到的。就如果你只用纯粹的C是办不到这件事的，因为你不能不知你用纯C你是不能知道底下有多少，你到底有多少函数调用的。但是你你实际上你是可以的。啊当然这是概念上，这概念上是记住的。实际上它只需要一个指针大概只要一个指针指向这个这个栈帧的这个位置就可以了。好，然后接下来你的程序可以任意执行。你你的程序可以任意地往后执行，比如说你可以调用100个函数、1万个函数。但是当你执行longjmp，当你有一天执行longjmp这个buffer的时候，它会一下子把所有的你栈上面的，不管有几百层、几千层所有的东西都弹掉。就像一个大的goto。你们知道在C里面的goto，C里的goto是可以在函数内随便跳转的。对吧？我我哪怕是在一个循环里，或者是在一个分支里，在这是一个随便的分支结构里，我都可以goto到外面。然后这也是一个C语言里面非常经典的呃一个好的用法。就比如说你你的程序，你的这个C函数很大，里面有分支有循环，然后你在循环和分支的时候都可能会申请内存、申请资源。那你你如果你在for循环里面不小心return了，你这个资源就漏了，忘记释放了。你们每个人都干过这样的事。C跟C++太容易做出犯这样的错误了。所以C里面就有一种非常idiomatic写法是呃你你在在你比如说你循环里面你要出去了，你就goto out，或者goto一个叫release。然后你在release的时候会检查你所有可能申请资源的点，然后把它们逐个释放。看起来不是最效率最高的，因为你可能会有一些额外的没有，不必要的检查。但是呃这个比较安全，对吧？你可以goto。然后setjmp和longjmp就是个，longjmp就是个大号的goto。setjmp就是把label，相当于label写在这个buffer里。它包括了当前栈的深度。对吧？然后所以set

setjmp。如果setjmp之后 你有了好深好深好深的栈， 你只要执行一个longjmp， 执行完了以后，它就会回到这个位置，回到这个位置，然后回到回到它记下来的PC。 一个setjmp和longjmp。然后这个这个函数非常有用，我们会在 会在有一个实验里面让你们用它来实现 实现实现协程。我这里也有一个代码的例子。 这个函数非常非常有趣。你你甚至啊你甚至不需要这个系统调用，它它不需要任何的系统调用，只要编译器配合它生成正确的指令序列 就 就可以。好，然后在这里面我有一个小的演示程序。 我会先执行 setjmp。 然后如果return value等于0， setjmp，我就会继续往后执行，比如说我会我会往后执行 呃 first。 然后往后执行first以后，我再会执行second。然后这时候你看我的函数调用已经一层一层一层累积起来了。 累积得起来的时候，我会在second里面执行longjmp。 然后这个longjmp就会跳转到我刚才setjmp这个地方，会跳回main。 然后我们可以调试一下，看一下是不是真的是。 setjmp 看到确实是setjmp，然后first，second，最后返回。然后我要带大家调试一下。 我我第一次我第一次setjmp， 它返回了 0， 如期返回了0，所以这个程序setjmp就好像什么也没有做过一样，它就继续往后执行。 哎，它打印出来了一个setjmp return 0。好，first。 好，那我就进到first里。 好。 好，我printf打印出来。好，我进second。 好。 我进second。你看这个时候我如果看函数调用栈的话， 我我我现在在second里，second里然后我到first里，然后到到main里。 呃三层的函数调用栈。但如果我直接next的话， longjmp就直接跳回来了。 跳到了相当于我在 我在这个程序执行的时候，我可以在这个地方做一个标记， 对吧？我今天我我要去上课了， 呃我我在出宿我在宿舍上我先setjmp一个， 然后我今天先到教学楼上课，上完以后去逸夫楼上课，上完以后去食堂吃饭，吃完饭以后直接longjmp你就回到宿舍了。 不管你，对吧？你 你你在这段时间里面干了什么，你longjmp直接就把 你的历史全部清掉，回到回到这个地方，但这个时候你你可以带一个 你可以把这个return value 传递回来。 呃这个return value它返回 返回main，对，然后就看到 这个特性。 你可以实现，对吧？就是因为C语言里面它的执行规则是 就就这么简单。它每每次都是取栈top-most frame的一条语句执行，所以你理论上说只要记记在标记一个栈的位置和一个PC， set setjmp就可以实现。当然在实际实现的时候，它还会保存一些寄存器啊， 呃这样的 呃 使得它和编译器一起能够 正确的 呃跳转回来，但是这个细节大家不需要不需要知道，你只需要知道我确实是可以通过一个longjmp跳回来就可以。 那么除了这些freestanding里面都能用的这些最基础的指令集体系结构和计算的封装以外，libc的一个很重要的 呃功能就是封装文件描述符。 在我们的struct FILE里面 呃其实是包含了一个文件描述符的，你们会看到 C语言里的 这些stdio里面的API 和Unix文件 操作的API对应几乎有一个一一对应的关系。 对吧？我用一个 我用一个你们用一个fopen可以打开一个FILE*， 一个struct FILE， 然后你用open可以返回一个文件描述符。 然后你在在C上面你也可以做fseek， 对吧？可以做ftell， 做feof。 然后这些其实都对应了文件描述符。你只要持有那个文件描述符，你就可以对它做什么lseek 这样的操作，对吧？ lseek也可以用来实现ftell。 呃等等。 所以你看到的 这个 呃 你看到的stdio上面的 这个代码就是文件描述符的封装。我们其实可以去看那个musl libc里面的 musl libc里面的struct FILE的实现就比glibc要干净得多。 这你们自己就可以理解了。 里面最重要的两个你看有一个fd，有一个pipefd， 还有一个pipe pid， 对吧？它里面包含了一个 文件描述符， 然后甚至有一个这样的pid，这都是和操作系统相关的。 通过这两个 整数，我们就可以访问到操作系统里面的对象。然后除此之外绝大部分东西，像比如说你看这里有一个buffer， 对吧，有一个buffer。你知道比如说我们的stdio是可以带buffer的，那buffer就是这个指针指针指向的。 呃 有一个有一个buffer。 呃然后以及 它用了C语言里实现的面相对象。 这里有一个函数指针，一个read，一个write，一个seek。就跟我刚才说我如果我要自己定义一个 在我自己的项目里面定义一个struct element，并且我要支持comparison的话，我要比较的话，我一定会用这种面向对象的方式。 呃对吧，read，write，seek，close。 呃 这样我一个IOFILE它的一一个IOFILE它就可以对应多个不同的后端的实现，比如说一个操作系统里面的文件，可能是一个就是一个buffer对吧？ 一个 呃像sprintf的时候，你可以往一个buffer里面打印，你可以相应地为那个一个字符串的buffer来实现了个对应的struct IOFILE，你只要把 close，read，write，seek这些函数给 呃相应的 呃实现好就行。 呃这里面能看到所有的 呃内部的内部的实现。那既然呃struct FILE里面 有一个 呃文件描述符，那么我们基本上就把所有的这个C C libc里面的像什么fopen啊，呃f 这个fread啊这些都可以相应地转译成 呃对那个文件描述符的 呃系统调用。 然后我们的printf的 呃 family也是，呃它实现的 如果大家对底层的库函数有兴趣的话，呃真的可以自己去 呃 编译一份带调试信息的musl libc，然后用gdb去调试它。 呃调试完以后你可以看到这个vfprintf真的就和你们 构想就脑子里面你会想，诶，如果我去实现一个printf会怎么样呢？ 你会实现一个小的一个parser， 你有你得到了一个这个 呃 字符串对吧？一个format format string，format string。然后这个format string里面有什么百分号d啊，百分号s啊，然后你就会 呃 你就会去从左到右遍历这个format string， 解析出你要打印的东西，然后再把它路由到一个相应的比如说百分号d的函数，百分号s的函数，然后都是往这个文件里面 呃 FILE里面写入。 然后你可以看到的是printf， 它会一级一级一级调到 f，所以printf实际上是 是fprintf一个stdout的什么什么。 对吧？printf， 它是 它最终会调用到fprintf的stdout，然后它就用了 用了我们的 呃 stdarg.h里面的这个va_list，它会把 它会把这个va_list它可以把参数列表， 因为这个参数列表它本质上是一段内存嘛，是是一段内存。然后这段内存 呃它可以可以从第一个参数一个一个一个往后走，然后它就会在printf调用的时候去把这个参数列表给 呃取出来，相当于是是用一个对象得到它，然后把这个对象不停地传递传递传递传递传递。 然后然后你会看到这里它是怎么样怎么样一个一个处理的。它就是一个死循环，然后一个parser不停地往后往后往后。如果你调试它的话会 呃 会学到很多C语言该怎么写。虽然我觉得可能现在去学C语言该怎么写也 呃也不是那么也不是那么必要了，但你至少可以看到一些有趣的设计。 你的printf family， printf， sprintf， fprintf， 它是没有code clone的， 只有一份代码实现。 就在这个printf core，就实际负责打印的只有一份就是printf core。 呃只有一份代码。这样减少了代码复用以后 呃你的 呃 你的项目维护就就会就会容易得多。 对。 然后我们的libc里面还封装了很多和进程管理相关 的 呃 功能， 呃 包括 呃 包括abort， exit和atexit。 这几个函数我之前也用过对吧？我们可以用atexit注册一个exit handler。 也就是说我希望程序在正常退出的时候，呃我还希望再调用一个我自己的函数。比如说 你可能会在你的C程序里面创建一个临时文件， 然后这个临时文件 当然你我知道你们做Online Judge做得多，所有的资源都是只管分配不管回收的，因为当你点击提交按钮以后， 什么事情都不是你的了。 Online Judge会负负责回收一切。但在实际的系统中其实不完全是这样的，对吧？你不管是是创在哪里创建一个临时文件，如果它真的不你你真的就不用了，你最好还是把它删掉， 哪怕你是在什么/tmp目录底下。 对吧？你你这样想， 呃如果你每次都会创建一个很大的临时文件， 然后你的这个工具会被调用很多很多次， 你你用着用着不就把它的 存存储空间给占满了嘛。 对吧？/tmp /tmp的空间是有限的。 你把它占满了以后，这个管理员 管理员在执行别的东西的时候他没空间了，他再会看，哦，产生了这么多 临时文件，不必要的临时文件。 呃所以atexit就是帮你干干这件事。当然是在正常退出的时候，如果异常退出，偶尔留一两个可能 呃 他也选择忍忍吧，对，忍，或者说 呃足够老的文件他就

它就删掉了。
OK。
但我们的
进程管理里面还有一个退出方法叫 abort。
abort 的行为是给自己发送 SIGABRT，它
因为是发一个信号，就像 SIGINT 一样。上次说我们 Ctrl+C，
我们的终端会给
会给你的前台的进程组里面每一个进程都发一个 SIGINT。
然后这样它就会触发一个 core dump。
也就是说，你当你 assert fail 的时候，
assert 会调用这个 abort。
这个行为大家知道吗？assert 终止调用的不是 exit。
它不是调用的一个 exit 1
告诉你一个错误。
然后你 assert fail 的时候，你看到那个 exit status 就是如果你看那个程序的返回值的话，它也是一百多，
是一个负数。在补码的意义上是一个负数。
负数，
它收到了一个 SIGABRT 的信号。然后这样如果你配置了 core dump 的 size 的话，
你就会会产生一个叫核心转储文件，它会把内存
会把这个进程的内存 dump 下来。这样你还可以用调试器
去去去调试它。
然后 core dump 这个词是怎么来的呢？core dump 是来自于以前的那个 core
core memory。
就是那种早期的那种磁芯内存。我我应该是提过一嘴的，你可以用一个
磁铁的方向来表示 0 或者 1。
然后就 core core memory。然后你需要把这个 core memory 的状态
转储到另外一个
永久的存储器上，所以这就叫 core dump。
core dump。然后 core dump 现在就用来讲进程内存，把进程内存放到一个文件里面的
过程。
然后我们的 libc 也也提供了非常有限的
和其它进程协同的
功能。比如说 system，system 你们用的最多。
system 也是最好用。
这 system 对你们来说就是最好用的那个
你想要干点别的，对吧？比如说你想，你的程序想要清屏，
想要清除屏幕。
你们你们知道，你们学过，用一个正确的 ANSI escape code 就可以清除屏幕。当然现在你可以直接让 AI，对吧？你在 Cursor 里面
直接
在这个时候我觉得 Cursor 还是比较有
还是有点用处的。你可以
可以在在在光标停在那个时候，你就帮我在这里方做一件什么事，
对吧？它就可以马上帮你
帮你了。
但在你
可能你真的，比如说手边没有 AI 的时候就直接 system("clear")，
对吧？然后它也可以帮你搞定。你只要知道
你用你用 sprintf 打印一个什么样的命令的字符串，然后你执行它，
还是挺方便的。
system 背后会启动一个 shell，会在 shell 里面执行
执行你的命令。所以它相对来说会重一些，但是
确实比较好用。然后你也可以创建一个 popen 和 pclose。
我们可以看看手册啊。
这个是
是标准库的 library function manual 的一部分。你看是是属于 standard C library 的，
libc。
然后它有有 popen 和 pclose，它会返回一个 FILE*。
它返回一个文件。
然后这个文件当然
popen 和 pclose 有一个缺点是它是单向的，它是单向的。你你你只有只有一个写，
一个读。
不像是你们在 Python 里面可以 subprocess.Popen，你可以可以两端
两两端可以互相读写，两个程序可以互相读写。但这个这个管道是单向的，因为你的 pipe 可以返回两个文件描述符，
但是 C 语言的 FILE* 里面只有一个文件描述符。
然后它如果它也要返回两个 FILE* 的话，这个 API 就会难看的不成样子。所以最终
API 的设计者选择了一个
比较我觉得是比较糟糕，有历史包袱的
妥协。它它的这个管道
管道是单向的。但单向的管道大部分时候也也挺有用了，如果你要调用另外一个程序，并且把它的
结果结果得到的话，它会比 system
更好用。因为 system 你没有办法
你没有办法控制 system 的输出，对。
以及
所有的系统调用，所有的系统调用你封装以后，它都会
返回错误。比如说，比如说，
你们有没有留意过
每一个系统调用 API 的手册里
都有一个叫 ERRORS，
它会告诉你
系统调用到底为什么出错了。
比如说你看这个 return value，return value。
他说
在 fork 成功的时候，
PID
如果是父进程，它会返回子进程的 PID。如果是子进程，它会返回 0。
还有一种情况，它会返回 -1。
-1 就代表失败了。
但是
-1 的失败
有很多种可能性。
比如说在这里面，fork 的
fork 的失败的可能性还相对来比来讲比较少。比如说有一个 EAGAIN，他说你现在暂时可能不行，你下一次可以再试一次。
然后比如说有 ENOMEM，比如说确实你
fork bomb 的时候它会返回这个
ENOMEM。
如果是 EAGAIN，很多系统调用都是它可能会暂时性的失败，
你现在失败了，过一会儿可能就会成功，它就会返回一个
EAGAIN。
但如果比如说我们
我们再看一个别的，比如 open。
如果你到 open 去找 ERRORS 的话，那它的错误可能就多了。
有那么多种可能的错误情况。然后有每一个
对吧？
每一个都有一个编号。
有一个编号。比如说最常见的是 EACCES。
EACCES。这个 EACCES
错误说的是 the requested access to the file is not allowed,
or permission is denied，或者是
别的等等等等。
还有比如说像什么 EEXIST，
对吧？如果如果这个
pass 已经存在了，并且你用了 O_CREAT
的时候，它会它会给你一个
一个错误，等等等等，有好多好多的
错误类型。
每一个系统调用的错误
在 libc 里面也都必须要得到非常好的处理。
不像你们写程序的时候会假设，就我觉得
你们做 OJ 的一个一个问题
一个问题就是它从来不让你们处理错误。
因为错误是 OJ 的事，或者 OJ 会告诉你们你错了，然后你
你的程序 100% 都必须是必须是对的。但是在实际中其实是是是是不客避免会有这种情况的，因为
我用户的输入是错的，对吧？我 gcc 一个 non-exist 的程序，
那它必须要给我一个好的
错误提示。
然后比如说，比如说，
我今今天会讲到这个。
比如说，
ls 一个 non-exist 的时候，ls 会告诉我，说不能访问
non-exist，
是没有 such file or directory。
但我可以做一件什么事呢？我可以
我可以加一个环境变量叫 LANGUAGE，
比如说我 non-exist，
完全变了。
ls 的行为是完全一样的。
这解释了为什么有的同学
你们打你们看到的那个 error message 是中文，我的 error message 是
是英文。
更有意思的是
这其实是同一个 ls 程序。
然后他们
访问的是同一个系统调用。
同一组系统调用。
对吧？那 ls 试图去 open 这个
open 这个这个目录，然后这个 open 这个失败了。那下一个问题，
为什么 ls 可以区分出，
对吧，为什么 ls 可以区分出
不同的语言？
谁的行为？
答案就是 libc。
libc。所以所以在这一，
我们的 libc 里面有有一个函数
叫 perror，perror。这个这个这个函数你们是不是在操作系统的课上第一次学？
你们之前的课程应该教过这个函数吗？
所有的 C 语言里面的 libc 这注意，
注意这是 standard C library。
这是一个老师应该教你们的东西。在所有 C library 里面都可能会失败，
然后在失败的时候，有的错误是你要容忍的。比如说你你你你你试图打开三个文件，
如果三个第一个文件不存在，你要打开第二个，第二个打开第三个，如果都不存在，你程序就直接退出，那你也可以不打任何的 error message。
但是如果你像是比如说像你要做一个给别人用的工具，像刚我那种 ls，
ls、gcc 这样的时候，当一个文件不存在的时候，你需要把这个 error message 打印给用户的时候，你就应该用这个 perror。
然后这个 perror
perror 它会打印一个 system error message，它首先它会打印你的这个字符，
你你的这个
你你想要打出来的提示。同时它会得到上一个系统调用或者上一个库函数调用错误的那个原因，并且把那个错误的原因的字符串给打印出来。
然后这个字符串打印的过程，libc 里面的代码会根据当前的
语言环境的配置
来输出一个
友好的 message。然后 perror 的时候，你看这个 ls，
你 perror 的时候 ls:
然后剩下的是由 perror 这个库打印打印出来的。
哪一个什么东西
不存在，
没有那个文件或者目录。OK。
这里面有很多有意思的机制。比如说有一个有一个变量叫 error number，
有个变量叫 error number。然后在 C 里面就可以访问那个 error
number 的
变量的。然后然后那个 error number 就对应了相应的错误的错错误的类型。
对，所以我们实际上我们如果用 perror 的话，
我们如果用自己的命令，就自己实现一个实实现一个工具，你也可以实现
正确的，
用正确的语言输出错误信息。
这是一个这是一个 portable 的实现。你也是一个好习惯好习惯的
实现。
好，这是
系统调用的封装。
然后以及以及
我们的 libc 还封装了进程的
运行环境。刚才那个例子。刚才那个例子我们可以通过一个环境变量，LANGUAGE 的环境变量来影响程序的行为。
然后
我们也可以看啊，这个
environ。
environ 的手册告诉我们，libc 里面有一个
变量，有个变量叫叫
environ。
然后通过这个变量你就可以得到一个 key-value

有的对吧，就是key等于value，key等于value，key等于value这样的一个环境变量。然后大家也都 host/都知道，我们在execve的系统调用里，我们是可以，可以给main函数传递 envp这个参数的，对吧？每 main函数我们有一个 程序的初始状态，一个C程序的初始状态， 除了argc, argv以外还有一个envp。 然后这个envp是在execve的时候指定的。 呃， 这是一个非常有趣的设计。呃大家应该读过这个手册吧，比如说 大家读过有一个系列的 用来execute a file的 系统调用，然后有一些是带这个e的， 有些是不带e的。 然后带e的这些函数它里面都有一个参数叫做envp。 然后在大部分时候你你不需要这个带e的，比如说你做execl 或者做这个 execvp的时候， 呃对吧，我可以执行，我可以执行一个路径对应的文件，然后我可以把参数argv传递进去。那这个 host/时候环境变量什么样呢？ 环境变量Unix做了这样的一个设计： 默认的时候子进程会继承父进程的环境变量。就在我调用刚才那些，虽然我execve必须指定一个环境变量，就是那个系统调用 呃底层的最底层的系统调用会 呃会是必须要由有完整的参数和环境变量，但是 你在用API，C，libc的API调用的时候，比如说刚才看到那几个API的时候，默认的时候子进程就会继承父进程的环境变量。这就会有一个什么样的效果呢？ 如果我这个进程设置了一个LANGUAGE， 这个LANGUAGE就会传递给我所有的子进程。 这样如果比如说我现在的这个logging/login的时候， 我我我现在登录以后，比如说我现在知道，知道这个用户的语言是什么的时候，我把当前用户的语言放到环境变量里， 那我后面生成的shell， shell再fork出来的shell，shell再fork出来的shell，shell再fork出来的应用程序，整个进程树里面所有的东西都带了这个LANGUAGE的环境变量。 只要你不改它。 然后这个环境变量它就会导致你你这个用户所有的看到都 都是英语。然后你login另外一个用户，他的配置是法语，他看到的就是法语error message。 就用这样一个简单的机制意想不到。 对吧，刚才我讲环境变量的时候，你们觉得哦，这就是一个操作系统传递参数的东西嘛。 但你没有想到说这个机制不断地在被使用。 所以Unix是一个千锤百炼的系统。它虽然有些机制是被滥用的，但是这些机制它微小的设计，对吧，环境变量的继承、 继承， 然后比如说这个进程树的机制、托孤的机制， 都是在反复淬炼当中最终收敛到一个大家都觉得也还算好用的 呃好用的状态。 对。好，那我们就我们有这个environ/environment的 呃这个符号，我们也可以试着 在内存里environ有一个有一个变量。但实际上我们这个这个environ这个变量和像stdout啊，stderr一样是由libc提供的。 然后libc提供的时候它只提供了这个符号，所以理论上说你可以用任意的方式来 来定义这个environ。当然正确的定义方法是这样定义的，对吧，这是它符合 这是符合它的类类型的。但比如说 libc还给我们一个这样的符号叫end。 end这个符号你可以定义成任何的类型。 我可以定义成这样的，它也是 它也是合法的。然后甚至，甚至刚才那个程序，你看我end等于NULL， end等于NULL我可以执行一下对吧。 end等于NULL，呃 environ。哎，这个程序正常， 正常终止，它没有没有crash。 哎，注意是这个啊，是这个。 end等于NULL。我我随便写了一个一个类型， 我也我我也也可以可以给它赋值。 当然， 这个函数 呃给environ正确的赋值了。 然后对这个程序其实也有一些非常有趣的问题。 首先， environ 我们说是libc提供的一个变量， 对吧，一个变量。 它 呃它是一个符号了，当然肯定是在进程的地址空间里，对吧？一个 在在进程被加载到内存里以后，它就一定有一个地址。 对吧，因为这这就是一个，这就是一个指针嘛对吧，一个 一定有一个地址，它是个变量，它一定有一个地址。 好。那这个地址 它是一个指针类型，对吧？它是一个char **，它是一个指针类型。它所以它是一个 它是一个八个字节的指针类型。 它在进程的地址空间里。 下一个问题是， 它是谁赋值的？ 你马上细/一想， 这事情就不对了。 因为 我们知道 呃根据ABI， 呃系 Application Binary Interface，在进程初始化或者说在进程reset以后， 进程 呃状态初始化以后，呃 参数啊、环境变量啊这些都是放在栈上的。 然后你也知道我们有地址空间随机化。 栈的地址是不确定的，或者说整个地址空间里面你那些所有的动态加载，除了那个binary指定我就要加载到那个固定位置的东西以外，所有东西的位置都是不确定的。 那不确定就意味着这个指针， 尤其是它每次运行的时候参数和环境都不一样， 这个指针每次的值也不一样。 对吧，如果你不放心的话，我还可以把这个代码改一下。 或者我取它的地址会发生什么。 对吧，我打印environ会发生什么？ 如果我打印*environ会发生什么？它们分别是什么意思？ 对吧，我可以 这些操作都是合法的啊。 我可以取environ的地址， 然后我得到了一个0x420108。 这个才是真正的这个8个bit的 呃8个byte的一个一个 一个这个environ所所所对应的那个那个地址。它是一个变量，它在数据区，应该是在数据区或者在B...应该是在bss区，对吧？bss区。 然后这个environ在在进程刚刚启动的时候应该是没有 对吧？ 如果我打印这个 如果我打我取这个environ的地址，你看到和刚才我在终端里面看到那个输出是一样的。我取， 取environ的地址， 哎， 确实是这个地址。 然后我取它以后它是一个这样的类型，是一个char ***。 但如果我现在打打印environ的话，它是在进程刚启动的那一刻， 这个地方放的是0。 这是对的，为什么呢？因为每一次，我们说每一次，每一次在进程加载到地址空间以后，你现在还不知道这个environ在哪。 所以其实啊，我们的libc干了很 很多的长/脏活和 呃和累活。 脏活累活，我可以在这儿打一个watchpoint。 呃OK，我可以在这儿打一个watchpoint。 哎！ 你就可以看到，实际上我们是我们的刚刚__init_libc这个函数， 可惜了我今天翻车了，这个这个源代码我都我没有 没没有准备好。呃这些都是在musl-libc里面，你可以看到、可以调到的 呃可以调到的代码。 然后这些代码，实际上它都是是C代码，我可以找到这个__init_libc。 在我们的musl-libc刚_start接管了，就接管了第一条指令以后， 接管了第一条指令以后，呃最终会调用到这里的__init_libc。然后我们的environ，环境变量那个environ就是由这段代码赋值的。 所以说，计算机系统里面没有任何的魔法，在C世界里面， 当我们说有了一个Simple-C的模型以后， Simple-C的模型以后，所有的一切都是围绕Simple-C的模型来的。 除了比如说在System V ABI里面规定了那个initial process stack，它初始的时候进程栈是长 长什么样的以外， 呃剩下的东西都是可以，就比如说你看这个 就在这个__init_libc的第一行，有一个__environ = envp。 然后就是刚才我在watchpoint的时候，其实就已经停到了，停到了这一句话上。停到了这一句话上， 我们的environ这个时候这个指针就有一个值指向了栈上面的 那个正确的 环境变量。 OK，这是这是关于环境变量的一些小的 呃小的补充。顺便 呃顺便我们就引入了一个概念叫C的runtime。 我们的终究程序还是从_start开始运行的。 然后这个_start也是libc的一部分。 _start是一段汇编的最小的代码， 然后那一段最小的代码就会跳到这个_start_c。 这这也是一段非常有意思的代码，对吧？他他说： 呃 我有一个long *p， 然后这个long *p就 我就从long *p上面读出了一个 argc。 p的第一个，对吧，long *p第一个就是argc，是参数的数量。 然后这个 argv就是p + 1。 然后如果你们去读那个System V ABI的手册，你会看到那个initial process stack就是长这个样子的。 他说的是：哦， 初始的时候栈是就是这样。所以这一小段汇编代码会得到一个这个，会找到一个正确指针，那是sp的指针， 一个stack pointer的指针。 然后 他会取出参数的个数和 所有的参数，然后再调到__libc_start_main。 然后这个__libc_start_main在 看到，刚才直接就调到了这个 __init_libc。然后环境变量是怎么算出来的？环境变量是用argv加argc加1算出来的。 如果你我回去对照那个手册的话，你会发现它和 它和手册上面描述画的那个initial process stack完全一模一样。 然后最后就到__init_libc，就到我们刚才看到那个代码，environ等于 呃等于envp。 所以真的是没有没有任何的 呃魔法。然然后你发现了一个东西叫C runtime，这个东西叫CRT。 刚才这个文件我再放大一点。 这个文件叫crt 1.c。 这个rt就是runtime。 然后这个C你如果你再去比如说让 呃大语言模型给你把整个编译的

的过程都打开，你还会看到一些其它的什么 crtbegin.o、crtend.o、crtn.o。然后这些都是和 runtime 相关的，也就是说，我们的 libc 不仅不仅，刚才你看看，我说 libc 提供了哪些，呃哪些功能。哪些功能？libc 提供了这个 free-standing 的环境，对吧？我可以 setjmp，可以 longjmp，可以可以知道整数有多长，可以怎么样打印一个整数。然后在这个基础上我们有 libc 呃系统调用的封装，文件描述符、进程，呃甚至错误的编号。然后最后最后，libc 还有一些必要的代码，对吧？一些必要的代码。然后这些必要代码可以在 ELF 的头上，因为 ELF 的头上面有一个相当 entry 对吧？你们在上计算机系统基础课的时候都都都做过这个，呃你们在计算机系统基础课上都做过这个操作，比如说用 readelf 去看看一个二进制的文件。然后在这个 ELF 的头上，对吧？你可以看到，我这是一个 amd64 的呃 ELF，entry point address 400 呃 168。这个是链接器最后填进去的。链接器会找到那个 _start 的符号，然后把把这个地址填到 ELF 的头里。然后这个代码就是我们的 c- crt1.o 里面的代码。所以 libc 里面就就这些东西。它有定义有宏定义，对吧？有有有那些一些宏的定义。然后宏的定义以外，它有用于最初始最初初始化的，包括比如设置这个 environment 的代码，包括那个尾巴上最后你结束的时候，你在 main 返回的时候，对吧？你看到这个 libc_start_main，它会调用 stage2。然后它会调用 libc_start_main stage2。你终于看到了这一行代码。也就是说如果要是你实现一个 libc，你会实现的是干这一件事。对吧？exit，然后你调用 main，调用 main 的 argc、argv、envp。在这之前的事情在这在这里。然后在这之后的事情在 exit 里。然后这个 exit 会调用 atexit 的 handler。你看，它会调用 functions on exit，调用 libc_exit_finish，然后最后 flush buffer，最后调用一个这个 _exit。这个 _exit 是一个系统调用，它调用了 syscall。当然这里假设的，你看 musl 假设是 Linux。啊所以这就是你看到的一个 C 语言程序的呃全部