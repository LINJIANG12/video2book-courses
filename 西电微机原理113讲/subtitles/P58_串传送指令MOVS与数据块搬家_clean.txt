上一节课我们讲到第三章 3.13 节 字符串操作指令。而且已经讲了 8086 指令系统中 关于字符串操作指令一共有五种， 分别是 字符串传送指令 MOVS， 这个每一种指令都可以按字节操作， 也可以按字操作。 像字符串传送指令有 MOVSB， MOVSW。 第二种是 字符串比较指令 CMPSB 或者 CMPSW。 第三种字符串操作指令是 字符串扫描指令， SCASB， 字操作 SCASW。 第四种字符串操作指令是 字符串装入指令， LODSB 和 LODSW。 第五种字符串操作指令是 字符串存，存，字符串存储指令，或者叫存入指令。 存储存入就都是存到存储器去了。 指令助记符是 STOSB 和 STOSW。 这五种字符串操作指令 每一个 指令的目的和源操作数都是隐含寻址。 是隐含寻址要遵循的规则就是，如果 源字符串在存储器， 那么源串在存储器的地址指针由 DS:SI 指明， 是隐含的。 所以在字符串操作指令前面 是不是要对 DS、SI 初始化？啊。 如果源串不在存储器，而在寄存器，字节操作，寄存器隐含是在 AL 中，字操作隐含在 AX 中。 第二，规则二。 如果目的串在存储器，那么存储器的地址必须由 ES:DI 指明。也就是说目的串必须定义在附加数据段，即 ES 段。 如果目的串不在存储器， 而由 寄存器来指明目的串，那么字节操作寄存器用 AL， 字操作寄存器用 AX。 第三， 字符串任何一条指令 CPU 执行以后， SI 和 DI 地址指针， 或者 SI 或者 DI， 所谓或者 SI 或者 DI，那可能源或者目的在寄存器嘛。 地址，如果 在寄存器，指针就是 SI 指明到源， 目的 字符串的地址指针是由 DI 指明。那么字符串操作指令执行以后，SI 和 DI 地址指针会自动的变。 变的方向受 DF 控制。 DF 等于 0， 自动的增。 DF 等于 1，自动的减。 那么增或者减 1 还是 2，受操作类型控制。 字节操作是不是增减 1？ 字操作增减 2。 这是规则三。 规则四， 任何一条字符串操作指令的左边，就可以带上重复前缀。 重复前缀有 REP。 啊，重复前缀我们再复习一下啊。 重复前缀有 REP。 带上重复前缀 REP， 即 CPU 对重复前缀执行就等效于执行了一条 LOOP 指令。 那是不是叫 CX 减 1 了？ 理解了没有？啊，CX 减 1 了。 重复前缀还有 REPZ 或者叫 REPE。 相等和结果为零，概念是一样的。 如果带上 REPZ 重复前缀， 就等效于 CPU 执行了一条 LOOPZ 指令。 我们讲过循环控制指令吧？ 也就是说， 字符串操作指令执行以后，叫 CX 减 1， 减 1 后不等于 0，且 ZF 等于 1（即结果相等嘛）， 字符串操作指令再执行一遍，就重复执行一遍。 重复前缀还有 REPNZ 或者叫 REPNE。 如果带上 REPNZ 重复前缀，就等效于 LOOPNZ 指令，对不对？ 等效于。 啊，这是三个重复前缀。 那么 REP 重复前缀可以挂在 字符 字符串传送指令的左边，像 REP MOVSB 或者 MOVSW， 也可以挂在 REP LODSW 或者 SB， 就是字符串装入的左边。 也可以挂在 REP STOSB 或者 STOSW，也就是字符串存入指令的左边，也可以带上 REP 重复前缀。 啊。 而 REPZ 重复前缀， REPZ 可以挂在 CMPSB， 字符串比较指令左边 可以带上 REPZ 这个重复前缀。 SCASB 或者 SCASW 左边也能带 REPZ 重复前缀。 啊。 那么 REPNZ 重复前缀， REPNZ CMPSW 或者 CMPSB，比较字符串比较指令左边也可以带 REPNZ 重复前缀。 也可以带 REPZ 重复前缀。 SCASB 或者 SCASW 字符串扫描指令左边也可以带上 REPNZ 重复前缀。 那也就是说，用这三种重复前缀， 重复前缀在哪些指令左边都可以挂上， 我列了一张表。 这是这一点。 然后我们今天来看，这个是上节课我们讲的。 今天我们来看 第一条 字符串操作指令，就是 字符串 传送指令。 字符串传送指令助记符是 MOVS。 啊，应用格式是 MOVSB 或者 MOVSW， 或者 MOVS 目的操作数, 源操作数。 三种书写格式。 那么 第一种，我们先看第一种。 MOVSB，一条指令。 CPU 执行完成的功能： 第一步， 第一步， 把字符串，源字符串传送到目的。 源字符串 默认是不是在 DS 段？ 以 SI 的内容作偏移地址。当然， 这个单元当然是字节型的， 还是字型的， 如果是 MOVSB 就是字节型的， 如果是 MOVSW 就是字型的。 啊。 把源传送到目的。目的 必须在 ES 段嘛。 那是固定在 ES 段。 偏移地址由 DI 的内容指明的单元传送到这个单元去了。 也就是说这是源， 这是目的。 那你看， 源和目的 是不是隐含寻址？ 隐含寻址。 这是第一步。 第二步， 啊，第二步。 SI 的内容 和 DI 的 内容会自动的增 （是不是 DF 等于 0 时？）， 或者 DF 等于 1 时会 自动的减 1。 字节操作嘛。 字操作是不是增减 2？ 好，增减 1， 然后给 SI， 给 DI。 自动的变了。 那么这条指令就执行完了。 如果这条指令左边 加上了 REP 重复前缀， 那么 这条指令还没执行完，是不是要进行第三步去了？ 第三步呢， 它让 CX 的内容减 1 后给 CX。 相当于刚才说执行了一条 LOOP 嘛。 然后判断： 若 CX 的值 不等于 0，也就是没减到 0， 则 这条指令 重复执行一遍。 啊。 重复执行。重复执行什么呢？是不是把这条指令重新执行一遍？ 那这条指令要执行多少遍， 是不是受 CX 限制？ 相当于一个循环程序。 显然，在字符串传送指令当中， CPU 每执行一次 传送， 字符串传送， 是不影响 PSW 中的状态。 因为数据搬家嘛， 数据传送。 这是 字符串传送指令。那我们再来看， 如果指令中写的是第三种格式。 这种格式。 这种格式呢， 它只是告诉汇编程序， 汇编程序对这条指令汇编翻译时， 以字节型传送翻译，还是以字型 MOVSW 的指令来翻译？ 是用这两个操作数指明的类型来确定这条指令的类型。 如果在 数据段定义时，就是在数据段或者附加段 定义时， 源和目的的类型是字节型的， 那么这条指令就等效于 MOVSB。 如果是字型的，是不是等效于 MOVSW？ 只是告诉汇编程序这么一个道理，这么一个汇编的 类型，再没有其他意义。 所在实际应用当中，这种格式肯定不太灵。 但是如果你要写这种格式，一定要做到源和目的的类型要一致。 是字节型都是字节型， 是字型都是字型。 否则语法就错了。 啊。 在这块还要注意什么问题？你看，字符串 MOVSB 或 MOVSW 字符串传送指令， 是从一个存储单元传送到另一个单元。 和我们前面讲的 MOV 指令是不是有区别的？ MOV 指令它不能实现两单元之间的直接传送。 这是这块。 那么关于 MOVS 这种第三种书写格式， 你看我们书上 已经举了个例子。 在书上的 74 页， 74 页 上方这一段， 前面定义了个 DATA 段，也定义了个 DATA1 段。是这样说， 并且让 DS 指向了 DATA 段， ES 指向了 DATA1 段。 那 DATA1 段是不是附加数据段了？ 那你看， 下面这一条指令，MOVS BUFFER2, BUFFER1。就是这种指令。 MOVS BUFFER2, BUFFER1。那么汇编程序汇编这条指令的时候，由于 BUFFER2 和 BUFFER1 在 DATA1 段，也就是 ES 段定义的，是用 DW 定义的。所以汇编程序把这条指令就按 MOVSW 指令来汇编。 那么在这条指令里面还有一个 源是 BUFFER1， 目的 是 BUFFER2。 由于 BUFFER1，源，是在 DATA1，ES 段定义的。 所以呢，源操作数的地址指针，存储器地址指针是不是 ES:SI？ 对不对呢？ 这个源串的段地址是不是可以更改的？ 用通过段超越前缀来更改。但目的字符串 的段地址必须用 ES 提供。 这两个概念不一样啊。 这是这一点。 其次我们来看字符串的应用。 字符串 MOVS 字符串传送指令的应用。 书上在 75 页 有一个例题，例 3.36。 把这个题呢， 我们 按标准的 8086 汇编语言程序来设计。 标准的， 完整的，那是加上段定义了。 先读题： 将存储区域， 这是， 例 3.36， 说将存储区域 BUFFER1 中的 100 个字节数据， 接着按次序传送到 BUFFER2 中。 题目说， 我们来看， 这是存储器 BUFFER1， BUFFER1 里面放的是字节数据。 一共 有 100 个字节数据，是不是这个意思？ 叫你编程序， 把它搬到 BUFFER2 之下。BUFFER2，那当然 BUFFER2 也是字节类型的。 那么你在 BUFFER2 之下是不是也要 定义 100 个字节自由单元， 自由空间？ 这也是 100 个字节。 实际是数据块搬家。 那么这个数据块搬家，如果用我们的字，字符串传送指令来实现， 我们看程序怎么设计。 用标准程序设计的话，是不是前面定义堆栈段？ 堆栈段我省略了啊。 我黑板空间不够。我直接定义数据段。 DATA SEGMENT 在数据段 定义 BUFFER1 和 BUFFER2 变量。 是用 DB 定义的。 当 BUFFER1 里面是不是要定义 100 个字节的数据？ 这里面有 100 个字节数据。 当然你做题的实际工作当中，是不是把实际数据也定义进去？ 但是做作业的话，你就写 100 DUP(?) 括号问号括号做也行。 BUFFER2 是目的字符串， 那就是 留 100 个自由空间。 按题目要求， 数据段的变量是不是定义完了？ 数据段结束，DATA ENDS。 然后编代码段，在代码段编写程序。SEGMENT。 ASSUME。在这块看，CS 段 是 CODE 段。 我们为了 能用字符串 MOVS 传送指令来解决这个题的问题， 我们来这样定：DS 段 是 DATA 段， 逗号，ES 段 也是 DATA 段。 啊。 也就是说， 按字符串传送指令的话，BUFFER2 是不是应该 在目的字符串嘛， 应该定义在 ES 段去。 但是我都定义在 DATA 数据段了，所以我现在同样指明 ES 段和 DS 段是同一个代码段。 然后程序开始编写，START: MOV AX, 把 DATA 段名对应的段地址给 AX。 MOV 初始化给 DS。 MOV 同时初始化给 ES。 ES 和 DS 值相等，是不是都指向了 DATA 段的段地址？ 做这样的初始化。 这样紧接着初始化， LEA SI， 把 源字符串 BUFFER 1 变量对应的有效地址给 SI。 是不是相当于 把这个单元的 偏移地址是不是给 SI 了？ 而这个单元的段地址留在 DS。 做这样的初始化。 然后我们再看， 当然用 MOV 指令是不是也能做？给 DI 初始化。 DI, OFFSET BUFFER2。 把 BUFFER2 的偏移地址给 DI。 给到 DI。 使 DI 指向了 BUFFER2 目的串的首地址的偏移地址。 那这个单元的段地址 按照字符串操作的话，是不是应该在 ES？ 做这样的初始化。 当然这块是不是还可以用 LEA 指令实现？ 也是可以的啊。 继续初始化，MOV 把重复次数，所以 CX， 重复次数给 CX。 那么现在要搬家了。 搬家之前， 显然， 我把首地址对应单元的一个字节数据 搬到目的地去。 搬完，SI 自动增 1， DI 自，自动增 1。是不是按地址增大的方向搬？ 那 要为了确保按地址增大方向搬，你就要确保 DF 等于 0。 是不是确保 DF 等于 0？ 当计算机一开机， DF 就等于 0。 或者 RESET 以后它等于 0。但是你要确保 DF 等于 0，是不是排上一条 CLD 指令？ 然后 MOVS B。 紧接着带上重复前缀 REP。 CPU 执行这条指令是不是要 执行 CX 遍？ 执行 CX 遍，每执行一遍，是不是把源搬到目的地搬一个字节？ 一直执行了 100 遍， 源块就搬到了目的地去了。 实现了数据块搬家。 搬完了， 我们 MOV AH, 4CH。 程序结束，INT 21H。 代码段编完了，CODE ENDS。 END START。 题目要求的功能就实现了。
