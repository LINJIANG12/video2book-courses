[00:10]
再定义数据段：`DATA SEGMENT`。

在数据段里面，定位类型缺省，隐性的定位类型缺省，本段就缺省了。组合类型是不是 NONE 型？本段与其它模块中的任何段没有关系。没有关系是告诉链接程序在链接时，本段就是本段，和任何段都没有关系。

那么在数据段是不是要定义变量？定变量：

```assembly
DATA SEGMENT
    BUFFER DB ...
```

可以定义一个字符串。字符串具体是什么字符我就不写了，做题了嘛，可以吧？

那我刚才说，字符串中字符个数是多少个？按说我们要定义一个符号常量 `COUNT`，用等号、用 `EQU` 是不是都可以？等于 `$` 减去 `BUFFER` 的偏移地址：

```assembly
    COUNT EQU $ - BUFFER
DATA ENDS
```

那么这个表达式的值赋给 `COUNT`，`COUNT` 的值是不是单引号中字符的个数？这讲过了。那么数据段就定义完了：`DATA ENDS`。

---

[01:45]
再定义代码段：`CODE SEGMENT`。

在代码段一开始，是不是要段寻址了？要段寻址：

```assembly
CODE SEGMENT
    ASSUME CS:CODE, DS:DATA
    ASSUME SS:STACK
```

`CS` 段是 `CODE` 段名指的这个段，逗号；`DS` 段冒号是 `DATA` 段名指的段。这写不下了，下一行写是不是还要段寻址一次？`SS` 段是 `STACK` 段名指的段。

---

[02:32]
下面就可以开始编程序了：`START`，首地址。首地址这个名称是自己起，一般我们叫 `START`，是不是容易理解一点？`START`。

代码段一开始，我们来看，是不是首先要对谁初始化？代码段一开始，有数据段了，肯定对 `DS` 要初始化：

```assembly
START:
    MOV AX, DATA
    MOV DS, AX
```

你们注意，我这应该说是第三遍了，这条指令的源操作数的寻址方式：立即寻址。`MOV DS, AX`。

---

[03:40]
初始化完了，我下边是不是要对 `BUFFER` 缓冲区中每个字节单元的字符要去测它是几个 1？通过测它是几个 1，从而就知道它里面是偶数个 1 还是奇数个 1，来实现偶校验码。

统计每个字节单元中是几个 1，上节课我们做过没有？做过。我们上节课是不是测试了一下 `BX` 中有多少个 1？那是 16 位数据，这是个 8 位数据。当时我们用的是不是移位法？

---

[04:31]
我们再看，那我把统计每个字节中、每个字符中有几个 1，采用子程序实现。

在采用子程序实现之前，你首先要告诉子程序：你被测试的字符在哪呢？这是个入口参数。你被测试的字符有多少个？这也是入口参数。

那这些入口参数，是不是要在 `CALL` 调用测试的子程序之前，作为一种参数传递方法传递到子程序去？那么我们先用寄存器传递。

譬如说，我 `MOV` 把字符的个数给 `CX`，是不是 `COUNT` 个？

```assembly
    MOV CX, COUNT
    LEA SI, BUFFER
```

我 `LEA`，譬如说 `SI, BUFFER`。被测字符所在缓冲区的首地址的偏移地址在 `SI`，段地址是不是在 `DS`？看到了没有？这些地方是不是都在做入口参数准备？

准备好了，你可以 `CALL` 去调那个子程序去：

```assembly
    CALL SETEVEN
```

调用了这个子程序，能完成对 `BUFFER` 单元中那个字符中有几个 1 进行统计。统计完了以后，从而进行是不是要在最高位加上个 1，还是加上个 0 进行偶校验。明白了。

---

[06:35]
我们书上这个子程序名叫什么呢？`SETEVEN`，是不是？

你看，在 `CALL` 调用 `SETEVEN` 子程序之前，把字符个数给 `CX`，缓冲区首部的偏移地址书上是不是给 `DI` 了？我这给 `SI` 了。一样不一样？我用的地址寄存器不一样，设计思想是不是一样的？

我们来看，假如它采用的是段内子程序调用，是不是直接把子程序用 `PROC` 开始定义了？如果是段间子程序呢？上面是不是还要段名 `SEGMENT`，再定义一个逻辑段嘛。`SEGMENT` 下面是不是还有 `ASSUME`，各个伪指令告诉汇编程序 `CS:` 这个段名？段地址是不是有了？然后才能定义子程序。

---

[07:48]
这个子程序名叫 `SETEVEN`，类型是 `NEAR`，缺省了：

```assembly
SETEVEN PROC NEAR
```

那子程序一开始，书上的子程序一开始：`PUSH AX`、`PUSH BX`、`PUSH CX`、`PUSH DI`。是不是把这四个寄存器的值压栈保护了？做现场保护。保护的目的，是不是它下面准备用这些寄存器？先要把这些寄存器内容保护了。

你看：

```assembly
    PUSH AX
    PUSH BX
    PUSH CX
    PUSH SI
```

按照我刚才不是用了 `SI` 嘛，那我应该 `PUSH SI`。

---

[08:52]
当然，你还可以在 `SETEVEN` 前边写一个子程序说明文件。我刚好把黑板擦了。子程序说明文件你可以自己写：

```assembly
; 1. 子程序名: SETEVEN
; 2. 子程序功能: 该子程序实现...
```

一、分号，子程序名：`SETEVEN`；二、这行又得分号开始，二、子程序功能：该子程序干什么干什么，说清楚。

---

[09:30]
那么我们对这个子程序读一下。实际上这个子程序用了嵌套，用了嵌套。

你看，我们写出来吧，写出来再阅读，要学会读程序：

```assembly
    MOV AL, [SI]
```

`MOV AL, [SI]`，间接寻址。是不是把 `SI` 的内容指的这个单元……

This request was blocked by Gemini's filters. They can occasionally trigger by mistake on safe coding, security, or biology-related queries. Please try rephrasing your prompt. You can [send feedback](https://ai.google.dev/gemini-api/docs/troubleshooting#file-bug) or read more about [our policies here](https://policies.google.com/terms/generative-ai/use-policy).

以下为根据所提供的音视频内容整理的高保真逐字转录文档：

---

### 汇编语言程序结构与堆栈分析

#### [00:00] 子程序返回与结束（`SET_SEG`）
先进后出。现场恢复了，应该子程序返回。

子程序返回，子程序返回了是不是子程序结束了？`ENDP`。哪个子程序结束了？`SET_SEG`。

```assembly
    RET
SET_SEG ENDP
```

---

#### [00:27] `COUNT_BYTE` 子程序定义与参数传递
在这个子程序下边，是不是有 `PROC`？就 `CALL` 到这个子程序去了。

`COUNT_BYTE`，定义子程序。这个子程序我就不编了，它的作用是统计一个字节中 1 的个数。

被统计的那个字节数，原操作数（入口参数）是不是用 `AL` 带进来了？统计后的结果是用 `BL` 带出，出口参数是 `BL`。

```assembly
COUNT_BYTE PROC
    ; 功能：统计一个字节中 1 的个数
    ; 入口参数：AL
    ; 出口参数：BL
    RET
COUNT_BYTE ENDP
```

这个子程序就定义完了。

---

#### [01:15] 代码段与程序结束结构（`CODE ENDS` / `END START`）
这个子程序定义完了，是不是整个程序结束了？

我接到这。整个程序结束了，是不是应该是 `CODE` 代码段 `ENDS`？这个代码段才结束，对吧？

`CODE SEGMENT`，这个代码从这开始，代码段是不是到这结束？代码段结束了，整个程序结束：

```assembly
CODE ENDS
END START
```

这就是这么一个程序结构。

---

#### [01:47] 堆栈段结构与 SP 指针变化分析
在这个程序结构当中，下面我们看一下堆栈的变化。

堆栈的变化画到这，这是堆栈段。堆栈段从这开始，这个当然是底，栈底在 `SS`，偏移地址是 $0$。一直到这，这是栈底。

初始化后，`SP` 指针指的就是栈底加 $100\text{H}$ 的偏移地址。那这个区域是不是定义了 $200$ 个字？是不是 $400$ 个字节呢？

$$200 \text{ 字} \times 2 = 400 \text{ 字节}$$

$400$ 个字节，你就能算出初始化以后 `SP` 等于多少。能算出来吧？能，能算出 `SP` 等于……

> ⏱️ **分页状态**: 全篇音频已处理完毕。