数据满了，设备如果再次准备好了一种新的数据，就不能给8255再发一个选通信号了。原因是，你刚送过来的数据，输入缓冲器是满的，就意味着CPU并未把这个数据没有取走。这是D0到D7，这边是不是CPU一方了？CPU把数据并没有取走，那么设备就不能发新的数据了。只有CPU把数据取走了，缓冲器是不是空了？空了，实际上里面是有数据的，那个数据无非是无效的，我们把它表述为空。只有输入缓冲器空了，设备把准备好的新的数据，才能再次发一个低电平选通信号，选通到8255内部的输入缓冲器。一旦选通进去，这个缓冲器又满了。那设备怎么知道，设备怎么知道输入缓冲器满了还是空了呢？谁知道？8255知道。它知道。就是说，8255呢，这个缓冲器是满的还是空的，它知道，它向设备发一个状态。发一个状态。这个状态叫什么呢？叫IBF_B口的。IBF大家看，I是输入，B是缓冲器，F是满了。输入缓冲器满了。大家看，一旦这个低电平一来，这不下降沿？下降沿一来，稍微有所延迟，IBF_B它就变高了。这个高电平是不是代表输入缓冲器满了？满了。但CPU把数据并没有取走，所以设备是不是不敢再发新的数据了？什么时候能发新的数据呢？只有当CPU把这个数据取走了，设备才能选通新的数据。那CPU一方，CPU一方怎么知道输入缓冲器满了呢？怎么知道呢？你可以用查询法。可以用查询法。查询什么呢？查询IBF你等于1吗？看到了没有？当查询到IBF等于1了，CPU对B口实施一次IN操作。一次IN操作是不是读了一次？就把数据读走了。一旦CPU一读，PB口输入缓冲器数据读走了，输入缓冲器是不是空了？那么来看，CPU读了一次。读了一次，假如在这个时候CPU IN操作了一次。那这个IN操作你看，读完了，IBF就变低。它就变低。这一变低，说明输入缓冲器是不是空了？输入缓冲器空了，那么你设备如果在输入缓冲器空期间，把新的数据准备好了，是不是给8255又要发一个选通信号？这个选通信号一来，IBF是不是又要变高？理解了没有？这么一种工作时序，查询法。那么，8255 PB口工作在方式1时，它是用PB口方式1，它的联络线肯定用的是B组的联络线。B组的联络线是PC口低四根。它用的是PC2做STB非输入，用PC1做IBF输出。做IBF输出。那，我们看，你程序设计的思路，显然是不是这么一个思路？一开始，对8255A初始化。初始化。初始化其中PB口是不是初始化成了方式1输入了？做了这样的初始化。初始化完了以后，什么时候读设备准备好选通到PB口输入缓冲器的数据呢？是不是应该去测IBF？测IBF你等于1吗？但在实际工程当中，有些同学就测STB非你等于0吗？思路是不是对的？但是实际是错的。实际为什么是错呢？STB非，这个选通信号是设备发来的。而CPU要一读这个状态测，是不是有它的时钟？设备是不是有它的工作时钟？这两个时钟是不是异步的？那你一读这个PC2状态时，你就不能确保，好比你一读的时候在这儿。它不是低电平。当你测完不是低电平，再次读的时候是不是跑到这儿来了？你可能抓不住。理解没有？但是当输入缓冲器满了以后，CPU没有读数据之前，IBF是不是一直是高电平？所以测IBF是不是永远能测到？所以这个时候你应该读PC口状态。读PC口状态。PC口状态读回去以后应该测试，测PC1，PC1是不是IBF_B？你等于1吗？是不是应该这么一个测试要点？你看，我no，不等于1。不等于1，说明输入缓冲器是不是空的？CPU就不能取数据。那我就跳回去，再读PC口状态。再测，输入缓冲器你满了吗？no，再读PC口状态。在这儿做什么呢？查询输入缓冲器满了吗？设备把数据选通进来了吗？理解没有？直至查询到输入缓冲器满了，满了，是不是要把数据取走？那取走怎么取呢？就IN，是不是要读PB口了？读PB口。读回去的数据是不是要做处理了？处理完了，处理完了以后，你接着还可以去测，去查查询什么呢？数据输入完毕了吗？如果没输入完，是不是又要读下一个数据，是不是又要读状态去了？查询法嘛。直至数据输入完是不是下来了？那么这种工作方式，方式1和我刚才的这种工作方式，是有明显区别的。区别说，在方式0时，我CPU任何时候IN读这个端口，是不是都能读到有效数据？但在方式1时不行。什么时候IN读这个端口才能读到有效数据呢？只有缓冲器满了。理解了没有？这是本质的，和方式0本质的区别。还有，还有什么呢？还有，8255的PB口工作在方式1输入时，刚才我讲了查询法，还可以工作在中断法。中断法什么意思呢？PB口可以利用PC0这一根线，向外部的8259发INTRB中断请求信号。什么时候发这个中断请求信号呢？只有8255 PB口处于中断允许时。这不一个中断源？中断源中断允许时。什么时候中断允许呢？在8255的内部，在8255内部，有一个中断允许触发器，叫INTE。B口的中断允许触发器叫INTE_B，A口的中断允许触发器叫INTE_A。当中断允许触发器置1了，B口才能朝外部发中断。明白了吧？它中断允许了。那么8255一加电RESET，中断允许触发器清零了。什么时候置1呢？显然是不是用户要编程使它置1？那么在8255的内部，在8255的内部，类似的逻辑功能上，相当于有这么一个与门。这个与门的一个输入端来自于中断允许触发器，另一个输入端来自于IBF引脚。输入缓冲器满了的引脚。那你看，当中断允许触发器置1了，当输入缓冲器满了，1与1得1，从这儿就输出了。原来这地方是0，这地方1、1得1，是不是变高了？当输入缓冲器满了，是不是高了？高了是不是来个上升沿？这个上升沿就能向8259提出中断请求。8259得到这个中断请求，由8259再向CPU提出中断请求。CPU如果处于中断允许状态是不是要响应？CPU响应是不是要转到服务程序去？在服务程序里面，IN把输入缓冲器的数据读走。一读走缓冲器是不是空了？IRET返回。是不是读了一次数据？这叫中断法。那么，8255的PB口的中断允许触发器INTE_B置1的方法是这样子。假如分配给8255的端口地址仍然是300到303H，那我们编程：MOV DX, 303HMOV AL，最高位是1，这两位是不是A组工作方式？这位是不是A口的方向？这位是C口高四位方向。这位是不是B组工作方式1？这位，B组做PB口方式1输入。这位写0、写1无所谓了。因为低四位，PC口低四位是要做联络信号了吗？你看，PC0、PC1、PC2是不是做联络信号了？这样，一个方式字，那不用位取0，取0的话，这个方式字是不是86H？然后CPU OUT DX, AL。这一条OUT指令就使得8255的PB口工作在了方式1的输入状态了。在方式1输入初始化，这是初始化嘛，做这样的初始化。初始化一旦完成了，紧跟着，紧跟着，对PC2位置1。紧跟着，你编程序给PC2位置1。达到INTE_B置1的目的。理解我思想没有？8255内部是不是有控制判断逻辑？你给它这样初始化完了，紧跟着对PC2位置1了，并不是让PC2位引脚置1，而是借助于这种逻辑顺序关系，实现INTE_B置1。明白思想吗？那么怎么置1呢？我MOV AL，最高位是0，这三位不用，这三位用010是不是PC2？最末位写1，置1，这就叫PC2位写1。OUT DX, AL，是不是对PC2位管理？你看我最高位是0嘛。还是向303端口写，是不是位管理？位管理叫PC2位置1，位管理使PC2置1，这种逻辑关系，8255就知道让它将INTE_B置1。明白了吧？借助这种逻辑关系来现让中断允许触发器置1的目的。那么一旦中断允许触发器置1了，后面你编程序编编，好比说用中断法实现设备选通到PB口输入缓冲器的数据取走。那你这地方有任务你就编程序实现任务，没任务是不是编一个小循环等待中断？一旦中断产生了，CPU一响应，是不是到中断服务程序去？中断服务程序里面把PB口数据取走，取走一处理，IRET是不是又返回回来了？理解我思想没有？和我们第八章讲的中断应用是一样的。这是PB口工作在方式1时。那么，PA口也可以工作在方式1输入。如是这样子，我画到这儿。这是PA口。PA口方式1输入。那我先把它讲完。我在这儿加一句。这是INTRB信号。当外部选通信号一来，看见没有？IBF是不是变高了？IBF一边高，INTR是不是变高了？变高变高变高变高。什么时候它变低的？当CPU IN把输入缓冲器数据取走了，IBF是不是变低了？IBF变低了，你是不是应该变低了？这次中断是不是结束了吗？再看PA口。这是PA口的PA0到PA7输入引脚，这边是不是接的输入设备？当输入设备把数据准备好了，准备好了，就要给8255的PA口发一个STBA非选通信号。这个低电平的选通信号，是把设备送来的有效数据选通到PA口输入缓冲器了。PA口输入缓冲器是不是满了？PA口输入缓冲器满了，PA口给外设发一个IBFA高电平的，你看，这个信号一来，这地方是不是变高了？一变高，告诉设备：输入缓冲器满了！但CPU把数据没取走。当CPU检测到IBF变高了，是不是用查询法吗？就可以用IN指令把PA口的数据取走，通过D0到D7，IN读到哪儿去了？是不是读到AL去了？然后读到AL你这么处理是你的事情。一旦IN，你看就是读控制端加低电平，把输入缓冲器数据取走了，输入缓冲器是不是空了？一空是不是IBF就变低了？当然，PA口也可以在方式1时，刚才此查询法，也可以工作在中断方式。当PA口工作在中断方式时，我们看，中断请求信号是用PC3引脚输出的。输出的是INTRA。是不是它的中断请求信号输出没有用A组的信号线？是不是B组的一个？看到了没有？用的B组的。因为PA口还可以工作在方式2，方式2输入时是不是用一对联络信号线？方式2输出时是不是又需要一对联络信号线？把A组的四根PC口线是不是用完了？所以PC、PA口要工作在方式1或者方式2时，中断方式时，中断请求信号线用的是PC3。用了PC3，所以PB口再不能工作在方式2了。联络信号线不够了。明白了吧？那么我们再看，你看说，在PA口工作在方式1输入时，PC口高四位用的哪一根线PC几做STB非选通了？PC4，PC4。那么IBF输出是不是PC5？做一对联络信号线了。那么在这个时候我们再来看，PC口高四位是不是还剩一个PC6和PC7？还剩两个嘛。这两根仍然可以作为方式0输出或者方式0输入做两根I/O线来用，明白了吗？再来看，在8255 PA口内部，也有一个中断允许触发器叫INTE_A。A口的中断允许触发器。当中断允许触发器置1了，A口是不是工作在中断允许状态？当A口工作在中断允许时，当IBF变高了，也就是输入缓冲器满了，是不是在这儿能，在这儿就能发生一个由低变高的上升沿，向8259提出中断请求。那就相当于我们来看，在这个内部，有一个与门，与门的一个输入来自于INTE_A，另一个输入是不是来自于IBFA？输出是不是接到PC3上去了？输出接到PC3。
