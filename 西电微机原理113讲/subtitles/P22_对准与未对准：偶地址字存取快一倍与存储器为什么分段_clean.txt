那我下面要说的问题是这。假如说我在这来，DB，是不是又得定义一个字节数据？而这个字节数据好比说我是写了个-1。那编译程序是不是在这个单元，这是，这个单元的地址是多少？偏移地址是不是0005？是不是定义了个FFH？-1的补码。明白了吧？好，我这下来再定义了一个字数据。好那我定义的是，啊，34A3H。十六进制数嘛。那按照我们刚才讲的，继续往下走，继续往下走，是不是，第一个字数据，是不是要定义在这个地址单元来了？是不是这个单元来了？这个单元和它下一个单元，相，相邻的单元是不是组成一个字单元？这个字单元的地址是多少呢？来说偏移地址，6，6H。我们按照刚才这个数据存放格式，字数据在存储器中的数据存放格式，啊，是不是，啊，低字节存放到了低地址单元来了？这个单元放了A3H。高字节存放到了高地址单元来了？34H。哎，你看我把字数据是不是讲了两种？这两种是有区别的。什么区别呢？我们看，这个字数据单元的地址是3，是不是奇地址？这个字单元的地址是6，是不是偶地址？我们把，字单元的地址是偶地址的字单元称为对准的。这对准了。啊称为对准的。而字单元的地址是奇地址的话，称为未对准，未对准的字单元。现在我是不是提出来一个对准和未对准的概念？显然，字单元是对准的，说明字单元的地址是偶地址。如果字单元的地址是奇地址，称该字单元为未对准的单元，字单元。那么，在实际操作当中，比如说我用MOV指令给字单元写数据，或者读数据，是字操作嘛。那么如果字单元是对准的，和字单元是未对准的，MOV，CPU MOV去写数据操作，对对准的字单元的操作速度要快一倍。理解了没有？速度上，也就是说我用专业术语上来说的话，CPU对对准的字单元的读或者写操作，只需要花费一个总线周期的时间。而对未对准的字单元的读写操，字单元的读写操作要花费两个总线周期的时间。听懂了这样没有？当然，我指的是8086系列的CPU。啊。这是一个概念。还有一个概念是什么呢？我们来看。8086 CPU，我们前面已经说了，哎，假如说，这是8086 CPU。我们已经知道8086有多少条数据线？是不是16根？16根。16位机嘛。有D0到D78根。是不是还有D8到D15，8根，一共是不是16根？我们把，从D，我们把D0到D7数据线称为低8位数据线。D8到D15叫高8位数据线。理解了没有？如果，对字单元，对字节单元读写操作时，该字节单元的地址是偶地址，是不是传送8位数据嘛，CPU自动地走低8位数据线传送，只用8位线嘛。如果一个字，字节单元的地址是奇地址，把一个8位数据写到这个奇地址单元来，那那8位数据走这16根数据线里的哪8位？如果该单元地址是奇地址，是不是走高8位？如果单元地址是偶地址，走低8位。听懂了没有？有这么一个数据传送在里面。还有，还有什么？现在是不是看到字了？先看偶，偶，偶字单元，就对准的字单元。啊。CPU给该字单元读写数据，读写数据，我举例子不？比如说，比如说我举这么一个例子。我在这举个例子啊。挨着它了啊，MOV BX，是什么？基址寄存器。给这个寄存器里面放了个6，0006H。是不是这个单元的偏移地址啊？啊。然后我MOV，我给AX 16位累加器里面，假如说我放34A3H，是不是一个字数据啊？啊，或者我另外放一个数据，我放了一个，好比说，3344H。然后我让CPU执行MOV [BX]，间接寻址，, AX是不是我把AX 16位数据写到这个字单元去？上节课讲，以BX间接寻址，BX提供的16位偏移地址是哪个段的偏移地址？DS段的。那现在CPU朝这个，MOV执行这条指令朝BX间接寻址这个单元写，是不是写到6单元来了？理解了没有？啊。给这个单元写什么数据呢？我们看AX，你看，我这条指令的解释：它，原来AX里面的内容，这是AL，这是AH嘛。它写的时候呢，AL是不是低字节？低字节是不是要放在低地址单元去？我把这些箭头擦掉。已经会了，那一块已经会了嘛。啊，AL低字节数据是不是要写到低地址单元来？给这个单元就写了个什么数据？44H嘛。44H。高字节单元的数据，是AH，是不是要写到高地址单元来？给这个单元放的是多少？33。高字节放在高地址单元来。CPU做这个工作，看，MOV，执行这一条字单元的写操作，把AX内容写到这个字单元去。为什么BX间接寻址这个单元是字单元呢？由于AX是16位的嘛。如果这地方写了个AL，BX间接寻址找到单元是不是字节单元了？啊。那你看，现在对这个字单元写，因为该字单元是对准的，所以这，CPU刚才我说，执行这样的操作只需要花费一个总线周期。也就是说，这8位和这8位哗并行写进去了。而这8位数据写到低地址单元，低地址单元的地址是偶地址，所以这8位数据朝这个单元传送的时候，走哪8位数据线？低8位。D0到D7。而高地址单元，高字节数据写高地址单元，高地址单元偏移地址是不是7？是奇地址，所以这8位数据走得哪8位数据线呢？高8位。高8位，D8到D15。啊。CPU执行这MOV这条指令，是不是把这两个8位，沿这两个8位数据线轰并行写进去？只花了一个总线周期时间。听懂了没有？那么反过来，反过来我们看，我再举个例子。刚才未对准的，我把这个擦掉。这个已经会了嘛，对吧？如果我们有这样的操作，比如说我有这样存，MOV SI，我现在是不是用的源变址寄存器？呃，这个单元的地址是3，我把3拿过来，0003。然后好比我MOV AX，我始终用累加器嘛。啊。好，我，现在是，5566H。然后我再MOV把AX中的5566这个16位数据写到SI变址寄存器间接寻址单元去。上节课我讲了，能做间接寻址的是不是有基址寄存器BX，基址指针寄存器BP，SI源变址寄存器，DI目的变址寄存器，是不是都可以打中括号用？都可以给存储器提供地址。但是，只有BP加打中括号间接寻址时，提供的偏移地址，默认CPU默认是SS段的偏移地址嘛。剩余，是不是都是DS段的？啊。那你看让CPU执行这一条指令。那么再来看，AX，AX 16位的，这是AL，这是AH。AH。CPU执行这一条指令，就把AL的内容，一个字的低字节，写到SI作地址单元去了。SI间接是不是默认是DS段的SI的内容作偏移地址的单元，是不是写到这个单元去了？由于这个单元呢，是现在操作是不是字操作？字操作。这是一个字嘛。由于这个单元地址是奇地址，所以这个数据传送，写到这个单元，走得哪8位数据线？高8位。D8到D15。沿这8位数据线，轰，把这个数据，AL数据是不是66H，写到这个单元来了。这样的传送花费了一个总线周期时间。它在下一个总线周期，再把高字节写到高地址单元来。高字节数据是不是55？在下一个总线周期。那么，下一个单元是不是偏移地址4？是偶地址，所以数据走的是低8位数据线。D0到D7。啊。但是这个数据传送，这8位数据传送花费了一个总线周期时间。这个8位数据传送花费了第二个总线周期时间。花了两个总线周期把这个字才写完了。和刚才对准的偶字单元，数据的传送不一样，是不是并行写过去了？只花费了一个总线周期时间。啊。这我从这个数据存放格式里面提到了一个对准的和未对准的。那么根据这个概念大家能够总结出，总结出什么呢？在实际的程序设计当中，如果你要在数据段定义你的字数据，然后在程序当中经常要对那个字单元进行读和写操作，为了提高程序处理的速度，你尽可能把字单元定义在偶地址上。听懂了没有？啊，定义在偶地址上。这是这一点。这是呃字节型数据和字数据的这个这个存放格式。明白这了没有？啊。但在实际应用当中，可能还会出现双字型数据，但是很少。啊。如比如说，这行这是存储器。存储器。啊。现在我要定义一个双字数据，就是用DD。这个D还是data，数据嘛。下一个D是double。还是一条伪指令。告诉汇编程序汇编时怎么操作的一条语句嘛。啊。好比我定义了一个呃，78，好啦，783H。假如说把这个字数，双字数据定义在这个双字单元来，那双字单元是不是由四个字节组成？那么汇编程序汇编时也是把低字节写到低地址单元来，高字节，看，没有了是不是补0了？写到高地址单元来。再高字节写到高地址单元来。再高字节，是一个双字嘛。0在这可以空缺的。哎，写到高地址单元来。哎，这个字数据就汇编完了。你看双字数据在存储器中的存放格式，是不是也遵循了低字节在低地址，高字节在高地址的这么一种数据存放格式？啊。那么通过这个讲解我也引出了在8086汇编语言程序设计当中数据定义的伪指令。字节型数据定义伪指令用DB，字型数据定义伪指令用DW，双字型数据定义用DD。啊，但这些个伪指令绝大多数场合下，数据定义伪指令都用在哪个逻辑段定义当中？数据段。啊。那这些个数据定义伪指令我们书上下节就有讲解。应该在第三章，啊。你看第三章的3.1.3节就讲“变量及伪指令”，27页嘛。到了28页，是不是讲“变量定义伪指令”？啊，有DB，DW，DD等等。这些伪指令。变量定义伪指令，变量定义伪指令就是数据定义伪指令。那这个地方叫了“变量”，为什么叫变量？我们讲到这一块再说。不可能把这个讲完嘛，对吧？这是这一块。啊。那么，这一节要，这第一个概念就讲完了。第二个概念，就要讲“存储器的分段与物理地址的形成”。啊。存储器的分段与物理地址的形成。那在这个问题当中我们看第一个问题，应该大家明白的，我就把它叫做“存储器为什么要分段？”在8086系统中。大家答：为什么要分段？分逻辑段，就这意思，为什么？上节课我都答了。对，概念基本上有些同学出来了。就这样叙述：也就是说，由于8086 CPU有20位地址线，它能寻址的外部存储器空间是1兆字节。但在8086系统当中用于存储器寻址的地址寄存器像BX，BP，SI，DI等等，都是16位的。只能给存储器提供2的16次方等于64K字节地址。那么这样，显然用这样的16位地址寄存器给存储器提供地址码来寻找要读写操作的存储器单元，是不是把1兆字节单元寻址不完？那么为了把1兆字节单元能寻址完，必须寻址完，所以把1兆字节的地址空间要分成许许多多的逻辑段来管理。理解了没有？这就是为什么要分段。用自己语言只要把概念答出来就行。理解了没有？啊。这是存储器为什么要分段，啊。那第二个概念，就是要掌握怎么分，怎么分段？啊，要分，那现在怎么分？那既然给存储器提供地址的地址寄存器都是16位的，因此，你分成的逻辑段，一个逻辑段最大空间只能是2的16次方64K字节嘛。啊。就是分的时候，每个逻辑段最大是64K字节，以这个原则去分。最大。但是在实际应用当中，你，你用的那个逻辑段空间有多大，当然我说了最大是64K，当然你用的那个逻辑段可能还是一个字节，可能是两个字节，那就根据需求了。理解了没有？比如说我们刚才用DB、DW来定义数据，数据是在数据段定义的。那我用了几个数据，我程序中再不用数据了，我告诉编译程序该段定义结束。那那个段是不是占了那几个字节单元？理解了没有？但是最大是64K。那么依据这样一个原则来分逻辑段的时候还要遵循一个原则。那么在整个的1兆地址空间当中，那我从这个地址开始，到哪个地址结束，我说是一个逻辑段？不行。也就对分的一个逻辑段的起始地址是不是有一个要求？啊。就是说，每个逻辑段先这样掌握啊，每个逻辑段的起始地址，我们现在掌握的是：必须啊，必须什么呢？能被16整除。也就是说，只能被16整除的地址才能作为一个逻辑段的起始地址。啊。比如说0地址单元是不是0能被16整除？那0地址就可以作为一个逻辑段的起始点。那下一个单元是不是1地址啊？1能被16整除吗？不能，所以1地址单元就不能作为一个逻辑段的起始地址了。明白了吧？那么依据这样一个分段原则，我们来看，从理论上这种分段原则，1兆地址空间最多能分成多少个逻辑段？是不是说能被16整除就意味着每隔16个单元的地址才能被16整除嘛？那你1兆除以16是不是才能，最多能分成1兆除以16个逻辑段？1兆除以16等于64K。
