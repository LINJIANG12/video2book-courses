我画一个图。这个图呢，呃如，是书上的8.13，是IBM PC-AT机中。

你看，前面我讲的是不是PC-XT？那主板的CPU是8088。我-AT主板的CPU是286，80286。其实到了80386、486、奔腾、奔二、奔三，是不是都是一样的啦？啊。

在这个机子当中呢就有两片8259级联。啊。第一片8259，我们来看，这是一片8259，这是一片8259。啊。8259A，8259A。如果这个SP非-EN非这个引脚，这个8259我接正5伏，它是不是主片？它就是主片。这个8259的SP非-EN非我把它接地，那这片8259是不是从片？哎，从片。主从关系先搞清楚。啊。

那你看，我们再来看。这片8259，从片8259，是不是有它的IR0、IR1一直到IR7八个中断请求输入？从，主片8259也有它的IR0、IR1、IR2到IR7八个中断请求输入。

在AT机中它是这样做的。呃，是这样做的啊。就是这个芯片不是有它的INT中断请求输出吗？输出以后，它级联到主片的IR2上来了。由主片的INT输出是不是接8086 CPU的INTR了？啊，是这样接的。级联。啊。

那么这里边就有呃主片的CAS2、CAS1、CAS0三个输出，就做输出了三位。那从片也有它的CAS2、CAS1、CAS0，是不是做输入了？啊。那么做电路时就应该CS2输出接从片的CS2，CS1输出接从片的CS1，CS0输出接从片的CS0。哎，这怎么漏了个S。啊，是不是你看？

这主片你看这地方向CPU，我们再来看啊，发中断请求。CPU如果要响应，是不是通过INTA非向这个器件的INTA非这个引脚发两……用两个总线周期是不是要发两个负脉冲？啊。我用一个颜色，我这画不下了啊。那这个线


【05:00-10:00补】
这段大学《微机原理》课程关于 **8259A 中断控制器级联工作过程及内部结构** 讲解的完整转写内容如下：

---

**00:00 - 01:00**
是不是？还要接到所有从片的 $\overline{\text{INTA}}$ 上去，所有从片。我现在画了一片嘛。啊。
你看，CPU发第一个 $\overline{\text{INTA}}$ 负脉冲，是不是主片、所有从片都收到了？都都知道CPU响应了。啊。
然后CPU在第二个总线周期发第二个 $\overline{\text{INTA}}$ 负脉冲，主片收到了，从片也收到了。
主片这个时候呢，收到以后，如果刚才发给CPU的中断请求是这8个引脚哪个引脚来的请求？那如果（IR2）是它的请求，是不是它过来的？
那么这个时候，主片8259收到第二个 $\overline{\text{INTA}}$ 负脉冲，就在 $\text{CAS}_2, \text{CAS}_1, \text{CAS}_0$ 上，是不是应该输出一个 010？010——2。

**01:00 - 02:00**
说：“我现在给CPU发出去的中断请求，是 $\text{IR}_2$ 上来的中断请求。”因为 $\text{IR}_2$ 是级联嘛，所以它把级联代码送出去。
那么后面所有的从片是不是都在这收到一个 010 代码？所有的从片在内部是不是在比较呢？
“这个代码和我初始化时候被连在主片的谁上了那个代码相同吗？”
一比较是不是它相同了？它相同了，它就应该从它的 $\text{D}_0 \sim \text{D}_7$ 引脚上输出——当然这是输入输出引脚啊——是不是输出中断类型号 $n$？CPU把 $n$ 取走了。啊。

**02:00 - 03:00**
当然，这个器件是不是也有它的 $\text{D}_0 \sim \text{D}_7$？但是中断类型号不是主片送出来的，是从片送过来的。啊。
这么一种级联方式。啊。
当然那个读、写控制，还有片选（$\overline{\text{CS}}$）、$\text{A}_0$ 我没画啊，那个是一样的，那个是很简单。这是级联的方式。啊。
那么，第二，我们看这，是不是看的是学一个器件的引脚功能，实际上是不是在学这个器件的外部特性？学这个器件的外部特性。啊。

**03:00 - 04:00**
现在我们看内部特性。啊。
8259的内部特性，也就是内部组成，更好理解一点。啊，内部组成。
8259的内部组成，看书上的图8.7。图8.7。通过图8.7我们来看：
8259内部首先有这么一个部件，8位的，叫——叫 IRR。

**04:00 - 05:00**
它书上叫什么呢？中断——I嘛，请求寄存器（Interrupt Request Register）。
显然，你看，如果……如果我这虚线框是8259，虚线以内是8259内部，你看，这个中断请求寄存器显然就是我们刚才说的 $\text{IR}_0$——它的输入嘛，$\text{IR}_1$ 一直到 $\text{IR}_7$，8个中断请求输入。
哪个中断请求输入端有中断请求，首先8259内部通过 IRR……


在对应位输出置1。也就是说，它有8个请求输入，是不是有8个请求锁存？那么这个器件很像373或者374。来个数据是不是锁存到输出端了？送到输出端了，就把这请求锁存到这。

那么，外部IR0到IR7外部中断源，通过IR0到IR7送过来的中断请求，有信号的形式，我们可以对内部寄存器初始化，来初始化成高电平有效或上升沿有效，一般都取上升沿。明白吧？一般都取上升沿有效。

那么，外部中断源通过IR0到IR7送过来的中断请求，能不能寄存在IRR的输出端，受这么一个寄存器的控制。内部有一个IMR，8位的，都是8位的。IMR呢，可以对IRR控制。

什么意思呢？我们看，我在这画一个图。这是IMR，我简单说一句。这是第0位、第1位，一直到第7位，这是IMR的状态。我可以给IMR这个寄存器里面——这是个寄存器咯，我OUT写数据。假如我把这位写1了，这是第2位，其他位写0了。意味着什么呢？要是第2位写1了，意味着我把IR2中断请求屏蔽了。

也就是说，IMR的第0位到第7位，用来管理IR0到IR7八个请求输入的屏蔽。对应位写1，对应的中断请求输入是不是屏蔽了？某位写0，对应的中断请求输入就未屏蔽。也就是说，外部中断请求可以受8259的屏蔽。理解这意思吧？啊。

只有未屏蔽的中断请求，才能寄存在IRR的对应位输出端置1。你请求来了，对应那位置1了。啊。

然后呢，在这内部有一个中断优先权分析器。啊，优先权分析器。优先权分析器把寄存在IRR输出端所有1——是不是所有的请求——分析一遍，看谁的优先权最高，就把谁的请求是不是要送给CPU呢？

怎么送呢？在8259内部有一个，啊，ISR。看，中断Server，服务寄存器。准备把谁的请求给CPU，通过INT变高给CPU，叫CPU去服务，就ISR对应位就置1。啊，明白这意思吧？

如IR1有请求，那么现在只有一个请求，未屏蔽，优先权分析器一分析，只有一个中断请求，那它的优先权是不是最高？那叫ISR的第1位就置1。理解没？

ISR的第1位置1了，有几层意思：一，表明——表明，这是8259，啊，我先画在这——表明8259它对外设已经服务了。这句话能听懂吗？就是对应的ISR位置1了，对应的接的那个中断……

想怎么……8259对它已经服务了，但CPU服务了没有？没有呢。CPU还没服务呢。CPU什么时候服务呢？这个时候你看，8259就通过INT是不是要变高，向CPU请求了？啊，你这里已经置1了吗？那么CPU，这接到CPU的INTR了。CPU通过INTA非——INTA非这个引脚接过来，是不是？好比如在这个时候CPU响应了，是不是通过这个引脚先发一个负脉冲：“我响应了”；在第二个总线周期再发一个负脉冲。再发一个负脉冲，是你，如果是芯片或者什么，是不是你要通过数据线把中断类型号送出来？啊？那么送出来以后，我们来看。CPU有了中断类型号，给中断类型号n乘以4，在向量表里边是不是才找到了中断服务程序入口地址啊？分别给IP和CS。CS:IP指向了中断服务程序入口，取指令执行去了，CPU才对外设服务了。理解了没有？CPU对外设才服务了。那么CPU能不能对外设服务是不是受IF的控制啊？啊？好。那么对于8259而言，如果，如果，有这么一个情况，刚才的中断服务，再看中断结束方式。8259有两种中断结束方式，一种叫自动结束。好，自动结束。自动结束是什么意思？就是你把8259初始化成中断自动结束方式，就意味着8259收到第二个INTA非负脉冲的时候，一方面把中断类型号送到数据线上，第二方面把ISR刚才要服务的那位不是置1了吗？清零。一清零，8259对外设而言服务结束了。这种中断服务结束方式称为自动结束方式。这种方式不用，一般都不用。我们常选用的，包括PC机BIOS对主板上的8259初始化选用的，是非自动结束方式。非自动结束方式。非自动结束方式是什么意思呢？CPU获得中断类型号以后，就是CPU发出INTA非第二个负脉冲后获取了中断类型号的同时，8259并不把ISR服务的那个置1位清零，并不清零。那么CPU是不是转到中断服务程序去执行去了？CPU执行中断服务程序就是对外设的服务。当CPU对外设服务结束了，要结……要结束了，是不是要执行IRET返回？在IRET返回之前，你在中断服务程序里边应该安排向8259 OUT写结束命令，告诉8259：我CPU马上IRET要结束了，你先结束吧。是不是用命令的形式通知8259结束中断？这种结束中断叫非自动结束。这是我们常选用的一种。明白了吧？因此在这里边包含了这层意思，你看：就一个中断源提出了可屏蔽中断请求，能不能响应，受两个对象的控制：一、看8259对你屏蔽了吗？如果8259对你未屏蔽，是不是有可能要响应？那么这个地方提出中断请求，CPU响应不响应受谁的控制？IF。这是响应过程。然后看服务结束。服务结束是不是有两种服务结束方式？一种自动结束，一种非自动结束。刚才叙述了。那么书上课后有两道题，我叙述一下。第一道题的意思是：如果外……

外部可屏蔽中断源啊，向8259有中断请求。在请求的那个时候，这一级未屏蔽。看见没？是不是它没屏蔽？这个时候有请求了。但，此时CPU关中断了，是不是IF清零了？问：这个请求CPU能否响应？能不能？肯定嘛，能不能？不能。

后面还有一句话，继续问：当CPU过了一会儿，还是STI让IF置1是不是开中断了？继续问：刚才的那个请求CPU能否响应？能否？能！因为在这寄存着呢。理解了没有？

还有一道题。还有一道题就是说：8259对某级中断请求输入屏蔽了，是不是通过这屏蔽了？CPU开中断状态，此时通过一个引脚有中断请求，CPU能响应吗？不能响应。这就看出，人家8259把你都屏蔽了。明白了吗？

因此呢，在8086系统当中，我们的外部可屏蔽中断的概念，以前我们一直讲的是受IF的控制。到了今天，不光光只受IF的控制，是不是还受对8259初始化时，对该中断请求是否屏蔽的概念？啊。

还有，在8259内部，还要掌握的一点是，是这个：内部还有一个控制逻辑。控制逻辑电路。啊，控制逻辑电路对它控制，对它控制，对它也控制等等。起什么作用呢？起这么一个作用。要把它引出来，在这个控制逻辑电路内部包含了哪些东西、部件？

包含了：一，包含了四个ICW寄存器，分别叫ICW——1，从1开始编号的，不是0，我在考虑的啊。ICW2，ICW3，ICW4。这四个ICW寄存器称为初始化命令寄存器，啊，初始化命令寄存器。

给ICW1里面初始化，OUT写8位命令，可以……可以设置8259是单片工作还是级联工作，理解了没有？一。还可以设置8259，IR0到IR7中断请求输入信号的有效形式是高电平还是上升沿。等等这些个工作方式的初始化，是给初始化命令字寄存器ICW1里面写命令完成的。

给ICW2里面写命令字，是给主片……如果是给主片的ICW2写，是给主片写中断类型号基址。中断类型号基址。中断类型号基址指的是IR0级对应的中断类型号。给8259初始化IR0到IR7对应的中断类型号，只需要给IR0级对应的初始化类型号就行了，也就是初始化中断类型号基址。一旦IR0级的中断类型号确定了，那么IR1……

很抱歉，当前模型在尝试调取实时信息时遇到了格式异常。若需要查询实时天气或最新资讯，请尝试使用联网模式（模型名带 -online 后缀）或配置天气/搜索插件。

> ⏱️ **分页状态**: 全篇音频已处理完毕。