好，接下来呢，我们把整个的回调这块，编码器还有解码器这块，我们把这个动作去给它完成了。呃这块来看一下应该怎么去写啊。好，第一个，定义一个encode编码，encoder，这个是你的编码部分。我需要一个src_x，需要一个src_mask。那么这里边做的事情是什么呢？叫得到编码器的一个输出结果。然后接下来我们要做的一件事，即，叫编码器，它这个叫前向传播过程。那src_x是什么呢？叫编码器的一个输入。好，编码器的输入它是啥呢？是batch_size，注意啊，batch_size，还有一个操作就是我们的一个叫seq_len，呃对应的就是我们的一个2 4。那src_mask就是你的输入序列的掩码，输入序列，呃序列的一个掩码。掩码呢，大家还记得，输入序列的掩码，那个掩码呢，还记得我们把它分成了几个头吗？诶分成了几个头啊，所以这个操作就是，叫n_head，这是几个头，seq_len，然后还有一个叫seq_len。这个动作就是我们以前一直在说的那个，8 4 4，呃8 4 4。然后这里边用的是哪个mask呢？是padding_mask，这里边我们要写一下，叫PAD DING，padding_mask。padding_mask在这里边，它从PAD DING，padding_mask在这里边，就是做填充的意思啊，是防止填充的一个操作，填充的pad值，然后影响，呃影响注意力，叫注意力。好，计算结果。好，那接下来，return，那你返回的值是什么呢？就是encoder_output。哎，encoder_output是谁？encoder_output就是这个紫色的这个位置，在这儿。好，那么这里边我们来看一下，这个操作，它做的mask是干嘛的呢？是在这里边注意啊，它做mask是为了更好的展示，它是为了掩码，它是为了计算注意力，这个也是。来，encoder_output，这个是编码器的火炎边，叫编码器的一个输出。好，编码器的输入是2 4，那输出呢，这个结果就是一个，叫2 4 512，一会儿得写这个啊，叫中括号，batch_size，然后seq_len，还有个叫d_model，它对应的维度就是一个2 4 512。来吧，咱看看这里边这个内容应该怎么写啊。首先第一个动作应该是叫对，src_x是谁？src_x是谁？src_x就是这个inputs，在这儿，这个位置，能跟上我思路不？在这儿，对src_x，所以第一步操作就是对，啊s o u r c e，src_x叫进行词嵌入，啊词嵌入处理，然后转成，转成向量。好，那来吧，那怎么做呀？embed，embed_x等于self.src_embed，把它扔进去。那第二个操作就是通过编码器，编码器处理。通过编码器处理，处理什么呢？处理嵌入后，嵌入后的一个数据。所以这个数据是谁呢？return self.点谁呢？encoder，encode就可以了。把谁扔进去啊？把你的embed_x，然后还有src_mask扔进去。就是你的这个src_x是谁呢？经过embed处理以后，你的embed_x是经过位置编码，还有你的一个词嵌入处理之后，这个绿色的点，叫embed_x取它。embed_x跟谁呢？跟src_mask，就是你注意力机制在这里边的掩码，经过把这两个参数传进去，那我的编码器就可以做处理了。return返回的这个结果其实就是这个紫色的这个点，还能跟上我思路吧？这是你的编码器。那么接下来我们来看看解码器它是怎么去做的。解码器呢是也是一样类似的套路，把这个操作给它折叠一下。折叠以后，def呢，这里边写上decoder，decode。好，那么也可以叫它decode，decode。这个叫decode呢，上面这个操作就把这个r去掉吧。编码解码动作啊，叫decode。self，它要的是谁呢？它要的动作比刚才那个就要多了。第一个，我要一个tgt，target y，目标y。我要一个encoder_result，就是这个encoder的一个output吧。output。咳，等一下。我还要一个操作什么呢？src_mask，我还要一个target_mask。来，开始了，跟着我的思路。首先这个操作是得到，得到，得到谁？得到解码器的一个输出结果。好，来，开始，即，即什么？解码器的一个什么？前向传播过程。target y告诉我这是啥？解码器的，解码器的一个输入。解码器的输入在这里边例如，例如谁呢？叫跟上面一样，2 4。所以我想说的是target y，是解码器的输入，就是它，这里边的输入。左边这个操作叫src_x，看下啊，左边这个操作叫src_x在这，2 4。右边叫target y，它也是2 4，因为这是自注意力，所以都是一样的。还能跟上来吗？接下来继续啊。再来，encoder_output这个是什么？这个是，叫，编码器的输出结果。叫谁？2 4 512。那这个是谁？encoder_output是谁？告诉我。就是这个紫色的点，就这。还能看懂不？好，再来，这个src_mask是干啥的？src_mask刚才说了，代表，用的是什么mask？PAD DING，padding。哎呦去，又开始了。PAD DING，代表padding_mask。padding_mask是干啥事的，告诉我。防止填充，填充的pad值，然后干嘛？影响注意力计算结果。好，所以就是padding_mask，是填充的。target这个操作是什么？叫S E N T N，sentence_mask。sentence，呃sentence_mask。那这个操作写一下，叫代表，那虽然都是掩码，但是你掩码掩的不一样。好，那这个是干啥的？防止未来的信息然后被提前，提前什么？提前利用。说白了就是防止偷看，偷看未来的词。未来的词，所以说我要做的掩码。那注意啊，target_mask是给谁用的？target_mask是给下面这个用的，就下面这个自注意力。然后这样的话，你的2 4我要给你遮掩起来，比如说你预测的是第一个句子的第一个词，那好，那我啥都让你看不见。你要预测第一个句子的第三个词，那你只能看到第一个句子的前两个词，第三个第四个看不见。是通过这个target_mask，是用它来掩码的。而你算注意力是通过这个src_mask来做一个注意力的计算。能跟上来吗？到这儿了，它就这几个参数啊在这儿。你一定要对着这个图去看，不看的话你看不同的。return解码器的一个输出结果。这个不用说了，跟刚才一样，2 4 512。来，说说解码器怎么玩的？一，叫对对target y然后进行一个词嵌入处理转成向量，是不是就是embed_y等于self.tgt_embed等于它。第二个动作就是干嘛呀，解码器处理嵌入后的数据。return做个返回，self.decoder就行了，把谁呢？embed_y、encoder_output、src_mask、target_mask，也就是说，你有没有发现一件事，你未来只需要调我就可以了，为啥呢？因为在我这里边，我已经把编码和解码给你封装好了，而不是像之前的，哎呦呵，咱还得在这儿use generator一下这个output。decoder呢，去这里边，哎呦呵，咱还得在这里边use decoder，咱还得在这里边去玩一下怎么怎么样，不需要。而现在这个活就是我已经封装到这个架构，就是我的Transformer，我就已经封装好了。看明白了吗？诶到这就封装好了。那我的Transformer有啥？你告诉我，我的Transformer里面，我有编码动作，我是不是还有解码动作呀？对，那其实原因就在这儿。好了，那这个动作封装完毕以后，咱是不是该测试了？好，那我把这个动作去先保存一下啊。
