好，我们第二章呢，我们在前面介-介绍了一些这个网络应用的一些例子和它相应的协议和工作原理。之后我们再看一下这一章比较特殊的这个socket编程。包括TCP的socket和UDP的socket编程。那我们知道啊，这个应用是怎么实现的？应用要跟另外远端的另外一个远程应用要交换应用报文，来实现应用协议，对吧？实现应用协议的目的是什么呢？那么来实现各种各样的网络应用嘛。刚才我们讲到的这个DNS啊、FTP啊、E-mail、Telnet、Web都是这样的一个情况。但是呢我们的问题是说，你的应用怎么样把message这个报文传到远端的另外一个应用呢？那是不是一个问题啊？就是说你的应用进程，这是一个另外一个远端的进程。对它来说，那个是一个远端进程。你怎么样把message发过来？然后呢对方怎么样呢能够反转回来相应的message？按照这个应用层协议的这种规范，那么我来解析你的报文，然后知道你要干嘛，然后呢形成相应的response报文再给你。啊，当然要不要返回给response报文还要应用层协议来规范啊。但是呢这个报文是怎么样传来传去的呢？是不是要借助于下层提供的这个通信才能够去做？对吧？两个应用进程是不能够完全，不能够直接来传输这个报文，不能够直接地隔空来传报文。必须要通过层间的接口交纳给下层，由下层所提供的服务，下层所包括的服务它包括什么呢？这个TCP，另外一个TCP，还包括底层所有的协议，和包括中间网络的一些这个网络所有的设备一起向上提供的这种服务。那这样的话呢，这边呢把报文物理的交下去，这边呢把报文物理的层层收到。啊，这个为什么要画成一个虚线呢？因为它是逻辑的。它真正的通信呢要交给下层，通过层间的接口，特别通过应用层和传输层的界面上面的这个socket，把报-报文交下去，对方呢才能够收到相应的这个报文，反过来也是一样。反过来也是一样。所以说呢应用进程之间要远程的传输报文，必须要借助于传输层所提供的服务才能够实现。那么传输层提供的服务在TCP/IP协议栈，它提供什么样的服务呢？提供的是这种socket API的这种服务。就应用进程调用一系列的socket API应用程序接口来创建socket，来使用socket来传、收，来关闭socket，这样的一种情况。所以说呢，在在什么地方把报文交下去？界面上。具体的来说是界面当中的这个socket位置上。对吧？然后呢什么形式？socket API的形式。然后呢你要传报文之前，你首先要建立socket。建立完有效的socket之后呢，借助于这个socket呢再进行收和发。收发完了之后呢，你再把这个socket使用完毕的socket再把它关掉，采用这种方法。就具体的什么地方？层间的接口上面的socket。然后呢什么形式？socket API的这种形式。所以说呢socket就像一个门一样。那么一个进程把这个东西往下交，对方的应用进程呢就能够收到相应的这个报文，反过来也是一样，就像一个门一样啊。你可以认为呢这个整个的都是传输层，包括传输层，包括这个IP的层次，包括更底的层次，包括中间的所有的链路，一起向上提供的这种服务，就这样的一个服务。就像一个你把它想象成一个门，这边推出去，这边呢就能够收到啊，就能够收到。应用进程啊是不需要考虑这个报文具体是怎么样传的，我只要借助于socket API，建socket，使用socket传就完了。然后呢这个使用这个socket收就完了，然后最后我们把socket把它关掉就完事了。那具体的，socket在下层收到这个报文之后，这个报文对于下层来说是它的SDU啊，对不对？收到这个SDU之后，具体怎么样去传，那是另外一回事，那是下层要讲的事。啊，我们是top-down top-down来讲。我们这一章呢不会讲，但是呢我们这章会讲这个应用怎么样使用层间接口上的socket上面的socket API来创建socket，使用socket，关闭socket，从而让两个应用进程可以逻辑的交换应用报文，实现应用协议，实现各种各样的网络应用，是这样吗？就本层协议的实现是为了向上层提供服务。但应用层它没有上层，那应用层的上层是什么？用户。应用层的上层可能是其它的应用，是这样吗？那么是这样的一种情况。我们讲的场景是这样，我们来看一下这个应用进程怎么样建立socket，使用socket来传和收，来关闭socket，这样的一个情况。那么这个应用层下面的这个传输层，在TCP/IP协议栈当中呢，它的传输层有两种类型的协议，一个呢叫TCP，另外一种叫UDP。TCP提供的是可靠的字节流的服务。就这边传过去是什么，对方就能够收到什么，原原本本。不错、不重复、不丢失、不失序，原原本本。但是呢它是一个字节流的服务，就byte stream的服务。字节流服务的概念是什么呢？它保证你一个按照流来说是对的，但是不保证报文的界限。所以说我们在很多的這個应用协议当中呢，都能看到这样的一个痕迹，什么样痕迹呢？应用报文报文之间的界限应用得自己维护，是这意思吗？应用得自己维护啊。我们学了很多的例子都有这个痕迹，大家可以去看一看。而UDP提供的是不可靠的服务，而且是无连接的服务。TCP是提供的连接的服务。所谓连接指的是什么？两个应用进程之间在通信之前打个招呼，你好吗？他说还还行啊，然后呢之后呢就在通信。UDP不管，上来就问："哎，那个域名解析告诉我啊。"然后对方就把域名解析换来给我，把域名解析的结果给我，没有一个打招呼的这样的一个过程。直接来实质性的通信。啊，就是干货，上来就干货。不需不需要这种这个，不需要这种这个握手，啊，没有握手的这样一个过程。那我们来看一下这个TCP socket的这种编程。socket我们前面的例子学过，它就是一个整数。上下层之间的一个约定。TCP的socket这样一个整数代表了什么？就像一个文件句柄一样，我打开一个文件，返回一个文件句柄，一个整数。我对这个整数的操作，读和写就是对这个文件的操作。我对这个socket的操作，就是对这个会话上的这个两个应用进程之间的这样操作。TCP的socket这个整数代表了什么？我的IP，我的TCP端口，对方的IP，对方TCP的端口。这个两个进程之间的连接关系的一个代表，是这意思吗？对吧？两个端节点的一个代表，前面我们学过。我对这个socket发，就是发给对方应用进程。我从这个socket去收，就从对方的应用进程当中来传过来的这样的一个报文。对方哪个应用进程？这个应用进程，我这个IP这个端口，对方那个IP那个TCP的端口，所对应的两个应用进程之间的一个会话关系，是这样的一种情况。然后呢从一个进程向另外一个进程可靠的传输字节流。它也就是说两个15k字节，可能到最后收到是一个30k-30k的一个字节。这个传输层它是不保证这个报文和报文之间的界限。然后呢我们来看一下啊，就是说这个整个的这个工作过程是什么样的一个工作过程。首先呢，服务器先运行。服务器在CS模式当中，服务器是不是要先运行啊？客户端先运行，你跟谁打招呼啊，那服务器都没起来呢。所以服务器先运行。创建一个socket。创建一个socket的目的是什么呢？返回一个整数。但这个整数呢什么都不是。之后呢，再把创建的这个socket的这个整数和本地的IP和端口相捆绑。这个socket我们把它叫welcoming socket，叫欢迎socket。好吧？欢迎socket。之后呢，再调用socket API的另外一个函数叫accept。就是接收来自于这个socket来自于远端的另外一些进程呢跟它的握手的关系。接收来自于其他人跟它建立TCP连接，好吧？但是呢你要调用accept之后啊，如果这时候没有人连接，连接没来，怎么办呢？这个函数就只能阻塞的，不走，不往下走，明白我意思吧？阻塞式的，不往下走，好吧？呃，我不知道有没有讲明白啊。就是说一会我画张图可能会更清楚一点。首先呢创建一个socket，创建socket是socket API当中的一个，第一个。创建socket实际上操作系统给它返回一个整数。在socket的表项当中创了一个表项。socket的值，本地IP本地端口。本地IP本地端口。然后呢，再bind。我创这个整数跟我本地的哪个IP地址和相应TCP端口相捆绑呢？就把它，就是说这个整数跟本地的进行捆绑。然后呢，再对这个socket我在accept，在调用另外一个函数accept，就等待来自于用户的连接建立请求。如果这时候没有用户连接建立请求，那就等着，好吧？是这样的一种情况。好，我们先讲一遍啊，一会儿我们再画张图啊可能会更清楚。

所以这时候你就觉得这个语言呢是非常这个苍白无力的啊，就是说要靠比划啊比划，这这个都用上哎也不行。

然后呢，
我们再把整个的过程讲一讲啊，然后呢我们再这个结合这个代码，结合一张这个示意图，让大家更清楚地这个socket是怎么样建立起来。这个表象是怎么样个情况哈，大概是这样过程。创建、捆绑、等待。好吧，这都是socket API当中的一些函数。socket API是一族函数，一套函数啊，它不是一个函数。之后呢，
客户端
是不是也要创建socket？
客户端也要返回一个整数。
然后呢隐式地，我的客户端我创建的这个整数和我本地的IP和TCP的端口要做出捆绑。
隐式的。
它不见得要调，但是呢你要不调的话呢，
客户端返回的这个socket的这样的一整数，就跟默认的当前没有用的一个端口相捆绑，好吧？
啊你哎这个呃稍安勿躁啊，就是说这个我们
这个交待的东西稍微多一点。之后呢，在调用socket API当中另外一个叫connect，
连接，就指定这个创建的socket作为一个参数，
connect。connect之后呢，
这个他也阻塞，除非
服务器
告诉他同意建立连接。如果不同意的话呢，或者怎么样，他一直在阻塞着啊，阻塞的。或者说在同意的包发出去之后呢，对方呢还没来，他一直在阻塞。这时候呢，如果来了
对方连接建立请求的确认，
这时候呢他的连接
这个socket API
就会返回一个有效值，这时候连接就建立起来。然后呢对于服务器来说呢，我前面不是在那accept嘛，在那等嘛等嘛等嘛，对吧？等啊。
然后呢这时候来了一个连接建立请求，他的TCP一方面同意建立他的连接请求。
同意连接建立请求的时候呢，客户端这时候呢他的connect就会解除阻塞，返回一个有效值，好吧？
另外一方面呢，就是说你看啊，前面我们不是创建了一个welcome socket嘛？
是吧？welcome socket它在等着。
然后accept这个welcome socket，等待用户连接建立请求。这时候来了一个用户连接建立请求，
怎么办？
又返回一个新的talk socket值。
这个socket值叫connection socket。
这socket值呢，仍然是原来的那个IP，服务器端IP守候的端口。但是呢跟客户端的那个IP和相应的端口相捆绑。所以说呢这个connection socket跟原来的welcome socket是不是都守候在原来的这个
服务器端的IP和相应的端口上？但是connect socket跟什么相捆绑？
跟服务器端本地的IP、守候的端口和客户端的IP、客户端的端口相捆绑。这时候accept解除阻塞，
就返回了connection socket。之后呢，这个连接就建立起来了。然后在这个连接上就可以收和发。
连接建立完了之后呢，我可以把connection socket把它关掉。
原来的welcome socket仍然在那守候着，好吧？
呃讲的稍微有点有点这个用语言描述可能比画对吧，可能不行，好吧？然后呢我们在刚才不是比嘛，马上再去画啊。
我们结合一个应用场景的例子来看啊，这个应用场景的例子是什么呢？就是客户端是一个应用进程。
这个应用进程干什么呢？从标准输入那里输入
keyboard，keyboard输入的一些字符串。
标准输入不是这个键盘吗？
然后呢通过socket把它发走，发给对方。
啊就是说收到一串小写的字符，
通过socket把它发走，发给服务器server。
服务器呢干嘛呢？把它转换成大写。
然后呢再通过建立起来socket反转回来，
传送给它。然后再通过标准输出的这个中断，就显示器把它显示出来。你说那不有病嘛，对，
你说把一个小写的变成一个大写的，然后你本地可以做啊，它其实就是一个例子对吧，这就是一个例子。什么样一个情况呢？就是说客户端发出了一系列的信息给服务器，服务器做了一些处理之后，然后再反转回来。这个处理的例子呢我们就讲了一个大小写的转换，虽然本地也可以做，但是呢我们是以这个例子来讲。就是说本地的处理不做，让远端服务器去做，好吧，这样的一种情况。
标准输入
输入一串字符串，通过建立起的socket
实际上发给对方，逻辑上是这样，路由也是这样发。对方收到这个小写的字符串之后呢，逻辑地再把这东西发回了发回来，实际上是通过建立起来的socket往下发，对方通过建立起来客户端建立起来的
客户端的这个socket能够收到这个报文，收到这个大写的转换之后的这个字符串，然后呢再通过标准的输出终端输出终端把它显示出来。那这样的话呢这个客户端应用进程从用户的角度来看是什么呢？输入一段字符串，按一个回车，一串大写的字符串来自于远端转换完毕的
这个大写的字符串就反转回来。他觉得很高大，就不是本地转换的，是远端服务器转换的。就是这样的一个例子，我们来看一下在客户端要做哪些工作，服务器要做哪些工作。那我们来看啊。
好，我们在讲这个之前啊，我们首先把两个重要的结构体稍微介绍一下，两个重要的结构体介绍一下。
画得有点快啊。
好，这是一个结构体，一会儿我们要反复用到，所以说我们先把它画下来。它有这几个这个域啊，有这几个这个组成部分。family，地址族啊，这个数数据结构的的这个定义啊不仅仅用于IP的通信，也可以用于IPX的通信，好吧？
地址族。所以说呢你要给它一个常量，就是表示是TCP/IP的协议族，明白我意思吗？就代表你用在TCP/IP还是用在IPX这种通信。
然后呢这是一个
端口号。
这个字段呢，代表的是IP的地址。
然后这个呢是起了一个对齐的作用，因为IPX的地址，IPX另外一种网，Novell网啊，这现在已经基本不用了。它的地址长度呢要比它还要长一些，要比它还要长一些。还有其他的一些地址，AppleTalk的这种地址，可能还要比它还要长一些，所以说呢它也起个对齐的作用。如果是IP地址的话呢，只是取它的这个这个部分，好吧？
就32位的IP地址。好，这是第一个这个数据结构。第二个数据结构呢，我们来看一下啊。
哇，写得快点。
这个
结构体呢它是这样，给一个指针，一个串的指针。
这里放的是什么呢？主机的域名。
然后呢这里给了一个指针的指针，实际上是一个二维的，主机的一系列的别名。
好吧，这个很容易理解，大家都学过C语言，好吧？
然后这里呢给出来一个地址的长度。
是这个32位还是多少位，对吧？这里呢给出来是一个IP地址的一个列表。
IP地址的列表，因为可能对应了不同的IP地址。
那第一个，IP0，啊，我们就代表
这个我们解析出来之后的IP地址。所以说这个主要作用是什么呢？作为域名解析的一个函数调用。
域名解析的函数调用。
然后呢作为它的一个输入值，作为它的一个返回值。返回值当中在这里放的是什么呢？
域名，
这里呢放的是IP地址。
明白我意思吗？放的是IP地址。
好，我们开始啊。

两个结构体。这个结构体的主要作用是什么呢？做域名解析，这个函数调用的时候呢，这个输入的是域名，最后得到的这个IP地址。最后呢，把这个结构体当中的IP地址啊，把它放到这里。放到这个结构体的这里，把它copy过来，好把？就这个作用。copy过来，然后呢，这个端口号呢，再通过赋值的方法赋上相应的端口号。因此呢，这里放的是什么什么地址啊？放的是一个两个TCP通信时候的一个端节点，或者UDP通信的时候一个端节点。就是说服务器守候的IP地址和相应的端口号，我可以用一个结构体来表征，用一个结构体的一个变量来表征。明白我意思吧？好吧？就这个结构体主要作用是什么呢？放IP、放端口。来代表的是这个应用进程本底的什么？端节点。本地的IP地址和相应的端口号啊。对吧？我们可用这个东西来表示。可以用这东西来表示。所以说呢，在这里放IP地址，这里放端口号，代表的是两个TCP进程通信时候的一个端节点，一个端节点。好，这个结构体讲完了，之后我们来看一下这个整个的这个过程啊。刚才，刚才使笔，现在我们就稍微画一下啊。稍微画一下。首先呢，服务器要运行这个socket这样一个函数啊。socket函数干什么呢？返回一个整数。这个socket呢，是什么？叫welcome socket，是welcome socket。第一，我我简写啊。哎，这就是welcome socket。然后呢，等于，这是一个整形变量，然后呢，等于socket，是API当中的一个，第一个，建立一个socket，操作系统就返回一个整数。这个整数是什么呢？假如是8888，啊，很吉利，啊，8888。然后呢，端口号、IP地址还没赋呢，对吗？还没有赋呢，啊，先不管。然后呢，在这个socket当中，有一个参数，指明你建立起来这个socket到底是一个TCP socket，还是一个UDP socket，它的参数是不一样的。好吧？TCP socket的话，你在参数当中给它相应的值，那么它建立起的socket就是TCP的socket。反过来呢，就是UDP的socket。甚至呢，可以是一个IP的socket。啊，可以吗？可以。然后第二步呢，要建立一个bind。哎，你弄了这样的一个整数值，跟我本地的哪个端口相捆绑呢？本地的IP相应的端口是什么呢？当然，我要借用这个函数，把这个值，welcome，然后呢，还要和本地的叫SAD，这SAD呢，就是这个结构体。明白了吗？当然在结构体之前我们要先赋值啊。啊，这个写的不是太清楚。SAD。啊，SAD。SAD就是那种类型的结构体，明白我意思吗？所以说呢，bind，我创建的一个整数，socket值和我本地的某个IP，某个端口号相捆绑。假如呢，我们现在举的这个例子Server守候在几？呃，Server的IP地址是1.1.1.1，然后呢，守候在80号端口。好吧？就相当于我创建了一个整数，跟我1.1.1服务器当时在80号端口相捆绑。服务器守候在1.1.1.1，80号端口。那么，客户端呢，是在2.2.2.2，777端口，好吧？假如是这样的一个情况。777端口。然后呢，第二步呢，我创建了一个socket之后呢，把这个socket的整数值跟我本地的IP和端口相捆绑。明白了吗？好。在这个表象当中，这个表象是应用层和特别是传输层所用的一个表象，好吧？之后呢，第三步，我要建立一个connect socket，我就简写啊，等于，啊，accept，啊，过了啊。在CP，连接。welcome socket。后面的参数我就稍微越界一点吧，啊，越界越界。connect socket等于accept，另外一个socket API。然后呢，相当于在这个welcome socket，在这个8888这个socket当中去等待来自用户的连接建立请求，可以吗？就是说我创建了一个整数socket值，把这个socket值跟我本地的IP和端口相捆绑。捆绑完了之后呢，我在等待在这个端口，在这个socket上等待来自于客户端的连接建立请求。如果调用这个函数的时候，socket API的时候，没有任何一个用户跟它建立连接请求，它干嘛？这不阻塞着，不走，啊，不走。那么这样的话呢，我们再看一下客户端的这样的一种情况。客户端的情况呢相当于这样。首先呢，这是第几步了？第四步。我们来要创建client socket。创建一个client socket。client socket。也是创建一个整数。然后呢，在这个socket当中，这个socket API，在这个socket参数当中指明是一个TCP socket，它返回一个整数。假如返回的是嗯，给它一个2吧，2222，这样的一个socket。然后呢，在客户端它不需要bind，不需要bind，因为客户端所用的端口号无所谓啊。服务器端它需要bind，为什么呢？因为要在这个端口上等待来自于客户端的连接建立请求啊。那么你这个东西不绑定的话呢，那么客户端找谁啊？所以说这一定要确认，一定要是确定的。他就在他的80号端口，等待着用户连接的请求。客户端呢不需要bind，但是呢实际上有一个隐晦的bind，你不bind实际上操作系统给你bind。啊。然后呢，再指明了CAD。CAD呢，给它的地址啊，地址的符号呢，我们就简写啊，这个，它一个地址。CAD是什么呢？CAD是这种结构体。相当于客户端的IP地址和777的这个端口，相当于我建立了这个整数和我客户端的IP 2.2.2.2，777端口，相捆绑。明白吧？相捆绑。但是呢，这个socket它的状态，我这里一个表象，还应该有个状态，这个状态还是一个无效的一个状态，对吗？无效的状态。bind完了之后呢，我再第六步，再connect。这也是socket API当中的一个，客户端的，然后呢，跟服务器端的SAD，当然，SAD之前要赋值，给它一个地址，啊。这个呢是socket API当中的一个，这个是socket API当中的一个，这个是socket API当中的一个，这也是，这也是，这也是，好吧。之后呢，怎么的？我建立起这个socket，返回一个整数2222，我把2222跟我本地的端口和IP地址，IP地址和端口相捆绑。同时呢，我要调用这个socket API，在这个socket API当中另外一个参数当中指明了1.1.1和80号端口的一个捆绑关系。那这样的话在这个表象当中对方的IP，对方的端口就有了一个实际的值了。他调用完了之后，阻塞的，相当于在底层啊，它的TCP要向对方发一个连接建立请求，这时候呢，他才会解除阻塞。同时呢，对方的TCP要给出一个应答，它的connect才返回一个有效的值。这时候连接才是一个真正连接建立的一个状态。明白我意思吗？好吧？是这意思吧？创建socket，隐式的把这个创建的整数值和我本地的IP端口相捆绑。然后呢，再调用connect，在参数当中指明对方的IP，对方的端口。那这样的话呢，实际上调用完了之后，应用层上下面的TCP的这个实体要向对方发出一个连接建立请求，然后呢，对方要给出一个应答，它会解除阻塞，连接才是一个真正建被建立起来的一个状态。好吧？然后呢，这个连接请求发出来之后呢，这个connect，这个accept原语就会解除阻塞。解除阻塞之后呢，它会返回一个新的值，新的值，假如呢把这个值呢叫做88999，啊，8899吧，啊，8899。
它的端口和IP是
1.1.1，80端口是我的。
然后对方的是什么呢？
是2.2.2.2，
777端口。
这是呢connection socket，
这个是什么socket？叫welcome socket。
connection socket呢就是返回一个有效的值了。
这时候呢这个连接呢就是一个有效状态。
啊，这就是一个有效状态。
之后呢，
第几步呢？就是第六步，啊，第七步的话呢，
它会返回一个这样的一个情况。
然后呢这个表项就有了这样的一个值。
啊。
然后呢这个
对方的这个connect就会返回一个有效值，这个连接就建立起来了。
之后呢，它再发出一个send，啊，第八。
send
to
再借助这个socket，
然后呢，再发发的是什么东西，
也是作为一个参数。
这事实是不是也是一个socket API？
使用这个2.2.2.2，
调用这个函数，
2.2.2.2这个socket的值作为其中的一个参数
喂给它。
然后呢同时把要传输的东西作为参数
给它。
之后呢，
交到下层，下层封装成一个TCP的报文段，
给了这边。
当然它在这个之前呢，
要
调用什么呢？要调用read，
read
这个connection socket作为其中的一个参数。
之后呢，
这样的话呢，你发送我就可以接了。
我收到之后呢，
我在本地，在9和10之间，我要对
传过来的东西干嘛？我把它从小写变成大写。
之后呢，再利用这个
write，啊，
翻转给你，啊，再翻转给你。
再翻转给你。那这样的话呢，
这个对方调用send之后呢，
再
再什么呢？在read，啊这里可能是写错了，不一定是sendto，可能是write，这里应该是read，啊。
read，
把这个socket拿来，
然后read的东西是什么？
就是对方转换完毕之后，
得到了大写的字符串。
之后呢，就可以在终端当中把它显示出来。最后再close掉这个socket。
然后对方呢，先read，建立起socket之后先read再write。
然后呢再把这个，
这个什么呢？第11，再把它close掉。
close掉，那这样的话这个表项就删掉了。
然后呢，
程序依然回到accept这里。
这个端口，啊，这个socket仍然有效。
等待着用户的下一个连接建立请求。
对吧？等待着用户下一个连接建立请求，啊。
是这样的一个情况，那你看看，
服务器端先建立socket，把socket跟本地的
IP与相-端口号相捆绑，
然后呢在这个端口上等待用户连接建立请求，一直阻塞着。
用户连接建立请求来了，它会返回一个有效值。
这个值就是这个新的socket，这个socket叫welcome socket，代表的是我跟对方的连-一个连接。
之后呢，我在这socket上去读，
读完之后呢，转换大小，
大写。然后呢，在这个socket当中去写，实际上发给对方。
然后呢再把socket关掉，然后再反转回来，在这里再等待用户连接建立请求。
当然了，
这个你可以把它变成一个多进程。
怎么样多进程呢？
就说这块呢accept之后，
它是一个有效值了，你可以产生一个新的进程。
这个进程呢，
为客户端提供大小写转换的服务，老的进程呢仍然在这里。
明白了吧？
等待着一个新的用户连接建立请求。我我的进程，原来老进程一直守候在welcome socket那里。
来了一个新的连接建立请求，我产生一个新的socket值，我再fork一个新的进程，用这个进程向它提供服务，而我老的进程仍然守候在那里。
然后又其他的人跟我建立连接建立请求，我产生了一个新的socket值，然后再fork一个新的进程，然后用这个进程呢再向用户提供服务。
然后我老的进程仍然守候在这里，这样的话就变成一个多进程的一个，这样的一服务了。啊，你说。
老师有一个问题，就是在多进程的时候，你8899在处理就是通信的时候，你用的还是80号端口，那新的服务，就是其那80号端口还能作为welcome socket吗？
80号端口是welcome socket，一直是等待着新的用户连接建立请求。
对，但是你8899这时候也在用80号socket，那对，所以说这个TCP socket它的这个特点，
TCP的socket我们前面说了，它是四元组的一个本地代表。
这一个socket值代表的是本地IP 80号端口和对方IP
相应的端口。
然后呢新的一个socket值仍然是我本地的这个IP 80号端口，
可能跟对方的另外一个IP和相应端口相捆绑。
这个概念呢还是前面的一个概念，什么概念呢？就是说
TCP的socket值啊，
这个相当于是个整数，代表的是一个会话的关系，好吧？
你再去翻一下我们前面介绍的TCP socket的这样概念，
而UDP的socket是什么呢？
是本地的IP和端口一个整数代表。
好吧，这是前面的铺垫应该讲得比较清楚了，
你再去回去看一下这方面的一些东西，好吧？
我的意思就是说两两个进程会同时 可以。 同一个端口。 可以，
可以，
可以的，没问题。
对。
那个两个进程可以守候同一个端口，但是它们的socket不一样。
但是呢它们的socket代表的是什么呢？就是说
这两个socket都指的是同一个
IP和相应端口，
但是代表的是
对方的IP和相应端口任何一个可以不一样。两个进程
都可以使用同样的一个端口号，都是可以的。那怎么
那怎么区分发进来这个端口的时候到底是哪个？
就你比如怎么区分，他它发过来的信息里面有 那你看啊，
你看，你发过来的东西你看你的，你你是底下的TCP的段不是发过来了吗？发过来呢有本地的IP和对方的IP，
你发过来是不是也有相应的端口和对方的端口？
跟这个不就对上了嘛！啊，就这样的一个情况，这是完备的，好吧？这是完备的。
好吧。
那整个的过程大概是这样的一个过程，好吧？
那么这个黑色的部分代表是底下
TCP交互的
这样的一个过程；
红色的代表是应用报文交互的这样一个过程。
到底是先发，先发再说还是先收再发？
那是取决于应用协议到底怎么定义的，
这样一个事情，好吧？
那是应用协议到底怎么定义的事情。
好吧。
然后呢这个呢是结构体的这个定义，左边这个结构体的这个定义。
然后呢
这个呢是右边的这个结构体的这个定义。
那么来看一下这个客户端的这个代码，啊。
那么客户端
有一个参数，
啊，这个参数有两个参数，
第一个参数指的是服务器端的域名，
第二个参数要指明
服务器端守候的端口号，对吗？
所以说呢在这个
argv当中，1当中放的是什么？
放的是主机的名字。
我客户端要运行的时候呢，必须要提供两个参数。第一个参数是什么呢？服务器它的域名是啥？
第二个呢就是服务器它对应的端口号是什么，
是这个意思吧？
所以说呢我的这个应用程序在启动的时候呢，必须要输入两个参数。
那么第一个参数放在argv，
argument argument的这个1当中，第二个参数放在argument 2当中，是这样吧？
都是字符串的形式放进来的。
那你猜argv 0是什么？
argv 0是什么？
大家学过C语言，argv 0是什么？
这个应用程序本身的名字啊，
你执行程序的名字啊。
然后我们来看一下，你看这个结构体sad，
这个客户端的这个程序，sad是服务器端的address。
就是说我是客户端，我得用一个结构体来描述服务器守候的IP和相应端口号，
明白我的意思吗？
好吧？
相应端口号。
然后呢你看一下这个socket，
客户端socket就是一个整数。
然后呢这是个结构体，
那边右边的这个，这样的一个结构体，
结构体的一个指针，
结构体的一个指针。
然后呢定义了两个char的字符，
sentence和modified sentence，
啊，modified sentence。
然后呢，
用户在输入的时候呢两个参数，第一个参数呢放在argv 1当中，第二个参数放在argv 2当中。
把argv 1当中的这个呢，放在一个
字符串，啊，host当中。
当然host前面没定义啊，这个我们是一个示例啊。
然后呢port，
这个是一个整型的变量，
啊，整型的变量，也没定义啊，这是个示例。
要把它从字符串转换成整型，
变成
port的变量的值。
明白了吗？那这时候呢host放的是主机的名字，
服务器的名字，port放的是什么呢？
以整型
放的是什么呢？
这个服务器守候的端口号。在这我们这个例子当中，
这个是什么呢？这是80，
而这里放的是什么？
是域名。
之后呢，干嘛呢？
你看，创建socket。
在这个参数当中指明的是Internet的
一组建 建立起来是一个TCP的socket，好吧？ 然后呢，这个是干什么呢？刚才你不是有一个这个变量，my_sad嘛，这个结构体的变量嘛，sad先清零啊。 sad 是一个结构体， 结构体的变量。 然后呢，他就分配给内存给你， 但内存当中可能有一些乱七八糟的东西把它清理。 清理完了之后呢，对sad的这个family赋值， 赋Internet的地址族。 赋Internet地址族，好吧？ 然后呢这个port呢， 要把这个，你前面通过这个参数不是把它转换成一个 整型了吗？ 要把，首先呢强制转换成一个短整型，无符号的短整型。 无符号的短整型， 然后呢再把它变成一个网络次序。 这网络次序指的是这个什么呢，就 因为不同的这个框架的计算机，它表示一个整数在发送的时候， 可能这个大，有个大端小端的问题。 啊，就是 呃俗话说，就是说， 说低、高位到底是在左边还是在右边，或者怎么样形式，把它变成网络次序。 那这样的话呢，不同这个框架表示的这个设备啊，通过网络传不会有歧义，你否则的话呢原来是0000 1111， 然后到你那儿之后呢，不做任何的转换，可能对方会收到1111 0000。 啊我不知道有没有讲明白。 调了个个儿。 好，调了个个儿，啊。 高位和低位调了个个儿，可能会这种情况。 所以说呢要强制变成一个短短整型。 那你看啊， gethostbyname， 作一个参数， 这是一个函数调用。这个函数调用是什么样一个函数调用？是不是调用解析器啊？ 输入的是主机的域名， 得到的是一个什么呢？ 得到的是一个结构体的一个指针，在这个指针当中，这个位置放了什么呢？ IP地址。 这是干嘛？就应用进程， 客户端应用进程调用解析器， 然后呢在这个，这个调用这个函数的时候呢， 给的是主机的域名，然后呢希望在这个结构体指针当中的这个位置放了 相应有效的IP地址。 然后呢第二步是什么呢？再把IP地址的 这块， 把它拷贝到这个结构体的这块，明白了吗？ 那这样的话呢，这个位置已经放了端口号了吗？ 这个位置放了 IP地址了吗？这地方已经放了Internet的地址族了吗？ 那么这sad的这个变量， 结构体的变量， 是不是放了哪些信息啊？ sad的结构体变量放了什么信息啊？ 服务器端的 IP地址 和 80号端口。 好不好？是这样子的情况，好吧。 之后呢， 再调用connect 函数，API。 connect函数呢，前面你不是建立起这个socket了吗？ 那么，然后呢为什么没有bind呢？我们前面讲有一个bind。 在客户端有一个bind，为什么这里没有bind？ 隐式的，你不调用的话它就，它就默认地搞一个这个当前没有用的端口号给你，按照一定规则，对吧？ 但呢就是说，不管怎么样，它就有一个隐式的一个bind。 那这样的话呢，本地IP和77端口， 然后呢在这个参数当中啊， 要指明 对方的IP 和对方的80号端口。 然后呢，把这个函数调用之后呢， TCP要发出一个连接建立请求， 它才会解除阻塞。 然后呢，这个才会返回。返回之后，它才会返回一个有效的值。 好了，这个连接 建立完了之后， get_sentence 是干嘛？ 从这个标准输入设备当中输入用户输入的字符串， 小写的。 然后呢，write。 再通过这个socket API， 通过这个socket值， 然后呢把这个用户输入的字符串把它打走， 对吧？ 打走完了之后呢，再read，再从这个 socket当中去读， 来自于这个socket，来自于 这个服务器端的 发过来的 转换完毕之后的大写字符串。 之后呢， 在这个printf把它打印出来。 然后把它close掉。 这样子的情况。 你看， 建socket， 隐式捆绑socket，连接socket， 使用这个socket 读，读，写，写和读， 关闭socket，好吧，关闭socket。 整个的过程就这样。 那么当然前面呢还有一些这个赋值的一些情况，对吧？ 这个呢，都是些铺垫。 啊都是些铺垫。关键的是什么呢？ 建立socket，捆绑socket，连接socket， 读和，写和读，这样一个情况。 那服务器端呢也是类似，但服务器端 TCP上面的这个应用进程啊， 它只有一个参数，这个参数是什么呢？ 就是本地守候的端口号， 本地本 当前应用进程守候的端口号，它给的参数是什么？80。 在我们这个例子当中给的这个80。 你看，它有两个结构体， cad、sad。sad放的是什么呢？我作为服务器本地的 这个端节点。 cad放的是client， 另外一个应用进程的 一个端节点的一个结构体变量， 好吧，结构体的变量。 这个呢，sad、cad放了什么呢？IP 和 port。 然后呢，这个cad放的也是IP和port，只不过是一个服务器端，一个客户端，好吧。 然后呢有两个socket，都是整数，你看到没有？ 你看打开文件句柄的时候也是返回一个整数。你看，在这个 呃操作系统编程啊， 那么你打开一个句柄，还打开一个 这个应用进程之间的连接，返回的就是一个 socket值，或返回了一个句柄。 对这个整数操作，就是对这个文件，或者是网络流，网络进程之间的通信的操作，就这样的情况。 然后呢welcome socket和connection socket。welcome socket 只是说本地的IP和端口有效，对方IP和端口号没有。 啊这是一个welcome，当然有相对应的状态 对应了什么东西， 对应了这个，它是一个welcome socket，还是一个 connection socket。如果是个welcome socket和connection socket的时候呢， 它有一系列的状态， 啊状态。当然，这位同学问得很好，到底交个哪个应用进程？ 相当于还有一个PID， process的ID号， 对吧？然后你来了报文， TCP的报文段， 我的IP、端口，对方的IP、对方的端口， 然后相对应的socket值，这个socket对应哪一个PID，哪一个应用进程，就交个哪一个应用进程，就这样的情况。这是操作系统所维护的这个信息，啊操作系统所维护的信息。 那你看啊， 呃 这是一个结构体的指针， 指的是什么呢？那种结构体。 然后呢， 这个sentence， capitalized sentence，就转换完之后了这个东西。 port 参数呢是一个字符串，你把它变成整数。 然后呢，你建立welcome socket， 建立完了之后返回一个这个东西。 然后呢你是不是还要 把这个sad赋值啊？ sad怎么赋值啊？ 就sad是我这个服务器本地的IP和相对应的端口号，好吧？ 相应地要赋值。 然后呢这个地址族， 然后呢这个当前的IP地址， 有一个常量 代表了本地的这个IP地址。 然后呢把输入的参数 强制转换成短整型， 然后变成网络次序， 赋在 这个绿色的字段，啊赋在这个绿色字段之上的这个字段，赋在这个。 啊把它变成网络次序，net，呃network sequence。 然后呢再bind。你不是建立socket了吗？ 然后呢，建立socket这个值 和本地的IP和端口，这不是放在sad的结构体变量当中吗？ 把它绑在一起。 这两个是放在sad的这个结构体当中了， 然后呢把它两个绑在一起， 相当于在这个表项当中，这两个 字段就有效了。 之后呢， 我们先别讲listen啊，我们先讲while，啊。 这块呢就while 1， while 1的话干嘛呢？ connection socket等于accept 这个东西。我建立起了这个socket， 我我在这个等待着 用户的连接建立请求。 如果呢没有用户连接建立请求，那是阻塞的，如果有的话呢我返回一个新的socket值。 这个socket值 和它的值不一样，代表的是本地IP、本地端口，对方IP、对方端口。 当然了还有一个状态的一些情况，还有PID的一些情况。 PID的一些情况。 之后呢， 这个连接建立起来了， 我干嘛呢？ 我在这个socket上进行 读， read， read是什么呢？就是对方发给我来的报文， 然后呢把它转换成大写，然后呢再write出去，再通过这个socket write出去，再打给对方，然后呢再把它close掉， 然后再反转回来，while 1依然在循环。 listen干什么呢？listen是这样，就是说 我在listen的队列 放置了一个队列，队列的长度呢是10个。 你看，来了请求， 我向你提供服务； 来了请求， 我建立socket向你提供服务。如果在我在服务的时候， 又来了连接的请求， 这时候呢， 连接的请求放在队列当中， 队列长度是10个。 然后呢我循环完了之后呢， 直接从队列当中再拿一个这个东西，直接 就知道又有人给我建立连接建立请求了，连接建立请求了。 这样的一个情况。 如果 我在服务别人的时候呢，别人连接的请求放在这个队列当中， 队列长度是10个，如果超过了10个的话，怎么办？ 拒绝了。 啊拒绝了。这样的一，这样的一个情况。 所以说呢这个队列呢是等待连接建立的这个队列。当然你可以 按照我们刚才那种方法啊， 就把一个 单进程呢，把它变成多进程，啊把它变成多进程。 也可以这个生成另外一些进程， 然后呢， welcome socket， 老的进程仍然守候在welcome socket， 来的一个新连接建立请求， connection socket返回一个有效值，你再fork一个新进程， 用这个进程向它提供服务， 老的进程仍然守候在那里。如果再来了， 再返回， 然后呢再fork一个新进程，再生成一个新的进程，然后用这个进程呢向 新的用户提供 这个大小写转换的这种服务，老的进程仍然守候在吧吧吧吧。 所以说呢在这个表当中呢， 可能有很多的connection socket，可能还有一个 Webcam socket 好吧 这就是这样的情况
