大家看幻灯片，今天咱们聊聊神经网络和反向传播。反向传播其实是个过程，我早年专门研究过这个，我常把它叫做神奇的过程，让神经网络从错误中学习，很像人类，但更有序，而且还要用到更多数学。咱们这就深入聊聊这个主题，我确信这会很有趣。这也是为这学期剩下的内容打基础，以后咱们讨论的每个算法，哪怕没提，都在用反向传播。所以理解这一讲和这些主题非常重要。好，咱们按惯例，先回顾一下前面讲过的内容。所以，我想大家应该还记得上次咱们聊了什么。咱们讲了怎么构建目标函数或损失函数，也就是这里说的，还有正则化。但要做到这点，咱们是通过 xy 对把一切都形式化了，定义数据对和评分函数，比如这里咱们用的是线性评分函数，还有最终定义损失函数。

右边这张图展示了咱们画出的整个学习流程。有人提了一些问题，呃就是上次留下的那些问题。谢谢。上次课甚至更早，虽然咱们只用 Softmax 函数，但我想强调，它绝不是我们唯一使用过的损失函数，它是深度学习中应用最广的函数之一，特别是在分类任务里。不过我们还有很多其他选择，用于其他任务、不同任务，甚至分类任务本身。如果你看过官网幻灯片，我在第二讲阅读材料里加了铰链损失（以前叫 SVM 损失）。幻灯片里有铰链损失的例子，它也是一种常用的损失函数，尤其在早期，神经网络的早期。为了让你对它有个大致了解，这是一种损失函数，和 Softmax 不同，它不把分数转成概率，所以转成概率不是唯一办法，对吧？我们可以用其他形式。这个函数鼓励正确项的分数，让我强调下，由 $s_{y_i}$ 定义的正确项分数要高于其他所有项 $s_j$ 的分数。你看，这里这个条件生成的值是 0，条件成立时是这样，否则就反过来。它的作用呢，就像我说的，是鼓励正确项的得分高于其他所有项，至少要高出一个边距，就是你看到的那个 1，这就是它生成的边距。如果条件不满足，损失就会按比例增加，相对于边距，这就是可视化效果。所以生成这个，嗯，这个函数的可视化，通过惩罚无关项得分过高的情况，来提升正确项的得分。大家再回顾一下讲义二的阅读材料，里面有不少例子，能帮你更好地理解这部分内容。接下来咱们聊聊通用优化，也就是怎么为神经网络找到最好的参数 W。在这个过程中，我们提到了损失景观，就像图里画的那样，它像一个大山谷，山谷里的每个点都对应着一组不同的权重参数。

咱们的目标就是找到能让损失景观值最小的那组参数 W。核心在于算出损失函数 L 对 W 的梯度，然后一步步用梯度来做优化，这就是梯度下降算法，对吧？所以权重基本上就被更新了。虽然从这个距离很难看清我指的是什么，不过我猜，呃，大概能猜到。总之，为了沿着损失景观走向最小值，我们需要定义一个步长，通常沿着梯度的负方向迈出步长大小的一步。这就是梯度下降算法。为了优化，我们之前讲过数值梯度和解析梯度这两种方法，各有优缺点。实践中我们更倾向于使用解析梯度。实践中我们推导解析梯度，如果实现或数学推导有难度，就用数值梯度来校验代码。之前提到的另一个难点就是要要在整个数据集上算损失函数和它的梯度。所以要是数据集很大，这开销可就大了，毕竟要在整个数据集上跑损失函数和它的导数嘛。这就是咱们之前聊到小批量处理的原因。从数据里抽取一部分样本，一般选 32 个不错，常用的可能是 32 或者 64，128 或 256 也行。用这些抽样数据算梯度，然后往最小值方向走一步。除了随机梯度下降，我们还聊了带动量的 SGD 优化法。关于 RMSProp 和 Adam 优化器，细节很多，具体可以参看第三讲。我们还讨论了学习率以及学习率调度的重要性。在一些优化器里，我们常先用较大的学习率，再换用不同的方法衰减学习率，或者把它的值按一定倍数降低。很多优化器都需要这样做，但像 Adam 及其变体这些较新的优化器，通常不用手动显式地去降，因为这一点已经内置在优化器里了。说到这儿，下面我想进入神经网络这个话题，看看我们怎么真正搭建神经网络并解决问题。接下来是更有趣，也更具挑战性的难题。咱们，之前我们聊过这个函数，也就是 W 乘以 x 的线性函数，它是能定义出来的最基础的神经网络，它只有一层。我们之前讲过层，这里请大家留意维度 D 和 C，它们代表输入数据的维度，x 是数据输入或特征数，C 则是类别数，也就是输出节点或神经元的数量，取决于我们需要多少输出。

那么为了构建第二层的神经网络，我们可以定义一组新权重 W2，并把它应用到上一层 W1 乘以 x 的结果上。注意这里的维度，C 是输出数，D 是输入特征数，我们还定义了 H，指的是神经元数量，也就是隐藏层节点或神经元的数量，这是第一点。第二点是这个 max 函数，我们稍后会再讲，我们稍后会解释它的涵义。这里的 max 操作是在 W1 和 W2 的线性变换之间引入非线性。这其实是整个过程中非常非常重要的一部分。我会简单讲讲非线性，不过趁还没忘，先看看最后这部分。实际上我们这里只包含 W 和 x，正如第一二讲所说，我们也加入了偏置项以保持完整，其实也包含偏置项，只是为了简洁，这里没写出来。总之，最大值操作引入了非线性，这点很关键。正如我们之前提到的，线性分类器，前几讲我们提到过，有很多不同的问题是我们没法处理的，仅用一条直线把样本分开，对吧？这就是其中一个例子。为了用神经网络用线性函数解决这个问题，我们需要某种非线性变换，从原始空间到新空间。现在在新空间里，你会看到它们呈现出了新的样子，可以用一条直线分开，对吧？这里，输入和第二个空间之间是一个非线性变换，它把 x 和 y 映射了过去，映射到极坐标 r 和 theta。但再说一次，这只是一个例子，还有很多其他的例子。那么借这个例子，我们回到对两层网络的定义上来。神经网络，你可能在文献里、在课堂之外见过，这类只依赖权重输入和层等的网络其实有很多种。这类只涉及乘法的网络一般叫做全连接网络或者多层感知机（MLP），这是一点。咱们可以堆叠更多层来搭建、构建更好、更大的网络。大家注意维度，特别是中间隐藏层的维度，它们得依次匹配。另一部分，所以咱们回到神经网络功能的可视化图示。在线性表示时，咱们聊过这个，通常网络会通过权重学习出某种模板。大家还记得上周吗？咱们聊过那些正在学习的模板。所以我再说一次，模板，其实它们不是模板，不是说它们是数据里图像的某种代表，这取决于训练数据。所以上周讨论的这些模板是把权重作用上去，由这 10 个输出生成的，作用在输入数据之上，输入神经元上，对吧？既然现在有了多层，我们就能创建更多模板了。现在中间层能生成 100 个模板，而线性分类器只有 10 个，当然那 10 个也还在。咱们再从宏观层面看，我来给你讲讲这意味这什么。中间这 100 个神经元让网络能为局部，而不只是整个对象创建模板。比如你在这里看到的类别，鸟、猫、鹿、狗、青蛙、马，它们都有眼睛，对吧？所以这 10 个或 100 个模板里可能有一个是各类别间共享的对象部件。所以从宏观层面看，这些能形成模板。等我们回到可视化以及神经网络学到的内容时，这个话题会揭示更多细节，刚才说的内容会有更多细节。谢谢。回到 max 函数，我们讲了 max 函数，也就是这里产生的非线性，在神经网络里这叫激活函数，对吧？它在构建模型中起着非常关键的作用。构建神经网络，我们来回答幻灯片上的问题，如果不用这些激活函数来构建神经网络会发生什么？以最大值函数为例，如果去掉 max，这就是我们的函数，对吧？所以结果会是 W2 乘以 W1 再乘以 x。这里会发生什么？

对，没错。正如你提到的，W2 乘 W1 其实可以用另一个矩阵 W3 轻松替代，而且这样函数就变成线性的，所有东西都能合并。因此我们需要某种非线性机制才能赋予我们解决非线性问题的能力。问题？我们刚才讨论的函数是 ReLU，它是整流线性单元，是神经网络中非常常用的激活函数。虽然还有许多其他变体，已在许多其他架构，甚至更现代的架构中接受过测试。ReLU 有个毛病，输入非正时，输出直接归零，容易制造出死神经元。为避开死神经元，你可以试试 Leaky ReLU，或者指数线性单元 ELU。ELU 稍好点，因为它的函数特性更以零为中心。还有更新的变体，像 GELU，还有线性单元。具体我也不太确定，我听过 GeLU 和 GELU 两种说法。它们能用，而且在 Transformer 架构里更常用。还有 SiLU，也叫 Swish，也就是 Sigmoid 线性单元。那个，在一些现代 CNN 架构里也用，Google 以前就用过，比如 EfficientNet 这类模型变体，效率确实很高。除了这些，还有 Sigmoid 和 Tanh 这类函数，它们也常用作激活函数，不过确实有一些问题，因为它们会把值压缩到很窄的范围里，有时会导致梯度消失。所以我们通常不在神经网络的中间层用 Sigmoid 或 Tanh。在神经网络里，它们常用于后面的层。比如需要把输出二值化的时候，诸如此类。所以像我说的，ReLU 通常是个不错的默认选择，它在很多架构里用得很广，而且我们聊过，同一个函数有很多变体。对，我来总结一下刚才聊的内容，接着答疑。咱们聊过加层之类的，但我想强调，激活函数往往是很关键的，是在层里运行的函数。另外还有定义权重的 W，也就是前一层和后一层之间的映射。再说一遍，这些是全连接神经网络，结构非常简单。

实现上，我们只需要能定义一个激活函数。看这个例子，激活函数定义为 Sigmoid 函数。用它很容易就能算出第一层和第二层的隐藏值，也就是隐藏神经元是通过 W1 乘以 x 再加上偏置，然后套用激活函数算出来的。然后，h2 也一样，输出就是 W3 和上一层输出之间的点积。隐藏值生成输出层。我先暂停一下，回答几个问题，然后继续。好问题，问题是面对新任务，我们怎么选用哪个激活函数？简短的回答是对多数情况下靠经验。不过我们一般先用 ReLU，或者用该架构常用的标准激活函数。就像我说的，CNN、Transformer 这些不同架构都有常用的激活函数。所以我们通常直接用那些验证过的。对，主要靠经验，给新问题设计新网络时，这就是你要做的选择之一，跟其他超参数很像。咱们来看看这些激活函数到底有什么共同点，它们究竟在发挥什么作用？我举几个例子，详细讲讲这些激活函数具体在做什么。简单来说，它们最核心的共同点就是制造非线性。咱们没用线性函数做激活，对吧？所以引入非线性非常关键。为什么变体这么多？我之前提过梯度消失的问题，我还简单提过函数的可微性，它们必须可微，毕竟咱们是在神经网络里用它们。另外，函数要是零中心且光滑，网络收敛会快得多。影响因素很多，我重点讲了这些，它们在定义或设计时起关键作用。我会在细讲激活函数时再展开说说。各层通常用同样的激活函数，但就像我说的，后层或输出层有时会用 Sigmoid 激活函数，或者正切函数。不过通常确实如此。那个问题是，我们是不是整个网络都用同一个？整个网络所有神经元用同一函数？好，咱们接着说。回到我们聊的话题，也就是这些怎么实现模型、这些，还有神经网络。所以方法很简单，用 Python 写两层神经网络代码不到 20 行，很简单。定义网络，N 是样本数，D 是输入维度，D_out 是输出维度，H 是隐藏层的维度，隐藏层的神经元数量。我们之前说过，这只是创建 x、y 并随机初始化权重 W。接着是前向传播，即把 W 作用到输入上，输入数据逐层处理，最终得到输出预测值，然后计算损失，损失函数，并输出该损失值。前向传播之后，我们需要一个优化过程，计算解析梯度，并利用这些梯度通过梯度下降优化 W1 和 W2，本质上就是朝网络的最优值迈出一步。但是计算解析梯度这部分是我们还没怎么深入讲的最关键内容。所以这次讲座剩下的内容主要就讲这个，也就是让它在不同场景下都能工作，都能扩展。那么，训练并构建好这样一个神经网络之后，取决于我们用了多少层、隐藏层里的节点，你会发现我们能识别出两类之间不同的分离模式。而神经元越多，通常意味着学习更复杂函数的能力越强，以及更好地分离这些节点。看看这个，它和我这里展示的图案很像，也和我第二讲里展示的那个相似，也就是 k 近邻。接着我们讲到，即 k 等于 1 的最近邻框架。这就相当于使用更多神经元。如果给网络太多容量，就会出现过拟合，无法一般，泛化到未见过的数据。但对此也有多种不同的解决方案。我想强调的一条经验法则是，不要用网络规模来做正则化。我们通常不会把它当作超参数来微调网络规模，尽管我们会尝试不同的规模及相关超参数。我们常选个比需求稍大的神经网络，再用正则化，特别是这个正则化项以及超参数，来测试不同的配置。所以，我们通常调的是正则化及其超参数，而非网络大小本身。本身。

好。嗯，这就是神经网络的核心概念。不过，我听说过神经网络以及它们如何与生物学相关联。确实有生物学灵感，我稍后讲。但有个问题。你的问题是，为什么增加 lambda 值会导致模型更欠拟合？没错，简单说，lambda 决定了正则化项在总损失里占多大比重，对吧？而你给正则化的贡献越大，记住，正则化是定义在 W 上的，所以它约束了 W，降低了自由度，限制了 W 的取值，对吧？所以自由度越少就等于决策边界会更通用，而不是给你那些复杂的细节部分，对吧？所以如果你过度约束模型，即使用了正则化，也会得到那样的值，那样的决策边界，对。正则化的作用就是防止过拟合。再说一次，你是在做权衡，在损失和正则化之间找平衡，预测正确的实际输出。所以损失函数的第一部分就是预测正确的输出，第二部分只调整权重值，不再关心输出结果。如果正则化权重过大，你就得不到好的分类器，对吧？所以保持平衡很重要。正则化是好事，但用过头就没用了，对吧？

我们再稍等一会儿。

为什么我们要采用这种正则化，而不是直接改网络的规模？

原因有很多，其中一个就是网络的规模。你搭建网络，有时得让它们跑上好几天才能得出一些结果，对吧？所以，咱们常这么做：先慢慢增加网络里的参数，直到出现明显过拟合，这说明网络懂了数据规律，开始死记硬背了。这时候咱们就得用正则化来遏制过拟合了。所以正则化很关键，要是参数太多，网络太复杂，就会出问题。咱们一般不那样干。遇到新问题，通常先从小网络做起，先建好网络，之后再用正则化手段来调整和优化。针对具体问题，咱们怎么知道该用多少个神经元来解决问题呢？这得靠实证研究。再看看类似案例，没有万能公式，你得多看看其他同类模型，还有在类似数据上训练的其他类型网络，从这个范围起步，然后你自己多做实验来平衡和调整网络的复杂度。所以这基本上总是取决于不断地尝试探索。所以你的问题是，有没有理论和基础研究来指导该用哪些激活函数、用多少层？有很多研究论文都在分析这些，也有一些优化它们的方法，也就是网络的原参数或者说超参数。我们不会细讲这些，因为这很大程度上取决于数据集和你要解决的具体问题。所以答案是肯定的，确实有相关研究，但要注意，这些研究都基于一些假设，未必适用于你的具体场景，或者说你的具体问题。实际上，我们借鉴了一些生物学灵感，这些灵感其实很松散，如果有神经科学家在场或在线观看，请别太当真。我举的所有例子都不是绝对的真理。但神经元的一般工作原理是如图所示，它有一个细胞体，通常会把树突传来的脉冲汇聚到细胞体中，然后通过轴突，这些脉冲被传向其他神经元，神经元。这与我们在神经网络中的做法非常相似，我们通常会用一个函数把所有信号汇总起来，这些信号是来自前一层的脉冲和激活值。在细胞体中有这个函数处理，它对输入进行处理，输出激活值，再传给下一层、下一个神经元。这就是为什么我们需要某种激活函数来产生脉冲，从而增大或减小这些数值。因此，生物神经元和我们构建的神经网络之间有很多差异，它实际上可能复杂得多，比起我们构建的神经网络的样子。但通常它们有一些共同的概念，我们构建的神经网络通常会按规则的模式来组织，之所以用这些模式，是为了在实现时获得更好的计算效率。虽然有研究尝试构建并优化这些复杂的神经网络，但从结果上看，它们和规则结构的网络几乎不相上下，我们常构建也会讲到的那些常规神经网络。在这门课里，我得反复提醒各位，用大脑做类比时一定要小心，以及这可能会被如何解读。所以差别太多了，我就先讲到这儿。要是有人对神经科学那块感兴趣，咱们可以接着聊。把前面那些都串起来看，咱们确实有个评分函数。这个评分函数通过一些权重向量或矩阵把输入变成分数。咱们常用做网络损失函数的是铰链损失、Softmax 或其他变体。另外，我们还定义了正则化项，这样我们就得到了总损失，也就是数据损失加上正则化项。还有，为了优化 W1 和 W2，我们需要能对 L 求偏导，也就是对 W1 和 W2 求偏导。细节真不少，首先得构建这些函数，再求导并写下来。这通常很繁琐，涉及大量矩阵计算，实现神经网络前得在纸上做很多工作。另一个问题是，如果你想调整损失，哪怕只和纸上做的略有不同，之前算的就全白费了。所以这时我们得全部重做，这就变得很棘手，有时甚至没法实现。损失函数很复杂，处理复杂函数就更难了。其实有个更好的思路，我们实现时常用，今天举几个例子，确保大家都能跟上，理解这些内容。也就是计算图和反向传播的概念。计算图就是把神经网络里的所有操作组合起来，一步步搭建出整个过程，从输入和所需的全部参数开始，最终得到损失作为输出。比如损失函数，无论是 Softmax 还是铰链损失，都要加上正则化项，也就是正则化项 R，它以 W 为输入，把这两项加起来，加总后算出损失。但在算损失前，我们通常还要先聚合 x 和 W 生成评分，这是乘法函数。这很实用，因为咱们搭建的多数神经网络都有对应的计算图。这些复杂函数都能用同一套框架来表示。接着咱们就能用它来搭建计算流程，计算图从输入图像或数据开始，网络中遍布权重，最后是损失函数。这之所以有用，是因为确实存在这类复杂的神经网络，比如神经图灵机，它专门处理时序和序列数据，所以得把这台机器展开很多层。要是全靠手工去做，那就会非常繁琐，难以处理，也不现实。所以，这就是构建计算图时我们要用反向传播的原因。反向传播，咱们先从一个简单的例子说起。咱们先定义一个函数 f，它是 x、y 和 z 的函数，也就是 (x + y) * z。画出它的计算图，你会看到有一个运算步骤，先是 x 和 y 相加，然后把它们的和与 z 相乘，z。好，假设输入 x = -2，y = 5，z = -4，咱们现在就能算出结果，也就是前向传播。在神经网络里，一步步往前算。第一步，把 x 和 y 加起来，结果是 3。为了逐步理解这些步骤，我给它起了个名字，所以 q = x + y。如果我想计算 q 对 x 和 y 的偏导数，这很简单，因为 x 与 q、x 与 y 之间都有公式，公式就在这儿。q 对 x 的偏导是 1，对 y 的偏导也是 1。同理，这是个简单的设定，既然已知存在，咱们心里有数就行。接着第二步是 f = q * z。同理，有了这个函数，写出偏导数很容易，对吧？df/dq = z，df/dz = q，所以 z 和 q 的角色互换了。对，希望大家线性代数里这些都学过。如果不熟，一定要复习一下，这些真的非常重要。嗯，至于剩下的，这学期我们在设定里想要也需要的，要完成这个反向传播的例子，我们需要 f 对 x 的偏导，为什么？看，反向传播是这样开始实现的：从网络的末端开始，我们从那里往回推，接着回溯反向传播所有梯度。这本质上是一个正在运行的递归过程。好，谢谢。f 对 f 的导数，呃，就是它对自身求导，对吧？所以它总是 1。损失函数对自身的导数总是 1，1。如果我们要回溯，首先看最直接的 z。这里有个 z，计算 f 对 z 的导数，我们已经有了结果，对吧？f 对 z 的导数是 q，所以 q 的值会传给这一项，嗯，这个值也作为梯度传过去。接下来看 q，q 是直接与 f 相连的下一个节点，所以这也很好算，因为我们已经有了 f 对 q 的导数，我们之前也算过，它等于 z。不管 z 是多少，这里的导数值就是它，-4。接下来是 y，它就在 q 的前面。我们知道，虽然要求 f 对 y 的导数，但 y 和 f 并不直接相连。这时候就要用链式法则了，它把对中间变量的导数计算拆分开了。所以 f 对 y 的偏导等于 f 对 q 的偏导乘以 q 对 y 的偏导。在这个例子中，链式法则可以写成这样。现在我要介绍两个重要的新术语：局部梯度和上游梯度。上游梯度通常指从网络末端反向传回当前节点的那个梯度，也就是咱们当前所在的这个节点。局部梯度指的是节点输出对其输入的梯度，也就是它的输出对其输入的梯度。这就是局部梯度。定义它们其实不难，因为咱们已经有了 f 对 q 的导数以及 q 对 y 的值，咱们也有这个值，所以是 1 乘以 z，结果就是 -4。同理，对于另一个变量 x 也是一样的，上游的局部梯度可用链式法则推导，结果同样为负，结果是 4。因为在这两种情况下，关于 x 或 y 的梯度都已经是已看倒。因此它们的值相同。借助这套计算设置和计算图，模块化操作变得非常简单。针对神经网络中的每个节点，以 x 和 y 或其他变量作为输入，以及 z 作为输出，我们需要先求局部梯度。我们总有函数 f，它是 x 和 y 的函数。输出对每个输入的梯度，每个节点都很容易算出来。我们需要能反向传播的是上游梯度，对吧？反向传播让我们能一步步拿到这个上游梯度。所以到了这个节点，后续节点算好的上游梯度我们已经有了。后面的节点，还有，还有，这就是我们要的。接下来把上游梯度和局部梯度乘起来就行，从而生成现在叫下游梯度的东西。所以下游梯度就是上一层的上游梯度，对吧？x 是这样计算的，y 也是一样的道理。这样，我们就能完全在本地一步步计算，再把结果反向传给前一层，让他们接着处理。所以这确实是最基础的操作之一，无论是神经网络，还是设计多层信息的优化过程。

如果我没理解错，你是想直观地了解梯度在做什么，对吧？咱们先退一步，看看当初为什么会走到这一步。我们需要计算损失函数对 W1、W2 以及所有 W 的梯度，以便能够采取行动，沿着梯度的反方向迈出这一步，从而找到最优值，对吧？最优损失。为此，我们需要计算损失 L 对所有变量的梯度。我们做的是将损失 L 对所有变量的梯度反向传播回网络中的每一个节点，无需坐下来为整个网络写出完整的函数表达式。如果网络有 100 层，我们不会单独为这 100 层逐一写出函数。这就是逐步反向传播，为优化过程算出每个权重需要的值。举个例子。再看个例子，这个函数更复杂，包含权重和 x，是 1 除以 1 加 e 的 x 线性组合次方。里面有一堆乘法、加法、取反和指数运算。最后，基于刚才算出的函数值，现在咱们结合这些来看这个例子。w0、x0、w1、x1 和 w2 的具体数值，有了这些数值，咱们就能做前向传播，算出流程里每个环节的值。提醒一下，咱们有些基础是已知的，比如指数函数 e 的 x 次方，我们知道它关于 x 的导数是多少，常数乘法求导，导数就是常数本身，1/x 的导数是 -1/x^2，这些也是代数里的常识。如果是常数加法，导数恒等于 1。所以正如我一开始说的，在网络末端，L 对 L 的导数总是一，起点就在这里。咱们开始用这个规则，就是 1/x 这个函数的导数。现在咱们往回算，刚才说了，末端梯度恒为 1，局部梯度等于 1 减去 1/x，不管输入 x 是多少，算出来都是负数，呃是 -0.53，这就是下游梯度，用来推导下一步的 upstream gradient。下一步是常数加法，局部梯度为 1，所以直接乘以 upstream gradient，值原样传回。下一步是 exp 函数，针对这一步 upstream 已知，局部梯度是 e 的 x 次方，x 是多少？就是这一步的输入减 1，算出来就是 -0.2。接下来看这一步，这里又乘以一个常数，局部梯度就等于这个常数，常数项定义出新的下游梯度。回到这里，这是一个加法函数，我们得到一些数据，或者说两个输入，这里有不同的值。同样，想算上游梯度的话，它是 0.2。已经有了下游的局部梯度等于，是 1，因为是加法，x + y 对 x 和 y 求导都是 1，所以两个输入一样。接下来是乘法运算，上游梯度还是那样，我们有数值，还有针对乘法运算的局部梯度。假设我们总是有类似 a 乘以 x 的情况，对 x 求导，结果总是 a，也就是另一个变量，对吧？所以第一个是 -1，也就是 x 的值；第二个是 2，也就是 w 的值，也就是另一个变量，不管它具体是多少。这样我们就能算出所有东西，接着再算出关于 w1 和 x1 的梯度。我们做这些计算就是为了搞清楚 w 该调整多少，从而朝着神经网络里的最优电迈进一步。再举个例子。画计算图的方法其实很多，我讲的不止这一种，可以把这些函数合并，定义成一个 Sigmoid，叫 Sigmoid，因为这本质上就是一个线性函数的 Sigmoid，对吧？线性函数可以设在这里，然后所有这些操作都定义为 Sigmoid。Sigmoid 其实很有趣，也很有用，因为它的局部梯度取决于它自身，所以 Sigmoid 对 x 的局部梯度化简后就是 (1 - Sigmoid) 再乘以该 x 对应的 Sigmoid。所以这其实是个非常实用的框架。总之，这函数好用又简单。要算下游梯度，记得之前说过上游梯度的值是一撇放倒，我来算一下局部梯度，把这个函数里的 x 替换成输入值 1 即可。这就是具体的计算过程，乘以 1 以后得 0.2，跟咱们之前算出的值一模一样。咱们单独说一下，我想总结一下数据里往往只有少数几种模式，非常感谢。针对这些节点，咱们其实可以把它记下来。这里有个专门针对加法门的加法门，正如我之前解释的，由于加法的特性，它始终是梯度分配器，梯度会保持不变，它的输出和输入是一样的。对于乘法门，它起到交换因子的作用，再次强调，xy 对 x 的偏导是 y，对 y 的偏导是 x，也就是互换一下。然后，呃，复制门里的操作其实就是把输入值加起来，数据传到网络节点或门，最后到最大值门，这跟次函数很像。这个门取输入的最大值，所以它的梯度就由此而来，所以哪个值最大，梯度就传给哪边。所以现在实现起来很简单，神经网络的前向传播快计算所有步骤，然后在反向传播中我们开始逐步计算梯度。我解释过，损失函数对自身的梯度总是 1。然后我们从网络末端开始向上回溯，你可以看到我们在向上走，这是 Sigmoid 函数，算梯度再往上走，刚才那是加法门，这还有个加法门，接下来得做乘法。这些门的概念其实很简单，帮我们实现了计算。用这种框架，我们就能把每个部分都模块化实现。我们在神经网络里定义函数，并为每个需要的函数写好前向和反向接口。比如这个乘法门，做乘法时我们需要用到那些输入值，反向传播要用到它们，我们一般会把这些值存下来，记住它们，然后在需要时再取用。先算前向传播的值，再做反向传播算出梯度。也就是说，我们可以把前向和反向传播都写进同一个函数，全都在一起。这就是 PyTorch 算子现在的样子。比如看 Sigmoid 层，它只有前向传播，虽然这个函数里没真正实现，它其实是在 PyTorch 的 C 代码里实现的，就在某个地方。Sigmoid 的反向传播也在计算我们刚才讨论的那个函数。所以到目前为止，咱们聊的这些，还有我本来想讲的大部分例子其实都已讲到了，都是用标量值来举例的。之前的例子用的都是标量，但我们知道情况并不止于此，其实这些操作都能写成向量或矩阵的形式，只要把刚才那点内容扩展一下就行。咱们刚才聊的是标量对标量的情况，也就是目前为止讨论的内容。当输入 x 和 y 都是标量时，导数也是标量。也就是说 x 稍微变一点，y 的值会改变多少，就会变。那如果是向量化的呢？比如 x 是个 n 维向量，y 是个标量，这就是向量到标量的情形。标量导数。这种情况下，导数也是一个向量。向量里的每个元素表示如果我们让 x 发生微小的变化，也就是 y 会变化多少，因为 y 是单一值，所以指的是 y 的总变化量。此外还有向量到向量的情况，这是 x 和 y 都是任意大小 n 和 m 的向量。这种情况下，导数会构成一个矩阵，也就是我们所说的雅可比矩阵。具体来说，对于 x 中的每个元素，如果它发生微小变化，这个导数就告诉我们 y 的每个元素会改变多少。注意这里的下标它们并不相同，雅可比矩阵中每个元素的下标可能各不相同。意思很明确，我们来看看怎么操作、观察并可视化它。如果你想用向量做反向传播，比如 x、y、z 分别是 Dx、Dy、Dz 维的向量。注意，损失 L 本身是个标量，因为毕竟我们只最小化一个值，但算上游梯度时结果也是个向量 Dz，大小同 z，变量 z 就是这样。下游梯度的情况也一样，讲下游梯度前我先说说局部梯度，在那里我们有 z 对 x 和 y 的梯度，这时就涉及我说的雅可比矩阵，因为梯度变成了矩阵，所以这里有两个雅可比矩阵，维度由输入乘输出决定。结果下游梯度就是上游梯度与局部梯度的乘积，结果的大小就和输入 x 一样，因为输入是向量，所以这里得到的还是向量，维度不变。再说梯度，我刚才说过，梯度是相对于损失函数的变量，它们的维度始终与原始变量相同，幻灯片上也展示了这一点。所以，你刚才用向量做的反向传播，这里只是个例子。假设有个函数取 0 和 x 的最大值，这就是 ReLU 函数，这是逐元素运算。输入后取 0 和 f 的最大值，如果非负就通过，否则替换为 0。假设你已经拿到了一些上游梯度，现在我们需要在这里构建一个雅可比矩阵。因为这是逐元素运算，所以这个雅可比矩阵不依赖于任何其他输入，它只跟值本身有关，这是个非常稀疏的矩阵，只有主对角线上有值。这些值要么是 0，要么是 1，取决于最大值有没有被选中。如果值通过了就保留，否则就用 0 替换它，拿它乘以所有梯度就得到了下游梯度，计算就是这么做的。如前所述，这里的雅可比矩阵是稀疏的，因为这是逐元素运算。在反向传播时，我们不去计算那个巨大的稀疏雅可比矩阵，而是直接用基于规则的方式来算梯度，就是最大值函数。所以我们并不真的存储那个矩阵，也不去计算它，因为我们清楚函数是怎么运作的。如果输入不是向量，这也能扩展到矩阵甚至张量，它们是更高维的数据，也就是高维数据。所以在那些情况下，同样是关于变量的梯度，它的大小会和那个具体变量一样。计算上游和下游的计算矩阵以及导数，会按我们之前讨论和展示过的方式来做的。之前是针对向量的，而说到局部梯度，它会是一个巨大的矩阵，一个巨大的雅可比矩阵，因为我们的输出是矩阵，输入也是矩阵。于是局部梯度将会是，它的大小就是输入尺寸乘以输出尺寸，所以它本身就是个巨大的矩阵。我来举个例子。假设输入 X 和 W 是矩阵乘法节点的输入，结果是，然后算出 Y 作为输出，L 对 Y 求导，就得到这些雅可比矩阵。假设批量大小是 64，矩阵维度是 4096，也就是说光一次矩阵乘法，雅可比矩阵就超过 256 GB。为了简化，我们通常会看这些值如何相互影响。比如，如果 X 的某个元素发生变化，会影响到哪部分结果？所以你看，X_n,d 的这个特定元素往往只影响输出矩阵的一行。这基本能帮我们理清每个节点的计算逻辑。咱们不用搞那么大的雅可比矩阵，可以直接为矩阵乘法写反向传播函数，这样更高效。我这就讲完了。所以你要回答 X 和 d 对 Y 和 m 影响多大，也就是 Y 和 m 受 X 和 d 影响的具体程度，影响有多大。这意味着我该把关于什么的梯度填进去？谢谢。具体的数值 X，X 和 d。提醒一下，这是乘法运算。谢谢。乘法门里应该是交换。总之，这个问题的答案是 W 里的某个值，对吧？记得我们提过这个乘法门，它是交换乘法器，所以这里发生了交换，因此你的那个值就换了过来。好的，X 受影响进而影响 Y 中的某个元素将取决于，即由 X 矩阵的第 d 行、Y 矩阵的第 m 列确定的 W。这就是交换数值，本质一样，但这里我们要看那些巨大的矩阵，找出具体该换哪个元素。然后基于这个结果，我们就能直接替换掉整个矩阵。利用矩阵乘法和矩阵运算，L 对 X 的梯度定义为这个简单的矩阵运算，而 L 对 W 的梯度则定义为这个简单的乘法。同样，这里要交换，求 X 的梯度时要用到完整的 W，求 W 的梯度时要用到完整的 X 来做乘法。有了这些公式，实现更大、更复杂的运算就简单多了，也能在反向传播中实现它们。好，今天就到这里，简单总结一下。咱们今天聊了全连接神经网络，咱们把反向传播的每一步都过了一遍，也就是前向和后向传播。下节课，咱们来聊聊卷积神经网络。谢谢大家。
