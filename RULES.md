# 本仓库的发布规则与文档写法

> 本文件记录**发布范围、同步来源与冲突处理**，以及仓库首页与课程页 **README 的写法**。
> 规则来自 2026-09 这一轮整理：重新生成不合格的课程产物，并把仓库收敛为「只发布整编产物」的形态。

## 1. 发布范围：每门课只放这四样

```text
<一级分类>/<二级分类>/.../<课程>/
├── README.md        # 课程页（必需）
├── textbooks/       # 模块合辑全书：模块XX_<主题>_精读全书.md
├── notes/           # 复习笔记：笔记XX_<主题>_笔记.md
└── subtitles/       # 逐字稿：PXX_<分集名>_clean.txt（可选 PXX_*_supplement.txt）
                     #          或块级稿 BLKxx_<span>_逐字稿.md（两种形态都算）
```

- **不再发布 `articles/`（单集精读长文）**：它是整编模块全书所用的中间产物。课程页里不得再出现指向 `articles/` 的条目，也不得出现「精读长文」「模块长文目录」这类字样。
- 课程目录下**不放任何其它内容**：`audio/`、`manifest.json`、`parts.json`、`topic_plan*.json`、`note_plan*.json`、`*_TASK.md`、`.dispatch_log.jsonl`、`.backup*/`、临时脚本（如 `_gen_*.ps1`）、以及 `PXX_<分集名>.txt` 这种无后缀的重复件——这些是生成侧的工作区状态，留在 `output/<课程工作区>/` 里即可。
- 没有逐字稿的课程就不建 `subtitles/`（例：`pink老师-AI前端入门`）。

## 2. 唯一数据源与同步方向

- **生成侧工作区**：`D:\project\项目\笔记sikll\成品\output\<课程工作区>\`（该目录不入库）。
- **发布侧**：本仓库目录。
- 同步方向固定为 **output → 本仓库**，**任何冲突以 output 为准**，含两种情况：
  1. 同名文件内容不同 → 用 output 的版本覆盖；
  2. 仓库里有、output 里已经没有 → 视为已废弃，从仓库删除（例如历史轮次的旧模块划分）。
- 上述优先级**没有例外**：文件内容、文件名、新增、删除、课程数量与目录结构，全部由 output 决定。
  同步时先遍历全部 output 工作区，再回写本仓库；不得为了让旧目录继续成立而选择性保留产物。
- 同步时**只取产物文件**：`*_精读全书.md`、`*_笔记.md`、`*_clean.txt`、`*_supplement.txt`、`*_逐字稿.md`。目录里的其它文件一律不拷（见 §1 排除清单），嵌套的 `.backup*/` 也不要递归拷。
- 同一集的转录若同时存在 `PXX_….txt` 与 `PXX_…_clean.txt`，只取 `_clean.txt`。
- 逐字稿两种形态都算产物：分集稿 `PXX_*_clean.txt` / `PXX_*_supplement.txt`，与块级稿 `BLKxx_<span>_逐字稿.md`。发布口径为**「只补不换、不删」**：仓库里该课已有逐字稿时保持原格式不动（不混入另一形态、不覆盖）；仓库一份都没有时，才把工作区现有的稿（不论哪种形态）补进去。因此只有**确实没有逐字稿**的课程才在首页写 `—`。

## 3. 课程分类元数据

- 分类唯一来源：`D:\project\项目\笔记sikll\成品\output\course_catalog.json`；发布侧同步副本为仓库根目录的 `course_catalog.json`。
- `course_catalog.json` 是根目录元数据，不进入任何课程目录；课程目录仍只允许 `README.md`、`textbooks/`、`notes/` 和可选 `subtitles/`。
- `schema_version` 当前为 `2`；`categories` 用稳定的 ASCII 点号 ID 表示逻辑多级分类，`courses` 的键必须精确等于稳定的课程 ID。
- 每门课程必须有唯一的 `publish_path`，表示仓库内物理分类目录；路径必须相对仓库根、不得包含绝对路径或 `..`，且末级目录必须等于课程 ID。
- `category_paths` 的第一条是主分类，其余是交叉分类；物理目录只按主分类归档，分类区块和 catalog 允许课程在多个逻辑分类下重复出现。
- **数学**是独立一级大类；只有课程简介或方向明确写出“考研数学”“考研完整体系”等表述，才加入 `postgraduate.math` 交叉分类。
- **心理与哲学**是合并的一级大类，下分 `心理学` 与 `哲学`；不要把“数学”默认归入考研。
- `sync_course.py` 负责校验并同步 catalog；catalog 缺失时保持旧流程兼容，正式发布可用 `--require-catalog` 强制要求存在。
- 分类 ID、课程键、`publish_path`、父分类存在性、循环路径和课程映射一致性必须先通过校验，再允许写入 catalog 或首页分类区块。

## 4. 课程目录与当前规模

| 课程目录 | 来源工作区（output/ 下的前缀） | 分集 | 模块全书 | 复习笔记 | 逐字稿 |
| :--- | :--- | ---: | ---: | ---: | ---: |
| `pink老师-AI前端入门` | `2025版pink老师…` | 200 | 12 | 6 | — |
| `黑马程序员-Python-AI` | `黑马程序员Python+AI…` | 185 | 25 | 17 | 124 |
| `MySQL数据库入门到大牛` | `MySQL数据库入门到大牛…` | 199 | 22 | 13 | 199 |
| `浙江大学-软件工程-陈越` | `【公开课】浙江大学：软件工程 陈越…` | 34 | 3 | 6 | 34 |
| `西电微机原理113讲` | `微机原理 （西安电子科技大学）…` | 113 | 8 | 8 | 115 |
| `黑马程序员-AI大模型NLP` | `黑马程序员AI大模型NLP…` | 189 | 16 | 11 | 189 |
| `零基础学习汇编` | `【汇编语言】零基础学习汇编 (从入门到入坟)…` | 21 | 4 | 4 | 21 |
| `3小时前端入门` | `3小时前端入门教程…` | 23 | 2 | 2 | 4 |
| `王爽汇编语言` | `通俗易懂的汇编语言（王爽老师的书）…` | 72 | 6 | 3 | 64 |
| `Stanford-CS336-从头构建大语言模型` | `【极致中配】2026年最新版 Stanford CS336…` | 35 | 9 | 12 | 35 |
| `Stanford-CS231N-深度学习计算机视觉` | `【极致中配】Stanford CS231N…` | 18 | 5 | 7 | 18 |
| `2026版LangChain教程` | `2026版LangChain教程…` | 120 | 5 | 5 | 27 |
| `南京大学-操作系统原理` | `[完结] 2026 南京大学 “操作系统原理”…` | 30 | 13 | 8 | 60 |
| `南京大学-生成式软件工程` | `[连载] 2026南京大学《生成式软件工程》…` | 6 | 4 | 5 | 12 |
| `南京大学-科研筑基` | `南京大学结丹期教授助你科研筑基…` | 12 | 3 | 3 | 5 |
| `北京大学-变态心理学` | `【北京大学】《变态心理学》…` | 22 | 6 | 8 | 26 |
| `英语语法精讲合集` | `英语语法精讲合集 …` | 29 | 5 | 7 | 14 |
| `宋浩高等数学2.0版` | `《高等数学》全程教学视频 2.0版…` | 149 | 17 | 14 | 150 |
| `宋浩高等数学同济版2024` | `《高等数学》同济版 2024年更新…` | 195 | 23 | 24 | 47 |
| `公考逻辑判断推理` | `【公考逻辑】2027判断推理系统课…` | 63 | 8 | 10 | 41 |
| `中科大计算机网络` | `中科大郑烇、杨坚全套《计算机网络…` | 69 | 12 | 12 | 61 |
| `数据库系统原理` | `数据库系统（DBMS）…` | 28 | 8 | 11 | 31 |
| `SQL两小时半入门` | `【SQL数据库】SQL两小时半快速入门…` | 1 | 1 | 1 | 3 |
| `耶鲁大学-死亡哲学` | `【公开课】耶鲁大学：哲学-死亡` | 26 | 2 | 7 | 26 |
| `耶鲁大学-情绪管理` | `【公开课】耶鲁大学：如何管理情绪（全35讲）` | 35 | 1 | 2 | 3 |
| `哈佛大学-积极心理学` | `【哈佛大学】积极心理学 Talben Shahar（全23讲）` | 23 | 21 | 7 | 46 |
| **合计** | | **1,897** | **241** | **213** | **1,355** |

- 目录名用**简短中文课程名**：不带 BV 号、不带平台前缀、不带原始长标题。
- 南京大学《生成式软件工程》处于连载中（P01、P03 ~ P07，594 分钟），后续将增量对齐更新。
  2026-09-25 以**字幕链路**（B 站中文字幕直出块级逐字稿、零听音）整体重做：块划分改为按 50 分钟装箱得
  12 块，笔记归并为 5 篇（软件仓库管理相关 4 块并成一篇），教材重划为 4 册；合集内 3 分钟的
  「测试切片」（P02）不是课程内容，未纳入。逐字稿因此从 10 份变为 12 份，且不再包含把 P02 并入的旧块稿。
- **`数据库系统概论` 已移除**（旧产物不合格）；2026-09-24 以重做后的 `数据库系统原理` 重新发布（28 讲 / 8 册 / 11 篇笔记），与 `MySQL数据库入门到大牛` 形成「原理 + 实战」互补，首页课程表与数据库方向说明均指向现有数据库课程。
- `宋浩高等数学2.0版`（149 讲）与 `宋浩高等数学同济版2024`（195 讲）是同一讲师的两门独立课程：前者按考研大纲完整覆盖（17 册），后者按同济教材章节逐章精讲（23 册，已扩到多元微积分与级数），按不同学习路线选用。
- **§4 的「来源工作区」前缀必须与 output 下的工作区名对得上**：`sync_course.py` 先做前缀匹配、失败再分词全包含匹配，两者都失配就整门课跳过。2026-09-24 西电就是因为前缀写成课程目录名 `西电微机原理113讲`、而工作区实为 `微机原理 （西安电子科技大学）…` 而被静默跳过；此行已修正为工作区名。改工作区名时**必须同步改这一列**。

## 5. 仓库首页 README 的固定结构

小节顺序不要变：

1. **标题 + 一句话定位**（「N 门真实网课的**模块合辑全书 + 思维导图复习笔记 + 逐字稿**」）+ 持续更新与课程点播/邮箱说明 + 逐字稿用途说明。
2. `## 分类浏览` —— 由 `course_catalog.json` 生成的 HTML 标记区块，按多级分类列出课程链接；所有链接指向 catalog 中的 `publish_path`，课程可在主分类和交叉分类中重复出现。
3. `## 收录课程` —— 表格列固定为：课程（链接到目录）｜方向｜分集｜模块全书｜复习笔记｜逐字稿；最后一行 `**合计**`。没有逐字稿的课程写 `—`，并在表下用一句话说明。
4. `## 三类产物怎么选` —— 表格：产物｜在哪｜粒度｜什么时候用；逐字稿用途统一为“核对讲师原话，以及开发适合自己习惯的课程”；表后写命名规则（`PXX_` 对应原始分集编号，`模块XX_` 与 `笔记XX_` 是整编后的编号，可用集号互相追溯）。
5. `## 阅读与工具` —— 只写本地克隆、Typora 的「内联公式」设置、脑图导入（VS Code Markmap / XMind）；在线阅读统一放在首页顶部入口，不在此重复展开。
6. `## 关于生成方式` + `## 版权与免责声明`（免责声明保持既有措辞，逐字沿用）。

## 6. 课程页 README 的固定结构

```text
# <课程名>（<N> 讲）

<2~4 句课程介绍：讲什么、主线是什么、特点是什么>

- **原视频**：[<视频标题>](<B 站链接>) — Bilibili，<BV 号>
- **全 <N> 集**，已产出 <A> 册模块全书、<B> 篇复习笔记、<C> 份逐字稿

## 怎么学
<4~6 条加粗引导句，结合本课程自身特点给读法；原课程有学法建议的可引用>

## 模块全书目录
<表： # | 模块 | 主要内容 ，每册一行，链接到 textbooks/ 下文件>

## 复习笔记目录
<表： # | 笔记 | 覆盖内容 ，每篇一行，链接到 notes/ 下文件>

笔记开头的知识拓扑树可直接导入 Markmap 或 XMind 生成脑图。

## 其他入口
- [逐字稿（<C> 份）](subtitles/) — 核对讲师原话，以及开发适合自己习惯的课程

---

> 本目录内容由 [Video2Book](https://github.com/LINJIANG12/video2book) 生成，属于对原课程的二次整理，
> 不代表原作者与所属机构的观点。原课程版权归原作者与所属机构所有，仅供个人学习使用。

[← 返回仓库首页](../README.md)  # 由 publish_path 深度生成，物理迁移后不得固定为两级
```

约定：

- **数字必须与磁盘一致**：分集数、册数、篇数、逐字稿份数都要对得上实际文件；产物变了就同步改数字。
- 链接只指向 `textbooks/`、`notes/`、`subtitles/`，**不出现 `articles/`**。
- 表格的「主要内容 / 覆盖内容」写短语，不写成句子；模块与笔记都按编号升序。
- 小节标题固定用 `## 模块全书目录`（不再叫「模块长文目录」）。
- 介绍段与「怎么学」要贴着这门课本身写（例如微机原理强调动手写汇编、NLP 强调跑代码、软件工程强调概念与管理方法），**不要套同一段模板**。

## 7. 格式约定

- Markdown 一律 **UTF-8 无 BOM**；行尾交给 Git 处理（仓库 `core.autocrlf=true`，索引里存 LF，Windows 检出为 CRLF 属正常）。
- **链接目标含空格时，把空格编码成 `%20`**（本仓库既有课程页的统一写法）：`[标题](textbooks/模块06_Web%20工程：…_精读全书.md)`；中文、全角冒号 `：`、顿号 `、` 一律直接写，不做编码。
- 标题只用 `#`~`###`；表格必须带 `| :--- |` 对齐行。
- 文档里**不写生成侧内部状态**（工作区绝对路径、`manifest.json` 字段名、任务书编号等）。

## 8. 每次发布的自检清单

1. 各课程目录下只有 `README.md` + `textbooks/` + `notes/` +（可选）`subtitles/`，没有 §1 排除清单里的任何东西。
2. 三个产物目录的文件数，与首页表格、对应课程页统计行**三者一致**。
3. 根目录 `course_catalog.json` 存在且通过 schema、分类父子关系、课程键、`publish_path` 和 RULES 映射校验；首页分类区块与 catalog 一致。
4. 所有相对链接的目标文件都存在（首页课程表、分类区块、课程页返回首页、课程间链接、`textbooks/`、`notes/` 清单逐条核对）。
5. 过时声明扫描（查**缺陷形态**，不是查关键词本身）：
   - `grep -rn "](articles/" --include="*.md" .` 无命中；
   - `grep -rn "长文目录" --include="*.md" .` 无命中；
   - `grep -rn "篇精读长文" --include="*.md" .` 无命中。
   （`RULES.md` 自身必须点名 `articles/`，不算命中；README 里「说明单集长文不再发布」属正确表述；
   产物正文里的「整理自《…》Pxx 单集精读长文」是**真实来源**，允许保留。）
   教材抬头若出现「单集长文讲义同步保留于 `articles/` 目录」这类**声称仓库内保留长文**的措辞，必须改掉。
6. 首页表格 `合计` 行等于各列之和，并与当前发布基线一致：26 门课、1,897 集、241 册、213 篇、1,355 份逐字稿。

---

> 本文件只约束**本仓库的发布形态与文档写法**；产物怎么生成、门禁怎么跑，见技能仓库 [Video2Book](https://github.com/LINJIANG12/video2book)。
